---
quick: 260713-j7f
type: execute
wave: 1
depends_on: []
files_modified:
  - .mdformat.toml
  - .codex/tasks/lessons.md
  - AGENTS.md
  - justfile
  - .github/workflows/ci.yml
autonomous: true
requirements: []
generated_by: gsd-planner
generated_at: 2026-07-13T18:51:40Z
must_haves:
  truths:
    - mdformat 1.0.0 is the canonical formatter for repository-owned non-GSD Markdown under Python 3.13.
    - Parser-owned GSD artifacts, vendored sources, build outputs, Git internals, and backup directories are excluded from formatting.
    - Contributors and Linux Cargo CI run the same read-only Markdown formatting contract before Rust quality checks.
  artifacts:
    - path: .mdformat.toml
      provides: Canonical mdformat defaults and repository exclusion boundary
    - path: justfile
      provides: Discoverable local Markdown check entrypoint
    - path: AGENTS.md
      provides: Concise repo-local formatter guidance outside managed blocks
    - path: .github/workflows/ci.yml
      provides: Pinned Python and mdformat enforcement in Linux Cargo CI
  key_links:
    - from: .mdformat.toml
      to: justfile
      via: mdformat --check resolves the root repository configuration
    - from: .mdformat.toml
      to: .github/workflows/ci.yml
      via: The isolated CI mdformat 1.0.0 executable checks the same repository root
    - from: AGENTS.md
      to: .codex/tasks/lessons.md
      via: Repo-local guidance establishes mdformat's repeated 1. marker convention for lesson fields
---

# Quick Plan: Adopt mdformat 1.0.0 for Non-GSD Repository Markdown

## Objective

Make mdformat 1.0.0 the reproducible local and CI formatting contract for repository-owned Markdown while preserving the existing untracked lesson file and keeping parser-owned, managed, vendored, generated, and backup content outside the formatter boundary.

<tasks>

<task id="260713-j7f-01" type="documentation">
  <name>Establish the repository-owned mdformat contract</name>
  <files>.mdformat.toml, .codex/tasks/lessons.md, AGENTS.md, justfile</files>
  <read_first>
    - .codex/tasks/lessons.md
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
    - standards/core/verification.md
    - standards/core/local-guidance.md
    - justfile
  </read_first>
  <action>
    Preserve `.codex/tasks/lessons.md` as the current source rather than recreating it. Add root `.mdformat.toml` with mdformat 1.0.0 defaults `wrap = "keep"`, `number = false`, `end_of_line = "lf"`, and `validate = true`. Add exclusion patterns for root `.planning/**`, `third_party/**`, `target/**`, and `.git/**`, plus root and nested `backup`, `backups`, `.backup`, and `.backups` directories; use config exclusions rather than command-line duplication and document that this requires Python 3.13. Add `markdown-check` to `justfile` as a separate recipe that runs exactly `mdformat --check .` without widening `just check`.

    In `AGENTS.md`, add a concise `## Repo-Local Guidance` immediately after `bright-builds-rules-managed:end` and before `GSD:project-start`. State that Markdown work uses Python 3.13 with mdformat 1.0.0, changed non-GSD Markdown is verified through `just markdown-check`, `.planning/**` is parser-owned and must never be formatted, and ordered task/lesson fields use repeated `1.` markers. Do not edit the Bright Builds managed block, any GSD managed block, `AGENTS.bright-builds.md`, managed standards pages, or `standards-overrides.md`.

    Run `mdformat .` once after the configuration and guidance are in place. Capture the Markdown diff before and after that formatter invocation and require the formatter-induced rewrite among previously existing Markdown to be limited to `.codex/tasks/lessons.md`, where each four-field lesson becomes repeated `1.` markers without changing wording, stable IDs, timestamps, or block order. Leave all other non-GSD Markdown byte-stable except the intentional new repo-local guidance.
  </action>
  <verify>
    - mdformat --check .
    - just markdown-check
    - git diff --exit-code -- .planning third_party
  </verify>
  <acceptance_criteria>
    - `.mdformat.toml` declares canonical defaults and excludes every approved path class on Python 3.13.
    - The existing three lesson blocks remain complete and append-only, with only ordered-list marker normalization.
    - `AGENTS.md` guidance is outside all managed blocks and no managed Bright Builds or GSD content changes.
    - The Just recipe is read-only, root-scoped, and separate from Cargo-only `just check`.
  </acceptance_criteria>
</task>

<task id="260713-j7f-02" type="ci">
  <name>Enforce the same Markdown contract in Linux Cargo CI and verify isolation</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .mdformat.toml
    - justfile
    - tools/xtask/tests/docs_contract.rs
  </read_first>
  <action>
    Extend only the `quality` job in Cargo CI. Immediately after checkout, add `actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1` with `python-version: "3.13"`. Before Rust format, lint, build, or test steps, create an isolated virtual environment under `${RUNNER_TEMP}`, install exactly `mdformat==1.0.0` with that environment's Python, and run that environment's `mdformat --check .`. Do not install into the checkout, use an unpinned dependency, alter the default-features/MSRV jobs, initialize the native reference tree, or fold Markdown checking into Cargo commands.

    Run the approved formatter, workflow, documentation-contract, and diff checks. Review the final repository diff separately from the global Codex lessons file and require repository changes to be limited to `.mdformat.toml`, `.codex/tasks/lessons.md`, the repo-local `AGENTS.md` section, `justfile`, and Cargo CI. If this quick-task workflow creates a commit, also run the mandatory Rust pre-commit sequence in exact order after the focused checks: `cargo fmt --all`, Clippy with denied warnings, all-target/all-feature build, and all-feature tests.
  </action>
  <verify>
    - mdformat --check .
    - just markdown-check
    - git diff --exit-code -- .planning third_party
    - actionlint .github/workflows/ci.yml
    - cargo test -p xtask --test docs_contract
    - cargo xtask docs check
    - git diff --check
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>
    - Linux quality CI uses the exact pinned setup-python action, Python 3.13, and mdformat 1.0.0 from an isolated runner-temporary virtual environment.
    - Markdown checking completes before Rust quality work and honors the root configuration exclusions.
    - Parser-owned `.planning/**` and vendored `third_party/**` have no implementation diff.
    - Workflow lint, docs contracts, Markdown checks, diff checks, and the required Rust pre-commit gate pass.
    - No global Codex lesson or guidance file is modified by this repository task.
  </acceptance_criteria>
</task>

</tasks>

## Verification

- `mdformat .`, followed by review proving its rewrite of existing Markdown is limited to `.codex/tasks/lessons.md`.
- `mdformat --check .`
- `just markdown-check`
- `git diff --exit-code -- .planning third_party`
- `actionlint .github/workflows/ci.yml`
- `cargo test -p xtask --test docs_contract`
- `cargo xtask docs check`
- `git diff --check`
- If committing: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, then `cargo test --all-features`.

## Success Criteria

- Local contributors and Linux Cargo CI share one pinned, read-only Markdown formatting contract.
- Only repository-owned non-GSD Markdown is in scope, and the lesson baseline is normalized without semantic changes.
- Managed, parser-owned, vendored, generated, backup, global, and unrelated files remain untouched.

