# Confirmed scope and pending discussion

Confirmed owner direction: fun experimental Rust physics project; Linux x64 qualification and a dedicated performance host are optional, not ordinary completion gates. Preserve truthful evidence, normal local checks, and historical records. Current implementation removes automatic Linux Cargo/oracle routes and retains existing macOS/Windows smoke jobs.

Pending choices: one hosted CI job versus local-only; manual heavy checks versus a scheduled sample versus removing unused workflows. No answer received; broader changes are not implemented. Source of truth: PROJECT-SCOPE.md.

## Accepted follow-up

The owner selected local checks plus one CI job and optional manual expensive checks. Default Cargo CI is one macOS smoke job. Earlier pending questions are answered; release/API simplifications remain proposals.
