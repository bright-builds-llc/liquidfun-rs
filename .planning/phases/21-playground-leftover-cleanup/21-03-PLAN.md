---
phase: 21-playground-leftover-cleanup
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/xtask/tests/upstream_cli.rs
  - tools/xtask/tests/upstream_cli/verify.rs
  - tools/xtask/tests/upstream_cli/configure.rs
  - tools/xtask/tests/upstream_cli/build.rs
  - tools/xtask/tests/upstream_cli/failures.rs
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-09-20T19-45-21
generated_at: 2026-09-20T20:03:03Z
must_haves:
  truths:
    - "color_mixer.rs, dam_break.rs, and water_wheel.rs each have ≤ 628 physical lines and are absent from file-lengths findings."
    - "tools/xtask/tests/upstream_cli.rs uses foo.rs plus foo/ child modules, every former #[test] name still exists, and bun scripts/bright-builds-check.ts file-lengths and all exit 0."
    - "xtask CLI argument vectors, fake-tool env vars, and playground-dam-break-bench registration stay unchanged; C++ timing is not run."
  artifacts:
    - path: tools/xtask/tests/upstream_cli.rs
      provides: "RepositoryFixture, FakeTools, helpers, and mod children"
    - path: tools/xtask/tests/upstream_cli/verify.rs
      provides: "verify_* tests"
    - path: tools/xtask/tests/upstream_cli/configure.rs
      provides: "configure_* tests"
    - path: tools/xtask/tests/upstream_cli/build.rs
      provides: "build_* and upstream_tests_build tests including playground-dam-break-bench registration"
    - path: tools/xtask/tests/upstream_cli/failures.rs
      provides: "LF gitattributes and clang compatibility tests"
  key_links:
    - from: tools/xtask/tests/upstream_cli.rs
      to: tools/xtask/tests/upstream_cli/build.rs
      via: "mod build; build_accepts_the_registered_playground_dam_break_bench"
      pattern: "mod build"
    - from: tools/xtask/tests/upstream_cli.rs
      to: env CARGO_BIN_EXE_xtask
      via: "RepositoryFixture::command"
      pattern: "CARGO_BIN_EXE_xtask"
---

<objective>
Confirm the three named WASM scene files are under the Bright Builds 628-line gate, then split `upstream_cli.rs` so `file-lengths` and `all` exit 0 without changing xtask CLI behavior.

Purpose: D-07/D-08/D-09. Named scene files already sit at 346/360/426 after the eut `tests.rs` extraction. The remaining finding is `tools/xtask/tests/upstream_cli.rs` at 642 lines. Phase verification requires `bun scripts/bright-builds-check.ts all` to exit 0, so split that test crate using `foo.rs` plus `foo/`. No TSV exception.
Output: unchanged scene physics; `upstream_cli.rs` plus four child test modules; checker `all` exit 0.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/code-shape.md
@standards/languages/rust.md
@standards/core/testing.md
@.planning/phases/21-playground-leftover-cleanup/21-CONTEXT.md
@.planning/phases/21-playground-leftover-cleanup/21-RESEARCH.md
@tools/xtask/tests/upstream_cli.rs
@tools/xtask/tests/inventory_cli.rs
@crates/liquidfun-wasm/src/scene/color_mixer.rs
@crates/liquidfun-wasm/src/scene/dam_break.rs
@crates/liquidfun-wasm/src/scene/water_wheel.rs

<interfaces>
Named scene files to confirm (do not split unless a parent exceeds 628 physical lines):

- `crates/liquidfun-wasm/src/scene/color_mixer.rs` — live 346; already has `#[cfg(test)] mod tests;` plus `color_mixer/tests.rs`
- `crates/liquidfun-wasm/src/scene/dam_break.rs` — live 360; already has `#[cfg(test)] mod tests;` plus `dam_break/tests.rs`
- `crates/liquidfun-wasm/src/scene/water_wheel.rs` — live 426; already has `#[cfg(test)] mod tests;` plus `water_wheel/tests.rs`

If a named parent is ≤ 628: do not edit it. Do not retune particle counts, gravity, joints, mix strength, pointer magnitudes, or catalog/WASM ids (D-08). Run `cargo test -p liquidfun-wasm --lib` only if a named parent actually moves.

Split `tools/xtask/tests/upstream_cli.rs` like `inventory_cli.rs` / rust.md `foo.rs` plus `foo/`. Prefer implicit `mod verify;` (Rust looks for `tests/upstream_cli/verify.rs`). Do not create `upstream_cli/mod.rs`. Do not add `.bright-builds-rules-checks.tsv`. Do not edit `scripts/bright-builds-check.ts`.

Parent keeps `RepositoryFixture`, `FakeTools`, `FAKE_TOOLS`, `FIXTURE_ID`, constants (`REPOSITORY`, `REVISION`, `WRONG_REVISION`, `ADAPTER_SOURCES`, `ADAPTER_INPUT_MANIFEST`), and helpers `workspace_root`, `fake_tools`, `compile_fake_tools`, `copy_fake_tool`, `executable_name`, `assert_failure_category`, `stdout`, `stderr`. Keep `RepositoryFixture::command` using `Command::new(env!("CARGO_BIN_EXE_xtask"))` and the same env keys: `LIQUIDFUN_XTASK_GIT`, `LIQUIDFUN_XTASK_CMAKE`, `LIQUIDFUN_XTASK_NINJA`, `LIQUIDFUN_XTASK_CXX`, `LIQUIDFUN_TEST_REVISION`, `LIQUIDFUN_TEST_REMOTE_URL`, `LIQUIDFUN_TEST_CMAKE_MARKER`. Do not change those argument vectors or env names (D-09).

Child grouping (move `#[test]` functions with identical names and assertions):

`tools/xtask/tests/upstream_cli/verify.rs`:
- `verify_accepts_matching_upstream_identity`
- `verify_rejects_wrong_lock_sha`
- `verify_rejects_dirty_checkout`
- `verify_rejects_missing_submodule`
- `verify_rejects_origin_url_mismatch`

`tools/xtask/tests/upstream_cli/configure.rs`:
- `configure_rejects_unknown_preset`
- `configure_passes_revision_and_adapter_digest_as_structured_arguments`
- `configure_digest_changes_when_vendored_json_header_changes`
- `configure_rejects_extra_path_input_before_cmake`
- `configure_propagates_cmake_failure`
- `configure_retains_stdout_only_cmake_failure_diagnostics`

`tools/xtask/tests/upstream_cli/build.rs`:
- `upstream_tests_build_selects_the_complete_test_suite`
- `build_targets_only_the_registered_reference_executable`
- `build_accepts_the_registered_phase9_lifecycle_contact_witness`
- `build_accepts_the_registered_phase10_group_topology_witness`
- `build_accepts_the_registered_playground_dam_break_bench`
- `build_rejects_unregistered_target_before_cmake`

`tools/xtask/tests/upstream_cli/failures.rs`:
- `repository_text_is_forced_to_lf_for_cross_platform_evidence`
- `upstream_clang_compatibility_covers_deprecated_copy_warning`

Keep `build_accepts_the_registered_playground_dam_break_bench` as a **registration** test (`upstream build --preset oracle-release --target playground-dam-break-bench` against fake cmake). Do not run `just playground-dam-break-bench`. Do not edit `docs/playground-dam-break-timing.md`.

Child modules import fixtures with `use super::{...};`. After the split every listed file must be ≤ 628 physical lines.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm named scene file-lengths then split upstream_cli tests</name>
  <files>tools/xtask/tests/upstream_cli.rs, tools/xtask/tests/upstream_cli/verify.rs, tools/xtask/tests/upstream_cli/configure.rs, tools/xtask/tests/upstream_cli/build.rs, tools/xtask/tests/upstream_cli/failures.rs</files>
  <read_first>
    1. `.planning/phases/21-playground-leftover-cleanup/21-CONTEXT.md` D-07, D-08, D-09
    1. `.planning/phases/21-playground-leftover-cleanup/21-RESEARCH.md` Pattern 4 and Pitfall 2 / Pitfall 4
    1. `standards/languages/rust.md` foo.rs plus foo/
    1. `standards/core/code-shape.md` 628-line trigger
    1. `tools/xtask/tests/upstream_cli.rs`
    1. `tools/xtask/tests/inventory_cli.rs` child `mod` layout
    1. `crates/liquidfun-wasm/src/scene/color_mixer.rs` trailing `mod tests;`
    1. `crates/liquidfun-wasm/src/scene/dam_break.rs` trailing `mod tests;`
    1. `crates/liquidfun-wasm/src/scene/water_wheel.rs` trailing `mod tests;`
  </read_first>
  <action>
    Per D-07, run `wc -l crates/liquidfun-wasm/src/scene/color_mixer.rs crates/liquidfun-wasm/src/scene/dam_break.rs crates/liquidfun-wasm/src/scene/water_wheel.rs`. If each parent is ≤ 628, do not edit those files or their `*/tests.rs` children. Split further only if a named parent is over 628, and then only by moving tests/helpers into existing `foo.rs` plus `foo/` — never by changing physics constants, controls, recipes, gravity, joints, pointer magnitudes, or catalog/WASM ids (D-08). Run `cargo test -p liquidfun-wasm --lib` only in that over-628 split case.

    Per D-09, split `tools/xtask/tests/upstream_cli.rs` (642 lines) using the grouping in this plan's interfaces block. Parent keeps fixtures and helpers; declare `mod verify; mod configure; mod build; mod failures;`. Create the four child files. Move each `#[test]` without renaming it and without changing `command.args([...])` vectors, `assert_eq!(fixture.cmake_arguments()?, [...])` expected arrays, or fake-tool env vars. Keep `build_accepts_the_registered_playground_dam_break_bench`. Do not add `upstream_cli/mod.rs`. Do not create `.bright-builds-rules-checks.tsv`. Do not edit the managed checker. Do not change xtask production CLI code under `tools/xtask/src/`. Do not run C++ timing.
  </action>
  <verify>
    <automated>wc -l crates/liquidfun-wasm/src/scene/color_mixer.rs crates/liquidfun-wasm/src/scene/dam_break.rs crates/liquidfun-wasm/src/scene/water_wheel.rs tools/xtask/tests/upstream_cli.rs tools/xtask/tests/upstream_cli/verify.rs tools/xtask/tests/upstream_cli/configure.rs tools/xtask/tests/upstream_cli/build.rs tools/xtask/tests/upstream_cli/failures.rs</automated>
  </verify>
  <acceptance_criteria>
    1. `wc -l crates/liquidfun-wasm/src/scene/color_mixer.rs` prints a number ≤ 628.
    1. `wc -l crates/liquidfun-wasm/src/scene/dam_break.rs` prints a number ≤ 628.
    1. `wc -l crates/liquidfun-wasm/src/scene/water_wheel.rs` prints a number ≤ 628.
    1. `test -f tools/xtask/tests/upstream_cli/verify.rs tools/xtask/tests/upstream_cli/configure.rs tools/xtask/tests/upstream_cli/build.rs tools/xtask/tests/upstream_cli/failures.rs`.
    1. `test ! -f tools/xtask/tests/upstream_cli/mod.rs`.
    1. `rg -n "^mod verify;|^mod configure;|^mod build;|^mod failures;" tools/xtask/tests/upstream_cli.rs` matches all four.
    1. `rg -n "fn build_accepts_the_registered_playground_dam_break_bench" tools/xtask/tests/upstream_cli/build.rs` matches.
    1. `rg -n "fn verify_accepts_matching_upstream_identity" tools/xtask/tests/upstream_cli/verify.rs` matches.
    1. `rg -n "fn configure_rejects_unknown_preset" tools/xtask/tests/upstream_cli/configure.rs` matches.
    1. `rg -n "CARGO_BIN_EXE_xtask" tools/xtask/tests/upstream_cli.rs` matches.
    1. `rg -n "playground-dam-break-bench" tools/xtask/tests/upstream_cli/build.rs` matches.
    1. Every file listed in this task files element has `wc -l` ≤ 628.
    1. `test ! -f .bright-builds-rules-checks.tsv`.
  </acceptance_criteria>
  <done>Named scene files remain under 628 without recipe changes; upstream_cli tests live in foo.rs plus foo/ children.</done>
</task>

<task type="auto">
  <name>Task 2: Prove xtask tests and Bright Builds all exit 0</name>
  <files>tools/xtask/tests/upstream_cli.rs, tools/xtask/tests/upstream_cli/verify.rs, tools/xtask/tests/upstream_cli/configure.rs, tools/xtask/tests/upstream_cli/build.rs, tools/xtask/tests/upstream_cli/failures.rs</files>
  <read_first>
    1. `.planning/phases/21-playground-leftover-cleanup/21-CONTEXT.md` D-09, D-10
    1. `AGENTS.bright-builds.md` checker routing
    1. `standards/core/verification.md`
  </read_first>
  <action>
    Per D-09 and D-10, run `cargo test -p xtask --test upstream_cli -- --test-threads=1` and require exit 0. Then run `bun scripts/bright-builds-check.ts file-lengths` and `bun scripts/bright-builds-check.ts all` and require both exit 0. The SUMMARY line must not report `tools/xtask/tests/upstream_cli.rs` as a finding. Named scene files must not appear in findings. Do not add a TSV exception. Do not edit `scripts/bright-builds-check.ts`. Do not run `just playground-dam-break-bench`. Do not run `cargo test -p liquidfun-wasm` unless Task 1 actually moved a named scene module. If `all` fails on an unrelated finding introduced by this split, fix the split rather than excepting the file.
  </action>
  <verify>
    <automated>cargo test -p xtask --test upstream_cli -- --test-threads=1 &amp;&amp; bun scripts/bright-builds-check.ts file-lengths &amp;&amp; bun scripts/bright-builds-check.ts all</automated>
  </verify>
  <acceptance_criteria>
    1. `cargo test -p xtask --test upstream_cli -- --test-threads=1` exits 0.
    1. `bun scripts/bright-builds-check.ts file-lengths` exits 0.
    1. `bun scripts/bright-builds-check.ts all` exits 0.
    1. `bun scripts/bright-builds-check.ts file-lengths` stdout does not contain `tools/xtask/tests/upstream_cli.rs`.
    1. `bun scripts/bright-builds-check.ts file-lengths` stdout does not contain `color_mixer.rs`, `dam_break.rs`, or `water_wheel.rs` as findings.
    1. `test ! -f .bright-builds-rules-checks.tsv`.
  </acceptance_criteria>
  <done>Bright Builds file-lengths and all exit 0 without a TSV exception or xtask CLI change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| xtask test argv → fake cmake | Extra path or unregistered target must still fail before cmake. |
| Checker exception TSV → process integrity | A TSV row could hide an oversized file instead of splitting it. |
| Scene module split → physics behavior | Moving production constants would change playground recipes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | T | `.bright-builds-rules-checks.tsv` | mitigate | Do not create a TSV exception; split `upstream_cli.rs` (D-09). |
| T-21-03-02 | T | `RepositoryFixture::command` | mitigate | Keep `CARGO_BIN_EXE_xtask` and the same env/argv assertions. |
| T-21-03-03 | T | named scene modules | mitigate | Edit scene parents only if `wc -l` &gt; 628; never retune recipes (D-08). |
| T-21-03-04 | T | `scripts/bright-builds-check.ts` | mitigate | Do not edit the managed checker. |
| T-21-03-05 | E | Dam Break C++ bench | mitigate | Keep the registration test; do not run `just playground-dam-break-bench`. |
| T-21-03-06 | T | `upstream_cli/mod.rs` | mitigate | Use `foo.rs` plus `foo/*.rs` only (`standards/languages/rust.md`). |

## Residual Risk

Low. Fake-tool tests do not invoke real CMake/Ninja against upstream. Residual risk is a missed assertion if a test body is accidentally rewritten during the move; identical function names plus `cargo test -p xtask --test upstream_cli` catch that.
</threat_model>

<verification>
Run `cargo test -p xtask --test upstream_cli -- --test-threads=1`, `bun scripts/bright-builds-check.ts file-lengths`, and `bun scripts/bright-builds-check.ts all`. Confirm named scene `wc -l` ≤ 628. Do not run `just playground-dam-break-bench`, Playwright, or `just web-smoke`.
</verification>

<success_criteria>
- Named scene files satisfy the 628-line gate without behavior change.
- `upstream_cli` tests are split under `foo.rs` plus `foo/`.
- Bright Builds `file-lengths` and `all` exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/21-playground-leftover-cleanup/21-03-SUMMARY.md`
</output>
