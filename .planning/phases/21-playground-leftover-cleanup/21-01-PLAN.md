---
phase: 21-playground-leftover-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/physics/loader.ts
  - README.md
  - TESTING.md
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-09-20T19-45-21
generated_at: 2026-09-20T20:03:03Z
must_haves:
  truths:
    - "web/src, web/tests, web/e2e, README.md, and TESTING.md contain zero loadProofSession identifiers."
    - "App still constructs worlds only through loadSceneSession(sceneId); generated ProofSession is not renamed."
    - "just web-player-smoke remains the product Chromium gate; just web-smoke stays opt-in Phase 16 forensic chrome behind PHASE16_CLOSURE_ATTEMPT_DIR and is documented as not the v1.1 product gate."
  artifacts:
    - path: web/src/physics/loader.ts
      provides: "loadSceneSession only"
      exports: ["loadSceneSession"]
    - path: README.md
      provides: "Historical just web-smoke wording"
    - path: TESTING.md
      provides: "Historical just web-smoke wording plus existing Phase 16 forensic section"
  key_links:
    - from: web/src/App.tsx
      to: web/src/physics/loader.ts
      via: "loadSceneSession import and await"
      pattern: "loadSceneSession"
    - from: web/package.json
      to: web/e2e/rust-wasm-proof.spec.ts
      via: "test:player omit; test:browser still runs the whole e2e tree"
      pattern: "test:player"
---

<objective>
Delete unused `loadProofSession` and document `just web-smoke` as historical Phase 16 forensic chrome, not the v1.1 product gate.

Purpose: D-01/D-02/D-03 close the unused Dam Break wrapper leftover without folding Dispose-session or PNG-hash forensics into `just web-player-smoke`.
Output: `loader.ts` exporting only `loadSceneSession`; README/TESTING wording that `just web-smoke` is historical opt-in chrome.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/phases/21-playground-leftover-cleanup/21-CONTEXT.md
@.planning/phases/21-playground-leftover-cleanup/21-RESEARCH.md
@web/src/physics/loader.ts
@web/src/App.tsx
@web/package.json
@web/e2e/rust-wasm-proof.spec.ts
@README.md
@TESTING.md

<interfaces>
Keep this exact export in `web/src/physics/loader.ts`. Do not rename generated `ProofSession`.

```ts
export async function loadSceneSession(sceneId: string): Promise<ProofSession> {
  await init({ module_or_path: wasmUrl });
  return new ProofSession(sceneId);
}
```

DELETE these exact lines (D-01). Do not keep a compatibility alias. Do not import this wrapper into App, tests, or `rust-wasm-proof.spec.ts`.

```ts
/** Dam Break wrapper so the Phase 17 player keeps compiling. */
export async function loadProofSession(): Promise<ProofSession> {
  return loadSceneSession("dam-break");
}
```

Live App call site (do not change in this plan):

```ts
const generatedSession = await loadSceneSession(id);
```

`web/package.json` `test:player` must remain exactly:

```
playwright test e2e/player.spec.ts e2e/demo-media-clock.spec.ts e2e/shell.spec.ts e2e/reset-honesty.spec.ts
```

`web/e2e/rust-wasm-proof.spec.ts` must keep:

```ts
test.skip(
  process.env.PHASE16_CLOSURE_ATTEMPT_DIR === undefined,
  "Phase 16 forensic smoke is opt-in and requires PHASE16_CLOSURE_ATTEMPT_DIR",
);
```

Do not add `e2e/rust-wasm-proof.spec.ts` to `test:player`. Do not edit `justfile` `web-smoke` / `web-player-smoke`. Do not set `PHASE16_CLOSURE_ATTEMPT_DIR` in this plan. Do not run `just web-smoke` as a passing gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete loadProofSession and keep loadSceneSession</name>
  <files>web/src/physics/loader.ts</files>
  <read_first>
    1. `.planning/phases/21-playground-leftover-cleanup/21-CONTEXT.md` D-01, D-02
    1. `.planning/phases/21-playground-leftover-cleanup/21-RESEARCH.md` Pattern 1 and Pitfall 3
    1. `web/src/physics/loader.ts`
    1. `web/src/App.tsx` `loadSceneSession` call site
    1. `web/e2e/rust-wasm-proof.spec.ts` skip guard
    1. `web/package.json` `test:player`
  </read_first>
  <action>
    Per D-01, delete `loadProofSession` and its comment from `web/src/physics/loader.ts`. Keep `loadSceneSession(sceneId)` returning `new ProofSession(sceneId)` after `init({ module_or_path: wasmUrl })`. Do not re-wire the Dam Break wrapper into `App.tsx`, `web/tests/**`, `web/e2e/**`, or `rust-wasm-proof.spec.ts`. Do not rename the generated class. Do not edit `web/src/App.tsx` in this plan. Confirm `rg -n "loadSceneSession" web/src/App.tsx` still matches the existing await. Confirm `web/package.json` `test:player` still omits `rust-wasm-proof.spec.ts` (D-02). Confirm the forensic spec still `test.skip`s when `PHASE16_CLOSURE_ATTEMPT_DIR` is undefined. Do not add that spec to `test:player`. Do not run `just web-smoke`.
  </action>
  <verify>
    <automated>test -z "$(rg -n 'loadProofSession' web/src web/tests web/e2e || true)" &amp;&amp; cd web &amp;&amp; bun run test:unit &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "export async function loadSceneSession" web/src/physics/loader.ts` matches.
    1. `rg -n "new ProofSession\(sceneId\)" web/src/physics/loader.ts` matches.
    1. `rg -n "loadProofSession" web/src web/tests web/e2e` is empty.
    1. `rg -n "loadSceneSession" web/src/App.tsx` matches.
    1. `rg -n "e2e/rust-wasm-proof.spec.ts" web/package.json` is empty.
    1. `rg -n "PHASE16_CLOSURE_ATTEMPT_DIR === undefined" web/e2e/rust-wasm-proof.spec.ts` matches.
    1. `rg -n "e2e/player.spec.ts e2e/demo-media-clock.spec.ts e2e/shell.spec.ts e2e/reset-honesty.spec.ts" web/package.json` matches.
    1. `cd web && bun run test:unit` exits 0.
    1. `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>The unused Dam Break wrapper is gone; the live constructor and forensic skip remain.</done>
</task>

<task type="auto">
  <name>Task 2: Document just web-smoke as historical Phase 16 chrome</name>
  <files>README.md, TESTING.md</files>
  <read_first>
    1. `.planning/phases/21-playground-leftover-cleanup/21-CONTEXT.md` D-02, D-03
    1. `.planning/phases/21-playground-leftover-cleanup/21-RESEARCH.md` Pattern 3
    1. `README.md` web playground command block
    1. `TESTING.md` `## Web playground` and `## Phase 16 browser proof`
    1. `AGENTS.md` Repo-Local Guidance markdown-check
  </read_first>
  <action>
    Per D-03, replace the README sentence that currently says `` `just web-smoke` remains the opt-in Phase 16 forensic closure; it is not the playground default. `` with this exact meaning, using these locked phrases: `historical Phase 16 forensic chrome`, `target/phase16/closure-attempt-N`, `PHASE16_CLOSURE_ATTEMPT_DIR`, `Dispose-session`, `PNG-hash`, `not the v1.1 product gate`, `not expected to pass against current Play/Pause/Reset chrome`, and `Ordinary playground proof is just web-player-smoke`. Keep the existing `just web-player-smoke` WEBTEST-01 paragraph and the Firefox/Safari non-claim. Do not document `loadProofSession`. Do not rewrite archived `.planning/**` Phase 16 records.

    In TESTING.md `## Web playground`, replace `` `just web-smoke` remains the opt-in Phase 16 forensic closure, not the product default. `` with the same locked phrases. In `## Phase 16 browser proof`, add one sentence that `just web-smoke` is historical forensic chrome, is not the v1.1 product gate, and is not expected to pass against current Play/Pause/Reset chrome because it still looks for Dispose-session and PNG-hash selectors. Keep the existing clean-checkout command list including `just web-smoke` as the opt-in invocation. Keep `PHASE16_CLOSURE_ATTEMPT_DIR` / closure-attempt directory rules. Do not fold the forensic spec into `test:player`. After README/TESTING edits, run `just markdown-check`. Do not mdformat `.planning/**`.
  </action>
  <verify>
    <automated>just markdown-check</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "historical Phase 16 forensic chrome" README.md TESTING.md` matches both files.
    1. `rg -n "not the v1.1 product gate" README.md TESTING.md` matches both files.
    1. `rg -n "not expected to pass against current Play/Pause/Reset chrome" README.md TESTING.md` matches both files.
    1. `rg -n "PHASE16_CLOSURE_ATTEMPT_DIR" README.md TESTING.md` matches TESTING.md (and README if the new paragraph includes it).
    1. `rg -n "just web-player-smoke" README.md TESTING.md` matches both files.
    1. `rg -n "loadProofSession" README.md TESTING.md web/src web/tests web/e2e` is empty.
    1. `rg -n "e2e/rust-wasm-proof.spec.ts" web/package.json` is empty.
    1. `just markdown-check` exits 0.
  </acceptance_criteria>
  <done>Contributors are told just web-smoke is historical forensic chrome, not the product gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TypeScript loader → generated WASM `ProofSession` | Scene id strings cross into the allowlisted constructor. |
| Contributor docs → which Playwright suite is the product gate | Mislabeling `just web-smoke` as the v1.1 gate would revive Dispose-session / PNG-hash oracles. |
| `PHASE16_CLOSURE_ATTEMPT_DIR` env → product smoke | Forensic attempt dirs must not leak into `just web-player-smoke`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | I | `loadProofSession` | mitigate | Delete the unused wrapper so the only constructor is `loadSceneSession(sceneId)` (D-01). |
| T-21-01-02 | T | `web/package.json` `test:player` | mitigate | Do not add `rust-wasm-proof.spec.ts`; keep the four-file allowlist (D-02). |
| T-21-01-03 | I | `PHASE16_CLOSURE_ATTEMPT_DIR` | mitigate | Do not set the env in this plan; player-smoke path remains `bun scripts/web-build.ts player-smoke`. |
| T-21-01-04 | T | WASM constructor | mitigate | Keep `new ProofSession(sceneId)` behind the existing Rust allowlist; do not pass hash `maybeRaw`. |
| T-21-01-05 | R | README/TESTING | mitigate | State `just web-smoke` is historical Phase 16 chrome, not the v1.1 product gate (D-03). |
| T-21-01-06 | E | crate/npm publish | accept | Publication remains unauthorized; this plan only deletes a helper and updates docs. |

## Residual Risk

Low. Forensic smoke still fails if someone invokes `just web-smoke` against current chrome; that failure is documented rather than rewritten (D-03). Constructor allowlisting remains in `liquidfun-wasm`, unchanged here.
</threat_model>

<verification>
Run `test -z "$(rg -n 'loadProofSession' web/src web/tests web/e2e README.md TESTING.md || true)"`, `cd web && bun run test:unit && bun run typecheck`, and `just markdown-check`. Do not run Playwright, `just web-smoke`, `just web-player-smoke`, or `just playground-dam-break-bench`.
</verification>

<success_criteria>
- `loadProofSession` is gone from product, tests, e2e, README, and TESTING.
- `loadSceneSession` remains the App constructor.
- Docs name `just web-smoke` as historical Phase 16 forensic chrome, not the v1.1 product gate.
</success_criteria>

<output>
After completion, create `.planning/phases/21-playground-leftover-cleanup/21-01-SUMMARY.md`
</output>
