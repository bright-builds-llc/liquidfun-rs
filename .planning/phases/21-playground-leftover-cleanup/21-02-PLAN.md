---
phase: 21-playground-leftover-cleanup
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/FallbackPanel.tsx
  - web/src/App.tsx
  - web/e2e/player.spec.ts
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-09-20T19-45-21
generated_at: 2026-09-20T20:03:03Z
must_haves:
  truths:
    - "Unknown hash #/scene/not-a-scene shows heading Scene not found, locked body copy, and Open Dam Break with href #/scene/dam-break."
    - "FallbackPanel has no empty or not-ready kinds, copy, or props; App Show fallback is FallbackPanel with no props."
    - "Empty hashes still history-replace to #/scene/dam-break; parser kind empty remains for tests and never renders Choose a scene."
  artifacts:
    - path: web/src/components/FallbackPanel.tsx
      provides: "No-prop unknown-only fallback"
      exports: ["FallbackPanel"]
    - path: web/src/App.tsx
      provides: "Show fallback={<FallbackPanel />} after normalizeSceneRoute"
    - path: web/e2e/player.spec.ts
      provides: "Unknown-hash Scene not found plus Open Dam Break"
  key_links:
    - from: web/src/App.tsx
      to: web/src/components/FallbackPanel.tsx
      via: "Show fallback={<FallbackPanel />}"
      pattern: "fallback=\\{<FallbackPanel />\\}"
    - from: web/src/App.tsx
      to: web/src/routing/hash.ts
      via: "normalizeSceneRoute on init and hashchange"
      pattern: "normalizeSceneRoute"
    - from: web/e2e/player.spec.ts
      to: web/e2e/player-helpers.ts
      via: "UNKNOWN_SCENE_PATH /liquidfun-rs/#/scene/not-a-scene"
      pattern: "UNKNOWN_SCENE_PATH"
---

<objective>
Remove dead FallbackPanel empty/not-ready chrome and keep the unknown-hash Scene not found path, without undoing empty-hash Dam Break normalization.

Purpose: D-04/D-05/D-06 and the approved 21-UI-SPEC preservation contract. Empty hashes already become Dam Break; all six catalog ids are ready; only unknown hashes are a real visitor fallback.
Output: no-prop `FallbackPanel`, App wiring without `fallbackProps`, strengthened unknown-hash Playwright assertions. Zero `app.css` edits.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards-overrides.md
@standards/core/frontend-ui.md
@standards/core/code-shape.md
@.planning/phases/21-playground-leftover-cleanup/21-CONTEXT.md
@.planning/phases/21-playground-leftover-cleanup/21-UI-SPEC.md
@.planning/phases/21-playground-leftover-cleanup/21-RESEARCH.md
@web/src/components/FallbackPanel.tsx
@web/src/App.tsx
@web/src/routing/hash.ts
@web/tests/hash.test.ts
@web/e2e/player.spec.ts
@web/e2e/shell.spec.ts

<interfaces>
Replace `web/src/components/FallbackPanel.tsx` with this no-prop component (D-04/D-05, 21-UI-SPEC unknown markup). Do not export `FallbackKind` or `FallbackPanelProps`. Do not interpolate `maybeRaw`. Do not use `innerHTML`. Do not pass `kind`, `sceneTitle`, or `maybeRaw`.

```tsx
const UNKNOWN_HEADING = "Scene not found";
const UNKNOWN_BODY =
  "This playground link does not match a known scene. Open Dam Break, or choose a demo from the navigation list.";
const OPEN_DAM_BREAK = "Open Dam Break";

/** Unknown-hash copy with a hash-only Open Dam Break control. */
export function FallbackPanel() {
  return (
    <section class="fallback-panel" aria-labelledby="player-title">
      <h2 id="player-title">{UNKNOWN_HEADING}</h2>
      <p class="fallback-copy">{UNKNOWN_BODY}</p>
      <a class="product-control" href="#/scene/dam-break">
        {OPEN_DAM_BREAK}
      </a>
    </section>
  );
}
```

DELETE from `FallbackPanel.tsx`: `EMPTY_HEADING`, `EMPTY_BODY`, `NOT_READY_BODY`, `FallbackKind`, `FallbackPanelProps`, `fallbackCopy`, and any `empty` / `not-ready` branches.

In `web/src/App.tsx`:

1. Change the FallbackPanel import to `import { FallbackPanel } from "./components/FallbackPanel";` (no `FallbackPanelProps` type import).
1. DELETE the entire `fallbackProps` function (current lines 38–52).
1. Change `fallback={<FallbackPanel {...fallbackProps(route())} />}` to `fallback={<FallbackPanel />}`.
1. Keep `normalizeSceneRoute` on init and hashchange, including `history.replaceState` when `maybeReplacementHash` is set (D-06).
1. Keep `routeIdentity`'s `"empty"` branch for union exhaustiveness; do not render empty FallbackPanel chrome.
1. Keep `loadSceneSession(id)` on ready scene ids only. Do not construct WASM for unknown hashes.
1. `wc -l web/src/App.tsx` must stay ≤ 628. Do not edit `web/src/app.css` (already 628). Do not restore `Choose a scene`.

Keep `web/src/routing/hash.ts` unchanged: `kind: "empty"` parse for `""`, `"#"`, `"#/"`, `"#/scene"`, `"#/scene/"`; `normalizeSceneRoute` still returns `{ kind: "scene", id: "dam-break" }` plus `maybeReplacementHash: "#/scene/dam-break"`.

Existing Playwright unknown-hash test in `web/e2e/player.spec.ts` (`returns from an unknown hash through Open Dam Break`) must keep:

```ts
await page.goto(UNKNOWN_SCENE_PATH, { waitUntil: "domcontentloaded" });
await expect(page.getByRole("heading", { name: "Scene not found" })).toBeVisible();
await page.getByRole("link", { name: "Open Dam Break" }).click();
await expect(page).toHaveURL(/#\/scene\/dam-break$/);
```

Add these two assertions before the click (same test, no new spec file, no PNG hashes, no Firefox/WebKit):

```ts
await expect(page.getByRole("heading", { name: "Choose a scene" })).toHaveCount(0);
await expect(
  page.getByText(
    "This playground link does not match a known scene. Open Dam Break, or choose a demo from the navigation list.",
  ),
).toBeVisible();
```

Do not import `FallbackPanel.tsx` into Vitest. Do not edit `web/tests/hash.test.ts` except to keep it passing unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse FallbackPanel to unknown-only and delete fallbackProps</name>
  <files>web/src/components/FallbackPanel.tsx, web/src/App.tsx</files>
  <read_first>
    1. `.planning/phases/21-playground-leftover-cleanup/21-CONTEXT.md` D-04, D-05, D-06
    1. `.planning/phases/21-playground-leftover-cleanup/21-UI-SPEC.md` Copywriting Contract and Unknown fallback markup
    1. `.planning/phases/21-playground-leftover-cleanup/21-RESEARCH.md` Pattern 2 and Pitfall 1
    1. `web/src/components/FallbackPanel.tsx`
    1. `web/src/App.tsx` `fallbackProps` and `Show` fallback
    1. `web/src/routing/hash.ts`
    1. `standards/core/code-shape.md` 628-line trigger
  </read_first>
  <action>
    Per D-04 and 21-UI-SPEC, rewrite `FallbackPanel.tsx` to the no-prop unknown-only component in this plan's interfaces block. Keep heading `Scene not found`, body `This playground link does not match a known scene. Open Dam Break, or choose a demo from the navigation list.`, CTA `Open Dam Break`, and `href="#/scene/dam-break"`. Render those strings as text children. Do not use `innerHTML`. Do not interpolate `maybeRaw` into heading, body, `aria-*`, or `href`. Delete `empty` and `not-ready` kinds, `EMPTY_HEADING`, `EMPTY_BODY`, `NOT_READY_BODY`, `FallbackKind`, `FallbackPanelProps`, and `fallbackCopy`.

    Per D-04, delete `fallbackProps` from `App.tsx` and mount `fallback={<FallbackPanel />}`. Per D-06, do not edit `normalizeSceneRoute` and do not undo empty-hash Dam Break `replaceState`. Do not restore `Choose a scene`. Do not add not-ready copy. Keep PlayerPanel `.empty-state` loading strings unchanged. Do not edit `web/src/app.css`. Do not add Tailwind, MysticUI, or shadcn. Keep Kobalte drawer-only. After edits, `wc -l web/src/App.tsx` must be ≤ 628 and `wc -l web/src/app.css` must remain 628.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck &amp;&amp; bun run test:unit -- tests/hash.test.ts tests/runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "export function FallbackPanel\(\)" web/src/components/FallbackPanel.tsx` matches.
    1. `rg -n "Scene not found" web/src/components/FallbackPanel.tsx` matches.
    1. `rg -n 'href="#/scene/dam-break"' web/src/components/FallbackPanel.tsx` matches.
    1. `rg -n "Choose a scene|EMPTY_HEADING|NOT_READY_BODY|not-ready|FallbackKind|FallbackPanelProps|fallbackCopy|fallbackProps" web/src` is empty.
    1. `rg -n "is not ready yet" web/src` is empty.
    1. `rg -n "innerHTML" web/src` is empty.
    1. `rg -n "maybeRaw" web/src/components/FallbackPanel.tsx` is empty.
    1. `rg -n "fallback=\\{<FallbackPanel />\\}" web/src/App.tsx` matches.
    1. `rg -n "normalizeSceneRoute" web/src/App.tsx web/src/routing/hash.ts` matches both.
    1. `rg -n 'id: "dam-break"' web/src/routing/hash.ts` matches.
    1. `wc -l web/src/App.tsx` prints a number ≤ 628.
    1. `wc -l web/src/app.css` prints 628.
    1. `cd web && bun run typecheck` exits 0.
    1. `cd web && bun run test:unit -- tests/hash.test.ts tests/runtime.test.ts` exits 0.
  </acceptance_criteria>
  <done>Unknown-only FallbackPanel is mounted with no props; empty/not-ready chrome is gone; empty-hash normalization remains.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen unknown-hash Playwright assertions without a browser matrix</name>
  <files>web/e2e/player.spec.ts</files>
  <read_first>
    1. `.planning/phases/21-playground-leftover-cleanup/21-CONTEXT.md` D-05, D-06, D-10
    1. `.planning/phases/21-playground-leftover-cleanup/21-UI-SPEC.md` Focused Browser Evidence
    1. `web/e2e/player.spec.ts` test `returns from an unknown hash through Open Dam Break`
    1. `web/e2e/player-helpers.ts` `UNKNOWN_SCENE_PATH`
    1. `web/e2e/shell.spec.ts` empty-hash history test
    1. `web/tests/hash.test.ts` `normalizeSceneRoute` empty cases
  </read_first>
  <action>
    Per D-05, edit only the existing `returns from an unknown hash through Open Dam Break` test in `web/e2e/player.spec.ts`. After the heading `Scene not found` assertion and before clicking Open Dam Break, add `getByRole("heading", { name: "Choose a scene" })` `toHaveCount(0)` and `getByText` for the locked unknown body. Keep `UNKNOWN_SCENE_PATH` (`/liquidfun-rs/#/scene/not-a-scene`), the Open Dam Break click, and the `/#\/scene\/dam-break$/` URL assertion. Do not add PNG hashes, `createHash`, screenshot oracles, Firefox/Safari/WebKit projects, or a Pages Playwright job. Do not add `rust-wasm-proof.spec.ts` to `test:player`. Do not import `FallbackPanel.tsx` into Vitest. Do not edit `web/src/routing/hash.ts` or `web/e2e/shell.spec.ts`. Confirm `web/tests/hash.test.ts` still expects empty hashes `""`, `"#"`, `"#/"`, `"#/scene"`, `"#/scene/"` to normalize to Dam Break. Do not run `just web-player-smoke` in this plan; Plan 21-04 runs it after Wave 1 completes.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/hash.test.ts &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "returns from an unknown hash through Open Dam Break" web/e2e/player.spec.ts` matches.
    1. `rg -n 'name: "Scene not found"' web/e2e/player.spec.ts` matches.
    1. `rg -n 'name: "Choose a scene"' web/e2e/player.spec.ts` matches.
    1. `rg -n "This playground link does not match a known scene" web/e2e/player.spec.ts` matches.
    1. `rg -n "UNKNOWN_SCENE_PATH" web/e2e/player.spec.ts` matches.
    1. `rg -n "e2e/rust-wasm-proof.spec.ts" web/package.json` is empty.
    1. `rg -n "normalizes every empty hash to Dam Break" web/tests/hash.test.ts` matches.
    1. `git diff -- web/src/routing/hash.ts web/src/app.css web/e2e/shell.spec.ts` is empty.
    1. `cd web && bun run test:unit -- tests/hash.test.ts` exits 0.
    1. `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Unknown-hash coverage still asserts Scene not found and Open Dam Break, and now also asserts Choose a scene is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hash-route token → fallback HTML | Unknown `maybeRaw` must never become HTML or `href`. |
| Empty hash → history replace | Normalization must stay Dam Break, not a second fallback view. |
| Fallback CTA → scene constructor | Open Dam Break is a hash-only in-page link, not a `javascript:` or origin-absolute URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | T | `FallbackPanel` copy | mitigate | Fixed text nodes only; no `innerHTML`; no `maybeRaw` interpolation (ASVS V5). |
| T-21-02-02 | S | Open Dam Break `href` | mitigate | Literal `href="#/scene/dam-break"`; never Pages origin or `javascript:`. |
| T-21-02-03 | I | unknown hash render | mitigate | Keep heading `Scene not found` plus Open Dam Break; do not leave a blank canvas (D-05). |
| T-21-02-04 | T | `normalizeSceneRoute` | mitigate | Do not undo empty-hash Dam Break replacement (D-06). |
| T-21-02-05 | T | `web/src/app.css` | mitigate | Zero CSS edits; file stays 628 lines; no design-system migration. |
| T-21-02-06 | E | WASM on unknown hash | mitigate | `Show` fallback mounts `FallbackPanel` and must not call `loadSceneSession`. |

## Residual Risk

Low. If `kind: "empty"` leaked past `normalizeSceneRoute`, the no-prop panel would still show unknown copy rather than a blank canvas. That leak is already prevented by App init/hashchange normalization plus unit tests.
</threat_model>

<verification>
Run `cd web && bun run typecheck && bun run test:unit -- tests/hash.test.ts tests/runtime.test.ts`. Confirm `rg` emptiness for `Choose a scene`, `EMPTY_HEADING`, `not-ready`, `fallbackProps`, and `innerHTML` under `web/src`. Do not run `just web-player-smoke` until Plan 21-04. Do not run Firefox/Safari, PNG hashes, or C++ timing.
</verification>

<success_criteria>
- FallbackPanel is unknown-only and takes no props.
- App Show fallback is a no-prop FallbackPanel element.
- Empty-hash Dam Break normalization is unchanged.
- Unknown-hash Playwright assertions cover Scene not found, locked body, absent Choose a scene, and Open Dam Break.
</success_criteria>

<output>
After completion, create `.planning/phases/21-playground-leftover-cleanup/21-02-SUMMARY.md`
</output>
