---
phase: 21-playground-leftover-cleanup
plan: "04"
type: execute
wave: 2
depends_on: [21-01, 21-02, 21-03]
files_modified:
  - .planning/phases/21-playground-leftover-cleanup/21-REVIEW.md
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-09-20T19-45-21
generated_at: 2026-09-20T20:03:03Z
must_haves:
  truths:
    - "just web-player-smoke exits 0 on phase HEAD with PHASE16_CLOSURE_ATTEMPT_DIR unset and rust-wasm-proof.spec.ts still omitted from test:player."
    - "A separate identified AI reviewer acknowledges the Phase 21 leftover-cleanup diff and smoke evidence at an exact SHA-256 digest."
    - "The implementing executor does not approve its own work; no crate/npm publish, release tag, or new GitHub Pages URL."
  artifacts:
    - path: .planning/phases/21-playground-leftover-cleanup/21-REVIEW.md
      provides: "Independent exact-digest acknowledgment"
  key_links:
    - from: .planning/phases/21-playground-leftover-cleanup/21-REVIEW.md
      to: digest
      via: "Separate reviewer identity and SHA-256"
      pattern: "digest:"
    - from: justfile
      to: scripts/web-build.ts
      via: "web-player-smoke → player-smoke mode"
      pattern: "web-player-smoke"
---

<objective>
Re-run Chromium `just web-player-smoke` after leftover player/fallback/docs/xtask edits, then obtain independent AI review bound to an exact digest.

Purpose: D-10/D-11. Product proof is the existing Chromium player suite, not `just web-smoke`. Owner policy 2026-09-16: the implementing agent must not approve its own work. No publish, tag, or new Pages URL.
Output: passing `just web-player-smoke` plus `21-REVIEW.md` bound to an exact digest.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@.planning/phases/21-playground-leftover-cleanup/21-CONTEXT.md
@.planning/phases/21-playground-leftover-cleanup/21-01-SUMMARY.md
@.planning/phases/21-playground-leftover-cleanup/21-02-SUMMARY.md
@.planning/phases/21-playground-leftover-cleanup/21-03-SUMMARY.md
@.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md
@.planning/phases/19-interaction-polish-and-browser-verification/19-REVIEW.md

<interfaces>
Do **not** push to main, create a release tag, publish npm/crates, write HOST-EVIDENCE, or claim a new GitHub Pages URL. D-11 local Chromium smoke is sufficient.

Do **not** run `just web-smoke` as a passing gate. Do **not** set `PHASE16_CLOSURE_ATTEMPT_DIR`. Do **not** run `just playground-dam-break-bench`. Do **not** add Playwright to `.github/workflows/pages.yml`. Do **not** add Firefox/Safari/WebKit. Do **not** add PNG-hash oracles to `test:player`.

`just web-player-smoke` must remain `bun scripts/web-build.ts player-smoke`, which runs `bun run test:player` listing only `e2e/player.spec.ts`, `e2e/demo-media-clock.spec.ts`, `e2e/shell.spec.ts`, and `e2e/reset-honesty.spec.ts`.

Review manifest — hash these paths that exist on disk after Plans 01–03; skip a path only if git shows it was never added, and record that skip. Concatenate file bytes in this exact order and compute one lowercase SHA-256 (`cat` the list `| shasum -a 256`). Record that exact command in `21-REVIEW.md`.

1. `web/src/physics/loader.ts`
1. `README.md`
1. `TESTING.md`
1. `web/src/components/FallbackPanel.tsx`
1. `web/src/App.tsx`
1. `web/e2e/player.spec.ts`
1. `web/package.json`
1. `web/e2e/rust-wasm-proof.spec.ts`
1. `tools/xtask/tests/upstream_cli.rs`
1. `tools/xtask/tests/upstream_cli/verify.rs`
1. `tools/xtask/tests/upstream_cli/configure.rs`
1. `tools/xtask/tests/upstream_cli/build.rs`
1. `tools/xtask/tests/upstream_cli/failures.rs`
1. `.planning/phases/21-playground-leftover-cleanup/21-CONTEXT.md`

Independent review:

1. Launch a separate identified AI reviewer (`gsd-code-reviewer` subagent). Do not resume the implementing agent. Do not treat this executor as the reviewer.
1. The reviewer inspects the complete relevant diff, the `just web-player-smoke` result, D-01..D-11 fidelity, and leftover deletion honesty (no `loadProofSession`, no empty/not-ready FallbackPanel, named scenes under 628, `bright-builds-check.ts all` exit 0).
1. Write `.planning/phases/21-playground-leftover-cleanup/21-REVIEW.md` with:
   - `digest:` 64 lowercase hex
   - `reviewer_identity:` actual reviewer id
   - `reviewed_at:` actual ISO timestamp
   - `reviewer_disclosure: AI reviewer, not a human`
   - `implementing_or_fixing_executor: no`
   - decision APPROVED or CHANGES_REQUESTED bound to that digest
1. Passing `just web-player-smoke` is not the acknowledgment.
1. Do not format `.planning/**` with mdformat.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run just web-player-smoke on phase HEAD</name>
  <files>.planning/phases/21-playground-leftover-cleanup/21-REVIEW.md</files>
  <read_first>
    1. `.planning/phases/21-playground-leftover-cleanup/21-CONTEXT.md` D-10, D-11
    1. `web/package.json` `test:player`
    1. `justfile` `web-player-smoke` and `web-smoke`
    1. `scripts/web-build.ts` `player-smoke` vs `smoke` modes
    1. `.planning/phases/21-playground-leftover-cleanup/21-01-SUMMARY.md`
    1. `.planning/phases/21-playground-leftover-cleanup/21-02-SUMMARY.md`
    1. `.planning/phases/21-playground-leftover-cleanup/21-03-SUMMARY.md`
  </read_first>
  <action>
    Per D-10, run `just web-player-smoke` on the current phase HEAD so WASM, production-base `/liquidfun-rs/` preview, `e2e/player.spec.ts` (including unknown-hash Open Dam Break), `e2e/demo-media-clock.spec.ts`, `e2e/shell.spec.ts` (including empty-hash Dam Break normalization), and `e2e/reset-honesty.spec.ts` all execute. Leave `PHASE16_CLOSURE_ATTEMPT_DIR` unset. Confirm `rg -n "e2e/rust-wasm-proof.spec.ts" web/package.json` is empty before relying on smoke. Confirm `rg -n "loadProofSession" web/src` is empty. Do not add Playwright to `.github/workflows/pages.yml`. Do not run `just web-smoke`. Do not run `just playground-dam-break-bench`. Do not push. Do not create a git tag. Record the smoke command and exit 0 in a `21-REVIEW.md` draft only after the command succeeds; leave reviewer identity and decision fields for Task 2.
  </action>
  <verify>
    <automated>just web-player-smoke</automated>
  </verify>
  <acceptance_criteria>
    1. `just web-player-smoke` exits 0.
    1. `rg -n "e2e/player.spec.ts e2e/demo-media-clock.spec.ts e2e/shell.spec.ts e2e/reset-honesty.spec.ts" web/package.json` matches.
    1. `rg -n "e2e/rust-wasm-proof.spec.ts" web/package.json` is empty.
    1. `rg -n "loadProofSession" web/src web/tests web/e2e README.md TESTING.md` is empty.
    1. `rg -n "playwright" .github/workflows/pages.yml` is empty.
    1. No new git tag is created (`git tag --contains HEAD` does not add a phase-21 release tag).
    1. `PHASE16_CLOSURE_ATTEMPT_DIR` was unset for the smoke run.
  </acceptance_criteria>
  <done>Chromium player-smoke covers leftover player/fallback cleanup on this HEAD without forensic env.</done>
</task>

<task type="auto">
  <name>Task 2: Obtain independent exact-digest AI review</name>
  <files>.planning/phases/21-playground-leftover-cleanup/21-REVIEW.md</files>
  <read_first>
    1. `AGENTS.md` Independent review
    1. `.planning/phases/21-playground-leftover-cleanup/21-CONTEXT.md` D-11
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md`
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-REVIEW.md`
  </read_first>
  <action>
    Per D-11 and the 2026-09-16 owner policy, the implementing agent must not approve its own work. Build the review manifest and lowercase SHA-256 digest over the files in this plan's interfaces block using the listed `cat | shasum -a 256` order. Launch a separate identified `gsd-code-reviewer` AI reviewer (new subagent, not this implementing executor) to inspect the complete Phase 21 diff, `just web-player-smoke` evidence, D-01..D-11, Bright Builds `all` exit 0, deleted `loadProofSession`, unknown-only FallbackPanel, and the xtask test split. Write `21-REVIEW.md` bound to that exact digest, the reviewer's actual identity, and the actual ISO review time. State `reviewer_disclosure: AI reviewer, not a human` and `implementing_or_fixing_executor: no`. Do not treat passing automated checks as the acknowledgment. Do not publish a crate. Do not create a release tag. Do not claim a new GitHub Pages URL. Do not represent the review as human approval. Do not mdformat `.planning/**`.
  </action>
  <verify>
    <automated>rg -n "digest:|reviewer_identity:|reviewed_at:|AI reviewer, not a human|implementing_or_fixing_executor: no" .planning/phases/21-playground-leftover-cleanup/21-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    1. `test -f .planning/phases/21-playground-leftover-cleanup/21-REVIEW.md`.
    1. `rg -n "^digest: [0-9a-f]{64}$" .planning/phases/21-playground-leftover-cleanup/21-REVIEW.md` matches.
    1. `rg -n "reviewer_identity:" .planning/phases/21-playground-leftover-cleanup/21-REVIEW.md` matches.
    1. `rg -n "AI reviewer, not a human" .planning/phases/21-playground-leftover-cleanup/21-REVIEW.md` matches.
    1. `rg -n "implementing_or_fixing_executor: no" .planning/phases/21-playground-leftover-cleanup/21-REVIEW.md` matches.
    1. The reviewer identity is not the implementing executor label.
    1. No self-approval sentence from the implementing agent is used as the acknowledgment.
    1. `rg -n "new GitHub Pages URL|crates.io|npm publish" .planning/phases/21-playground-leftover-cleanup/21-REVIEW.md` does not grant publication or a new Pages URL.
  </acceptance_criteria>
  <done>Independent review is bound to the exact Phase 21 digest; the implementer did not self-approve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Review digest → acknowledgment | Only a separate reviewer may bind approval. |
| Product smoke allowlist → claimed coverage | `rust-wasm-proof.spec.ts` must not be cited as D-10 evidence. |
| Forensic env → player-smoke | `PHASE16_CLOSURE_ATTEMPT_DIR` must stay unset. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | R | self-review | mitigate | Separate `gsd-code-reviewer` plus exact digest (D-11). |
| T-21-04-02 | T | stale smoke | mitigate | Re-run `just web-player-smoke` on phase HEAD before review. |
| T-21-04-03 | T | `just web-smoke` as pass gate | mitigate | Do not invoke forensic smoke; D-02/D-03 keep it historical. |
| T-21-04-04 | S | human-approval claim | mitigate | Disclose AI reviewer; never call it human approval. |
| T-21-04-05 | E | package release / new Pages URL | accept | Publication and a new Pages URL remain unauthorized; do not tag. |
| T-21-04-06 | I | forensic env leak | mitigate | Leave `PHASE16_CLOSURE_ATTEMPT_DIR` unset; confirm `test:player` omit list. |

## Residual Risk

Low. Independent review can still miss a wording drift in README; mechanical `rg` in Task 1 plus digest-bound file bytes reduce that. No live Pages re-proof is required (D-11).
</threat_model>

<verification>
Confirm `21-REVIEW.md` digest identity fields and that `just web-player-smoke` exited 0 with `PHASE16_CLOSURE_ATTEMPT_DIR` unset. Do not claim Firefox/Safari coverage. Do not format `.planning/**` with mdformat. Do not publish, tag, or deploy.
</verification>

<success_criteria>
- Combined player-smoke still passes after leftover cleanup.
- Independent AI review acknowledges the exact Phase 21 digest.
- No Pages deploy, package publish, self-approval, or forensic-smoke gate.
</success_criteria>

<output>
After completion, create `.planning/phases/21-playground-leftover-cleanup/21-04-SUMMARY.md`
</output>
