---
phase: 16-rust-wasm-browser-bridge
plan: "02"
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - .gitignore
  - justfile
  - scripts/web-build.ts
  - web/package.json
  - web/bun.lock
  - web/tsconfig.json
  - web/vite.config.ts
  - web/vitest.config.ts
  - web/index.html
  - web/src/physics/frame.ts
  - web/src/physics/loader.ts
  - web/src/physics/session.ts
  - web/src/render/camera.ts
  - web/src/render/canvas.ts
  - web/tests/frame.test.ts
  - web/tests/session.test.ts
  - web/tests/camera.test.ts
autonomous: true
requirements: [WASM-02, WASM-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 16-2026-09-17T01-48-41
generated_at: 2026-09-17T02:08:07Z
must_haves:
  truths:
    - "A contributor can regenerate the current-checkout WASM package and build the minimal frontend through one exact-version repo command."
    - "TypeScript parses five copied typed arrays into a bounded RenderFrame before Canvas code can consume them."
    - "One idempotent TypeScript owner frees every temporary Rust frame and frees its opaque Rust session exactly once."
    - "Pure projection preserves aspect ratio, applies the approved 16px inset, and inverts only the world y-axis."
  artifacts:
    - path: scripts/web-build.ts
      provides: "Exact-tool, rerunnable WASM generation and frontend verification/build orchestration"
    - path: web/package.json
      provides: "Exact SolidJS, Vite, TypeScript, Vitest, and Playwright package pins with Bun 1.4.2"
    - path: web/src/physics/frame.ts
      provides: "Runtime parser and invariant-bearing RenderFrame"
    - path: web/src/physics/session.ts
      provides: "Exactly-once frame/session ownership shell"
    - path: web/src/render/camera.ts
      provides: "Pure fixed-world Canvas projection"
    - path: web/src/render/canvas.ts
      provides: "Imperative bulk-array Canvas 2D renderer"
  key_links:
    - from: scripts/web-build.ts
      to: crates/liquidfun-wasm
      via: "wasm-pack 0.15.0 --target web regeneration from the current checkout"
      pattern: "wasm-pack.*--target.*web"
    - from: web/src/physics/loader.ts
      to: web/src/generated/liquidfun-wasm
      via: "generated initializer plus Vite ?url WASM asset"
      pattern: "_bg\\.wasm\\?url"
    - from: web/src/physics/session.ts
      to: web/src/physics/frame.ts
      via: "temporary ProofFrame parsed inside try/finally before generated free()"
      pattern: "finally"
    - from: web/src/render/canvas.ts
      to: web/src/physics/frame.ts
      via: "one RenderFrame input with bulk arrays, not Solid particle components"
      pattern: "RenderFrame"
---

<objective>
Create the exact-pinned SolidJS/Vite package, reproducible WASM generation command, typed copied-frame adapter, and pure Canvas projection/rendering core.

Purpose: Turn the generated Rust package into a safe, testable frontend boundary while keeping framework and browser effects outside pure frame/camera logic.
Output: A committed Bun lockfile, ignored regenerated bindings, repo-owned web build command, and focused TypeScript ownership/shape/projection tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@PROJECT-SCOPE.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/frontend-ui.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-rust-wasm-browser-bridge/16-CONTEXT.md
@.planning/phases/16-rust-wasm-browser-bridge/16-RESEARCH.md
@.planning/phases/16-rust-wasm-browser-bridge/16-UI-SPEC.md
@.planning/phases/16-rust-wasm-browser-bridge/16-01-SUMMARY.md
@.gitignore
@justfile
@rust-toolchain.toml

<interfaces>
Generated by Plan 16-01 and consumed as an app-local build artifact:

```typescript
export class ProofSession {
  constructor();
  advance(stepCount: number): void;
  captureFrame(): ProofFrame;
  stepIndex(): number;
  particleCount(): number;
  rigidShapeCount(): number;
  free(): void;
}

export class ProofFrame {
  stepIndex(): number;
  particleCount(): number;
  rigidShapeCount(): number;
  particlePositions(): Float32Array;
  particleColors(): Uint8Array;
  particleRadii(): Float32Array;
  rigidSegments(): Float32Array;
  rigidCircles(): Float32Array;
  free(): void;
}
```

The exact emitted declaration is authoritative if wasm-bindgen changes constructor/result spelling. Preserve the semantic method names and typed-array contract; adapt only the thin loader/import shell if generation proves a syntactic difference.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the Bun frontend and make generation reproducible</name>
  <files>.gitignore, justfile, scripts/web-build.ts, web/package.json, web/bun.lock, web/tsconfig.json, web/vite.config.ts, web/vitest.config.ts, web/index.html</files>
  <read_first>.gitignore; justfile; Cargo.toml; rust-toolchain.toml; crates/liquidfun-wasm/Cargo.toml; target/phase16/plan-01/pkg/package.json; target/phase16/plan-01/pkg/liquidfun_wasm.d.ts; 16-CONTEXT.md D-09, D-10, D-14, D-15; 16-RESEARCH.md Standard Stack, Contributor Preparation, Pattern 4, Build and Isolation Gates; standards/core/code-shape.md; standards/core/verification.md; standards/languages/typescript-javascript.md</read_first>
  <action>Per D-09, D-10, D-14, and D-15, if `bun --version` is not exactly `1.4.2`, install the reviewed exact tag with `curl -fsSL https://bun.com/install | bash -s "bun-v1.4.2"`, activate the resulting Bun path for the current shell, and fail unless `bun --version` prints `1.4.2`. Create `web/package.json` with `private: true`, `"packageManager": "bun@1.4.2"`, exact dependency `"solid-js": "1.9.15"`, and exact dev dependencies `"vite": "8.3.0"`, `"vite-plugin-solid": "2.11.14"`, `"typescript": "7.0.2"`, `"vitest": "5.0.1"`, `"@playwright/test": "1.63.0"`, and `"@types/node": "22.20.3"`; use no caret, tilde, git branch, physics, component, router, state, DOM-test-environment, or WASM-plugin dependency. Define scripts `typecheck = "tsc --noEmit"`, `test:unit = "vitest run"`, `build:app = "vite build"`, `preview = "vite preview --host 127.0.0.1 --port 4173"`, `test:browser = "playwright test"`, and `browser:install = "playwright install chromium"`. Run `bun install` once to create and commit `web/bun.lock`, then prove `cd web &amp;&amp; bun install --frozen-lockfile` succeeds without lockfile drift.

Create strict TypeScript/Vite/Vitest configuration for Solid JSX, DOM/ES2024 libraries, `noEmit`, strict/noUncheckedIndexedAccess/exactOptionalPropertyTypes, Vite client types, app root `web/`, local root base `/`, output `web/dist`, and Node-environment Vitest tests under `web/tests/**/*.test.ts`. Create only a semantic root `index.html` mounting `#root`; the approved visual page is Plan 16-03.

Extend `.gitignore` with exact entries `/web/src/generated/liquidfun-wasm/`, `/web/dist/`, `/web/test-results/`, `/web/playwright-report/`, and `/target/web-build/`. Add rerunnable `scripts/web-build.ts` with modes `wasm` and `build`. Resolve repository paths from `import.meta.dir`; verify exact `bun 1.4.2`, `rustc 1.97.0`, and `wasm-pack 0.15.0`; validate the canonical generated directory equals `<repo>/web/src/generated/liquidfun-wasm` before recursive removal; regenerate with `wasm-pack build crates/liquidfun-wasm --target web --release --out-dir ../../web/src/generated/liquidfun-wasm --out-name liquidfun_wasm`; and for `build`, then run from `web/` in order `bun install --frozen-lockfile`, `bun run typecheck`, `bun run test:unit`, and `bun run build:app`. Emit concise breadcrumbs and a JSON/text summary under ignored `target/web-build/`; propagate every nonzero status. Add transparent `web-wasm` and `web-build` recipes that invoke `bun scripts/web-build.ts wasm|build`. Do not hide logic in the justfile, fetch generated bindings at runtime, track generated files, or add deployment/publication behavior.</action>
  <verify>
    <automated>test "$(bun --version)" = "1.4.2" &amp;&amp; cd web &amp;&amp; bun install --frozen-lockfile &amp;&amp; cd .. &amp;&amp; just web-build</automated>
  </verify>
  <acceptance_criteria>
    - `web/package.json` contains `"packageManager": "bun@1.4.2"` and every direct dependency version exactly matches the seven reviewed pins with no `^` or `~`.
    - `web/bun.lock` exists, is tracked, and `bun install --frozen-lockfile` exits 0 without changing it.
    - `.gitignore` contains all five exact generated/build/test paths, and `git status --short -- web/src/generated web/dist target/web-build` is empty after `just web-build`.
    - `scripts/web-build.ts` checks exact Bun/Rust/wasm-pack versions, validates the generated deletion path, runs wasm-pack with `--target web`, and executes typecheck, unit tests, then Vite build in that order.
    - `just web-build` exits 0 and creates `web/dist/index.html`, a hashed JavaScript asset, and a bundled `.wasm` asset from the current checkout.
    - No package release, npm publish, Pages, route, catalog, player, retry, or deployment file is added.
  </acceptance_criteria>
  <done>Exact Bun/package inputs and one repo-owned command reproducibly regenerate ignored bindings and build the local frontend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse copied frames and enforce exactly-once ownership</name>
  <files>web/src/physics/frame.ts, web/src/physics/loader.ts, web/src/physics/session.ts, web/tests/frame.test.ts, web/tests/session.test.ts</files>
  <read_first>web/src/generated/liquidfun-wasm/liquidfun_wasm.d.ts; web/src/generated/liquidfun-wasm/liquidfun_wasm.js; web/package.json; web/tsconfig.json; crates/liquidfun-wasm/src/frame.rs; crates/liquidfun-wasm/src/lib.rs; 16-CONTEXT.md D-04, D-05, D-07, D-12; 16-RESEARCH.md Pattern 2, Pattern 4, Common Pitfalls 3 and 4; standards/core/architecture.md; standards/core/testing.md; standards/languages/typescript-javascript.md</read_first>
  <behavior>
    - "The parser accepts only Float32Array/Uint8Array lanes with exact strides 2/4/1/4/3, count equality, particle cap 512, segment cap 16, circle cap 8, finite floats, and positive radii."
    - "A successful `nextFrame()` advances exactly one bounded Rust step, captures once, returns copied arrays, and frees the temporary generated ProofFrame in `finally`."
    - "Calling `dispose()` zero, one, or multiple additional times frees the generated ProofSession exactly once; any `nextFrame()` after disposal throws the fixed message `Rust/WASM session is disposed`."
    - "An advance, capture, or parse failure marks the owner unusable and frees the generated session once; it never attempts catch-and-continue recovery."
  </behavior>
  <action>Per D-04, D-05, D-07, and D-12, define `RenderFrame` with readonly `stepIndex`, `particleCount`, `rigidShapeCount`, and the five concrete typed arrays. Define a narrow `RawProofFrame` structural interface matching the generated methods. Implement `parseRenderFrame(frame)` as the only raw-to-domain boundary: require integer nonnegative step/count values; cap particles/segments/circles at 512/16/8; call each bulk getter once; require `Float32Array` for positions/radii/segments/circles and `Uint8Array` for colors; use checked/safe-integer expected lengths; reject every non-finite float, every non-positive radius, every length mismatch, and a rigidShapeCount unequal to `segmentCount + circleCount`. Return the copied arrays directly because wasm-bindgen boxed-slice getters already create JavaScript-owned arrays; do not retain the Rust frame wrapper, WASM memory, a pointer, or per-particle objects.

In `loader.ts`, statically import the generated `init` function and `ProofSession`, import `liquidfun_wasm_bg.wasm?url`, and call the generated web initializer with that Vite-resolved URL before constructing the session. If wasm-pack 0.15.0 emits the current `{ module_or_path: wasmUrl }` initializer option, use it; use only the actual generated declaration, not manual `WebAssembly.instantiate` glue.

In `session.ts`, define a composition-based `SceneSession` and `createSceneSession(generatedSession)` owner. Keep a private `disposed` boolean. `nextFrame()` must guard first, call `advance(1)`, call `captureFrame()`, parse inside `try`, and always call `frame.free()` in `finally`. If advance/capture/parse throws, cancel reuse by marking disposed and calling generated session `free()` exactly once before rethrowing a bounded `Error`; do not use exception text as HTML. `dispose()` is idempotent and frees exactly once. Add focused Arrange/Act/Assert Vitest cases for each parser rejection, successful copied-frame retention after `frame.free()`, frame-free on parse error, exact one-step call, failure poisoning, idempotent disposal, and use-after-dispose. Use fakes implementing the structural generated interfaces; do not mock physics motion or replace the Plan 16-03 real browser test.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/frame.test.ts tests/session.test.ts &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/physics/frame.ts` exports `RenderFrame` and contains literal bounds 512, 16, 8 plus stride checks for 2, 4, 1, 4, and 3.
    - Tests independently reject wrong typed-array constructors, each wrong lane length, NaN/infinity, non-positive radii, out-of-bound counts, and inconsistent rigid count.
    - `web/src/physics/session.ts` contains `finally` with `frame.free()` and an idempotent `dispose()` guard; focused tests assert both frame and session free-call counts exactly.
    - `rg "WebAssembly\\.Memory|WebAssembly\\.instantiate|\\.buffer|pointer|ptr" web/src/physics` finds no raw-memory implementation.
    - The generated loader imports `liquidfun_wasm_bg.wasm?url`, awaits initialization, and returns a generated `ProofSession`.
    - Focused unit tests and strict typecheck exit 0 against regenerated declarations.
  </acceptance_criteria>
  <done>Only validated bounded JavaScript-owned arrays reach rendering, and generated Rust owners cannot be reused or double-freed through the TypeScript adapter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement pure projection and bulk Canvas drawing</name>
  <files>web/src/render/camera.ts, web/src/render/canvas.ts, web/tests/camera.test.ts</files>
  <read_first>web/src/physics/frame.ts; web/tsconfig.json; 16-UI-SPEC.md Viewport Contract and Color; 16-CONTEXT.md D-02, D-03, D-05, D-11, D-12, D-15; 16-RESEARCH.md Pattern 5; standards/core/architecture.md; standards/core/testing.md</read_first>
  <behavior>
    - "Projection fits world bounds `(-6,-1)` through `(6,8)` into any positive viewport with a 16px inset, preserves aspect ratio, centers unused space, and maps greater world y to smaller canvas y."
    - "Backing-store sizing uses CSS dimensions times `min(devicePixelRatio, 2)` and rejects non-finite/non-positive dimensions without changing physics coordinates."
    - "Drawing clears `#071018`, draws every particle from bulk positions/radii/RGBA first, then three `#94A3B8` 2px basin segments, then the dynamic circle with `#334155` fill and `#CBD5E1` 2px stroke."
  </behavior>
  <action>Per D-02, D-03, D-05, D-11, D-12, D-15, and the approved UI contract, create pure `camera.ts` types for fixed `WORLD_BOUNDS = { minX: -6, minY: -1, maxX: 6, maxY: 8 }`, validated viewport size, scale/offset calculation, projected point, and projected radius. Use a 16px internal inset, the minimum x/y scale, centered letterboxing, and y inversion only in `projectPoint`; no interpolation, browser elapsed time, particle integration, or mutable physics state. Test exact corners/center, aspect preservation, inset, y inversion, and invalid dimensions with focused Arrange/Act/Assert cases.

Create imperative `canvas.ts` functions `resizeCanvasBackingStore(canvas, cssWidth, cssHeight, devicePixelRatio)` and `drawRenderFrame(context, frame, camera)`. Cap DPR at 2, set the transform so all drawing uses CSS pixels, clear every frame to `#071018`, iterate the validated typed arrays by their strides, convert Rust RGBA to CSS color with alpha/255, draw particle filled circles first, then rigid segments, then rigid circles using the exact approved colors/strokes. Do not create Solid signals/components per particle, attach pointer/keyboard handlers, draw status text, add trails/effects, or maintain a second motion model. Keep Canvas effects thin around the tested projection core.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/camera.test.ts &amp;&amp; bun run typecheck &amp;&amp; bun run build:app</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/render/camera.ts` contains fixed world bounds `-6`, `-1`, `6`, `8`, inset `16`, and exactly one y-axis inversion in projection.
    - Camera tests prove centered aspect-preserving mapping at 960x540 and a narrow viewport, with no coordinate outside the 16px inset.
    - `web/src/render/canvas.ts` contains exact colors `#071018`, `#94A3B8`, `#334155`, and `#CBD5E1`, DPR cap 2, and draw order particles then segments then circles.
    - `rg "requestAnimationFrame|addEventListener|PointerEvent|KeyboardEvent|setInterval|delta|elapsed" web/src/render` returns no scheduling, interaction, or JavaScript integration logic.
    - Unit tests, strict typecheck, and production app build exit 0.
  </acceptance_criteria>
  <done>The renderer consumes one validated bulk frame through a tested coordinate projection and contains presentation only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Generated WASM package to TypeScript | Generated classes and typed arrays enter a runtime parser and explicit owner. |
| Typed RenderFrame to Canvas | Validated numeric lanes become draw commands and backing-store allocations. |
| Package registries/tools to local build | Exact Bun packages and exact wasm-pack output form executable browser artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-16-02-01 | D | Frame parser, step count, and Canvas allocation | mitigate | Rust owner advances one step only; parser caps 512/16/8, checks safe lengths before iteration, and DPR is capped at 2. |
| T-16-02-02 | T/D | Typed-array geometry and projection | mitigate | Reject wrong constructors, lane mismatches, NaN/infinity, non-positive radii, and invalid viewport dimensions before Canvas; never clamp geometry silently. |
| T-16-02-03 | D | Frame/session disposal | mitigate | Use one composition owner, `finally`-free every frame, make session dispose idempotent, poison on any call failure, and test exact free-call counts plus use-after-dispose. |
| T-16-02-04 | T/D | WASM memory growth | mitigate | Consume only boxed-slice copies, retain no generated frame after `finally`, and forbid `.buffer`, pointer, Memory, or zero-copy views in the adapter. |
| T-16-02-05 | T | Dependency/build integrity | mitigate | Pin Bun 1.4.2 and all seven direct packages exactly, commit bun.lock, require frozen install, pin wasm-pack 0.15.0, regenerate before typecheck/build, and ignore outputs. |
</threat_model>

<verification>
Run `just web-build` twice from the same checkout; require both passes, unchanged `web/bun.lock`, ignored generated/build outputs, strict typecheck, all Vitest tests, and a production bundle containing WASM. Run `cargo build -p liquidfun`, plain `cargo build`, and `cargo xtask package verify` to confirm the new web graph remains outside ordinary native and packaged-consumer paths. Run `bun scripts/bright-builds-check.ts all` and `git diff --check`.
</verification>

<success_criteria>
- Bun 1.4.2 plus exact locked packages regenerate the current Rust wrapper and produce a local Vite build through `just web-build`.
- The frontend receives five validated copied typed arrays through one typed frame boundary, never raw WASM memory or one call per particle.
- Exactly-once frame/session disposal and use-after-dispose behavior are covered by focused tests.
- Canvas projection and drawing follow the approved fixed bounds, colors, order, aspect, inset, DPR cap, and no-interaction scope.
- D-02 through D-05, D-07, D-09 through D-12, D-14, and D-15 are implemented without catalog, player, routing, deployment, or publication scope.
</success_criteria>

<output>
After completion, create `.planning/phases/16-rust-wasm-browser-bridge/16-02-SUMMARY.md`
</output>
