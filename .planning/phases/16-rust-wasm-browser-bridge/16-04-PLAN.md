---
phase: 16-rust-wasm-browser-bridge
plan: "04"
type: execute
wave: 4
depends_on: [16-03]
files_modified:
  - justfile
  - scripts/web-build.ts
  - web/playwright.config.ts
  - web/e2e/rust-wasm-proof.spec.ts
  - README.md
  - TESTING.md
  - .planning/phases/16-rust-wasm-browser-bridge/16-REVIEW.md
autonomous: true
requirements: [WASM-01, WASM-02, WASM-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 16-2026-09-17T01-48-41
generated_at: 2026-09-17T02:17:00Z
must_haves:
  truths:
    - "A real package-pinned Chromium instance initializes generated Rust WASM, draws its first valid frame, and visibly advances later Rust-produced frame state."
    - "Every successful Chromium proof retains attached initial, moving, and disposed Canvas PNGs plus machine-readable proof metadata in one immutable closure-attempt directory."
    - "Disposal cancels animation before freeing the single session, preserves the last frame, and prevents all later steps, captures, and draws."
    - "Documented exact commands rebuild and smoke-test the proof while native Cargo and the packaged liquidfun crate remain browser-tool independent."
    - "A separate identified reviewer acknowledges the complete implementation and evidence, including the exact successful Canvas artifacts, at one non-self-referential digest."
  artifacts:
    - path: web/e2e/rust-wasm-proof.spec.ts
      provides: "Real Chromium initialization, movement, visible Canvas, disposal, and retained successful proof artifacts"
    - path: target/phase16/closure-attempt-N/browser/canvas-initial.png
      provides: "Retained first successful visible Rust/WASM Canvas frame"
    - path: target/phase16/closure-attempt-N/browser/canvas-moving.png
      provides: "Retained later successful Canvas frame with changed Rust state and pixels"
    - path: target/phase16/closure-attempt-N/browser/canvas-disposed.png
      provides: "Retained post-disposal Canvas frame proving preserved pixels"
    - path: target/phase16/closure-attempt-N/browser/browser-proof.json
      provides: "Machine-readable source, observation, attachment, and SHA-256 metadata for the three PNGs"
    - path: README.md
      provides: "Pinned clean-checkout browser proof build instructions"
    - path: TESTING.md
      provides: "Focused native/web/Chromium/isolation verification contract"
    - path: .planning/phases/16-rust-wasm-browser-bridge/16-REVIEW.md
      provides: "Independent exact-digest implementation and retained-evidence acknowledgment"
  key_links:
    - from: web/e2e/rust-wasm-proof.spec.ts
      to: web/src/App.tsx
      via: "DOM step/moved instrumentation plus visible Canvas screenshots and Dispose session action"
      pattern: "Dispose session"
    - from: scripts/web-build.ts
      to: target/phase16/closure-attempt-N
      via: "smoke mode allocates a fresh non-overwriting attempt and passes its absolute path to Playwright"
      pattern: "PHASE16_CLOSURE_ATTEMPT_DIR"
    - from: target/phase16/closure-attempt-N/browser/browser-proof.json
      to: target/phase16/closure-attempt-N/browser/canvas-*.png
      via: "relative paths, lowercase SHA-256, byte lengths, dimensions, and initial/moving/disposed observations"
      pattern: "sha256"
    - from: .planning/phases/16-rust-wasm-browser-bridge/16-REVIEW.md
      to: target/phase16/closure-attempt-N/browser
      via: "fixed review manifest includes the exact metadata and three successful PNG byte hashes"
      pattern: "review_digest"
---

<objective>
Prove real Rust-produced visible motion and explicit disposal in Chromium, retain inspectable successful Canvas evidence, document the reproducible workflow, verify native/package isolation, and obtain exact-digest independent review.

Purpose: Close Phase 16 on source-bound browser evidence that a later reviewer can inspect, rather than compile-only, mock, or failure-only artifacts.
Output: One focused Chromium test, a retained successful proof quartet in an immutable closure attempt, contributor/testing documentation, complete verification evidence, and an independent review record.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@PROJECT-SCOPE.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/frontend-ui.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@standards/languages/typescript-javascript.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-rust-wasm-browser-bridge/16-CONTEXT.md
@.planning/phases/16-rust-wasm-browser-bridge/16-RESEARCH.md
@.planning/phases/16-rust-wasm-browser-bridge/16-UI-SPEC.md
@.planning/phases/16-rust-wasm-browser-bridge/16-01-SUMMARY.md
@.planning/phases/16-rust-wasm-browser-bridge/16-02-SUMMARY.md
@.planning/phases/16-rust-wasm-browser-bridge/16-03-SUMMARY.md
@web/src/App.tsx
@web/src/physics/loader.ts
@web/src/physics/session.ts
@web/src/render/canvas.ts
@RELEASE.md

<interfaces>
Plan 16-03 supplies a built page with these proof-only DOM observables:

```typescript
type ProofObservables = {
  readonly wasmInitialized: "true";
  readonly stepIndex: number;
  readonly movedFrameCount: number;
  readonly particleCount: 192;
  readonly rigidShapeCount: 4;
};
```

The exact DOM remains governed by `16-UI-SPEC.md`: fixed lifecycle text, runtime identity, the accessible Canvas, and the native `Dispose session` button. The Chromium harness may read proof-only data attributes but must not introduce a public browser API.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove Chromium motion and retain successful Canvas artifacts</name>
  <files>justfile, scripts/web-build.ts, web/playwright.config.ts, web/e2e/rust-wasm-proof.spec.ts</files>
  <read_first>web/package.json; web/bun.lock; web/src/App.tsx; web/src/physics/loader.ts; web/src/physics/session.ts; web/src/render/canvas.ts; scripts/web-build.ts; justfile; 16-CONTEXT.md D-01, D-12, D-13, D-14, D-15; 16-RESEARCH.md Browser Proof Contract, Build and Isolation Gates, Pitfall 1, and Open Questions (RESOLVED); 16-UI-SPEC.md State and Interaction Contract; standards/core/verification.md</read_first>
  <action>Per D-01, D-12, D-13, D-14, and D-15, create `web/playwright.config.ts` with exactly one package-pinned Chromium project, `testDir: "./e2e"`, no retries for deterministic physics, bounded 30-second test timeout, `use.baseURL = "http://127.0.0.1:4173"`, built-in trace/screenshot retained on failure, and `webServer.command = "bun run preview -- --strictPort"` using the already-built `web/dist` on host `127.0.0.1`, port 4173, with server reuse disabled in CI. Set Playwright output under the exact attempt path supplied by `PHASE16_CLOSURE_ATTEMPT_DIR`, and preserve explicit test attachments on passing runs. Before smoke, run the local exact package script `cd web &amp;&amp; bun run browser:install`; this must install the Chromium revision required by `@playwright/test` 1.63.0, not a floating system browser.

Extend `scripts/web-build.ts` with a `smoke` mode that first executes the complete existing build mode, verifies local Playwright package version 1.63.0, allocates the next unused ignored `target/phase16/closure-attempt-N/` directory without deleting or overwriting any prior attempt, writes source/tool provenance, sets `PHASE16_CLOSURE_ATTEMPT_DIR` to its absolute canonical path, runs `bun run browser:install`, and then runs `bun run test:browser`. Preserve breadcrumb logs, command status, and the exact attempt path; a failed attempt remains failed and a rerun receives a new number. Add transparent `web-smoke: bun scripts/web-build.ts smoke` to `justfile`.

Add one `rust-wasm-proof.spec.ts` test. Require the canonical `PHASE16_CLOSURE_ATTEMPT_DIR` and create its `browser/` child. Navigate to `/`; assert exact Loading identity is present initially or as the first observable state; poll the fixed status to exact `Running Rust/WASM session`; assert `data-wasm-initialized="true"`, numeric particle count 192, rigid count 4, positive step index, and moved-frame count greater than zero. Capture the Canvas to `browser/canvas-initial.png` and attach that exact file to the passing Playwright result with `testInfo.attach`. Poll until a later step index and moved count, capture and attach `browser/canvas-moving.png`, and require byte/pixel difference from the initial PNG so hidden numeric movement alone cannot pass. Assert the runtime text `Rust engine · WebAssembly` and Canvas accessible name. Click the native `Dispose session` button, assert exact Disposed status and disabled button, record final step/moved values, capture and attach `browser/canvas-disposed.png`, wait/poll for at least two animation opportunities, and assert step/moved values plus recaptured Canvas pixels remain unchanged.

Only after every browser assertion passes, atomically write and attach `browser/browser-proof.json` with a schema version, attempt identity, source revision/working-tree identity, package-pinned browser identity, page URL, CSS/backing dimensions, initial/moving/disposed step and moved counts, particle/rigid counts, assertion outcomes, and for each exact PNG its repository-relative path, lowercase SHA-256, byte length, and dimensions. Validate the JSON against the files before the test exits. The test must import no fake frame, mock server, JS physics engine, browser memory view, or generated stub. If Chromium reveals a trap, malformed frame, loader mismatch, or blank Canvas, preserve the failed attempt and fix the responsible Rust/TS boundary with affected native/unit/build reruns; do not replace physics, weaken assertions, add automatic retry, or claim a compile-only pass.</action>
  <verify>
    <automated>just web-smoke</automated>
  </verify>
  <acceptance_criteria>
    - `web/playwright.config.ts` defines one Chromium project, local built preview on `127.0.0.1:4173`, 30-second timeout, zero deterministic retries, and attempt-scoped output.
    - The smoke uses package-pinned Playwright 1.63.0 and its matching installed Chromium; it does not select a system Chrome channel.
    - The single test proves generated WASM initialization, 192 Rust particles, four rigid shapes, increasing Rust step index, moved-frame count above zero, and differing initial/moving Canvas PNG bytes.
    - After `Dispose session`, exact Disposed copy appears, the button is disabled, and step index, moved count, and Canvas pixels remain unchanged.
    - A successful fresh closure attempt contains nonempty `browser/canvas-initial.png`, `browser/canvas-moving.png`, `browser/canvas-disposed.png`, and `browser/browser-proof.json`; all four are attached to the passing Playwright result.
    - `browser-proof.json` names and hashes those exact three PNGs, records the three observation states and source/browser identities, and is validated before `just web-smoke` exits 0.
    - Rerunning smoke never overwrites a prior success or failure and creates no tracked generated, test, or closure output.
    - No broad browser matrix, route, Pages base path, deployment, navigation, play/pause/reset/retry, interaction, or six-scene coverage is added.
  </acceptance_criteria>
  <done>Real Chromium visibly runs and advances this repository's generated Rust WASM frame, demonstrates explicit disposal, and retains reviewer-inspectable passing Canvas proof.</done>
</task>

<task type="auto">
  <name>Task 2: Document workflow and retain complete isolation evidence</name>
  <files>README.md, TESTING.md</files>
  <read_first>AGENTS.md hobby scope and standing authorization; PROJECT-SCOPE.md; README.md; TESTING.md; RELEASE.md publication boundary; Cargo.toml; crates/liquidfun/Cargo.toml; justfile; scripts/web-build.ts; web/package.json; the exact successful closure attempt from Task 1; 16-CONTEXT.md D-08 through D-15; 16-RESEARCH.md Contributor Preparation and Build and Isolation Gates; standards/core/frontend-ui.md; standards/core/verification.md</read_first>
  <action>Per D-08 through D-10 and D-12 through D-15, add a concise `Web browser proof` section to README.md and a focused Phase 16 browser-proof section to TESTING.md. Document prerequisites and exact clean-checkout commands: `rustup target add wasm32-unknown-unknown --toolchain 1.97.0`, `cargo install wasm-pack --version 0.15.0 --locked`, `curl -fsSL https://bun.com/install | bash -s "bun-v1.4.2"`, `cd web &amp;&amp; bun install --frozen-lockfile`, `cd .. &amp;&amp; just web-build`, and `just web-smoke`. State that `web/src/generated/liquidfun-wasm`, `web/dist`, Playwright output, `target/web-build`, and `target/phase16` are ignored/regenerated or retained local evidence; the wrapper is unpublished/private; the proof uses Rust engine frames and Canvas 2D; plain Cargo/native packaged consumers require none of Bun, browser, wasm-pack, C++, or upstream; and Phase 17 owns the shared player, retry/reset, routing, Pages, and reusable lifecycle. Do not add release, npm publish, crate publish, deployment, URL, or parity claims. Run `just markdown-check` after these non-GSD Markdown edits; never format `.planning/**` with mdformat.

Using the exact successful non-overwritten attempt from Task 1, retain its already executed `just web-smoke` log plus actual command logs and a machine-readable closure summary beneath that same `target/phase16/closure-attempt-N/` for: `cargo fmt --all --check`; `cargo test -p liquidfun-wasm`; `cargo clippy -p liquidfun-wasm --all-targets -- -D warnings`; exact `just web-build`; `cargo build -p liquidfun`; `cargo test -p liquidfun --all-features`; plain `cargo build`; `cargo xtask package verify`; `just check`; `just markdown-check`; `bun scripts/bright-builds-check.ts all`; and `git diff --check`. Run `cargo tree -p liquidfun --edges normal` and Cargo metadata checks proving `liquidfun` remains the sole default member, has no wasm-bindgen/web dependency, and its packaged archive excludes `crates/liquidfun-wasm`, `web/`, generated JS/declarations/WASM, browsers, C++, protocol, differential, benchmark, and testbed artifacts. Preserve failed logs and correct scoped defects in a new attempt before rerunning; never relabel or overwrite a failed result. Optional Linux/C++, sanitizer, fuzz, coverage, performance, and release qualification are not completion gates.</action>
  <verify>
    <automated>cargo build &amp;&amp; cargo xtask package verify &amp;&amp; just markdown-check &amp;&amp; bun scripts/bright-builds-check.ts all &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - README.md and TESTING.md contain the exact Rust target, wasm-pack 0.15.0, Bun 1.4.2, frozen install, `just web-build`, and `just web-smoke` commands plus generated/evidence-output ownership.
    - Every listed native/wrapper/web/browser/package/aggregate/Markdown/managed/diff command has a retained actual successful result for the exact reviewed source; failures are preserved rather than overwritten.
    - The retained closure summary includes the exact successful `just web-smoke` result from Task 1 and references its `browser-proof.json` plus three PNG hashes without copying or merging incompatible attempts.
    - Root `Cargo.toml` still contains exactly `default-members = ["crates/liquidfun"]`; `cargo tree -p liquidfun --edges normal` contains no wasm-bindgen, Solid, Vite, Playwright, renderer, protocol, differential, benchmark, or testbed dependency.
    - `cargo xtask package verify` exits 0 with no browser wrapper, web source, generated binding, WASM, Bun, or Chromium file in the liquidfun archive.
    - No package is published, no version/tag/release is selected, no deployment runs, and no strict qualification result is claimed.
  </acceptance_criteria>
  <done>The phase has reproducible contributor instructions, proportional passing evidence, retained successful browser proof, and proven native/package isolation without publication.</done>
</task>

<task type="auto">
  <name>Task 3: Bind implementation and successful Canvas proof to independent review</name>
  <files>.planning/phases/16-rust-wasm-browser-bridge/16-REVIEW.md</files>
  <read_first>AGENTS.md Independent review; PROJECT-SCOPE.md; complete Phase 16 implementation diff; 16-UI-SPEC.md; generated liquidfun_wasm.d.ts; Cargo.lock; web/bun.lock; the exact successful closure summary, browser-proof.json, canvas-initial.png, canvas-moving.png, canvas-disposed.png, and command logs selected by Task 2; all Phase 16 threat models; standards/core/verification.md</read_first>
  <action>After implementation and all evidence are stable, compute a non-self-referential review digest over the complete implementation diff and selected command/Chromium/isolation evidence. Use sorted UTF-8 repository-relative paths, NUL, lowercase SHA-256 of each file's bytes, and LF; hash the concatenation with SHA-256. The fixed manifest must include the exact retained `browser-proof.json`, `canvas-initial.png`, `canvas-moving.png`, and `canvas-disposed.png` paths and byte hashes from the selected successful closure attempt, along with the implementation content and command evidence. Exclude `16-REVIEW.md`, plan summaries, final verification, and other mutable acknowledgments.

Have a separate identified AI or human reviewer—not the implementing agent—inspect the complete relevant diff, UI-SPEC compliance, generated declaration shape, all three retained successful PNGs, their machine-readable proof metadata and hash agreement, visible initial-to-moving pixel change, disposed-frame stability, dependency/lock integrity, native/package isolation, and all five Phase 16 security threats. Write `.planning/phases/16-rust-wasm-browser-bridge/16-REVIEW.md` with actual reviewer identity, AI/human disclosure, actual review time, source commit or exact working-tree base/head, fixed manifest entries, aggregate `review_digest`, findings and resolutions, and explicit acknowledgment of that same digest. If implementation or selected evidence changes after review, rerun affected gates and obtain a new digest acknowledgment. The review grants neither package publication nor release authority.</action>
  <verify>
    <automated>test -s .planning/phases/16-rust-wasm-browser-bridge/16-REVIEW.md &amp;&amp; rg "review_digest|browser-proof\\.json|canvas-initial\\.png|canvas-moving\\.png|canvas-disposed\\.png|reviewer_identity|acknowledg" .planning/phases/16-rust-wasm-browser-bridge/16-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - `16-REVIEW.md` identifies a separate actual reviewer and review time with honest AI/human disclosure.
    - Its fixed content/evidence manifest includes the exact selected successful browser metadata and all three PNGs with hashes matching both disk bytes and `browser-proof.json`.
    - The record contains the exact aggregate SHA-256 and an explicit acknowledgment of that same digest with zero unresolved high-severity finding.
    - The reviewer explicitly records inspection of visible initial/moving difference, disposed stability, approved UI contract, generated typed-array declarations, dependency isolation, and all five threats.
    - No mutable review/summary/final-verification file contributes to its own digest, and any post-review implementation/evidence change invalidates the acknowledgment until renewed.
  </acceptance_criteria>
  <done>A separate reviewer acknowledges the stable Phase 16 implementation and exact retained successful Canvas evidence at one reproducible digest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Browser scheduler and DOM to WASM owner | Browser effects drive one bounded call at a time and can release the owner. |
| Validated Rust frame to visible Canvas | Numeric movement must become visible pixels without JavaScript physics substitution. |
| Passing browser process to retained evidence | Explicit screenshots and metadata cross from ephemeral Chromium state into immutable local proof files. |
| Local lockfiles/tools/evidence to completion claims | Exact executable dependencies and source-bound retained results support the final claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-16-04-01 | D | RAF stepping, frame allocation, and Canvas backing store | mitigate | One step per RAF, Rust step bound 1 through 4, frame caps 512/16/8, DPR cap 2, no catch-up loop, and Chromium asserts bounded counts. |
| T-16-04-02 | T/D | Rust/TS/Canvas geometry | mitigate | Rust checked construction plus TS finite/stride/radius parsing gates all geometry; browser failure stops the session and no JavaScript integration or silent clamp exists. |
| T-16-04-03 | D | Dispose/failure/unmount lifecycle | mitigate | One TS owner, cancel RAF before idempotent free, free temporary frames in finally, poison on error, preserve the last frame, and assert no post-dispose step/draw in Chromium. |
| T-16-04-04 | T/D | Copied arrays and retained Canvas proof | mitigate | Use boxed-slice JavaScript-owned arrays with no raw memory path; hash all three explicit successful PNGs, bind their observations in JSON, attach them to the passing result, and include exact bytes in review. |
| T-16-04-05 | T | Dependency, browser, build, and evidence integrity | mitigate | Exact Cargo/Bun/tool pins and lockfiles, package-pinned Chromium, regenerate-before-smoke, fresh non-overwriting attempt directories, source/tool metadata, native/package isolation checks, and separate exact-digest review. |
</threat_model>

<verification>
Execute the complete closure command list from Task 2 on stable implementation content. Inspect generated declarations and the successful `canvas-initial.png`, `canvas-moving.png`, `canvas-disposed.png`, and `browser-proof.json`; verify attachment retention, metadata-to-byte hash agreement, ignored output ownership, approved UI copy/layout, native/package isolation, and the independent acknowledgment over the exact fixed manifest. Do not substitute optional strict qualification, failure-only attachments, or a compile-only WASM result for retained successful browser proof.
</verification>

<success_criteria>
- A real Chromium browser reaches Running only after generated Rust WASM initialization and a successful first Canvas draw, then observes advancing Rust state and changing pixels.
- Successful browser execution retains and attaches exact initial, moving, and disposed Canvas PNGs plus validated machine-readable source/observation/hash metadata.
- Explicit disposal frees the session once, freezes step/draw observables, and preserves visible Canvas pixels.
- The pinned clean-checkout workflow builds through one repo-owned command; native default and packaged consumers remain independent.
- A separate reviewer inspects and acknowledges the complete stable implementation and exact retained successful evidence digest with no unresolved high-severity finding.
- WASM-01, WASM-02, WASM-03, and D-01 through D-15 are fully evidenced; no Phase 17–19 feature, package publication, release, or deployment action occurs.
</success_criteria>

<output>
After completion, create `.planning/phases/16-rust-wasm-browser-bridge/16-04-SUMMARY.md`
</output>
