---
phase: 16-rust-wasm-browser-bridge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/liquidfun-wasm/Cargo.toml
  - crates/liquidfun-wasm/src/lib.rs
  - crates/liquidfun-wasm/src/frame.rs
  - crates/liquidfun-wasm/src/scene.rs
  - crates/liquidfun-wasm/src/session.rs
autonomous: true
requirements: [WASM-01, WASM-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 16-2026-09-17T01-48-41
generated_at: 2026-09-17T02:08:07Z
must_haves:
  truths:
    - "The existing Rust engine constructs and advances one persistent particle-and-rigid-body basin through an opaque private session."
    - "One coherent frame exposes bounded particle positions, colors, radii, basin segments, and a dynamic rigid circle as owned copied arrays."
    - "The wrapper compiles as real wasm32-unknown-unknown code with wasm-pack 0.15.0 while liquidfun remains the sole default workspace member."
  artifacts:
    - path: crates/liquidfun-wasm/src/session.rs
      provides: "Native-testable world ownership, bounded fixed stepping, frame capture, and the opaque wasm-bindgen session"
    - path: crates/liquidfun-wasm/src/frame.rs
      provides: "Validated FrameData and copied ProofFrame typed-array ABI"
    - path: crates/liquidfun-wasm/src/scene.rs
      provides: "Bounded proof basin, particles, and retained private rigid descriptors"
    - path: crates/liquidfun-wasm/Cargo.toml
      provides: "Unpublished cdylib plus rlib wrapper depending outward on liquidfun"
  key_links:
    - from: crates/liquidfun-wasm/src/session.rs
      to: crates/liquidfun/src/world/step/execution.rs
      via: "SessionCore calls ordinary World::step with fixed checked configuration"
      pattern: "\\.step\\("
    - from: crates/liquidfun-wasm/src/session.rs
      to: crates/liquidfun/src/particle/view.rs
      via: "capture_frame borrows ParticleSystemView once and copies aligned semantic lanes"
      pattern: "particle_system_view"
    - from: crates/liquidfun-wasm/src/frame.rs
      to: "generated JavaScript typed arrays"
      via: "boxed f32/u8 slice return values copied by wasm-bindgen"
      pattern: "Box<\\["
    - from: crates/liquidfun-wasm/Cargo.toml
      to: crates/liquidfun/Cargo.toml
      via: "one-way public path dependency"
      pattern: "liquidfun = \\{ path = \"../liquidfun\" \\}"
---

<objective>
Build and compile the native-testable private Rust/WASM session, bounded proof scene, and owned bulk-frame contract.

Purpose: Retire the highest-risk target/runtime boundary first without changing the published engine or involving frontend presentation.
Output: An unpublished `liquidfun-wasm` crate with focused native tests and a real wasm-pack `--target web` generation result under ignored `target/`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@PROJECT-SCOPE.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-rust-wasm-browser-bridge/16-CONTEXT.md
@.planning/phases/16-rust-wasm-browser-bridge/16-RESEARCH.md
@Cargo.toml
@crates/liquidfun/src/lib.rs
@crates/liquidfun/src/particle/view.rs
@crates/liquidfun/src/world/particle_object/system.rs
@crates/liquidfun/src/world/step/execution.rs

<interfaces>
Existing engine contracts to use directly:

```rust
pub fn World::set_gravity(&mut self, gravity: Vec2) -> Result<(), WorldConfigurationError>;
pub fn World::create_body(&mut self, definition: &BodyDef) -> Result<BodyId, CreateObjectError>;
pub fn World::create_fixture(
    &mut self,
    body: BodyId,
    definition: &FixtureDef,
) -> Result<FixtureId, CreateObjectError>;
pub fn World::create_particle_system_with_def(
    &mut self,
    definition: &ParticleSystemDef,
) -> Result<ParticleSystemId, ArenaInsertError>;
pub fn World::create_particle_with_def<UserAssociation>(
    &mut self,
    system: ParticleSystemId,
    maybe_group: Option<ParticleGroupId>,
    definition: &ParticleDef<UserAssociation>,
) -> Result<ParticleCreationReceipt, CreateObjectError>;
pub fn World::particle_system_view(
    &self,
    system: ParticleSystemId,
) -> Result<ParticleSystemView<'_>, HandleError>;
pub fn World::body_snapshot(&self, body: BodyId) -> Result<BodySnapshot, HandleError>;
pub fn World::step<H: CollisionDecisionHook>(
    &mut self,
    configuration: StepConfiguration,
    hook: &mut H,
    limits: StepLimits,
) -> Result<StepReport, StepError>;
```

`ParticleSystemView::positions()` and `maybe_colors()` are borrow-scoped aligned lanes. `BodySnapshot::position()` is owned. Use these semantic APIs; do not import private storage or the diagnostic/evidence crates.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the private wrapper and validated copied-frame ABI</name>
  <files>Cargo.toml, Cargo.lock, crates/liquidfun-wasm/Cargo.toml, crates/liquidfun-wasm/src/lib.rs, crates/liquidfun-wasm/src/frame.rs</files>
  <read_first>Cargo.toml; Cargo.lock; crates/liquidfun/Cargo.toml; crates/liquidfun/src/lib.rs; crates/liquidfun/src/particle/view.rs; 16-CONTEXT.md D-04 through D-08; 16-RESEARCH.md Pattern 1 and Pattern 2; standards/core/architecture.md; standards/core/testing.md; standards/languages/rust.md</read_first>
  <behavior>
    - "A valid frame with 192 particles, three segments, and one circle reports exact counts and five correctly typed, correctly strided copied lanes."
    - "Frame construction rejects particle counts above 512, more than 16 segments, more than 8 circles, mismatched position/color/radius lengths, non-finite floats, and non-positive radii before export."
    - "Every `ProofFrame` numeric-array method returns a fresh boxed slice; no method returns a pointer, `WebAssembly.Memory`, borrowed slice, engine handle, or per-particle object."
  </behavior>
  <action>Per D-04, D-05, D-07, and D-08, add `crates/liquidfun-wasm` to workspace `members` only and preserve the exact line `default-members = ["crates/liquidfun"]`. Define the package as version `0.0.0`, `publish = false`, edition/rust-version/license/repository from workspace, `[lib] crate-type = ["cdylib", "rlib"]`, `[lints] workspace = true`, and dependencies `liquidfun = { path = "../liquidfun" }` plus `wasm-bindgen = "=0.2.128"`; update the shared `Cargo.lock`. Keep `#![forbid(unsafe_code)]`.

In `frame.rs`, define private constants `MAX_PARTICLE_COUNT: usize = 512`, `MAX_RIGID_SEGMENTS: usize = 16`, `MAX_RIGID_CIRCLES: usize = 8`, strides 2/4/1/4/3, a typed `FrameError`, and native `FrameData` owning `step_index: u32`, `Vec<f32>` positions/radii/segments/circles, and `Vec<u8>` colors. Its fallible constructor must use checked multiplication, exact lane lengths, finite checks, positive-radius checks, and rigid bounds; it must never clamp malformed data. Define one `#[wasm_bindgen] pub struct ProofFrame` around validated owned data. Export `stepIndex`, `particleCount`, `rigidShapeCount`, `particlePositions`, `particleColors`, `particleRadii`, `rigidSegments`, and `rigidCircles`; the five array methods return cloned `Box<[f32]>` or `Box<[u8]>`, producing JavaScript-owned copies. Add focused Arrange/Act/Assert unit tests in `frame.rs`. Do not add serde, raw pointer access, a public engine change, a stable npm API promise, or generated output to source control.</action>
  <verify>
    <automated>cargo test -p liquidfun-wasm frame -- --nocapture &amp;&amp; cargo clippy -p liquidfun-wasm --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `"crates/liquidfun-wasm"` in `members` and still contains exactly `default-members = ["crates/liquidfun"]`.
    - `crates/liquidfun-wasm/Cargo.toml` contains `publish = false`, `crate-type = ["cdylib", "rlib"]`, and `wasm-bindgen = "=0.2.128"`.
    - `rg "MAX_PARTICLE_COUNT: usize = 512|MAX_RIGID_SEGMENTS: usize = 16|MAX_RIGID_CIRCLES: usize = 8" crates/liquidfun-wasm/src/frame.rs` finds all three hard bounds.
    - `rg "Box&lt;\\[(f32|u8)\\]&gt;" crates/liquidfun-wasm/src/frame.rs` finds the five copied-array return paths, while `rg "unsafe|\\*const|\\*mut|WebAssembly::Memory|js_sys::WebAssembly" crates/liquidfun-wasm` finds no implementation occurrence.
    - The focused frame tests and deny-warnings wrapper Clippy command exit 0.
  </acceptance_criteria>
  <done>The private non-default wrapper and bounded owned frame ABI compile natively, reject malformed frame state, and expose no live memory or engine identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the persistent proof scene and opaque session</name>
  <files>crates/liquidfun-wasm/src/lib.rs, crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/session.rs</files>
  <read_first>crates/liquidfun-wasm/src/frame.rs; crates/liquidfun/src/lib.rs; crates/liquidfun/src/world/config.rs; crates/liquidfun/src/world/body.rs; crates/liquidfun/src/world/fixture.rs; crates/liquidfun/src/world/particle_object/system.rs; crates/liquidfun/src/world/particle_object/particle.rs; crates/liquidfun/src/particle/definition/system_definition.rs; crates/liquidfun/src/particle/definition/particle.rs; crates/liquidfun/src/particle/view.rs; crates/liquidfun/tests/rigid_contact_solver.rs; 16-CONTEXT.md D-01, D-03, D-04, D-06, D-07, D-12, D-14, D-15; 16-RESEARCH.md Proof Scene Specification</read_first>
  <behavior>
    - "Construction creates gravity `(0.0, -10.0)`, three static rectangular basin fixtures, one positive-density dynamic circle, one particle system capped at 512, and exactly 192 explicitly colored water particles."
    - "`advance` accepts only `1..=4`, calls ordinary `World::step` at `1.0 / 60.0` with velocity/position/particle iterations `8/3/2`, and increments `step_index` with checked arithmetic."
    - "A captured frame has finite aligned lanes and bounded rigid geometry; after four steps at least one particle position or the dynamic circle center changes by a finite nonzero amount."
    - "The exported opaque session exposes checked construction, bounded `advance`, one bulk `captureFrame`, coarse step/count accessors, and generated explicit `free()` ownership only."
  </behavior>
  <action>Per D-01, D-03, D-04, D-06, D-07, D-12, D-14, and D-15, create a native `SessionCore` that exclusively owns `World`, `ParticleSystemId`, one private dynamic-circle `BodyId`, constant basin segment descriptors, the circle radius, `StepConfiguration`, and checked `step_index`. Build an argument-free scene with world bounds `x = -6.0..6.0`, `y = -1.0..8.0`, gravity `Vec2::new(0.0, -10.0)`, floor polygon vertices `(-6,-1),(6,-1),(6,0),(-6,0)`, left wall `(-6,0),(-5.5,0),(-5.5,8),(-6,8)`, right wall `(5.5,0),(6,0),(6,8),(5.5,8)`, and a dynamic local-zero circle of radius `0.75` centered initially at `(2.5, 5.5)` with density `1.0`, friction `0.2`, and restitution `0.0`. Use checked `BodyDef`, `PolygonShape`, `CircleShape`, `FixtureDef`, and `FilterData::default()` APIs.

Create one `ParticleSystemDef::default().with_radius(0.2)?.with_maximum_count(512)?`; insert a source-ordered 16-column by 12-row grid beginning at `(-4.7, 0.4)` with spacing `0.32`, `ParticleFlags::WATER`, and `ParticleColor::new(57, 211, 199, 255)`. Use `StepConfiguration::new(1.0 / 60.0, 8, 3)?.with_particle_iterations(2)?`, `NoDecisionHook`, `StepLimits::default()`, and ordinary `World::step`; never call `step_profiled`, `Instant`, `catch_unwind`, C++, protocol, catalog, differential, benchmark, or testbed code. Define `MAX_ADVANCE_STEPS: u32 = 4`; reject zero/five and checked step-index overflow before stepping.

`capture_frame()` must borrow `ParticleSystemView` once, verify IDs/positions/colors alignment and the 512 cap, copy x/y and RGBA in source order, fill one radius per particle, end the borrow, then append the three inner basin segments `(-5.5,0)-(5.5,0)`, `(-5.5,0)-(-5.5,8)`, `(5.5,0)-(5.5,8)` and the current dynamic circle from `body_snapshot().position()`. Pass all lanes through `FrameData` validation. In `lib.rs`, expose only `#[wasm_bindgen] pub struct ProofSession` with constructor `new() -> Result<ProofSession, JsError>`, `advance(step_count: u32) -> Result<(), JsError>`, `captureFrame() -> Result<ProofFrame, JsError>`, `stepIndex()`, `particleCount()`, and `rigidShapeCount()`; generated `free()` is the explicit disposal operation. Map typed internal errors to fixed bounded `JsError` messages at the shell. Add focused native Arrange/Act/Assert tests for construction, advance bounds/no-effect, frame invariants, and real movement. Do not change `crates/liquidfun` unless compilation proves an unavoidable missing public operation; if that occurs, stop and replan instead of speculatively widening the engine.</action>
  <verify>
    <automated>cargo test -p liquidfun-wasm &amp;&amp; cargo clippy -p liquidfun-wasm --all-targets -- -D warnings &amp;&amp; cargo build -p liquidfun-wasm</automated>
  </verify>
  <acceptance_criteria>
    - `rg "MAX_ADVANCE_STEPS: u32 = 4|with_particle_iterations\\(2\\)|1\\.0 / 60\\.0|16|12|ParticleColor::new\\(57, 211, 199, 255\\)" crates/liquidfun-wasm/src` finds the fixed bounded scene and step contract.
    - `rg "step_profiled|Instant::|catch_unwind|liquidfun_test_protocol|liquidfun_differential|liquidfun_testbed" crates/liquidfun-wasm` returns no implementation match.
    - Native tests prove exact 192 particle count, three rigid segments, one rigid circle, rejection of step counts 0 and 5, finite aligned frame lanes, and changed Rust state after four steps.
    - `cargo tree -p liquidfun-wasm --edges normal` contains `liquidfun` and exact `wasm-bindgen v0.2.128`, and contains no C++, renderer, protocol, differential, benchmark, or testbed package.
    - All focused test, Clippy, and native build commands exit 0 without modifying `crates/liquidfun`.
  </acceptance_criteria>
  <done>A persistent bounded native engine scene advances and captures coherent copied frames behind one opaque wasm-bindgen owner.</done>
</task>

<task type="auto">
  <name>Task 3: Prove exact-tool real WASM generation</name>
  <files>target/phase16/plan-01/pkg/</files>
  <read_first>rust-toolchain.toml; Cargo.toml; crates/liquidfun-wasm/Cargo.toml; crates/liquidfun-wasm/src/lib.rs; .gitignore; 16-RESEARCH.md Standard Stack, Contributor Preparation, Environment Availability, and Pitfall 1; PROJECT-SCOPE.md; standards/core/verification.md</read_first>
  <action>Prepare the missing exact tools before claiming compatibility: run `rustup target add wasm32-unknown-unknown --toolchain 1.97.0`; if `wasm-pack --version` is not exactly `wasm-pack 0.15.0`, run `cargo install wasm-pack --version 0.15.0 --locked --force`, then verify the exact output. Remove only the existing ignored `target/phase16/plan-01/pkg` directory and run `wasm-pack build crates/liquidfun-wasm --target web --release --out-dir ../../target/phase16/plan-01/pkg --out-name liquidfun_wasm`. Inspect the generated package for `liquidfun_wasm.js`, `liquidfun_wasm.d.ts`, `liquidfun_wasm_bg.wasm`, and `package.json`; confirm declarations contain `ProofSession`, `ProofFrame`, typed-array return types, and generated `free(): void`. Run a static ES-module import/initialization probe only if the generated package provides a Node-compatible path; do not mislabel Node import or compilation as browser execution because Plan 16-03 owns real Chromium. Preserve failure output under `target/phase16/plan-01/` and diagnose target/compiler errors before proceeding. Do not install or invoke CMake, C++, the upstream checkout, a JS physics package, or publication commands.</action>
  <verify>
    <automated>rustup target list --installed --toolchain 1.97.0 | rg '^wasm32-unknown-unknown$' &amp;&amp; test "$(wasm-pack --version)" = "wasm-pack 0.15.0" &amp;&amp; wasm-pack build crates/liquidfun-wasm --target web --release --out-dir ../../target/phase16/plan-01/pkg --out-name liquidfun_wasm &amp;&amp; test -f target/phase16/plan-01/pkg/liquidfun_wasm_bg.wasm</automated>
  </verify>
  <acceptance_criteria>
    - Rust 1.97.0 reports `wasm32-unknown-unknown` installed and `wasm-pack --version` prints exactly `wasm-pack 0.15.0`.
    - The exact wasm-pack command exits 0 and creates all four expected generated package files beneath ignored `target/phase16/plan-01/pkg/`.
    - `target/phase16/plan-01/pkg/liquidfun_wasm.d.ts` contains `ProofSession`, `ProofFrame`, `free(): void`, `Float32Array`, and `Uint8Array`.
    - `git status --short -- target/phase16/plan-01/pkg` is empty because generated output is ignored.
    - No browser-runtime success claim is recorded until Plan 16-03 executes Chromium.
  </acceptance_criteria>
  <done>The exact Rust target and wasm-pack tool produce a real browser-targeted package from the current wrapper source, with generated disposal and typed-array declarations confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| JavaScript numeric call to Rust session | Untrusted JS values become bounded step requests at the wasm-bindgen edge. |
| Rust world to exported frame | Borrow-scoped engine state is copied into bounded owned arrays. |
| Registry/toolchain to generated package | Exact crates and tools produce executable glue and WASM bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-16-01-01 | D | `ProofSession::advance` and `FrameData` allocation | mitigate | Accept only 1 through 4 steps, cap particles at 512, segments at 16 and circles at 8, use checked multiplication, and test rejected bounds before allocation or stepping. |
| T-16-01-02 | T/D | Scene and frame geometry | mitigate | Construct geometry only through checked engine APIs and reject every non-finite position/radius plus non-positive radius before export; never clamp silently. |
| T-16-01-03 | D | Exported session/frame lifetime | mitigate | Export opaque wasm-bindgen classes only, expose no raw handles/pointers, rely on generated `free()`, and leave exactly-once owner enforcement to the typed adapter in Plan 16-02. |
| T-16-01-04 | T/D | WASM memory growth versus JavaScript arrays | mitigate | Return cloned boxed numeric slices only, which wasm-bindgen copies into JavaScript-owned typed arrays; forbid memory and pointer exports. |
| T-16-01-05 | T | Dependency and build integrity | mitigate | Pin wasm-bindgen `=0.2.128`, Rust `1.97.0`, target identity, and wasm-pack `0.15.0`; commit Cargo.lock and generate from the current checkout into ignored output. |
</threat_model>

<verification>
Run `cargo fmt --all --check`, `cargo test -p liquidfun-wasm`, `cargo clippy -p liquidfun-wasm --all-targets -- -D warnings`, `cargo build -p liquidfun-wasm`, the exact wasm-pack generation command, `cargo build -p liquidfun`, and plain `cargo build`. Confirm `default-members = ["crates/liquidfun"]`, generated output is ignored, the wrapper dependency tree points outward only, and no public engine file changed.
</verification>

<success_criteria>
- The native-testable wrapper constructs 192 Rust particles plus basin and dynamic rigid geometry, then proves state movement after bounded fixed steps.
- The five frame arrays have exact documented element types/strides, are finite and bounded, and cross the ABI as owned copies without pointers or engine handles.
- Rust 1.97.0 plus wasm-pack 0.15.0 produces a real `--target web` package while ordinary native Cargo defaults remain unchanged.
- D-01, D-03 through D-08, D-12, D-14, and D-15 are implemented at this slice without pulling in frontend, catalog, player, deployment, or publication scope.
</success_criteria>

<output>
After completion, create `.planning/phases/16-rust-wasm-browser-bridge/16-01-SUMMARY.md`
</output>
