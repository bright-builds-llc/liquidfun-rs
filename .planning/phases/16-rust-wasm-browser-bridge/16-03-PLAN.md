---
phase: 16-rust-wasm-browser-bridge
plan: "03"
type: execute
wave: 3
depends_on: [16-02]
files_modified:
  - justfile
  - scripts/web-build.ts
  - web/playwright.config.ts
  - web/src/main.tsx
  - web/src/App.tsx
  - web/src/app.css
  - web/e2e/rust-wasm-proof.spec.ts
  - README.md
  - TESTING.md
  - .planning/phases/16-rust-wasm-browser-bridge/16-REVIEW.md
autonomous: true
requirements: [WASM-01, WASM-02, WASM-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 16-2026-09-17T01-48-41
generated_at: 2026-09-17T02:08:07Z
must_haves:
  truths:
    - "A real package-pinned Chromium instance initializes generated Rust WASM, draws its first valid frame, and visibly advances later Rust-produced frame state."
    - "The proof page visibly distinguishes loading, running, failure, and disposed states and identifies the runtime as Rust engine plus WebAssembly."
    - "Disposal cancels animation before freeing the single session, preserves the last frame, and prevents all later steps, captures, and draws."
    - "Documented exact commands rebuild and smoke-test the proof while native Cargo and the packaged liquidfun crate remain browser-tool independent."
    - "A separate identified reviewer acknowledges the complete implementation and evidence at an exact non-self-referential digest."
  artifacts:
    - path: web/src/App.tsx
      provides: "SolidJS proof lifecycle, semantic status/count DOM, bounded RAF stepping, and explicit disposal"
    - path: web/src/app.css
      provides: "Approved dark single-panel visual contract and narrow-width containment"
    - path: web/e2e/rust-wasm-proof.spec.ts
      provides: "Real Chromium initialization, movement, visible Canvas, and disposal proof"
    - path: README.md
      provides: "Pinned clean-checkout browser proof build instructions"
    - path: TESTING.md
      provides: "Focused native/web/Chromium/isolation verification contract"
    - path: .planning/phases/16-rust-wasm-browser-bridge/16-REVIEW.md
      provides: "Independent exact-digest implementation and evidence acknowledgment"
  key_links:
    - from: web/src/App.tsx
      to: web/src/physics/loader.ts
      via: "onMount awaits generated initializer and constructs the only session"
      pattern: "loadProofSession"
    - from: web/src/App.tsx
      to: web/src/render/canvas.ts
      via: "validated Rust frame is drawn before status changes to running"
      pattern: "drawRenderFrame"
    - from: web/e2e/rust-wasm-proof.spec.ts
      to: web/src/App.tsx
      via: "DOM step/moved instrumentation plus Canvas screenshots and Dispose session action"
      pattern: "Dispose session"
    - from: scripts/web-build.ts
      to: web/playwright.config.ts
      via: "smoke mode rebuilds then runs the local package-pinned Chromium project"
      pattern: "test:browser"
    - from: .planning/phases/16-rust-wasm-browser-bridge/16-REVIEW.md
      to: "complete Phase 16 implementation content set"
      via: "sorted path-NUL-file-hash-LF aggregate SHA-256 acknowledged by a separate reviewer"
      pattern: "review_digest"
---

<objective>
Assemble the approved minimal page, prove real Rust-produced visible motion and explicit disposal in Chromium, document the reproducible workflow, verify native/package isolation, and obtain exact-digest independent review.

Purpose: Close Phase 16 on executable browser evidence rather than compile-only or mock success while preserving all later player/gallery/deployment boundaries.
Output: The running local SolidJS/Canvas proof, one focused Chromium test, contributor/testing documentation, complete verification evidence, and an independent review record.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@PROJECT-SCOPE.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/frontend-ui.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@standards/languages/typescript-javascript.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-rust-wasm-browser-bridge/16-CONTEXT.md
@.planning/phases/16-rust-wasm-browser-bridge/16-RESEARCH.md
@.planning/phases/16-rust-wasm-browser-bridge/16-UI-SPEC.md
@.planning/phases/16-rust-wasm-browser-bridge/16-01-SUMMARY.md
@.planning/phases/16-rust-wasm-browser-bridge/16-02-SUMMARY.md
@web/src/physics/frame.ts
@web/src/physics/loader.ts
@web/src/physics/session.ts
@web/src/render/camera.ts
@web/src/render/canvas.ts
@RELEASE.md

<interfaces>
Plan 16-02 supplies:

```typescript
export type RenderFrame = {
  readonly stepIndex: number;
  readonly particleCount: number;
  readonly rigidShapeCount: number;
  readonly particlePositions: Float32Array;
  readonly particleColors: Uint8Array;
  readonly particleRadii: Float32Array;
  readonly rigidSegments: Float32Array;
  readonly rigidCircles: Float32Array;
};

export type SceneSession = {
  nextFrame(): RenderFrame;
  dispose(): void;
};

export async function loadProofSession(): Promise<SceneSession>;
export function drawRenderFrame(
  context: CanvasRenderingContext2D,
  frame: RenderFrame,
  camera: Camera,
): void;
```

Use the actual exported spelling from Plan 16-02. Keep App as an imperative lifecycle shell; it must not reinterpret frame values or create per-particle reactive state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the approved SolidJS proof page and lifecycle</name>
  <files>web/src/main.tsx, web/src/App.tsx, web/src/app.css</files>
  <read_first>16-UI-SPEC.md in full; 16-CONTEXT.md D-01, D-02, D-03, D-11, D-13, D-15; 16-RESEARCH.md Pattern 5 and Browser Proof Contract; web/src/physics/loader.ts; web/src/physics/session.ts; web/src/render/camera.ts; web/src/render/canvas.ts; web/index.html; standards/core/frontend-ui.md; standards/languages/typescript-javascript.md</read_first>
  <action>Implement the approved `16-UI-SPEC.md` exactly. `main.tsx` mounts one `App` into the existing root and imports only `app.css`. In `App.tsx`, model `loading`, `running`, `failure`, and `disposed` as a discriminated union so invalid status/count/error combinations are not representable. Render semantic `<main aria-labelledby="proof-title">`, header, exact single h1 `LiquidFun Rust/WASM browser proof`, exact summary copy, one `<section aria-labelledby="session-status-title">`, exact h2 `Session status`, atomic polite status output, visible fixed runtime `<dl>` value `Rust engine · WebAssembly`, particle and rigid counts, `<figure>`, non-focusable Canvas with `role="img"`, exact accessible name and fallback text, visible exact figcaption, and one native `<button type="button">Dispose session</button>`. Use the UI-SPEC exact loading/running/failure/disposed, empty, count, and error strings through text nodes; never use `innerHTML` or raw exception HTML.

On mount, clear/size the 960x540-design Canvas, await `loadProofSession()`, request one validated Rust frame, draw it, and only then transition from Loading to Running. Keep one session owner and one pending animation-frame ID. While Running, request one animation frame, call exactly one `nextFrame()` (therefore one bounded Rust step), draw it, update coarse DOM counts and proof-only `data-wasm-initialized="true"`, `data-step-index`, and `data-moved-frame-count` attributes derived from actual consecutive Rust frame positions/circle values, then schedule the next frame. Browser timestamps schedule only; do not pass elapsed time to Rust, accumulate catch-up, interpolate, integrate, or script positions. On any load/step/capture/parse/draw error, cancel animation, dispose once when an owner exists, retain the last successful counts/frame, set the fixed Failure copy plus a bounded escaped `Details:` text only in development, and do not retry. Dispose activation cancels animation first, calls owner `dispose()` once, retains the last frame/counts, sets Disposed, and performs no further step/capture/draw. Register Solid cleanup that uses the same idempotent cancel/dispose function.

Create scoped page CSS with exactly the approved system font, four sizes (14/16/20/28), weights 400/600, colors, 4/8/16/24/32/48/64 spacing scale, 960px centered column, 32px desktop inline and 48px block padding, one `#151D28` 12px-radius 1px-border panel with 24px padding, 16:9 full-width Canvas, tabular counts, destructive button colors, 44px minimum button block size, and 2px `#39D3C7` focus-visible outline at 4px offset. At `max-width: 480px`, use 16px page/panel padding/gaps, stack metadata/counts, make the button full width, and preserve no horizontal overflow at 320px. Add no transition, animation, gradient, glow, glass, navbar, route, card, footer, source/version/build chrome, retry/reset/play/pause control, canvas focus, pointer/touch handler, component library, or responsive behavior beyond the approved containment minimum.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck &amp;&amp; bun run test:unit &amp;&amp; bun run build:app</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/App.tsx` contains all exact UI-SPEC copy strings, the required semantic elements/ARIA attributes, one `Dispose session` button, and no `innerHTML`.
    - Running is assigned only after generated initialization, session construction, frame validation, and `drawRenderFrame` complete.
    - One RAF iteration calls one `nextFrame()` and has no elapsed-time, catch-up, interpolation, integration, setInterval, or per-particle Solid signal/component path.
    - Failure and disposal both cancel RAF before idempotent owner disposal; Disposed preserves the last frame/counts and schedules no new draw.
    - `web/src/app.css` contains the exact UI-SPEC palette, dimensions, focus ring, 44px target, 960px column, 16:9 Canvas, and 480px breakpoint, with no gradients/transitions/animations.
    - Typecheck, all unit tests, and production build exit 0.
  </acceptance_criteria>
  <done>The single approved dark page reaches Running only from a real first Rust frame, visibly draws ongoing Rust state, and exposes a safe explicit disposal action.</done>
</task>

<task type="auto">
  <name>Task 2: Prove real Chromium initialization, motion, pixels, and disposal</name>
  <files>justfile, scripts/web-build.ts, web/playwright.config.ts, web/e2e/rust-wasm-proof.spec.ts</files>
  <read_first>web/package.json; web/bun.lock; web/src/App.tsx; web/src/physics/loader.ts; web/src/physics/session.ts; web/src/render/canvas.ts; scripts/web-build.ts; justfile; 16-CONTEXT.md D-01, D-12, D-13, D-14, D-15; 16-RESEARCH.md Browser Proof Contract, Build and Isolation Gates, Pitfall 1; 16-UI-SPEC.md State and Interaction Contract; standards/core/verification.md</read_first>
  <action>Per D-01, D-12, D-13, D-14, and D-15, create `web/playwright.config.ts` with exactly one package-pinned Chromium project, `testDir: "./e2e"`, no retries for deterministic physics, bounded 30-second test timeout, `use.baseURL = "http://127.0.0.1:4173"`, trace/screenshot retained only on failure, and `webServer.command = "bun run preview -- --strictPort"` using the already-built `web/dist` on host `127.0.0.1`, port 4173, with server reuse disabled in CI. Before smoke, run the local exact package script `cd web &amp;&amp; bun run browser:install`; this must install the Chromium revision required by `@playwright/test` 1.63.0, not a floating system browser.

Add one `rust-wasm-proof.spec.ts` test. Navigate to `/`; assert exact Loading identity is present initially or as the first observable state; poll the fixed status to exact `Running Rust/WASM session`; assert `data-wasm-initialized="true"`, numeric particle count 192, rigid count 4, positive step index, and moved-frame count greater than zero. Capture the Canvas screenshot and step index, poll until a later step index and moved count, capture again, and require byte/pixel difference so hidden numeric movement alone cannot pass. Assert the runtime text `Rust engine · WebAssembly` and Canvas accessible name. Click the native `Dispose session` button, assert exact Disposed status and disabled button, record final step/moved values and Canvas screenshot, wait/poll for at least two animation opportunities, and assert step/moved values and Canvas pixels remain unchanged. The test must import no fake frame, mock server, JS physics engine, browser memory view, or generated stub.

Extend `scripts/web-build.ts` with a `smoke` mode that first executes the complete existing build mode, verifies local Playwright package version 1.63.0, runs `bun run browser:install`, then `bun run test:browser`; preserve breadcrumb logs and status propagation under `target/web-build/`. Add transparent `web-smoke: bun scripts/web-build.ts smoke` to `justfile`. Run `just web-smoke` against the generated current-checkout package. If Chromium reveals a trap, malformed frame, loader mismatch, or blank Canvas, preserve failure evidence and fix the responsible Rust/TS boundary with affected native/unit/build reruns; do not replace physics, weaken assertions, add automatic retry, or claim a compile-only pass.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run browser:install &amp;&amp; cd .. &amp;&amp; just web-smoke</automated>
  </verify>
  <acceptance_criteria>
    - `web/playwright.config.ts` defines one Chromium project, local built preview on `127.0.0.1:4173`, 30-second timeout, and zero deterministic retries.
    - The smoke uses package-pinned Playwright 1.63.0 and its matching installed Chromium; it does not select a system Chrome channel.
    - The single test proves generated WASM initialization, 192 Rust particles, four rigid shapes, increasing Rust step index, moved-frame count above zero, and differing Canvas screenshots.
    - After clicking `Dispose session`, exact Disposed copy appears, the button is disabled, and step index, moved count, and Canvas screenshot remain unchanged.
    - `just web-smoke` rebuilds regenerated WASM before launching Chromium and exits 0 without tracked generated/test output.
    - No broad browser matrix, route, Pages base path, deployment, navigation, play/pause/reset/retry, interaction, or six-scene coverage is added.
  </acceptance_criteria>
  <done>Real Chromium visibly runs and advances this repository's generated Rust WASM frame, then demonstrates explicit leak-free session disposal.</done>
</task>

<task type="auto">
  <name>Task 3: Document, verify isolation, and obtain independent exact-digest review</name>
  <files>README.md, TESTING.md, .planning/phases/16-rust-wasm-browser-bridge/16-REVIEW.md</files>
  <read_first>AGENTS.md hobby scope, standing authorization, and Independent review; PROJECT-SCOPE.md; README.md; TESTING.md; RELEASE.md publication boundary; Cargo.toml; crates/liquidfun/Cargo.toml; justfile; scripts/web-build.ts; web/package.json; all Phase 16 implementation files and generated evidence; 16-CONTEXT.md D-08 through D-15; 16-RESEARCH.md Contributor Preparation and Build and Isolation Gates; standards/core/frontend-ui.md; standards/core/verification.md</read_first>
  <action>Per D-08 through D-10 and D-12 through D-15, add a concise `Web browser proof` section to README.md and a focused Phase 16 browser-proof section to TESTING.md. Document prerequisites and exact clean-checkout commands: `rustup target add wasm32-unknown-unknown --toolchain 1.97.0`, `cargo install wasm-pack --version 0.15.0 --locked`, `curl -fsSL https://bun.com/install | bash -s "bun-v1.4.2"`, `cd web &amp;&amp; bun install --frozen-lockfile`, `cd .. &amp;&amp; just web-build`, and `just web-smoke`. State that `web/src/generated/liquidfun-wasm`, `web/dist`, Playwright output, and `target/web-build` are ignored/regenerated; the wrapper is unpublished/private; the proof uses Rust engine frames and Canvas 2D; plain Cargo/native packaged consumers require none of Bun, browser, wasm-pack, C++, or upstream; and Phase 17 owns the shared player, retry/reset, routing, Pages, and reusable lifecycle. Do not add release, npm publish, crate publish, deployment, URL, or parity claims. Run `just markdown-check` after these non-GSD Markdown edits; never format `.planning/**` with mdformat.

Run and retain actual results under a fresh ignored `target/phase16/closure-attempt-N/`: `cargo fmt --all --check`; `cargo test -p liquidfun-wasm`; `cargo clippy -p liquidfun-wasm --all-targets -- -D warnings`; exact `just web-build`; exact `just web-smoke`; `cargo build -p liquidfun`; `cargo test -p liquidfun --all-features`; plain `cargo build`; `cargo xtask package verify`; `just check`; `just markdown-check`; `bun scripts/bright-builds-check.ts all`; and `git diff --check`. Run `cargo tree -p liquidfun --edges normal` and Cargo metadata checks proving `liquidfun` remains the sole default member, has no wasm-bindgen/web dependency, and its packaged archive excludes `crates/liquidfun-wasm`, `web/`, generated JS/declarations/WASM, browsers, C++, protocol, differential, benchmark, and testbed artifacts. Preserve failed logs and correct scoped defects before rerunning; optional Linux/C++, sanitizer, fuzz, coverage, performance, and release qualification are not completion gates.

After implementation and all evidence are stable, compute a non-self-referential review digest over the complete implementation diff and selected command/Chromium/isolation evidence. Use sorted UTF-8 repository-relative paths, NUL, lowercase SHA-256 of each file's bytes, and LF; hash the concatenation with SHA-256. Exclude `16-REVIEW.md`, plan summaries, final verification, and other mutable acknowledgments. Have a separate identified AI or human reviewer—not the implementing agent—inspect the complete relevant diff, UI-SPEC compliance, generated declaration shape, browser evidence, dependency/lock integrity, native/package isolation, and all five security threats. Write `.planning/phases/16-rust-wasm-browser-bridge/16-REVIEW.md` with actual reviewer identity, AI/human disclosure, actual review time, source commit or exact working-tree base/head, fixed manifest entries, aggregate `review_digest`, findings and resolutions, and explicit acknowledgment of that digest. If implementation changes after review, rerun affected gates and obtain a new digest acknowledgment. The review grants neither package publication nor release authority.</action>
  <verify>
    <automated>just web-smoke &amp;&amp; cargo build &amp;&amp; cargo xtask package verify &amp;&amp; just markdown-check &amp;&amp; bun scripts/bright-builds-check.ts all &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - README.md and TESTING.md contain the exact Rust target, wasm-pack 0.15.0, Bun 1.4.2, frozen install, `just web-build`, and `just web-smoke` commands plus generated-output ownership.
    - Every listed native/wrapper/web/browser/package/aggregate/Markdown/managed/diff command has a retained actual successful result for the reviewed source; failures are preserved rather than overwritten.
    - Root `Cargo.toml` still contains exactly `default-members = ["crates/liquidfun"]`; `cargo tree -p liquidfun --edges normal` contains no wasm-bindgen, Solid, Vite, Playwright, renderer, protocol, differential, benchmark, or testbed dependency.
    - `cargo xtask package verify` exits 0 with no browser wrapper, web source, generated binding, WASM, Bun, or Chromium file in the liquidfun archive.
    - `16-REVIEW.md` identifies a separate actual reviewer and time, lists the fixed content/evidence manifest, records the exact aggregate SHA-256, and contains an explicit acknowledgment of that same digest with zero unresolved high-severity finding.
    - No package is published, no version/tag/release is selected, no deployment runs, and no strict qualification result is claimed.
  </acceptance_criteria>
  <done>The phase has reproducible contributor instructions, proportional passing evidence, proven native/package isolation, and a separate exact-digest review without publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Browser scheduler and DOM to WASM owner | Browser effects drive one bounded call at a time and can release the owner. |
| Validated Rust frame to visible Canvas | Numeric movement must become visible pixels without JavaScript physics substitution. |
| Local lockfiles/tools/evidence to completion claims | Executable dependencies and retained results support the final source-bound claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-16-03-01 | D | RAF stepping, frame allocation, and Canvas backing store | mitigate | One step per RAF, Rust step bound 1 through 4, frame caps 512/16/8, DPR cap 2, no catch-up loop, and Chromium asserts bounded counts. |
| T-16-03-02 | T/D | Rust/TS/Canvas geometry | mitigate | Rust checked construction plus TS finite/stride/radius parser gates all geometry; browser failure stops the session and no JavaScript integration or silent clamp exists. |
| T-16-03-03 | D | Dispose/failure/unmount lifecycle | mitigate | One TS owner, cancel RAF before idempotent free, free temporary frames in finally, poison on error, preserve last frame, and assert no post-dispose step/draw in Chromium. |
| T-16-03-04 | T/D | Copied array ownership during WASM memory growth | mitigate | Boxed-slice JavaScript-owned arrays survive frame.free/WASM growth; review declarations/source for no raw Memory, buffer, pointer, or zero-copy path. |
| T-16-03-05 | T | Dependency, browser, build, and evidence integrity | mitigate | Exact Cargo/Bun/tool pins and lockfiles, package-pinned Chromium, regenerate-before-build/smoke, ignored outputs, native/package isolation checks, and separate exact-digest review. |
</threat_model>

<verification>
Execute the complete closure command list from Task 3 on the stable implementation, verify the approved UI copy/layout statically, inspect the successful Chromium trace/screenshots and generated declarations, confirm all outputs are ignored, and confirm the independent acknowledgment binds the exact reviewed digest. Do not substitute optional strict qualification or a compile-only WASM result for the real browser proof.
</verification>

<success_criteria>
- A real Chromium browser reaches Running only after generated Rust WASM initialization and a successful first Canvas draw, then observes advancing Rust state and changing pixels.
- The exact approved loading/running/failure/disposed page and dark single-panel Canvas contract are implemented without Phase 17–19 features.
- Explicit disposal frees the session once, freezes step/draw observables, and is verified in unit and browser tests.
- The pinned clean-checkout workflow builds through one repo-owned command; native default and packaged consumers remain independent.
- A separate reviewer acknowledges the complete stable implementation/evidence digest with no unresolved high-severity finding.
- WASM-01, WASM-02, and WASM-03 are fully evidenced; no package, release, or deployment action occurs.
</success_criteria>

<output>
After completion, create `.planning/phases/16-rust-wasm-browser-bridge/16-03-SUMMARY.md`
</output>
