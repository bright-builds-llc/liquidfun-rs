---
phase: 16-rust-wasm-browser-bridge
plan: "03"
type: execute
wave: 3
depends_on: [16-02]
files_modified:
  - web/src/render/camera.ts
  - web/src/render/canvas.ts
  - web/tests/camera.test.ts
  - web/src/main.tsx
  - web/src/App.tsx
  - web/src/app.css
autonomous: true
requirements: [WASM-01, WASM-02, WASM-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 16-2026-09-17T01-48-41
generated_at: 2026-09-17T02:08:07Z
must_haves:
  truths:
    - "Pure projection preserves aspect ratio, applies the approved 16px inset, and inverts only the world y-axis."
    - "Canvas drawing consumes one validated bulk frame and follows the approved color, backing-store, and draw-order contract."
    - "The proof page visibly distinguishes loading, running, failure, and disposed states and identifies the runtime as Rust engine plus WebAssembly."
    - "Disposal cancels animation before freeing the single session, preserves the last frame, and prevents all later steps, captures, and draws."
    - "The first complete frontend build runs only after the tested renderer and main application entrypoints exist."
  artifacts:
    - path: web/src/render/camera.ts
      provides: "Pure fixed-world Canvas projection"
    - path: web/src/render/canvas.ts
      provides: "Imperative bulk-array Canvas 2D renderer"
    - path: web/tests/camera.test.ts
      provides: "Focused projection and viewport validation"
    - path: web/src/App.tsx
      provides: "SolidJS proof lifecycle, semantic status/count DOM, bounded RAF stepping, and explicit disposal"
    - path: web/src/app.css
      provides: "Approved dark single-panel visual contract and narrow-width containment"
  key_links:
    - from: web/src/render/canvas.ts
      to: web/src/physics/frame.ts
      via: "one RenderFrame input with bulk arrays, not Solid particle components"
      pattern: "RenderFrame"
    - from: web/src/App.tsx
      to: web/src/physics/loader.ts
      via: "onMount awaits generated initializer and constructs the only session"
      pattern: "loadProofSession"
    - from: web/src/App.tsx
      to: web/src/render/canvas.ts
      via: "validated Rust frame is drawn before status changes to running"
      pattern: "drawRenderFrame"
    - from: scripts/web-build.ts
      to: web/src/main.tsx
      via: "complete build mode typechecks, runs all unit tests, and bundles the now-reachable app entrypoint"
      pattern: "build:app"
---

<objective>
Implement the pure Canvas rendering core and assemble the approved minimal SolidJS proof page, then run the first complete frontend build.

Purpose: Make the generated typed frame visibly reachable through the approved page while preserving a pure projection core and keeping browser closure evidence in the following plan.
Output: Tested camera/rendering modules, the running local SolidJS/Canvas proof, and a complete production bundle from the current generated WASM package.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@PROJECT-SCOPE.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/frontend-ui.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@standards/languages/typescript-javascript.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-rust-wasm-browser-bridge/16-CONTEXT.md
@.planning/phases/16-rust-wasm-browser-bridge/16-RESEARCH.md
@.planning/phases/16-rust-wasm-browser-bridge/16-UI-SPEC.md
@.planning/phases/16-rust-wasm-browser-bridge/16-01-SUMMARY.md
@.planning/phases/16-rust-wasm-browser-bridge/16-02-SUMMARY.md
@web/src/physics/frame.ts
@web/src/physics/loader.ts
@web/src/physics/session.ts
@web/src/render/camera.ts
@web/src/render/canvas.ts

<interfaces>
Plan 16-02 supplies:

```typescript
export type RenderFrame = {
  readonly stepIndex: number;
  readonly particleCount: number;
  readonly rigidShapeCount: number;
  readonly particlePositions: Float32Array;
  readonly particleColors: Uint8Array;
  readonly particleRadii: Float32Array;
  readonly rigidSegments: Float32Array;
  readonly rigidCircles: Float32Array;
};

export type SceneSession = {
  nextFrame(): RenderFrame;
  dispose(): void;
};

export async function loadProofSession(): Promise<SceneSession>;
export function drawRenderFrame(
  context: CanvasRenderingContext2D,
  frame: RenderFrame,
  camera: Camera,
): void;
```

Use the actual exported spelling from Plan 16-02. Keep App as an imperative lifecycle shell; it must not reinterpret frame values or create per-particle reactive state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pure projection and bulk Canvas drawing</name>
  <files>web/src/render/camera.ts, web/src/render/canvas.ts, web/tests/camera.test.ts</files>
  <read_first>web/src/physics/frame.ts; web/tsconfig.json; 16-UI-SPEC.md Viewport Contract and Color; 16-CONTEXT.md D-02, D-03, D-05, D-11, D-12, D-15; 16-RESEARCH.md Pattern 5; standards/core/architecture.md; standards/core/testing.md</read_first>
  <behavior>
    - "Projection fits world bounds `(-6,-1)` through `(6,8)` into any positive viewport with a 16px inset, preserves aspect ratio, centers unused space, and maps greater world y to smaller canvas y."
    - "Backing-store sizing uses CSS dimensions times `min(devicePixelRatio, 2)` and rejects non-finite/non-positive dimensions without changing physics coordinates."
    - "Drawing clears `#071018`, draws every particle from bulk positions/radii/RGBA first, then three `#94A3B8` 2px basin segments, then the dynamic circle with `#334155` fill and `#CBD5E1` 2px stroke."
  </behavior>
  <action>Per D-02, D-03, D-05, D-11, D-12, D-15, and the approved UI contract, create pure `camera.ts` types for fixed `WORLD_BOUNDS = { minX: -6, minY: -1, maxX: 6, maxY: 8 }`, validated viewport size, scale/offset calculation, projected point, and projected radius. Use a 16px internal inset, the minimum x/y scale, centered letterboxing, and y inversion only in `projectPoint`; no interpolation, browser elapsed time, particle integration, or mutable physics state. Test exact corners/center, aspect preservation, inset, y inversion, and invalid dimensions with focused Arrange/Act/Assert cases.

Create imperative `canvas.ts` functions `resizeCanvasBackingStore(canvas, cssWidth, cssHeight, devicePixelRatio)` and `drawRenderFrame(context, frame, camera)`. Cap DPR at 2, set the transform so all drawing uses CSS pixels, clear every frame to `#071018`, iterate the validated typed arrays by their strides, convert Rust RGBA to CSS color with alpha/255, draw particle filled circles first, then rigid segments, then rigid circles using the exact approved colors/strokes. Do not create Solid signals/components per particle, attach pointer/keyboard handlers, draw status text, add trails/effects, or maintain a second motion model. Keep Canvas effects thin around the tested projection core.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/camera.test.ts &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/render/camera.ts` contains fixed world bounds `-6`, `-1`, `6`, `8`, inset `16`, and exactly one y-axis inversion in projection.
    - Camera tests prove centered aspect-preserving mapping at 960x540 and a narrow viewport, with no coordinate outside the 16px inset.
    - `web/src/render/canvas.ts` contains exact colors `#071018`, `#94A3B8`, `#334155`, and `#CBD5E1`, DPR cap 2, and draw order particles then segments then circles.
    - `rg "requestAnimationFrame|addEventListener|PointerEvent|KeyboardEvent|setInterval|delta|elapsed" web/src/render` returns no scheduling, interaction, or JavaScript integration logic.
    - Focused camera tests and strict typecheck exit 0; this task does not claim a complete Vite build before the app entrypoint exists.
  </acceptance_criteria>
  <done>The renderer consumes one validated bulk frame through a tested coordinate projection and contains presentation only.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the approved SolidJS proof page and complete build</name>
  <files>web/src/main.tsx, web/src/App.tsx, web/src/app.css</files>
  <read_first>16-UI-SPEC.md in full; 16-CONTEXT.md D-01, D-02, D-03, D-09 through D-13, D-15; 16-RESEARCH.md Pattern 4, Pattern 5, and Browser Proof Contract; 16-02-SUMMARY.md; scripts/web-build.ts; web/src/physics/loader.ts; web/src/physics/session.ts; web/src/render/camera.ts; web/src/render/canvas.ts; web/index.html; standards/core/frontend-ui.md; standards/languages/typescript-javascript.md</read_first>
  <action>Implement the approved `16-UI-SPEC.md` exactly. `main.tsx` mounts one `App` into the existing root and imports only `app.css`. In `App.tsx`, model `loading`, `running`, `failure`, and `disposed` as a discriminated union so invalid status/count/error combinations are not representable. Render semantic `<main aria-labelledby="proof-title">`, header, exact single h1 `LiquidFun Rust/WASM browser proof`, exact summary copy, one `<section aria-labelledby="session-status-title">`, exact h2 `Session status`, atomic polite status output, visible fixed runtime `<dl>` value `Rust engine · WebAssembly`, particle and rigid counts, `<figure>`, non-focusable Canvas with `role="img"`, exact accessible name and fallback text, visible exact figcaption, and one native `<button type="button">Dispose session</button>`. Use the UI-SPEC exact loading/running/failure/disposed, empty, count, and error strings through text nodes; never use `innerHTML` or raw exception HTML.

On mount, clear/size the 960x540-design Canvas, await `loadProofSession()`, request one validated Rust frame, draw it, and only then transition from Loading to Running. Keep one session owner and one pending animation-frame ID. While Running, request one animation frame, call exactly one `nextFrame()` (therefore one bounded Rust step), draw it, update coarse DOM counts and proof-only `data-wasm-initialized="true"`, `data-step-index`, and `data-moved-frame-count` attributes derived from actual consecutive Rust frame positions/circle values, then schedule the next frame. Browser timestamps schedule only; do not pass elapsed time to Rust, accumulate catch-up, interpolate, integrate, or script positions. On any load/step/capture/parse/draw error, cancel animation, dispose once when an owner exists, retain the last successful counts/frame, set the fixed Failure copy plus a bounded escaped `Details:` text only in development, and do not retry. Dispose activation cancels animation first, calls owner `dispose()` once, retains the last frame/counts, sets Disposed, and performs no further step/capture/draw. Register Solid cleanup that uses the same idempotent cancel/dispose function.

Create scoped page CSS with exactly the approved system font, four sizes (14/16/20/28), weights 400/600, colors, 4/8/16/24/32/48/64 spacing scale, 960px centered column, 32px desktop inline and 48px block padding, one `#151D28` 12px-radius 1px-border panel with 24px padding, 16:9 full-width Canvas, tabular counts, destructive button colors, 44px minimum button block size, and 2px `#39D3C7` focus-visible outline at 4px offset. At `max-width: 480px`, use 16px page/panel padding/gaps, stack metadata/counts, make the button full width, and preserve no horizontal overflow at 320px. Add no transition, animation, gradient, glow, glass, navbar, route, card, footer, source/version/build chrome, retry/reset/play/pause control, canvas focus, pointer/touch handler, component library, or responsive behavior beyond the approved containment minimum.</action>
  <verify>
    <automated>just web-build</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/App.tsx` contains all exact UI-SPEC copy strings, the required semantic elements/ARIA attributes, one `Dispose session` button, and no `innerHTML`.
    - Running is assigned only after generated initialization, session construction, frame validation, and `drawRenderFrame` complete.
    - One RAF iteration calls one `nextFrame()` and has no elapsed-time, catch-up, interpolation, integration, setInterval, or per-particle Solid signal/component path.
    - Failure and disposal both cancel RAF before idempotent owner disposal; Disposed preserves the last frame/counts and schedules no new draw.
    - `web/src/app.css` contains the exact UI-SPEC palette, dimensions, focus ring, 44px target, 960px column, 16:9 Canvas, and 480px breakpoint, with no gradients/transitions/animations.
    - `just web-build` regenerates the current-checkout WASM package, then runs strict typecheck, all unit tests, and the production Vite build successfully now that `main.tsx`, `App.tsx`, and every planned unit test exist.
  </acceptance_criteria>
  <done>The single approved dark page reaches Running only from a real first Rust frame, visibly draws ongoing Rust state, and exposes a safe explicit disposal action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Browser scheduler and DOM to WASM owner | Browser effects drive one bounded call at a time and can release the owner. |
| Validated Rust frame to visible Canvas | Numeric movement must become visible pixels without JavaScript physics substitution. |
| Generated package and app entrypoint to production bundle | The exact current-checkout WASM and reachable Solid entrypoint form the first complete browser artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-16-03-01 | D | RAF stepping, frame allocation, and Canvas backing store | mitigate | One step per RAF, Rust step bound 1 through 4, frame caps 512/16/8, DPR cap 2, and no catch-up loop. |
| T-16-03-02 | T/D | Rust/TS/Canvas geometry | mitigate | Rust checked construction plus TS finite/stride/radius parser gates all geometry; the camera rejects invalid viewports and no JavaScript integration or silent geometry clamp exists. |
| T-16-03-03 | D | Dispose/failure/unmount lifecycle | mitigate | One TS owner, cancel RAF before idempotent free, free temporary frames in finally, poison on error, preserve the last frame, and expose no post-dispose scheduling path. |
| T-16-03-04 | T/D | Copied array ownership during WASM memory growth | mitigate | Draw only boxed-slice JavaScript-owned arrays after frame parsing; use no raw Memory, buffer, pointer, or zero-copy path. |
| T-16-03-05 | T | Dependency and build integrity | mitigate | Use exact Cargo/Bun/tool pins and lockfiles, regenerate before the first complete build, and require the now-present tests and app entrypoint before Vite bundling. |
</threat_model>

<verification>
Run the focused camera tests and strict typecheck after Task 1. After Task 2 creates `main.tsx`, `App.tsx`, and `app.css`, run `just web-build` so regeneration, all unit tests, typechecking, and production bundling execute in dependency order. Verify the approved UI copy/layout statically and confirm the bundle contains JavaScript plus WASM. Plan 16-04 owns real Chromium execution and retained visible-Canvas evidence.
</verification>

<success_criteria>
- Pure camera and Canvas modules consume validated bulk arrays, preserve the approved coordinate/draw contract, and contain no scheduling or JavaScript physics.
- The exact approved loading/running/failure/disposed page and dark single-panel Canvas contract are implemented without Phase 17–19 features.
- Explicit disposal cancels animation before the idempotent owner release and preserves the last frame/counts.
- `just web-build` is first invoked only after every required unit test and app entrypoint exists, and produces the current-checkout production bundle.
- D-01 through D-05, D-09 through D-13, and D-15 are implemented at this slice; real browser proof, final isolation evidence, and review remain Plan 16-04.
</success_criteria>

<output>
After completion, create `.planning/phases/16-rust-wasm-browser-bridge/16-03-SUMMARY.md`
</output>
