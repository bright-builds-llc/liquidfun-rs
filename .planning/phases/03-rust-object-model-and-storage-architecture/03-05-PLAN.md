---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 3-2026-07-11T01-23-59
generated_at: 2026-07-11T01:32:13.331Z
phase: 03-rust-object-model-and-storage-architecture
plan: "05"
type: execute
wave: 5
depends_on:
  - "03-03"
  - "03-04"
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/tests/object_model.rs
  - crates/liquidfun/tests/hook_contract.rs
  - crates/liquidfun/tests/particle_identity.rs
  - ARCHITECTURE.md
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - API-07
  - API-08
  - DOCS-02
must_haves:
  truths:
    - Public and compile-fail evidence proves the Phase-3 contracts from a consumer perspective.
    - ARCHITECTURE.md records D-01 through D-17 and the enforceable dependency, lifecycle, storage, oracle, and renderer boundaries.
    - The complete required Rust verification sequence passes in order.
  artifacts:
    - crates/liquidfun/tests/object_model.rs
    - crates/liquidfun/tests/hook_contract.rs
    - crates/liquidfun/tests/particle_identity.rs
    - ARCHITECTURE.md
  key_links:
    - Integration tests use only curated lib.rs exports and cannot reach private arenas, dense indices, or contact records.
    - Architecture decisions map directly to executable tests and deferred Phase-9 boundaries.
---

# Plan 03-05: Public Contract Evidence and Architecture Sign-off

<objective>
Close Phase 3 with consumer-facing integration and compile-fail evidence, a complete architecture decision record, and the repository-mandated Rust verification sequence.
</objective>

<threat_model>
- Public tests must attempt stale, destroyed, wrong-type, cross-world, and reused-slot misuse rather than relying only on private unit coverage.
- Compile-time restrictions should prove wrong-handle-kind use, retained hook views, and unrestricted callback mutation are unavailable where Rust can express the rejection.
- Regression coverage must include exhaustion, finite command/event limits, panic poisoning, and particle lane corruption/state-machine evidence without allocating unbounded inputs.
- Documentation must not imply broad solver parity or advertise the deferred Phase-9 bulk mutation/external-buffer surface as complete.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Curate the public surface and add consumer integration evidence</name>
  <files>crates/liquidfun/src/lib.rs, crates/liquidfun/tests/object_model.rs, crates/liquidfun/tests/hook_contract.rs, crates/liquidfun/tests/particle_identity.rs</files>
  <read_first>
    - crates/liquidfun/src/lib.rs
    - crates/liquidfun/src/identity.rs
    - crates/liquidfun/src/world.rs
    - crates/liquidfun/src/world/step.rs
    - crates/liquidfun/src/association.rs
    - crates/liquidfun/src/particle.rs
  </read_first>
  <action>
Review `lib.rs` exports and expose only the minimal stable Phase-3 contract. Add black-box integration tests for API-01 through API-08: all distinct handles; stale/destroyed/cross-world/reused-slot failures; documented destruction cascades and owned records; transient contact/hook views; narrow directives; post-unlock sequential commands with stale-command results; owned event timing/order/multiplicity; and typed association cleanup. Add public stable-particle tests across representative permutation/compaction operations only if such operations are intentionally exposed for the spike; otherwise keep permutation proof private and test consumer-visible stable identity through supported world fixtures. Use compile-fail rustdoc/type evidence where practical for wrong handle kinds, private constructors/dense indices, escaping contact views, and attempted `&mut World` hook access. Each runtime test covers one concern and uses Arrange/Act/Assert.
  </action>
  <verify>
    - `cargo test -p liquidfun --test object_model --all-features`
    - `cargo test -p liquidfun --test hook_contract --all-features`
    - `cargo test -p liquidfun --test particle_identity --all-features`
    - `cargo test -p liquidfun --doc --all-features`
    - `cargo fmt --all`
    - `cargo clippy --all-targets --all-features -- -D warnings`
    - `cargo build --all-targets --all-features`
    - `cargo test --all-features`
  </verify>
  <acceptance_criteria>
    - Every API-01 through API-08 requirement has at least one consumer-level proof or an explicit compile-time restriction.
    - Tests use no private module access and no C++ oracle dependency.
    - Public docs state invalidation, ownership, timing, bounds, poison, and failure behavior for the exposed types.
    - No raw pointer, durable contact handle, dense particle index, arbitrary callback closure command, or premature full Phase-9 buffer API is public.
  </acceptance_criteria>
  <done>All three black-box integration targets and compile-fail doctests pass using only curated exports, covering API-01 through API-08 without exposing deferred or unsafe surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ARCHITECTURE.md and sign off all locked decisions</name>
  <files>ARCHITECTURE.md</files>
  <read_first>
    - ARCHITECTURE.md
    - .planning/phases/03-rust-object-model-and-storage-architecture/03-CONTEXT.md
  </read_first>
  <action>
Update `ARCHITECTURE.md` for DOCS-02. Preserve the existing one-way dependency and process-isolated oracle text, then add enforceable sections for world ownership, typed handle validation/invalidation, object adjacency and destruction cascade order, transient contacts, restricted hooks, step lock/directive/event/command timing, panic poison behavior, safe association ownership, stable particle identity versus dense indices, authoritative permutation and group/lane invariants, and renderer independence. Include a compact decision sign-off table mapping D-01 through D-17 to the implemented module/test evidence. Clearly mark broad solver behavior and API-09/API-10 bulk mutation/external-buffer completion as Phase 9 or later; describe D-17 only as the locked safe ownership/capacity direction proved by a private spike.
  </action>
  <verify>
    - `rg "D-(0[1-9]|1[0-7])|Dependency direction|handle|callback|particle storage|step|oracle|renderer" ARCHITECTURE.md`
    - Manually confirm each D-01 through D-17 appears exactly once in the sign-off table and every required DOCS-02 boundary has an enforceable statement.
    - `cargo fmt --all`
    - `cargo clippy --all-targets --all-features -- -D warnings`
    - `cargo build --all-targets --all-features`
    - `cargo test --all-features`
  </verify>
  <acceptance_criteria>
    - DOCS-02 covers crate/module boundaries, dependency direction, handles, callbacks, particle storage, step order, oracle isolation, and renderer independence.
    - All D-01 through D-17 have a documented disposition and evidence link.
    - The document accurately states Phase-3 maturity and deferrals without claiming solver or full particle API parity.
  </acceptance_criteria>
  <done>ARCHITECTURE.md contains every required DOCS-02 boundary and exactly one evidence-backed sign-off entry for each D-01 through D-17, with Phase-9 and solver work explicitly deferred.</done>
</task>

<task type="auto">
  <name>Task 3: Run ordered Rust gates and final simplification review</name>
  <files>None (verification only)</files>
  <read_first>
    - Cargo.toml
    - crates/liquidfun/Cargo.toml
    - crates/liquidfun/src/lib.rs
    - ARCHITECTURE.md
  </read_first>
  <action>
Perform a review-only required simplification audit: verify that there are no duplicate validation or permutation paths, destruction and step lifecycle remain centralized, and no abstraction exceeds the representative Phase-3 scope. Do not edit source, tests, documentation, configuration, or any other file in this task. First run the read-only preflight `cargo fmt --all -- --check` and abort immediately if it reports drift; this proves the mandatory subsequent format command cannot change tracked Rust files. Only after that preflight passes, run the exact repository Rust verification order, stopping at the first failure: (1) `cargo fmt --all`; (2) `cargo clippy --all-targets --all-features -- -D warnings`; (3) `cargo build --all-targets --all-features`; (4) `cargo test --all-features`. Finally review the diff for unintended public exports, solver work, C++/harness leakage, unsafe code, unbounded collections, and inaccurate architecture claims. If a command fails or the audit finds duplicate paths, decentralized lifecycle logic, scope creep, unintended exports, or any other issue, fail verification and report the exact command/error or file and line for a follow-up task instead of mutating undeclared paths.
  </action>
  <verify>
    - `cargo fmt --all -- --check`
    - `cargo fmt --all`
    - `cargo clippy --all-targets --all-features -- -D warnings`
    - `cargo build --all-targets --all-features`
    - `cargo test --all-features`
    - `git diff --check`
  </verify>
  <acceptance_criteria>
    - The read-only formatting preflight passes before the four Rust gates, proving the subsequent mandatory `cargo fmt --all` command makes no changes.
    - All four Rust gates pass in the mandated order with no warnings.
    - Focused unit, compile-fail/doctest, integration, arena property, and permutation state-machine evidence all pass.
    - Diff review finds no accidental Phase-9 API, broad solver work, unsafe code, or oracle dependency in `liquidfun`.
    - Architecture sign-off and executable behavior agree.
    - This task changes no files; any failed gate or review finding is reported with the exact command/error or file and line for follow-up.
  </acceptance_criteria>
  <done>The task makes no file changes, all four Rust gates and `git diff --check` pass, and the review finds no duplicate paths, decentralized lifecycle logic, unintended exports, scope creep, unsafe code, oracle leakage, unbounded collections, or inaccurate architecture claims.</done>
</task>

</tasks>

<verification>
- Confirm API-01, API-02, API-03, API-04, API-05, API-06, API-07, API-08, and DOCS-02 each have consumer-visible or architecture acceptance evidence.
- After focused checks for each task, run the repository pre-commit gate in this exact order before that task's commit:
- `cargo fmt --all`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo build --all-targets --all-features`
- `cargo test --all-features`
- Confirm focused tests use Arrange/Act/Assert and property tests cover both arena and particle permutation state machines.
- Confirm no public API claims API-09 or API-10 completion.
</verification>

<success_criteria>
- All Phase-3 requirements pass from the public consumer boundary.
- All D-01 through D-17 are implemented or explicitly locked/deferred at the correct boundary with test evidence.
- ARCHITECTURE.md is the signed-off enforceable description of object identity, callbacks, storage, step order, oracle isolation, and renderer independence.
</success_criteria>

<must_haves>
- Tests and documentation agree on failures, ordering, lifetimes, bounds, and poison behavior.
- Final verification is reproducible with ordinary Cargo and no C++ toolchain.
- Phase 3 ends with a small native-Rust architecture foundation, not a partial solver.
</must_haves>
