---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 3-2026-07-11T01-23-59
generated_at: 2026-07-11T01:32:13.331Z
phase: 03-rust-object-model-and-storage-architecture
plan: "04"
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/liquidfun/src/particle.rs
  - crates/liquidfun/src/particle/storage.rs
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/Cargo.toml
autonomous: true
requirements:
  - API-01
  - API-02
  - API-08
must_haves:
  truths:
    - Stable ParticleId identity is separate from private ephemeral dense position.
    - One authoritative permutation updates every lane, identity map, derived index, and group range transactionally.
    - Pending deletion rejects ordinary mutation while preserving owned destruction snapshot data.
    - Future external-buffer ownership and fixed-capacity semantics are locked as a private Phase-3 contract, not exposed as Phase-9 API.
  artifacts:
    - crates/liquidfun/src/particle.rs
    - crates/liquidfun/src/particle/storage.rs
  key_links:
    - ParticleId validation uses world plus particle-system scope before dense lookup.
    - dense_to_id and id_to_dense are updated inside the same permutation transaction as all representative lanes.
---

# Plan 03-04: Dense Particle Identity and Permutation Spike

<objective>
Retire the highest-risk particle storage questions with a bounded private spike: stable public identity over dense SoA movement, explicit lifecycle states, authoritative lane permutation, remappable derived indices, group ranges, and future owned-buffer capacity semantics.
</objective>

<threat_model>
- Forged, stale, cross-world, or cross-particle-system `ParticleId` values must never index a dense row.
- Identity-slot or declared lane capacity exhaustion must fail explicitly; allocation capacity must never silently redefine the contract.
- A malformed permutation, lane length mismatch, duplicate destination, or derived-index remap could corrupt lane alignment, proxies, contacts, pairs, triads, lifetime data, or group ranges and must be rejected before mutation.
- Create/reorder/delete/compact sequences must remain bounded and transactional; property generators may not cause unbounded allocations.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement stable particle identity and lifecycle state mapping</name>
  <files>crates/liquidfun/src/particle.rs, crates/liquidfun/src/particle/storage.rs, crates/liquidfun/src/lib.rs</files>
  <read_first>
    - .planning/phases/03-rust-object-model-and-storage-architecture/03-CONTEXT.md
    - crates/liquidfun/src/identity.rs
    - crates/liquidfun/src/arena.rs
  </read_first>
  <action>
Implement D-12 and D-13 in a private representative `ParticleStorage`. Keep `ParticleIndex` private and ephemeral. Validate public `ParticleId` against both world and particle-system scope before consulting `id_to_dense`. Maintain `dense_to_id` plus identity entries in explicit `Live`, `PendingDelete`, and `Vacant` states. Mark-delete rejects ordinary mutation but retains the row and owned snapshot data; compaction emits destruction information, makes the ID stale, and advances or retires its generation. Use a deterministic group-contiguous SoA with a small set of required and optional lanes. Do not expose full Phase-9 bulk mutation APIs.
  </action>
  <verify>
    - `cargo test -p liquidfun particle::storage::identity --all-features`
    - Tests cover cross-system IDs, stale IDs after compaction, pending-delete mutation rejection, and identity exhaustion.
    - `cargo fmt --all`
    - `cargo clippy --all-targets --all-features -- -D warnings`
    - `cargo build --all-targets --all-features`
    - `cargo test --all-features`
  </verify>
  <acceptance_criteria>
    - Stable IDs resolve to the same semantic particle across reorder and lane rotation.
    - Pending-delete and stale states have distinct behavior and neither permits ordinary mutation.
    - Dense indices and backing vectors do not cross the public boundary.
    - Tests use focused Arrange/Act/Assert and cover fixed capacity without implicit growth.
  </acceptance_criteria>
  <done>Focused particle identity tests pass for reorder, pending deletion, compaction, cross-system access, stale access, exhaustion, and declared fixed capacity.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize transactional lane permutation and derived remapping</name>
  <files>crates/liquidfun/src/particle.rs, crates/liquidfun/src/particle/storage.rs</files>
  <read_first>
    - crates/liquidfun/src/particle.rs
    - crates/liquidfun/src/particle/storage.rs
  </read_first>
  <action>
Implement D-14 as the only authoritative reorder/compaction operation. Validate the entire permutation and all required/optional lane lengths first, build bounded scratch state, then commit every required and present optional lane, both identity maps, representative proxies, contacts, pairs, triads, lifetime indices, and group ranges together. Define deterministic remapping for removed rows and reject invalid derived references. Never use hash-map iteration for solver-visible order. Add a central invariant checker used by tests after every transaction; error paths must leave byte-for-byte-equivalent semantic state.
  </action>
  <verify>
    - `cargo test -p liquidfun particle::storage::permutation --all-features`
    - Invalid duplicate/out-of-range permutations and mismatched optional lanes prove no partial commit.
    - `cargo fmt --all`
    - `cargo clippy --all-targets --all-features -- -D warnings`
    - `cargo build --all-targets --all-features`
    - `cargo test --all-features`
  </verify>
  <acceptance_criteria>
    - Every representative lane and derived-index fixture follows the same old-to-new mapping.
    - Identity maps remain mutual inverses for live and pending rows.
    - Group ranges stay contiguous and lifetime order stays deterministic.
    - All validation completes before authoritative storage changes.
  </acceptance_criteria>
  <done>Focused permutation tests pass, proving one validate-then-commit mapping updates every lane, identity map, derived index, and group range while invalid inputs leave semantic state unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Property-test storage state machines and lock future buffer semantics</name>
  <files>Cargo.toml, Cargo.lock, crates/liquidfun/Cargo.toml, crates/liquidfun/src/particle.rs, crates/liquidfun/src/particle/storage.rs</files>
  <read_first>
    - crates/liquidfun/src/particle.rs
    - crates/liquidfun/src/particle/storage.rs
    - Cargo.toml
    - Cargo.lock
    - crates/liquidfun/Cargo.toml
  </read_first>
  <action>
Implement D-15 with a bounded model-based property suite over arbitrary create, reorder, group rotation, mark-delete, compact, stale-access, optional-lane, and capacity-failure operations. Compare stable semantic state to an independent simple model after every operation and persist a readable operation trace on failure. Implement D-17 only as a private validated owned-lane-bundle constructor/teardown spike: track declared fixed capacity separately from allocation capacity, return explicit `CapacityExceeded`, expose only borrow-scoped slices internally, and return owned buffers on teardown. Do not publish borrowed lifetime-long arrays, raw pointers, or the full API-09/API-10 Phase-9 surface. Declare the reviewed `proptest = "1.11.0"` version in root `Cargo.toml` under `[workspace.dependencies]`, then add `proptest = { workspace = true }` under `[dev-dependencies]` in `crates/liquidfun/Cargo.toml`. Run `cargo check -p liquidfun --all-features` to resolve the dependency and update `Cargo.lock`; do not edit the lockfile by hand.
  </action>
  <verify>
    - `cargo test -p liquidfun particle::storage::properties --all-features`
    - Run the property suite with enough bounded cases to exercise every operation class while keeping PR runtime finite.
    - `cargo fmt --all`
    - `cargo clippy --all-targets --all-features -- -D warnings`
    - `cargo build --all-targets --all-features`
    - `cargo test --all-features`
  </verify>
  <acceptance_criteria>
    - The model suite proves stable identity and lane/derived invariants across arbitrary bounded state-machine sequences.
    - Root `Cargo.toml` declares `proptest = "1.11.0"`, `crates/liquidfun/Cargo.toml` uses `proptest = { workspace = true }`, and Cargo-generated `Cargo.lock` resolves the dependency.
    - Declared fixed capacity, not spare allocation capacity, controls admissible growth.
    - Teardown returns the owned representative buffers.
    - No public Phase-9 bulk-view, external-buffer, raw-pointer, or particle-solver API is introduced.
  </acceptance_criteria>
  <done>The bounded storage state-machine property suite passes, Cargo resolves the reviewed proptest dependency, fixed declared capacity governs growth, teardown returns owned buffers, and no Phase-9 API is public.</done>
</task>

</tasks>

<verification>
- Run focused identity, permutation, invalid-transaction, capacity, teardown, and property state-machine tests.
- After focused checks for each task, run the repository pre-commit gate in this exact order before that task's commit:
- `cargo fmt --all`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo build --all-targets --all-features`
- `cargo test --all-features`
- Review public exports to confirm only stable particle identity needed by API-01 is exposed; dense indices and Phase-9 buffer mutation remain private.
</verification>

<success_criteria>
- D-12 through D-15 prove stable identity over dense permutations and compaction.
- D-17 has executable private ownership/capacity evidence without prematurely implementing API-09 or API-10.
- API-01/API-02 cover stable particle identity and explicit stale/cross-scope failures; the safe association model can key by `ParticleId` under API-08.
</success_criteria>

<must_haves>
- Lane corruption is prevented by validate-then-commit permutation handling.
- Property tests exercise permutations and lifecycle transitions, not merely isolated helpers.
- The spike stays representative and bounded rather than growing into a solver.
</must_haves>
