---
phase: 20-playground-catalog-previews-and-reset-honesty
plan: "06"
type: execute
wave: 4
depends_on: [20-05]
files_modified:
  - .planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md
autonomous: true
requirements: [WEB-01, WEB-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-09-20T17-36-31
generated_at: 2026-09-20T17:56:00Z
must_haves:
  truths:
    - "just web-player-smoke still passes on the phase HEAD after catalog previews and Reset honesty land."
    - "A separate identified AI reviewer acknowledges the Phase 20 diff and smoke evidence at an exact SHA-256 digest."
    - "The implementing executor does not approve its own work; passing automated checks are not the acknowledgment."
  artifacts:
    - path: .planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md
      provides: "Independent exact-digest acknowledgment"
  key_links:
    - from: .planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md
      to: "digest"
      via: "Separate reviewer identity and SHA-256"
      pattern: "digest"
---

<objective>
Re-run the Chromium player-smoke gate and obtain independent AI review of the Phase 20 preview and Reset-honesty diff.

Purpose: D-11 keeps independent AI review eligible. Owner policy 2026-09-16: the implementing agent must not approve its own work. This phase does not require a new GitHub Pages URL, Firefox/WebKit, Linux qualification, or Dam Break C++ timing.
Output: passing `just web-player-smoke` plus `20-REVIEW.md` bound to an exact digest.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-CONTEXT.md
@.planning/phases/18-six-native-physics-demos/18-REVIEW.md
@.planning/phases/19-interaction-polish-and-browser-verification/19-REVIEW.md

<interfaces>
Do **not** push to main, create a release tag, publish npm/crates, or write HOST-EVIDENCE. D-11 local Chromium smoke is sufficient.

Review manifest (hash these paths that exist on disk after Plans 01–05; skip a path only if git shows it was never added, and record that skip):

1. `web/src/catalog/previews.tsx`
1. `web/src/components/DemoNavigation.tsx`
1. `web/src/app.css`
1. `web/src/App.tsx`
1. `web/src/components/PlayerSceneChrome.tsx`
1. `web/src/player/runtime.ts`
1. `web/src/player/viewport.ts`
1. `web/tests/runtime.test.ts`
1. `web/tests/controls.test.ts`
1. `web/e2e/shell.spec.ts`
1. `web/e2e/reset-honesty.spec.ts`
1. `web/package.json`
1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-CONTEXT.md`

Compute one lowercase SHA-256 over the concatenated file bytes (or `git hash-object` list then hash the sorted object ids). Record the exact command in `20-REVIEW.md`.

Independent review:

1. Launch a separate identified AI reviewer (`gsd-code-reviewer` subagent). Do not resume the implementing agent. Do not treat this executor as the reviewer.
1. The reviewer inspects the complete relevant diff and the `just web-player-smoke` result.
1. Write `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md` with:
   - `digest:` 64 lowercase hex
   - `reviewer_identity:` actual reviewer id
   - `reviewed_at:` actual ISO timestamp
   - `reviewer_disclosure: AI reviewer, not a human`
   - `implementing_or_fixing_executor: no`
   - decision APPROVED or CHANGES_REQUESTED bound to that digest
1. Passing `just web-player-smoke` is not the acknowledgment.
1. Do not format `.planning/**` with mdformat.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run just web-player-smoke on phase HEAD</name>
  <files>.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md</files>
  <read_first>
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-CONTEXT.md` D-10, D-11
    1. `web/package.json` `test:player`
    1. `justfile` `web-player-smoke`
  </read_first>
  <action>
    Per D-10, run `just web-player-smoke` on the current phase HEAD so WASM, production-base `/liquidfun-rs/` preview, `e2e/shell.spec.ts`, `e2e/player.spec.ts`, `e2e/demo-media-clock.spec.ts`, and `e2e/reset-honesty.spec.ts` all execute. Do not set `PHASE16_CLOSURE_ATTEMPT_DIR`. Do not add Playwright to `.github/workflows/pages.yml`. Do not push. Confirm `rg -n "e2e/reset-honesty.spec.ts" web/package.json` still matches before relying on smoke. Record the smoke command and exit 0 in the review file draft only after the command succeeds; leave reviewer identity fields for Task 2.
  </action>
  <verify>
    <automated>just web-player-smoke</automated>
  </verify>
  <acceptance_criteria>
    1. `just web-player-smoke` exits 0.
    1. `rg -n "e2e/reset-honesty.spec.ts|e2e/shell.spec.ts" web/package.json` matches both.
    1. `rg -n "playwright" .github/workflows/pages.yml` is empty.
    1. No new git tag is created.
  </acceptance_criteria>
  <done>Combined Chromium player-smoke covers catalog previews and Reset honesty on this HEAD.</done>
</task>

<task type="auto">
  <name>Task 2: Obtain independent exact-digest AI review</name>
  <files>.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md</files>
  <read_first>
    1. `AGENTS.md` Independent review
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-CONTEXT.md` D-11
    1. `.planning/phases/18-six-native-physics-demos/18-REVIEW.md`
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-REVIEW.md`
  </read_first>
  <action>
    Per D-11 and the 2026-09-16 owner policy, the implementing agent must not approve its own work. Build the review manifest and lowercase SHA-256 digest over the files in `<interfaces>`. Launch a separate identified `gsd-code-reviewer` AI reviewer (new subagent, not this implementing executor) to inspect the complete Phase 20 diff, `just web-player-smoke` evidence, and WEB-01/WEB-03 honesty. Write `20-REVIEW.md` bound to that exact digest, the reviewer's actual identity, and the actual ISO review time. State AI review honestly. Do not treat passing automated checks as the acknowledgment. Do not publish a crate. Do not represent the review as human approval. Do not mdformat `.planning/**`.
  </action>
  <verify>
    <automated>rg -n "digest|reviewer_identity|reviewed_at|AI reviewer" .planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    1. `test -f .planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md`.
    1. `rg -n "digest:" .planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md` matches a 64-hex digest.
    1. `rg -n "reviewer_identity:" .planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md` matches.
    1. `rg -n "AI reviewer, not a human" .planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md` matches.
    1. `rg -n "implementing_or_fixing_executor: no" .planning/phases/20-playground-catalog-previews-and-reset-honesty/20-REVIEW.md` matches.
    1. The reviewer identity is not the implementing executor label.
    1. No self-approval sentence from the implementing agent is used as the acknowledgment.
  </acceptance_criteria>
  <done>Independent review is bound to the exact Phase 20 digest; the implementer did not self-approve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Review digest → acknowledgment | Only a separate reviewer may bind approval. |
| Smoke allowlist → claimed coverage | Unlisted specs must not be cited as D-10 evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06-01 | R | self-review | mitigate | Separate `gsd-code-reviewer` plus exact digest (D-11). |
| T-20-06-02 | T | stale smoke | mitigate | Re-run `just web-player-smoke` on phase HEAD before review. |
| T-20-06-03 | S | human-approval claim | mitigate | Disclose AI reviewer; never call it human approval. |
| T-20-06-04 | E | package release | accept | Publication remains unauthorized; do not tag. |
| T-20-06-05 | E | Pages PAT / force push | accept | This plan does not deploy; no push required. |
| T-20-06-06 | I | omitted reset spec | mitigate | Task 1 confirms `reset-honesty.spec.ts` remains on `test:player`. |
</threat_model>

<verification>
Confirm `20-REVIEW.md` digest identity fields and that `just web-player-smoke` exited 0. Do not claim Firefox/Safari coverage. Do not format `.planning/**` with mdformat.
</verification>

<success_criteria>
- Combined player-smoke still passes.
- Independent AI review acknowledges the exact Phase 20 digest.
- No Pages deploy, package publish, or self-approval.
</success_criteria>

<output>
After completion, create `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-06-SUMMARY.md`
</output>
