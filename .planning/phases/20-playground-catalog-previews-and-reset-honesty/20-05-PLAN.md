---
phase: 20-playground-catalog-previews-and-reset-honesty
plan: "05"
type: execute
wave: 3
depends_on: [20-03, 20-04]
files_modified:
  - web/e2e/reset-honesty.spec.ts
  - web/package.json
autonomous: true
requirements: [WEB-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-09-20T17-36-31
generated_at: 2026-09-20T17:56:00Z
must_haves:
  truths:
    - "After Reset scene, Fountain Emission rate is medium, Float or Sink Body is wood, Color Mixer Stir speed is slow, and Water Wheel Jet strength is medium."
    - "After Apply setting then Reset scene, Dam Break Water amount is medium and Color Mixer Mix strength is strong."
    - "Pause scene then Play scene does not restore a live select that the visitor changed."
    - "just web-player-smoke runs e2e/reset-honesty.spec.ts because test:player lists that file."
  artifacts:
    - path: web/e2e/reset-honesty.spec.ts
      provides: "Live and construction Reset label proofs plus play/pause negative"
    - path: web/package.json
      provides: "test:player allowlist includes reset-honesty.spec.ts"
  key_links:
    - from: web/package.json
      to: web/e2e/reset-honesty.spec.ts
      via: "test:player playwright file list"
      pattern: "e2e/reset-honesty.spec.ts"
    - from: web/e2e/reset-honesty.spec.ts
      to: "Reset scene"
      via: "getByRole button then toHaveValue documented initial"
      pattern: "Reset scene"
---

<objective>
Prove Reset live-control honesty in Chromium and register the new spec on the `test:player` allowlist that `just web-player-smoke` actually runs.

Purpose: D-08, D-10, D-11. `player.spec.ts` is 324 lines; a new file avoids the 400-line split trigger. `test:player` is an explicit file list, not `e2e/*.spec.ts`.
Output: `web/e2e/reset-honesty.spec.ts` and an updated `web/package.json` script. No Firefox. No screenshot hashes. No WASM scene edits.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-CONTEXT.md
@.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-UI-SPEC.md
@web/e2e/player-helpers.ts
@web/e2e/player.spec.ts
@web/package.json

<interfaces>
Create `web/e2e/reset-honesty.spec.ts`. Import `expect, test` from `@playwright/test` and reuse `expectReadySceneChrome`, `SCENE_HASH_PATHS`, `PLAYING_STATUS`, `PAUSED_STATUS`, `CONSTRUCTION_RESET_HINT` from `./player-helpers`. Do not import Solid. Do not use `toHaveScreenshot`.

Shared helper inside the spec (keep it local; do not grow `player-helpers.ts`):

```ts
async function resetPlayingScene(page: Page, title: string): Promise<void> {
  await page.getByRole("button", { name: "Reset scene" }).click();
  await expect(page.getByRole("status")).toHaveText(PLAYING_STATUS);
  await expectReadySceneChrome(page, title);
}
```

Live cases (D-10). For each: `goto` the hash, `expectReadySceneChrome`, `selectOption` the dirty value, assert dirty `toHaveValue`, Reset, assert documented initial `toHaveValue`.

| Title | Path key | Label | Dirty option | After Reset |
|-------|----------|-------|--------------|-------------|
| Fountain | fountain | Emission rate | high | medium |
| Float or Sink | float-or-sink | Body | cork | wood |
| Color Mixer | color-mixer | Stir speed | fast | slow |
| Water Wheel | water-wheel | Jet strength | strong | medium |

Construction cases (D-08). Same Reset path, but dirty via `selectOption` plus visible `CONSTRUCTION_RESET_HINT` plus `getByRole("button", { name: "Apply setting" })` before Reset.

| Title | Path key | Label | Dirty option | After Reset |
|-------|----------|-------|--------------|-------------|
| Dam Break | dam-break | Water amount | large | medium |
| Color Mixer | color-mixer | Mix strength | gentle | strong |

Play/pause negative (D-07): Fountain, select Emission rate `high`, Pause scene, assert `PAUSED_STATUS` and value still `high`, Play scene, assert `PLAYING_STATUS` and value still `high`. Do not click Reset in this test.

`web/package.json` `test:player` must become exactly:

```
playwright test e2e/player.spec.ts e2e/demo-media-clock.spec.ts e2e/shell.spec.ts e2e/reset-honesty.spec.ts
```

Do not add firefox or webkit. Do not bump `@kobalte/core`. `wc -l < web/e2e/reset-honesty.spec.ts` must be `<= 400`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write live preset Reset honesty tests</name>
  <files>web/e2e/reset-honesty.spec.ts</files>
  <read_first>
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-CONTEXT.md` D-07, D-08, D-10
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-UI-SPEC.md` Reset Honesty
    1. `web/e2e/player-helpers.ts` `expectReadySceneChrome`, `SCENE_HASH_PATHS`
    1. `web/e2e/player.spec.ts` Gravity Apply setting pattern
  </read_first>
  <action>
    Per D-10, create `web/e2e/reset-honesty.spec.ts` with Arrange/Act/Assert tests for the four live rows in `<interfaces>`. Use `page.getByLabel(label).selectOption(dirty)` and `toHaveValue` for both dirty and post-Reset values. After Reset, status text must be `Playing`. Do not wait on canvas pixel hashes. Do not edit `player.spec.ts`. One concern per test (one scene/control pair).
  </action>
  <verify>
    <automated>rg -n "Emission rate|Body|Stir speed|Jet strength" web/e2e/reset-honesty.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `test -f web/e2e/reset-honesty.spec.ts`.
    1. `rg -n "getByLabel\\(\"Emission rate\"\\)" web/e2e/reset-honesty.spec.ts` matches.
    1. `rg -n "toHaveValue\\(\"medium\"\\)" web/e2e/reset-honesty.spec.ts` matches Fountain and Water Wheel after Reset.
    1. `rg -n "toHaveValue\\(\"wood\"\\)" web/e2e/reset-honesty.spec.ts` matches.
    1. `rg -n "toHaveValue\\(\"slow\"\\)" web/e2e/reset-honesty.spec.ts` matches.
    1. `rg -n "Reset scene" web/e2e/reset-honesty.spec.ts` matches.
    1. `rg -n "toHaveScreenshot|innerHTML" web/e2e/reset-honesty.spec.ts` is empty.
  </acceptance_criteria>
  <done>Representative live selects recover documented initials after Reset scene.</done>
</task>

<task type="auto">
  <name>Task 2: Construction Reset, play/pause negative, and test:player allowlist</name>
  <files>web/e2e/reset-honesty.spec.ts, web/package.json</files>
  <read_first>
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-RESEARCH.md` Pitfall 3, Pitfall 6
    1. `web/package.json` `scripts.test:player`
    1. `web/e2e/player.spec.ts` Apply setting Gravity test
    1. `scripts/web-build.ts` `bun run test:player`
  </read_first>
  <action>
    Per D-08 and D-07, add the two construction Reset tests (Water amount large + Apply setting, Mix strength gentle + Apply setting) and the Fountain play/pause negative test from `<interfaces>`. Then set `web/package.json` `test:player` to the exact four-file playwright command in `<interfaces>`. Do not remove `e2e/shell.spec.ts` from that list. Do not add a new Playwright project. Run `just web-player-smoke` so WASM rebuild, production-base preview, shell previews, player specs, and reset-honesty all run together.
  </action>
  <verify>
    <automated>just web-player-smoke</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "Water amount" web/e2e/reset-honesty.spec.ts` matches.
    1. `rg -n "Mix strength" web/e2e/reset-honesty.spec.ts` matches.
    1. `rg -n "Apply setting" web/e2e/reset-honesty.spec.ts` matches.
    1. `rg -n "Pause scene" web/e2e/reset-honesty.spec.ts` matches.
    1. `rg -n "toHaveValue\\(\"high\"\\)" web/e2e/reset-honesty.spec.ts` matches the play/pause negative test.
    1. `rg -n "e2e/reset-honesty.spec.ts" web/package.json` matches inside `test:player`.
    1. `rg -n "e2e/shell.spec.ts" web/package.json` still matches inside `test:player`.
    1. `rg -n "firefox|webkit" web/playwright.config.ts web/e2e/reset-honesty.spec.ts` is empty of new projects (playwright.config stays chromium-only).
    1. `wc -l < web/e2e/reset-honesty.spec.ts` is `<= 400`.
    1. `just web-player-smoke` exits 0.
  </acceptance_criteria>
  <done>D-10 Reset honesty is on the allowlist that CI and just web-player-smoke actually execute.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Select option ids → reconstructed world | Tests prove chrome tokens match documented initials after Reset. |
| package.json allowlist → smoke | Unlisted specs never run in `just web-player-smoke`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05-01 | I | stale pendingValue | mitigate | Assert `toHaveValue` after Reset for live and construction ids. |
| T-20-05-02 | T | construction bag | mitigate | Apply setting then Reset must return water-amount medium / mix-strength strong. |
| T-20-05-03 | I | play/pause remount | mitigate | Negative test keeps Emission rate high across pause/play. |
| T-20-05-04 | E | omitted spec | mitigate | Add `e2e/reset-honesty.spec.ts` to `test:player`. |
| T-20-05-05 | D | browser matrix | mitigate | Chromium-only; D-11 forbids Firefox/WebKit. |
| T-20-05-06 | T | screenshot hashes | mitigate | `toHaveValue` / status text only. |
</threat_model>

<verification>
`just web-player-smoke` must exit 0. Confirm `web/package.json` `test:player` lists `e2e/reset-honesty.spec.ts` and still lists `e2e/shell.spec.ts`.
</verification>

<success_criteria>
- Representative live and construction selects show documented initials after Reset.
- Play/pause does not remount those selects.
- The new spec is on the player-smoke allowlist.
</success_criteria>

<output>
After completion, create `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-05-SUMMARY.md`
</output>
