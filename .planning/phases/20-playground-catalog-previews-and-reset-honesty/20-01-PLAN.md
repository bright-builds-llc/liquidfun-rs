---
phase: 20-playground-catalog-previews-and-reset-honesty
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - web/src/player/runtime.ts
  - web/tests/runtime.test.ts
  - web/tests/controls.test.ts
autonomous: true
requirements: [WEB-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-09-20T17-36-31
generated_at: 2026-09-20T17:56:00Z
must_haves:
  truths:
    - "sceneControlsIdentity always returns a non-empty sceneId:generation string, including generation 0."
    - "An empty construction bag yields no constructionEntriesForScene rows so Reset can rebuild WASM build([]) defaults."
    - "initialPresetValue with an empty bag returns DEFAULT_PRESET_VALUES for emission-rate, body, stir-speed, jet-strength, water-amount, and mix-strength."
  artifacts:
    - path: web/src/player/runtime.ts
      provides: "sceneControlsIdentity plus Reset-honest constructionEntriesForScene comment"
      exports: ["sceneControlsIdentity", "constructionEntriesForScene"]
    - path: web/tests/runtime.test.ts
      provides: "Empty-bag and identity unit tests"
    - path: web/tests/controls.test.ts
      provides: "Documented initialPresetValue cases from DEFAULT_PRESET_VALUES"
  key_links:
    - from: web/src/player/runtime.ts
      to: web/src/catalog/scenes.ts
      via: "sceneControlsIdentity interpolates SceneId"
      pattern: "sceneControlsIdentity"
    - from: web/tests/controls.test.ts
      to: web/src/components/scene-controls.ts
      via: "initialPresetValue uses DEFAULT_PRESET_VALUES"
      pattern: "initialPresetValue"
---

<objective>
Lock the pure Reset-honesty helpers: a truthy Solid remount identity, empty construction bags, and documented preset initials.

Purpose: D-07/D-08 require chrome to match `DEFAULT_PRESET_VALUES` after Reset. App.tsx is 627 lines and cannot absorb identity string logic. Solid `Show` treats `0` as falsy, so the identity must be a non-empty string.
Output: `sceneControlsIdentity`, an updated construction-bag comment, and Vitest coverage. No Solid JSX, no WASM scene edits.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/code-shape.md
@standards/languages/typescript-javascript.md
@.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-CONTEXT.md
@.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-RESEARCH.md
@web/src/player/runtime.ts
@web/src/components/scene-controls.ts
@web/tests/runtime.test.ts
@web/tests/controls.test.ts

<interfaces>
Add this exact export to `web/src/player/runtime.ts`. Do not add DOM or Solid imports.

```ts
export function sceneControlsIdentity(
  sceneId: SceneId,
  generation: number,
): string {
  return `${sceneId}:${generation}`;
}
```

`SceneId` is already imported from `../catalog/scenes`.

Replace the `constructionEntriesForScene` doc comment. Current text is `Last applied construction presets that must survive Reset.` New text must be exactly:

```ts
/** Last applied construction presets until Reset or scene switch clears the bag. */
```

Do not change the function body of `constructionEntriesForScene`. An empty `values` object already returns `[]`.

From `web/src/components/scene-controls.ts` (do not edit this file in this plan):

```ts
export const DEFAULT_PRESET_VALUES: Readonly<Record<string, string>> = {
  "water-amount": "medium",
  gravity: "normal",
  "emission-rate": "medium",
  "launch-speed": "medium",
  "aim-angle": "up",
  body: "wood",
  "mix-strength": "strong",
  "stir-speed": "slow",
  shape: "circle",
  softness: "medium",
  "jet-strength": "medium",
  emission: "on",
};

export function initialPresetValue(
  control: Extract<SceneControl, { kind: "preset" }>,
  maybeValues: Readonly<Record<string, string>> | undefined,
): string;
```
</interfaces>
</context>

<feature>
  <name>Reset identity and documented initials</name>
  <files>web/src/player/runtime.ts, web/tests/runtime.test.ts, web/tests/controls.test.ts</files>
  <behavior>
    Pure helpers only. Cases:
    - `sceneControlsIdentity("fountain", 1)` equals `"fountain:1"`
    - `sceneControlsIdentity("dam-break", 0)` equals `"dam-break:0"` and is not `""`
    - `constructionEntriesForScene(dam-break, {})` equals `[]`
    - `initialPresetValue` with `{}` or `undefined` returns the DEFAULT_PRESET_VALUES id for the D-08 representative controls
    - `initialPresetValue` with `{ "water-amount": "large" }` returns `"large"`
  </behavior>
  <implementation>
    Add `sceneControlsIdentity` in `runtime.ts`. Keep `constructionEntriesForScene` body unchanged. Do not import Solid, Playwright, or WASM. Do not edit `crates/liquidfun-wasm/**`.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove documented initials and an empty construction bag</name>
  <files>web/tests/controls.test.ts, web/tests/runtime.test.ts</files>
  <read_first>
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-CONTEXT.md` D-07, D-08
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-RESEARCH.md` Pattern 4 table
    1. `web/src/components/scene-controls.ts`
    1. `web/src/catalog/scenes.ts` preset ids
    1. `web/tests/controls.test.ts`
    1. `web/tests/runtime.test.ts`
    1. `standards/core/testing.md`
  </read_first>
  <behavior>
    1. Test: `initialPresetValue` on Fountain `emission-rate` with `{}` returns `"medium"`.
    1. Test: Float or Sink `body` with `undefined` maybeValues returns `"wood"`.
    1. Test: Color Mixer `stir-speed` with `{}` returns `"slow"`.
    1. Test: Water Wheel `jet-strength` with `{}` returns `"medium"`.
    1. Test: Dam Break `water-amount` with `{}` returns `"medium"`.
    1. Test: Color Mixer `mix-strength` with `{}` returns `"strong"`.
    1. Test: Dam Break `water-amount` with `{ "water-amount": "large" }` returns `"large"`.
    1. Test: `constructionEntriesForScene(maybeSceneById("dam-break"), {})` equals `[]`.
    1. Test: `constructionEntriesForScene(maybeSceneById("color-mixer"), {})` equals `[]`.
  </behavior>
  <action>
    Per D-08, write Arrange/Act/Assert Vitest cases first, one concern each. Import `initialPresetValue` from `../src/components/scene-controls` and load real preset controls from `maybeSceneById` in `web/src/catalog/scenes.ts` (do not duplicate option tables). Do not change `DEFAULT_PRESET_VALUES` values. Do not import Solid JSX. Do not edit `SceneControls.tsx`. Keep the existing `constructionHintVisible` and copy tests. After tests exist, run them; empty-bag and default-value cases should pass against current helpers without production edits in this task.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/controls.test.ts tests/runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "initialPresetValue" web/tests/controls.test.ts` matches.
    1. `rg -n "emission-rate|stir-speed|jet-strength|water-amount|mix-strength" web/tests/controls.test.ts` matches those ids.
    1. `rg -n 'constructionEntriesForScene\\(maybeScene, \\{\\}\\)|constructionEntriesForScene\\([^,]+, \\{\\}\\)' web/tests/runtime.test.ts` matches.
    1. `rg -n "must survive Reset" web/tests/runtime.test.ts web/tests/controls.test.ts` is empty.
    1. `cd web && bun run test:unit -- tests/controls.test.ts tests/runtime.test.ts` exits 0.
  </acceptance_criteria>
  <done>Documented initials and empty construction bags are unit-tested without a browser.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add sceneControlsIdentity and the Reset bag comment</name>
  <files>web/src/player/runtime.ts, web/tests/runtime.test.ts</files>
  <read_first>
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-RESEARCH.md` Pattern 3 and Pitfall 4
    1. `web/src/player/runtime.ts`
    1. `web/tests/runtime.test.ts`
    1. `standards/core/code-shape.md`
  </read_first>
  <behavior>
    1. Test: `sceneControlsIdentity("fountain", 1)` equals `"fountain:1"`.
    1. Test: `sceneControlsIdentity("dam-break", 0)` equals `"dam-break:0"`.
    1. Test: `sceneControlsIdentity("water-wheel", 0) === ""` is false.
    1. Test: `sceneControlsIdentity("color-mixer", 2)` equals `"color-mixer:2"`.
  </behavior>
  <action>
    Per D-07, write the failing identity tests first, then add `export function sceneControlsIdentity(sceneId: SceneId, generation: number): string` using exactly `` `${sceneId}:${generation}` ``. Do not stringify generation alone. Do not return `""` for any SceneId. Replace the `constructionEntriesForScene` comment with `Last applied construction presets until Reset or scene switch clears the bag.` Do not change that function body. Do not import `solid-js`. Do not edit `App.tsx` in this plan.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/runtime.test.ts tests/controls.test.ts &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "export function sceneControlsIdentity" web/src/player/runtime.ts` matches.
    1. `rg -n "sceneId:\\$\\{generation\\}|\\`\\$\\{sceneId\\}:\\$\\{generation\\}\\`" web/src/player/runtime.ts` matches the template.
    1. `rg -n "until Reset or scene switch clears the bag" web/src/player/runtime.ts` matches.
    1. `rg -n "must survive Reset" web/src/player/runtime.ts` is empty.
    1. `rg -n "solid-js|ProofSession|innerHTML" web/src/player/runtime.ts` is empty.
    1. `cd web && bun run test:unit -- tests/runtime.test.ts tests/controls.test.ts` exits 0.
    1. `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Keyed remount can pass a truthy epoch string without growing App.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preset id string → select value | Catalog option ids are application-authored; tests must not accept arbitrary caller markup. |
| Construction bag → WASM applyControl | Empty bag must not re-apply stale Large/High construction tokens after Reset. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | T | `constructionEntriesForScene` | mitigate | Empty `{}` returns `[]`; Reset later assigns a new object. |
| T-20-01-02 | T | `initialPresetValue` | mitigate | Allowlist against `control.values`; unknown bag tokens fall through to `DEFAULT_PRESET_VALUES`. |
| T-20-01-03 | S | `sceneControlsIdentity` | mitigate | Identity is `${sceneId}:${generation}` from allowlisted `SceneId`, not hash-route raw text. |
| T-20-01-04 | I | runtime helpers | mitigate | No engine pointers; pure strings and tuples only. |
| T-20-01-05 | D | Solid `Show when={0}` | mitigate | Identity string stays truthy at generation 0. |
| T-20-01-06 | E | WASM constructors | accept | This plan does not edit scene recipes (D-09). |
</threat_model>

<verification>
Run `cd web && bun run test:unit -- tests/runtime.test.ts tests/controls.test.ts && bun run typecheck`. Do not run Playwright, wasm-pack, or `just web-player-smoke`.
</verification>

<success_criteria>
- `sceneControlsIdentity` is exported and never `""`.
- Empty construction bags emit no applyControl entries.
- Representative D-08 initials resolve through `DEFAULT_PRESET_VALUES`.
</success_criteria>

<output>
After completion, create `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-01-SUMMARY.md`
</output>
