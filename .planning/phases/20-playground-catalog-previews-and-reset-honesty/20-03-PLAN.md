---
phase: 20-playground-catalog-previews-and-reset-honesty
plan: "03"
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - web/src/App.tsx
  - web/src/components/PlayerSceneChrome.tsx
  - web/src/player/viewport.ts
autonomous: true
requirements: [WEB-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-09-20T17-36-31
generated_at: 2026-09-20T17:56:00Z
must_haves:
  truths:
    - "Reset scene and Retry scene assign constructionValues = {} then bump resetGeneration then call startScene, so selects remount on DEFAULT_PRESET_VALUES."
    - "Play scene and Pause scene do not clear constructionValues and do not bump resetGeneration."
    - "SceneControls remounts through Solid Show keyed on sceneControlsIdentity, not a React key prop."
    - "App.tsx stays at or under 628 physical lines."
  artifacts:
    - path: web/src/App.tsx
      provides: "resetGeneration signal and recreateScene bag clear"
    - path: web/src/components/PlayerSceneChrome.tsx
      provides: "Keyed SceneControls plus SceneCredits"
      exports: ["PlayerSceneChrome"]
    - path: web/src/player/viewport.ts
      provides: "prefersReducedMotion and isUsableViewport extracted from App"
      exports: ["prefersReducedMotion", "isUsableViewport"]
  key_links:
    - from: web/src/App.tsx
      to: web/src/components/PlayerSceneChrome.tsx
      via: "PlayerPanel children render PlayerSceneChrome"
      pattern: "PlayerSceneChrome"
    - from: web/src/components/PlayerSceneChrome.tsx
      to: web/src/player/runtime.ts
      via: "Show keyed when={sceneControlsIdentity(scene.id, resetGeneration)}"
      pattern: "sceneControlsIdentity"
    - from: web/src/App.tsx
      to: web/src/App.tsx
      via: "recreateScene clears constructionValues then bumps resetGeneration"
      pattern: "setResetGeneration"
---

<objective>
Make Reset (and Retry) rebuild documented physics and remount preset selects without growing App.tsx past the 628-line file-lengths gate.

Purpose: D-07, D-08, D-09. Native reconstruct already exists; pendingValue is a one-time createSignal snapshot. Solid does not remount on a React-style `key=` prop.
Output: extracted viewport helpers, `PlayerSceneChrome` with `Show keyed`, and `recreateScene` that clears the construction bag. No WASM recipe edits.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/code-shape.md
@standards/languages/typescript-javascript.md
@.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-CONTEXT.md
@.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-RESEARCH.md
@.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-UI-SPEC.md
@web/src/App.tsx
@web/src/components/SceneControls.tsx
@web/src/player/runtime.ts

<interfaces>
`web/src/player/viewport.ts` must export exactly:

```ts
export function prefersReducedMotion(): boolean {
  return window.matchMedia("(prefers-reduced-motion: reduce)").matches;
}

export function isUsableViewport(width: number, height: number): boolean {
  return Number.isFinite(width) && Number.isFinite(height) && width > 0 && height > 0;
}
```

Move those two functions out of `App.tsx` and import them. Do not change their behavior.

`web/src/components/PlayerSceneChrome.tsx` must export `PlayerSceneChrome` with this contract:

```ts
export type PlayerSceneChromeProps = {
  readonly scene: SceneRecord;
  readonly resetGeneration: number;
  readonly disabled: boolean;
  readonly maybeValues: Readonly<Record<string, string>>;
  readonly onApplyControl: (name: string, value: string) => void;
  readonly onApplyAction: (name: string) => void;
};
```

Render:

1. `<Show when={sceneControlsIdentity(props.scene.id, props.resetGeneration)} keyed>` wrapping a callback that returns `<SceneControls controls={props.scene.controls} disabled={props.disabled} maybeValues={props.maybeValues} onApplyControl={props.onApplyControl} onApplyAction={props.onApplyAction} />`.
1. `<SceneCredits implementationPath={props.scene.credits.implementationPath} inspiration={props.scene.credits.inspiration} />` **outside** the keyed Show so credits do not remount with selects.

Do not put `key={` on `SceneControls`. Do not import React.

In `App`:

1. `const [resetGeneration, setResetGeneration] = createSignal(1);` — start at `1`, never pass the raw number as `Show` `when`.
1. `recreateScene` body after the ready-id guard, in this order:
   1. `constructionValues = {};` (new object, do not mutate keys in place)
   1. `setResetGeneration((current) => current + 1);`
   1. `void startScene(maybeReadyId);`
1. Keep `onReset={recreateScene}` and `onRetry={recreateScene}`.
1. `playScene` and `pauseScene` must not mention `constructionValues` or `setResetGeneration`.
1. Keep the `startScene` loop over `constructionEntriesForScene(maybeScene, constructionValues)` so Apply-setting bags still apply until Reset clears them.
1. Replace the inner `SceneControls` plus `SceneCredits` JSX with:

```tsx
<Show when={maybeCurrentScene()}>
  {(currentScene) => (
    <PlayerSceneChrome
      scene={currentScene()}
      resetGeneration={resetGeneration()}
      disabled={sceneControlsDisabled()}
      maybeValues={constructionValues}
      onApplyControl={applySceneControl}
      onApplyAction={applySceneAction}
    />
  )}
</Show>
```

Do not edit `SceneControls.tsx` PresetControl visuals, `APPLY_SETTING_LABEL`, or any file under `crates/liquidfun-wasm/src/scene/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract viewport helpers and keyed PlayerSceneChrome</name>
  <files>web/src/player/viewport.ts, web/src/components/PlayerSceneChrome.tsx, web/src/App.tsx</files>
  <read_first>
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-RESEARCH.md` Pattern 3, Pitfall 4, Pitfall 8
    1. `web/src/App.tsx` `prefersReducedMotion`, `isUsableViewport`, PlayerPanel children
    1. `web/src/components/SceneControls.tsx`
    1. `web/src/components/SceneCredits.tsx`
    1. `web/src/player/runtime.ts` `sceneControlsIdentity`
  </read_first>
  <action>
    Per D-07, create `web/src/player/viewport.ts` and `web/src/components/PlayerSceneChrome.tsx` with the exact contracts in `<interfaces>`. Use Solid `<Show keyed>` from `solid-js`, not a `key=` prop. Import `sceneControlsIdentity` from `../player/runtime`. Remove the two viewport helpers from `App.tsx` and import them from `./player/viewport`. Swap the PlayerPanel inner SceneControls/SceneCredits block for `PlayerSceneChrome` even before Reset wiring if needed, but do not yet add `resetGeneration` if that would leave a missing identifier — this task may introduce `const [resetGeneration, setResetGeneration] = createSignal(1)` so the chrome compiles. Do not change `recreateScene` yet. Do not edit WASM scene files. After edits `wc -l < web/src/App.tsx` must be `<= 628`.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "export function prefersReducedMotion|export function isUsableViewport" web/src/player/viewport.ts` matches both.
    1. `rg -n "function prefersReducedMotion|function isUsableViewport" web/src/App.tsx` is empty.
    1. `rg -n "from \"./player/viewport\"" web/src/App.tsx` matches.
    1. `rg -n "keyed" web/src/components/PlayerSceneChrome.tsx` matches.
    1. `rg -n "sceneControlsIdentity" web/src/components/PlayerSceneChrome.tsx` matches.
    1. `rg -n "key=\\{" web/src/components/PlayerSceneChrome.tsx web/src/App.tsx` is empty.
    1. `rg -n "PlayerSceneChrome" web/src/App.tsx` matches.
    1. `rg -n "<SceneControls" web/src/App.tsx` is empty.
    1. `wc -l < web/src/App.tsx` is `<= 628`.
    1. `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>App.tsx is under the file-lengths cap and SceneControls can remount from a keyed Show.</done>
</task>

<task type="auto">
  <name>Task 2: Clear the construction bag and bump resetGeneration only on recreateScene</name>
  <files>web/src/App.tsx</files>
  <read_first>
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-CONTEXT.md` D-07, D-08, D-09
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-RESEARCH.md` Pattern 4 Reset sequence
    1. `web/src/App.tsx` `recreateScene`, `playScene`, `pauseScene`, `startScene`, `applySceneControl`
  </read_first>
  <action>
    Per D-07, D-08, and D-09, edit `recreateScene` so that after `maybeReadyId` is defined it runs exactly: `constructionValues = {};` then `setResetGeneration((current) => current + 1);` then `void startScene(maybeReadyId);`. Do not clear the bag inside `playScene` or `pauseScene`. Do not bump `resetGeneration` inside `applySceneControl` (Apply setting must keep the pending construction draft). Keep `onRetry={recreateScene}`. Keep the `constructionEntriesForScene` loop in `startScene`. Do not change WEB-04 control ids, `Apply setting` copy, or files under `crates/liquidfun-wasm/src/scene/`. Re-run typecheck and file-lengths.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck &amp;&amp; bun run test:unit -- tests/runtime.test.ts tests/controls.test.ts &amp;&amp; cd .. &amp;&amp; bun scripts/bright-builds-check.ts file-lengths</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "function recreateScene" -A 12 web/src/App.tsx` includes `constructionValues = {}` and `setResetGeneration`.
    1. `rg -n "constructionValues = \\{\\}" web/src/App.tsx` matches at least twice (abandonScene already clears; recreateScene must too).
    1. `rg -n "constructionValues|setResetGeneration" web/src/App.tsx` does not appear between `function playScene` and `function pauseScene` bodies except that pause/play must have zero hits for those identifiers — confirm with `rg -n "function playScene" -A 20 web/src/App.tsx` and `rg -n "function pauseScene" -A 20 web/src/App.tsx` showing no `constructionValues` and no `setResetGeneration`.
    1. `rg -n "onRetry=\\{recreateScene\\}" web/src/App.tsx` matches.
    1. `rg -n "constructionEntriesForScene" web/src/App.tsx` matches.
    1. `git diff --name-only -- crates/liquidfun-wasm/src/scene web/src/components/SceneControls.tsx web/src/components/scene-controls.ts` is empty.
    1. `wc -l < web/src/App.tsx` is `<= 628`.
    1. `bun scripts/bright-builds-check.ts file-lengths` exits 0.
    1. `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Reset/Retry remount honest selects and rebuild from documented initials; play/pause leave drafts alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reset click → construction bag | Clearing the bag decides which tokens reach `applyControl`. |
| resetGeneration → Show remount | Falsy `when` would hide all scene controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-01 | T | `recreateScene` | mitigate | Assign a new `{}` bag before `startScene` so Large/High cannot survive Reset. |
| T-20-03-02 | T | `PlayerSceneChrome` | mitigate | `Show keyed` remounts `pendingValue`; no React `key=`. |
| T-20-03-03 | D | `Show when={0}` | mitigate | Start `resetGeneration` at `1`; `when` is `sceneControlsIdentity` string. |
| T-20-03-04 | I | UI vs world | mitigate | Chrome matches `build([])` defaults after empty-bag startScene. |
| T-20-03-05 | T | Apply setting | mitigate | Do not bump generation on `applySceneControl`; drafts stay until Apply or Reset. |
| T-20-03-06 | E | WASM recipes | accept | D-09: do not edit scene constructors. |
</threat_model>

<verification>
Run `cd web && bun run typecheck && bun run test:unit -- tests/runtime.test.ts tests/controls.test.ts` and `bun scripts/bright-builds-check.ts file-lengths`. Do not run Playwright in this plan.
</verification>

<success_criteria>
- Reset clears constructionValues and remounts SceneControls through keyed Show.
- Play and pause do not remount controls.
- App.tsx is at most 628 lines.
- Native scene files are unmodified.
</success_criteria>

<output>
After completion, create `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-03-SUMMARY.md`
</output>
