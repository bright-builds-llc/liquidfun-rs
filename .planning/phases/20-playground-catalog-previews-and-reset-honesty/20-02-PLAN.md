---
phase: 20-playground-catalog-previews-and-reset-honesty
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/catalog/previews.tsx
  - web/src/components/DemoNavigation.tsx
  - web/src/app.css
autonomous: true
requirements: [WEB-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-09-20T17-36-31
generated_at: 2026-09-20T17:56:00Z
must_haves:
  truths:
    - "Each DemoNavigation hash link contains a token-only inline SVG, the caption Static preview, the scene title, and the locked one-sentence description."
    - "Previews never start a WASM world, load docs/assets/demos WebP files, or use innerHTML."
    - "web/src contains zero .catalog-card, .catalog-grid, or CatalogNav restorations; PlaygroundShell is unchanged."
  artifacts:
    - path: web/src/catalog/previews.tsx
      provides: "ScenePreview switch on SceneId"
      exports: ["ScenePreview"]
    - path: web/src/components/DemoNavigation.tsx
      provides: "Preview plus Static preview inside the existing hash link"
    - path: web/src/app.css
      provides: ".demo-nav-preview and .demo-nav-preview-caption compact rules"
  key_links:
    - from: web/src/components/DemoNavigation.tsx
      to: web/src/catalog/previews.tsx
      via: "ScenePreview sceneId={item.id} inside the hash <a>"
      pattern: "ScenePreview"
    - from: web/src/catalog/previews.tsx
      to: web/src/catalog/scenes.ts
      via: "switch on SceneId only"
      pattern: "case \"dam-break\""
---

<objective>
Restore compact static SVG previews inside the existing DemoNavigation list items without reviving the Phase 18 card grid.

Purpose: WEB-01 and D-01..D-06. The Kobalte shell already lists six titles, descriptions, and `#/scene/{id}` links. This plan adds the missing still illustrations.
Output: `ScenePreview`, compact `.demo-nav-preview*` CSS, and updated `DemoNavigation`. No Playwright in this plan.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/frontend-ui.md
@standards-overrides.md
@.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-CONTEXT.md
@.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-UI-SPEC.md
@.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-RESEARCH.md
@web/src/components/DemoNavigation.tsx
@web/src/app.css
@web/src/catalog/scenes.ts

<interfaces>
Restore `web/src/catalog/previews.tsx` from git `f054135:web/src/catalog/previews.tsx` with these required diffs from that blob:

1. Do **not** put `class="catalog-preview"` on the SVG. Omit `class` on the `<svg>` (the CSS parent is `.demo-nav-preview`).
1. Keep `viewBox="0 0 160 90"`, `role="presentation"`, and `aria-hidden="true"`.
1. Keep token constants exactly: `WATER = "#39D3C7"`, `MIX_RED = "#F87171"`, `RIGID = "#CBD5E1"`, `JELLY = "#F4F7FA"`, `CANVAS = "#071018"`.
1. Keep the six preview geometries from that commit (Dam Break rect+three lines+circle, Fountain five dots + U path, Float or Sink band + two rects, Color Mixer overlapping circles, Jelly Drop two bars + rounded rect, Water Wheel hub + four paddles + dashed teal jet).
1. Export `ScenePreview({ sceneId }: { readonly sceneId: SceneId })` with an exhaustive `switch` on `props.sceneId`.

`DemoNavigation` list item markup (D-01, D-03, D-05). The whole item stays one `<a class="demo-nav-link" href={item.href}>`. Insert preview above title. Exact child order:

1. `<span class="demo-nav-preview"><ScenePreview sceneId={item.id} /></span>`
1. `<span class="demo-nav-preview-caption">Static preview</span>`
1. existing `<span class="demo-nav-title">{item.title}</span>`
1. existing `<span class="demo-nav-description">{item.description}</span>`

Do not add a nested `Open` button, nested `<button>`, or `aria-hidden` on the caption. Keep `aria-current={item.isCurrent ? "page" : undefined}` and `onClick={() => props.onNavigate?.()}`.

Insert this CSS immediately after the `.demo-nav-description` rule in `web/src/app.css`. Do not add extra blank-line padding beyond one blank line before the first new rule:

```css
.demo-nav-preview {
  display: block;
  width: 100%;
  max-block-size: 72px;
  aspect-ratio: 16 / 9;
  background: #071018;
  border: 1px solid #2A3441;
  border-radius: 8px;
  overflow: hidden;
}
.demo-nav-preview svg {
  display: block;
  width: 100%;
  height: 100%;
}
.demo-nav-preview-caption {
  color: #A9B4C0;
  font-size: 14px;
  font-weight: 400;
  line-height: 1.4;
}
```

Do not change `.demo-nav-description` (frozen 13px). Do not add a 12px caption. Do not copy historical `.catalog-card` / `.catalog-grid` rules. After the CSS edit, `wc -l < web/src/app.css` must be `<= 628`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore token-only ScenePreview SVG</name>
  <files>web/src/catalog/previews.tsx</files>
  <read_first>
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-CONTEXT.md` D-01, D-02, D-03
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-UI-SPEC.md` Catalog Previews
    1. `git show f054135:web/src/catalog/previews.tsx`
    1. `web/src/catalog/scenes.ts` `SceneId`
  </read_first>
  <action>
    Per D-01 and D-02, create `web/src/catalog/previews.tsx` by restoring the `f054135` ScenePreview module. Change only the SVG `class="catalog-preview"` attribute: delete that class (leave the SVG classless). Do not import `ProofSession`, `loadSceneSession`, `requestAnimationFrame`, or any path under `docs/assets/demos`. Do not use `innerHTML`, `<img>`, `<canvas>`, SMIL, or CSS animation. Do not add `id` attributes (sidebar and drawer both mount this). Do not edit native scene recipes. Caption text lives in DemoNavigation, not in this file.
  </action>
  <verify>
    <automated>test -f web/src/catalog/previews.tsx &amp;&amp; rg -n "export function ScenePreview" web/src/catalog/previews.tsx</automated>
  </verify>
  <acceptance_criteria>
    1. `test -f web/src/catalog/previews.tsx` succeeds.
    1. `rg -n "export function ScenePreview" web/src/catalog/previews.tsx` matches.
    1. `rg -n "case \"dam-break\"|case \"fountain\"|case \"float-or-sink\"|case \"color-mixer\"|case \"jelly-drop\"|case \"water-wheel\"" web/src/catalog/previews.tsx` matches all six.
    1. `rg -n "aria-hidden=\"true\"" web/src/catalog/previews.tsx` matches.
    1. `rg -n "catalog-preview|catalog-card|innerHTML|webp|ProofSession|loadSceneSession|requestAnimationFrame" web/src/catalog/previews.tsx` is empty.
    1. `rg -n "id=\"" web/src/catalog/previews.tsx` is empty.
    1. `wc -l < web/src/catalog/previews.tsx` is `<= 628`.
  </acceptance_criteria>
  <done>Six still SVG illustrations exist as a catalog helper with no WASM imports.</done>
</task>

<task type="auto">
  <name>Task 2: Pack compact previews into DemoNavigation and app.css</name>
  <files>web/src/components/DemoNavigation.tsx, web/src/app.css</files>
  <read_first>
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-CONTEXT.md` D-04, D-05, D-06
    1. `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-UI-SPEC.md` Spacing, Typography, Catalog Previews
    1. `web/src/components/DemoNavigation.tsx`
    1. `web/src/components/PlaygroundShell.tsx`
    1. `web/src/app.css` `.demo-nav-*` rules
  </read_first>
  <action>
    Per D-04 and D-05, import `ScenePreview` into `DemoNavigation.tsx` and insert the preview span plus the exact caption text `Static preview` inside the existing hash `<a>`, in the child order from `<interfaces>`. Do not edit `PlaygroundShell.tsx`. Do not restore `CatalogNav.tsx`. Do not add MysticUI, Tailwind, shadcn, or an icon package. Do not bump `@kobalte/core`. Add only the three `.demo-nav-preview*` rules from `<interfaces>` to `web/src/app.css`. Do not add `grid-template-columns: repeat(3`. After CSS, `wc -l < web/src/app.css` must be `<= 628`. Then run typecheck and file-lengths.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck &amp;&amp; cd .. &amp;&amp; bun scripts/bright-builds-check.ts file-lengths</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "ScenePreview" web/src/components/DemoNavigation.tsx` matches.
    1. `rg -n "Static preview" web/src/components/DemoNavigation.tsx` matches.
    1. `rg -n "demo-nav-preview-caption" web/src/components/DemoNavigation.tsx web/src/app.css` matches both files.
    1. `rg -n "Open" web/src/components/DemoNavigation.tsx` is empty.
    1. `rg -n "catalog-card|catalog-grid|CatalogNav" web/src` is empty.
    1. `rg -n "max-block-size: 72px" web/src/app.css` matches.
    1. `rg -n "font-size: 14px" web/src/app.css` matches inside `.demo-nav-preview-caption`.
    1. `test -f web/src/components/PlaygroundShell.tsx` and `git diff --name-only -- web/src/components/PlaygroundShell.tsx` is empty.
    1. `wc -l < web/src/app.css` is `<= 628`.
    1. `cd web && bun run typecheck` exits 0.
    1. `bun scripts/bright-builds-check.ts file-lengths` exits 0.
  </acceptance_criteria>
  <done>Sidebar and drawer list items show compact static previews inside the existing Kobalte shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog markup → DOM | SVG and caption are authored TSX text nodes, not hash-route HTML. |
| Hash href → scene id | Navigation stays `#/scene/${SceneId}` from `sceneNavigationItems`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-01 | T | `previews.tsx` | mitigate | No `innerHTML`; inline SVG elements only; no WebP URLs from hash input. |
| T-20-02-02 | S | caption copy | mitigate | Locked visible text `Static preview`; no live-thumbnail claim. |
| T-20-02-03 | D | catalog thumbnails | mitigate | D-02 forbids per-row WASM sessions; grep ProofSession empty. |
| T-20-02-04 | T | `href` | mitigate | Keep `item.href` from `sceneNavigationItems`; do not concatenate hash raw. |
| T-20-02-05 | I | duplicate SVG ids | mitigate | No `id=` attributes in `previews.tsx` (dual DemoNavigation mount). |
| T-20-02-06 | E | design-system migration | accept | D-06 keeps Kobalte 0.13.12; no new packages. |
</threat_model>

<verification>
Run `cd web && bun run typecheck` and `bun scripts/bright-builds-check.ts file-lengths`. Grep `web/src` for zero `catalog-card`. Do not run Playwright in this plan.
</verification>

<success_criteria>
- Six token-only SVGs render inside DemoNavigation hash links with caption `Static preview`.
- Compact 72px 16:9 frames use `.demo-nav-preview*` only.
- Card-grid classes and PlaygroundShell remain untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/20-playground-catalog-previews-and-reset-honesty/20-02-SUMMARY.md`
</output>
