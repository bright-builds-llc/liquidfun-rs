---
phase: 01-oracle-provenance-and-repository-foundation
plan: "01"
generated_by: gsd-plan-phase
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitmodules
  - third_party/liquidfun
  - reference/upstream-lock.toml
  - reference/source-map.toml
  - docs/decisions/0001-oracle-selection.md
  - UPSTREAM.md
  - THIRD_PARTY_NOTICES.md
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-04
  - DOCS-03
phase_lifecycle_id: 1-2026-07-10T02-00-42
lifecycle_mode: yolo
generated_at: 2026-07-10T02:22:20.953Z
must_haves:
  truths:
    - The canonical oracle is an immutable official commit whose release context and post-release rationale are explicit.
    - The upstream checkout is read-only and every derived local artifact has a traceable provenance policy.
    - License and alteration obligations are recorded before broad translation begins.
  artifacts:
    - path: reference/upstream-lock.toml
      provides: Machine-readable canonical repository, tag, release commit, selected revision, submodule path, and patch-set identity.
    - path: docs/decisions/0001-oracle-selection.md
      provides: Auditable release-to-candidate delta and decision rationale.
    - path: UPSTREAM.md
      provides: Maintainer-facing initialize, verify, update, ancestry, build, license, and patch policy.
  key_links:
    - from: .gitmodules
      to: reference/upstream-lock.toml
      via: Repository URL and submodule path match the lock.
      pattern: third_party/liquidfun|google/liquidfun
    - from: UPSTREAM.md
      to: docs/decisions/0001-oracle-selection.md
      via: Oracle-selection rationale link.
      pattern: 0001-oracle-selection
---

<objective>
Freeze the official LiquidFun oracle at `7f20402173fd143a3988c921bc384459c6a858f2` and create the provenance, ancestry, license, and alteration records that make the selection reproducible and reviewable.

Purpose: eliminate moving-upstream, wrong-ancestry, and lost-notice risk before implementation assumptions harden.
Output: exact submodule pin, lock/source map, oracle ADR, upstream guide, and third-party notices.
</objective>

<threat_model>
- **Protected assets:** canonical revision identity, submodule contents, license notices, and source mappings.
- **Trust boundaries:** remote Git repository to local gitlink; upstream source to translated/local artifacts; maintainer update input to repository records.
- **Threats:** moving-ref substitution, tag/commit confusion, malicious revision input, unreviewed in-place patches, and omitted notices.
- **Mitigations:** accept only full 40-hex revisions, record annotated tag and peeled commit separately, pin the gitlink, classify `patch_set = "none"`, keep upstream read-only, and require source-map/notice entries for derived material.
- **Blocking condition:** any selected revision mismatch, writable patch without a hashed register, or unresolved high-severity license/provenance gap blocks completion.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pin the selected official upstream commit</name>
  <read_first>
    - .planning/phases/01-oracle-provenance-and-repository-foundation/01-CONTEXT.md
    - .planning/phases/01-oracle-provenance-and-repository-foundation/01-RESEARCH.md
    - .gitmodules (if present)
  </read_first>
  <files>.gitmodules, third_party/liquidfun, reference/upstream-lock.toml</files>
  <action>Add `https://github.com/google/liquidfun.git` as submodule `third_party/liquidfun`, detach it at exact commit `7f20402173fd143a3988c921bc384459c6a858f2`, and do not configure a tracking branch. Create `reference/upstream-lock.toml` with `schema_version = 1`, the exact repository URL, `revision = "7f20402173fd143a3988c921bc384459c6a858f2"`, `release_tag = "v1.1.0"`, annotated tag object `d15bcf1879144bf2a4c8ebcc73f6418186756fb2`, `release_commit = "f38db7c627c3dc5ec879d726e16fa5a12ad6e478"`, `submodule_path = "third_party/liquidfun"`, and `patch_set = "none"`. Keep the upstream checkout unmodified.</action>
  <verify>Run `git submodule status third_party/liquidfun`, `git -C third_party/liquidfun rev-parse HEAD`, and a TOML parse/check that asserts every exact value.</verify>
  <acceptance_criteria>
    - `git -C third_party/liquidfun rev-parse HEAD` prints exactly `7f20402173fd143a3988c921bc384459c6a858f2`.
    - `.gitmodules` contains path `third_party/liquidfun` and URL `https://github.com/google/liquidfun.git` and contains no `branch =` entry.
    - `reference/upstream-lock.toml` contains all seven exact identity fields and `patch_set = "none"`.
    - `git -C third_party/liquidfun status --short` prints no output.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Record the oracle decision and maintainer workflow</name>
  <read_first>
    - reference/upstream-lock.toml
    - .planning/phases/01-oracle-provenance-and-repository-foundation/01-RESEARCH.md
    - .planning/research/FEATURES.md
    - .planning/research/PITFALLS.md
  </read_first>
  <files>docs/decisions/0001-oracle-selection.md, UPSTREAM.md</files>
  <action>Write ADR 0001 with status Accepted. Compare release commit `f38db7…` with `7f204…`, explicitly naming the particle-group split memory fix, particle/fixture filtering and test, growable-buffer correction, build/warning fixes, and bindings/docs-only deltas. Select `7f204…` as the behavioral oracle and state that wrapper-only build compatibility is preferred over source patches. Write `UPSTREAM.md` with canonical identifiers, Box2D 2.3.0/revision-280 ancestry evidence, tree scope, read-only rule, initialization/verification commands, intentional 40-hex update procedure, patch policy, build entrypoint placeholder `cargo xtask upstream`, and license/notice locations.</action>
  <verify>Run targeted `rg` checks for all exact SHAs, ancestry, update/patch/license headings, and the ADR link; compare documented values to `reference/upstream-lock.toml`.</verify>
  <acceptance_criteria>
    - ADR status is `Accepted` and contains both full commit SHAs plus the material native changes that justify the selection.
    - `UPSTREAM.md` contains headings for Identity, Ancestry, Initialize, Verify, Intentional Update, Build, Patches, and Licenses and Notices.
    - `UPSTREAM.md` links `docs/decisions/0001-oracle-selection.md` and contains no moving-branch update command.
    - All documented repository/revision/tag values equal `reference/upstream-lock.toml`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Establish source mapping and notice policy</name>
  <read_first>
    - LICENSE
    - third_party/liquidfun/liquidfun/Box2D/License.txt
    - third_party/liquidfun/liquidfun/NOTICE
    - reference/upstream-lock.toml
    - UPSTREAM.md
  </read_first>
  <files>reference/source-map.toml, THIRD_PARTY_NOTICES.md</files>
  <action>Create a versioned `reference/source-map.toml` schema with documented fields for local path, upstream revision/path, derivation kind, alteration summary, and notice class; include a non-derived bootstrap record for the pinned inventory source so the schema is exercised without falsely claiming translated code exists. Create `THIRD_PARTY_NOTICES.md` that preserves the LiquidFun/Box2D notice and identifies developer-only GoogleTest/freeglut licenses without copying those trees into the Cargo package. State that the root MIT license covers original project work and does not replace upstream notice/alteration duties.</action>
  <verify>Parse the TOML, verify required schema keys, and compare notice references to files present in the pinned tree. Run `rg` for the root-license qualification and altered-source policy.</verify>
  <acceptance_criteria>
    - `reference/source-map.toml` declares schema version 1 and the five required mapping fields.
    - `THIRD_PARTY_NOTICES.md` references `liquidfun/Box2D/License.txt`, `liquidfun/NOTICE`, GoogleTest, and freeglut.
    - `THIRD_PARTY_NOTICES.md` states that altered or translated source must carry provenance and alteration records.
    - No upstream file is modified by this task.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Verify exact gitlink and lock agreement.
- Verify the upstream worktree is clean.
- Validate TOML files parse and documentation contains every required identity and policy field.
- Review `git diff --submodule=log` to confirm only the intended submodule pin and repository-owned records changed.
</verification>

<success_criteria>
- Maintainers can identify and reproduce the exact official oracle without a moving reference.
- Release ancestry, material post-release deltas, patches, notices, and intentional updates are explicit.
- Derived artifacts have a machine-readable provenance/alteration contract before translation begins.
</success_criteria>

<output>
After completion, create `01-01-SUMMARY.md` with exact gitlink/lock evidence, commands run, and any unresolved legal caveat.
</output>
