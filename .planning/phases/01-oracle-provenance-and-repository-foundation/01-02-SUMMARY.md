---
phase: 01-oracle-provenance-and-repository-foundation
plan: "02"
subsystem: foundation
tags: [rust, cargo, xtask, msrv, packaging]

requires:
  - phase: 01-oracle-provenance-and-repository-foundation/01
    provides: Immutable upstream oracle and provenance assets that the consumer package must exclude
provides:
  - Resolver-3 workspace with liquidfun as the only default member
  - Dependency-free publishable crate with an explicit Rust 1.92 MSRV and no C++ build path
  - Standard-library xtask dispatcher with typed errors and stable module seams
  - Verified package and temporary-copy isolation from upstream and reference assets
affects: [01-03, 01-04, 01-05, cargo, xtask, packaging, ci]

tech-stack:
  added: [Rust 1.97.0 development toolchain, Cargo resolver 3]
  patterns: [Cargo default-member isolation, private standard-library xtask, typed CLI diagnostics]

key-files:
  created:
    - Cargo.toml
    - Cargo.lock
    - rust-toolchain.toml
    - .cargo/config.toml
    - crates/liquidfun/Cargo.toml
    - crates/liquidfun/src/lib.rs
    - tools/xtask/Cargo.toml
    - tools/xtask/src/main.rs
    - tools/xtask/src/upstream.rs
    - tools/xtask/src/inventory.rs
    - tools/xtask/src/provenance.rs
    - tools/xtask/src/package.rs
  modified: []

key-decisions:
  - "Use version 0.0.0 for both initial packages so repository metadata does not imply physics or release maturity."
  - "Keep xtask dependency-free and dispatch only exact allowlisted commands through typed module seams."
  - "Verify the independently unpacked consumer crate explicitly with Rust 1.92.0 rather than inheriting a host default toolchain."

patterns-established:
  - "Consumer boundary: ordinary Cargo commands select only crates/liquidfun and require no repository-only inputs."
  - "Orchestration seam: cargo xtask delegates exact commands to focused sibling modules without shell command construction."

requirements-completed:
  - FND-05
  - FND-07
  - TEST-09
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-07-10T02-00-42
generated_at: 2026-07-10T02:55:42Z

duration: 8 min
completed: 2026-07-10
---

# Phase 1 Plan 02: Cargo-First Workspace and Private Orchestration Summary

**Resolver-3 Cargo workspace with an isolated dependency-free `liquidfun` package, verified Rust 1.92 consumer boundary, and typed standard-library xtask dispatcher**

## Performance

- **Duration:** 8 min
- **Started:** 2026-07-10T02:47:17Z
- **Completed:** 2026-07-10T02:55:42Z
- **Tasks:** 3
- **Implementation files:** 13

## Accomplishments

- Established Rust 1.97.0, Edition 2024, resolver 3, shared lints, a Rust 1.92
  publishable-crate MSRV, and `liquidfun` as the sole default Cargo member.
- Created a truthful, dependency-free `liquidfun` skeleton with no build script,
  no physics or parity claim, and unsafe code forbidden at both workspace and
  crate boundaries.
- Added a private standard-library-only xtask with exact `upstream`,
  `inventory`, `provenance`, `package`, and `check` commands, stable help and
  failure diagnostics, and focused dispatch tests.
- Proved that default builds and an independently unpacked package work without
  `third_party/` or `reference/`, including a successful Rust 1.92.0 package
  build, test, and documentation run.

## Task Commits

1. **Task 1: Establish the pinned workspace and publishable crate** -
   `ebd804b` (`chore`)
2. **Task 2: Create the private modular xtask dispatcher** - `21b95dd`
   (`feat`)
3. **Task 3: Prove default-member and MSRV isolation** - no new tracked
   changes; the shared lockfile was generated in `ebd804b` because Cargo
   required it during Task 1, and Task 3 supplied verification evidence only.

## Files Created/Modified

- `.gitignore` - Excludes the shared Cargo target directory.
- `Cargo.toml` - Resolver-3 workspace, default-member boundary, shared package
  metadata, and warning-conscious lints.
- `Cargo.lock` - Shared dependency-free workspace lockfile generated by Rust
  1.97.0.
- `rust-toolchain.toml` - Exact Rust 1.97.0 development pin with rustfmt and
  Clippy.
- `.cargo/config.toml` - Thin `cargo xtask` alias.
- `crates/liquidfun/Cargo.toml` - Publishable Rust 1.92 crate with narrow
  package contents and no dependencies or build script.
- `crates/liquidfun/src/lib.rs` - Safe, documented, truthfully incomplete crate
  root.
- `tools/xtask/Cargo.toml` - Unpublished, dependency-free orchestration package.
- `tools/xtask/src/main.rs` - Typed allowlisted dispatcher, help output, exit
  behavior, and focused unit tests.
- `tools/xtask/src/{upstream,inventory,provenance,package}.rs` - Stable module
  seams for later Phase 1 plans.

## Decisions Made

- Used package version `0.0.0` so the initial manifests do not imply feature or
  release maturity before physics exists.
- Kept all orchestration on the standard library and represented CLI failure as
  typed `XtaskError` values, preserving inspectable argument handling and
  avoiding shell construction.
- Kept the four planned orchestration areas as explicit modules even though
  their current bodies are placeholders; this is the smallest structure that
  preserves the later plan seams without introducing Phase 2 crates.

## Verification Evidence

- `cargo fmt --all --check`, `cargo clippy --all-targets --all-features -- -D
  warnings`, `cargo build --all-targets --all-features`, and `cargo test
  --all-features` passed in the required order.
- `cargo clippy -p xtask --all-targets --all-features -- -D warnings` and
  `cargo test -p xtask` passed; five focused tests cover help, missing input,
  unknown input, all four module delegations, and aggregate-check dispatch.
- `cargo xtask --help` exited zero. Missing, unknown, and recognized placeholder
  commands exited nonzero with stable diagnostics and no panic.
- Cargo metadata contained exactly two workspace members and selected only
  `liquidfun` as the default member.
- `cargo +1.92.0 check -p liquidfun --all-features` passed locally with Rust
  1.92.0 (`rustc ded5c06cf 2025-12-08`).
- A clean temporary archive of `HEAD` passed default build, test, documentation,
  and package verification after `third_party/` and `reference/` were removed.
- The package list contained only Cargo metadata/lock files, `README.md`, and
  `src/lib.rs`; it contained no tooling, upstream/reference path, CMake file, or
  C++ source/header.
- An independently unpacked package passed build, test, and documentation on
  Rust 1.92.0.
- Source inspection found no `unwrap()`, shell interpreter construction,
  `bash -c`, or `cmd /C`; `git diff --check` also passed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Materialized the declared xtask member during Task 1**

- **Found during:** Task 1 (Establish the pinned workspace and publishable crate)
- **Issue:** The required root workspace listed `tools/xtask`, but Task 2 owned
  creation of that member. Cargo metadata/build/package commands reject a
  workspace whose declared member manifest is absent.
- **Fix:** Added the minimal private xtask package shell during Task 1, then
  replaced its no-op entrypoint with the planned dispatcher in Task 2. Cargo.lock
  was consequently generated and committed with Task 1 rather than Task 3.
- **Files modified:** `tools/xtask/Cargo.toml`, `tools/xtask/src/main.rs`,
  `Cargo.lock`
- **Verification:** Task 1 Cargo metadata/build/test/package checks passed; Task
  2 xtask checks passed after the dispatcher replaced the shell.
- **Committed in:** `ebd804b` and `21b95dd`

**2. [Rule 3 - Blocking] Ignored Cargo build output**

- **Found during:** Task 1 (Establish the pinned workspace and publishable crate)
- **Issue:** The greenfield repository had no `.gitignore`, leaving `target/`
  as untracked generated output after mandatory verification.
- **Fix:** Added a root `/target/` ignore rule.
- **Files modified:** `.gitignore`
- **Verification:** Repeated Cargo verification left no generated files in git
  status.
- **Committed in:** `ebd804b`

**Total deviations:** 2 auto-fixed (2 blocking).
**Impact on plan:** Both fixes were minimal prerequisites for a valid, clean
Cargo workspace. They introduced no consumer dependencies, C++ access, or
unplanned architecture.

## Issues Encountered

- The first unpacked-package smoke directory was nested under the temporary
  workspace, so Cargo correctly treated it as an unlisted workspace member.
  Moving the unpacked crate to a separate temporary root resolved the issue.
- That independent root exposed the machine's default Rust 1.91.1, which is
  below the declared MSRV and correctly failed. Selecting the installed Rust
  1.92.0 toolchain made the intended MSRV contract explicit and passed.
- The available `mdformat` installation interpreted YAML frontmatter as
  Markdown and rewrote its delimiters. The summary was restored with
  `apply_patch`, then its frontmatter was parsed and checked directly instead.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Ready for `01-03-PLAN.md` to replace the `upstream` placeholder with pinned
  lock/gitlink validation and repository-owned CMake/Ninja orchestration.
- Ready for later inventory, provenance, and package plans to deepen their
  existing module seams without changing ordinary Cargo defaults.
- No blocker remains for the Cargo-first consumer boundary.

## Self-Check: PASSED

- All key workspace, package, alias, and xtask module files exist.
- Two scoped `01-02` implementation commits are present in git history.
- Summary lifecycle metadata matches the plan, and all three requirement IDs
  are copied verbatim.
- Protected orchestrator-owned `.planning/config.json`, `.planning/STATE.md`,
  and `.planning/ROADMAP.md` remain unstaged and uncommitted.

***

_Phase: 01-oracle-provenance-and-repository-foundation_
_Completed: 2026-07-10_
