---
phase: 01-oracle-provenance-and-repository-foundation
plan: "05"
generated_by: gsd-plan-phase
type: execute
wave: 3
depends_on:
  - "03"
  - "04"
files_modified:
  - tools/xtask/src/main.rs
  - justfile
  - .github/workflows/ci.yml
  - .github/workflows/oracle.yml
  - ARCHITECTURE.md
  - TESTING.md
  - README.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - FND-03
  - FND-05
  - FND-07
  - FND-08
  - TEST-09
  - DOCS-03
phase_lifecycle_id: 1-2026-07-10T02-00-42
lifecycle_mode: yolo
generated_at: 2026-07-10T02:22:20.953Z
must_haves:
  truths:
    - Contributors can discover transparent fast, package, inventory, provenance, and oracle workflows from the root.
    - CI separates Cargo-only consumer proof from submodule/CMake oracle proof and pins external actions by full SHA.
    - Public architecture/testing/status documentation matches verified Phase 1 maturity and does not claim physics parity.
  artifacts:
    - path: justfile
      provides: Thin aliases over documented Cargo and xtask commands.
    - path: .github/workflows/ci.yml
      provides: Cargo-only quality/platform/MSRV/package checks with no submodule initialization.
    - path: .github/workflows/oracle.yml
      provides: Provenance and pinned CMake/Ninja oracle smoke lane.
    - path: ARCHITECTURE.md
      provides: Enforceable dependency direction and native-Rust/oracle-isolation boundaries.
    - path: TESTING.md
      provides: Fast versus expensive verification tiers and exact commands.
  key_links:
    - from: justfile
      to: tools/xtask/src/main.rs
      via: Thin recipes invoke stable aggregate/subcommands.
      pattern: cargo xtask
    - from: .github/workflows/oracle.yml
      to: reference/upstream-lock.toml
      via: CI verifies exact submodule/provenance before build.
      pattern: upstream verify|provenance check
    - from: README.md
      to: COMPATIBILITY.md
      via: Truthful maturity and evidence link.
      pattern: COMPATIBILITY.md
---

<objective>
Integrate the Phase 1 foundation into discoverable local workflows, separated CI lanes, and truthful maintainer/contributor documentation, then run the complete clean verification gate.

Purpose: turn individual foundation mechanisms into a sustainable repository contract that contributors and CI exercise the same way.
Output: aggregate xtask/just commands, Cargo-only and oracle workflows, architecture/testing docs, and updated public contributor guidance.
</objective>

<threat_model>
- **Protected assets:** CI source checkout, dependency caches, provenance gate, package boundary, and public maturity claims.
- **Trust boundaries:** third-party GitHub Actions and package installers; pull-request content to CI commands; caches to build outputs; internal evidence to public documentation.
- **Threats:** mutable action tags, cache poisoning, workflow-file privilege abuse, unverified submodule checkout, command injection, and misleading parity claims.
- **Mitigations:** full action SHAs, least permissions/read-only default tokens, lock-aware cache keys, no secret-dependent PR steps, structured repository commands, provenance check before CMake, and generated compatibility/status links.
- **Blocking condition:** mutable action refs, write permissions without need, unverified oracle build, package leakage, or a public parity/production claim is high severity and blocks completion.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Finish the aggregate check command and thin just recipes</name>
  <read_first>
    - tools/xtask/src/main.rs
    - tools/xtask/src/inventory.rs
    - tools/xtask/src/provenance.rs
    - tools/xtask/src/package.rs
    - tools/xtask/src/upstream.rs
    - justfile (if present)
  </read_first>
  <files>tools/xtask/src/main.rs, justfile</files>
  <action>Implement `cargo xtask check` as a fail-fast ordered aggregate of inventory check, package verify, and—only when the submodule is initialized—upstream/provenance checks; absence of the optional submodule must be reported as a clearly labeled Cargo-only skip, while an initialized mismatch is a hard failure. Create a root `justfile` whose default recipe lists commands and whose thin recipes are exactly `check`, `fmt`, `clippy`, `build`, `test`, `package-verify`, `inventory-check`, `upstream-verify`, and `oracle-debug`; each recipe prints and invokes one documented Cargo/xtask command without embedding validation logic.</action>
  <verify>Run `just --list`, `just check`, inspect recipes for loops/conditionals, and run aggregate check both with the valid submodule and in a temporary Cargo-only copy without it.</verify>
  <acceptance_criteria>
    - `cargo xtask check` fails on any initialized provenance/inventory/package error and succeeds in a clean checkout.
    - Cargo-only absence of `third_party/liquidfun` is an explicit skip, not an error or silent success.
    - `just --list` shows all nine exact recipes and the default command returns quickly.
    - `justfile` contains no substantive loops, inline scripts, CMake logic, or hidden error swallowing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add separated Cargo-only and oracle CI workflows</name>
  <read_first>
    - Cargo.toml
    - rust-toolchain.toml
    - justfile
    - reference/upstream-lock.toml
    - tools/reference/CMakePresets.json
    - .github/workflows/ci.yml (if present)
    - .github/workflows/oracle.yml (if present)
  </read_first>
  <files>.github/workflows/ci.yml, .github/workflows/oracle.yml</files>
  <action>Create least-privilege GitHub Actions workflows using `actions/checkout` pinned to `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0` (v7) and `actions/cache` pinned to `55cc8345863c7cc4c66a329aec7e433d2d1c52a9` (v6), never mutable tags. `ci.yml` uses `permissions: contents: read`, concurrency cancellation, and no submodules; run Linux format/Clippy/build/test/docs/inventory/package checks, default-feature build/test on Ubuntu/macOS/Windows, and an MSRV 1.92 `liquidfun` check. `oracle.yml` explicitly initializes submodules, installs/validates CMake 4.3.3 and Ninja 1.13.2 through version-pinned commands, validates Clang 22 for the canonical Linux job, runs upstream/provenance/inventory checks, then configures/builds `oracle-debug`. Add manual portability oracle jobs for macOS and Windows using documented native compilers without publishing canonical artifacts. Do not run secrets or write permissions on pull requests.</action>
  <verify>Parse YAML, assert all `uses:` refs contain 40-hex SHAs, inspect permissions/submodule separation, and run every underlying local command available. Use a workflow linter already present on PATH if available; do not install one solely for this task.</verify>
  <acceptance_criteria>
    - Every external `uses:` entry is pinned to a 40-hex SHA; checkout/cache friendly majors appear only in comments/names.
    - Cargo-only jobs do not initialize submodules or install/run CMake.
    - Oracle job runs `upstream verify` and `provenance check` before CMake configure/build.
    - Workflows default to `contents: read`, use no repository secrets, and contain no write permission.
    - Linux canonical job asserts exact CMake 4.3.3, Ninja 1.13.2, and Clang major 22 identities.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Publish truthful architecture, testing, and contributor guidance</name>
  <read_first>
    - .planning/PROJECT.md
    - .planning/phases/01-oracle-provenance-and-repository-foundation/01-CONTEXT.md
    - UPSTREAM.md
    - COMPATIBILITY.md
    - README.md
    - CONTRIBUTING.md
  </read_first>
  <files>ARCHITECTURE.md, TESTING.md, README.md, CONTRIBUTING.md</files>
  <action>Write `ARCHITECTURE.md` with dependency direction: published native Rust `liquidfun` is independent; private xtask owns effects; CMake/upstream is read-only development oracle; compatibility/provenance records are repository evidence; Phase 2 adds process protocol later. Document Cargo default-member and renderer independence. Write `TESTING.md` with exact fast local sequence, package/inventory/provenance/oracle commands, CI lane mapping, deterministic no-retry policy, and scheduled/manual expensive suite policy. Update README with honest foundation-only status, Cargo-only quick start, links to UPSTREAM/COMPATIBILITY/ARCHITECTURE/TESTING, and no parity/production claim. Update CONTRIBUTING with prerequisites, submodule initialization, just/xtask discovery, required Rust pre-commit order, oracle workflow, generated-file ownership, and source-map/license duties.</action>
  <verify>Run targeted `rg` checks for all boundaries/commands/links and scan public docs for unqualified `full parity`, `production-ready`, or `complete` claims. Run any existing Markdown checker in check mode if already available.</verify>
  <acceptance_criteria>
    - `ARCHITECTURE.md` explicitly prohibits production C++ delegation and C++ from `liquidfun/build.rs`/default features.
    - `TESTING.md` lists the required Rust order and separates fast PR, oracle, and scheduled/manual suites.
    - README labels the project as foundation/incomplete and links all four evidence/architecture docs.
    - CONTRIBUTING documents generated-file ownership, exact submodule verification, and provenance/notice requirements.
    - No public document claims verified physics parity or production readiness.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Run the complete Phase 1 verification and simplification pass</name>
  <read_first>
    - AGENTS.md
    - justfile
    - TESTING.md
    - .github/workflows/ci.yml
    - .github/workflows/oracle.yml
    - all files listed in Plans 01-05 `files_modified`
  </read_first>
  <files></files>
  <action>Run the repository's complete clean verification: format, Clippy denied warnings, build all targets/features, all-feature tests, docs with denied warnings, inventory check, upstream verify, provenance check, package verify, CMake oracle-debug configure/build, and MSRV check when available. Run a Cargo-only temporary-copy proof without submodule/reference inputs. Review the total diff and simplify duplicated command logic, shallow abstractions, hidden shell snippets, stale generated output, and misleading documentation without expanding scope. Do not commit or claim completion while any required command fails; record only genuinely unavailable platform-specific checks as explicit residual CI evidence.</action>
  <verify>All listed commands exit 0, `git diff --check` exits 0, generated check mode leaves no diff, upstream stays clean, and package/Cargo-only smoke passes.</verify>
  <acceptance_criteria>
    - `cargo fmt --all --check`, Clippy with `-D warnings`, build-all-targets/features, and all-feature tests all exit 0 in order.
    - Inventory, upstream, provenance, package, and CMake oracle-debug commands exit 0.
    - Cargo-only unpacked/temp-copy checks pass without `third_party/` or `reference/`.
    - `git diff --check` exits 0; no generated output is stale; no upstream file is dirty.
    - The final diff contains no broad physics implementation or Phase 2 protocol code.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Execute every documented local fast and oracle command from a clean working tree.
- Validate action SHA pins, permissions, Cargo-only isolation, exact oracle identity, deterministic generated files, and public maturity wording.
- Review all Phase 1 requirement IDs against concrete artifacts and command evidence.
</verification>

<success_criteria>
- Local and CI workflows expose the same transparent foundation commands with strict consumer/oracle separation.
- All Phase 1 provenance, build, inventory, package, CI, architecture, testing, and documentation requirements have passing evidence.
- The repository is ready for Phase 2 planning without claiming or implementing broad physics parity.
</success_criteria>

<output>
After completion, create `01-05-SUMMARY.md` with the final command matrix, CI/documentation integration, simplification review, and residual platform evidence.
</output>
