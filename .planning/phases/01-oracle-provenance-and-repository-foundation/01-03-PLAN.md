---
phase: 01-oracle-provenance-and-repository-foundation
plan: "03"
generated_by: gsd-plan-phase
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - tools/reference/CMakeLists.txt
  - tools/reference/CMakePresets.json
  - tools/xtask/src/upstream.rs
  - tools/xtask/tests/upstream_cli.rs
autonomous: true
requirements:
  - FND-02
  - FND-03
  - FND-08
  - TEST-09
phase_lifecycle_id: 1-2026-07-10T02-00-42
lifecycle_mode: yolo
generated_at: 2026-07-10T02:22:20.953Z
must_haves:
  truths:
    - Contributors can verify, configure, and build the exact read-only oracle through structured xtask commands.
    - CMake legacy-policy adaptation lives outside the pinned upstream tree.
    - Canonical builds record deterministic baseline settings and never use fast-math or native-only CPU targeting.
  artifacts:
    - path: tools/reference/CMakeLists.txt
      provides: Modern wrapper that sets legacy compatibility before adding pinned LiquidFun.
    - path: tools/reference/CMakePresets.json
      provides: Named debug, release, and sanitizer-ready out-of-tree configurations.
    - path: tools/xtask/src/upstream.rs
      provides: Cross-platform tool/submodule validation and structured CMake invocation.
  key_links:
    - from: tools/xtask/src/upstream.rs
      to: reference/upstream-lock.toml
      via: Verify command reads the selected revision and submodule path.
      pattern: upstream-lock.toml
    - from: tools/xtask/src/upstream.rs
      to: tools/reference/CMakePresets.json
      via: Configure/build allowlist named presets.
      pattern: oracle-debug|oracle-release|oracle-asan-ubsan
---

<objective>
Build a reproducible contributor-only CMake/Ninja path for the pinned oracle without leaking C++ into ordinary Cargo workflows.

Purpose: prove the selected source can be validated and built through repository-owned, actionable commands on a modern toolchain.
Output: CMake wrapper/presets, complete `cargo xtask upstream` implementation, and command-level integration tests.
</objective>

<threat_model>
- **Protected assets:** selected revision, build flags, output directory, compiler identity, and upstream source integrity.
- **Trust boundaries:** lock/submodule metadata to subprocess arguments; user-supplied preset/revision to CMake/Git; build outputs to later evidence manifests.
- **Threats:** command injection, path escape, unallowlisted preset flags, source mutation, wrong revision, and nondeterministic fast-math/native tuning.
- **Mitigations:** structured `std::process::Command` arguments, fixed preset allowlist, canonical target directories under `target/reference`, full-SHA comparison, clean-worktree check, and explicit prohibited-flag assertions.
- **Blocking condition:** any untrusted shell evaluation, wrong gitlink/checkout, source mutation, or canonical preset containing `-ffast-math`/`-march=native` is high severity and blocks completion.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add the external CMake wrapper and presets</name>
  <read_first>
    - reference/upstream-lock.toml
    - third_party/liquidfun/liquidfun/Box2D/CMakeLists.txt
    - .planning/phases/01-oracle-provenance-and-repository-foundation/01-RESEARCH.md
    - tools/reference/CMakeLists.txt (if present)
  </read_first>
  <files>tools/reference/CMakeLists.txt, tools/reference/CMakePresets.json</files>
  <action>Create a CMake 3.25+ wrapper that sets `CMAKE_POLICY_VERSION_MINIMUM` to `3.5` before adding `../../third_party/liquidfun/liquidfun/Box2D` as an out-of-tree subdirectory. Keep examples/testbed off by default and expose the upstream Box2D library build. Add schema-valid presets `oracle-debug`, `oracle-release`, and `oracle-asan-ubsan` using Ninja and binary directories under `${sourceParentDir}/../target/reference/<preset>`. Debug enables assertions; release uses ordinary optimization; sanitizer preset requires Clang and adds address/undefined sanitizers. Do not add `-ffast-math`, `-march=native`, downloads, or in-tree output.</action>
  <verify>Run `cmake --list-presets -S tools/reference`, configure `oracle-debug`, and build target `Box2D`; inspect cache/compile commands for prohibited flags and verify `git -C third_party/liquidfun status --short` is empty.</verify>
  <acceptance_criteria>
    - Wrapper sets minimum 3.25 and legacy policy minimum 3.5 before `add_subdirectory`.
    - All three exact preset names appear and use Ninja with outputs below `target/reference/`.
    - `cmake --preset oracle-debug` and `cmake --build --preset oracle-debug --target Box2D` exit 0 on the local supported platform.
    - Canonical debug/release presets contain neither fast-math nor native CPU targeting and do not modify the submodule.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement upstream verify/configure/build commands</name>
  <read_first>
    - tools/xtask/src/main.rs
    - tools/xtask/src/upstream.rs
    - reference/upstream-lock.toml
    - tools/reference/CMakePresets.json
    - UPSTREAM.md
  </read_first>
  <files>tools/xtask/src/upstream.rs</files>
  <action>Implement `cargo xtask upstream verify`, `configure --preset <name>`, and `build --preset <name>`. Verify parses the lock, compares `.gitmodules`, gitlink, checked-out HEAD, clean upstream worktree, and repository URL, and prints actionable mismatch fields without revealing secrets. Configure/build accept only the three named presets and invoke CMake with separate arguments and `current_dir("tools/reference")`; capture nonzero status and stderr as typed errors. Print detected CMake/Ninja/compiler identities and warn, rather than silently mutate, when local versions differ from canonical CMake 4.3.3/Ninja 1.13.2/Clang 22.1.8 or documented floors.</action>
  <verify>Run all success commands plus negative tests for missing submodule, wrong lock SHA, dirty submodule, unknown preset, and subprocess failure; search source for shell evaluation.</verify>
  <acceptance_criteria>
    - `cargo xtask upstream verify` exits 0 only when lock, gitlink, checkout, URL, and clean state agree.
    - Unknown preset exits nonzero and lists exactly `oracle-debug`, `oracle-release`, and `oracle-asan-ubsan`.
    - Configure/build use structured `Command` arguments and never invoke `sh`, `bash`, `cmd /C`, or PowerShell command strings.
    - Diagnostics identify the mismatched field and expected/actual non-secret values.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add upstream command integration coverage</name>
  <read_first>
    - tools/xtask/src/upstream.rs
    - tools/xtask/src/main.rs
    - standards/core/testing.md
    - standards/languages/rust.md
  </read_first>
  <files>tools/xtask/tests/upstream_cli.rs</files>
  <action>Add focused Arrange/Act/Assert integration tests using temporary repository fixtures and fake executable shims passed by explicit environment variables. Test successful identity validation and one concern each for wrong SHA, dirty checkout, missing submodule, URL mismatch, unknown preset, and CMake failure propagation. Keep production validation pure where possible and inject process execution so tests do not require a full C++ build.</action>
  <verify>Run `cargo test -p xtask --test upstream_cli`, then the full Rust verification sequence; run one real local `oracle-debug` configure/build smoke separately.</verify>
  <acceptance_criteria>
    - Each negative condition has a separate test with explicit Arrange/Act/Assert sections.
    - Tests assert stable error categories and nonzero exit behavior, not incidental full stderr formatting.
    - `cargo test -p xtask --test upstream_cli` exits 0.
    - A real wrapper configure/build smoke exits 0 and leaves upstream clean.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Run the Rust pre-commit sequence in required order.
- Run `cargo xtask upstream verify`, configure, and build for `oracle-debug`.
- Validate presets and ensure upstream remains clean and consumer Cargo commands remain C++-free.
</verification>

<success_criteria>
- Contributors have one reproducible, external, modern wrapper for the pinned upstream tree.
- Wrong provenance, unsafe preset input, missing tools, and build failures fail visibly.
- The ordinary `liquidfun` Cargo path remains independent of all reference tooling.
</success_criteria>

<output>
After completion, create `01-03-SUMMARY.md` with tool identities, preset/build evidence, tests, and platform limitations.
</output>
