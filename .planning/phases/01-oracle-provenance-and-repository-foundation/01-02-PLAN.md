---
phase: 01-oracle-provenance-and-repository-foundation
plan: "02"
generated_by: gsd-plan-phase
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - rust-toolchain.toml
  - .cargo/config.toml
  - crates/liquidfun/Cargo.toml
  - crates/liquidfun/src/lib.rs
  - tools/xtask/Cargo.toml
  - tools/xtask/src/main.rs
  - tools/xtask/src/upstream.rs
  - tools/xtask/src/inventory.rs
  - tools/xtask/src/provenance.rs
  - tools/xtask/src/package.rs
autonomous: true
requirements:
  - FND-05
  - FND-07
  - TEST-09
phase_lifecycle_id: 1-2026-07-10T02-00-42
lifecycle_mode: yolo
generated_at: 2026-07-10T02:22:20.953Z
must_haves:
  truths:
    - Plain Cargo operations default to the publishable Rust crate and never discover or build C++.
    - The workspace pins the development compiler and declares a provisional publishable-crate MSRV.
    - Private orchestration has a stable module seam without pulling Phase 2 protocol crates into Phase 1.
  artifacts:
    - path: Cargo.toml
      provides: Resolver-3 workspace with one publishable default member and private xtask member.
    - path: crates/liquidfun/Cargo.toml
      provides: Cargo-only publishable crate boundary with Rust 1.92 MSRV.
    - path: tools/xtask/src/main.rs
      provides: Thin standard-library command dispatcher for upstream, inventory, provenance, package, and aggregate checks.
  key_links:
    - from: .cargo/config.toml
      to: tools/xtask/Cargo.toml
      via: `cargo xtask` alias selects package xtask.
      pattern: run --package xtask
    - from: Cargo.toml
      to: crates/liquidfun/Cargo.toml
      via: default-members isolates ordinary Cargo commands.
      pattern: crates/liquidfun
---

<objective>
Create the minimal Cargo-first Rust workspace and private orchestration seam while proving that ordinary consumers do not inherit C++, the submodule, reference data, or developer tooling.

Purpose: establish the independent publishable product boundary before reference tooling grows.
Output: pinned toolchain, resolver-3 workspace, skeleton `liquidfun` crate, and modular private `xtask` dispatcher.
</objective>

<threat_model>
- **Protected assets:** consumer dependency graph, package contents, command arguments, and reproducible toolchain identity.
- **Trust boundaries:** CLI input to xtask modules; workspace tooling to publishable crate; repository-only paths to packaged archive.
- **Threats:** C++ leakage through build scripts/features, shell injection, path traversal, accidental tooling publication, and compiler drift.
- **Mitigations:** no `build.rs`, no shell command concatenation, allowlisted subcommands, `publish = false` for xtask, explicit `default-members`, package include/exclude policy, and exact toolchain/MSRV values.
- **Blocking condition:** any Cargo default path that touches CMake/submodules/reference data, or any high-severity CLI injection/package-leakage risk, blocks completion.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Establish the pinned workspace and publishable crate</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/01-oracle-provenance-and-repository-foundation/01-CONTEXT.md
    - .planning/phases/01-oracle-provenance-and-repository-foundation/01-RESEARCH.md
    - Cargo.toml (if present)
  </read_first>
  <files>Cargo.toml, Cargo.lock, rust-toolchain.toml, crates/liquidfun/Cargo.toml, crates/liquidfun/src/lib.rs</files>
  <action>Create a resolver-3 workspace with `members = ["crates/liquidfun", "tools/xtask"]` and `default-members = ["crates/liquidfun"]`. Centralize edition `2024`, rust-version `1.92`, license `MIT`, repository metadata, and warning-conscious workspace lints. Pin `rust-toolchain.toml` to channel `1.97.0` with `rustfmt` and `clippy`. Create package `liquidfun` with no runtime dependencies, no build script, no default feature that reaches tooling, and package include rules limited to its manifest, source, repository license/readme/notice files that actually exist. Add a documented safe skeleton `lib.rs` that truthfully states no physics parity is implemented yet and denies unsafe code.</action>
  <verify>Run `cargo metadata --no-deps`, `cargo build`, `cargo test`, `cargo package -p liquidfun --allow-dirty --list`, and assert metadata default-members contains only `liquidfun`.</verify>
  <acceptance_criteria>
    - Root `Cargo.toml` uses resolver `3` and exact default member `crates/liquidfun`.
    - `crates/liquidfun/Cargo.toml` declares edition 2024, rust-version 1.92, no `build` key, and no dependencies.
    - `rust-toolchain.toml` pins 1.97.0 and includes rustfmt/clippy.
    - `cargo build`, `cargo test`, and `cargo package -p liquidfun --allow-dirty --list` exit 0 without invoking CMake or accessing `third_party/`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create the private modular xtask dispatcher</name>
  <read_first>
    - Cargo.toml
    - tools/xtask/Cargo.toml (if present)
    - standards/core/code-shape.md
    - standards/languages/rust.md
  </read_first>
  <files>.cargo/config.toml, tools/xtask/Cargo.toml, tools/xtask/src/main.rs, tools/xtask/src/upstream.rs, tools/xtask/src/inventory.rs, tools/xtask/src/provenance.rs, tools/xtask/src/package.rs</files>
  <action>Create unpublished package `xtask` using only the standard library initially. Add Cargo alias `xtask = "run --package xtask --"`. Implement a thin dispatcher with modules `upstream`, `inventory`, `provenance`, and `package`; recognize top-level commands `upstream`, `inventory`, `provenance`, `package`, and `check`, returning typed nonzero errors for unknown/missing input. Each module exposes a `run(&[String]) -> Result<(), XtaskError>` seam and initially returns an explicit `not implemented by this plan` error for recognized module commands. Do not build shell command strings and do not use `unwrap()`.</action>
  <verify>Run `cargo build -p xtask`, `cargo test -p xtask`, `cargo xtask --help`, and negative commands that must fail with stable diagnostics rather than panic.</verify>
  <acceptance_criteria>
    - `.cargo/config.toml` contains alias `run --package xtask --`.
    - `tools/xtask/Cargo.toml` contains `publish = false`.
    - Main dispatcher recognizes all five exact command names and delegates to named modules.
    - Unknown and missing commands exit nonzero with help text; source contains no `unwrap()` and no `Command::new("sh")`/`bash -c`/`cmd /C` construction.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Prove default-member and MSRV isolation</name>
  <read_first>
    - Cargo.toml
    - rust-toolchain.toml
    - crates/liquidfun/Cargo.toml
    - tools/xtask/Cargo.toml
  </read_first>
  <files>Cargo.lock</files>
  <action>Generate and commit the shared lockfile with Rust 1.97.0. Run ordinary default-member build/test/doc/package checks from a temporary copy in which `third_party/` and `reference/` are absent. Run `cargo +1.92.0 check -p liquidfun --all-features` when the toolchain is available; if it cannot be installed locally, record the exact missing prerequisite in the summary and ensure a later CI task owns it rather than weakening `rust-version`. Do not add a dependency merely to test the lockfile.</action>
  <verify>Run `cargo metadata --format-version 1 --no-deps`, default `cargo build/test/doc`, package listing, and the temporary-copy isolation check; run the MSRV check or capture the exact unavailable-toolchain evidence.</verify>
  <acceptance_criteria>
    - `Cargo.lock` exists and default Cargo commands select only package `liquidfun`.
    - Cargo-only checks pass in a copy without `third_party/` and `reference/`.
    - `cargo package --list` contains no `tools/`, `third_party/`, `reference/`, CMake file, or C++ source/header.
    - MSRV verification either exits 0 locally or is explicitly assigned to a concrete CI job without changing the declared 1.92 value.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Run `cargo fmt --all --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` in that order.
- Inspect Cargo metadata/default members and packaged file list.
- Repeat the consumer checks with repository-only upstream/reference paths absent.
</verification>

<success_criteria>
- The repository has one deep publishable crate and one private orchestration crate with resolver-3/default-member isolation.
- Normal Cargo use remains independent of C++ and upstream inputs.
- Toolchain, MSRV, safety, package, and command-dispatch contracts are explicit and verifiable.
</success_criteria>

<output>
After completion, create `01-02-SUMMARY.md` with metadata/package evidence and the local or CI-backed MSRV result.
</output>
