---
phase: 01-oracle-provenance-and-repository-foundation
plan: "04"
generated_by: gsd-plan-phase
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - Cargo.toml
  - Cargo.lock
  - tools/xtask/Cargo.toml
  - tools/xtask/src/inventory.rs
  - tools/xtask/src/provenance.rs
  - tools/xtask/src/package.rs
  - tools/xtask/tests/inventory_cli.rs
  - tools/xtask/tests/provenance_cli.rs
  - tools/xtask/tests/package_cli.rs
  - reference/compatibility.json
  - reference/discovery.json
  - reference/artifacts/manifest.toml
  - COMPATIBILITY.md
autonomous: true
requirements:
  - FND-04
  - FND-05
  - FND-08
  - COMP-01
  - COMP-02
phase_lifecycle_id: 1-2026-07-10T02-00-42
lifecycle_mode: yolo
generated_at: 2026-07-10T02:22:20.953Z
must_haves:
  truths:
    - Compatibility scope has one authoritative machine-readable ledger and a conservative discovery coverage check.
    - Evidence dimensions remain independent and generated human documentation cannot drift silently.
    - Provenance and package checks reject mismatched artifacts or consumer leakage.
  artifacts:
    - path: reference/compatibility.json
      provides: Stable-ID compatibility ledger spanning subsystems, APIs, sources, tests, examples, and build options.
    - path: reference/discovery.json
      provides: Deterministic pinned-tree discovery snapshot tied to the selected revision.
    - path: COMPATIBILITY.md
      provides: Generated human-readable report with precise evidence states and gaps.
  key_links:
    - from: tools/xtask/src/inventory.rs
      to: reference/compatibility.json
      via: Schema and evidence-state validation.
      pattern: compatibility.json
    - from: tools/xtask/src/provenance.rs
      to: reference/upstream-lock.toml
      via: Revision agreement across lock, source map, discovery, and artifacts.
      pattern: upstream-lock.toml
---

<objective>
Create the authoritative compatibility inventory, discovery snapshot, generated report, provenance validation, and packaged-crate isolation checks required for evidence-based parity work.

Purpose: make omissions and false maturity claims mechanically visible from the first phase onward.
Output: inventory/discovery/artifact schemas, xtask checkers/generator, focused tests, and generated `COMPATIBILITY.md`.
</objective>

<threat_model>
- **Protected assets:** compatibility truth, artifact hashes, provenance relationships, and consumer package boundary.
- **Trust boundaries:** pinned upstream filenames/symbols to generated discovery; curated JSON/TOML to generated docs; repository files to packaged archive.
- **Threats:** omitted scope, forged evidence, path traversal, hash substitution, stale generated docs, silently regenerated artifacts, and packaged third-party leakage.
- **Mitigations:** stable IDs, strict serde schemas with unknown-field denial, canonical sorted generation, relative-path confinement, SHA-256 verification, check-only default behavior, and archive allow/deny assertions.
- **Blocking condition:** any unmapped discovery item, invalid evidence combination, hash mismatch, stale report, or forbidden package path is high severity and blocks completion.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Define and populate the compatibility/discovery schemas</name>
  <read_first>
    - reference/upstream-lock.toml
    - reference/source-map.toml
    - .planning/research/FEATURES.md
    - .planning/phases/01-oracle-provenance-and-repository-foundation/01-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D
    - third_party/liquidfun/liquidfun/Box2D/Testbed/Tests
  </read_first>
  <files>reference/compatibility.json, reference/discovery.json, reference/artifacts/manifest.toml</files>
  <action>Create schema version 1 JSON ledgers. Compatibility rows require stable `id`, `kind` from `subsystem|public_api|source_area|test|example|build_option`, upstream path and optional symbol, applicability plus reviewed exclusion rationale, Rust target, provenance reference, and separate evidence records for investigated, planned, implemented, unit_tested, differentially_validated, platform_validated, documented_difference, and intentionally_unsupported. Seed exhaustive top-level subsystem/source/test/example/build-option coverage from the pinned tree and the existing feature inventory; public API rows cover every public header discovered under `Box2D/Box2D`. Create a sorted discovery snapshot with oracle revision and deterministic entries for public headers, source areas, tests, examples, and CMake build options. Create an empty versioned artifact manifest whose record schema includes path, SHA-256, generator revision, oracle revision, preset, compiler, target, flags, notice refs, and review status.</action>
  <verify>Parse all files, check stable-ID uniqueness and ordering, compare every discovered entry to a compatibility row or reviewed exclusion, and assert the exact selected revision appears in discovery/artifact metadata.</verify>
  <acceptance_criteria>
    - All files declare schema version 1 and selected revision `7f20402173fd143a3988c921bc384459c6a858f2` where applicable.
    - Every compatibility row carries all eight independent evidence dimensions.
    - Every discovered public header, source area, test, example, and build option is mapped or excluded with nonempty rationale.
    - Stable IDs are unique and entries are deterministically sorted.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement inventory generation and validation</name>
  <read_first>
    - Cargo.toml
    - tools/xtask/Cargo.toml
    - tools/xtask/src/inventory.rs
    - reference/compatibility.json
    - reference/discovery.json
    - .planning/phases/01-oracle-provenance-and-repository-foundation/01-RESEARCH.md
  </read_first>
  <files>Cargo.toml, Cargo.lock, tools/xtask/Cargo.toml, tools/xtask/src/inventory.rs, COMPATIBILITY.md</files>
  <action>Add private tooling dependencies `serde` with derive, `serde_json`, `toml`, and `sha2` at current compatible stable major/minor requirements recorded in the workspace lock. Implement `cargo xtask inventory discover`, `generate`, and `check`. Discover walks only allowlisted pinned-tree roots, normalizes relative UTF-8 paths, sorts entries, and writes only with explicit `discover`; check is read-only and compares expected discovery/report bytes. Validate unknown fields, duplicate IDs, applicability rationale, evidence consistency (for example differential validation requires implementation and unit-test evidence), and discovery coverage. Generate `COMPATIBILITY.md` with oracle identity, state legend, per-kind tables, explicit gaps, and a do-not-edit marker.</action>
  <verify>Run `cargo xtask inventory discover`, `generate`, then `check`; rerun discover/generate and compare hashes for determinism; introduce temporary invalid fixtures in tests rather than editing canonical files.</verify>
  <acceptance_criteria>
    - `inventory check` is read-only and exits 0 on the committed ledgers/report.
    - Running discovery/generation twice produces byte-identical output.
    - `COMPATIBILITY.md` says it is generated, includes all eight evidence-state definitions, and reports the exact oracle SHA.
    - Invalid/duplicate/unmapped/inconsistent fixtures fail with stable categorized diagnostics.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement provenance and packaged-crate isolation checks</name>
  <read_first>
    - tools/xtask/src/provenance.rs
    - tools/xtask/src/package.rs
    - reference/upstream-lock.toml
    - reference/source-map.toml
    - reference/artifacts/manifest.toml
    - crates/liquidfun/Cargo.toml
  </read_first>
  <files>tools/xtask/src/provenance.rs, tools/xtask/src/package.rs</files>
  <action>Implement `cargo xtask provenance check` to cross-check lock, gitlink/checkout, source-map revisions, discovery revision, artifact SHA-256 values, notice refs, and generator revision without rewriting files. Implement `cargo xtask package verify` to run `cargo package -p liquidfun --allow-dirty`, inspect the `.crate` file list, reject `tools/`, `third_party/`, `reference/`, CMake files, and C/C++ headers/sources, unpack into a temporary directory, and run build/test there with repository-only paths unavailable. Confine all extracted paths beneath the temporary root and reject absolute or parent-traversal archive entries.</action>
  <verify>Run both commands and negative fixtures for wrong revision/hash, missing notice, forbidden path, absolute path, and `..` traversal; confirm commands do not mutate tracked inputs.</verify>
  <acceptance_criteria>
    - Provenance check detects every cross-record revision/hash/notice mismatch and exits nonzero.
    - Package verification builds/tests the unpacked crate and rejects every forbidden prefix/extension.
    - Archive extraction rejects absolute and parent-traversal paths before writing.
    - Successful checks leave `git status --short` unchanged except for intended plan files.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add focused validator and CLI regression tests</name>
  <read_first>
    - tools/xtask/src/inventory.rs
    - tools/xtask/src/provenance.rs
    - tools/xtask/src/package.rs
    - standards/core/testing.md
  </read_first>
  <files>tools/xtask/tests/inventory_cli.rs, tools/xtask/tests/provenance_cli.rs, tools/xtask/tests/package_cli.rs</files>
  <action>Add one-concern Arrange/Act/Assert tests for schema parsing, duplicate IDs, unmapped discovery, invalid evidence dependency, deterministic report generation, revision mismatch, SHA mismatch, missing notice, forbidden package file, and archive traversal. Use bounded temporary fixtures and injected roots; never rely on the user's real Cargo cache contents or modify canonical artifacts during negative tests.</action>
  <verify>Run the three named integration test targets plus the full Rust verification sequence and all three successful xtask checks.</verify>
  <acceptance_criteria>
    - Every listed failure mode has a focused test and stable error-category assertion.
    - Tests use bounded temporary roots and do not rewrite canonical reference files.
    - All xtask tests, Clippy with denied warnings, build-all-targets, and all-feature tests pass.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Run the required Rust format/Clippy/build/test sequence.
- Run `cargo xtask inventory check`, `cargo xtask provenance check`, and `cargo xtask package verify` twice where determinism matters.
- Verify `git diff --exit-code COMPATIBILITY.md reference/discovery.json` after check-mode commands.
</verification>

<success_criteria>
- The compatibility inventory is exhaustive at the declared Phase 1 granularity and exposes independent evidence states.
- Generated human documentation is deterministic and stale output is rejected.
- Provenance/package checks prevent wrong-oracle evidence and C++/reference leakage to consumers.
</success_criteria>

<output>
After completion, create `01-04-SUMMARY.md` with inventory counts by kind/state, command results, package proof, and residual limitations.
</output>
