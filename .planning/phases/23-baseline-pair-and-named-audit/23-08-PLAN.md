---
phase: 23-baseline-pair-and-named-audit
plan: "08"
type: execute
wave: 6
depends_on: [23-06, 23-07]
files_modified:
  - docs/native-performance-audit.md
  - docs/playground-dam-break-timing.md
autonomous: true
requirements: [PERF-AUDIT, PERF-HEAP]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-09-21T02-44-37
generated_at: 2026-09-21T03:02:06Z
must_haves:
  truths:
    - "docs/native-performance-audit.md exists with an Unreviewed local sample banner, cites MEASURED_HEAD from 23-07, cites the audit-bundle stamp path, and records rust_over_cpp_ratio from that stamp's pair.json only."
    - "Named-function ranking reads pair.json / rust.json.gz / *syms* from $(dirname $(git rev-parse --path-format=absolute --git-common-dir))/target/dam-break-perf/ first (hydrating this checkout if empty); missing durable stamps is a stop, not a hunt-list paste."
    - "The audit names dominating Rust functions with approximate samply shares taken from the live profile (sidecar and/or samply load), each classified into extra per-particle work / per-step allocation / checks that survive --release / algorithm/shape differences."
    - "Hunt-list symbols that do not appear in the live profile are listed under Not found or Not ranked; SIMD, default Rayon, PGO, unsafe indexing, WASM-vs-C++, and the Phase 12 sealed matrix are explicitly Not found as first moves."
    - "C++ counterpart names appear only for frames that the live profile actually named and that are shape mismatches, by comparison to pinned LiquidFun source — not from a C++ samply wrap."
    - "just playground-dam-break-heap either wrote a gitignored dhat dump after needle match, or the audit records Heap skipped with the heuristic's distinct reason; dhat is not run just in case."
    - "docs/playground-dam-break-timing.md keeps the Unreviewed local sample banner and locked Medium recipe, now showing the 23-07 SHA-bound pair as the current sample, with a one-line historical pointer to 1e5cbcc124becd363049c4d62c60b715bc0d7897."
  artifacts:
    - path: docs/native-performance-audit.md
      provides: "Committed named-function audit"
      contains: "Not found"
    - path: docs/playground-dam-break-timing.md
      provides: "Refreshed unreviewed SHA-bound pair sample"
      contains: "Unreviewed local sample"
  key_links:
    - from: docs/native-performance-audit.md
      to: primary-tree target/dam-break-perf/<bundle-stamp>/
      via: "git-common-dir durable root first, then cited bundle path plus pair.json ratio"
      pattern: "git-common-dir"
    - from: docs/native-performance-audit.md
      to: pair.json rust_over_cpp_ratio
      via: "unprofiled wall-clock only"
      pattern: "unprofiled"
---

<objective>
Write the committed named-function audit from the live 23-07 bundle, run or skip private dhat according to the heuristic, and refresh the timing doc as an unreviewed SHA-bound sample.

Purpose: PERF-AUDIT / PERF-HEAP / D-02 / D-07 / D-08 / D-09 / D-13 / D-14. Phase 24 consumes these names; inventing hunt-list causes is forbidden.
Output: `docs/native-performance-audit.md`, refreshed `docs/playground-dam-break-timing.md`. No physics diffs. Manifest stays empty.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md
@.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md
@.planning/phases/23-baseline-pair-and-named-audit/23-07-SUMMARY.md
@docs/playground-dam-break-timing.md
@BENCHMARKING.md
@reference/performance/manifest.toml
@.planning/research/PITFALLS.md
@crates/liquidfun/src/particle/proxy.rs
@crates/liquidfun/src/dynamics/execution.rs

<interfaces>
**Durable evidence root (mandatory, D-01).** Default execute-phase worktrees merge only committed files; 23-07's live stamps live under the primary tree after Task 3. Resolve before ranking or running heap:

```bash
COMMON="$(git rev-parse --path-format=absolute --git-common-dir)"
PRIMARY_ROOT="$(dirname "$COMMON")"
DURABLE_EVIDENCE="${PRIMARY_ROOT}/target/dam-break-perf"
WORKTREE_EVIDENCE="$(git rev-parse --show-toplevel)/target/dam-break-perf"
```

Read MEASURED_HEAD, pair stamp, profile stamp, bundle stamp, `DURABLE_EVIDENCE`, sidecar filenames, and `rust_over_cpp_ratio` from `23-07-SUMMARY.md`. Prefer `$DURABLE_EVIDENCE/<bundle-stamp>/` as the ranking source. If `$WORKTREE_EVIDENCE/<bundle-stamp>/audit-bundle-identity.json` is missing, `mkdir -p "$WORKTREE_EVIDENCE"` and `cp -R` the three 23-07 stamp directories from `$DURABLE_EVIDENCE` into `$WORKTREE_EVIDENCE` (copy, never move; never `git add`). Then `just playground-dam-break-heap` can see stamps under the current checkout. Do not invent hunt-list names if both roots lack the bundle stamp — stop; 23-07 Task 3 did not land.

Open that bundle's `pair.json` and copy numbers from there — never from samply, `[profile.profiling]`, `step_profiled`, or dhat.

**Naming functions (D-01/D-02):** Do not paste the in-tree hunt list as ranked causes. Procedure:

1. Resolve `DURABLE_EVIDENCE` / hydrate as above. `ls` the durable bundle stamp (and profile stamp) for `*syms*` / `rust.json.gz`. Do not rank from an empty worktree `target/dam-break-perf/`.
2. Prefer reading the presymbolicate sidecar strings. Count occurrences of demangled-like strings containing `liquidfun::`, `alloc::`, `core::clone::`, `RawVec`, `to_vec`. Rank by occurrence as **approximate** shares. If a gecko `stackTable` is readily present inside decompressed JSON, you may use sample weights; do **not** hand-roll a full Firefox-profiler UI.
3. Optionally `samply load <bundle>/rust.json.gz` for a human flame. Record that reproduce command in the audit. If the GUI cannot run headless, sidecar ranking is acceptable when the audit says shares are approximate string/sample weights from the live stamp — still not from PITFALLS.md.
4. List the smallest set that covers the bulk of named sampled time **or** the top 8–12 `liquidfun::` / allocator frames, whichever is clearer.
5. Classify **each listed frame** into exactly one of: extra per-particle work; per-step allocation; checks that survive `--release`; algorithm/shape differences.
6. Hunt-list items (`backup_step_limit_state`, `ParticleNeighborhood::from_view`, `velocities().to_vec`, `replace_solver_candidate` / release invariants) go under **Not found** or **Not ranked** unless they actually appear with a share.
7. C++ counterparts (D-08) **only** when a listed Rust frame is a shape mismatch. Use pinned submodule `7f20402173fd143a3988c921bc384459c6a858f2` source (`UpdateProxies`/`SortProxies` vs `from_view`; in-place `b2World::Step` vs full-world clone; `NDEBUG` vs release `check_invariants`). Do not run C++ samply.

**Audit doc required sections:**

- Title + **Unreviewed local sample** banner (must include that sentence). Must say this is not a public “Rust is N× slower” product claim and not Phase 12 evidence.
- Reproduce: `just playground-dam-break-bench`, `just playground-dam-break-profile`, `just playground-dam-break-audit-bundle`, and `samply load target/dam-break-perf/<bundle>/rust.json.gz`.
- git HEAD: full 40-hex MEASURED_HEAD (the capture SHA, which will be an ancestor of this docs commit).
- Bundle stamp path: `target/dam-break-perf/<utc-stamp>/`.
- Unprofiled Dam Break Medium table sourced from `pair.json` (Rust wall_ms, C++ wall_ms, ratio, compilers, host). One sentence: profiled/timer/dhat clocks are `not_timing_authority`.
- Named functions table: symbol, approximate share, cause class, optional C++ counterpart.
- Heap subsection: either dump stamp path + matched needles + not-the-3× disclaimer, or **Heap skipped:** plus the exact heuristic reason (`no allocator/Vec time` vs `could not read symbols`).
- **Not found:** SIMD-first, default Rayon, PGO, lifting `unsafe_code = "forbid"` / unsafe indexing, WASM-vs-C++, Phase 12 sealed 32-case matrix, plus unnamed hunt-list suspects.

Do not paste flamegraphs, `.json.gz` contents, or `.trace` files into git. Do not edit `reference/performance/manifest.toml`. Do not edit `crates/liquidfun/src/**`.

**Timing doc (D-14):** Keep the existing banner and Reproduce/workload paragraphs. Replace **First recorded sample** with **Current recorded sample** using 23-07 pair.json numbers and MEASURED_HEAD. Cite the pair or bundle stamp path. Add one sentence historical pointer to `1e5cbcc124becd363049c4d62c60b715bc0d7897` (the first exploratory sample). Do not call the refresh a Phase 12 public claim.

After Markdown edits: `just markdown-check` (mdformat 1.0.0 / Python 3.13). Never mdformat `.planning/**`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/native-performance-audit.md from the live bundle</name>
  <files>docs/native-performance-audit.md</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-01, D-02, D-03, D-06, D-07, D-08, D-09, D-10
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-07-SUMMARY.md`
    1. Durable bundle `pair.json` and sidecar `ls` under `$(dirname $(git rev-parse --path-format=absolute --git-common-dir))/target/dam-break-perf/<bundle>/` (hydrate into this checkout's `target/dam-break-perf/` if that path is empty)
    1. `.planning/research/PITFALLS.md` hunt list (reading map only)
    1. `docs/playground-dam-break-timing.md` banner wording
  </read_first>
  <action>
    Create `docs/native-performance-audit.md` with the sections listed in interfaces. Before ranking, resolve `DURABLE_EVIDENCE` via `git-common-dir` as specified in interfaces and hydrate this checkout if needed. Ratio digits must match `pair.json` `rust_over_cpp_ratio` / wall_ms from the **bundle** stamp under that durable (or hydrated) path, not a retyped samply clock. If the bundle stamp is missing from both roots, stop — do not invent dominating functions from PITFALLS.md (D-02).

    If a function is not in the live profile strings/samples, do not rank it. Prefer “Not found” over a guessed percent.

    Do not add a “samply ms” column next to Rust/C++ walls. Do not write “Rust is N× slower” as a product claim; the ratio may appear as an unreviewed sample figure bound to HEAD and stamp.

    Do not edit physics sources to make names look better. Do not fill `manifest.toml`.
  </action>
  <verify>
    <automated>python3 -c 'from pathlib import Path
p=Path("docs/native-performance-audit.md")
t=p.read_text()
assert "Unreviewed local sample" in t
assert "Not found" in t
assert "target/dam-break-perf/" in t
assert "just playground-dam-break-bench" in t
for label in ("extra per-particle", "per-step allocation", "survive `--release`", "algorithm/shape"):
    assert any(s in t.lower() for s in (label, label.replace("`", ""))), label
print("audit bytes", p.stat().st_size)'</automated>
  </verify>
  <acceptance_criteria>
    1. `docs/native-performance-audit.md` exists and contains `Unreviewed local sample`.
    1. The file contains `Not found` as a heading or section.
    1. The file contains `target/dam-break-perf/` and a stamp name matching 23-07's bundle directory.
    1. The file contains the 40-hex MEASURED_HEAD from 23-07-SUMMARY (not a placeholder `abc123`).
    1. Ranking used the durable `git-common-dir` evidence root (or a byte-identical hydrate); the executor did not invent names because this checkout's `target/dam-break-perf/` started empty.
    1. `rg -n "not_timing_authority|not the 3" docs/native-performance-audit.md` matches a denial that profile/dhat clocks are the gate.
    1. `rg -n "reviewed_reports" docs/native-performance-audit.md` does not instruct filling the manifest.
    1. `git diff -- crates/liquidfun/src reference/performance/manifest.toml` is empty.
  </acceptance_criteria>
  <done>Committed audit names live-profile functions, classifies causes, records the unprofiled ratio, and has an explicit Not found section.</done>
</task>

<task type="auto">
  <name>Task 2: Run or skip heap, then refresh the timing doc</name>
  <files>docs/native-performance-audit.md, docs/playground-dam-break-timing.md</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-12, D-13, D-14
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md` Pattern 3, Pitfall 7, Open Question 2
    1. `docs/playground-dam-break-timing.md`
    1. `docs/native-performance-audit.md` from Task 1
  </read_first>
  <action>
    Per D-13, run `just playground-dam-break-heap` **once** against the live bundle/profile stamp (defaults 60/600) **after** hydrating `$WORKTREE_EVIDENCE` from `$DURABLE_EVIDENCE` if this checkout's evidence root is empty. dhat may take many minutes; do not shrink the recipe. If the command fails because of `no allocator` / `no allocator/Vec`, record **Heap skipped: samply showed no allocator/Vec time** in the audit and do **not** retry with xctrace. If it fails because of `could not read symbols`, record that distinct skip and still name CPU functions from `samply load` / sidecar — do not claim “no allocator time.” If dhat hangs or crashes on this host, only then consider `xcrun xctrace` Allocations as a fallback (D-13); do not script Instruments as the default. Never leave `dhat-heap.json` in the repo root; it must live under `target/dam-break-perf/<stamp>/`.

    On success, add the heap stamp path, `heap-identity.json` `matched_needles`, and a not-the-3× sentence to the audit. Do not copy dhat totals into the ratio table. Also `cp -R` the new heap stamp directory to `$DURABLE_EVIDENCE/` so it survives this worktree; still never `git add` `dhat-heap.json`.

    Then refresh `docs/playground-dam-break-timing.md` per D-14:

    - Keep the opening **Unreviewed local sample** paragraph and the Reproduce console block (`just playground-dam-break-bench`) and the 1920-particle workload paragraph.
    - Replace the old table with current pair.json numbers from 23-07 (same digits as the audit).
    - Cite `target/dam-break-perf/<pair-or-bundle-stamp>/`.
    - One-line historical pointer to `1e5cbcc124becd363049c4d62c60b715bc0d7897`.
    - Do not add samply duration.

    Run `just markdown-check`. Do not mdformat `.planning/**`. Do not edit `reference/performance/manifest.toml` (`reviewed_reports` stays `[]`).
  </action>
  <verify>
    <automated>just markdown-check &amp;&amp; rg -n "Unreviewed local sample" docs/playground-dam-break-timing.md docs/native-performance-audit.md &amp;&amp; rg -n "reviewed_reports = \\[\\]" reference/performance/manifest.toml</automated>
  </verify>
  <acceptance_criteria>
    1. `docs/native-performance-audit.md` contains either `dhat-heap.json` plus a dump stamp path, or `Heap skipped` with `no allocator` or `could not read symbols`.
    1. `test ! -e dhat-heap.json` at the repo root.
    1. `docs/playground-dam-break-timing.md` still starts its honesty banner with `Unreviewed local sample` and still documents 1920 particles and 60+600 steps.
    1. Timing doc contains MEASURED_HEAD and `target/dam-break-perf/`.
    1. Timing doc contains `1e5cbcc124becd363049c4d62c60b715bc0d7897` as a historical pointer.
    1. `rg -n "reviewed_reports = \\[\\]" reference/performance/manifest.toml` matches.
    1. `just markdown-check` exits 0.
    1. `git diff -- crates/liquidfun/src` is empty.
  </acceptance_criteria>
  <done>Heap was run or skip-recorded from the live heuristic; timing doc is the current SHA-bound unreviewed sample.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live profile bytes → committed Markdown | Only names, approximate shares, SHA, ratio, stamp paths — no gzip/trace blobs. |
| Hunt list vs live samples | Source-reading must not become ranked causes. |
| dhat dump path | Must stay gitignored; cwd default is unsafe. |
| Manifest / README | Must not gain a public speed claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08-01 | S | invented hot functions | mitigate | Rank only symbols present in the live sidecar/gzip; hunt-list misses go to Not found (D-02). |
| T-23-08-02 | S | samply/dhat duration as 3× | mitigate | Ratio digits copied from bundle `pair.json` only; no samply-ms column (D-03). |
| T-23-08-03 | I | committing `.json.gz` / flamegraphs | mitigate | Docs cite paths; `git ls-files '*.json.gz' '*.trace'` stays empty (D-09). |
| T-23-08-04 | T | filling `manifest.toml` | mitigate | Do not edit it; verify `reviewed_reports = []` (D-09). |
| T-23-08-05 | T | physics kernel edits | mitigate | `git diff -- crates/liquidfun/src` empty (D-10). |
| T-23-08-06 | I | repo-root `dhat-heap.json` | mitigate | Heap xtask sets `LIQUIDFUN_DHAT_HEAP_FILE`; assert no root dump (Pitfall 7). |
| T-23-08-07 | S | “Rust is N× slower” product claim | mitigate | Unreviewed banner on both docs; D-09 wording required. |
| T-23-08-08 | S | empty worktree evidence root | mitigate | Rank from `git-common-dir` primary `target/dam-break-perf/` first; hydrate local stamps by copy; stop rather than invent hunt-list names (D-01/D-02). |
</threat_model>

<verification>
`just markdown-check`, greps on both docs, empty liquidfun src diff, empty manifest reviewed_reports. Independent AI review is 23-09 / post-verify — do not self-approve.
</verification>

<success_criteria>
A developer can read SHA-bound named-function notes with cause classes and Not found, point at the bundle stamp, and see either a private heap dump or an explicit skip.
</success_criteria>

<output>
After completion, create `.planning/phases/23-baseline-pair-and-named-audit/23-08-SUMMARY.md`
</output>
