---
phase: 23-baseline-pair-and-named-audit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/xtask/tests/playground_cli.rs
  - tools/xtask/tests/playground_cli/support.rs
  - tools/xtask/tests/playground_cli/pair.rs
  - tools/xtask/tests/playground_cli/profile.rs
  - tools/xtask/tests/playground_cli/timers.rs
  - tools/xtask/tests/playground_cli/bundle.rs
  - tools/xtask/tests/playground_cli/heap.rs
autonomous: true
requirements: [PERF-AUDIT, PERF-HEAP]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-09-21T02-44-37
generated_at: 2026-09-21T03:02:06Z
must_haves:
  truths:
    - "tools/xtask/tests/playground_cli.rs is a foo.rs dispatcher plus playground_cli/ child modules, with no playground_cli/mod.rs and no playground_cli/main.rs."
    - "Existing fake-tool pair, profile, and timers CLI tests still pass under cargo test -p xtask --test playground_cli."
    - "playground_cli.rs and every playground_cli/*.rs file stay at or under 628 physical lines so 23-03 and 23-06 can add bundle/heap cases without a Bright Builds file-lengths failure."
  artifacts:
    - path: tools/xtask/tests/playground_cli.rs
      provides: "Integration-test crate root with mod declarations only"
      contains: "mod bundle;"
    - path: tools/xtask/tests/playground_cli/support.rs
      provides: "RepositoryFixture, fake-tools compile, stamp path helpers"
      contains: "struct RepositoryFixture"
    - path: tools/xtask/tests/playground_cli/bundle.rs
      provides: "Empty stub so 23-03 owns only this file"
    - path: tools/xtask/tests/playground_cli/heap.rs
      provides: "Empty stub so 23-06 owns only this file"
  key_links:
    - from: tools/xtask/tests/playground_cli.rs
      to: tools/xtask/tests/playground_cli/pair.rs
      via: "mod pair"
      pattern: "mod pair;"
    - from: tools/xtask/tests/playground_cli/support.rs
      to: tools/xtask/tests/fixtures/fake_upstream_tool.rs
      via: "compile fake git/cmake/cargo/samply"
      pattern: "fake_upstream_tool.rs"
---

<objective>
Split the 622-line `playground_cli` integration test into `playground_cli.rs` plus `playground_cli/` before any Phase 23 bundle or heap cases are added.

Purpose: Bright Builds file-length trigger is 628 physical lines (`floor(100 * tau)`). RESEARCH pitfall 5: appending bundle/heap tests to the current file fails CI at 629. D-04/D-12 tests need room.
Output: Behavior-identical CLI tests in child modules, plus empty `bundle.rs` / `heap.rs` stubs with exclusive later-plan ownership. No new playground subcommands.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/code-shape.md
@standards/languages/rust.md
@standards/core/testing.md
@.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md
@.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md
@tools/xtask/tests/playground_cli.rs
@tools/xtask/tests/release_cli.rs
@tools/xtask/src/inventory.rs

<interfaces>
Keep this an integration test crate named `playground_cli` (`cargo test -p xtask --test playground_cli`). Use `foo.rs` plus `foo/` — never `tests/playground_cli/mod.rs` and never convert to `tests/playground_cli/main.rs`.

`tools/xtask/tests/playground_cli.rs` after the split must contain only module docs plus:

```rust
mod bundle;
mod heap;
mod pair;
mod profile;
mod support;
mod timers;
```

Do not add `dam-break-audit-bundle` or `dam-break-heap` commands, just aliases, dhat, or physics edits in this plan.

Move helpers into `support.rs` as `pub(super)` so pair/profile/timers tests keep compiling: `RepositoryFixture`, `FakeTools`, `STAMP_UNIX`, `FIRST_STAMP`, `SECOND_STAMP`, `workspace_root`, `fake_tools`, `stdout`, `stderr`, `run_pair`, `run_profile`, `rust_profiles`, `command_strings`, `executable_name`. Keep `LIQUIDFUN_XTASK_*` injection identical, including `LIQUIDFUN_XTASK_STAMP_UNIX` = `1000000000`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split playground_cli.rs into foo.rs plus foo/ without behavior change</name>
  <files>tools/xtask/tests/playground_cli.rs, tools/xtask/tests/playground_cli/support.rs, tools/xtask/tests/playground_cli/pair.rs, tools/xtask/tests/playground_cli/profile.rs, tools/xtask/tests/playground_cli/timers.rs, tools/xtask/tests/playground_cli/bundle.rs, tools/xtask/tests/playground_cli/heap.rs</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-04, D-05, D-12
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md` Pattern 1, Pitfall 5, Recommended Project Structure
    1. `standards/languages/rust.md` foo.rs plus foo/
    1. `standards/core/code-shape.md` 628-line trigger
    1. `tools/xtask/tests/playground_cli.rs` entire file (622 lines)
    1. `tools/xtask/src/inventory.rs` as the in-repo `foo.rs` plus `foo/` split example
  </read_first>
  <action>
    Split `tools/xtask/tests/playground_cli.rs` (622 physical lines) before adding any Phase 23 cases. Do not change assertions, env vars, fake-tool stems, stamp constants, or justfile recipe string checks.

    `playground_cli.rs`: module docs stating this crate covers fake-tool playground CLI, then the six `mod` lines listed in interfaces. No tests in this file.

    `playground_cli/support.rs`: all fixture/compile/path helpers currently above the first `#[test]`. Export types and functions as `pub(super)`. Keep compiling `fake_upstream_tool.rs` with rustc edition 2024 into `target/xtask-test-tools/playground-cli-{pid}/`.

    `playground_cli/pair.rs`: move `dam_break_bench_persists_unprofiled_pair_without_profile_cmake_flags`, `second_pair_in_the_same_unix_second_mints_a_new_stamp`, `justfile_keeps_the_one_line_dam_break_bench_alias`.

    `playground_cli/profile.rs`: move `justfile_adds_one_line_dam_break_profile_alias`, `dam_break_profile_persists_rust_profile_without_pair_json`, `missing_samply_fails_closed_without_placeholder_gzip`, `second_profile_in_the_same_unix_second_mints_a_new_stamp`.

    `playground_cli/timers.rs`: move `dam_break_timers_persists_timers_json_without_pair`.

    `playground_cli/bundle.rs` and `playground_cli/heap.rs`: module docs only (`//! Audit-bundle CLI tests land in 23-03.` / `//! Heap CLI tests land in 23-06.`). No `#[test]` yet.

    After the move, `cargo fmt --all`. Do not create `playground_cli/mod.rs`. Do not edit `tools/xtask/src/**`, `justfile`, `crates/liquidfun/**`, or `crates/liquidfun-wasm/**`.
  </action>
  <verify>
    <automated>cargo test -p xtask --test playground_cli -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `test ! -e tools/xtask/tests/playground_cli/mod.rs` succeeds.
    1. `test ! -e tools/xtask/tests/playground_cli/main.rs` succeeds.
    1. `rg -n "^mod bundle;|^mod heap;|^mod pair;|^mod profile;|^mod support;|^mod timers;" tools/xtask/tests/playground_cli.rs` matches all six.
    1. `rg -n "#\[test\]" tools/xtask/tests/playground_cli.rs tools/xtask/tests/playground_cli/bundle.rs tools/xtask/tests/playground_cli/heap.rs` is empty.
    1. `rg -n "fn dam_break_bench_persists_unprofiled_pair_without_profile_cmake_flags" tools/xtask/tests/playground_cli/pair.rs` matches.
    1. `rg -n "fn missing_samply_fails_closed_without_placeholder_gzip" tools/xtask/tests/playground_cli/profile.rs` matches.
    1. `rg -n "fn dam_break_timers_persists_timers_json_without_pair" tools/xtask/tests/playground_cli/timers.rs` matches.
    1. `rg -n "dam-break-audit-bundle|dam-break-heap|dhat-heap" tools/xtask/tests/playground_cli.rs tools/xtask/tests/playground_cli` is empty.
    1. `cargo test -p xtask --test playground_cli -- --test-threads=1` exits 0.
  </acceptance_criteria>
  <done>CLI tests are split and still pass; bundle/heap stub modules exist for later exclusive ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce the 628-line cap on every split test file</name>
  <files>tools/xtask/tests/playground_cli.rs, tools/xtask/tests/playground_cli/support.rs, tools/xtask/tests/playground_cli/pair.rs, tools/xtask/tests/playground_cli/profile.rs, tools/xtask/tests/playground_cli/timers.rs, tools/xtask/tests/playground_cli/bundle.rs, tools/xtask/tests/playground_cli/heap.rs</files>
  <read_first>
    1. `standards/core/code-shape.md` 628-line trigger
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md` Pitfall 5
    1. Every file created in Task 1
  </read_first>
  <action>
    Count physical lines (including blanks and comments) for `tools/xtask/tests/playground_cli.rs` and every `tools/xtask/tests/playground_cli/*.rs`. Each file must be ≤628. Do not add a `.bright-builds-rules-checks.tsv` exact-file exception. If any file is over 628, split that module further using `foo.rs` plus `foo/` rather than suppressing the check.

    Record the line counts in `23-01-SUMMARY.md`. Do not run `bun scripts/bright-builds-check.ts all` until 23-09 (this plan only proves the split files themselves are under the cap).
  </action>
  <verify>
    <automated>python3 -c 'from pathlib import Path
root=Path("tools/xtask/tests/playground_cli.rs")
paths=[root]+sorted(Path("tools/xtask/tests/playground_cli").glob("*.rs"))
assert not Path("tools/xtask/tests/playground_cli/mod.rs").exists()
oversized=[f"{p}:{sum(1 for _ in p.open())}" for p in paths if sum(1 for _ in p.open())>628]
raise SystemExit("oversized "+str(oversized) if oversized else 0)'</automated>
  </verify>
  <acceptance_criteria>
    1. Every listed playground_cli test file has ≤628 physical lines.
    1. `tools/xtask/tests/playground_cli/mod.rs` does not exist.
    1. No new row was added to `.bright-builds-rules-checks.tsv` for these files (`git diff -- .bright-builds-rules-checks.tsv` is empty, or the file remains unmodified if untracked-absent).
  </acceptance_criteria>
  <done>Split test files are under the Bright Builds line cap with room for 23-03 and 23-06 cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fake-tool CLI tests → host filesystem | Tests write under `target/xtask-test-fixtures/` only. |
| Integration test crate layout | Split must not introduce a second test binary that skips isolation env injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | T | `playground_cli` split | mitigate | Keep `LIQUIDFUN_XTASK_*` program-path injection in `support.rs`; never `sh -c`. |
| T-23-01-02 | I | fixture dirs | mitigate | Continue writing under gitignored `target/xtask-test-fixtures/` and `target/xtask-test-tools/`. |
| T-23-01-03 | S | file-length exception | mitigate | No TSV exception; split instead of hiding a 629-line file. |
| T-23-01-04 | T | premature bundle/heap commands | mitigate | Stubs contain no tests and no `dam-break-audit-bundle` / `dhat-heap` strings. |
</threat_model>

<verification>
`cargo test -p xtask --test playground_cli -- --test-threads=1` plus the Python line-count assertion. Do not run live Dam Break, cmake, or host samply. Do not add dhat.
</verification>

<success_criteria>
The playground CLI test crate is split under the 628-line cap; existing fake-tool coverage still passes; later plans can add bundle/heap tests without touching this dispatcher except via their stub files.
</success_criteria>

<output>
After completion, create `.planning/phases/23-baseline-pair-and-named-audit/23-01-SUMMARY.md`
</output>
