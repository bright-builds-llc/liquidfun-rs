---
phase: 23-baseline-pair-and-named-audit
plan: "07"
type: execute
wave: 4
depends_on: [23-03]
files_modified: []
autonomous: true
requirements: [PERF-AUDIT]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-09-21T02-44-37
generated_at: 2026-09-21T03:02:06Z
must_haves:
  truths:
    - "A live just playground-dam-break-bench stamp exists under target/dam-break-perf/<utc>/ with pair.json kind unprofiled_pair, finite rust and cpp wall_ms, rust_over_cpp_ratio, and git_head equal to git rev-parse HEAD at capture time."
    - "A live just playground-dam-break-profile stamp exists with nonempty rust.json.gz, profile-identity.json kind samply_cpu, not_timing_authority true, and git_head equal to the pair stamp git_head."
    - "just playground-dam-break-audit-bundle created a third exclusive stamp that contains copied pair.json, pair.md, rust.json.gz, any *syms* siblings, and audit-bundle-identity.json naming both source stamps; sources were not moved."
    - "The measured 40-hex is recorded in 23-07-SUMMARY.md together with the three stamp directory names and an ls of the profile and bundle stamps (including sidecar filenames)."
  artifacts:
    - path: target/dam-break-perf
      provides: "Live gitignored pair, profile, and audit-bundle stamps"
      contains: "pair.json"
  key_links:
    - from: just playground-dam-break-bench
      to: pair.json
      via: "unprofiled --release vs oracle-release"
      pattern: "unprofiled_pair"
    - from: just playground-dam-break-audit-bundle
      to: copied rust.json.gz
      via: "same-HEAD copy"
      pattern: "audit_bundle"
---

<objective>
Run the SHA-bound live unprofiled Dam Break Medium pair and live samply profile, then mint the copy-only audit-bundle stamp that 23-08 will cite.

Purpose: D-01. Fake cmake/samply fixtures cannot name dominating functions. `target/dam-break-perf` is empty until this plan.
Output: Three gitignored stamps plus a SUMMARY that records HEAD, stamp names, sidecar `ls`, and pair.json `rust_over_cpp_ratio`. No committed docs yet (23-08). No physics edits. Do not run dhat here (D-13 waits on the heuristic in 23-08).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md
@.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md
@docs/playground-dam-break-timing.md
@justfile
@tools/xtask/src/playground/pair.rs
@tools/xtask/src/playground/profile.rs
@tools/xtask/src/playground/bundle.rs

<interfaces>
Locked recipe (do not change counts): 1920 particles, `--warmup 60 --steps 600` defaults, Rust `cargo run -p liquidfun-wasm --release --bin dam-break-bench` vs C++ `oracle-release` extra target. Unprofiled wall clock is the only 3× number (D-03).

Host tools already verified in RESEARCH: samply 0.13.1, rustc 1.97.0, CMake ≥3.25, Ninja. Missing samply must fail closed with Phase 22 install text — that is a hard stop, not a fake-tool skip.

Capture on a **clean** git tree (`git status --porcelain` empty) so `git rev-parse HEAD` matches the bytes that ran. If the tree is dirty, commit or stash nothing that is out of this phase's standing authorization — finish 23-01..23-06 commits first. Record `worktree_dirty` if the bundle identity includes that field.

Expected wall time: exploratory Rust was ~70 s at `1e5cbcc…`; live pair + samply wrap of the same 600 steps will take several minutes. Do **not** shrink particle count or step count. Do **not** pass `MAX_ADVANCE_STEPS` 600.

After the profile stamp exists, `ls -la` it and copy **whatever `*syms*` file actually appears** (RESEARCH A1). Bundle must include those files.

Do not quote samply's own duration as the Dam Break ratio in the SUMMARY. Record `pair.json` `rust.wall_ms`, `cpp.wall_ms`, `rust_over_cpp_ratio`, `git_head` only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live unprofiled pair at current HEAD</name>
    <files>.planning/phases/23-baseline-pair-and-named-audit/23-07-SUMMARY.md</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-01, D-03
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md` Environment Availability, Pitfall 6
    1. `docs/playground-dam-break-timing.md` locked Medium recipe
    1. `justfile` `playground-dam-break-bench`
  </read_first>
  <action>
    Per D-01, this live run is definition-of-done, not optional.

    1. `git rev-parse HEAD` → record 40-hex `MEASURED_HEAD`.
    1. `git status --porcelain` must be empty. If not empty, stop and finish outstanding Phase 23 plan commits; do not capture a dirty tree as the cited SHA.
    1. Confirm `samply --version` prints `samply 0.13.1` (needed for Task 2; fail now if missing).
    1. Run `just playground-dam-break-bench` from the repo root with default warmup/steps. Allow many minutes. Do not pass `--features dhat-heap`.
    1. Locate the newest `target/dam-break-perf/*/pair.json`. Assert `kind==unprofiled_pair`, `timing_authority==unprofiled_wall_clock`, `git_head==MEASURED_HEAD`, `particles==1920`, `warmup_steps==60`, `measured_steps==600`, finite `rust.wall_ms`, finite `cpp.wall_ms > 0`, finite `rust_over_cpp_ratio`.
    1. Assert that stamp has no `rust.json.gz` (pair and profile stay sibling stamps until the bundle copy).

    Record stamp dir name, HEAD, both wall_ms, and ratio in `23-07-SUMMARY.md`. Do not edit `docs/**` or `reference/performance/manifest.toml`.
  </action>
  <verify>
    <automated>git rev-parse HEAD &amp;&amp; python3 -c 'import json,pathlib,sys
root=pathlib.Path("target/dam-break-perf")
assert root.is_dir(), "missing evidence root"
pairs=list(root.glob("*/pair.json"))
assert pairs, "no pair.json"
latest=max(pairs, key=lambda p: p.parent.name)
rep=json.loads(latest.read_text())
assert rep.get("kind")=="unprofiled_pair"
assert rep.get("timing_authority")=="unprofiled_wall_clock"
assert isinstance(rep.get("rust_over_cpp_ratio"), (int,float))
assert rep["cpp"]["wall_ms"]>0
print(latest.parent.name, rep["git_head"], rep["rust_over_cpp_ratio"])'</automated>
  </verify>
  <acceptance_criteria>
    1. `git status --porcelain` is empty at capture time (recorded in SUMMARY).
    1. Newest `pair.json` `kind` is `unprofiled_pair` and `git_head` equals `git rev-parse HEAD`.
    1. `warmup_steps` is 60 and `measured_steps` is 600 and `particles` is 1920.
    1. `cpp.wall_ms` is finite and &gt; 0; ratio is finite (not Inf).
    1. That pair stamp does not contain `rust.json.gz`.
    1. `just playground-dam-break-bench` was invoked without `--features`.
  </acceptance_criteria>
  <done>Live unprofiled pair stamp exists at the current HEAD with a finite Rust/C++ ratio.</done>
</task>

<task type="auto">
  <name>Task 2: Live samply profile, list sidecars, mint the audit bundle</name>
    <files>.planning/phases/23-baseline-pair-and-named-audit/23-07-SUMMARY.md</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-01, D-04, D-05, D-06
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md` Pattern 1, Pitfall 4, A1
    1. Task 1 SUMMARY notes (pair stamp name + MEASURED_HEAD)
    1. `justfile` `playground-dam-break-profile` and `playground-dam-break-audit-bundle`
  </read_first>
  <action>
    Per D-01, run `just playground-dam-break-profile` (defaults 60/600) on the same clean HEAD. Allow several minutes beyond the unprofiled Rust wall because samply + `[profile.profiling]` add overhead. Do not pass `--release` or `--features dhat-heap`.

    After success:

    1. `ls -la` the newest stamp that contains `rust.json.gz`. Record every filename, especially `*syms*`.
    1. Assert `profile-identity.json` `kind==samply_cpu`, `not_timing_authority==true`, `samply_version==0.13.1`, `git_head==MEASURED_HEAD`, and that stamp has **no** `pair.json`.
    1. `rust.json.gz` size &gt; 0.
    1. Run `just playground-dam-break-audit-bundle` with no extra flags (select latest same-HEAD pair + profile).
    1. Assert a third stamp contains `pair.json`, `pair.md`, `rust.json.gz`, `audit-bundle-identity.json` with `kind==audit_bundle`, matching `git_head`, `source_pair_stamp` / `source_profile_stamp` set, `not_timing_authority==true`, `profile_blob==rust.json.gz`.
    1. If the profile stamp had a `*syms*` file, the bundle stamp must contain a byte-identical copy.
    1. Source pair and profile stamps still exist with original `pair.json` / `rust.json.gz` bytes (`cmp` or checksum).
    1. Do **not** run `just playground-dam-break-heap` in this plan.

    Record bundle stamp path for 23-08. Do not commit `target/**`. Do not paste flamegraphs.
  </action>
  <verify>
    <automated>python3 -c 'import json,pathlib
root=pathlib.Path("target/dam-break-perf")
idents=list(root.glob("*/audit-bundle-identity.json"))
assert idents, "no audit bundle"
latest=max(idents, key=lambda p: p.parent.name)
ident=json.loads(latest.read_text())
assert ident.get("kind")=="audit_bundle"
assert ident.get("not_timing_authority") is True
stamp=latest.parent
for name in ("pair.json","pair.md","rust.json.gz"):
    assert (stamp/name).is_file(), name
    assert (stamp/name).stat().st_size &gt; 0, name
pair=json.loads((stamp/"pair.json").read_text())
assert pair.get("kind")=="unprofiled_pair"
assert pair.get("git_head")==ident.get("git_head")
print(stamp.name, ident.get("git_head"), ident.get("source_pair_stamp"), ident.get("source_profile_stamp"))'</automated>
  </verify>
  <acceptance_criteria>
    1. Live profile stamp `git_head` equals the pair stamp `git_head` and `git rev-parse HEAD`.
    1. Profile identity `not_timing_authority` is true and the profile stamp has no `pair.json`.
    1. Bundle stamp contains copied `pair.json`, `pair.md`, `rust.json.gz`, and identity `kind=audit_bundle`.
    1. Every `*syms*` file present next to the live `rust.json.gz` is also present in the bundle stamp.
    1. Source stamps still exist (copy, not move).
    1. `23-07-SUMMARY.md` lists MEASURED_HEAD and the three stamp directory names.
    1. `git ls-files 'target/dam-break-perf/**'` is empty.
  </acceptance_criteria>
  <done>Live pair, live samply gzip, and copy-only audit-bundle stamps exist at one SHA, with sidecar filenames recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host samply / cmake output → git | Raw blobs must remain gitignored. |
| Dirty worktree vs recorded HEAD | `git rev-parse HEAD` ignores uncommitted physics edits. |
| samply duration vs pair.json | Two clocks; only pair.json is the ratio. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07-01 | I | committing `rust.json.gz` | mitigate | Evidence stays under `/target/` (already gitignored); SUMMARY forbids `git add` of gzip/trace (D-09). |
| T-23-07-02 | S | quoting samply duration as 3× | mitigate | SUMMARY records only `pair.json` walls/ratio (D-03). |
| T-23-07-03 | T | SHA drift | mitigate | Require clean `git status --porcelain`; bundle requires matching `git_head` (Pitfall 6). |
| T-23-07-04 | T | moving Phase 22/23 source stamps | mitigate | Bundle is copy-only; checksum sources after copy (D-04). |
| T-23-07-05 | T | shrinking the recipe | mitigate | Locked 60+600 / 1920 particles; reject any `--warmup 0` live capture as DoD. |
</threat_model>

<verification>
Python assertions over `target/dam-break-perf` plus recorded SUMMARY fields. Do not commit evidence. Do not write `docs/native-performance-audit.md` yet.
</verification>

<success_criteria>
A developer can point at one dated audit-bundle stamp from the current HEAD that contains the live unprofiled pair report and the live samply `rust.json.gz`.
</success_criteria>

<output>
After completion, create `.planning/phases/23-baseline-pair-and-named-audit/23-07-SUMMARY.md`
</output>
