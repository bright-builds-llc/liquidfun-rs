---
phase: 23-baseline-pair-and-named-audit
plan: "03"
type: tdd
wave: 3
depends_on: [23-02]
files_modified:
  - tools/xtask/tests/playground_cli/bundle.rs
autonomous: true
requirements: [PERF-AUDIT]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-09-21T02-44-37
generated_at: 2026-09-21T03:02:06Z
must_haves:
  truths:
    - "A fake pair stamp plus a fake profile stamp that share git_head, run through dam-break-audit-bundle, produce a third exclusive stamp with copied pair.json, pair.md, rust.json.gz, optional *syms* sidecar, and audit-bundle-identity.json; source stamps keep original bytes."
    - "HEAD mismatch, missing rust.json.gz, path-traversal --pair-stamp, and pair.json that contains a samply key each exit nonzero and write no audit-bundle-identity.json."
    - "The destination stamp is not one of the source stamp names; mint uses exclusive create_dir."
  artifacts:
    - path: tools/xtask/tests/playground_cli/bundle.rs
      provides: "Fake-stamp success and fail-closed CLI coverage"
      contains: "dam-break-audit-bundle"
  key_links:
    - from: tools/xtask/tests/playground_cli/bundle.rs
      to: tools/xtask/src/playground/bundle.rs
      via: "cargo xtask playground dam-break-audit-bundle"
      pattern: "dam-break-audit-bundle"
---

<objective>
Prove the audit-bundle command through fake-stamp CLI tests: copy success, sidecar copy, and fail-closed mismatch/missing/path cases.

Purpose: D-04 fail-closed plumbing. Fake cmake/samply still cannot name dominating functions (D-01/D-02); this plan only covers copy correctness.
Output: Tests in `tools/xtask/tests/playground_cli/bundle.rs` only. Do not edit `playground_cli.rs` (stubs already declare `mod bundle`).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md
@.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md
@.planning/phases/23-baseline-pair-and-named-audit/23-02-PLAN.md
@tools/xtask/tests/playground_cli/support.rs
@tools/xtask/tests/playground_cli/bundle.rs
@tools/xtask/src/playground/bundle.rs

<interfaces>
Use `crate::support::{RepositoryFixture, FIRST_STAMP, SECOND_STAMP, ...}` from 23-01. `FIRST_STAMP` is `2001-09-09T01-46-40Z` when `LIQUIDFUN_XTASK_STAMP_UNIX=1000000000`.

Recommended fixture flow for the success test:

1. `run_pair` (writes pair into FIRST_STAMP).
2. Change `LIQUIDFUN_XTASK_STAMP_UNIX` to `1000000001` (SECOND_STAMP) or rely on AlreadyExists bump: after pair occupies FIRST_STAMP, `run_profile` lands in SECOND_STAMP.
3. Write a sibling `rust.json.syms.json` into the profile stamp with at least 16 bytes of JSON `{"string_table":["alloc::vec::Vec"]}`.
4. Run `playground dam-break-audit-bundle` with `LIQUIDFUN_XTASK_STAMP_UNIX=1000000002` so the bundle mint is `2001-09-09T01-46-42Z` (define `THIRD_STAMP` in the test file or support if needed — if you add a constant, put it in `bundle.rs` locally rather than editing `support.rs` if that keeps 23-01 files untouched).
5. Assert destination files exist; `fs::read` source vs dest bytes are equal; `audit-bundle-identity.json` `source_pair_stamp` / `source_profile_stamp` match the actual source dir names; `copied` includes `rust.json.syms.json` when that sidecar was present.

Fake samply still writes raw `fake-samply-json-gz` bytes, not real gzip — that is OK for copy tests. Do not treat those bytes as a live profile.

`--pair-stamp ../secret` (and `--pair-stamp 2001-09-09T01:46:40Z`) must fail before any copy.

Do not run host samply. Do not `git add` any `.json.gz`. Keep `bundle.rs` ≤628 lines.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI success copies pair, gzip, and *syms* into a new stamp</name>
  <files>tools/xtask/tests/playground_cli/bundle.rs</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-04, D-06
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md` Pattern 1, Pitfall 4
    1. `tools/xtask/tests/playground_cli/support.rs`
    1. `tools/xtask/tests/playground_cli/pair.rs` success test as the fixture pattern
    1. `tools/xtask/src/playground/bundle.rs` from 23-02
  </read_first>
  <behavior>
    1. CLI `playground dam-break-audit-bundle` after a fake pair+profile at the same fixture git HEAD writes a new stamp containing nonempty `pair.json`, `pair.md`, `rust.json.gz`, copied `rust.json.syms.json`, and `audit-bundle-identity.json` with `kind=audit_bundle`, `not_timing_authority=true`, `timing_authority=unprofiled_wall_clock`, `profile_blob=rust.json.gz`.
    2. Source pair and profile stamp bytes are unchanged after the bundle command.
    3. Destination stamp path is not equal to the source pair stamp or source profile stamp.
    4. `justfile` recipe `playground-dam-break-audit-bundle` is exactly `cargo xtask playground dam-break-audit-bundle` (string check via `include_str` of the repo justfile, same style as profile tests).
  </behavior>
  <action>
    Write the success test first (RED if 23-02 sidecar copy is incomplete), then fix only `playground/bundle.rs` if a 23-02 gap appears — prefer keeping production edits in 23-02; this plan should only need `playground_cli/bundle.rs` unless a copy bug is found, in which case patch `tools/xtask/src/playground/bundle.rs` and list it in the SUMMARY deviation.

    Do not edit `pair.rs` (engine) or `playground_cli.rs`. Do not add dhat.
  </action>
  <verify>
    <automated>cargo test -p xtask --test playground_cli -- --test-threads=1 bundle</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "dam-break-audit-bundle" tools/xtask/tests/playground_cli/bundle.rs` matches.
    1. `rg -n "audit-bundle-identity.json" tools/xtask/tests/playground_cli/bundle.rs` matches.
    1. `rg -n "syms" tools/xtask/tests/playground_cli/bundle.rs` matches.
    1. `rg -n "not_timing_authority" tools/xtask/tests/playground_cli/bundle.rs` matches.
    1. `cargo test -p xtask --test playground_cli -- --test-threads=1 bundle` exits 0.
    1. `tools/xtask/tests/playground_cli/bundle.rs` has ≤628 physical lines.
  </acceptance_criteria>
  <done>Fake-tool CLI proves copy-only bundling including a presymbolicate sidecar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI fail-closed for mismatch, missing gzip, traversal, and profile-tainted pair.json</name>
  <files>tools/xtask/tests/playground_cli/bundle.rs</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-04
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md` Pattern 1 fail-closed table, Security Domain V5
    1. Task 1 tests in `playground_cli/bundle.rs`
  </read_first>
  <behavior>
    1. After pair+profile, rewrite profile-identity `git_head` to a different 40-hex; `dam-break-audit-bundle` exits nonzero; no new `audit-bundle-identity.json` appears.
    2. Delete `rust.json.gz` from the profile stamp; command exits nonzero.
    3. `--pair-stamp ../etc` and `--pair-stamp 2001-09-09T01:46:40Z` exit nonzero; stderr mentions the stamp/bundle error; no directory named `etc` is created under the fixture evidence root.
    4. Inject `"samply": true` into source `pair.json`; command exits nonzero and does not copy that tainted pair into a new stamp as a successful bundle.
  </behavior>
  <action>
    One concern per test function. Use Arrange/Act/Assert. Do not treat a skip-shaped stderr as success (`skip` must not appear as a success path).

    Keep editing only `playground_cli/bundle.rs` plus 23-02 production files if a fail-closed hole exists. Do not run live Dam Break.
  </action>
  <verify>
    <automated>cargo test -p xtask --test playground_cli -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "git_head" tools/xtask/tests/playground_cli/bundle.rs` matches a mismatch test.
    1. `rg -n "\\.\\./|01:46:40Z" tools/xtask/tests/playground_cli/bundle.rs` matches a traversal/invalid-stamp test.
    1. `rg -n "samply" tools/xtask/tests/playground_cli/bundle.rs` matches a tainted-pair test.
    1. `cargo test -p xtask --test playground_cli -- --test-threads=1` exits 0 (full crate, including pair/profile/timers).
    1. `git ls-files '*.json.gz'` is still empty.
  </acceptance_criteria>
  <done>Bundle command fails closed on HEAD mismatch, missing gzip, path traversal, and profile-tainted pair.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI stamp flags in tests | Must prove traversal is rejected, not only happy-path flags. |
| Fake gzip bytes vs live samply | Tests must not treat `fake-samply-json-gz` as named-function evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | T | `--pair-stamp` traversal | mitigate | Dedicated CLI test with `../` and colon timestamps (D-04 / V5). |
| T-23-03-02 | T | HEAD mismatch bundle | mitigate | CLI test rewrites `git_head` and expects nonzero + no identity file. |
| T-23-03-03 | S | fake gzip as audit names | mitigate | Tests assert copy/identity only; no function-name assertions from fake bytes (D-01). |
| T-23-03-04 | I | committing gzip | mitigate | Do not `git add` `*.json.gz`; keep fixtures under `target/`. |
</threat_model>

<verification>
`cargo test -p xtask --test playground_cli -- --test-threads=1`. No live pair. No host samply.
</verification>

<success_criteria>
Fake-stamp CLI covers copy success with sidecar and the locked fail-closed cases; sources are never moved.
</success_criteria>

<output>
After completion, create `.planning/phases/23-baseline-pair-and-named-audit/23-03-SUMMARY.md`
</output>
