---
phase: 23-baseline-pair-and-named-audit
plan: "09"
type: execute
wave: 7
depends_on: [23-08]
files_modified:
  - BENCHMARKING.md
autonomous: true
requirements: [PERF-AUDIT, PERF-HEAP]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-09-21T02-44-37
generated_at: 2026-09-21T03:02:06Z
must_haves:
  truths:
    - "cargo xtask package verify passes and cargo tree -p liquidfun --edges normal lists bitflags and none of dhat, samply, serde, flate2, or cmake."
    - "just playground-dam-break-bench, profile, timers, audit-bundle, and heap are each one-line cargo xtask aliases with no cmake/samply/dhat flags."
    - "pair.rs argv still has no --features dhat-heap; workspace [profile.profiling] is unchanged; no [profile.release] debug override."
    - "reference/performance/manifest.toml still has reviewed_reports = []; git ls-files has no json.gz, trace, or dhat-heap.json; crates/liquidfun/src is unmodified in this phase."
    - "BENCHMARKING.md points at docs/native-performance-audit.md as unreviewed named-function notes and still forbids copying playground numbers into the manifest."
    - "The implementing agent does not write an independent-review acknowledgment; 23-09-SUMMARY.md states that D-16 independent AI review remains required after /gsd-verify-work."
  artifacts:
    - path: BENCHMARKING.md
      provides: "Pointer to native-performance-audit.md without a public speed claim"
      contains: "native-performance-audit.md"
    - path: crates/liquidfun/Cargo.toml
      provides: "Still bitflags-only production deps"
      contains: "bitflags"
    - path: reference/performance/manifest.toml
      provides: "Empty reviewed_reports"
      contains: "reviewed_reports = []"
  key_links:
    - from: BENCHMARKING.md
      to: docs/native-performance-audit.md
      via: "unreviewed named-function notes"
      pattern: "native-performance-audit.md"
    - from: tools/xtask/src/package.rs
      to: crates/liquidfun
      via: "package verify isolation"
      pattern: "FORBIDDEN_PREFIXES"
---

<objective>
Prove package isolation, Bright Builds file-length, honest benchmarking copy, and that this phase did not become a physics or manifest change — without self-approving independent review.

Purpose: D-15 / D-16 / D-09 / D-10. Isolation is easy to lose once `dhat` enters the workspace lockfile.
Output: One BENCHMARKING.md sentence, passing verify/tree/clippy/file-length gates, SUMMARY that leaves D-16 review to a separate AI.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/verification.md
@PROJECT-SCOPE.md
@.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md
@BENCHMARKING.md
@docs/native-performance-audit.md
@crates/liquidfun/Cargo.toml
@reference/performance/manifest.toml
@justfile
@Cargo.toml

<interfaces>
BENCHMARKING.md already names gitignored `target/dam-break-perf/` stamps and denies profiled/timer clocks as the 3× number. Add one sentence in the Exploratory local diagnosis section pointing to `[docs/native-performance-audit.md](docs/native-performance-audit.md)` as unreviewed SHA-bound named-function notes, stating raw `.json.gz` / `dhat-heap.json` / `.trace` stay gitignored and must not be copied into `reference/performance/manifest.toml`. Do not paste wall_ms or the ratio.

Do not edit `crates/liquidfun/src/**`. Do not add TSV file-length exceptions for repo-owned files; split instead if `bun scripts/bright-builds-check.ts all` fails.

Independent review (D-16, owner 2026-09-16): the implementing agent must not approve its own work. Do not create a REVIEW.md acknowledgment bound to this agent's identity. Record in SUMMARY that `/gsd-verify-work 23` and a separate AI reviewer remain required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point BENCHMARKING.md at the unreviewed audit notes</name>
  <files>BENCHMARKING.md</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-09, D-15
    1. `BENCHMARKING.md` Exploratory local diagnosis section
    1. `docs/native-performance-audit.md` banner
    1. `reference/performance/manifest.toml`
    1. `AGENTS.md` Repo-Local Guidance for `just markdown-check`
  </read_first>
  <action>
    Edit only `BENCHMARKING.md`. Keep the existing “not a Phase 12 reviewed report” wording. Insert one additional sentence (two short sentences if wrapping requires it) that names `docs/native-performance-audit.md`, repeats that it is an unreviewed local sample, and forbids committing profile/heap blobs or copying the ratio into `reference/performance/manifest.toml`.

    Do not paste wall-ms, host CPU, or “Rust is N× slower.” Run `just markdown-check`.
  </action>
  <verify>
    <automated>just markdown-check &amp;&amp; rg -n "native-performance-audit.md" BENCHMARKING.md</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "native-performance-audit.md" BENCHMARKING.md` matches.
    1. `rg -n "manifest.toml" BENCHMARKING.md` still forbids copying playground numbers into the manifest.
    1. `git diff -- reference/performance/manifest.toml` is empty.
    1. `just markdown-check` exits 0.
  </acceptance_criteria>
  <done>BENCHMARKING.md links the named audit without turning it into a public speed claim.</done>
</task>

<task type="auto">
  <name>Task 2: Isolation, file-length, and no self-approval</name>
  <files>BENCHMARKING.md</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-10, D-15, D-16
    1. `standards/core/verification.md`
    1. `crates/liquidfun/Cargo.toml`
    1. `Cargo.toml` `[profile.profiling]`
    1. `justfile` playground recipes
    1. `tools/xtask/src/package.rs` FORBIDDEN_PREFIXES
  </read_first>
  <action>
    Per D-15, run isolation and honesty checks. Do not weaken `package verify`. Do not add `dhat` to `liquidfun`. If file-lengths fail, split the offending repo-owned file rather than adding a TSV exception.

    Required commands (all must exit 0), from the repo root:

    1. `cargo xtask package verify`
    1. `cargo tree -p liquidfun --edges normal` with a check that crate names `dhat`, `samply`, `serde`, `serde_json`, `flate2`, `cmake` do not appear
    1. `cargo fmt --all --check`
    1. `cargo clippy -p xtask -p liquidfun-wasm -p liquidfun --all-targets --all-features -- -D warnings`
    1. `cargo test -p xtask --lib playground -- --test-threads=1`
    1. `cargo test -p xtask --test playground_cli -- --test-threads=1`
    1. `cargo deny --locked check`
    1. `bun scripts/bright-builds-check.ts all`
    1. `just markdown-check`

    Also assert:

    - justfile playground recipes are exactly five one-line `cargo xtask playground dam-break-{bench,profile,timers,audit-bundle,heap}` aliases; `rg samply|cmake|dhat justfile` empty
    - `Cargo.toml` has `[profile.profiling]` `debug = true` and no `[profile.release]`
    - `crates/liquidfun/Cargo.toml` has `bitflags` and no `dhat`/`serde`/`samply`
    - `rg dhat-heap tools/xtask/src/playground/pair.rs` empty
    - `rg step_profiled crates/liquidfun-wasm/src/bin/dam_break_bench.rs` empty
    - `reference/performance/manifest.toml` `reviewed_reports = []`
    - `git diff -- crates/liquidfun/src` empty
    - `git ls-files '*.json.gz' '*.trace' 'dhat-heap.json'` empty
    - no `playground/mod.rs` or `tests/playground_cli/mod.rs`
    - `docs/native-performance-audit.md` contains `Unreviewed local sample` and `Not found`

    Record outputs in `23-09-SUMMARY.md`. Explicitly write that D-16 independent AI review is **not** satisfied by this plan and that the implementing agent must not approve the phase.
  </action>
  <verify>
    <automated>cargo xtask package verify &amp;&amp; cargo tree -p liquidfun --edges normal &amp;&amp; bun scripts/bright-builds-check.ts all</automated>
  </verify>
  <acceptance_criteria>
    1. `cargo xtask package verify` exits 0.
    1. `cargo tree -p liquidfun --edges normal` contains no `dhat`, `samply`, or `serde`.
    1. `rg -n "samply|cmake|dhat" justfile` is empty.
    1. `rg -n "dhat-heap" tools/xtask/src/playground/pair.rs` is empty.
    1. `rg -n "reviewed_reports = \\[\\]" reference/performance/manifest.toml` matches.
    1. `git ls-files '*.json.gz' '*.trace' 'dhat-heap.json'` is empty.
    1. `git diff -- crates/liquidfun/src` is empty.
    1. `bun scripts/bright-builds-check.ts all` exits 0.
    1. `23-09-SUMMARY.md` states independent AI review (D-16) is still required and is not performed by the implementer.
  </acceptance_criteria>
  <done>Isolation, just/xtask layering, Bright Builds, and honesty gates pass; the implementer does not self-approve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published `liquidfun` graph | Optional `dhat` on `liquidfun-wasm` must not leak here. |
| Committed docs vs gitignored stamps | BENCHMARKING.md may name paths, not embed dumps. |
| Review acknowledgment | Implementer identity must not be recorded as independent review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-09-01 | E | `liquidfun` supply chain | mitigate | `package verify` + `cargo tree -p liquidfun`; Cargo.toml bitflags-only (D-15). |
| T-23-09-02 | I | committed gzip/trace/dhat dumps | mitigate | `git ls-files` empty; BENCHMARKING.md forbids commit (D-09). |
| T-23-09-03 | S | public timing claim / manifest row | mitigate | Manifest `reviewed_reports = []`; no wall-ms pasted into BENCHMARKING.md (D-09). |
| T-23-09-04 | T | hiding flags in just | mitigate | rg justfile; five one-line cargo xtask recipes (D-05). |
| T-23-09-05 | T | physics edits sneaking in | mitigate | `git diff -- crates/liquidfun/src` empty (D-10). |
| T-23-09-06 | S | self-approval | accept | Independent AI review required later by owner policy; this plan only records automated evidence (D-16). |
| T-23-09-07 | T | dhat on the pair argv | mitigate | rg `pair.rs` for `dhat-heap` empty; gate stays unfeatured `--release`. |
</threat_model>

<verification>
`cargo xtask package verify`, `cargo tree -p liquidfun --edges normal`, `bun scripts/bright-builds-check.ts all`, `just markdown-check`, `cargo deny --locked check`. No package publication. No self-review.
</verification>

<success_criteria>
The named audit and optional heap tooling are isolated from published `liquidfun`, just stays a one-line printer, committed docs stay unreviewed samples, and independent review remains a separate step.
</success_criteria>

<output>
After completion, create `.planning/phases/23-baseline-pair-and-named-audit/23-09-SUMMARY.md`
</output>
