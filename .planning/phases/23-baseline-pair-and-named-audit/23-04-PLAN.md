---
phase: 23-baseline-pair-and-named-audit
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun-wasm/Cargo.toml
  - crates/liquidfun-wasm/src/bin/dam_break_bench.rs
  - Cargo.lock
autonomous: true
requirements: [PERF-HEAP]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-09-21T02-44-37
generated_at: 2026-09-21T03:02:06Z
must_haves:
  truths:
    - "liquidfun-wasm exposes optional feature dhat-heap = [\"dep:dhat\"] that is not default; dhat 0.3.3 is an optional dependency of liquidfun-wasm only."
    - "#[global_allocator] and dhat::Profiler live only in crates/liquidfun-wasm/src/bin/dam_break_bench.rs behind cfg(all(feature = \"dhat-heap\", not(target_arch = \"wasm32\")))."
    - "Default cargo run -p liquidfun-wasm --release --bin dam-break-bench does not enable dhat-heap; crates/liquidfun still depends only on bitflags."
    - "cargo deny --locked check passes after the lockfile update; cargo check -p liquidfun-wasm --bin dam-break-bench --features dhat-heap succeeds."
  artifacts:
    - path: crates/liquidfun-wasm/Cargo.toml
      provides: "Optional dhat-heap feature, never default"
      contains: "dhat-heap"
    - path: crates/liquidfun-wasm/src/bin/dam_break_bench.rs
      provides: "cfg-gated Alloc + Profiler writing LIQUIDFUN_DHAT_HEAP_FILE"
      contains: "dhat::Alloc"
  key_links:
    - from: crates/liquidfun-wasm/src/bin/dam_break_bench.rs
      to: dhat 0.3.3
      via: "optional feature dhat-heap"
      pattern: "feature = \"dhat-heap\""
    - from: crates/liquidfun/Cargo.toml
      to: bitflags
      via: "unchanged production deps"
      pattern: "bitflags"
---

<objective>
Add private optional `dhat` 0.3.3 heap instrumentation on the `dam-break-bench` binary only, without contaminating the unprofiled `--release` gate artifact or published `liquidfun`.

Purpose: D-11 / D-15. Heap command wiring is 23-05; this plan only makes the feature compile and stay off by default.
Output: `dhat-heap` feature, bin-only allocator, updated `Cargo.lock`. No xtask command yet.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/languages/rust.md
@.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md
@.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md
@crates/liquidfun-wasm/Cargo.toml
@crates/liquidfun-wasm/src/bin/dam_break_bench.rs
@crates/liquidfun-wasm/src/lib.rs
@crates/liquidfun/Cargo.toml
@deny.toml

<interfaces>
Workspace `unsafe_code = "forbid"` remains. `dhat`'s unsafe stays inside the `dhat` crate. Do not add `unsafe` to `liquidfun` or `liquidfun-wasm` sources. Do not put `#[global_allocator]` in `lib.rs` (cdylib / wasm / timers must stay uninstrumented).

`crates/liquidfun-wasm/Cargo.toml` must gain:

```toml
[features]
default = []
dhat-heap = ["dep:dhat"]

[dependencies]
dhat = { version = "0.3.3", optional = true }
```

Keep existing `liquidfun` and `wasm-bindgen` deps. Do not add `dhat` to `[workspace.dependencies]`. Do not add `dhat` to `crates/liquidfun/Cargo.toml`.

Bin wiring (docs.rs/dhat/0.3.3), inside `fn run` **before** `parse_counts` / `run_dam_break_bench`:

```rust
#[cfg(all(feature = "dhat-heap", not(target_arch = "wasm32")))]
#[global_allocator]
static ALLOC: dhat::Alloc = dhat::Alloc;

#[cfg(all(feature = "dhat-heap", not(target_arch = "wasm32")))]
let _profiler = match std::env::var_os("LIQUIDFUN_DHAT_HEAP_FILE") {
    Some(path) => dhat::Profiler::builder().file_name(path).build(),
    None => dhat::Profiler::new_heap(),
};
```

Keep `_profiler` in scope until `run` returns so Drop writes the dump. Do not call `World::step_profiled` from this bin. Default unfeatured path must still print the existing JSON report on stdout.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional dhat-heap on dam-break-bench only</name>
  <files>crates/liquidfun-wasm/Cargo.toml, crates/liquidfun-wasm/src/bin/dam_break_bench.rs, Cargo.lock</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-11, D-15
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md` Pattern 2, Pitfall 3, Pitfall 7
    1. `crates/liquidfun-wasm/Cargo.toml`
    1. `crates/liquidfun-wasm/src/bin/dam_break_bench.rs`
    1. `crates/liquidfun-wasm/src/lib.rs` `#![forbid(unsafe_code)]`
    1. `crates/liquidfun/Cargo.toml`
  </read_first>
  <action>
    Per D-11, add optional `dhat` 0.3.3 and feature `dhat-heap` that is **not** default. Wire `#[global_allocator]` and `Profiler` only in `src/bin/dam_break_bench.rs` under `cfg(all(feature = "dhat-heap", not(target_arch = "wasm32")))`.

    Honor `LIQUIDFUN_DHAT_HEAP_FILE` as an `OsString` path for `Profiler::builder().file_name(path)` so 23-05 can point at a minted stamp. Unset env may use `new_heap()` (cwd `dhat-heap.json`) — production xtask must set the env later; do not create a repo-root dump in this plan.

    Do not enable the feature on `dam-break-timers`. Do not edit `crates/liquidfun/src/**`. Do not edit `justfile` or xtask. After adding the dep, update `Cargo.lock` via `cargo check -p liquidfun-wasm --bin dam-break-bench --features dhat-heap` (or `cargo update -p dhat --precise 0.3.3` if needed). `cargo fmt` the bin.

    If `dhat` 0.3.3 is yanked or newer-only, stop and record a blocker — do not silently pick another version (CONTEXT locks 0.3.3).
  </action>
  <verify>
    <automated>cargo check -p liquidfun-wasm --bin dam-break-bench --features dhat-heap</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "dhat-heap = \[\"dep:dhat\"\]" crates/liquidfun-wasm/Cargo.toml` matches.
    1. `rg -n "^default = \[\]" crates/liquidfun-wasm/Cargo.toml` matches (feature default empty).
    1. `rg -n "version = \"0.3.3\"" crates/liquidfun-wasm/Cargo.toml` matches on the dhat dep.
    1. `rg -n "dhat::Alloc" crates/liquidfun-wasm/src/bin/dam_break_bench.rs` matches.
    1. `rg -n "LIQUIDFUN_DHAT_HEAP_FILE" crates/liquidfun-wasm/src/bin/dam_break_bench.rs` matches.
    1. `rg -n "global_allocator" crates/liquidfun-wasm/src/lib.rs crates/liquidfun-wasm/src/bin/dam_break_timers.rs crates/liquidfun/src` is empty.
    1. `rg -n "dhat|samply|serde" crates/liquidfun/Cargo.toml` is empty.
    1. `rg -n "step_profiled" crates/liquidfun-wasm/src/bin/dam_break_bench.rs` is empty.
    1. `cargo check -p liquidfun-wasm --bin dam-break-bench --features dhat-heap` exits 0.
  </acceptance_criteria>
  <done>Optional dhat-heap compiles on the bench bin and is absent from liquidfun and from lib.rs.</done>
</task>

<task type="auto">
  <name>Task 2: Prove default gate binary stays unfeatured and deny/tree isolation</name>
  <files>crates/liquidfun-wasm/Cargo.toml, Cargo.lock</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-11, D-15
    1. `deny.toml` license allowlist
    1. `crates/liquidfun/Cargo.toml`
    1. `tools/xtask/src/playground/pair.rs` rust cargo argv (read only; must stay `--release` without `--features`)
  </read_first>
  <action>
    Per D-15, run isolation checks. Do not "fix" a deny failure by weakening `deny.toml` allowlists unless `dhat` 0.3.3 uses a license already in the allow list (MIT/Apache-2.0 are already allowed). If deny fails for another reason, diagnose rather than adding `dhat` to `liquidfun`.

    Required from repo root:

    1. `cargo check -p liquidfun-wasm --bin dam-break-bench` **without** `--features` exits 0.
    1. `cargo tree -p liquidfun --edges normal` does not list `dhat`, `samply`, `serde`, `serde_json`, `flate2`, or `cmake`.
    1. `cargo tree -p liquidfun-wasm --edges features -i dhat` (or `cargo tree -p liquidfun-wasm --features dhat-heap -i dhat`) shows dhat only when the feature is enabled.
    1. `cargo deny --locked check` exits 0.
    1. `rg -n "dhat-heap|--features" tools/xtask/src/playground/pair.rs` is empty.

    Record command outputs in `23-04-SUMMARY.md`. Do not add xtask heap command yet.
  </action>
  <verify>
    <automated>cargo check -p liquidfun-wasm --bin dam-break-bench &amp;&amp; cargo deny --locked check &amp;&amp; cargo tree -p liquidfun --edges normal</automated>
  </verify>
  <acceptance_criteria>
    1. `cargo check -p liquidfun-wasm --bin dam-break-bench` (no features) exits 0.
    1. `cargo tree -p liquidfun --edges normal` output contains no line with crate name `dhat`.
    1. `rg -n "name = \"dhat\"" Cargo.lock` matches (lockfile records the optional dep).
    1. `rg -n "dhat-heap" tools/xtask/src/playground/pair.rs` is empty.
    1. `cargo deny --locked check` exits 0.
    1. `git diff -- crates/liquidfun/Cargo.toml crates/liquidfun/src` is empty.
  </acceptance_criteria>
  <done>Default --release bench path is unfeatured; liquidfun tree stays bitflags-only; deny passes with dhat 0.3.3 in the lockfile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Optional crate `dhat` → `liquidfun-wasm` | crates.io only; must not enter published `liquidfun`. |
| Feature fingerprint → `target/release/dam-break-bench` | Enabling dhat-heap on `--release` would replace the gate binary. This plan must not add `--features` to the pair argv. |
| `#[global_allocator]` → process | Must not apply to wasm cdylib or timers bin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | E | `liquidfun` supply chain | mitigate | dhat only on `liquidfun-wasm` (`publish = false`); `cargo tree -p liquidfun` has no dhat (D-15). |
| T-23-04-02 | T | gate binary contamination | mitigate | Feature is optional and not default; pair.rs argv unchanged; heap spawn uses `--profile profiling` in 23-05 (D-11, RESEARCH Pitfall 3). |
| T-23-04-03 | T | unknown-git dhat | mitigate | crates.io 0.3.3; `deny.toml` `unknown-git = "deny"`; `cargo deny --locked check`. |
| T-23-04-04 | I | cwd `dhat-heap.json` | mitigate | Bin honors `LIQUIDFUN_DHAT_HEAP_FILE`; 23-05 must set it to a stamp path (Pitfall 7). This plan does not run the bin. |
| T-23-04-05 | E | `unsafe` in repo sources | mitigate | No `unsafe` in wasm/liquidfun sources; workspace `unsafe_code = "forbid"`; dhat unsafe stays upstream. |
</threat_model>

<verification>
`cargo check` with and without `--features dhat-heap`, `cargo tree -p liquidfun --edges normal`, `cargo deny --locked check`. Do not run Dam Break. Do not add just aliases.
</verification>

<success_criteria>
`dhat-heap` exists as a private optional bin feature; the unprofiled `--release` path and `liquidfun` graph are unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/23-baseline-pair-and-named-audit/23-04-SUMMARY.md`
</output>
