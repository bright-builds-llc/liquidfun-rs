---
phase: 23-baseline-pair-and-named-audit
plan: "02"
type: tdd
wave: 2
depends_on: [23-01]
files_modified:
  - tools/xtask/src/playground.rs
  - tools/xtask/src/playground/bundle.rs
  - tools/xtask/src/playground/stamp.rs
  - tools/xtask/src/main.rs
  - justfile
autonomous: true
requirements: [PERF-AUDIT]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-09-21T02-44-37
generated_at: 2026-09-21T03:02:06Z
must_haves:
  truths:
    - "just playground-dam-break-audit-bundle is a one-line alias for cargo xtask playground dam-break-audit-bundle with no cmake, samply, or dhat flags."
    - "dam-break-audit-bundle mints a new exclusive target/dam-break-perf/<utc-stamp>/, copies (never moves) pair.json, pair.md, rust.json.gz, and any *syms* sibling from same-HEAD source stamps, and writes audit-bundle-identity.json."
    - "The command fails closed when either source is missing, git_head values differ, pair.json is not kind unprofiled_pair, rust.json.gz is missing, or a --pair-stamp/--profile-stamp value is not a filename-safe YYYY-MM-DDTHH-MM-SSZ."
    - "Copied pair.json keeps timing_authority unprofiled_wall_clock; the bundle identity marks the profile blob not_timing_authority true and does not recompute rust_over_cpp_ratio from samply."
  artifacts:
    - path: justfile
      provides: "Thin playground-dam-break-audit-bundle alias"
      contains: "cargo xtask playground dam-break-audit-bundle"
    - path: tools/xtask/src/playground/bundle.rs
      provides: "Copy-only same-HEAD audit bundle"
      contains: "audit-bundle-identity.json"
    - path: tools/xtask/src/playground.rs
      provides: "Dispatcher arm for dam-break-audit-bundle"
      contains: "bundle::run"
  key_links:
    - from: justfile
      to: tools/xtask/src/playground.rs
      via: "one-line cargo xtask playground dam-break-audit-bundle"
      pattern: "dam-break-audit-bundle"
    - from: tools/xtask/src/playground/bundle.rs
      to: tools/xtask/src/playground/stamp.rs
      via: "mint_exclusive_stamp plus parse_stamp_name"
      pattern: "mint_exclusive_stamp"
---

<objective>
Add the copy-only same-HEAD audit-bundle command and thin just alias so a developer can point at one dated directory that contains the unprofiled pair report and the samply `rust.json.gz`.

Purpose: D-04 / D-05 / D-06. Phase 22 writes pair and profile into sibling exclusive stamps; Phase 23 must copy, never merge or overwrite.
Output: `playground/bundle.rs`, `mod bundle` dispatcher, `just playground-dam-break-audit-bundle`, stamp-name parser. Fake-stamp CLI coverage is 23-03.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md
@.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md
@tools/xtask/src/playground.rs
@tools/xtask/src/playground/stamp.rs
@tools/xtask/src/playground/pair.rs
@tools/xtask/src/playground/profile.rs
@justfile

<interfaces>
Do **not** grow `pair.rs` (616/628) or `tools/xtask/tests/playground_cli.rs`. New work lives in `playground/bundle.rs` and a small `parse_stamp_name` helper on `stamp.rs`.

CLI (D-05, RESEARCH Pattern 1):

```text
cargo xtask playground dam-break-audit-bundle
  [--pair-stamp <utc>] [--profile-stamp <utc>]
```

`just` recipe (exact body, one command line, no flags):

```just
playground-dam-break-audit-bundle:
    cargo xtask playground dam-break-audit-bundle
```

Stamp names must match `^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}-[0-9]{2}-[0-9]{2}Z$`. Reject `/`, `\`, `..`, NUL, empty, or any other charset. Join only as `repository_root.join("target/dam-break-perf").join(stamp)`.

Add to `stamp.rs` (keep file ≤628):

```rust
pub(super) fn evidence_dir(repository_root: &Path) -> PathBuf;
pub(super) fn parse_stamp_name(raw: &str) -> Result<String, PlaygroundError>;
```

`bundle::run` sequence:

1. Parse optional `--pair-stamp` / `--profile-stamp`; reject unknown flags (no `--warmup` on this command).
2. `repository_root` + `host_identity` for current `git_head`.
3. If flags omitted: scan `evidence_dir` child directories; pick the lexicographically latest dir whose `pair.json` has `kind == "unprofiled_pair"` and `git_head` equal to current HEAD, and the lexicographically latest dir whose `profile-identity.json` has `kind == "samply_cpu"` and the same `git_head`.
4. If flags present: use those dirs; still require the JSON kinds and matching `git_head` values (pair vs profile vs each other).
5. Require source files: `pair.json`, `pair.md`, `rust.json.gz` (nonempty). Fail if `pair.json` contains keys `samply`, `profile`, `not_timing_authority`, or `cargo_profile`.
6. `mint_exclusive_stamp` (honor `LIQUIDFUN_XTASK_STAMP_UNIX` the same way `profile.rs` does — duplicate the env read in `bundle.rs`, do not edit `pair.rs`).
7. `fs::copy` each required file. Also copy every source-profile file whose name contains `syms` (e.g. `rust.json.syms.json`). Never `rename`, never `remove_dir_all`, never write into the source stamps.
8. Write `audit-bundle-identity.json` with exactly these keys (values as specified): `kind: "audit_bundle"`, `timing_authority: "unprofiled_wall_clock"`, `not_timing_authority: true`, `git_head`, `source_pair_stamp`, `source_profile_stamp`, `copied` (array of copied filenames), `profile_blob: "rust.json.gz"`, `disclaimer` equal to `Unreviewed local playground Dam Break sample. Profile blobs are not the 3x number.`, optional `worktree_dirty` bool from `git status --porcelain` (warn on stderr if dirty; do not hard-fail).
9. eprint `wrote {stamp_dir}`. Do not print samply durations. Do not recompute the ratio.

Error kind string: `"bundle"`. `PlaygroundError::new("bundle", ...)`.

Update `playground.rs` USAGE and the missing-subcommand expected string to list `dam-break-audit-bundle` after the three Phase 22 commands. Dispatcher: `"dam-break-audit-bundle" => bundle::run(command_args)`. Update `tools/xtask/src/main.rs` playground blurb to mention the audit bundle. Do not add `dam-break-heap` yet (23-05).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD stamp-name parse, same-HEAD selection, and copy-only bundle core</name>
  <files>tools/xtask/src/playground/stamp.rs, tools/xtask/src/playground/bundle.rs, tools/xtask/src/playground.rs</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-03, D-04, D-06
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md` Pattern 1, Pitfall 2, Pitfall 4, Security Domain V5
    1. `standards/core/testing.md` Arrange / Act / Assert
    1. `tools/xtask/src/playground/stamp.rs` `format_utc_stamp` / `mint_exclusive_stamp`
    1. `tools/xtask/src/playground/pair.rs` `pair_report_json` keys (read only)
    1. `tools/xtask/src/playground/profile.rs` identity keys (read only)
  </read_first>
  <behavior>
    1. `parse_stamp_name("2001-09-09T01-46-40Z")` returns Ok of that string.
    1. `parse_stamp_name("../etc")`, `parse_stamp_name("2001-09-09T01:46:40Z")`, `parse_stamp_name("2001-09-09T01-46-40Z/foo")`, and `parse_stamp_name("")` each return Err whose Display contains `bundle` or `stamp` and does not succeed.
    1. Given a temp evidence tree with pair stamp A (`kind=unprofiled_pair`, `git_head=aaa...`) and profile stamp B (`kind=samply_cpu`, same head, nonempty `rust.json.gz` plus `rust.json.syms.json`), `bundle::copy_audit_bundle` (or `run` against that root) creates a third exclusive stamp containing byte-identical copies of `pair.json`, `pair.md`, `rust.json.gz`, and the syms sidecar; source files still exist with original bytes.
    1. HEAD mismatch (pair `git_head` `aaa`, profile `bbb`) returns Err and mints no bundle stamp (or mints none that contain `audit-bundle-identity.json`).
    1. Missing `rust.json.gz` returns Err; sources are untouched.
    1. `audit-bundle-identity.json` has `kind=audit_bundle`, `not_timing_authority=true`, `timing_authority=unprofiled_wall_clock`, `profile_blob=rust.json.gz`, and `copied` including `pair.json`, `pair.md`, `rust.json.gz`.
    1. `pair.json` in the destination still has `timing_authority=unprofiled_wall_clock` and no `samply` key.
  </behavior>
  <action>
    Per D-04, write failing unit tests in `playground/bundle.rs` (and stamp-name tests in `stamp.rs`) first, confirm RED, then implement.

    Tests must use a temp directory under `target/xtask-test-fixtures/`, never the live `target/dam-break-perf` of the real repo. Do not call host samply or cmake.

    Implement copy with `fs::copy` only. Never `fs::rename`. Never `remove_dir_all` on source or dest stamps. If destination mint fails because eight consecutive stamps exist, propagate the existing stamp error.

    `not_timing_authority` on the identity applies to the profile blob. Do not strip `timing_authority` from copied `pair.json` and do not write a new `rust_over_cpp_ratio`.

    `mod bundle;` in `playground.rs`. Keep every new/touched playground source file ≤628 lines. No `unwrap()` in production paths. Optional values named `maybe_*`. `let...else` for missing JSON fields.

    Do not wire the just alias until Task 2 if that keeps TDD focused; dispatcher `bundle::run` may exist after this task so unit tests can call it.
  </action>
  <verify>
    <automated>cargo test -p xtask --lib playground::bundle -- --test-threads=1 &amp;&amp; cargo test -p xtask --lib playground::stamp -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "mod bundle;" tools/xtask/src/playground.rs` matches.
    1. `rg -n "fn parse_stamp_name" tools/xtask/src/playground/stamp.rs` matches.
    1. `rg -n "fs::rename|remove_dir_all" tools/xtask/src/playground/bundle.rs` is empty.
    1. `rg -n "fs::copy" tools/xtask/src/playground/bundle.rs` matches.
    1. `rg -n "not_timing_authority" tools/xtask/src/playground/bundle.rs` matches.
    1. `rg -n "unprofiled_wall_clock" tools/xtask/src/playground/bundle.rs` matches.
    1. `rg -n "unwrap\\(" tools/xtask/src/playground/bundle.rs` is empty.
    1. `cargo test -p xtask --lib playground::bundle -- --test-threads=1` exits 0.
    1. `tools/xtask/src/playground/bundle.rs` and `stamp.rs` each have ≤628 physical lines.
  </acceptance_criteria>
  <done>Unit-tested copy-only bundle core rejects bad stamp names and HEAD mismatch and never moves source files.</done>
</task>

<task type="auto">
  <name>Task 2: Wire dispatcher USAGE and the one-line just alias</name>
  <files>tools/xtask/src/playground.rs, tools/xtask/src/main.rs, justfile</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-05
    1. `justfile` existing `playground-dam-break-bench` / `profile` / `timers` recipes
    1. `tools/xtask/src/playground.rs` USAGE and unit tests
    1. `tools/xtask/src/main.rs` playground command blurb
  </read_first>
  <action>
    Per D-05, add `playground-dam-break-audit-bundle` immediately after `playground-dam-break-timers` in `justfile`. The recipe body must be exactly one line: `cargo xtask playground dam-break-audit-bundle`. The justfile must still contain zero occurrences of `samply`, `cmake`, `-g`, `dhat`, or `--features`.

    Update `USAGE` in `playground.rs` to:
    `Usage: cargo xtask playground <dam-break-bench|dam-break-profile|dam-break-timers|dam-break-audit-bundle> [--warmup <n>] [--steps <n>] [--pair-stamp <utc>] [--profile-stamp <utc>]`

    Update the missing-subcommand error text to mention `dam-break-audit-bundle`. Keep unknown commands as usage errors. Do not add `dam-break-heap`.

    Update `main.rs` playground help line to include audit-bundle. Run `cargo fmt --all`.
  </action>
  <verify>
    <automated>rg -n "playground-dam-break-audit-bundle:" justfile &amp;&amp; cargo test -p xtask --lib playground -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `justfile` contains a `playground-dam-break-audit-bundle` recipe whose only command line is `cargo xtask playground dam-break-audit-bundle`.
    1. `rg -n "samply|cmake|-g|dhat" justfile` is empty.
    1. `rg -n "bundle::run" tools/xtask/src/playground.rs` matches.
    1. `rg -n "dam-break-heap" tools/xtask/src/playground.rs justfile` is empty.
    1. `cargo test -p xtask --lib playground -- --test-threads=1` exits 0.
    1. `justfile` `playground-dam-break-bench` recipe remains `cargo xtask playground dam-break-bench`.
  </acceptance_criteria>
  <done>Developer-facing one-line audit-bundle alias exists; just still hides no profiler flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--pair-stamp` / `--profile-stamp` CLI text → filesystem path | Untrusted filename joined under `target/dam-break-perf/`. |
| Source stamp files → destination stamp | Copy only; sources stay authoritative until a new mint. |
| Copied `pair.json` vs profile blob | Pair remains timing authority; gzip is not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | T | stamp CLI flags | mitigate | Allowlist `YYYY-MM-DDTHH-MM-SSZ` only; reject `/`, `..`, `:`, NUL (ASVS V5). |
| T-23-02-02 | T | existing stamps | mitigate | `mint_exclusive_stamp`; copy into the new dir; never `remove_dir_all` or merge (D-04). |
| T-23-02-03 | T | mixing profiled times into pair.json | mitigate | Fail if source `pair.json` has profile keys; do not rewrite ratio from samply (D-03). |
| T-23-02-04 | I | `rust.json.gz` | mitigate | Destination stays under gitignored `/target/`; just recipe has no samply flags (D-05). |
| T-23-02-05 | E | `git status` / `git rev-parse` | mitigate | `Command::new(LIQUIDFUN_XTASK_GIT else git)` argv lists, never `sh -c`. |
</threat_model>

<verification>
`cargo test -p xtask --lib playground::bundle` and `playground::stamp`, plus justfile greps. CLI fake-stamp cases are 23-03. Do not run live Dam Break.
</verification>

<success_criteria>
`just playground-dam-break-audit-bundle` exists as a one-line printer; unit tests prove copy-only same-HEAD bundling and stamp-name allowlisting.
</success_criteria>

<output>
After completion, create `.planning/phases/23-baseline-pair-and-named-audit/23-02-SUMMARY.md`
</output>
