---
phase: 23-baseline-pair-and-named-audit
plan: "05"
type: tdd
wave: 3
depends_on: [23-02, 23-04]
files_modified:
  - tools/xtask/src/playground.rs
  - tools/xtask/src/playground/symbols.rs
  - tools/xtask/src/playground/heap.rs
  - tools/xtask/src/main.rs
  - justfile
autonomous: true
requirements: [PERF-HEAP]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-09-21T02-44-37
generated_at: 2026-09-21T03:02:06Z
must_haves:
  truths:
    - "just playground-dam-break-heap is a one-line alias for cargo xtask playground dam-break-heap with no dhat, samply, or cmake flags."
    - "playground.rs USAGE is exactly Usage: cargo xtask playground <dam-break-bench|dam-break-profile|dam-break-timers|dam-break-audit-bundle|dam-break-heap> [--warmup <n>] [--steps <n>] [--pair-stamp <utc>] [--profile-stamp <utc>] [--stamp <utc>]; missing-subcommand text lists all five commands; each command rejects flags it does not implement."
    - "The heap gate scans rust.json.syms.json (Path::with_extension(\"syms.json\") on rust.json.gz) first, then any other *syms* file, then decompressed rust.json.gz JSON strings, and matches case-sensitive needles alloc::, __rust_alloc, __rdl_alloc, __rg_alloc, alloc::vec::Vec, RawVec, to_vec, GlobalAlloc, core::alloc::, core::clone::."
    - "Needles present => heap proceeds; symbols present but zero needles => nonzero exit, no dump; gzip that is not JSON / address-only with no sidecar => nonzero exit whose message is could not read symbols, not no allocator time."
    - "On a match, cargo run uses --profile profiling --bin dam-break-bench --features dhat-heap and never --release; LIQUIDFUN_DHAT_HEAP_FILE points at a newly minted sibling stamp dhat-heap.json; identity has not_timing_authority true."
  artifacts:
    - path: tools/xtask/src/playground/symbols.rs
      provides: "Fail-closed allocator/Vec heuristic"
      exports: ["classify_profile_symbols"]
    - path: tools/xtask/src/playground/heap.rs
      provides: "Gated dhat spawn into exclusive stamp"
      contains: "LIQUIDFUN_DHAT_HEAP_FILE"
    - path: justfile
      provides: "Thin playground-dam-break-heap alias"
      contains: "cargo xtask playground dam-break-heap"
  key_links:
    - from: tools/xtask/src/playground/heap.rs
      to: tools/xtask/src/playground/symbols.rs
      via: "classify_profile_symbols before spawn"
      pattern: "classify_profile_symbols"
    - from: heap::run
      to: dam-break-bench
      via: "cargo run --profile profiling --features dhat-heap"
      pattern: "dhat-heap"
---

<objective>
Add the fail-closed samply-symbol heuristic and the `dam-break-heap` xtask/just command that runs private dhat only after allocator/`Vec` needles match.

Purpose: D-12 / D-13. Do not run dhat just in case. Prefer `--profile profiling --features dhat-heap` so `target/release/dam-break-bench` stays the unprofiled gate binary (RESEARCH Pattern 2).
Output: `symbols.rs`, `heap.rs`, dispatcher arm, one-line just alias. Fake-cargo CLI tests are 23-06.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md
@.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md
@tools/xtask/src/playground.rs
@tools/xtask/src/playground/stamp.rs
@tools/xtask/src/playground/profile.rs
@crates/liquidfun-wasm/src/bin/dam_break_bench.rs
@justfile

<interfaces>
flate2 is already an xtask dependency. Use `flate2::read::GzDecoder`. Do not add another gzip crate.

```rust
pub(super) enum HeapGate {
    Run { matched_needles: Vec<String> },
    SkipNoAllocator,
    SkipNoSymbols,
}

pub(super) fn classify_profile_symbols(profile_dir: &Path) -> Result<HeapGate, PlaygroundError>;
```

Scan order (D-12, RESEARCH Pattern 3):

1. `profile_dir.join("rust.json.gz")` with `with_extension("syms.json")` → `rust.json.syms.json` if that file exists.
2. Any other `*syms*` file in `profile_dir`.
3. Decompress `rust.json.gz` and walk JSON strings (`threads[].stringArray` if present, else recursive string walk via `serde_json::Value`).

Needles (case-sensitive, D-12 plus RESEARCH demangled list). A match is substring presence in any scanned string:

- `alloc::`
- `__rust_alloc`
- `__rdl_alloc`
- `__rg_alloc`
- `alloc::vec::Vec`
- `RawVec`
- `to_vec`
- `GlobalAlloc`
- `core::alloc::`
- `core::clone::`

`clone` alone is **not** a needle (too broad). Use `core::clone::` as specified.

`HeapGate` mapping:

| Evidence | Return | heap::run |
|----------|--------|-----------|
| ≥1 needle | `Run` | spawn dhat |
| Readable symbols, zero needles | `SkipNoAllocator` | Err kind `heap`, message contains `no allocator` or `no allocator/Vec`; write **no** dump |
| Missing gzip, invalid gzip, non-JSON, or only `0x…` addresses with no sidecar | `SkipNoSymbols` | Err kind `heap`, message contains `could not read symbols`; **not** the no-allocator wording |

CLI:

```text
cargo xtask playground dam-break-heap [--stamp <utc>] [--warmup <n>] [--steps <n>]
```

`--stamp` uses `stamp::parse_stamp_name`. If omitted, pick the lexicographically latest evidence dir for current `git_head` that contains `rust.json.gz` and either `audit-bundle-identity.json` (`kind=audit_bundle`) or `profile-identity.json` (`kind=samply_cpu`). Prefer an audit-bundle stamp when two latest names tie on HEAD (if both exist, latest name still wins because UTC sorts).

On `Run`:

1. `mint_exclusive_stamp` (sibling stamp, not reuse of the bundle dir — RESEARCH Pitfall 7 / D-04 file set stays a pure copy).
2. Set `LIQUIDFUN_DHAT_HEAP_FILE` to `{stamp}/dhat-heap.json`.
3. `Command::new(cargo)` with `LIQUIDFUN_XTASK_CARGO` else `CARGO` else `cargo`, argv exactly: `run -p liquidfun-wasm --profile profiling --bin dam-break-bench --features dhat-heap -- --warmup N --steps M` (defaults 60/600 via `parse_counts`). **Never** pass `--release`.
4. Require destination `dhat-heap.json` exists and `metadata.len() > 0`.
5. Write `heap-identity.json`: `kind: "dhat_heap"`, `not_timing_authority: true`, `cargo_profile: "profiling"`, `features: ["dhat-heap"]`, `git_head`, `source_stamp`, `matched_needles`, `dump: "dhat-heap.json"`, `disclaimer` stating the dump is not the 3× number.

`just` recipe:

```just
playground-dam-break-heap:
    cargo xtask playground dam-break-heap
```

Combined USAGE constant in `playground.rs` (exact string, one line):

```text
Usage: cargo xtask playground <dam-break-bench|dam-break-profile|dam-break-timers|dam-break-audit-bundle|dam-break-heap> [--warmup <n>] [--steps <n>] [--pair-stamp <utc>] [--profile-stamp <utc>] [--stamp <utc>]
```

Missing-subcommand error text (exact, used by `missing_subcommand_is_a_usage_error`):

```text
expected `dam-break-bench`, `dam-break-profile`, `dam-break-timers`, `dam-break-audit-bundle`, or `dam-break-heap`
```

USAGE lists every flag family; each command still rejects flags it does not implement (`parse_counts` already errors on unknown arguments for bench/profile/timers):

- `dam-break-bench` / `dam-break-profile` / `dam-break-timers`: `--warmup` / `--steps` only (reject `--pair-stamp`, `--profile-stamp`, `--stamp`)
- `dam-break-audit-bundle`: `--pair-stamp` / `--profile-stamp` only (reject `--warmup`, `--steps`, `--stamp`)
- `dam-break-heap`: `--stamp` / `--warmup` / `--steps` only (reject `--pair-stamp`, `--profile-stamp`)

Keep `unsafe_code = "forbid"`. Do not edit `pair.rs`. Do not grow `playground_cli.rs`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD classify_profile_symbols sidecar-first heuristic</name>
  <files>tools/xtask/src/playground.rs, tools/xtask/src/playground/symbols.rs</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-12, D-13
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md` Pattern 3, Pitfall 4
    1. `standards/core/testing.md`
    1. `tools/xtask/Cargo.toml` flate2 dep
  </read_first>
  <behavior>
    1. Directory with `rust.json.syms.json` containing `alloc::vec::Vec` returns `HeapGate::Run` whose `matched_needles` includes `alloc::` or `alloc::vec::Vec`, even if `rust.json.gz` is raw non-JSON bytes.
    2. Directory with valid gzip JSON `{"threads":[{"stringArray":["liquidfun::particle::proxy::from_view"]}]}` and no sidecar returns `SkipNoAllocator`.
    3. Directory with gzip JSON whose stringArray includes `core::clone::Clone` returns `Run`.
    4. Directory with uncompressed fake bytes `fake-samply-json-gz` and no sidecar returns `SkipNoSymbols`.
    5. Directory with gzip of hex-only strings `["0x1000","0x2000"]` and no sidecar returns `SkipNoSymbols`.
    6. Bare substring `clone` in `MycloneHelper` without `core::clone::` does not produce `Run`.
  </behavior>
  <action>
    `mod symbols;` in `playground.rs`. Write failing tests in `symbols.rs` using temp dirs under `target/xtask-test-fixtures/`. Build gzip fixtures with `flate2::write::GzEncoder`. Confirm RED, then implement `classify_profile_symbols`.

    Do not spawn cargo or dhat in this task. No `unwrap()` in production paths. File ≤628 lines.
  </action>
  <verify>
    <automated>cargo test -p xtask --lib playground::symbols -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "mod symbols;" tools/xtask/src/playground.rs` matches.
    1. `rg -n "fn classify_profile_symbols" tools/xtask/src/playground/symbols.rs` matches.
    1. `rg -n "SkipNoSymbols|SkipNoAllocator" tools/xtask/src/playground/symbols.rs` matches both.
    1. `rg -n "core::clone::" tools/xtask/src/playground/symbols.rs` matches.
    1. `rg -n "GzDecoder" tools/xtask/src/playground/symbols.rs` matches.
    1. `cargo test -p xtask --lib playground::symbols -- --test-threads=1` exits 0.
    1. `tools/xtask/src/playground/symbols.rs` has ≤628 physical lines.
  </acceptance_criteria>
  <done>Heuristic distinguishes needle match, no-allocator, and no-symbols, scanning the sidecar first.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dam-break-heap command and one-line just alias</name>
  <files>tools/xtask/src/playground.rs, tools/xtask/src/playground/heap.rs, tools/xtask/src/main.rs, justfile</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-05, D-11, D-12, D-13
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md` Pattern 2, Pitfall 3, Pitfall 7
    1. `tools/xtask/src/playground/profile.rs` cargo lookup and stamp unix env
    1. `crates/liquidfun-wasm/src/bin/dam_break_bench.rs` from 23-04
    1. `justfile` after 23-02
  </read_first>
  <action>
    Per D-12, `heap::run` must call `classify_profile_symbols` **before** minting a stamp or spawning cargo. `SkipNoAllocator` and `SkipNoSymbols` return `PlaygroundError::new("heap", ...)` with the distinct messages above and must not call `mint_exclusive_stamp`.

    On `Run`, mint a **sibling** exclusive stamp (not the source bundle directory). Set `LIQUIDFUN_DHAT_HEAP_FILE`. Spawn cargo with argv lists only (`Command::new`, no shell). After the process, require nonempty `dhat-heap.json` in that stamp; if missing, return Err and do not write a success identity.

    Cargo argv **must** include `--profile` `profiling` and `--features` `dhat-heap` and **must not** include `--release`.

    Add the just alias with no extra arguments. `rg` justfile must still find no `samply`, `cmake`, `dhat`, or `--features`.

    Set `USAGE` in `playground.rs` to this exact one-line string (23-02 locked a 4-command USAGE; this plan must not drop `dam-break-audit-bundle` or skip the unit tests):

    `Usage: cargo xtask playground <dam-break-bench|dam-break-profile|dam-break-timers|dam-break-audit-bundle|dam-break-heap> [--warmup <n>] [--steps <n>] [--pair-stamp <utc>] [--profile-stamp <utc>] [--stamp <utc>]`

    Update `missing_subcommand_is_a_usage_error` so the expected message is exactly:

    expected `dam-break-bench`, `dam-break-profile`, `dam-break-timers`, `dam-break-audit-bundle`, or `dam-break-heap`

    Keep `unknown_subcommand_is_a_usage_error` as a usage error that still prints USAGE. Do not leave the 23-02 4-command USAGE in place.

    Reject unknown flags per command (USAGE is the union; parsers stay command-local): heap `run` accepts only `--stamp`, `--warmup`, and `--steps`; bundle continues to reject `--warmup`/`--steps`/`--stamp`; bench/profile/timers continue to reject `--pair-stamp`/`--profile-stamp`/`--stamp` via `parse_counts`.

    Update `main.rs` playground help line to mention audit-bundle and private heap. `mod heap;`. Dispatcher `"dam-break-heap" => heap::run(command_args)`. `cargo fmt --all`.

    Do not run a live 60+600 dhat in this plan (23-08). Unit-test skip paths by calling `heap::run` against temp evidence dirs if cheap; full fake-cargo spawn coverage is 23-06.
  </action>
  <verify>
    <automated>rg -n "playground-dam-break-heap:" justfile &amp;&amp; cargo test -p xtask --lib playground -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `justfile` recipe `playground-dam-break-heap` body is exactly `cargo xtask playground dam-break-heap`.
    1. `rg -n "samply|cmake|dhat|--features" justfile` is empty.
    1. `rg -n "heap::run" tools/xtask/src/playground.rs` matches.
    1. `rg -F "Usage: cargo xtask playground <dam-break-bench|dam-break-profile|dam-break-timers|dam-break-audit-bundle|dam-break-heap> [--warmup <n>] [--steps <n>] [--pair-stamp <utc>] [--profile-stamp <utc>] [--stamp <utc>]" tools/xtask/src/playground.rs` matches.
    1. `rg -n "dam-break-audit-bundle, or \`dam-break-heap\`" tools/xtask/src/playground.rs` matches both `fn run` missing-subcommand text and `missing_subcommand_is_a_usage_error`.
    1. `rg -n "--release" tools/xtask/src/playground/heap.rs` is empty.
    1. `rg -n "dhat-heap" tools/xtask/src/playground/heap.rs` matches.
    1. `rg -n "LIQUIDFUN_DHAT_HEAP_FILE" tools/xtask/src/playground/heap.rs` matches.
    1. `rg -n "not_timing_authority" tools/xtask/src/playground/heap.rs` matches.
    1. `rg -n "dhat-heap" tools/xtask/src/playground/pair.rs` is empty.
    1. `cargo test -p xtask --lib playground -- --test-threads=1` exits 0.
  </acceptance_criteria>
  <done>Heap command exists, is gated on the heuristic, and cannot rebuild the --release gate binary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile JSON / sidecar bytes → heap spawn decision | Attacker-controlled gzip must not inject commands; scan is string contains only. |
| `--stamp` CLI → filesystem | Same allowlist as bundle stamps. |
| `LIQUIDFUN_DHAT_HEAP_FILE` → dhat | xtask must set an evidence-stamp path, never a repo-root relative default. |
| cargo feature fingerprint | `--features dhat-heap` on `--release` would replace the gate binary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05-01 | T | gate binary | mitigate | Heap cargo uses `--profile profiling --features dhat-heap` and never `--release` (D-11, Pitfall 3). |
| T-23-05-02 | T | dhat just-in-case | mitigate | SkipNoAllocator / SkipNoSymbols return Err before mint/spawn (D-13). |
| T-23-05-03 | I | cwd `dhat-heap.json` | mitigate | Always set `LIQUIDFUN_DHAT_HEAP_FILE` to `{minted-stamp}/dhat-heap.json` (Pitfall 7). |
| T-23-05-04 | T | stamp flag traversal | mitigate | Reuse `parse_stamp_name`; join only under `target/dam-break-perf/`. |
| T-23-05-05 | S | mixing dhat wall into pair.json | mitigate | Sibling stamp; identity `not_timing_authority`; do not write `pair.json` (D-03). |
| T-23-05-06 | I | dump commit | mitigate | Dump stays under gitignored `/target/`. |
</threat_model>

<verification>
`cargo test -p xtask --lib playground::symbols` and `playground` lib tests; justfile greps. Fake cargo dump CLI is 23-06. Do not run live dhat.
</verification>

<success_criteria>
Developer-facing `just playground-dam-break-heap` exists; the heuristic fail-closes without a dump when needles or symbols are absent; matching runs would use the profiling profile plus dhat-heap.
</success_criteria>

<output>
After completion, create `.planning/phases/23-baseline-pair-and-named-audit/23-05-SUMMARY.md`
</output>
