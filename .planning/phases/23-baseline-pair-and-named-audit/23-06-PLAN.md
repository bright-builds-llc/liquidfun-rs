---
phase: 23-baseline-pair-and-named-audit
plan: "06"
type: tdd
wave: 4
depends_on: [23-05]
files_modified:
  - tools/xtask/tests/playground_cli/heap.rs
  - tools/xtask/tests/fixtures/fake_upstream_tool.rs
autonomous: true
requirements: [PERF-HEAP]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-09-21T02-44-37
generated_at: 2026-09-21T03:02:06Z
must_haves:
  truths:
    - "Fake cargo that sees run + dam-break-bench + --features dhat-heap writes nonempty dhat-heap.json to LIQUIDFUN_DHAT_HEAP_FILE and records argv containing --profile profiling and dhat-heap and not --release."
    - "A profile stamp whose sidecar contains alloc:: causes dam-break-heap to succeed, mint a new stamp with dhat-heap.json and heap-identity.json not_timing_authority true, and leave the source stamp without dhat-heap.json."
    - "A readable sidecar with no needles exits nonzero, writes no dhat-heap.json, and stderr contains no-allocator wording without skip-as-success."
    - "A fake-samply non-JSON gzip without sidecar exits nonzero with could not read symbols, not no allocator time, and writes no dump."
  artifacts:
    - path: tools/xtask/tests/playground_cli/heap.rs
      provides: "Heap CLI success and skip coverage"
      contains: "dam-break-heap"
    - path: tools/xtask/tests/fixtures/fake_upstream_tool.rs
      provides: "Fake cargo dhat-heap dump writer"
      contains: "LIQUIDFUN_DHAT_HEAP_FILE"
  key_links:
    - from: tools/xtask/tests/playground_cli/heap.rs
      to: tools/xtask/src/playground/heap.rs
      via: "cargo xtask playground dam-break-heap"
      pattern: "dam-break-heap"
    - from: fake cargo
      to: LIQUIDFUN_DHAT_HEAP_FILE
      via: "write dump when features include dhat-heap"
      pattern: "dhat-heap"
---

<objective>
Cover `dam-break-heap` with fake-cargo CLI tests for needle match, no-allocator skip, and inconclusive-symbols skip.

Purpose: D-12 / D-13 fail-closed plumbing. Fake dumps cannot rank physics functions (D-01).
Output: `playground_cli/heap.rs` tests plus fake cargo behavior when `--features dhat-heap` is present. Do not edit `playground_cli.rs`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md
@.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md
@tools/xtask/tests/fixtures/fake_upstream_tool.rs
@tools/xtask/tests/playground_cli/support.rs
@tools/xtask/tests/playground_cli/heap.rs
@tools/xtask/src/playground/heap.rs

<interfaces>
`run_cargo` currently treats `--profile profiling` first (`install_profiling_bench`) and `run` + `dam-break-bench` as the unprofiled JSON printer. Heap uses `cargo run --profile profiling --features dhat-heap --bin dam-break-bench`. Reorder fake cargo:

1. If args contain `run` AND `dam-break-bench` AND `dhat-heap`: write at least 16 nonempty bytes to `LIQUIDFUN_DHAT_HEAP_FILE` (fail if unset), write argv to `LIQUIDFUN_TEST_CARGO_MARKER` if set, print a native_rust bench JSON line so the process looks successful, exit 0. Do **not** require real gzip.
2. Keep existing `--profile profiling` **build** path (`install_profiling_bench`) for `dam-break-profile`.
3. Keep existing `run` + `dam-break-bench` without `dhat-heap` as the 22-02 pair fake.

If `run` + `profiling` + `dhat-heap` currently hits `install_profiling_bench` and never writes the dump, heap CLI tests will fail — that reorder is mandatory.

Do not run host dhat. Do not `git add` `dhat-heap.json`. Keep `heap.rs` test file ≤628 lines.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fake cargo writes LIQUIDFUN_DHAT_HEAP_FILE on dhat-heap run</name>
  <files>tools/xtask/tests/fixtures/fake_upstream_tool.rs, tools/xtask/tests/playground_cli/heap.rs</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-11, D-12
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md` Pitfall 3, Pitfall 7
    1. `tools/xtask/tests/fixtures/fake_upstream_tool.rs` `run_cargo`
    1. `tools/xtask/src/playground/heap.rs`
  </read_first>
  <behavior>
    1. CLI: fixture with pair+profile plus `rust.json.syms.json` containing `alloc::vec::Vec`; `playground dam-break-heap --warmup 0 --steps 1` exits 0; a stamp other than the profile stamp contains nonempty `dhat-heap.json` and `heap-identity.json` with `kind=dhat_heap`, `not_timing_authority=true`, `cargo_profile=profiling`, `features` including `dhat-heap`.
    2. Cargo marker/args contain `--profile` `profiling`, `--features` `dhat-heap`, `--bin` `dam-break-bench`, and do not contain `--release`.
    3. Source profile stamp has no `dhat-heap.json`.
    4. `justfile` `playground-dam-break-heap` recipe is exactly `cargo xtask playground dam-break-heap`.
  </behavior>
  <action>
    Implement the fake-cargo dhat-heap branch first so the CLI test can go RED on missing heap::run wiring then GREEN. One concern per test. Use the 23-01 `RepositoryFixture` and a local THIRD/FOURTH stamp unix if needed.

    To seed symbols, write `rust.json.syms.json` into the profile stamp after `run_profile`. Do not parse fake gzip as names.
  </action>
  <verify>
    <automated>cargo test -p xtask --test playground_cli -- --test-threads=1 heap</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "LIQUIDFUN_DHAT_HEAP_FILE" tools/xtask/tests/fixtures/fake_upstream_tool.rs` matches.
    1. `rg -n "dhat-heap" tools/xtask/tests/fixtures/fake_upstream_tool.rs` matches.
    1. `rg -n "not_timing_authority" tools/xtask/tests/playground_cli/heap.rs` matches.
    1. `rg -n "--release" tools/xtask/tests/playground_cli/heap.rs` matches an assertion that cargo args must **not** contain `--release`.
    1. `cargo test -p xtask --test playground_cli -- --test-threads=1 heap` exits 0.
  </acceptance_criteria>
  <done>Needle-match CLI path persists a sibling dhat dump marked not timing authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Skip paths write no dump and distinguish no-allocator vs no-symbols</name>
  <files>tools/xtask/tests/playground_cli/heap.rs</files>
  <read_first>
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-CONTEXT.md` D-13
    1. `.planning/phases/23-baseline-pair-and-named-audit/23-RESEARCH.md` Pattern 3 fail-closed table
    1. Task 1 tests
  </read_first>
  <behavior>
    1. Profile stamp with sidecar JSON `{"string_table":["liquidfun::particle::proxy"]}` (no allocator needles): `dam-break-heap` exits nonzero; stderr contains `no allocator` (or `no allocator/Vec`); does not contain `could not read symbols` as the sole reason; no `dhat-heap.json` under the fixture evidence root.
    2. Profile stamp with only fake-samply non-JSON `rust.json.gz` and no sidecar: exits nonzero; stderr contains `could not read symbols`; does not claim `no allocator` as the skip reason; no dump.
    3. Neither skip test's stderr treats `skip` as a successful Ok path (nonzero exit required).
  </behavior>
  <action>
    Arrange/Act/Assert, one concept per test. Do not spawn real dhat. After these pass, run the full `playground_cli` crate to ensure pair/profile/timers/bundle still pass.
  </action>
  <verify>
    <automated>cargo test -p xtask --test playground_cli -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "no allocator" tools/xtask/tests/playground_cli/heap.rs` matches.
    1. `rg -n "could not read symbols" tools/xtask/tests/playground_cli/heap.rs` matches.
    1. `cargo test -p xtask --test playground_cli -- --test-threads=1` exits 0.
    1. `tools/xtask/tests/playground_cli/heap.rs` has ≤628 physical lines.
    1. `git ls-files 'dhat-heap.json' '*.json.gz'` is empty.
  </acceptance_criteria>
  <done>Heap CLI fail-closes without dumps for no-allocator and no-symbols, with distinct stderr.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fake cargo vs production cargo | Success tests inject fake cargo; must still assert `--release` is absent. |
| Skip stderr wording | Callers of the audit doc must be able to tell "no Vec time" from "could not parse symbols". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06-01 | S | fake cargo hiding `--release` | mitigate | Assert cargo marker contains `profiling` + `dhat-heap` and not `--release`. |
| T-23-06-02 | T | skip writing a dump anyway | mitigate | Tests assert zero `dhat-heap.json` files on both skip paths (D-13). |
| T-23-06-03 | S | conflating no-symbols with no-allocator | mitigate | Distinct stderr assertions (RESEARCH Pitfall 4). |
| T-23-06-04 | I | fixture dumps | mitigate | Remain under `target/xtask-test-fixtures/`; do not git add. |
</threat_model>

<verification>
`cargo test -p xtask --test playground_cli -- --test-threads=1`. No live dhat. No host samply required.
</verification>

<success_criteria>
Fake-tool tests prove heap runs only on needle match, uses the profiling profile, and fail-closes with distinct messages otherwise.
</success_criteria>

<output>
After completion, create `.planning/phases/23-baseline-pair-and-named-audit/23-06-SUMMARY.md`
</output>
