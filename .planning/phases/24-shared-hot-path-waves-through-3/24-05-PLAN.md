---
phase: 24-shared-hot-path-waves-through-3
plan: "05"
type: execute
wave: 5
depends_on: [24-04]
files_modified:
  - crates/liquidfun-wasm/src/lib.rs
  - crates/liquidfun-wasm/src/scene_spot.rs
  - crates/liquidfun-wasm/src/bin/playground_scene_spot.rs
  - crates/liquidfun-wasm/src/session.rs
  - crates/liquidfun-wasm/Cargo.toml
  - tools/xtask/src/playground.rs
  - tools/xtask/src/playground/spot.rs
  - tools/xtask/src/main.rs
  - tools/xtask/tests/playground_cli.rs
  - tools/xtask/tests/playground_cli/spot.rs
  - justfile
  - docs/playground-scene-spot-check.md
autonomous: true
requirements: [PERF-SPOT, PERF-CANARY2]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 24-2026-09-21T15-01-47
generated_at: 2026-09-21T15:19:51Z
must_haves:
  truths:
    - "Developer can run just playground-scene-spot and get a new exclusive target/dam-break-perf stamp with native --release headless wall ms for Fountain, Float or Sink, Color Mixer, Jelly Drop, and Water Wheel."
    - "Each scene uses SessionCore::create(SceneId::…) and loop advance(1); Fountain emission hooks run; dam-break-bench is not reused as the spot timer."
    - "Spot JSON is kind native_scene_spot with not_timing_authority true and no rust_over_cpp_ratio / C++ pair."
    - "Committed unreviewed notes table records start/end particle counts and wall ms or ms/step; hangs, timeouts, or construction errors fail the plan."
    - "Notes default to the same dominant particle-contact cluster as Dam Break (PERF-CANARY2) unless a scene's time or failure mode clearly differs — then a second native profile of that scene is recorded instead."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene_spot.rs
      provides: "Native-only five-scene SessionCore stepper"
      contains: "SessionCore::create"
      min_lines: 30
    - path: tools/xtask/src/playground/spot.rs
      provides: "Exclusive stamp persist for scene-spot.json"
      contains: "native_scene_spot"
      min_lines: 30
    - path: justfile
      provides: "One-line playground-scene-spot alias"
      contains: "cargo xtask playground scene-spot"
    - path: docs/playground-scene-spot-check.md
      provides: "Unreviewed five-scene table plus same-cluster or second-canary note"
      contains: "Unreviewed local sample"
  key_links:
    - from: crates/liquidfun-wasm/src/scene_spot.rs
      to: crates/liquidfun-wasm/src/session.rs
      via: "SessionCore::create + advance(1)"
      pattern: "advance\\(1\\)"
    - from: tools/xtask/src/playground/spot.rs
      to: tools/xtask/src/playground/stamp.rs
      via: "mint_exclusive_stamp"
      pattern: "mint_exclusive_stamp"
    - from: docs/playground-scene-spot-check.md
      to: crates/liquidfun-wasm/src/scene.rs
      via: "five SceneId builders, not Dam Break C++ pair"
      pattern: "Fountain"
---

<objective>
Spot-check the five non-Dam-Break playground scenes with native `--release` headless `SessionCore` stepping after the shared-path gate, and record the default same-cluster canary note.

Purpose: D-11/D-12/PERF-SPOT/PERF-CANARY2. Shared kernel fixes must not hang Fountain emission or the other particle/rigid demos. This is not a C++ pair and not Phase 12.
Output: `just playground-scene-spot`, exclusive `scene-spot.json`, committed unreviewed table, same-cluster (or second-canary) note.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@crates/liquidfun-wasm/src/session.rs
@crates/liquidfun-wasm/src/scene.rs
@crates/liquidfun-wasm/src/dam_break_bench.rs
@crates/liquidfun-wasm/src/lib.rs
@crates/liquidfun-wasm/Cargo.toml
@tools/xtask/src/playground.rs
@tools/xtask/src/playground/stamp.rs
@tools/xtask/src/playground/timers.rs
@justfile
@docs/playground-dam-break-timing.md

<interfaces>
`SceneId` (already): `Fountain`, `FloatOrSink`, `ColorMixer`, `JellyDrop`, `WaterWheel`. Do **not** include Dam Break in the spot table (gate already covers it).

`SessionCore::create(id)` then loop `advance(1)` (MAX_ADVANCE_STEPS is 4, so never `advance(120)`). Fountain `on_advance` emits; skipping hooks is a PERF-SHARED cheat.

Add `pub(crate) fn live_particle_count(&amp;self) -&gt; Result&lt;usize, SessionError&gt;` on `SessionCore` that snapshots the live system (construction `particle_count` field is stale after Fountain emit). Map snapshot failure to `SessionError::SceneConstruction`. Do not call `capture_frame` (WASM copy path).

Native module `crates/liquidfun-wasm/src/scene_spot.rs` gated `#[cfg(not(target_arch = "wasm32"))]` like `dam_break_bench.rs`. Constants:
- `DEFAULT_WARMUP_STEPS: u32 = 60`
- `DEFAULT_MEASURED_STEPS: u32 = 120`
- `SCENE_WALL_TIMEOUT: Duration = Duration::from_secs(180)` per scene including warmup+measured

Bin `playground-scene-spot` (`src/bin/playground_scene_spot.rs`, `test = false`, `bench = false`) prints one JSON object per scene, hand-built like `DamBreakBenchReport::to_json` (no serde on this crate):
`scene`, `warmup_steps`, `measured_steps`, `start_particles`, `end_particles`, `wall_ms`, `ms_per_step`, `timed_out` (false on success). Fail the process (exit 1) on construction error, step error, or timeout. Do not enable `dhat-heap`. Do not call `advance_profiled`.

xtask `playground/spot.rs` (new file; do not grow `pair.rs`):
- `cargo run --release -p liquidfun-wasm --bin playground-scene-spot`
- `mint_exclusive_stamp`
- write `scene-spot.json` with `kind: "native_scene_spot"`, `not_timing_authority: true`, `disclaimer` unreviewed, `git_head`, host identity, `scenes: [...]`
- no `pair.json`, no `rust_over_cpp_ratio`

`just playground-scene-spot:` exactly `cargo xtask playground scene-spot`. Update `playground.rs` USAGE + match arm + unit tests. Add `#[path = "playground_cli/spot.rs"] mod spot;` in `tests/playground_cli.rs`. Fake-cargo CLI test: success writes `scene-spot.json` without `pair.json`; justfile alias is one line.

PERF-CANARY2 default (D-12): if all five finish without timeout/error, committed notes state that Dam Break and these spot-checks share the same dominant particle-contact cluster. Run `just playground-dam-break-profile` is **not** required. Only if a scene's wall time or failure mode indicates a different cluster (for example rigid-solve dominated hang vs particle-contact), capture a native `--release` profile of **that** scene into a new exclusive stamp and say so. Do not build a 32-case matrix.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scene-spot binary, xtask command, just alias, CLI tests</name>
  <files>crates/liquidfun-wasm/src/lib.rs, crates/liquidfun-wasm/src/scene_spot.rs, crates/liquidfun-wasm/src/bin/playground_scene_spot.rs, crates/liquidfun-wasm/src/session.rs, crates/liquidfun-wasm/Cargo.toml, tools/xtask/src/playground.rs, tools/xtask/src/playground/spot.rs, tools/xtask/src/main.rs, tools/xtask/tests/playground_cli.rs, tools/xtask/tests/playground_cli/spot.rs, justfile</files>
  <read_first>
    1. `.planning/phases/24-shared-hot-path-waves-through-3/24-CONTEXT.md` D-11 D-12 D-15
    1. `crates/liquidfun-wasm/src/dam_break_bench.rs` Instant + `advance(1)` pattern
    1. `crates/liquidfun-wasm/src/session.rs` `create` / `advance` / `MAX_ADVANCE_STEPS`
    1. `crates/liquidfun-wasm/src/scene.rs` `SceneId`
    1. `tools/xtask/src/playground.rs` USAGE match
    1. `tools/xtask/src/playground/timers.rs` persist + `not_timing_authority`
    1. `tools/xtask/tests/playground_cli.rs` `#[path]` modules
    1. `standards/languages/rust.md` foo.rs plus foo/
  </read_first>
  <behavior>
    - Test 1: `justfile` contains `playground-scene-spot:` followed by only `cargo xtask playground scene-spot`.
    - Test 2: fake cargo+stamp CLI writes `scene-spot.json` with `kind native_scene_spot`, `not_timing_authority true`, and no `pair.json` in that stamp.
    - Test 3: playground unit test unknown subcommand still fail-closed; `scene-spot` is in USAGE.
  </behavior>
  <action>
    Implement the interfaces. Keep every new/touched file ≤628 physical lines. No `scene_spot/mod.rs`, no `playground/mod.rs`. `just` stays a one-line printer (no cmake/samply flags). Honor `LIQUIDFUN_XTASK_CARGO` / `LIQUIDFUN_XTASK_STAMP_UNIX` like other playground commands.

    `live_particle_count` is required so Fountain end counts can grow. Do not put spot timing into `dam_break_bench.rs`. Do not compare scenes to C++.

    After code: `cargo fmt --all`. Fake-tool tests must not require a live 180s run.
  </action>
  <verify>
    <automated>cargo test -p xtask playground -- --test-threads=1 &amp;&amp; cargo test -p xtask --test playground_cli -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "playground-scene-spot:\n    cargo xtask playground scene-spot" -U justfile` matches.
    1. `rg -n "samply|cmake|dhat" justfile` is empty.
    1. `rg -n "kind.: .native_scene_spot|native_scene_spot" tools/xtask/src/playground/spot.rs` matches.
    1. `rg -n "SessionCore::create" crates/liquidfun-wasm/src/scene_spot.rs` matches.
    1. `rg -n "advance\\(1\\)" crates/liquidfun-wasm/src/scene_spot.rs` matches.
    1. `rg -n "advance_profiled|step_profiled" crates/liquidfun-wasm/src/scene_spot.rs crates/liquidfun-wasm/src/bin/playground_scene_spot.rs` is empty.
    1. `rg -n "mod.rs" crates/liquidfun-wasm/src/scene_spot.rs` does not apply — `test ! -e crates/liquidfun-wasm/src/scene_spot/mod.rs` succeeds.
    1. `test ! -e tools/xtask/src/playground/mod.rs` succeeds.
    1. `wc -l tools/xtask/src/playground/pair.rs` prints ≤616 (file not grown as the owner of this feature).
    1. `cargo test -p xtask --test playground_cli -- --test-threads=1` exits 0.
  </acceptance_criteria>
  <done>Headless five-scene recipe exists as a one-line just/xtask path with fake-tool coverage and not_timing_authority persist.</done>
</task>

<task type="auto">
  <name>Task 2: Live five-scene stamp, notes table, same-cluster canary</name>
  <files>docs/playground-scene-spot-check.md</files>
  <read_first>
    1. Task 1 recipe
    1. `.planning/phases/24-shared-hot-path-waves-through-3/24-04-SUMMARY.md` gate pair
    1. `docs/playground-dam-break-timing.md` banner style
  </read_first>
  <action>
    Run `just playground-scene-spot` on this host (real `--release` scenes, not fake cargo). Confirm a new exclusive stamp containing `scene-spot.json` with five scenes, finite `wall_ms`, `timed_out` false, and start/end particle counts. Hangs or nonzero exit fail this task.

    Create `docs/playground-scene-spot-check.md`:
    1. Banner: **Unreviewed local sample.** Not a C++ pair, not Phase 12, not the Dam Break 3× number.
    1. Reproduce: `just playground-scene-spot`.
    1. Stamp path, `git_head`, host, warmup 60, measured 120, `advance(1)`.
    1. Table: scene, start_particles, end_particles, wall_ms, ms/step.
    1. Improvement vs pre-Phase-24 is optional; **non-catastrophic completion** is required (D-11).
    1. PERF-CANARY2 paragraph: default sentence that Dam Break and these five scenes share the same dominant particle-contact cluster. Only if a scene’s time or failure mode disagrees, attach a second native profile stamp path instead of that sentence.

    Do not copy spot ms into `manifest.toml` or README. `just markdown-check`.
  </action>
  <verify>
    <automated>python3 -c 'import json,pathlib; spots=sorted(pathlib.Path("target/dam-break-perf").glob("*/scene-spot.json")); assert spots, "missing scene-spot.json"; r=json.loads(spots[-1].read_text()); assert r["kind"]=="native_scene_spot" and r.get("not_timing_authority") is True; assert "rust_over_cpp_ratio" not in r; names={s["scene"] for s in r["scenes"]}; assert names=={"fountain","float-or-sink","color-mixer","jelly-drop","water-wheel"}, names; print(spots[-1].parent.name, names)'</automated>
  </verify>
  <acceptance_criteria>
    1. Newest `scene-spot.json` is `native_scene_spot` with `not_timing_authority` true and five named scenes.
    1. That stamp has no `pair.json`.
    1. `docs/playground-scene-spot-check.md` contains `Unreviewed local sample` and all five scene names.
    1. The doc contains either `same dominant` cluster wording or a second-canary profile stamp path.
    1. `git diff -- reference/performance/manifest.toml` is empty.
    1. `just markdown-check` exits 0.
  </acceptance_criteria>
  <done>Five playground scenes stepped headless after the shared-path gate; same-cluster (or explicit second canary) is recorded as unreviewed notes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spot JSON vs Dam Break pair.json | Spot walls must never become the 3× number. |
| Session hooks | Skipping `on_advance` would fake Fountain cheapness. |
| WASM frame copy | `capture_frame` is not a physics spot-check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-01 | S | `scene-spot.json` | mitigate | `kind native_scene_spot`, `not_timing_authority true`, no `rust_over_cpp_ratio`. |
| T-24-05-02 | D | Fountain emit unbounded time | mitigate | 180s per-scene wall timeout; fail closed on timeout rather than hang forever. |
| T-24-05-03 | T | scene construction | mitigate | Typed `SessionError`; nonzero exit; do not `unwrap`. |
| T-24-05-04 | S | cheat via World::step without hooks | mitigate | Require `SessionCore::create` + `advance(1)` in `scene_spot.rs`. |
| T-24-05-05 | T | `liquidfun` deps | mitigate | Spot tooling stays in `liquidfun-wasm` + xtask; no serde on `liquidfun`. |
</threat_model>

<verification>
Fake CLI tests plus one live `just playground-scene-spot` stamp. Notes unreviewed. Manifest empty.
</verification>

<success_criteria>
Five native `--release` scene spot-checks complete without hang/timeout, documented as unreviewed, with default same-cluster PERF-CANARY2 wording unless a differing cluster was actually observed.
</success_criteria>

<output>
After completion, create `.planning/phases/24-shared-hot-path-waves-through-3/24-05-SUMMARY.md`
</output>
