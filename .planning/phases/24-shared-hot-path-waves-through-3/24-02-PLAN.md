---
phase: 24-shared-hot-path-waves-through-3
plan: "02"
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - docs/playground-dam-break-timing.md
  - docs/native-performance-audit.md
autonomous: true
requirements: [PERF-ADMIT]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 24-2026-09-21T15-01-47
generated_at: 2026-09-21T15:19:51Z
must_haves:
  truths:
    - "A new exclusive unprofiled pair stamp exists with kind unprofiled_pair, timing_authority unprofiled_wall_clock, 1920 particles, 60 warmup, 600 measured steps, and rust_over_cpp_ratio strictly below the Phase 23 sample 327.53395024734476."
    - "The new stamp directory name differs from 2026-09-21T04-32-19Z and 2026-09-21T04-34-32Z; those stamps still exist."
    - "A sibling samply profile stamp exists with profile-identity.json not_timing_authority true and no pair.json in that stamp."
    - "Committed timing and audit notes remain Unreviewed local sample, cite the new pair.json ratio, keep a historical pointer to MEASURED_HEAD 6d98531, and do not claim PERF-GATE unless that new rust_over_cpp_ratio is ≤ 3."
    - "reference/performance/manifest.toml still has reviewed_reports = []."
  artifacts:
    - path: docs/playground-dam-break-timing.md
      provides: "Unreviewed current pair sample after wave 1"
      contains: "Unreviewed local sample"
    - path: docs/native-performance-audit.md
      provides: "Wave-1 admission note without treating samply ms as the 3× number"
      contains: "not_timing_authority"
    - path: reference/performance/manifest.toml
      provides: "Empty reviewed_reports"
      contains: "reviewed_reports = []"
  key_links:
    - from: docs/playground-dam-break-timing.md
      to: target/dam-break-perf
      via: "new exclusive pair.json stamp path"
      pattern: "target/dam-break-perf/"
    - from: docs/native-performance-audit.md
      to: docs/playground-dam-break-timing.md
      via: "unprofiled pair.json is the only 3× number"
      pattern: "unprofiled"
---

<objective>
Admit the `particle_rows` wave only if a new exclusive unprofiled Dam Break Medium pair improves on this host, then capture a sibling samply profile to retarget leftovers.

Purpose: D-04/D-05/D-14/PERF-ADMIT. Wave 1 is necessary and may still be far above 3× (research A1). Do not claim PERF-GATE here unless `pair.json` `rust_over_cpp_ratio` is ≤ 3.
Output: New gitignored pair + profile stamps, refreshed unreviewed notes. Failed physics or a non-improving ratio is a failed candidate (keep the stamp; do not call it a win).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-shared-hot-path-waves-through-3/24-01-SUMMARY.md
@docs/playground-dam-break-timing.md
@docs/native-performance-audit.md
@docs/native-performance-audit.md
@BENCHMARKING.md
@justfile
@reference/performance/manifest.toml
@tools/xtask/src/playground/pair.rs
@tools/xtask/src/playground/stamp.rs

<interfaces>
Gate command (do not replace):
```console
just playground-dam-break-bench
```
Writes `target/dam-break-perf/&lt;YYYY-MM-DDTHH-MM-SSZ&gt;/pair.json` with:
```json
{
  "kind": "unprofiled_pair",
  "timing_authority": "unprofiled_wall_clock",
  "particles": 1920,
  "warmup_steps": 60,
  "measured_steps": 600,
  "rust_over_cpp_ratio": "<f64>"
}
```
Profile command (never the 3× number):
```console
just playground-dam-break-profile
```
Writes a **new** exclusive stamp with `rust.json.gz`, `profile-identity.json` (`kind: samply_cpu`, `not_timing_authority: true`) and **no** `pair.json`.

Phase 23 baseline to beat (not to overwrite):
- MEASURED_HEAD `6d98531ac799987c209d3fd1e572e482fcab5da6`
- pair stamp `2026-09-21T04-32-19Z`
- `rust_over_cpp_ratio` `327.53395024734476`

`stamp::mint_exclusive_stamp` already fails closed on `AlreadyExists`. Never `rm -rf` a stamp. Never merge into an existing directory. Never `git add` `*.json.gz`, `*.syms.json`, `dhat-heap.json`, or `target/`.

Do not grow `tools/xtask/src/playground/pair.rs` (616 / 628). Do not add `--features dhat-heap` to the pair. Do not put `step_profiled` in `dam_break_bench.rs`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unprofiled re-pair into a new exclusive stamp</name>
  <files>docs/playground-dam-break-timing.md</files>
  <read_first>
    1. `.planning/phases/24-shared-hot-path-waves-through-3/24-CONTEXT.md` D-04 D-05 D-14
    1. `.planning/phases/24-shared-hot-path-waves-through-3/24-01-SUMMARY.md`
    1. `docs/playground-dam-break-timing.md`
    1. `justfile` `playground-dam-break-bench` recipe
    1. `reference/performance/manifest.toml`
  </read_first>
  <action>
    Confirm 24-01 landed (`rg -n "fn particle_rows" crates/liquidfun/src/particle/contact.rs` empty). Re-run native tests if the worktree moved:
    `cargo test -p liquidfun --test particle_contacts`.

    List existing stamps: `ls target/dam-break-perf | sort`. Keep every existing directory.

    Run `just playground-dam-break-bench` with default counts (no `--warmup 0`, no `--steps` other than the recipe default 60+600). Do not pass `--release` flags through `just`; xtask already uses `--release` vs `oracle-release`.

    Locate the newest `target/dam-break-perf/*/pair.json`. Assert with Python (do not trust stdout-only):
    ```python
    import json, pathlib
    stamps = sorted(pathlib.Path("target/dam-break-perf").glob("*/pair.json"))
    rep = json.loads(stamps[-1].read_text())
    assert rep["kind"] == "unprofiled_pair"
    assert rep["timing_authority"] == "unprofiled_wall_clock"
    assert rep["particles"] == 1920
    assert rep["warmup_steps"] == 60
    assert rep["measured_steps"] == 600
    assert rep["rust"]["wall_ms"] &gt; 0
    assert rep["cpp"]["wall_ms"] &gt; 0
    assert stamps[-1].parent.name != "2026-09-21T04-32-19Z"
    assert pathlib.Path("target/dam-break-perf/2026-09-21T04-32-19Z/pair.json").is_file()
    ```
    Admission (D-04): `rust_over_cpp_ratio` must be **strictly less** than `327.53395024734476`. If it is not, keep the new stamp, record failure in SUMMARY, and stop this plan as a failed candidate — do not refresh notes as a win and do not proceed to samply retarget as if wave 1 succeeded.

    If improved: refresh `docs/playground-dam-break-timing.md` **Current recorded sample** from this new `pair.json` only (walls, ms/step, compilers, host, git HEAD, stamp path, ratio). Keep the Unreviewed local sample banner. Add one historical bullet pointing at MEASURED_HEAD `6d98531ac799987c209d3fd1e572e482fcab5da6` / `327.53395024734476` so the Phase 23 sample remains visible. Do not paste samply duration. Do not write README or crates.io “Rust is N×”. Do not edit `reference/performance/manifest.toml`. Run `just markdown-check`.
  </action>
  <verify>
    <automated>python3 -c 'import json,pathlib; p=sorted(pathlib.Path("target/dam-break-perf").glob("*/pair.json"))[-1]; r=json.loads(p.read_text()); assert r["kind"]=="unprofiled_pair" and r["particles"]==1920 and r["warmup_steps"]==60 and r["measured_steps"]==600 and r["rust_over_cpp_ratio"]&lt;327.53395024734476; print(p.parent.name, r["git_head"], r["rust_over_cpp_ratio"])'</automated>
  </verify>
  <acceptance_criteria>
    1. Newest `pair.json` `kind` is `unprofiled_pair` and `timing_authority` is `unprofiled_wall_clock`.
    1. Newest `pair.json` has `particles == 1920`, `warmup_steps == 60`, `measured_steps == 600`.
    1. Newest stamp directory is not `2026-09-21T04-32-19Z` and that old stamp still has `pair.json`.
    1. Newest `rust_over_cpp_ratio` is finite and strictly less than `327.53395024734476`.
    1. `docs/playground-dam-break-timing.md` contains `Unreviewed local sample` and the new stamp path.
    1. `docs/playground-dam-break-timing.md` still mentions `6d98531ac799987c209d3fd1e572e482fcab5da6`.
    1. `rg -n "reviewed_reports = \\[\\]" reference/performance/manifest.toml` matches.
    1. `git ls-files 'target/dam-break-perf' '*.json.gz'` is empty.
  </acceptance_criteria>
  <done>Wave 1 is admitted by an improving exclusive unprofiled pair.json on the locked 1920/60+600 recipe.</done>
</task>

<task type="auto">
  <name>Task 2: Sibling samply retarget and unreviewed audit note</name>
  <files>docs/native-performance-audit.md</files>
  <read_first>
    1. `.planning/phases/24-shared-hot-path-waves-through-3/24-CONTEXT.md` D-05 D-06 D-14
    1. Task 1 newest `pair.json` path and ratio
    1. `docs/native-performance-audit.md`
    1. `justfile` `playground-dam-break-profile`
  </read_first>
  <action>
    Only run this task if Task 1 admitted the wave (ratio improved). Run `just playground-dam-break-profile`. Confirm the newest `profile-identity.json` has `kind == "samply_cpu"` and `not_timing_authority == true`, lives in a **different** directory from the Task 1 pair stamp, and that stamp has no `pair.json`.

    Update `docs/native-performance-audit.md`:
    1. Keep the Unreviewed local sample banner and **Not found** SIMD/Rayon/PGO/`unsafe` section.
    1. Add a short “Wave 1 admission” subsection that names `particle_rows` as the landed concern, cites the **new** pair stamp path, copies `rust_over_cpp_ratio` from that `pair.json` only, and cites the sibling profile stamp as retarget input (`not_timing_authority`).
    1. State explicitly: do **not** claim PERF-GATE unless that pair `rust_over_cpp_ratio` is ≤ 3. If the ratio is still &gt; 3, write that leftover waves (D-07/D-09) remain required and that `check_invariants` was not gated in wave 1 (D-08).
    1. Do not quote samply wall-clock / `[profile.profiling]` duration as the Dam Break number.
    1. Do not fill `manifest.toml`. Do not add README speed claims.

    Run `just markdown-check`. Record pair stamp, profile stamp, `git rev-parse HEAD`, and ratio in `24-02-SUMMARY.md`.
  </action>
  <verify>
    <automated>python3 -c 'import json,pathlib; profiles=sorted(pathlib.Path("target/dam-break-perf").glob("*/profile-identity.json")); idn=json.loads(profiles[-1].read_text()); assert idn.get("kind")=="samply_cpu" and idn.get("not_timing_authority") is True; assert not (profiles[-1].parent/"pair.json").exists(); print(profiles[-1].parent.name)'</automated>
  </verify>
  <acceptance_criteria>
    1. Newest `profile-identity.json` is `kind samply_cpu` with `not_timing_authority` true.
    1. That profile stamp directory contains `rust.json.gz` and does not contain `pair.json`.
    1. Profile stamp name differs from the Task 1 pair stamp.
    1. `docs/native-performance-audit.md` still contains `Unreviewed local sample` and `Not found`.
    1. `docs/native-performance-audit.md` names the new pair stamp and does not call samply duration the 3× number.
    1. If newest pair `rust_over_cpp_ratio` &gt; 3, the audit notes contain `PERF-GATE` only as **not claimed** / still required — not as achieved.
    1. `git diff -- reference/performance/manifest.toml` is empty.
  </acceptance_criteria>
  <done>Sibling samply stamp exists for leftover ranking; notes stay unreviewed; PERF-GATE is claimed only if pair.json is already ≤ 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unprofiled `pair.json` vs profile stamp | Only unprofiled walls are the 3× number. |
| Committed notes vs gitignored blobs | Docs may name stamp paths, never embed dumps. |
| Historical Phase 23 stamp | Must not be deleted or overwritten to fake improvement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | S | `pair.json` | mitigate | Require `kind unprofiled_pair`, locked 1920/60+600, exclusive new stamp, ratio strictly below 327.53. Preserve failed stamps (D-04/D-05). |
| T-24-02-02 | S | samply duration | mitigate | Profile identity must set `not_timing_authority`. Never copy samply ms into timing doc as the gate. |
| T-24-02-03 | T | `manifest.toml` | mitigate | Leave `reviewed_reports = []`. No README “Rust is N×”. |
| T-24-02-04 | D | stamp mint | mitigate | Reuse `mint_exclusive_stamp`; never `remove_dir_all` on evidence. |
</threat_model>

<verification>
Newest pair.json improves on 327.53× with the locked recipe; old stamps remain; sibling profile is not_timing_authority; notes stay unreviewed; manifest empty.
</verification>

<success_criteria>
Wave 1 is admitted by unprofiled pair evidence on this host. PERF-GATE is not claimed unless that same pair.json ratio is ≤ 3.
</success_criteria>

<output>
After completion, create `.planning/phases/24-shared-hot-path-waves-through-3/24-02-SUMMARY.md`
</output>
