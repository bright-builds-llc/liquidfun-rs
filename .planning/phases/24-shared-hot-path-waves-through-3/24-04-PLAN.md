---
phase: 24-shared-hot-path-waves-through-3
plan: "04"
type: execute
wave: 4
depends_on: [24-03]
files_modified:
  - crates/liquidfun/src/particle/storage/runtime.rs
  - crates/liquidfun/src/particle/storage/lifecycle.rs
  - crates/liquidfun/src/particle/proxy.rs
  - crates/liquidfun/src/particle/contact.rs
  - crates/liquidfun/src/particle/body_contact.rs
  - crates/liquidfun/src/particle/solver
  - docs/playground-dam-break-timing.md
  - docs/native-performance-audit.md
autonomous: true
requirements: [PERF-GATE, PERF-ADMIT, PERF-SHARED, PERF-BASELINE]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 24-2026-09-21T15-01-47
generated_at: 2026-09-21T15:19:51Z
must_haves:
  truths:
    - "Unprofiled just playground-dam-break-bench pair.json rust_over_cpp_ratio is ≤ 3.0 on this host for 1920 particles, 60 warmup, 600 measured steps, scalar --release vs oracle-release."
    - "That pair.json records host, git HEAD, compilers, rust.wall_ms, cpp.wall_ms, and ratio; timing_authority is unprofiled_wall_clock."
    - "Further ranked frames were not gold-plated after the ratio crossed ≤ 3 (D-06)."
    - "No default Rayon or SIMD, no -ffast-math / -march=native on the pair, workspace unsafe_code = forbid unchanged, liquidfun still bitflags-only."
    - "reference/performance/manifest.toml still has reviewed_reports = []."
  artifacts:
    - path: docs/playground-dam-break-timing.md
      provides: "Unreviewed gate pair sample with rust_over_cpp_ratio ≤ 3"
      contains: "rust_over_cpp_ratio"
    - path: docs/native-performance-audit.md
      provides: "Leftover-until-gate notes; samply not the 3× number"
      contains: "Unreviewed local sample"
    - path: Cargo.toml
      provides: "unsafe_code forbid and no [profile.release] debug override"
      contains: "unsafe_code = \"forbid\""
    - path: reference/performance/manifest.toml
      provides: "Empty reviewed reports"
      contains: "reviewed_reports = []"
  key_links:
    - from: docs/playground-dam-break-timing.md
      to: target/dam-break-perf
      via: "gate pair.json stamp"
      pattern: "unprofiled_pair"
    - from: crates/liquidfun/src/world/particle_coupling.rs
      to: crates/liquidfun/src/particle
      via: "shared stepping still used by World::step"
      pattern: "update_particle_contacts"
---

<objective>
Continue samply-retargeted scalar leftover waves until the unprofiled Dam Break Medium pair is ≤ 3× pinned C++.

Purpose: D-06/D-09/D-14/PERF-GATE. 24-03 ran one leftover cycle. This plan loops the same protocol until `pair.json` `rust_over_cpp_ratio` ≤ 3.0. Do not complete the plan while the ratio is still above 3.
Output: Gate-proving exclusive `pair.json` plus unreviewed notes. SIMD/`unsafe`/PGO remain forbidden even if leftover is stubborn.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-shared-hot-path-waves-through-3/24-03-SUMMARY.md
@.planning/phases/24-shared-hot-path-waves-through-3/24-CONTEXT.md
@docs/native-performance-audit.md
@docs/playground-dam-break-timing.md
@Cargo.toml
@crates/liquidfun/Cargo.toml
@reference/performance/manifest.toml

<interfaces>
Loop until gate (D-09). Each iteration is one named concern (D-04):

```
while true:
  latest_pair = newest target/dam-break-perf/*/pair.json with kind unprofiled_pair
  if latest_pair.rust_over_cpp_ratio <= 3.0:
    stop leftover kernels (D-06)
    break
  latest_profile = newest same-HEAD samply stamp (kind samply_cpu)
  rank sidecar-first like Phase 23
  pick next leftover:
    remaining D-07 names with non-trivial share, in D-07 order
    else highest named extra-work frame on THIS profile (D-09)
  land that one shared liquidfun change
  cargo test -p liquidfun (+ particle_contacts, permutation, solver_order, solver_baseline)
  just playground-dam-break-bench  # NEW exclusive stamp
  if tests fail OR ratio did not improve:
    keep failed stamp; revert this iteration's kernel; do not call it a win
    diagnose and retry a different named leftover — never SIMD/unsafe
  else if ratio still > 3:
    just playground-dam-break-profile  # sibling, not_timing_authority
    continue
  else:
    break
```

Criterion micros are allowed **only** if the latest pair ratio is already in `(3, 5]` **and** a named kernel remains. Criterion is never the 3× number.

Forbidden closers: `std::simd`, `rayon`, `-C target-cpu=native`, `-ffast-math`, PGO, lifting `unsafe_code = "forbid"`, `get_unchecked`, `HashMap&lt;ParticleId, usize&gt;` as the hot path, shrinking `PARTICLE_COUNT`, skipping solver passes, `step_profiled` inside `dam-break-bench`.

Do not grow `tools/xtask/src/playground/pair.rs` or `lifecycle.rs` past 628. Split with `foo.rs` plus `foo/` if a particle file would exceed 628.

PERF-GATE record (D-14) must include: stamp path, `git_head`, os/arch/cpu, rustc, AppleClang (or recorded C++ compiler), `rust.wall_ms`, `cpp.wall_ms`, `rust_over_cpp_ratio` ≤ 3, `particles` 1920, warmup 60, measured 600, `timing_authority: unprofiled_wall_clock`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Loop leftover waves until pair.json ratio ≤ 3</name>
  <files>crates/liquidfun/src/particle/storage/runtime.rs, crates/liquidfun/src/particle/storage/lifecycle.rs, crates/liquidfun/src/particle/proxy.rs, crates/liquidfun/src/particle/contact.rs, crates/liquidfun/src/particle/body_contact.rs, crates/liquidfun/src/particle/solver</files>
  <read_first>
    1. `.planning/phases/24-shared-hot-path-waves-through-3/24-03-SUMMARY.md`
    1. `.planning/phases/24-shared-hot-path-waves-through-3/24-CONTEXT.md` D-06 D-07 D-09 D-15
    1. Latest `pair.json` and latest `rust.json.syms.json`
    1. `Cargo.toml` `unsafe_code = "forbid"`
  </read_first>
  <action>
    Run the loop in interfaces until `rust_over_cpp_ratio` ≤ 3.0. If 24-03 already met the gate and HEAD matches that pair's `git_head`, do not land further frames.

    Each landed iteration: one named function/typed bottleneck from the **current** samply, shared `liquidfun` particle/rigid stepping only, tests, new exclusive unprofiled pair, samply retarget if still &gt; 3.

    Record every iteration in SUMMARY: leftover name, share, stamp, old ratio, new ratio, pass/fail. Preserve failed stamps.

    This task is not done while the newest same-HEAD unprofiled `pair.json` ratio is &gt; 3. Do not “defer” PERF-GATE to Phase 25. Do not invent names. Do not complete with SIMD.
  </action>
  <verify>
    <automated>python3 -c 'import json,pathlib,subprocess; head=subprocess.check_output(["git","rev-parse","HEAD"],text=True).strip(); pairs=sorted(pathlib.Path("target/dam-break-perf").glob("*/pair.json"));
cands=[]
for p in pairs:
    r=json.loads(p.read_text());
    if r.get("kind")=="unprofiled_pair" and r.get("git_head")==head: cands.append((p,r))
assert cands, "no unprofiled pair.json for HEAD"
p,r=cands[-1]
assert r["particles"]==1920 and r["warmup_steps"]==60 and r["measured_steps"]==600
assert r["timing_authority"]=="unprofiled_wall_clock"
assert r["rust_over_cpp_ratio"]&lt;=3.0, r["rust_over_cpp_ratio"]
print(p.parent.name, r["rust_over_cpp_ratio"], r["rust"]["wall_ms"], r["cpp"]["wall_ms"])'</automated>
  </verify>
  <acceptance_criteria>
    1. Newest same-HEAD `pair.json` has `rust_over_cpp_ratio` ≤ 3.0.
    1. That report is `kind unprofiled_pair`, `timing_authority unprofiled_wall_clock`, 1920/60+600.
    1. `rg -n "std::simd|rayon" crates/liquidfun` is empty.
    1. `rg -n "unsafe_code = \"forbid\"" Cargo.toml` matches.
    1. `rg -n "^bitflags" crates/liquidfun/Cargo.toml` matches and that Cargo.toml has no `serde`/`dhat`/`samply` dependencies.
    1. `git diff -- crates/liquidfun-wasm/src/scene/dam_break.rs` is empty.
  </acceptance_criteria>
  <done>Unprofiled Dam Break Medium pair.json on this HEAD is ≤ 3× C++ without SIMD or unsafe.</done>
</task>

<task type="auto">
  <name>Task 2: Record PERF-GATE in unreviewed notes without filling the manifest</name>
  <files>docs/playground-dam-break-timing.md, docs/native-performance-audit.md</files>
  <read_first>
    1. Task 1 gate `pair.json`
    1. `docs/playground-dam-break-timing.md`
    1. `docs/native-performance-audit.md`
    1. `reference/performance/manifest.toml`
    1. `BENCHMARKING.md` (read only — do not add a public N× claim)
  </read_first>
  <action>
    Copy host, HEAD, compilers, both wall times, and `rust_over_cpp_ratio` from the gate `pair.json` into `docs/playground-dam-break-timing.md` as the current unreviewed sample. Keep the Unreviewed banner and the historical MEASURED_HEAD `6d98531…` / `327.53×` pointer.

    Update `docs/native-performance-audit.md` with the leftover-until-gate list actually landed (names from live profiles only) and state that samply / timers / dhat clocks remain `not_timing_authority`. Do not gold-plate leftover ~2% frames after the gate (D-06).

    Do not set `reviewed_reports`. Do not add README or crates.io “Rust is N× slower/faster”. Run `just markdown-check`.

    SUMMARY must quote the gate stamp path and the six numeric fields listed in interfaces. Independent review is still not performed here.
  </action>
  <verify>
    <automated>just markdown-check &amp;&amp; rg -n "reviewed_reports = \\[\\]" reference/performance/manifest.toml</automated>
  </verify>
  <acceptance_criteria>
    1. `docs/playground-dam-break-timing.md` contains `Unreviewed local sample` and a `rust_over_cpp_ratio` copied from the gate pair.json.
    1. That documented ratio is ≤ 3.
    1. `docs/native-performance-audit.md` still contains `Not found` for SIMD-first as the closer.
    1. `reference/performance/manifest.toml` still has `reviewed_reports = []`.
    1. `rg -n "Rust is .*×" README.md` is empty (no new universal speed claim).
  </acceptance_criteria>
  <done>PERF-GATE is demonstrated by pair.json ≤ 3 and recorded as an unreviewed local sample, not a sealed public claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate number | Only unprofiled pair.json walls. |
| Scalar baseline | SIMD/unsafe would change the compatibility story. |
| Public claims | Manifest/README must not gain a reviewed N×. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | S | PERF-GATE evidence | mitigate | Require same-HEAD pair.json ≤ 3 with locked 1920/60+600 and `unprofiled_wall_clock`. |
| T-24-04-02 | T | leftover kernels | mitigate | Keep `resolve_live` for stale IDs; fail-closed typed errors; no `unwrap` on `try_reserve`. |
| T-24-04-03 | T | `unsafe_code` / SIMD | mitigate | Workspace forbid stays; grep `std::simd`/`rayon` empty. |
| T-24-04-04 | S | `manifest.toml` | mitigate | `reviewed_reports` remains `[]`. |
| T-24-04-05 | D | unbounded leftover loop | accept | Hobby host; loop is finite because each win must improve ratio and D-06 stops at ≤3. Do not skip the gate. |
</threat_model>

<verification>
Python assertion on newest same-HEAD pair.json ratio ≤ 3. Isolation greps for SIMD/unsafe. Notes unreviewed. Manifest empty.
</verification>

<success_criteria>
Native Dam Break Medium unprofiled wall time is ≤ 3× pinned C++ on this host under the locked recipe. Scalar deterministic baseline unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/24-shared-hot-path-waves-through-3/24-04-SUMMARY.md`
</output>
