---
phase: 24-shared-hot-path-waves-through-3
plan: "03"
type: execute
wave: 3
depends_on: [24-02]
files_modified:
  - crates/liquidfun/src/particle/storage/runtime.rs
  - crates/liquidfun/src/particle/storage/lifecycle.rs
  - crates/liquidfun/src/particle/proxy.rs
  - crates/liquidfun/src/particle/contact.rs
  - crates/liquidfun/src/particle/body_contact.rs
  - docs/playground-dam-break-timing.md
  - docs/native-performance-audit.md
autonomous: true
requirements: [PERF-ADMIT, PERF-SHARED]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 24-2026-09-21T15-01-47
generated_at: 2026-09-21T15:19:51Z
must_haves:
  truths:
    - "If 24-02 pair.json rust_over_cpp_ratio is already ≤ 3, no further ranked kernel is landed in this plan (D-06)."
    - "If the ratio is still > 3, exactly one leftover concern is admitted from the 24-02 samply ranking using D-07 order (first leftover may now be check_invariants because wave 1 already landed)."
    - "Leftover names come from the live sidecar/profile, never from inventing hot functions or pasting the Phase 23 hunt list as causes."
    - "A new exclusive unprofiled pair stamp records whether that leftover improved the ratio; failed physics or a non-improving ratio is not a win."
    - "Fixes stay in shared liquidfun particle/rigid stepping; Dam Break scene count stays 1920; no SIMD/unsafe/HashMap-cache."
  artifacts:
    - path: docs/native-performance-audit.md
      provides: "First leftover admit-or-skip note bound to the new samply"
      contains: "Unreviewed local sample"
    - path: docs/playground-dam-break-timing.md
      provides: "Unreviewed pair sample after leftover admit-or-skip"
      contains: "rust_over_cpp_ratio"
  key_links:
    - from: docs/native-performance-audit.md
      to: target/dam-break-perf
      via: "24-02 profile stamp leftover ranking"
      pattern: "target/dam-break-perf/"
---

<objective>
From the post-wave-1 samply, admit-or-skip the first leftover named concern in D-07 order.

Purpose: D-06/D-07/D-08/PERF-ADMIT. Wave 1 likely leaves the pair above 3×. Do not gold-plate if the gate already holds. Do not start `check_invariants` gating in the same breath as `particle_rows` — that already landed in 24-01/24-02.
Output: Either a skip note (ratio ≤ 3) or one shared-path leftover plus a new exclusive unprofiled pair.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-shared-hot-path-waves-through-3/24-02-SUMMARY.md
@docs/native-performance-audit.md
@crates/liquidfun/src/particle/storage/runtime.rs
@crates/liquidfun/src/particle/storage/lifecycle.rs
@crates/liquidfun/src/particle/proxy.rs
@crates/liquidfun/src/particle/contact.rs
@crates/liquidfun/src/particle/body_contact.rs

<interfaces>
Leftover protocol (this plan runs **one** cycle):

1. Read 24-02 SUMMARY for pair stamp, profile stamp, and `rust_over_cpp_ratio`.
1. If that ratio is ≤ 3.0: write skip notes (D-06). Do not edit kernels. Do not run Criterion.
1. Else rank the 24-02 sibling `rust.json.syms.json` (sidecar first) plus gecko `stackTable` sample weights on the `dam-break-bench` thread, same method as Phase 23 `docs/native-performance-audit.md`. Approximate leaf shares, not samply milliseconds.
1. Walk D-07 in this exact order and pick the **first** symbol that still has non-trivial named share (≥ ~0.5% self, or the largest remaining named `liquidfun::` leftover if all D-07 shares are smaller but still visible):
   1. `ParticleStorage::check_invariants` / `SliceContains::slice_contains`
   1. `ParticleStorage::replace_solver_candidate`
   1. `ParticleNeighborhood::from_view`
   1. `Vec&lt;ParticleContact&gt;` collect / `RawVecInner::finish_grow`
   1. `particle::contact::listener_effects` / `body_contact::listener_effects` / `body_contact::particle_row`
1. If none of those still appear, pick the highest remaining **named** extra-work frame from **this** profile (D-09). Never invent a name. Never SIMD, Rayon, `-ffast-math`, `-march=native`, PGO, `unsafe`, or `HashMap&lt;ParticleId, usize&gt;`.

Concrete leftover implementations when selected (only the chosen one):

- **check_invariants (D-08 later wave, now allowed):** In `replace_solver_candidate` only (`runtime.rs` ~line 94), gate `candidate.check_invariants()?` behind `#[cfg(debug_assertions)]`. Do **not** remove `check_invariants()?` from `creation.rs`, `mutation.rs`, `group.rs`, or other user-facing checked mutation. Typed `ParticleStorageError` / public API errors stay fail-closed.
- **replace_solver_candidate clone:** Stop cloning the entire `ParticleStorage` on the solver commit path. Validate lane lengths/NaNs, then assign the candidate lanes in place (or swap buffers) so `oracle-release` C++ in-place mutation is the shape. Keep invalid bundles returning `ParticleStorageError::InvalidLaneBundle`. Do not `unwrap`.
- **from_view:** Only if still named. Reuse proxy storage / avoid rebuilding from scratch if that is the sampled cost. Do **not** fuse `AddContact` unless this profile names pair `Vec` allocation rather than tag walk.
- **Vec collect / RawVec:** Reuse a caller-owned contact buffer or `clear`+append (C++ `SetCount(0)` shape) in shared contact generation. If you `try_reserve`, map failure to a typed error — never `unwrap`.
- **listener_effects / particle_row:** Replace remaining identity scans, including `body_contact.rs` `particle_row` `.expect("generated body contacts retain a current particle")`, with `maybe_live_row`. Return typed errors; do not `expect`/`unwrap` on lookup.

After the one kernel change: `cargo test -p liquidfun` plus `particle_contacts`, `particle_permutation_coherence`, `particle_solver_order`, `particle_solver_baseline`. Then `just playground-dam-break-bench` into a **new** stamp. Ratio must improve vs 24-02 pair.json to admit. If tests fail or ratio does not improve: keep the failed stamp, revert the leftover kernel, record failure.

Do not edit `crates/liquidfun-wasm/src/scene/dam_break.rs` particle count. Do not grow `pair.rs` past 628. Do not grow `lifecycle.rs` past 628.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rank 24-02 samply and admit-or-skip one leftover</name>
  <files>crates/liquidfun/src/particle/storage/runtime.rs, crates/liquidfun/src/particle/storage/lifecycle.rs, crates/liquidfun/src/particle/proxy.rs, crates/liquidfun/src/particle/contact.rs, crates/liquidfun/src/particle/body_contact.rs</files>
  <read_first>
    1. `.planning/phases/24-shared-hot-path-waves-through-3/24-02-SUMMARY.md`
    1. `.planning/phases/24-shared-hot-path-waves-through-3/24-CONTEXT.md` D-06 D-07 D-08 D-09
    1. `docs/native-performance-audit.md` ranking method
    1. The 24-02 `rust.json.syms.json` sidecar
    1. The candidate source file for the selected leftover only
  </read_first>
  <action>
    Execute the leftover protocol in interfaces for **one** cycle. Record in SUMMARY: pair ratio, whether skip-because-≤3, ranked leftover table (name + approximate leaf share), chosen concern or “none named”, files actually edited.

    If skipping because ≤ 3: zero kernel diffs. If admitting: land only that one concern in shared `liquidfun` (D-10). Keep `maybe_` naming, no production `unwrap`, `let...else` guards, files ≤628 lines.

    Forbidden: SIMD, Rayon, `unsafe`, HashMap identity cache, shrinking 1920 particles, Dam Break-only shortcuts, treating samply ms as the 3× number, gating **user-facing** `check_invariants` on create/mutate APIs.
  </action>
  <verify>
    <automated>cargo test -p liquidfun --test particle_contacts &amp;&amp; cargo test -p liquidfun --test particle_solver_order &amp;&amp; cargo test -p liquidfun --test particle_solver_baseline &amp;&amp; cargo test -p liquidfun</automated>
  </verify>
  <acceptance_criteria>
    1. `24-03-SUMMARY.md` records either `skip leftover: pair.json rust_over_cpp_ratio ≤ 3` or exactly one named leftover symbol from the 24-02 profile.
    1. If a leftover landed, `rg -n "HashMap|std::simd|rayon" crates/liquidfun/src/particle` is empty in the diff.
    1. `rg -n "unsafe " crates/liquidfun/src` is empty.
    1. `crates/liquidfun-wasm/src/scene/dam_break.rs` is not in `git diff --name-only`.
    1. `cargo test -p liquidfun` exits 0.
    1. If `check_invariants` was gated, `rg -n "check_invariants" crates/liquidfun/src/particle/storage/creation.rs crates/liquidfun/src/particle/storage/mutation.rs` still shows `check_invariants()?` on those user-facing paths.
  </acceptance_criteria>
  <done>Either leftover kernels were skipped because the gate already holds, or one D-07/D-09 named leftover landed in shared liquidfun with tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Exclusive unprofiled re-pair after admit-or-skip</name>
  <files>docs/playground-dam-break-timing.md, docs/native-performance-audit.md</files>
  <read_first>
    1. Task 1 SUMMARY decision (skip vs landed leftover)
    1. `docs/playground-dam-break-timing.md`
    1. 24-02 pair.json ratio
  </read_first>
  <action>
    If Task 1 skipped because ratio ≤ 3: do not run a new pair unless HEAD no longer matches the 24-02 pair `git_head`. Refresh audit notes with “first leftover skipped (D-06)” and stop.

    If Task 1 landed a leftover: `just playground-dam-break-bench` into a **new** exclusive stamp. Compare `rust_over_cpp_ratio` to the 24-02 pair.json. Admission requires a strictly smaller finite ratio, same 1920/60+600 recipe, and old stamps preserved. Then `just playground-dam-break-profile` into another new stamp (`not_timing_authority`) so 24-04 can rank the next leftover.

    Refresh timing + audit docs as unreviewed local samples from the new `pair.json` only. Keep the Phase 23 MEASURED_HEAD pointer. Do not claim PERF-GATE unless this new ratio is ≤ 3. Do not edit `manifest.toml`. `just markdown-check`.
  </action>
  <verify>
    <automated>python3 -c 'import json,pathlib; pairs=sorted(pathlib.Path("target/dam-break-perf").glob("*/pair.json")); r=json.loads(pairs[-1].read_text()); assert r["kind"]=="unprofiled_pair" and r["particles"]==1920; print(pairs[-1].parent.name, r["rust_over_cpp_ratio"])'</automated>
  </verify>
  <acceptance_criteria>
    1. If a leftover landed, newest pair.json is a new stamp (not the 24-02 stamp) with 1920/60+600 and an improved `rust_over_cpp_ratio` versus 24-02.
    1. If leftover skipped for ≤3, notes say D-06 skip and kernels were not required for admission.
    1. `docs/playground-dam-break-timing.md` remains `Unreviewed local sample`.
    1. `git diff -- reference/performance/manifest.toml` is empty.
    1. `git ls-files '*.json.gz'` is empty.
  </acceptance_criteria>
  <done>First leftover cycle is evidenced by skip-or-new pair.json without overwriting prior stamps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Release `check_invariants` vs user-facing mutation | Only the solver hot path may follow C++ NDEBUG; create/mutate stays fail-closed. |
| Samply names vs hunt list | Invented frames would optimize the wrong code. |
| Stale body-contact rows | `particle_row` `.expect` is a panic path; leftover must use `maybe_live_row`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-01 | T | `replace_solver_candidate` / `check_invariants` | mitigate | Gate assertions only on the solver candidate path. Keep creation/mutation `check_invariants()?`. |
| T-24-03-02 | I | leftover ranking | mitigate | Sidecar-first names from the 24-02 profile only; D-07 order; no hunt-list paste. |
| T-24-03-03 | D | `try_reserve` / contact `Vec` | mitigate | Typed error on reserve failure; never `unwrap`. |
| T-24-03-04 | T | `body_contact::particle_row` | mitigate | Replace `.expect` with `maybe_live_row` + typed error if that leftover is selected. |
| T-24-03-05 | S | Dam Break cheats | mitigate | Shared `liquidfun` only; 1920 recipe unchanged. |
</threat_model>

<verification>
Tests stay green. Either D-06 skip or one named leftover plus an improving exclusive pair.json.
</verification>

<success_criteria>
The first leftover is either skipped because the unprofiled pair is already ≤ 3×, or admitted from live samply names in D-07 order with a new exclusive pair stamp.
</success_criteria>

<output>
After completion, create `.planning/phases/24-shared-hot-path-waves-through-3/24-03-SUMMARY.md`
</output>
