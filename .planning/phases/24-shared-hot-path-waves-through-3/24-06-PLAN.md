---
phase: 24-shared-hot-path-waves-through-3
plan: "06"
type: execute
wave: 6
depends_on: [24-05]
files_modified:
  - BENCHMARKING.md
autonomous: true
requirements: [PERF-BASELINE]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 24-2026-09-21T15-01-47
generated_at: 2026-09-21T15:19:51Z
must_haves:
  truths:
    - "cargo xtask package verify passes and cargo tree -p liquidfun --edges normal is liquidfun → bitflags only."
    - "Workspace unsafe_code remains forbid; liquidfun has no rayon, std::simd, serde, samply, dhat, or cmake dependency."
    - "just playground recipes are one-line cargo xtask aliases with no cmake/samply/dhat flags, including playground-scene-spot."
    - "reference/performance/manifest.toml still has reviewed_reports = []; git ls-files has no json.gz, trace, or dhat-heap.json."
    - "README.md has no universal Rust is N× speed claim; BENCHMARKING.md still forbids copying playground numbers into the manifest."
    - "The implementing agent does not write an independent-review acknowledgment; 24-06-SUMMARY.md states /gsd-verify-work 24 and a separate AI reviewer remain required."
  artifacts:
    - path: BENCHMARKING.md
      provides: "Pointer to Phase 24 unreviewed timing and spot-check notes"
      contains: "playground-scene-spot-check.md"
    - path: crates/liquidfun/Cargo.toml
      provides: "bitflags-only production deps"
      contains: "bitflags"
    - path: Cargo.toml
      provides: "unsafe_code forbid"
      contains: "unsafe_code = \"forbid\""
    - path: reference/performance/manifest.toml
      provides: "Empty reviewed_reports"
      contains: "reviewed_reports = []"
  key_links:
    - from: BENCHMARKING.md
      to: docs/playground-scene-spot-check.md
      via: "unreviewed spot-check notes"
      pattern: "playground-scene-spot-check.md"
    - from: tools/xtask/src/package.rs
      to: crates/liquidfun
      via: "package verify isolation"
      pattern: "FORBIDDEN_PREFIXES"
---

<objective>
Prove package isolation, scalar baseline, empty manifest, honest benchmarking pointers, and that the implementing agent does not self-approve independent review.

Purpose: D-13/D-15/PERF-BASELINE. Kernel and spot-check plans can accidentally leak tooling deps or public speed claims.
Output: One BENCHMARKING.md sentence, passing isolation gates, SUMMARY that leaves independent AI review to `/gsd-verify-work 24` plus a **separate** reviewer.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@BENCHMARKING.md
@docs/playground-dam-break-timing.md
@docs/native-performance-audit.md
@docs/playground-scene-spot-check.md
@crates/liquidfun/Cargo.toml
@Cargo.toml
@reference/performance/manifest.toml
@justfile
@tools/xtask/src/package.rs

<interfaces>
Independent review (owner 2026-09-16, D-13): the implementing agent must not approve its own work. Do **not** create a REVIEW.md acknowledgment bound to this agent's identity. Do **not** write `reviewer: <this agent>` anywhere.

BENCHMARKING.md: add one sentence in the exploratory local diagnosis section pointing at `[docs/playground-scene-spot-check.md](docs/playground-scene-spot-check.md)` as unreviewed native scene spot-checks that are **not** the Dam Break 3× number. Keep existing pointers to `docs/native-performance-audit.md` and the empty-manifest rule. Do not paste wall-ms.

`just` playground aliases after 24-05 must remain one-line `cargo xtask playground …` printers. Allowed names: `dam-break-bench`, `dam-break-profile`, `dam-break-timers`, `dam-break-audit-bundle`, `dam-break-heap`, `scene-spot`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point BENCHMARKING.md at unreviewed spot-check notes</name>
  <files>BENCHMARKING.md</files>
  <read_first>
    1. `.planning/phases/24-shared-hot-path-waves-through-3/24-CONTEXT.md` D-14 D-15
    1. `BENCHMARKING.md`
    1. `docs/playground-scene-spot-check.md`
    1. `reference/performance/manifest.toml`
    1. `AGENTS.md` Repo-Local Guidance for `just markdown-check`
  </read_first>
  <action>
    Edit only `BENCHMARKING.md`. Insert one additional sentence that names `docs/playground-scene-spot-check.md`, calls it an unreviewed local sample, and states it is not the Dam Break 3× gate and must not be copied into `reference/performance/manifest.toml`. Do not paste wall-ms or “Rust is N×”. Run `just markdown-check`.
  </action>
  <verify>
    <automated>just markdown-check &amp;&amp; rg -n "playground-scene-spot-check.md" BENCHMARKING.md</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "playground-scene-spot-check.md" BENCHMARKING.md` matches.
    1. `rg -n "manifest.toml" BENCHMARKING.md` still forbids copying playground numbers into the manifest.
    1. `git diff -- reference/performance/manifest.toml` is empty.
    1. `just markdown-check` exits 0.
  </acceptance_criteria>
  <done>BENCHMARKING.md points at the five-scene spot-check notes without turning them into a public speed claim.</done>
</task>

<task type="auto">
  <name>Task 2: Isolation, Bright Builds, and no self-approval</name>
  <files>BENCHMARKING.md</files>
  <read_first>
    1. `AGENTS.md` Independent review
    1. `standards/core/verification.md`
    1. `crates/liquidfun/Cargo.toml`
    1. `Cargo.toml`
    1. `justfile` playground recipes
    1. `tools/xtask/src/package.rs` FORBIDDEN_PREFIXES
    1. `.planning/phases/24-shared-hot-path-waves-through-3/24-04-SUMMARY.md` gate claim
  </read_first>
  <action>
    Run isolation and honesty checks. If file-lengths fail, split the offending repo-owned file (`foo.rs` plus `foo/`) rather than adding a TSV exception.

    Required commands (all must exit 0), from the repo root:

    1. `cargo xtask package verify`
    1. `cargo tree -p liquidfun --edges normal` — crate names `dhat`, `samply`, `serde`, `serde_json`, `flate2`, `cmake`, `rayon` must not appear
    1. `cargo fmt --all --check`
    1. `cargo clippy -p liquidfun --all-targets --all-features -- -D warnings`
    1. `cargo test -p liquidfun`
    1. `cargo deny --locked check`
    1. `bun scripts/bright-builds-check.ts all`
    1. `just markdown-check`

    Also assert:
    - `rg -n "unsafe_code = \"forbid\"" Cargo.toml` matches
    - no `[profile.release]` debug override (named `[profile.profiling]` may remain)
    - `rg -n "std::simd|rayon" crates/liquidfun/src` is empty
    - `rg -n "samply|cmake|dhat" justfile` is empty
    - `rg -n "reviewed_reports = \\[\\]" reference/performance/manifest.toml` matches
    - `git ls-files '*.json.gz' '*.trace' 'dhat-heap.json'` is empty
    - `rg -n "Rust is .*×" README.md` is empty
    - no new `mod.rs` under `crates/liquidfun/src/particle` or `tools/xtask/src/playground`

    Record command outcomes in `24-06-SUMMARY.md`. Explicitly write that independent AI review is **not** satisfied by this plan, that the implementing agent must not approve the phase, and that `/gsd-verify-work 24` plus a separate AI reviewer remain required (owner policy 2026-09-16).
  </action>
  <verify>
    <automated>cargo xtask package verify &amp;&amp; cargo tree -p liquidfun --edges normal &amp;&amp; bun scripts/bright-builds-check.ts all</automated>
  </verify>
  <acceptance_criteria>
    1. `cargo xtask package verify` exits 0.
    1. `cargo tree -p liquidfun --edges normal` contains `bitflags` and does not contain `dhat`, `samply`, or `serde`.
    1. `rg -n "unsafe_code = \"forbid\"" Cargo.toml` matches.
    1. `rg -n "reviewed_reports = \\[\\]" reference/performance/manifest.toml` matches.
    1. `git ls-files '*.json.gz' '*.trace' 'dhat-heap.json'` is empty.
    1. `bun scripts/bright-builds-check.ts all` exits 0.
    1. `24-06-SUMMARY.md` states independent AI review is still required and is not performed by the implementer.
    1. No `24-REVIEW.md` (or similar) acknowledgment file is authored by this implementing agent claiming phase approval.
  </acceptance_criteria>
  <done>Isolation, scalar baseline, empty manifest, and honesty gates pass; the implementer does not self-approve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published `liquidfun` graph | Playground/xtask/dhat must not leak here. |
| Committed docs vs gitignored stamps | BENCHMARKING.md may name paths, not embed dumps. |
| Review acknowledgment | Implementer identity must not be recorded as independent review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06-01 | T | `liquidfun` supply chain | mitigate | `package verify` + `cargo tree -p liquidfun --edges normal`; Cargo.toml bitflags-only (D-15). |
| T-24-06-02 | S | public speed claim | mitigate | Empty `reviewed_reports`; no README N×; unreviewed banners on timing/audit/spot docs. |
| T-24-06-03 | E | self-approval | mitigate | SUMMARY forbids implementer REVIEW acknowledgment; `/gsd-verify-work 24` + separate AI reviewer (D-13). |
| T-24-06-04 | T | `unsafe_code` | mitigate | Workspace lint remains `forbid`; grep SIMD/rayon empty (PERF-BASELINE). |
| T-24-06-05 | I | profile blobs in git | mitigate | `git ls-files` has no `json.gz` / `trace` / `dhat-heap.json`. |
</threat_model>

<verification>
package verify, cargo tree bitflags-only, Bright Builds, empty manifest, no self-approval text.
</verification>

<success_criteria>
`liquidfun` remains a bitflags-only safe scalar crate. Evidence stays unreviewed and gitignored. Independent review is explicitly not done by the implementer.
</success_criteria>

<output>
After completion, create `.planning/phases/24-shared-hot-path-waves-through-3/24-06-SUMMARY.md`
</output>
