---
phase: 25-wasm-sanity-and-honest-close
plan: "02"
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - docs/playground-dam-break-timing.md
autonomous: true
requirements: [PERF-WASM]
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 25-2026-09-21T22-11-01
generated_at: 2026-09-21T22:20:00Z
must_haves:
  truths:
    - "just web-player-smoke exits 0 and exercises Dam Break, Fountain, Float or Sink, Color Mixer, Jelly Drop, and Water Wheel via the existing Chromium suite."
    - "Committed timing notes include an optional Dam Break step-time sentence citing native Rust ms_per_step 1.053103 from gate stamp 2026-09-21T20-38-50Z and the exact phrase no prior WASM step-time sample."
    - "The WASM note never compares browser Dam Break to oracle-release or any C++ wall."
    - "MAX_ADVANCE_STEPS remains 4; no Instant, step_profiled, or samply is added to the cdylib."
    - "Smoke artifacts under target/web-build are not committed."
  artifacts:
    - path: docs/playground-dam-break-timing.md
      provides: "Optional WASM Dam Break step-time note versus native Rust only"
      contains: "no prior WASM step-time sample"
    - path: crates/liquidfun-wasm/src/session.rs
      provides: "Catch-up cap invariant verification only"
      contains: "MAX_ADVANCE_STEPS: u32 = 4"
    - path: justfile
      provides: "Existing web-player-smoke alias (unchanged)"
      contains: "web-player-smoke"
  key_links:
    - from: docs/playground-dam-break-timing.md
      to: target/dam-break-perf/2026-09-21T20-38-50Z/pair.json
      via: "native ms_per_step 1.053103 citation"
      pattern: "1\\.053103"
    - from: justfile
      to: scripts/web-build.ts
      via: "web-player-smoke → bun scripts/web-build.ts player-smoke"
      pattern: "web-player-smoke"
    - from: crates/liquidfun-wasm/src/session.rs
      to: web/src/physics/clock.ts
      via: "MAX_ADVANCE_STEPS / MAX_STEPS_PER_FRAME stay 4"
      pattern: "MAX_ADVANCE_STEPS"
---

<objective>
Prove the six playground scenes still run with the existing Chromium smoke and record the optional Dam Break WASM step-time note versus native Rust only.

Purpose: Close PERF-WASM (D-01 through D-05). No new browser matrix, no Instant in the cdylib, no C++ comparison.
Output: Exit-0 `just web-player-smoke` cited in SUMMARY; one-line/short WASM note in `docs/playground-dam-break-timing.md`; catch-up cap still 4.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-wasm-sanity-and-honest-close/25-CONTEXT.md
@.planning/phases/25-wasm-sanity-and-honest-close/25-RESEARCH.md
@.planning/phases/25-wasm-sanity-and-honest-close/25-01-SUMMARY.md
@.planning/phases/24-shared-hot-path-waves-through-3/24-VERIFICATION.md
@docs/playground-dam-break-timing.md
@justfile
@scripts/web-build.ts
@crates/liquidfun-wasm/src/session.rs
@web/src/physics/clock.ts
@target/dam-break-perf/2026-09-21T20-38-50Z/pair.json

<interfaces>
Smoke chain (do not rewrite):

```bash
just web-player-smoke
# → bun scripts/web-build.ts player-smoke
# → regenerateWasm + frontend build + bun run test:player
# Playwright: e2e/player.spec.ts, demo-media-clock.spec.ts, shell.spec.ts, reset-honesty.spec.ts
# Expected: 36 Chromium tests; SCENE_IDS include dam-break, fountain, float-or-sink, color-mixer, jelly-drop, water-wheel
```

Optional WASM note (D-03/D-04) — add after the gate table in `docs/playground-dam-break-timing.md` (exact required phrases):

```markdown
## WASM Dam Break step-time note (optional, unreviewed)

No prior WASM step-time sample exists in committed docs. Native Rust
`ms_per_step` from gate stamp `2026-09-21T20-38-50Z` is `1.053103`
(copied from that `pair.json` only). This is versus native Rust only —
never versus `oracle-release` or any other C++ wall. Not a Phase 12 sealed
public claim.
```

Invariants to assert, not change:
- `crates/liquidfun-wasm/src/session.rs`: `pub(crate) const MAX_ADVANCE_STEPS: u32 = 4;`
- `web/src/physics/clock.ts`: `MAX_STEPS_PER_FRAME = 4`
- No `Instant` / `step_profiled` / samply added under `crates/liquidfun-wasm/` for this plan

On smoke failure (D-02): diagnose shared stepping or existing player; do not rewrite scene builders or WASM copy lanes to force a pass; do not change physics to make the browser look faster. Independent AI review is a later separate-AI step — do not self-approve.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional WASM Dam Break step-time note versus native Rust</name>
  <files>docs/playground-dam-break-timing.md</files>
  <read_first>
    1. `.planning/phases/25-wasm-sanity-and-honest-close/25-CONTEXT.md` (D-03, D-04, D-05)
    1. `.planning/phases/25-wasm-sanity-and-honest-close/25-RESEARCH.md` (Pattern 3; no prior WASM sample)
    1. `.planning/phases/24-shared-hot-path-waves-through-3/24-VERIFICATION.md`
    1. `target/dam-break-perf/2026-09-21T20-38-50Z/pair.json` (confirm `rust.ms_per_step` is `1.053103`)
    1. `docs/playground-dam-break-timing.md` (must already lead with gate stamp from 25-01)
    1. `crates/liquidfun-wasm/src/session.rs`
  </read_first>
  <action>
Per D-03/D-04/D-05:

1. Confirm `docs/playground-dam-break-timing.md` already leads with gate stamp `2026-09-21T20-38-50Z` and ratio `2.956857456935513` from plan 25-01. If not, stop and finish/fix 25-01 first — do not re-reconcile here beyond the WASM note.
1. Append the optional WASM Dam Break step-time note (section or short sibling paragraph) containing exactly:
   - the phrase `no prior WASM step-time sample`
   - native Rust `ms_per_step` `1.053103`
   - gate stamp `2026-09-21T20-38-50Z`
   - explicit never-versus-`oracle-release` / never-versus-C++ wording
   - unreviewed / not a Phase 12 sealed wording
1. Do not invent a browser-measured ms/step. Do not attach `rust_over_cpp_ratio` to WASM/browser wording. Do not compare to `oracle-release`.
1. Do not add `Instant`, `step_profiled`, or samply to the cdylib. Do not edit `MAX_ADVANCE_STEPS` or `MAX_STEPS_PER_FRAME`.
1. Assert (read-only) `MAX_ADVANCE_STEPS: u32 = 4` remains in `crates/liquidfun-wasm/src/session.rs`.
  </action>
  <verify>
    <automated>rg -n "no prior WASM step-time sample|1\.053103|2026-09-21T20-38-50Z|oracle-release" docs/playground-dam-break-timing.md && rg -n "MAX_ADVANCE_STEPS: u32 = 4" crates/liquidfun-wasm/src/session.rs && python3 -c 'from pathlib import Path; t=Path("docs/playground-dam-break-timing.md").read_text(); assert "no prior WASM step-time sample" in t; assert "1.053103" in t; assert "oracle-release" in t; assert "MAX_ADVANCE_STEPS" not in t'</automated>
  </verify>
  <acceptance_criteria>
    1. `docs/playground-dam-break-timing.md` contains exact string `no prior WASM step-time sample`.
    1. `docs/playground-dam-break-timing.md` contains exact string `1.053103` and `2026-09-21T20-38-50Z` in the WASM note context.
    1. The WASM note mentions never versus `oracle-release` (or equivalent never-versus-C++ wording including the string `oracle-release`).
    1. `crates/liquidfun-wasm/src/session.rs` still contains `MAX_ADVANCE_STEPS: u32 = 4`.
    1. No new Instant / step_profiled / samply additions under `crates/liquidfun-wasm/src/` for this task.
  </acceptance_criteria>
  <done>Optional WASM note cites native gate ms_per_step 1.053103 and states no prior WASM step-time sample; catch-up cap still 4.</done>
</task>

<task type="auto">
  <name>Task 2: Run just web-player-smoke and cite exit 0 without committing artifacts</name>
  <files></files>
  <read_first>
    1. `.planning/phases/25-wasm-sanity-and-honest-close/25-CONTEXT.md` (D-01, D-02)
    1. `.planning/phases/25-wasm-sanity-and-honest-close/25-RESEARCH.md` (smoke chain; A1)
    1. `justfile` (`web-player-smoke` recipe)
    1. `scripts/web-build.ts` (player-smoke branch)
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-VERIFICATION.md` (prior coverage)
  </read_first>
  <action>
Per D-01/D-02:

1. Run `just web-player-smoke` once on the current HEAD (expected: exit 0; 36 Playwright Chromium tests across the four e2e files covering all six scenes).
1. On failure: treat as a real regression in shared stepping or the existing player. Diagnose and fix only those seams. Do **not** rewrite scene builders, WASM copy lanes, particle counts, or `MAX_ADVANCE_STEPS` / `MAX_STEPS_PER_FRAME` to force a pass. Do not change physics solely to make the browser look faster. Do not add a Firefox/Safari matrix or live Pages redeploy.
1. Record in `25-02-SUMMARY.md`: command `just web-player-smoke`, exit status `0`, and test count (36) or Playwright summary line. Do **not** `git add` `target/web-build/`, Playwright traces, `pair.json` dumps, or `*.json.gz`.
1. Do not self-approve independent review.
  </action>
  <verify>
    <automated>just web-player-smoke</automated>
  </verify>
  <acceptance_criteria>
    1. `just web-player-smoke` exits 0.
    1. SUMMARY cites `just web-player-smoke` exit 0 (and preferably 36 tests / six scenes).
    1. `git status` does not stage `target/web-build` or `*.json.gz` from this smoke run.
    1. No scene-builder or `MAX_ADVANCE_STEPS` rewrite was used to force the pass.
  </acceptance_criteria>
  <done>Six-scene Chromium smoke exits 0; evidence cited in SUMMARY without committing web-build artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser smoke → committed notes | Smoke proves scenes run; must not become a C++ speed claim |
| Local build outputs → git index | `target/web-build` logs must stay gitignored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | Tampering / Spoofing | WASM step-time note | mitigate | Cite native gate `ms_per_step` `1.053103` + exact `no prior WASM step-time sample`; forbid `oracle-release` / C++ comparison (D-03/D-04) |
| T-25-02-02 | Tampering | cdylib / clock catch-up | mitigate | Assert `MAX_ADVANCE_STEPS == 4`; ban Instant / step_profiled / samply in cdylib (D-05) |
| T-25-02-03 | Information disclosure | `target/web-build/**`, `*.json.gz` | mitigate | Cite smoke in SUMMARY only; do not `git add` smoke artifacts (Pitfall 4) |
| T-25-02-04 | Elevation of privilege (reputation) | README / public claims | accept | This plan does not edit README; 25-01 already verified claim-free surface; smoke is not a universal speed claim |
| T-25-02-05 | Denial of service / Integrity | Forced smoke green via scene/copy cheats | mitigate | D-02: on failure diagnose shared stepping/player only; no scene or copy-lane rewrite to force pass |
</threat_model>

<verification>
1. Timing doc contains `no prior WASM step-time sample` and `1.053103`.
1. `just web-player-smoke` exits 0.
1. `MAX_ADVANCE_STEPS: u32 = 4` unchanged; no web-build commit.
</verification>

<success_criteria>
PERF-WASM is satisfied: six scenes still run under existing smoke; optional WASM note is native-fallback only; catch-up cap remains 4.
</success_criteria>

<output>
After completion, create `.planning/phases/25-wasm-sanity-and-honest-close/25-02-SUMMARY.md`
</output>
