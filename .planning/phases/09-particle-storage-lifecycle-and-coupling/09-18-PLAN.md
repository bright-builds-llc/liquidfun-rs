---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 18
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - crates/liquidfun/src/world/step.rs
  - crates/liquidfun/src/world/particle_lifecycle.rs
  - crates/liquidfun/src/particle/storage.rs
  - crates/liquidfun/tests/particle_step_guards.rs
  - crates/liquidfun/tests/particle_zombie_authority.rs
autonomous: true
requirements: [PART-01, PART-02, PART-07, PART-08, PART-14, PART-15]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T23:45:08Z
must_haves:
  truths:
    - "Particle lifetime, zombie compaction, contacts, and rigid coupling run only for a fresh World step whose dt is strictly positive."
    - "The public ZOMBIE bit and the authoritative pending-delete identity state transition together in both directions before compaction."
    - "Paused systems still receive source-ordered lifetime/zombie maintenance on a fresh positive-dt step, then skip active contact/coupling work."
  artifacts:
    - path: crates/liquidfun/src/world/step.rs
      provides: "One shared fresh-positive-dt particle stage guard"
    - path: crates/liquidfun/src/world/particle_lifecycle.rs
      provides: "Zombie-flag synchronization before lifetime solve and compaction"
    - path: crates/liquidfun/tests/particle_step_guards.rs
      provides: "Zero-dt, continuation, pause, and zombie-authority regressions"
  key_links:
    - from: crates/liquidfun/src/world/step.rs
      to: crates/liquidfun/src/world/particle_lifecycle.rs
      via: "fresh-positive-dt guard shared with the discrete rigid solve"
      pattern: "ContinuousStepKind::Fresh.*time_step"
    - from: crates/liquidfun/src/world/particle_lifecycle.rs
      to: crates/liquidfun/src/particle/storage.rs
      via: "flag-to-pending synchronization and one compaction transaction"
      pattern: "ZOMBIE|PendingDelete|mark_delete"
---

<objective>
Close the Phase 9 step-timing and zombie-authority defects without widening the particle solver surface.

Purpose: Match the pinned `b2World::Step` fresh-positive-dt boundary and make one authoritative lifecycle state drive flags, access errors, snapshots, callbacks, and compaction.
Output: A shared particle-stage guard, bidirectional zombie synchronization, and focused public regressions.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-REVIEW.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-07-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate every particle step stage on one fresh positive-dt predicate</name>
  <files>crates/liquidfun/src/world/step.rs, crates/liquidfun/tests/particle_step_guards.rs</files>
  <read-first>crates/liquidfun/src/world/step.rs; crates/liquidfun/src/world/particle_lifecycle.rs; crates/liquidfun/src/world/particle_coupling.rs; crates/liquidfun/tests/particle_lifecycle.rs; crates/liquidfun/tests/particle_body_contacts.rs; third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp lines containing `m_stepComplete && step.dt > 0.0f`; 09-REVIEW WR-01; 09-VERIFICATION G09-STEP-GUARD.</read-first>
  <action>Write failing public regressions first. Extract or compute exactly one predicate equivalent to `step_kind == ContinuousStepKind::Fresh && configuration.time_step() > 0.0`. Place `run_particle_lifecycle_step`, `run_particle_contact_prefix`, particle timestamp changes, and body reaction behind that predicate at the existing pre-rigid-solve seam. Keep rigid contact discovery and continuous-stage behavior unchanged. Add one zero-dt case with a finite lifetime, pending zombie, populated particle/body contacts, and body velocity witness; assert no particle clock, compaction, contact, velocity, or lifecycle occurrence changes. Add one forced incomplete-continuous-step case whose continuation call would expose duplicate particle work; assert the fresh call changes the particle state once and every continuation leaves it byte/semantically unchanged. Retain a fresh-positive-dt control that proves the particle stages still execute.</action>
  <verify><automated>cargo test -p liquidfun --test particle_step_guards && cargo test -p liquidfun --test particle_body_contacts step</automated></verify>
  <acceptance-criteria>`rg -n "run_particle_lifecycle_step|run_particle_contact_prefix" crates/liquidfun/src/world/step.rs` shows both calls inside the same fresh-positive-dt branch; the zero-dt and continuation tests fail before the fix and pass afterward; existing rigid and fresh particle-step tests remain green.</acceptance-criteria>
  <done>The automated verification exits zero, and the `rg` check plus zero-dt, continuation, rigid, and fresh positive-dt regressions prove all particle stages share the fresh-positive-dt guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize ZOMBIE flags and pending-delete identity state</name>
  <files>crates/liquidfun/src/particle/storage.rs, crates/liquidfun/src/world/particle_lifecycle.rs, crates/liquidfun/tests/particle_zombie_authority.rs</files>
  <read-first>crates/liquidfun/src/particle/definition.rs; crates/liquidfun/src/particle/storage.rs; crates/liquidfun/src/world/particle_lifecycle.rs; crates/liquidfun/src/world/particle_object.rs; crates/liquidfun/tests/particle_objects.rs; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp `SolveZombie`; Phase 9 D-09/D-11; 09-REVIEW WR-02; 09-VERIFICATION zombie-authority row.</read-first>
  <action>Centralize the transition in storage. An explicit public destruction mark must atomically insert `ParticleFlags::ZOMBIE`, preserve any existing `DESTRUCTION_LISTENER` bit, capture the post-flag snapshot, and set `IdentityState::PendingDelete`; a repeated mark remains a typed pending-delete error with no duplicate occurrence. At the start of each eligible lifecycle pass, scan live rows in ascending dense order, transition every row whose public flags already contain `ZOMBIE` into pending-delete through the same helper, then solve lifetimes and perform the existing single survivor permutation. Add regressions for a particle created with ZOMBIE, an ordinary live particle marked through the public API, a listener-flagged zombie, repeated marking, public view flags while pending, and callback-before-ID-invalidation order. Do not run Phase 10 group or solver passes.</action>
  <verify><automated>cargo test -p liquidfun --test particle_zombie_authority</automated></verify>
  <acceptance-criteria>A created ZOMBIE row becomes pending and compacts on the next fresh positive-dt lifecycle pass; `mark_particle_for_destruction` returns a snapshot whose flags contain ZOMBIE; every pending row exposed to lifecycle code has the ZOMBIE bit; listener occurrences retain ascending old-row order and occur exactly once.</acceptance-criteria>
  <done>The focused zombie-authority test exits zero and proves flag-to-state/state-to-flag synchronization, next-step compaction, ZOMBIE-bearing snapshots, and exactly-once ordered listener occurrences.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Public flags and step configuration into lifecycle authority | Crafted flags or repeated/zero-time calls could destroy state twice or advance simulation unexpectedly. | Mitigate with one checked state transition, one shared time guard, transactional candidates, and regressions. |
| JSONL/process oracle | This runtime-only plan does not decode JSONL or invoke a process oracle. | No new boundary; later evidence plans exercise these behaviors through the existing bounded decoder. |
| Artifact/workflow integrity | This plan produces no compatibility artifact or workflow claim. | No promotion is permitted from local runtime tests. |
| Resource exhaustion | The zombie scan is bounded by the already validated particle count and performs no unbounded allocation or recursion. | Existing Phase 9 limits remain authoritative. |

ASVS L1 review: no authentication, secrets, network, filesystem, or unsafe-memory surface changes. No new high-severity threat is introduced.
</threat-model>

<verification>
Run focused RED/GREEN tests. Before every implementation commit run exactly in this order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Review the diff against the pinned step guard and confirm no Phase 10 solver/group behavior was added.
</verification>

<success-criteria>
- G09-STEP-GUARD and WR-01 are closed by public zero-dt and continuation regressions.
- G09-ZOMBIE-AUTHORITY and WR-02 are closed in both flag-to-state and state-to-flag directions.
- Existing positive-dt, pause, rollback, panic-poisoning, rigid, and particle-contact behavior remains green.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-18-SUMMARY.md`.</output>
