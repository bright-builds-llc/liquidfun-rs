---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 05
type: execute
wave: 5
depends_on: [09-03, 09-04]
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/particle.rs
  - crates/liquidfun/src/particle/view.rs
  - crates/liquidfun/src/particle/editor.rs
  - crates/liquidfun/tests/particle_views.rs
autonomous: true
requirements: [API-09, PART-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "Consumers can inspect all Phase 9 semantic lanes through one borrow-scoped aggregate view without seeing dense rows."
    - "Supported edits cannot escape their borrow or return with stale spatial/contact-derived state."
  artifacts:
    - path: crates/liquidfun/src/particle/view.rs
      provides: "Borrow-scoped semantic ParticleSystemView"
    - path: crates/liquidfun/src/particle/editor.rs
      provides: "Narrow typed and closure-scoped mutation operations"
  key_links:
    - from: crates/liquidfun/src/lib.rs
      to: crates/liquidfun/src/particle.rs
      via: "public particle child-module re-exports"
      pattern: "pub use"
    - from: crates/liquidfun/src/particle/editor.rs
      to: crates/liquidfun/src/particle/storage.rs
      via: "validate-edit-repair transaction"
      pattern: "edit_position|invalidate|rebuild"
---

<objective>
Expose safe bulk inspection and narrow mutation without aliasing or stale derived state.

Purpose: Deliver API-09/PART-05 while keeping dense storage and structural mutation private.
Output: Aggregate views, typed editors, invalidation repair, integration and compile-fail evidence.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-03-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-04-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the aggregate semantic view</name>
  <files>crates/liquidfun/src/particle/view.rs, crates/liquidfun/tests/particle_views.rs</files>
  <read_first>Phase 9 D-04; authoritative storage public boundary; PART-05 list of observable lanes.</read_first>
  <action>Declare the new particle child module from particle.rs, re-export supported public types through particle.rs and lib.rs, and prove external-crate reachability. Implement a documented borrow-scoped ParticleSystemView exposing positions, velocities, colors, weights, flags, stable group identities, safe user associations, particle contacts, body contacts, pairs, triads, and expiration order as semantic slices/wrappers. Translate internal row references to stable IDs. Keep dense indices, raw capacities, scratch arrays, mutable slices, and reusable internal contacts inaccessible. Test optional-lane absence/presence and stable semantics across completed permutations.</action>
  <verify><automated>cargo test -p liquidfun --test particle_views aggregate_view</automated></verify>
  <acceptance_criteria>Every PART-05 property is inspectable in one coherent borrow and no private row/capacity representation leaks.</acceptance_criteria>
  <done>Every PART-05 property is inspectable in one coherent borrow and no private row/capacity representation leaks.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add scoped mutation with derived-state repair</name>
  <files>crates/liquidfun/src/particle/editor.rs, crates/liquidfun/tests/particle_views.rs</files>
  <read_first>Phase 9 D-05; proxy/contact invalidation inventory; storage transaction rules.</read_first>
  <action>Add stable-ID typed operations and closure-scoped editors for supported Phase 9 mutations. Validate all candidates before editing. Position changes must invalidate and synchronously rebuild or clear/rederive every inventoried proxy/contact spatial record before return; flags, groups, lifetimes, identities, and derived records must not be exposed as raw mutable slices. Add compile-fail doctests showing views block structural mutation and editors/slices cannot escape.</action>
  <verify><automated>cargo test -p liquidfun --test particle_views && cargo test -p liquidfun --doc</automated></verify>
  <acceptance_criteria>Successful edits return coherent derived state; rejected edits and panicking editor closures leave coherent documented state.</acceptance_criteria>
  <done>Successful edits return coherent derived state; rejected edits and panicking editor closures leave coherent documented state.</done>
</task>
</tasks>

<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Public view/editor to private SoA | Borrowing or mutation could expose internal rows or stale derived records. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-05-01 | I | view.rs | mitigate | Expose stable semantic wrappers only; compile-fail private/alias access. |
| T-09-05-02 | T | editor.rs | mitigate | Validate first and repair all inventoried derived state before returning. |
</threat-model>

<verification>
Run `cargo check -p liquidfun --all-features` to verify entrypoint wiring.
Run view/editor tests and doctests, then before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Diff-review public rustdoc, privacy, and derived-state repair coverage.
</verification>
<success-criteria>
- Bulk inspection covers all required semantic properties.
- Rust borrowing prevents structural mutation while views are live.
- Narrow edits cannot leave proxies or contacts stale.
</success-criteria>
<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-05-SUMMARY.md`.</output>
