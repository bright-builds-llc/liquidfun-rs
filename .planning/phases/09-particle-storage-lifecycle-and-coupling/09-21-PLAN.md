---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 21
type: execute
wave: 2
depends_on: [09-20]
gap_closure: true
files_modified:
  - crates/liquidfun-differential/src/rigid_world.rs
  - crates/liquidfun-differential/src/rigid_world/phase9.rs
  - crates/liquidfun-differential/tests/particle_protocol.rs
  - crates/liquidfun-differential/tests/particle_oracle.rs
autonomous: true
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T23:45:08Z
must_haves:
  truths:
    - "The identical validated request executes through NativeRigidWorldExecutor and the pinned process oracle."
    - "Every phase9-v1 policy path is consumed and no undeclared or wildcard path can pass."
    - "Semantic mismatch has a stable first-divergence signature distinct from harness failure."
  artifacts:
    - path: crates/liquidfun-differential/src/rigid_world/phase9.rs
      provides: "Typed Phase 9 semantic comparator and complete policy-consumption ledger"
    - path: crates/liquidfun-differential/tests/particle_oracle.rs
      provides: "Native-versus-C++ pass, mismatch mutation, and harness-failure regressions"
  key_links:
    - from: crates/liquidfun-differential/src/rigid_world.rs
      to: crates/liquidfun-differential/src/rigid_world/phase9.rs
      via: "validated result pair passed to the phase9-v1 comparator"
      pattern: "compare.*phase9|PHASE9_REQUIRED_POLICY_PATHS"
    - from: crates/liquidfun-differential/src/rigid_world/phase9.rs
      to: crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs
      via: "exhaustive observation-field traversal"
      pattern: "Phase9ParticleObservation"
---

<objective>
Implement the missing native-Rust-versus-pinned-C++ Phase 9 semantic comparison.

Purpose: Convert separate engine replays into real differential evidence with complete policy consumption.
Output: Typed comparator, one-request/two-engine runner, and stable mismatch diagnostics.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-REVIEW.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-20-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compare every Phase 9 semantic field under one named policy</name>
  <files>crates/liquidfun-differential/src/rigid_world.rs, crates/liquidfun-differential/src/rigid_world/phase9.rs, crates/liquidfun-differential/tests/particle_protocol.rs</files>
  <read-first>existing `compare_rigid_world_results` and mismatch types; crates/liquidfun-differential/src/rigid_world/phase9.rs policy registry; every Phase9ParticleObservation field; Phase 4 numeric comparison helpers; 09-REVIEW CR-01; 09-VERIFICATION G09-DIFFERENTIAL-COMPARISON.</read-first>
  <action>Add an exhaustive typed walker over corresponding Phase 9 checkpoint observations. Compare identity/order/multiplicity/branches/counts exactly, exact-bit fields by bits, ULP fields with the reviewed finite ULP helper, and absolute-relative/dimensioned paths with explicit per-path constants. Insert each selected path into a closed consumed set; fail as a harness error on missing, duplicate, unknown, wildcard, non-finite, or structurally unmatched fields. Return the first `(timeline, checkpoint, observation, semantic path, mismatch kind)` in source order.</action>
  <verify><automated>cargo test -p liquidfun-differential --test particle_protocol comparator</automated></verify>
  <acceptance-criteria>One-field mutations for every policy class yield the expected stable first path; equal results consume all `PHASE9_REQUIRED_POLICY_PATHS`; deleting a policy or adding an unknown observation fails closed.</acceptance-criteria>
  <done>The comparator test exits zero and proves stable first-path mismatches for every policy class, complete required-path consumption for equal results, and fail-closed missing/unknown policy handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the exact same request through native Rust and pinned C++</name>
  <files>crates/liquidfun-differential/src/rigid_world.rs, crates/liquidfun-differential/tests/particle_oracle.rs</files>
  <read-first>NativeRigidWorldExecutor; execute_rigid_world_process; corrected Phase 9 request/result validation; OracleExecutable preset resolution; existing physics-mismatch versus harness-failure tests.</read-first>
  <action>Add a runner that serializes and hashes one validated request once, executes that exact request through native Rust and the selected closed oracle preset, validates both result records, calls the Phase 9 comparator, and returns request digest plus consumed paths. Prove a mixed request passes debug comparison, a deterministic one-field native/oracle mutation fails as physics mismatch, malformed child output remains a harness failure, and two replays preserve first-divergence identity.</action>
  <verify><automated>cargo xtask upstream configure --preset oracle-debug && cargo xtask upstream build --preset oracle-debug && cargo test -p liquidfun-differential --test particle_oracle differential</automated></verify>
  <acceptance-criteria>Logs prove both roles consumed the same request digest; arbitrary semantic disagreement cannot pass; harness errors never become physics mismatches.</acceptance-criteria>
  <done>The debug oracle build and differential test exit zero, and logs prove native/C++ digest equality, semantic disagreement rejection, and distinct harness-failure classification.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Validated request to two engines | Substitution could compare different scenarios. | Serialize/hash once and pass identical bytes to both roles. |
| Process result to comparator | Malformed/oversized output could bypass comparison. | Bounded decode and action-specific result validation first. |
| Policy registry | Wildcards or unconsumed fields could bless differences. | Closed exact paths, exhaustive walker, complete consumed set. |
| Artifact/workflow integrity | Local comparison is not D1 authority. | New exact-ref evidence remains mandatory in 09-23. |

All collections remain protocol-bounded. ASVS L1 review finds no secrets, network mutation, unsafe Rust, or unmitigated high-severity threat.
</threat-model>

<verification>
Run comparator/oracle tests and provenance. Before every implementation commit run exactly in this order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Build debug/release/sanitizer oracle presets and keep the pinned submodule read-only.
</verification>

<success-criteria>
- CR-01 and G09-DIFFERENTIAL-COMPARISON are closed by an executed Rust-versus-C++ comparison.
- Every declared semantic policy is consumed; unknown or missing paths fail closed.
- Mismatch diagnostics are stable and distinct from harness failures.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-21-SUMMARY.md`.</output>
