---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 04
type: execute
wave: 4
depends_on: [09-02, 09-03]
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/particle.rs
  - crates/liquidfun/src/particle/buffer.rs
  - crates/liquidfun/tests/particle_buffers.rs
autonomous: true
requirements: [API-10, PART-06]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "Consumers can transfer a complete owned lane bundle into fixed or growable storage and receive it back on teardown."
    - "Fixed capacity is explicit, independent of Vec capacity, and never silently reallocates."
  artifacts:
    - path: crates/liquidfun/src/particle/buffer.rs
      provides: "Validated owned lane bundle, fixed/growable mode, capacity errors, and teardown receipt"
  key_links:
    - from: crates/liquidfun/src/lib.rs
      to: crates/liquidfun/src/particle.rs
      via: "public particle child-module re-exports"
      pattern: "pub use"
    - from: crates/liquidfun/src/particle/buffer.rs
      to: crates/liquidfun/src/particle/storage.rs
      via: "validated adoption and owned teardown"
      pattern: "Fixed|Growable|into_lanes"
---

<objective>
Implement the safe external-buffer equivalent as owned unified lanes with explicit fixed/growable contracts.

Purpose: Reproduce upstream capacity effects without pointers, arbitrary borrowed lifetimes, aliasing, or allocator-capacity leakage.
Output: Buffer adoption/return APIs and transactional capacity behavior.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-02-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate and adopt complete owned lane bundles</name>
  <files>crates/liquidfun/src/particle/buffer.rs</files>
  <read_first>Phase 9 D-06 through D-08; 09-01 lane inventory; 09-RESEARCH ReallocateInternalAllocatedBuffers behavior.</read_first>
  <action>Declare the new particle child module from particle.rs, re-export supported public types through particle.rs and lib.rs, and prove external-crate reachability. Write failing tests, then implement owned required/optional lane bundles with complete length/invariant validation, explicit declared capacity, and fixed-owned versus growable-owned modes. Derive the effective limit from the declared contract rather than Vec::capacity. Make adoption all-or-nothing and return the original owned lanes plus semantic contents on teardown. Do not add allocator traits, borrowed raw buffers, unsafe aliases, or GPU/pointer interoperability.</action>
  <verify><automated>cargo test -p liquidfun --test particle_buffers adoption</automated></verify>
  <acceptance_criteria>Only complete consistent bundles are adopted, and ownership has one unambiguous holder throughout the lifecycle.</acceptance_criteria>
  <done>Only complete consistent bundles are adopted, and ownership has one unambiguous holder throughout the lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce fixed/growable/full capacity transactionality</name>
  <files>crates/liquidfun/tests/particle_buffers.rs</files>
  <read_first>Phase 9 D-07, D-08, D-12; public construction errors and storage creation transaction.</read_first>
  <action>Add tests for unequal underlying Vec capacities, optional lanes absent/present, exact fixed fullness, fixed failure with no identity/lane mutation, growable expansion, maximum-count interaction, teardown after permutations, and repeated adopt/use/return cycles. Include compile-fail rustdoc coverage proving lane aliases cannot coexist after ownership transfer.</action>
  <verify><automated>cargo test -p liquidfun --test particle_buffers</automated></verify>
  <acceptance_criteria>Fixed storage never grows, growable storage follows declared maximum behavior, and failures preserve the complete pre-call state.</acceptance_criteria>
  <done>Fixed storage never grows, growable storage follows declared maximum behavior, and failures preserve the complete pre-call state.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Consumer-owned lane bundle to storage | Inconsistent lengths/capacities could cause aliasing or partial rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-04-01 | T | bundle adoption | mitigate | Validate every required and allocated optional lane before ownership commit. |
| T-09-04-02 | D | fixed capacity | mitigate | Use explicit declared limits and no-effect typed full errors. |
</threat-model>

<verification>
Run `cargo check -p liquidfun --all-features` to verify entrypoint wiring.
Run buffer tests and doctests, then before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Review the diff for unsafe code, allocator traits, and Vec-capacity-dependent public behavior.
</verification>

<success-criteria>
- API-10 uses owned safe transfer only.
- Fixed and growable capacity semantics are explicit and independently tested.
- Teardown returns complete supplied lanes without dangling aliases.
</success-criteria>

<output>
After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-04-SUMMARY.md`.
</output>
