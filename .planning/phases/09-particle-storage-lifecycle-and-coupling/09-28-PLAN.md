---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 28
type: execute
wave: 4
depends_on: [09-27]
gap_closure: true
files_modified:
  - crates/liquidfun-differential/tests/phase9_corpus.rs
autonomous: false
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-17T17:19:43Z
must_haves:
  truths:
    - "The retained process-oracle fixture is hermetic and passes without any workspace oracle-debug build output."
    - "A human approves a new immutable full SHA containing the comparator, semantic-witness, and hermetic-fixture fixes before dispatch."
    - "Exactly one later workflow_dispatch run for that new approved SHA owns one successful canonical job and one successful sanitizer job."
    - "Fresh artifacts bind passing logs, 7 cases, 58 semantic branch bindings, 22 policies per case, and recomputed request/native/oracle/comparison/binding hashes."
    - "Runs 29439515367, 29583793056, and failed run 29625083184 cannot authorize the new compatibility promotion."
  artifacts:
    - path: target/phase9-evidence/run.json
      provides: "New approved SHA, dispatch boundary, run/jobs/artifacts, URLs, and API digests"
    - path: target/phase9-evidence/phase9-canonical/identity.json
      provides: "Fresh canonical identity for the new approved SHA"
    - path: target/phase9-evidence/phase9-sanitizer/identity.json
      provides: "Fresh sanitizer identity for the new approved SHA"
  key_links:
    - from: .planning/phases/09-particle-storage-lifecycle-and-coupling/09-27-SUMMARY.md
      to: .github/workflows/oracle.yml
      via: "clean published commit inspected before dispatch"
      pattern: "phase9"
    - from: target/phase9-evidence/run.json
      to: target/phase9-evidence/phase9-canonical/identity.json
      via: "exact run, head, job, artifact, and API digest equality"
      pattern: "head_sha|run_id"
---

<objective>
Repair the sanitizer-only fixture failure, then publish, approve, dispatch, and independently validate new exact-ref authority after both remaining Phase 9 gaps are fixed.

Purpose: Remove the fake runner's ambient oracle-debug dependency and establish fresh canonical and sanitizer evidence; prior and failed runs remain historical and cannot promote the changed implementation.
Output: One approved immutable SHA, one successful workflow run, and two locally revalidated artifacts. No compatibility promotion.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-25-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-27-SUMMARY.md
@.github/workflows/oracle.yml
@TESTING.md
@scripts/phase9-evidence.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair the hermetic process fixture and publish one clean recovery target</name>
  <files>crates/liquidfun-differential/tests/phase9_corpus.rs</files>
  <read-first>09-25, 09-26, and 09-27 summaries; failed run 29625083184 and sanitizer job 88027761125; current branch/upstream/status; fake oracle handshake and checkout-identity validation; workflow, evidence validator, and semantic fixture at HEAD; prior approved and superseded run metadata; TESTING exact-ref procedure.</read-first>
  <action>Replace the retained process fixture's copy from the ambient `target/reference/oracle-debug/compile_commands.json` with a deterministic compile database written inside `FakePhase9OracleRoot`. Include exactly `collision_probe.cpp`, `math_probe.cpp`, `protocol_bits.cpp`, and `rigid_world.cpp`, all rooted in the fake repository and using one coherent `clang++ -O0 -g` debug command shape. Keep the fake executable and handshake on `oracle-debug`; do not change production comparator code, CMake presets, workflow jobs, evidence schemas, or public APIs. Add an Arrange/Act/Assert regression proving the fake database has exactly those four unique units and a valid effective-command digest. Prove the retained process runner passes while the real workspace debug compile database is unavailable, then restore the build artifact. Require a clean non-detached branch and intended GitHub origin. Run the exact mandatory Rust sequence in order, focused Phase 9 corpus/protocol tests, oracle-debug and oracle-release builds plus complete canonical corpus, fail-fast oracle-asan-ubsan protocol plus complete sanitizer corpus, and the reusable Phase 9 evidence validator in local mode over both outputs with runs 29439515367, 29583793056, and 29625083184 denylisted. Then run provenance, inventory, cargo deny, actionlint, Markdown, schema-drift blocking:false, upstream read-only, and diff gates. Record `REPO`, `BRANCH`, and a new 40-character `HEAD_SHA`; push normally without rewriting history; require remote branch SHA equals local HEAD. Prove HEAD contains 09-25, 09-26, 09-27, and the hermetic-fixture commit plus the complete workflow/runner/validator. Print the immutable commit URL and full SHA. Do not dispatch, do not reuse any of the three denylisted runs, and never pair canonical artifact 8423580554 from failed run 29625083184 with another run's sanitizer output.</action>
  <verify><automated>cargo test -p liquidfun-differential --test phase9_corpus phase9_comparator_rejects_retained_process_result_through_runner -- --exact --nocapture && cargo test -p liquidfun-differential --test phase9_corpus fake_phase9_oracle_root_writes_closed_compile_database -- --exact --nocapture && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && cargo xtask upstream configure --preset oracle-debug && cargo xtask upstream build --preset oracle-debug && cargo xtask upstream configure --preset oracle-release && cargo xtask upstream build --preset oracle-release && LIQUIDFUN_PHASE9_ORACLE_MODE=canonical LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/canonical/phase9-manifest.json bash scripts/phase9-evidence.sh canonical target/phase9-evidence-local/canonical && cargo xtask upstream configure --preset oracle-asan-ubsan && cargo xtask upstream build --preset oracle-asan-ubsan && cmake --build target/reference/oracle-asan-ubsan --target liquidfun-reference-protocol-tests && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 ctest --test-dir target/reference/oracle-asan-ubsan --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$' && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 LIQUIDFUN_PHASE9_ORACLE_MODE=sanitizer LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/sanitizer/phase9-manifest.json bash scripts/phase9-evidence.sh sanitizer target/phase9-evidence-local/sanitizer && cargo xtask phase9-evidence validate --mode local --canonical-dir target/phase9-evidence-local/canonical --sanitizer-dir target/phase9-evidence-local/sanitizer --deny-run-id 29439515367 --deny-run-id 29583793056 --deny-run-id 29625083184 && cargo xtask provenance check && cargo xtask inventory check && cargo deny check && actionlint .github/workflows/oracle.yml && just markdown-check && node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 09 | jq -e '.blocking==false' && git diff --exit-code -- third_party/liquidfun && git diff --check && BRANCH=$(git branch --show-current) && HEAD_SHA=$(git rev-parse HEAD) && test "$HEAD_SHA" != 7ed430c497efbaa8585ee9ef3862be1abda29ef5 && test -z "$(git status --porcelain)" && test -n "$BRANCH" && test "$(git ls-remote --heads origin "refs/heads/$BRANCH" | awk 'NR==1 {print $1}')" = "$HEAD_SHA"</automated></verify>
  <acceptance-criteria>The retained process runner passes without ambient oracle-debug output; every local gate passes; and one new clean immutable remote SHA contains the retained-rigid comparator, all 58 semantic witnesses, the hermetic fixture, and the fail-closed evidence workflow. No workflow run has been dispatched for the new SHA.</acceptance-criteria>
  <done>The exact recovery publication verification exits zero and prints the sole new full SHA eligible for human approval.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve the exact published semantic-evidence SHA</name>
  <files></files>
  <read-first>The immutable commit URL/full SHA from Task 1 and its Phase 9-only diff.</read-first>
  <action>Pause. Require the user to inspect the exact new recovery commit and respond `approved &lt;full-commit-sha&gt;`. Copy the SHA verbatim. A short SHA, mismatch, dirty worktree, moved remote head, approval of any earlier SHA, or approval of `7ed430c497efbaa8585ee9ef3862be1abda29ef5` blocks dispatch.</action>
  <what-built>Task 1 published one clean remote commit containing the retained rigid comparator, semantic 58-branch corpus, and hermetic process-oracle fixture, but no workflow was dispatched for the new SHA.</what-built>
  <how-to-verify>Open the immutable commit URL, inspect the Phase 9 comparator/corpus/evidence diff, and confirm the displayed 40-character SHA exactly equals clean local HEAD and remote branch HEAD.</how-to-verify>
  <resume-signal>approved &lt;full-commit-sha&gt;</resume-signal>
  <verify><automated>BRANCH=$(git branch --show-current) && HEAD_SHA=$(git rev-parse HEAD) && test -z "$(git status --porcelain)" && test "$(git ls-remote --heads origin "refs/heads/$BRANCH" | awk 'NR==1 {print $1}')" = "$HEAD_SHA"</automated></verify>
  <acceptance-criteria>The user explicitly approves the exact new 40-character SHA; it differs from the SHAs associated with runs 29439515367, 29583793056, and 29625083184.</acceptance-criteria>
  <done>The verbatim approval matches the clean local and remote full SHA and authorizes exactly one later dispatch.</done>
</task>

<task type="auto">
  <name>Task 3: Dispatch exactly once and validate fresh canonical and sanitizer artifacts</name>
  <files></files>
  <read-first>The verbatim approved recovery SHA; failed run 29625083184 and its canonical-only artifact; dispatch timestamp boundary; workflow job names; artifact naming contract; semantic manifest schema and fixed download directories.</read-first>
  <action>Immediately before dispatch require the new approved SHA equals clean local and remote branch heads and is not the head associated with any denylisted run. Record UTC dispatch time, dispatch `oracle.yml` exactly once with `evidence_phase=phase9`, and select exactly one later workflow_dispatch run matching workflow, branch, and full SHA. Never rerun or redispatch SHA `7ed430c497efbaa8585ee9ef3862be1abda29ef5`. Watch with `--exit-status`; any failure blocks without redispatch. Require exactly one successful `Canonical Linux oracle` and one successful `Scheduled fail-fast sanitizer and reset corpus` job. Require exact unexpired artifact names `phase9-{canonical,sanitizer}-${RUN_ID}-${HEAD_SHA}` from that same run and record live artifact IDs, URLs, sizes, expiry, and API SHA-256 digests in `target/phase9-evidence/run.json`. Before extraction validate safe relative archive entries, no symlinks, and the exact bounded file set. Preserve old evidence under a gitignored superseded directory, delete only the two fixed new download directories, and download exact names. Run `cargo xtask phase9-evidence validate --mode exact-ref` with run.json, the two fixed directories, and runs 29439515367, 29583793056, and 29625083184 denylisted. The reusable validator must re-query or cross-check recorded GitHub metadata, identities, logs, all hashes, seven cases, 58 exact semantic bindings, complete identical 22-policy arrays, retained Phase 8 rigid proof, and canonical/sanitizer agreement. No compatibility or ledger file may change.</action>
  <verify><automated>RUN_ID=$(jq -r .run_id target/phase9-evidence/run.json) && HEAD_SHA=$(jq -r .head_sha target/phase9-evidence/run.json) && test "$RUN_ID" != 29439515367 && test "$RUN_ID" != 29583793056 && test "$RUN_ID" != 29625083184 && test "$HEAD_SHA" != 7ed430c497efbaa8585ee9ef3862be1abda29ef5 && gh run view "$RUN_ID" --json conclusion,headSha,event,jobs,workflowName | jq -e --arg sha "$HEAD_SHA" '.conclusion=="success" and .headSha==$sha and .event=="workflow_dispatch" and .workflowName=="Oracle CI" and ([.jobs[]|select(.name=="Canonical Linux oracle" and .conclusion=="success")]|length==1) and ([.jobs[]|select(.name=="Scheduled fail-fast sanitizer and reset corpus" and .conclusion=="success")]|length==1)' && cargo xtask phase9-evidence validate --mode exact-ref --run-json target/phase9-evidence/run.json --canonical-dir target/phase9-evidence/phase9-canonical --sanitizer-dir target/phase9-evidence/phase9-sanitizer --deny-run-id 29439515367 --deny-run-id 29583793056 --deny-run-id 29625083184 && cargo xtask provenance check</automated></verify>
  <acceptance-criteria>Only the newly approved SHA owns exactly one passing post-approval run and one canonical/sanitizer artifact pair; fresh downloads prove complete semantic witness, policy, log, identity, and digest integrity; compatibility remains untouched.</acceptance-criteria>
  <done>The run/job/artifact validator exits zero and the new exact-ref authority is ready for independent promotion review.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Human-approved ref to dispatch | A moved branch can run unreviewed code. | Verbatim full SHA plus clean local/remote equality immediately before one dispatch. |
| GitHub metadata and artifacts | An old, failed, duplicate, expired, mixed-run, or substituted artifact can impersonate authority. | Post-approval time boundary, cardinality, exact names, live API digests, same-run pairing, and three denylisted runs. |
| Archive extraction | Malicious paths or symlinks can escape fixed evidence directories. | Safe-relative-path validation, no symlinks, exact bounded file set. |
| Logs and manifests | Successful job status can conceal failed or trivial evidence. | Passing-log checks, semantic binding digest, payload digests, and canonical/sanitizer equality. |

ASVS L1: any high-severity integrity, authentication, path, secret, shell-injection, or resource-bound finding blocks completion.
</threat-model>

<verification>
Task 1 runs the full local matrix before publication. Task 3 requires one exact post-approval run, unique successful jobs/artifacts, safe downloads, passing logs, 7 cases, 58 semantic bindings, complete policies, and recomputed hashes. This plan creates no implementation or compatibility commit.
</verification>

<success-criteria>
- One new human-approved immutable SHA owns the evidence.
- Runs 29439515367, 29583793056, and 29625083184 are rejected as new authority.
- Canonical and sanitizer artifacts both prove complete semantic comparison coverage.
- No compatibility promotion occurs in this plan.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-28-SUMMARY.md` with the rejected 29625083184 attempt, hermetic-fixture repair, new exact approval, successful replacement run, job, artifact, log, manifest, and hash evidence.</output>
