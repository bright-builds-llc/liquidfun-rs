---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 27
type: execute
wave: 3
depends_on: [09-26]
gap_closure: true
files_modified:
  - scripts/phase9-evidence.sh
  - crates/liquidfun-differential/tests/phase9_corpus.rs
  - tools/xtask/src/phase9_evidence.rs
  - tools/xtask/src/main.rs
  - tools/xtask/tests/phase9_evidence_cli.rs
autonomous: true
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-17T17:19:43Z
must_haves:
  truths:
    - "Each case records and digest-binds a successful retained Phase 8 rigid comparison before its Phase 9 particle comparison."
    - "The evidence validator proves 7 cases, exactly 58 unique semantic bindings, complete identical 22-policy arrays, all payload hashes, and canonical/sanitizer agreement."
    - "Local and exact-ref validation share one fail-closed implementation; exact-ref additionally binds immutable GitHub run, job, artifact, and approved-head metadata."
    - "Runs 29439515367 and 29583793056 are explicit negative authorities."
  artifacts:
    - path: scripts/phase9-evidence.sh
      provides: "Identity-last canonical and sanitizer evidence production"
    - path: tools/xtask/src/phase9_evidence.rs
      provides: "Reusable bounded local and exact-ref Phase 9 evidence validator"
    - path: tools/xtask/tests/phase9_evidence_cli.rs
      provides: "Corruption, substitution, prior-run, and archive-safety rejection tests"
  key_links:
    - from: crates/liquidfun-differential/tests/phase9_corpus.rs
      to: tools/xtask/src/phase9_evidence.rs
      via: "versioned manifest record and recomputed semantic binding digest"
      pattern: "retained_rigid|witness_binding_sha256"
    - from: scripts/phase9-evidence.sh
      to: tools/xtask/src/phase9_evidence.rs
      via: "identity is emitted only after the content validator succeeds"
      pattern: "identity.json"
---

<objective>
Make retained-rigid proof and semantic branch evidence portable, digest-bound, and reusable before any exact-ref publication.

Purpose: Prevent later publication or promotion plans from relying on ad hoc shell predicates or a particle-only manifest.
Output: Identity-last evidence generation, a versioned manifest contract, and one tested validator for local and exact-ref evidence.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-25-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-26-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
@scripts/phase9-evidence.sh
@crates/liquidfun-differential/tests/phase9_corpus.rs
@tools/xtask/src/main.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit retained-rigid and semantic witness records into every case</name>
  <files>crates/liquidfun-differential/tests/phase9_corpus.rs, scripts/phase9-evidence.sh</files>
  <read-first>The complete Phase 9 comparison outcome from 09-25; the 58 typed bindings from 09-26; current manifest structs and digest canonicalization; Phase 6/7/8 policy profiles and their checked-in SHA-256 values; identity emission order.</read-first>
  <action>First add Arrange/Act/Assert tests that fail when a case omits or mutates retained-rigid proof, any policy digest, semantic binding content, or payload digest. Extend each of the seven case records with `retained_rigid` containing comparator `phase8-v1`, the exact Phase 6, 7, and 8 policy SHA-256 values, outcome `match`, and a `comparison_sha256` computed from the retained rigid outcome. Record `witness_binding_sha256` over the canonical ordered action/checkpoint/observation/assertion bindings, plus request, native-result, oracle-result, and complete-comparison SHA-256 fields. Bind the complete per-case records into the canonical semantic manifest digest. Make the runner write payloads and manifest first, validate them, and write `identity.json` last with hashes of the already-closed files; any interruption or validation failure must leave no valid identity.</action>
  <verify><automated>cargo test -p liquidfun-differential --test phase9_corpus retained_rigid -- --nocapture && cargo test -p liquidfun-differential --test phase9_corpus witness_binding -- --nocapture</automated></verify>
  <acceptance-criteria>All seven manifest records contain the exact retained-rigid object, policy digests, match outcome, semantic binding digest, and payload digests; mutation or omission tests fail closed; identity is never emitted before successful content validation.</acceptance-criteria>
  <done>The manifest itself proves that the complete Phase 8 rigid comparator and the semantic Phase 9 comparator both matched for the same request.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement one bounded local and exact-ref evidence validator</name>
  <files>tools/xtask/src/phase9_evidence.rs, tools/xtask/src/main.rs, tools/xtask/tests/phase9_evidence_cli.rs</files>
  <read-first>The Task 1 manifest/identity schema; current xtask command routing and error style; TESTING exact-ref procedure; `gh run view` and artifact API JSON shapes; exact workflow job/artifact names; archive path and symlink risks; both historical run records.</read-first>
  <action>Add `cargo xtask phase9-evidence validate` with `--mode local|exact-ref`, canonical and sanitizer directories, repeatable `--deny-run-id`, and exact-ref-only `--run-json`. In both modes enforce an exact bounded regular-file set, no symlinks, schema/version limits, passing logs with no FAILED marker, seven distinct cases, exactly 58 unique semantic bindings, exact action/checkpoint/observation/assertion binding validity, positive finite collision energy, nonempty stable stuck candidates, complete identical 22-policy arrays per case, exact retained-rigid comparator/policy-hash/match records, recomputed request/native/oracle/retained-rigid/complete-comparison/witness-binding/manifest/trace/identity hashes, and canonical/sanitizer semantic-manifest equality. In exact-ref mode additionally reject absent or denylisted run IDs, require the approved 40-character head, workflow_dispatch/Oracle CI identity, exactly one successful named canonical job and sanitizer job, exact artifact names, unique artifact IDs, unexpired metadata, recorded API SHA-256 digests, archive-entry validation before extraction, and equality between run.json, live metadata snapshots, identities, and extracted contents. Use typed bounded deserialization, safe relative path checks, and argument-based process invocation; never interpolate untrusted paths into a shell.</action>
  <verify><automated>cargo test -p xtask --test phase9_evidence_cli</automated></verify>
  <acceptance-criteria>Arrange/Act/Assert CLI tests reject both old runs, duplicate or wrong jobs/artifacts, expired metadata, traversal, absolute paths, symlinks, extra/missing files, failed logs, identity substitution, canonical-as-sanitizer substitution, missing or malformed retained-rigid records, policy mismatch, 58 labels without exact bindings, zero energy, empty stuck witnesses, incomplete policies, digest corruption, and canonical/sanitizer semantic disagreement.</acceptance-criteria>
  <done>One fail-closed validator can be called unchanged by local prepublication, exact-ref download, and independent promotion workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Produce and validate fresh local canonical and sanitizer evidence</name>
  <files>scripts/phase9-evidence.sh, crates/liquidfun-differential/tests/phase9_corpus.rs, tools/xtask/src/phase9_evidence.rs, tools/xtask/src/main.rs, tools/xtask/tests/phase9_evidence_cli.rs</files>
  <read-first>The complete Task 1-2 diff; local evidence directory contract; repository exact Rust precommit order; oracle debug/release/sanitizer presets; pinned upstream read-only rule.</read-first>
  <action>Run the exact Rust sequence, focused Phase 9 tests, oracle-debug and oracle-release builds, the complete canonical corpus through the identity-last runner, the fail-fast oracle-asan-ubsan protocol and complete sanitizer corpus, and `phase9-evidence validate --mode local` over both directories with both historical runs denylisted. Run provenance, upstream read-only, and diff checks. Review for a simpler shared parser/hash path and remove duplicated ad hoc validation when the new module can own it. Commit the validator/evidence contract atomically only after all gates pass; do not publish, dispatch, or edit compatibility authority.</action>
  <verify><automated>cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && cargo xtask upstream configure --preset oracle-debug && cargo xtask upstream build --preset oracle-debug && cargo xtask upstream configure --preset oracle-release && cargo xtask upstream build --preset oracle-release && LIQUIDFUN_PHASE9_ORACLE_MODE=canonical LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/canonical/phase9-manifest.json bash scripts/phase9-evidence.sh canonical target/phase9-evidence-local/canonical && cargo xtask upstream configure --preset oracle-asan-ubsan && cargo xtask upstream build --preset oracle-asan-ubsan && cmake --build target/reference/oracle-asan-ubsan --target liquidfun-reference-protocol-tests && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 ctest --test-dir target/reference/oracle-asan-ubsan --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$' && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 LIQUIDFUN_PHASE9_ORACLE_MODE=sanitizer LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/sanitizer/phase9-manifest.json bash scripts/phase9-evidence.sh sanitizer target/phase9-evidence-local/sanitizer && cargo xtask phase9-evidence validate --mode local --canonical-dir target/phase9-evidence-local/canonical --sanitizer-dir target/phase9-evidence-local/sanitizer --deny-run-id 29439515367 --deny-run-id 29583793056 && cargo xtask provenance check && git diff --exit-code -- third_party/liquidfun && git diff --check</automated></verify>
  <acceptance-criteria>All mandatory Rust, canonical, debug/release, fail-fast sanitizer, validator-negative, provenance, read-only, and diff gates pass; local outputs contain valid retained-rigid and 58-binding proof; no remote or ledger state changes.</acceptance-criteria>
  <done>The reusable evidence contract is committed and locally proven before exact-ref publication.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Comparator outcome to evidence manifest | A particle-only result can impersonate complete Phase 9 comparison. | Exact retained-rigid record, policy hashes, match outcome, and digest binding per case. |
| Manifest labels to semantic execution | Fifty-eight names can exist without the named behavior. | Typed action/checkpoint/observation/assertion bindings, positive/nonempty predicates, and corruption tests. |
| Downloaded archives to local filesystem | Traversal, symlinks, or extra files can escape or substitute evidence. | Pre-extraction safe-entry checks, exact bounded regular-file set, no symlinks. |
| GitHub metadata to exact-ref authority | A stale or duplicate run/job/artifact can impersonate approval. | Full-SHA/run/job/artifact cardinality, live snapshot equality, expiry/digest checks, denylisted historical runs. |
| Shell and process boundary | Untrusted values can alter commands or expose secrets. | Typed arguments, fixed commands, no shell interpolation, no secret output, bounded diagnostics. |

ASVS L1: unsafe extraction, unbounded parsing, shell injection, secret leakage, digest substitution, or any high-severity evidence-integrity finding blocks completion.
</threat-model>

<verification>
Require focused mutation tests, the exact Rust precommit sequence, canonical/debug/release/sanitizer corpus execution, local validation of both outputs, provenance, upstream read-only, and diff checks.
</verification>

<success-criteria>
- Every case carries digest-bound retained-rigid and semantic particle proof.
- The validator rejects stale, trivial, incomplete, unsafe, substituted, or corrupted evidence.
- Publication and promotion can reuse the same exact validator contract.
- No compatibility authority changes in this plan.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-27-SUMMARY.md`.</output>
