---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 17
type: execute
wave: 15
depends_on: [09-16]
files_modified:
  - reference/compatibility.json
  - tools/xtask/src/inventory.rs
  - tools/xtask/src/inventory/report.rs
  - tools/xtask/tests/inventory_cli.rs
  - COMPATIBILITY.md
  - TESTING.md
autonomous: true
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "Evidence is independently re-downloaded and revalidated before any Phase 9 D1 promotion."
    - "reference/compatibility.json is the only edited compatibility source and COMPATIBILITY.md is regenerated deterministically."
    - "Promotion occurs only when canonical and sanitizer identities both match the approved exact commit."
  artifacts:
    - path: target/phase9-evidence/run.json
      provides: "Plan 09-16 exact run and artifact metadata revalidated before promotion"
    - path: reference/compatibility.json
      provides: "Authoritative Phase 9 evidence references and statuses"
    - path: COMPATIBILITY.md
      provides: "Generated truthful Phase 9 compatibility report"
  key_links:
    - from: .planning/phases/09-particle-storage-lifecycle-and-coupling/09-16-SUMMARY.md
      to: reference/compatibility.json
      via: "independently revalidated exact-run artifact identities"
      pattern: "phase9-canonical|phase9-sanitizer"
    - from: reference/compatibility.json
      to: COMPATIBILITY.md
      via: "cargo xtask inventory generate/check"
      pattern: "@generated"
---
<objective>
Independently revalidate exact-ref evidence, then update the authoritative compatibility ledger and regenerate the report.

Purpose: Separate remote evidence collection from promotion so stale or substituted artifacts cannot alter compatibility status.
Output: Validated JSON evidence references, deterministic generated report, and updated testing authority documentation.
</objective>
<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-16-SUMMARY.md
@reference/compatibility.json
@COMPATIBILITY.md
@TESTING.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Independently revalidate canonical and sanitizer evidence</name>
  <files>TESTING.md</files>
  <read_first>09-16 approved SHA/run/artifact identities; D-31 authority contract; current TESTING evidence procedure.</read_first>
  <action>Load `REPO`, `BRANCH`, `HEAD_SHA`, `RUN_ID`, exact artifact names, URLs, and GitHub artifact digests from the Plan 09-16 `target/phase9-evidence/run.json`, and cross-check every value against 09-16-SUMMARY.md. Require `git merge-base --is-ancestor "$HEAD_SHA" HEAD`, require the exact remote branch still resolves to `HEAD_SHA`, and inspect `.github/workflows/oracle.yml` from that commit with `git show "$HEAD_SHA:.github/workflows/oracle.yml"` to prove `evidence_phase`, `phase9-canonical`, and `phase9-sanitizer` are present.

Delete only `target/phase9-evidence/phase9-canonical` and `target/phase9-evidence/phase9-sanitizer`, re-query the run and artifact APIs, and re-download both exact artifacts by RUN_ID/name. Independently require run conclusion `success`, event `workflow_dispatch`, workflow `Oracle CI`, exact head SHA, and exactly one successful canonical and sanitizer job. Require exactly one unexpired occurrence of each exact artifact name; require each current GitHub `sha256:` artifact digest to match both `run.json` and 09-16-SUMMARY.md. Re-run the complete `jq` identity contract and recompute each artifact's named trace and manifest hashes with `shasum -a 256 -c`, exactly as in 09-16; a policy-only check is insufficient. Then run `cargo xtask provenance check`. Only after all commands pass, record immutable run/artifact URLs and the verified procedure in TESTING.md. Any failed, missing, duplicate, expired, unsafe-path, or mismatched value leaves Phase 9 at D2 and blocks the remainder of this plan.</action>
  <verify><automated>REPO=$(jq -r .repository target/phase9-evidence/run.json) && BRANCH=$(jq -r .branch target/phase9-evidence/run.json) && HEAD_SHA=$(jq -r .head_sha target/phase9-evidence/run.json) && RUN_ID=$(jq -r .run_id target/phase9-evidence/run.json) && CANONICAL_NAME=$(jq -r .artifacts.canonical.name target/phase9-evidence/run.json) && CANONICAL_DIGEST=$(jq -r .artifacts.canonical.digest target/phase9-evidence/run.json) && SANITIZER_NAME=$(jq -r .artifacts.sanitizer.name target/phase9-evidence/run.json) && SANITIZER_DIGEST=$(jq -r .artifacts.sanitizer.digest target/phase9-evidence/run.json) && git merge-base --is-ancestor "$HEAD_SHA" HEAD && test "$(git ls-remote --heads origin "refs/heads/$BRANCH" | awk 'NR == 1 { print $1 }')" = "$HEAD_SHA" && git show "$HEAD_SHA:.github/workflows/oracle.yml" | grep -F evidence_phase && git show "$HEAD_SHA:.github/workflows/oracle.yml" | grep -F phase9-canonical && git show "$HEAD_SHA:.github/workflows/oracle.yml" | grep -F phase9-sanitizer && gh run view "$RUN_ID" -R "$REPO" --json conclusion,headSha,event,jobs,workflowName | jq -e --arg head_sha "$HEAD_SHA" '.conclusion == "success" and .headSha == $head_sha and .event == "workflow_dispatch" and .workflowName == "Oracle CI" and ([.jobs[] | select(.name == "Canonical Linux oracle" and .conclusion == "success")] | length == 1) and ([.jobs[] | select(.name == "Scheduled fail-fast sanitizer and reset corpus" and .conclusion == "success")] | length == 1)' && gh api "repos/$REPO/actions/runs/$RUN_ID/artifacts?per_page=100" | jq -e --arg canonical "$CANONICAL_NAME" --arg canonical_digest "$CANONICAL_DIGEST" --arg sanitizer "$SANITIZER_NAME" --arg sanitizer_digest "$SANITIZER_DIGEST" '([.artifacts[] | select(.name == $canonical and .expired == false and .digest == $canonical_digest and (.digest | test("^sha256:[0-9a-f]{64}$")))] | length == 1) and ([.artifacts[] | select(.name == $sanitizer and .expired == false and .digest == $sanitizer_digest and (.digest | test("^sha256:[0-9a-f]{64}$")))] | length == 1)' && for SPEC in "canonical:canonical-linux" "sanitizer:sanitizer-linux"; do KIND=${SPEC%%:*}; JOB=${SPEC#*:}; DIR="target/phase9-evidence/phase9-$KIND"; IDENTITY="$DIR/identity.json"; jq -e --argjson run_id "$RUN_ID" --arg head_sha "$HEAD_SHA" --arg job "$JOB" --arg upstream 7f20402173fd143a3988c921bc384459c6a858f2 '.run_id == $run_id and .head_sha == $head_sha and .job == $job and .upstream_revision == $upstream and .target == "x86_64-unknown-linux-gnu" and .rust == "1.97.0" and .clang == "22.1.8" and .cmake == "4.3.3" and .ninja == "1.13.2" and .policy_profile == "phase9-v1" and (.trace.path | test("^[A-Za-z0-9._/-]+$") and startswith("/") == false and contains("..") == false) and (.manifest.path | test("^[A-Za-z0-9._/-]+$") and startswith("/") == false and contains("..") == false) and (.trace.sha256 | test("^[0-9a-f]{64}$")) and (.manifest.sha256 | test("^[0-9a-f]{64}$"))' "$IDENTITY" && TRACE_CHECK=$(jq -r '.trace.sha256 + "  " + .trace.path' "$IDENTITY") && MANIFEST_CHECK=$(jq -r '.manifest.sha256 + "  " + .manifest.path' "$IDENTITY") && (cd "$DIR" && printf '%s\n%s\n' "$TRACE_CHECK" "$MANIFEST_CHECK" | shasum -a 256 -c -); done && cargo xtask provenance check</automated></verify>
  <acceptance_criteria>Freshly downloaded canonical and sanitizer evidence independently proves the exact approved commit and full D1 identity.</acceptance_criteria>
  <done>Freshly downloaded canonical and sanitizer evidence independently proves the exact approved commit and full D1 identity.</done>
</task>
<task type="auto">
  <name>Task 2: Promote authoritative JSON and regenerate compatibility report</name>
  <files>reference/compatibility.json, tools/xtask/src/inventory.rs, tools/xtask/src/inventory/report.rs, tools/xtask/tests/inventory_cli.rs, COMPATIBILITY.md, TESTING.md</files>
  <read_first>Validated evidence from Task 1; compatibility JSON schema; generated-report ownership; Phase 9 requirement rows.</read_first>
  <action>Only after Task 1 succeeds, update API-09/API-10/PART-01..08/PART-14..17 evidence in `reference/compatibility.json` with immutable run/artifact references and exact identity digests. Extend generator validation/report tests where necessary so incomplete or noncanonical Phase 9 promotion is rejected. Never edit COMPATIBILITY.md directly: run `cargo xtask inventory generate`, then `cargo xtask inventory check` and a second generate/check for idempotence. Preserve Phase 6-8 rows and make no Phase 10/full-parity claim.</action>
  <verify><automated>cargo test -p xtask --test inventory_cli && cargo xtask inventory generate && cargo xtask inventory check && cargo xtask inventory generate && cargo xtask inventory check && just markdown-check</automated></verify>
  <acceptance_criteria>Authoritative JSON cites validated exact-ref evidence, generated COMPATIBILITY.md is current/idempotent, and no promotion is possible on failed evidence.</acceptance_criteria>
  <done>Authoritative JSON cites validated exact-ref evidence, generated COMPATIBILITY.md is current/idempotent, and no promotion is possible on failed evidence.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Downloaded evidence to generated public claim | Stale/substituted evidence or direct generated-file edits could falsify D1 status. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-17-01 | S | evidence revalidation | mitigate | Fresh exact-name download and complete identity/digest checks before edits. |
| T-09-17-02 | T | compatibility report | mitigate | Edit JSON source only; validate schema and regenerate/check idempotently. |
</threat-model>
<verification>
Fresh evidence validation must precede all ledger edits. Before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then inventory generate/check idempotence and `just markdown-check`. Failures block completion and promotion.
</verification>
<success-criteria>
- Exact-ref canonical and sanitizer evidence is revalidated independently.
- JSON is authoritative and COMPATIBILITY.md is generated/current.
- Phase 9 promotes only on complete D1 success; all failure paths block completion.
</success-criteria>
<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-17-SUMMARY.md`.</output>
