---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 10
type: execute
wave: 9
depends_on: [09-09]
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/particle.rs
  - crates/liquidfun/src/particle/force.rs
  - crates/liquidfun/src/particle/statistics.rs
  - crates/liquidfun/tests/particle_forces_statistics.rs
autonomous: true
requirements: [PART-16]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "Checked per-particle and contiguous-range forces/impulses are transactional and source-scaled."
    - "Consumers can inspect collision energy, stuck candidates, counts, pause, and capacity statistics semantically."
  artifacts:
    - path: crates/liquidfun/src/particle/force.rs
      provides: "Stable-ID/range force and impulse preparation/application"
    - path: crates/liquidfun/src/particle/statistics.rs
      provides: "Owned/borrowed Phase 9 statistics"
  key_links:
    - from: crates/liquidfun/src/lib.rs
      to: crates/liquidfun/src/particle.rs
      via: "public particle child-module re-exports"
      pattern: "pub use"
    - from: crates/liquidfun/src/particle/force.rs
      to: crates/liquidfun/src/particle/storage.rs
      via: "validate-all then accumulate force/update velocity"
      pattern: "force|impulse"
---
<objective>
Expose transactional forces/impulses and complete Phase 9 semantic statistics.

Purpose: Deliver PART-16 without leaking scratch counters or partially mutating ranges.
Output: Checked force/impulse APIs, statistics views, and behavior tests.
</objective>
<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-09-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement checked particle/range forces and impulses</name>
  <files>crates/liquidfun/src/particle/force.rs, crates/liquidfun/tests/particle_forces_statistics.rs</files>
  <read_first>Phase 9 D-22; pinned ApplyForce/ApplyLinearImpulse range scaling; stable range/handle rules.</read_first>
  <action>Declare the new particle child module from particle.rs, re-export supported public types through particle.rs and lib.rs, and prove external-crate reachability. Validate every stable ID/range endpoint, system scope, liveness, finiteness, wall restriction, count, mass, and derived distribution scale before mutation. Accumulate force and update impulse velocity in source order. Test singleton/range equivalence, empty/invalid ranges, pending/stale/cross-system IDs, wall particles, non-finite values, and full no-effect failure.</action>
  <verify><automated>cargo test -p liquidfun --test particle_forces_statistics forces</automated></verify>
  <acceptance_criteria>All failure paths are no-effect and successful totals/distribution match source-derived expectations.</acceptance_criteria>
  <done>All failure paths are no-effect and successful totals/distribution match source-derived expectations.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Expose semantic Phase 9 statistics</name>
  <files>crates/liquidfun/src/particle/statistics.rs, crates/liquidfun/tests/particle_forces_statistics.rs</files>
  <read_first>Phase 9 D-24; contact/body-contact/stuck state from 09-08/09-09; public view conventions from 09-05.</read_first>
  <action>Expose collision energy, particle/system/contact/body-contact counts, stuck candidates as stable IDs, paused state, declared/effective capacity, configured maximum, and other named Phase 9 observables as owned or borrow-scoped semantic data. Keep timestamps, scratch buffers, raw capacities, and internal iteration counters private unless a pinned observable requires them. Test updates after mutation, contact refresh, pause, compaction, and teardown.</action>
  <verify><automated>cargo test -p liquidfun --test particle_forces_statistics statistics</automated></verify>
  <acceptance_criteria>PART-16 statistics remain coherent across lifecycle/contact changes and expose no private implementation state.</acceptance_criteria>
  <done>PART-16 statistics remain coherent across lifecycle/contact changes and expose no private implementation state.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Public vectors/ranges to hot particle state | Invalid values could poison all particles or partially mutate a range. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-10-01 | T | force.rs | mitigate | Validate complete range and derived scale before any write. |
| T-09-10-02 | I | statistics.rs | mitigate | Expose stable semantic snapshots only, not scratch/capacity internals. |
</threat-model>
<verification>
Run `cargo check -p liquidfun --all-features` to verify entrypoint wiring.
Run focused tests, then before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Review source-order arithmetic and public privacy.
</verification>
<success-criteria>
- Forces and impulses are checked, transactional, and source-scaled.
- All required statistics are stable-ID based and coherent.
- No internal counters or dense rows leak.
</success-criteria>
<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-10-SUMMARY.md`.</output>
