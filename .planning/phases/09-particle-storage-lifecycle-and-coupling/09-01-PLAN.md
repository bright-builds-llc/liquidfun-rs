---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/particle.rs
  - crates/liquidfun/src/particle/definition.rs
  - crates/liquidfun/src/particle/storage/lane_inventory.rs
  - crates/liquidfun/tests/particle_definitions.rs
autonomous: true
requirements: [PART-01, PART-02, PART-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "Particle-system and particle definitions reject invalid units and non-finite values before world mutation."
    - "The complete Phase 9 lane and remap inventory is executable and excludes Phase 10 generation behavior."
  artifacts:
    - path: crates/liquidfun/src/particle/definition.rs
      provides: "Checked particle flags, color, system definition, particle definition, and typed construction errors"
    - path: crates/liquidfun/src/particle/storage/lane_inventory.rs
      provides: "Single checked inventory of required, optional, derived, and remapped particle state"
  key_links:
    - from: crates/liquidfun/src/lib.rs
      to: crates/liquidfun/src/particle.rs
      via: "public particle export surface"
      pattern: "pub mod particle|pub use particle"
    - from: crates/liquidfun/src/particle.rs
      to: crates/liquidfun/src/particle/definition.rs
      via: "public re-exports"
      pattern: "pub use.*definition"
    - from: crates/liquidfun/src/particle/storage/lane_inventory.rs
      to: "pinned b2ParticleSystem allocation/rotation/zombie sites"
      via: "source-cited inventory tests"
      pattern: "LaneKind|RemapKind"
---

<objective>
Define the production Particle System and Particle contracts plus the exhaustive state-permutation inventory.

Purpose: Give every later storage, lifecycle, contact, and harness plan one checked vocabulary and prevent an optional or derived lane from disappearing during a reorder.
Output: Public checked definitions, exact flag/color representation, typed errors, and an executable lane/remap matrix.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the production definitions and validation boundary</name>
  <files>crates/liquidfun/src/lib.rs, crates/liquidfun/src/particle.rs, crates/liquidfun/src/particle/definition.rs, crates/liquidfun/tests/particle_definitions.rs</files>
  <read_first>Phase 9 D-01, D-02, D-07, D-08; the pinned b2Particle flag/default/unit declarations cited by 09-RESEARCH; current public ParticleSystemId and ParticleId types.</read_first>
  <action>Declare and re-export supported particle definitions through particle.rs and lib.rs, and prove reachability with an external-crate integration import. Write focused failing tests, then implement documented invariant-bearing ParticleSystemDef and ParticleDef constructors/builders, exact bitflags, color representation, pause/strict-contact/capacity/maximum/iteration controls, position/velocity/color/flags/lifetime/user-association inputs, and typed errors. Validate finite values, positive physical parameters, maximum/capacity relationships, and upstream defaults at the boundary. Preserve unknown bits only according to the pinned flag policy. Do not expose dense rows, raw pointers, allocator capacity, group construction, or solver controls belonging to Phase 10.</action>
  <verify><automated>cargo test -p liquidfun --test particle_definitions</automated></verify>
  <acceptance_criteria>Definitions cover PART-01/PART-02 inputs and reject every invalid candidate without mutating a World.</acceptance_criteria>
  <done>Definitions cover PART-01/PART-02 inputs and reject every invalid candidate without mutating a World.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode the complete Phase 9 lane and remap matrix</name>
  <files>crates/liquidfun/src/particle/storage/lane_inventory.rs</files>
  <read_first>Phase 9 D-03 and D-08; 09-RESEARCH allocation/lane inventory; pinned ReallocateInternalAllocatedBuffers, SolveZombie, and RotateBuffer statements.</read_first>
  <action>Create a closed internal inventory that classifies stable-ID mapping, required lanes, lazily allocated lanes, proxies, particle/body contacts, pairs, triads, expiration records, stuck state, force/weight state, and group ranges by allocation, clear, permutation, and reference-remap obligation. Add tests that fail when a production lane is unclassified. Record pairs/triads/group ranges only as permutation-safe references; do not generate topology or implement Phase 10 solvers.</action>
  <verify><automated>cargo test -p liquidfun lane_inventory</automated></verify>
  <acceptance_criteria>Every Phase 9 production lane and pre-existing deferred reference has exactly one checked permutation disposition.</acceptance_criteria>
  <done>Every Phase 9 production lane and pre-existing deferred reference has exactly one checked permutation disposition.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public definitions to particle domain | Untrusted floats, flags, counts, and capacities enter invariant-bearing types. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-01-01 | T | Particle definitions | mitigate | Parse all values before construction and test no-effect rejection. |
| T-09-01-02 | D | Lane inventory | mitigate | Closed inventory tests fail when state lacks a permutation disposition. |
</threat-model>

<verification>
Run `cargo check -p liquidfun --all-features` to verify crate-root wiring.
Run focused tests, then before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Review the diff for Phase 10 behavior and public dense-index leakage.
</verification>

<success-criteria>
- Checked public definitions express the entire Phase 9 creation/configuration input surface.
- The inventory names every lane/remap obligation and makes omissions test failures.
- No group topology, pair/triad generation, or particle solver behavior is added.
</success-criteria>

<output>
After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-01-SUMMARY.md`.
</output>
