---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 24
type: execute
wave: 5
depends_on: [09-23]
gap_closure: true
files_modified:
  - reference/compatibility.json
  - tools/xtask/src/inventory/validation.rs
  - tools/xtask/tests/inventory_cli.rs
  - COMPATIBILITY.md
  - TESTING.md
autonomous: true
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T23:45:08Z
must_haves:
  truths:
    - "Freshly downloaded artifacts independently reprove the approved SHA, passing logs, executable branches, complete policies, and all payload hashes before ledger mutation."
    - "Only supported Phase 9 rows cite the new authority; superseded run 29439515367 cannot authorize promotion."
    - "Generated compatibility output, Rust gates, oracle/sanitizer evidence, provenance, schema drift, Markdown, and ASVS L1 security gates all pass."
  artifacts:
    - path: reference/compatibility.json
      provides: "Authoritative scoped Phase 9 rows bound to the new approved run/artifacts"
    - path: COMPATIBILITY.md
      provides: "Deterministically regenerated truthful report"
    - path: tools/xtask/tests/inventory_cli.rs
      provides: "Rejection of failed logs, incomplete manifests, substituted/superseded authority, and Phase 10 promotion"
  key_links:
    - from: .planning/phases/09-particle-storage-lifecycle-and-coupling/09-23-SUMMARY.md
      to: reference/compatibility.json
      via: "independently revalidated exact run/artifact identities"
      pattern: "phase9-canonical|phase9-sanitizer"
    - from: reference/compatibility.json
      to: COMPATIBILITY.md
      via: "inventory generate/check"
      pattern: "@generated"
---

<objective>
Independently validate the newly approved evidence, promote only supported Phase 9 rows, and run final verification/security/schema gates.

Purpose: Replace unsupported prior authority with complete passing evidence while preserving the Phase 10 boundary.
Output: Fail-closed ledger validation, regenerated report, documentation, and final green gate evidence.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-23-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
@reference/compatibility.json
@COMPATIBILITY.md
@TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Independently redownload and revalidate the approved artifacts</name>
  <files>TESTING.md</files>
  <read-first>09-23 approved SHA/run/artifact identities; target run.json; workflow at approved SHA; D-31 authority contract; superseded run 29439515367 references.</read-first>
  <action>Cross-check every run.json field against 09-23-SUMMARY, require approved SHA is an ancestor and exact remote branch head, and inspect workflow from that SHA. Delete only the two download directories; re-query run/jobs/artifacts and redownload exact unexpired names. Recheck run/event/workflow/head/job cardinality, API digests, identity toolchain/upstream/target/policy/safe paths, and recompute trace/manifest hashes. Require passing logs with no FAILED marker. Validate every manifest case, exact reached-branch closure, complete consumed policies, and request/native/oracle/comparison SHA-256 fields. Require the run ID differs from `29439515367`. Record immutable new URLs/procedure in TESTING.md only after all checks pass.</action>
  <verify><automated>RUN_ID=$(jq -r .run_id target/phase9-evidence/run.json) && test "$RUN_ID" != 29439515367 && for d in canonical sanitizer; do DIR=target/phase9-evidence/phase9-$d; grep -F 'test result: ok.' "$DIR/phase9-trace.log"; ! grep -E 'test result: FAILED|FAILED' "$DIR/phase9-trace.log"; jq -e '(.reached_branches|length)>0 and (.consumed_policy_paths|length)>0 and all(.cases[]; (.request_sha256|test("^[0-9a-f]{64}$")) and (.native_result_sha256|test("^[0-9a-f]{64}$")) and (.oracle_result_sha256|test("^[0-9a-f]{64}$")))' "$DIR/phase9-manifest.json"; done && cargo xtask provenance check</automated></verify>
  <acceptance-criteria>Fresh downloads independently prove the new approved SHA and complete passing executable evidence; any stale, failed, incomplete, unsafe, or superseded value blocks all later edits.</acceptance-criteria>
  <done>The independent download/content/provenance verification exits zero, proves the run is not 29439515367, and validates passing logs plus complete branch/policy/request/native/oracle digest evidence before any ledger edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote only new-authority Phase 9 rows and regenerate reports</name>
  <files>reference/compatibility.json, tools/xtask/src/inventory/validation.rs, tools/xtask/tests/inventory_cli.rs, COMPATIBILITY.md, TESTING.md</files>
  <read-first>validated Task 1 authority; compatibility JSON schema; existing four-row Phase 9 promotion gate; all blocked Phase 9 requirement rows; generated report ownership; Phase 10 exclusions.</read-first>
  <action>Update only supported API-09/API-10/PART-01..08/PART-14..17 evidence fields whose implementation/unit/differential/platform claims are proven by the new artifacts. Replace every superseded Phase 9 run/artifact reference used for promotion. Extend inventory validation/tests so promotion requires both new canonical/sanitizer artifacts, passing-log evidence, complete executable branch/policy manifest digests, identical approved head/run, and actual native-versus-C++ comparison; reject run 29439515367, partial/substituted authority, and every Phase 10 group/pair/triad/solver/source-area claim. Never hand-edit COMPATIBILITY.md; generate/check twice. Keep rows whose full requirement is not evidenced pending rather than overclaiming.</action>
  <verify><automated>cargo test -p xtask --test inventory_cli && cargo xtask inventory generate && cargo xtask inventory check && cargo xtask inventory generate && cargo xtask inventory check && just markdown-check</automated></verify>
  <acceptance-criteria>JSON contains only new validated authority for supported rows; the generated report is byte-idempotent; negative tests reject superseded/failed/incomplete/substituted/Phase10 promotion.</acceptance-criteria>
  <done>The inventory tests, double generate/check cycle, and Markdown check exit zero, proving only supported rows use new authority, generated output is byte-idempotent, and superseded/failed/incomplete/substituted/Phase 10 evidence is rejected.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Rust, oracle, schema, security, provenance, and diff gates</name>
  <files></files>
  <read-first>AGENTS.md Rust precommit order; standards/core/verification.md; 09-REVIEW and original 09-VERIFICATION gap list; Phase 9 threat models; repo schema-drift command; pinned source read-only contract.</read-first>
  <action>Run the exact Rust sequence below, focused Phase 9 protocol/comparator/corpus tests, debug/release oracle comparison, ASan/UBSan corpus, provenance/inventory checks, `just markdown-check`, actionlint, and upstream read-only diff. Run `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 09` and require `blocking:false`. Perform the ASVS L1 threat review across untrusted JSONL/process boundaries, resource bounds, workflow/artifact integrity, safe paths, secrets, and shell injection; run `cargo deny check`; any high-severity finding blocks completion. Review the complete 09-18..24 diff and confirm every G09/CR/WR item has an executable regression/evidence link and no Phase 10 scope.</action>
  <verify><automated>cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && cargo xtask upstream configure --preset oracle-debug && cargo xtask upstream build --preset oracle-debug && cargo xtask upstream configure --preset oracle-release && cargo xtask upstream build --preset oracle-release && LIQUIDFUN_PHASE9_ORACLE_MODE=canonical LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/canonical/phase9-manifest.json bash scripts/phase9-evidence.sh canonical target/phase9-evidence-local/canonical && cargo xtask upstream configure --preset oracle-asan-ubsan && cargo xtask upstream build --preset oracle-asan-ubsan && cmake --build target/reference/oracle-asan-ubsan --target liquidfun-reference-protocol-tests && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 ctest --test-dir target/reference/oracle-asan-ubsan --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$' && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 LIQUIDFUN_PHASE9_ORACLE_MODE=sanitizer LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/sanitizer/phase9-manifest.json bash scripts/phase9-evidence.sh sanitizer target/phase9-evidence-local/sanitizer && cargo test -p liquidfun-differential --test phase9_corpus && cargo xtask provenance check && cargo xtask inventory check && cargo deny check && node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 09 && actionlint .github/workflows/oracle.yml && just markdown-check && git diff --exit-code -- third_party/liquidfun && git diff --check</automated></verify>
  <acceptance-criteria>All commands exit zero; schema drift is nonblocking; security has no high findings; every original gap/finding is closed by named tests/evidence; phase completion can proceed without waiver.</acceptance-criteria>
  <done>The exact ordered Rust gate, explicit debug/release canonical corpus, fail-fast sanitizer protocol/corpus, focused Phase 9, provenance/inventory/security/schema/workflow/Markdown/read-only/diff commands all exit zero with `blocking:false`, no high finding, and no waiver.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Downloaded GitHub evidence to public compatibility claim | Stale/substituted/failed artifacts could falsify parity. | Fresh exact-name download, metadata/identity/log/manifest/hash revalidation before edits. |
| Untrusted JSONL/process oracle | Malformed or resource-heavy records could affect native tooling. | Closed schemas, reviewed hard limits, timeouts, strict result validation, sanitizer pass. |
| Workflow/shell/path integrity | Injection, unsafe paths, or masked exit status could corrupt artifacts. | Fixed arguments/paths, pipefail runner, safe-relative checks, no secret output. |
| Generated reports | Manual or partial edits could overstate coverage. | JSON-only authority, fail-closed validator, deterministic double generation/check. |

ASVS L1 is enforced and high severity blocks. Phase 10 groups/topology/pair/triad/solver behavior remains explicitly unpromoted.
</threat-model>

<verification>
Before every implementation/docs commit run exactly in this order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then run inventory generate/check twice, provenance, executable corpus, debug/release/sanitizer, schema drift, `cargo deny check`, actionlint, Markdown, upstream read-only, and diff checks.
</verification>

<success-criteria>
- New approved artifacts independently close evidence, comparison, coverage, and pipeline gaps.
- Runtime/protocol gaps and CR-01/CR-02/WR-01..08 have focused passing regressions.
- Compatibility claims cite only supported new authority; Phase 10 remains pending.
- Full verification, ASVS L1 security, schema drift, provenance, generated-report, and repository diff gates pass without waiver.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-24-SUMMARY.md`.</output>
