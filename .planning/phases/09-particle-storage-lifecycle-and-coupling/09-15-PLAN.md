---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/reference/CMakeLists.txt
  - tools/reference/src/phase9_lifecycle_contact_witness.cpp
  - reference/artifacts/phase9/lifecycle-contact-witnesses.json
  - reference/artifacts/phase9/lifecycle-contact-witnesses.provenance.json
autonomous: true
requirements: [PART-07, PART-08, PART-15]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "Equal-expiration ordering and strict-contact pruning ties are captured from the pinned C++ oracle before Rust implements either rule."
    - "Committed semantic witnesses carry exact upstream, adapter, compiler, preset, command, and content-digest provenance."
  artifacts:
    - path: reference/artifacts/phase9/lifecycle-contact-witnesses.json
      provides: "Stable semantic equal-expiration and strict-pruning outcomes"
    - path: reference/artifacts/phase9/lifecycle-contact-witnesses.provenance.json
      provides: "Exact pinned-oracle generation identity and SHA-256"
  key_links:
    - from: tools/reference/src/phase9_lifecycle_contact_witness.cpp
      to: reference/artifacts/phase9/lifecycle-contact-witnesses.json
      via: "reproducible pinned C++ probe output"
      pattern: "equal_quantized_expiration|strict_contact_pruning"
---
<objective>
Capture the two Phase 9 canonical ordering witnesses before Rust implementation can self-bless them.

Purpose: Resolve D-13 and D-19 from the pinned C++ source with reproducible semantic artifacts and provenance.
Output: A narrow C++ probe target plus committed witness and provenance records consumed by 09-06, 09-09, and 09-13.
</objective>
<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-RESEARCH.md
@reference/upstream-lock.toml
@UPSTREAM.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the narrow pinned-C++ lifecycle/contact probe</name>
  <files>tools/reference/CMakeLists.txt, tools/reference/src/phase9_lifecycle_contact_witness.cpp</files>
  <read_first>Pinned `SolveLifetimes`, `DestroyOldestParticle`, `UpdateBodyContacts`, and `RemoveSpuriousBodyContacts`; upstream lock revision; existing oracle preset/provenance conventions.</read_first>
  <action>Add a private CMake target linked to the pinned read-only LiquidFun oracle. It must construct bounded cases with equal i32 quantized expirations and strict-pruning ties, emit stable scenario IDs and semantic order/keep-remove results only, and reject any upstream revision other than `7f20402173fd143a3988c921bc384459c6a858f2`. Do not call Rust or read Rust traces.</action>
  <verify><automated>cargo xtask upstream configure --preset oracle-debug && cargo xtask upstream build --preset oracle-debug --target phase9-lifecycle-contact-witness</automated></verify>
  <acceptance_criteria>The probe is reproducible from the pinned C++ tree and contains no production or Rust-derived expected behavior.</acceptance_criteria>
  <done>The probe is reproducible from the pinned C++ tree and contains no production or Rust-derived expected behavior.</done>
</task>
<task type="auto">
  <name>Task 2: Generate and verify semantic witness provenance</name>
  <files>reference/artifacts/phase9/lifecycle-contact-witnesses.json, reference/artifacts/phase9/lifecycle-contact-witnesses.provenance.json</files>
  <read_first>Probe target, upstream lock, existing generated reference artifact review rules, and D-13/D-19.</read_first>
  <action>Run `target/reference/oracle-debug/phase9-lifecycle-contact-witness --output reference/artifacts/phase9/lifecycle-contact-witnesses.json --provenance reference/artifacts/phase9/lifecycle-contact-witnesses.provenance.json`. Record upstream revision, adapter content SHA-256, compiler identity/version, target, CMake preset, exact argv, generation timestamp, and witness SHA-256. Run the command twice and require byte-identical semantic output; provenance verification and any mismatch are blocking.</action>
  <verify><automated>cargo xtask provenance check && git diff --exit-code -- third_party/liquidfun</automated></verify>
  <acceptance_criteria>Both required witnesses are committed with verified exact-revision provenance and cannot be refreshed from Rust output.</acceptance_criteria>
  <done>Both required witnesses are committed with verified exact-revision provenance and cannot be refreshed from Rust output.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Pinned C++ probe to committed expectations | Wrong source/tool identity or Rust-derived data could self-bless compatibility. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-15-01 | S | witness provenance | mitigate | Require exact upstream revision, adapter digest, tool identities, argv, and output hash. |
| T-09-15-02 | R | semantic output | mitigate | Generate twice byte-identically and prohibit Rust input/output dependencies. |
</threat-model>
<verification>
The pinned probe build, double generation, provenance check, and upstream read-only diff must all pass. Before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
</verification>
<success-criteria>
- D-13 and D-19 have reproducible semantic oracle witnesses before Rust implementation.
- Provenance identifies exact revision, adapter, tools, command, target, preset, and content.
- Failure or non-identical generation blocks dependent plans.
</success-criteria>
<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-15-SUMMARY.md`.</output>
