---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 14
type: execute
wave: 13
depends_on: [09-13]
files_modified:
  - .github/workflows/oracle.yml
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/
  - crates/liquidfun-differential/tests/phase9_corpus.rs
  - TESTING.md
autonomous: true
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "A closed local corpus witnesses every Phase 9 branch and rejects missing declarations and Phase 10 observations."
    - "The committed oracle workflow can produce identity-bound Phase 9 canonical and sanitizer artifacts when explicitly dispatched."
    - "Workflow and corpus changes pass locally and are cleanly committable before any evidence run targets them."
  artifacts:
    - path: crates/liquidfun-differential/tests/phase9_corpus.rs
      provides: "Closed declaration coverage, replay, D0, mismatch, and Phase 10 rejection gate"
    - path: .github/workflows/oracle.yml
      provides: "Explicit Phase 9 canonical and sanitizer workflow_dispatch jobs"
  key_links:
    - from: .github/workflows/oracle.yml
      to: crates/liquidfun-differential/tests/phase9_corpus.rs
      via: "phase9 canonical/sanitizer job execution"
      pattern: "phase9-canonical|phase9-sanitizer|phase9_corpus"
---
<objective>
Prepare and commit the complete Phase 9 corpus and exact-identity evidence workflow before dispatch.

Purpose: Ensure remote evidence runs an already-published, locally verified commit rather than an uncommitted workflow/corpus mutation.
Output: Closed corpus, fail-fast workflow jobs, and documented dispatch/artifact contract.
</objective>
<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-13-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
@TESTING.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the closed Phase 9 witness corpus</name>
  <files>crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/, crates/liquidfun-differential/tests/phase9_corpus.rs</files>
  <read_first>Phase 9 D-28 through D-31; required differential witnesses; phase9-v1 declarations; 09-13 oracle semantics.</read_first>
  <action>Create bounded named witnesses covering multiple/newest-first and paused systems; stable IDs under sort/rotate/compact; optional lanes and fixed/growable/full/teardown modes; finite/infinite/equal/oldest/maximum lifetimes and requested/unrequested callbacks; particle/body contacts, strict modes, listener/filter gates, coupling fields; force/impulse/statistics; and AABB/ray control/culling. Require exact declaration coverage, unchanged Phase 6-8 families, Phase 10 rejection, replay/minimization identity, first-divergence stability, D0 byte identity, and debug/release agreement. Expected data must come from the pinned oracle or independently derived calculations, never Rust output.</action>
  <verify><automated>cargo test -p liquidfun-differential --test phase9_corpus</automated></verify>
  <acceptance_criteria>Every Phase 9 declaration has branch-complete coverage and undeclared behavior fails closed.</acceptance_criteria>
  <done>Every Phase 9 declaration has branch-complete coverage and undeclared behavior fails closed.</done>
</task>
<task type="auto">
  <name>Task 2: Add commit-ready Phase 9 canonical and sanitizer workflow jobs</name>
  <files>.github/workflows/oracle.yml, TESTING.md</files>
  <read_first>Existing Phase 8 canonical/sanitizer jobs; D-31 toolchain/authority rules; GitHub workflow_dispatch and artifact naming contract.</read_first>
  <action>Add an explicit `evidence_phase=phase9` workflow_dispatch input and fail-fast Phase 9 canonical/ASan-UBSan paths pinned to Linux x86_64, Rust 1.97.0, Clang 22.1.8, CMake 4.3.3, Ninja 1.13.2, and upstream `7f20402173fd143a3988c921bc384459c6a858f2`. Canonical runs debug/release compare, replay, two-run D0, phase9 corpus, provenance/inventory/read-only checks; sanitizer runs the Phase 9 corpus with abort/halt options. Upload `phase9-canonical-${run_id}-${sha}` and `phase9-sanitizer-${run_id}-${sha}` using `if-no-files-found: error`, each with logs plus an `identity.json` containing numeric `run_id`, `job`, `head_sha`, `upstream_revision`, `rust`, `cmake`, `ninja`, `clang`, `target`, policy `phase9-v1`, and `trace`/`manifest` objects. Each object must contain an artifact-relative `path` and lowercase 64-hex `sha256`, and the named trace/manifest files must be included in the same artifact so consumers can recompute both digests. Use target value `x86_64-unknown-linux-gnu` and job values `canonical-linux`/`sanitizer-linux`. Document this exact artifact contract and later dispatch/promotion sequence in TESTING.md, but do not dispatch or promote here.</action>
  <verify><automated>cargo test -p liquidfun-differential --test phase9_corpus && cargo xtask inventory check && just markdown-check</automated></verify>
  <acceptance_criteria>The workflow/corpus/docs are locally green and ready to be committed and published as one exact evidence target.</acceptance_criteria>
  <done>The workflow/corpus/docs are locally green and ready to be committed and published as one exact evidence target.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Committed workflow/corpus to later remote evidence | Uncommitted or mismatched workflow content would invalidate evidence identity. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-14-01 | R | corpus | mitigate | Closed declarations, provenance, replay, and D0 before commit. |
| T-09-14-02 | S | workflow artifacts | mitigate | Exact head SHA/tool/upstream/policy/hash identity in both artifacts. |
</threat-model>
<verification>
Before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then `cargo xtask inventory check` and `just markdown-check`. Review the diff for workflow syntax, exact pins, artifact identity, Phase 10 claims, and a clean commit boundary.
</verification>
<success-criteria>
- Corpus and workflow changes are complete, verified, and independently committable.
- Both future remote jobs fail fast and emit identity-bound artifacts.
- No remote dispatch or compatibility promotion occurs in this plan.
</success-criteria>
<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-14-SUMMARY.md`.</output>
