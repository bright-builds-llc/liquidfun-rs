---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 22
type: execute
wave: 3
depends_on: [09-18, 09-19, 09-20, 09-21]
gap_closure: true
files_modified:
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/phase9-v1.json
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/
  - crates/liquidfun-differential/tests/phase9_corpus.rs
  - tools/reference/src/rigid_world_phase9_decode.hpp
  - tools/reference/src/rigid_world_phase9_execute.hpp
  - tools/reference/adapter-inputs.txt
  - scripts/phase9-evidence.sh
  - .github/workflows/oracle.yml
  - TESTING.md
autonomous: true
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T23:45:08Z
must_haves:
  truths:
    - "Every declared Phase 9 branch is reached by a named executable case and a branch-specific semantic observation."
    - "Each case executes identical request bytes through native Rust and pinned C++, consumes all applicable policies, and records request/result digests."
    - "Canonical and sanitizer workflow steps preserve cargo-test exit status through logging and refuse artifacts whose bound trace is not passing."
  artifacts:
    - path: crates/liquidfun-differential/tests/phase9_corpus.rs
      provides: "Executable branch witnesses, differential comparisons, reached-branch ledger, and digest manifest"
    - path: scripts/phase9-evidence.sh
      provides: "Fail-fast pipe-safe evidence runner with passing-log assertions"
    - path: .github/workflows/oracle.yml
      provides: "Canonical/sanitizer jobs that upload generated executed-case manifests"
  key_links:
    - from: crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/phase9-v1.json
      to: crates/liquidfun-differential/tests/phase9_corpus.rs
      via: "case path, request SHA-256, action/checkpoint witness selector, and expected reached branch"
      pattern: "request_sha256|checkpoint_id|branch"
    - from: .github/workflows/oracle.yml
      to: scripts/phase9-evidence.sh
      via: "closed canonical/sanitizer evidence mode"
      pattern: "phase9-evidence.sh"
---

<objective>
Replace declarative branch labels with executable differential cases and make artifact publication fail closed on the real test outcome.

Purpose: Close CR-02/G09-EXECUTABLE-COVERAGE and repair the masked failed-evidence pipeline before any new remote authority is requested.
Output: Branch-specific case fixtures, reached-branch/digest manifest, standalone Phase 9 checkpoint support, and pipe-safe workflow execution.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-REVIEW.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-21-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-14-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make every branch declaration point to an executable semantic witness</name>
  <files>crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/phase9-v1.json, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/, crates/liquidfun-differential/tests/phase9_corpus.rs</files>
  <read-first>current manifest/59 labels and bounded request builder; 09-REVIEW CR-02; 09-VERIFICATION G09-EXECUTABLE-COVERAGE; required witness list in 09-RESEARCH; comparator output from 09-21.</read-first>
  <action>Replace each label-only case with a strict record containing `case_id`, authority, fixture-relative JSONL path, lowercase request SHA-256, and one or more witnesses naming exact branch token, action ID, checkpoint ID, observation kind, and semantic predicate/value. Add bounded fixtures that actually execute lifetime finite/infinite/negative/equal/oldest/maximum, public/pending zombies, immediate listener eviction, permutations with contacts/weights, strict/non-strict/filter/listener branches, particle/body contacts and coupling with live bodies, force/impulse/statistics, AABB culling/control, and ray ignore/continue/clip/terminate/start-inside behavior. For each case hash and decode the checked-in bytes, execute native and selected oracle on the identical request, validate both results, run the 09-21 comparator, evaluate every witness against actual observations, and require exact-once closure over required branches. Preserve explicit Phase 10 rejection.</action>
  <verify><automated>cargo test -p liquidfun-differential --test phase9_corpus executable_cases</automated></verify>
  <acceptance-criteria>Deleting an action/observation or changing a declared request digest fails its owning branch; all required branches are reached exactly once by executable observations; every case proves same request digest, cross-engine comparison success, and applicable policy consumption.</acceptance-criteria>
  <done>The executable-cases test exits zero and its negative mutations prove every required branch has exactly one digest-bound semantic witness with same-request cross-engine comparison and applicable policy consumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Support Phase 9-only checkpoints and emit an executed-case evidence manifest</name>
  <files>tools/reference/src/rigid_world_phase9_decode.hpp, tools/reference/src/rigid_world_phase9_execute.hpp, tools/reference/adapter-inputs.txt, crates/liquidfun-differential/tests/phase9_corpus.rs</files>
  <read-first>`strip_phase9_for_legacy_decode`; `apply_phase9_timeline`; failed text `Phase 9 checkpoint has no legacy predecessor`; result checkpoint schema/count validation; artifact manifest contract in TESTING.md.</read-first>
  <action>Remove the legacy-predecessor requirement. Preserve metadata for a checkpoint whose action window is entirely Phase 9 and construct/merge its complete rigid checkpoint shell from validated expected counts and declaration-ordered live rigid state; do not weaken retained checkpoint checks. In corpus execution build a deterministic manifest containing profile, upstream revision, case IDs, reached branches, consumed policy paths, and lowercase SHA-256 digests for each request, native validated result, oracle validated result, and comparison summary. Write it only when `LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST` names the workflow-owned static output path; otherwise validate it in memory. Reject duplicates, missing branches/policies/digests, unsafe paths, or empty result sets.</action>
  <verify><automated>cargo xtask upstream configure --preset oracle-debug && cargo xtask upstream build --preset oracle-debug && LIQUIDFUN_PHASE9_ORACLE_MODE=canonical LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-test.json cargo test -p liquidfun-differential --test phase9_corpus -- --nocapture && jq -e '.cases | length > 0 and all(.[]; (.request_sha256|test("^[0-9a-f]{64}$")) and (.native_result_sha256|test("^[0-9a-f]{64}$")) and (.oracle_result_sha256|test("^[0-9a-f]{64}$")))' target/phase9-evidence-test.json</automated></verify>
  <acceptance-criteria>The previously failing first Phase 9 checkpoint passes without a legacy predecessor; retained Phase 6-8 results remain unchanged; generated manifest binds every executed request/result/branch/policy with valid digests.</acceptance-criteria>
  <done>The debug build, canonical corpus, and jq contract exit zero, proving Phase 9-only checkpoints execute, retained Phase 6-8 results stay unchanged, and every executed request/result/branch/policy is bound by valid manifest digests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve test exit status and assert passing logs before artifact upload</name>
  <files>scripts/phase9-evidence.sh, .github/workflows/oracle.yml, TESTING.md</files>
  <read-first>current Phase 9 canonical/sanitizer workflow steps; Bash repo rules; approved artifact identity fields; failed artifact logs showing `4 passed; 1 failed`; 09-VERIFICATION G09-EVIDENCE-PIPELINE.</read-first>
  <action>Create a checked-in Bash runner with `#!/usr/bin/env bash` and `set -euo pipefail`, accepting only `canonical` or `sanitizer` plus its fixed repository-owned output directory. Run `cargo test ... --nocapture 2>&1 | tee phase9-trace.log` under the selected required oracle mode and generated-manifest environment. After the pipeline, require the log contains the Phase 9 test target's `test result: ok.` and contains neither `test result: FAILED` nor `FAILED`; require the generated manifest is nonempty and passes the exact case/branch/policy/digest jq contract. Update both workflow jobs to call this runner, rerun provenance/inventory/read-only checks with pipeline failure propagation, hash the generated manifest rather than a label-only jq document, and create identity/upload only after every assertion passes. Add shell tests that inject a failing command/log and prove nonzero exit blocks identity creation.</action>
  <verify><automated>bash -n scripts/phase9-evidence.sh && cargo test -p liquidfun-differential --test phase9_corpus workflow_contract && cargo xtask upstream configure --preset oracle-debug && cargo xtask upstream build --preset oracle-debug && cargo xtask upstream configure --preset oracle-release && cargo xtask upstream build --preset oracle-release && LIQUIDFUN_PHASE9_ORACLE_MODE=canonical LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/canonical/phase9-manifest.json bash scripts/phase9-evidence.sh canonical target/phase9-evidence-local/canonical && cargo xtask upstream configure --preset oracle-asan-ubsan && cargo xtask upstream build --preset oracle-asan-ubsan && cmake --build target/reference/oracle-asan-ubsan --target liquidfun-reference-protocol-tests && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 ctest --test-dir target/reference/oracle-asan-ubsan --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$' && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 LIQUIDFUN_PHASE9_ORACLE_MODE=sanitizer LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/sanitizer/phase9-manifest.json bash scripts/phase9-evidence.sh sanitizer target/phase9-evidence-local/sanitizer && actionlint .github/workflows/oracle.yml && just markdown-check</automated></verify>
  <acceptance-criteria>A nonzero cargo test behind tee returns nonzero; a `4 passed; 1 failed` log cannot produce identity.json; both workflow artifacts bind a passing trace and executed-case manifest; no direct compatibility promotion occurs.</acceptance-criteria>
  <done>The shell/workflow contract, debug/release canonical corpus, fail-fast ASan/UBSan protocol and sanitizer corpus, actionlint, and Markdown commands all exit zero; injected failing/FAILED logs create no identity and neither mode promotes compatibility.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Untrusted bounded JSONL/process oracle | Malformed cases or child output could crash, hang, or falsify coverage. | Existing hard limits/timeouts, strict decode/result validation, identical request digests. |
| Branch declarations | Labels could claim unexecuted behavior. | Action/checkpoint/observation predicates evaluated against both validated results. |
| Workflow/artifacts | `tee` or static metadata could mask failure or upload fabricated authority. | `set -euo pipefail`, passing-log assertions, generated executed-case manifest, hash binding before upload. |
| Resource exhaustion/path traversal | Case arrays, output paths, and child diagnostics could be attacker-controlled. | Existing protocol maxima; fixed repo-owned output directories; safe relative fixture paths; bounded logs. |

ASVS L1 review blocks any high-severity finding. No secrets are printed, no arbitrary command/path input is accepted, and the pinned submodule remains read-only.
</threat-model>

<verification>
Run focused corpus/oracle/workflow tests, provenance, inventory, actionlint, and `just markdown-check` for TESTING.md. Before every implementation commit run exactly in this order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Build debug/release/ASan-UBSan and review for Phase 10 exclusions.
</verification>

<success-criteria>
- CR-02 and G09-EXECUTABLE-COVERAGE are closed with branch-specific observations and request/result digests.
- G09-EVIDENCE-PIPELINE is mechanically fixed: legacy predecessor, pipe status, passing-log assertion, and generated manifest.
- Local canonical and sanitizer dry runs pass, but no D1 claim is promoted before 09-23/09-24.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-22-SUMMARY.md`.</output>
