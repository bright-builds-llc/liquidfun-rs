---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 11
type: execute
wave: 10
depends_on: [09-09, 09-10]
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/particle.rs
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/particle/query.rs
  - crates/liquidfun/src/world/query.rs
  - crates/liquidfun/tests/particle_queries.rs
autonomous: true
requirements: [PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "Per-system and mixed AABB queries return stable particle hits with checked continue/terminate control."
    - "Particle ray casts preserve start-inside exclusion, clipping, ignore/continue/terminate, culling, and no-effect invalid directives."
  artifacts:
    - path: crates/liquidfun/src/particle/query.rs
      provides: "Pure per-system AABB/ray candidate and hit kernels"
    - path: crates/liquidfun/src/world/query.rs
      provides: "Fixture-first mixed traversal with newest-first culled systems"
  key_links:
    - from: crates/liquidfun/src/world.rs
      to: crates/liquidfun/src/particle.rs
      via: "world child declarations and public particle records"
      pattern: "mod particle|pub use"
    - from: crates/liquidfun/src/world/query.rs
      to: crates/liquidfun/src/particle/query.rs
      via: "shared termination/current clip state"
      pattern: "particle.*query|particle.*ray"
---
<objective>
Add per-system and mixed-world particle AABB queries and ray casts using the established typed control model.

Purpose: Deliver PART-17 while retaining rigid query semantics and separating production traversal from evidence canonicalization.
Output: Particle query kernels, mixed traversal wiring, and directive/culling tests.
</objective>
<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-09-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement per-system particle AABB and ray kernels</name>
  <files>crates/liquidfun/src/particle/query.rs, crates/liquidfun/tests/particle_queries.rs</files>
  <read_first>Phase 9 D-25/D-26; pinned QueryAABB/RayCast; existing QueryDirective/RayCastDirective checked fraction contract.</read_first>
  <action>Declare all new particle/world child modules from particle.rs/world.rs, re-export supported public records through particle.rs/lib.rs, and prove external-crate reachability. Implement checked per-system AABB proxy enumeration and ray hit calculation over stable IDs. Preserve start-inside exclusion, repeated occurrences, ignore, terminate, continue without clipping, clip to checked fraction, shared current fraction, and no mutation on invalid geometry/directive. Keep upstream-unspecified callback order unspecified; do not canonicalize production callbacks.</action>
  <verify><automated>cargo test -p liquidfun --test particle_queries per_system</automated></verify>
  <acceptance_criteria>Every typed directive and edge case has one-concern tests and invalid directives have no effect.</acceptance_criteria>
  <done>Every typed directive and edge case has one-concern tests and invalid directives have no effect.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Wire fixture-first mixed World traversal</name>
  <files>crates/liquidfun/src/world/query.rs, crates/liquidfun/tests/particle_queries.rs</files>
  <read_first>Existing rigid World query/ray implementation; Phase 9 D-25/D-26; newest-first system ordering.</read_first>
  <action>Extend mixed World AABB/ray APIs to traverse rigid fixtures first, then newest-first particle systems with system-AABB culling and shared termination/current clip state. Preserve existing fixture results and callback semantics exactly. Add tests for culling, multiple systems, fixture-before-particle relationship, clip propagation, early termination, repeated hits, and rigid-only regression equivalence.</action>
  <verify><automated>cargo test -p liquidfun --test particle_queries mixed_world</automated></verify>
  <acceptance_criteria>Mixed queries obey pinned cross-domain traversal and retain all pre-existing rigid behavior.</acceptance_criteria>
  <done>Mixed queries obey pinned cross-domain traversal and retain all pre-existing rigid behavior.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Callback directives to traversal state | Invalid fractions or retained callbacks could corrupt clipping or borrow safety. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-11-01 | T | ray directives | mitigate | Checked fraction type and validate-before-update current clip. |
| T-09-11-02 | D | mixed traversal | mitigate | Per-system culling plus immediate shared termination. |
</threat-model>
<verification>
Run `cargo check -p liquidfun --all-features` to verify entrypoint wiring.
Run query/ray and rigid query regressions, then before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Diff-review that canonicalization appears only in collectors.
</verification>
<success-criteria>
- Per-system and mixed-world queries use stable particle IDs.
- Clipping, termination, filtering, culling, and start-inside behavior match source.
- Rigid query semantics remain unchanged.
</success-criteria>
<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-11-SUMMARY.md`.</output>
