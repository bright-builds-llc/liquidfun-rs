---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 23
type: execute
wave: 4
depends_on: [09-22]
gap_closure: true
files_modified: []
autonomous: false
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T23:45:08Z
must_haves:
  truths:
    - "A human approves the immutable full SHA containing every gap fix and the repaired evidence workflow before dispatch."
    - "A new exact-ref run has one successful canonical job and one successful sanitizer job for exactly that SHA."
    - "Downloaded artifacts bind passing logs, executed-case manifests, complete branches/policies, and recomputed payload hashes."
  artifacts:
    - path: target/phase9-evidence/run.json
      provides: "Approved SHA, dispatch time, exact run/jobs/artifact names, URLs, and API digests"
    - path: target/phase9-evidence/phase9-canonical/identity.json
      provides: "New canonical identity for the approved gap-fix commit"
    - path: target/phase9-evidence/phase9-sanitizer/identity.json
      provides: "New sanitizer identity for the approved gap-fix commit"
  key_links:
    - from: .planning/phases/09-particle-storage-lifecycle-and-coupling/09-22-SUMMARY.md
      to: .github/workflows/oracle.yml
      via: "clean published commit inspected before dispatch"
      pattern: "phase9"
    - from: target/phase9-evidence/run.json
      to: target/phase9-evidence/phase9-canonical/identity.json
      via: "exact run/head/artifact equality"
      pattern: "head_sha|run_id"
---

<objective>
Publish, human-approve, dispatch, and validate new canonical plus sanitizer evidence for the repaired Phase 9 implementation.

Purpose: Establish fresh exact-ref authority; superseded run `29439515367` is never reused.
Output: Approved immutable SHA, successful run, and locally downloaded identity-bound artifacts. No compatibility promotion.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-22-SUMMARY.md
@.github/workflows/oracle.yml
@TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish a clean exact evidence target</name>
  <files></files>
  <read-first>09-22 summary; current git status/branch/upstream; workflow and case fixtures at HEAD; prior run IDs and superseded approved SHA.</read-first>
  <action>Require a clean worktree, nonempty branch, GitHub origin slug equality, and all mandatory Phase 9 local gates from 09-22. Record `REPO`, `BRANCH`, and full `HEAD_SHA`; push without rewriting history; require remote branch SHA equals HEAD; prove HEAD contains 09-18..22 implementation/comparator/corpus/workflow commits; print the immutable commit URL and full SHA. Do not dispatch and do not reuse any prior run/artifact.</action>
  <verify><automated>REPO=$(gh repo view --json nameWithOwner --jq .nameWithOwner) && BRANCH=$(git branch --show-current) && HEAD_SHA=$(git rev-parse HEAD) && test -z "$(git status --porcelain)" && test -n "$BRANCH" && test "$(git ls-remote --heads origin "refs/heads/$BRANCH" | awk 'NR==1 {print $1}')" = "$HEAD_SHA" && git show "$HEAD_SHA:.github/workflows/oracle.yml" | grep -F phase9-evidence.sh</automated></verify>
  <acceptance-criteria>The intended clean gap-fix SHA is immutable on the intended remote and includes the repaired runtime, protocol, comparator, executable corpus, and workflow.</acceptance-criteria>
  <done>The publication verification exits zero, proving a clean nonempty branch has identical local/remote full SHA and that immutable commit contains the repaired Phase 9 evidence runner/workflow and all preceding gap-fix commits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve the exact published gap-fix SHA</name>
  <files></files>
  <read-first>The immutable commit URL/full SHA from Task 1 and its Phase 9-only diff.</read-first>
  <action>Pause. Require the user to inspect the exact commit and respond `approved &lt;full-commit-sha&gt;`. Copy that SHA verbatim; any mismatch, short SHA, changed remote head, or dirty worktree blocks dispatch.</action>
  <what-built>Task 1 published one clean immutable remote commit whose full SHA contains the repaired runtime, protocol, comparator, executable corpus, and evidence workflow but has not been dispatched.</what-built>
  <how-to-verify>Open the immutable commit URL from Task 1, inspect its Phase 9-only diff, confirm its displayed 40-character SHA exactly matches clean local HEAD and the remote branch head, then approve only that full SHA.</how-to-verify>
  <resume-signal>approved &lt;full-commit-sha&gt;</resume-signal>
  <verify><automated>BRANCH=$(git branch --show-current) && HEAD_SHA=$(git rev-parse HEAD) && test -z "$(git status --porcelain)" && test "$(git ls-remote --heads origin "refs/heads/$BRANCH" | awk 'NR==1 {print $1}')" = "$HEAD_SHA"</automated></verify>
  <acceptance-criteria>The human explicitly approves the full immutable SHA that will be dispatched.</acceptance-criteria>
</task>

<task type="auto">
  <name>Task 3: Dispatch and validate new exact-ref canonical and sanitizer artifacts</name>
  <files></files>
  <read-first>verbatim approved SHA; TESTING.md artifact contract; workflow job names; phase9 executed-case manifest schema from 09-22.</read-first>
  <action>Require approved SHA equals clean local and remote branch heads, record UTC dispatch time, dispatch `oracle.yml` with `evidence_phase=phase9`, and select exactly one later workflow_dispatch run matching workflow, branch, and full SHA. Watch with `--exit-status`. Require exactly one successful `Canonical Linux oracle` and sanitizer job. Require exact unexpired names `phase9-{canonical,sanitizer}-${RUN_ID}-${HEAD_SHA}` and API SHA-256 digests; record run metadata in `target/phase9-evidence/run.json`. Download exact names into fresh fixed directories. Validate identity run/head/job/upstream/toolchain/target/policy/safe paths; recompute trace and manifest hashes; require both logs contain `test result: ok.` and neither `test result: FAILED` nor `FAILED`; require both manifests contain every case/branch/policy and valid request/native/oracle/comparison digests. No ledger file may change.</action>
  <verify><automated>RUN_ID=$(jq -r .run_id target/phase9-evidence/run.json) && HEAD_SHA=$(jq -r .head_sha target/phase9-evidence/run.json) && gh run view "$RUN_ID" --json conclusion,headSha,event,jobs,workflowName | jq -e --arg sha "$HEAD_SHA" '.conclusion=="success" and .headSha==$sha and .event=="workflow_dispatch" and .workflowName=="Oracle CI" and ([.jobs[]|select(.name=="Canonical Linux oracle" and .conclusion=="success")]|length==1) and ([.jobs[]|select(.name=="Scheduled fail-fast sanitizer and reset corpus" and .conclusion=="success")]|length==1)' && for d in canonical sanitizer; do DIR=target/phase9-evidence/phase9-$d; grep -F 'test result: ok.' "$DIR/phase9-trace.log"; ! grep -E 'test result: FAILED|FAILED' "$DIR/phase9-trace.log"; jq -e '.cases|length>0' "$DIR/phase9-manifest.json"; done</automated></verify>
  <acceptance-criteria>Only the newly human-approved SHA has one passing canonical and sanitizer authority pair; both downloaded payloads prove passing execution and complete digest-bound executable coverage; compatibility remains untouched.</acceptance-criteria>
  <done>The run/job/artifact verification exits zero and proves the approved full SHA alone owns one passing canonical/sanitizer pair whose downloaded logs and manifests provide complete digest-bound executable coverage without compatibility changes.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Human-approved ref to workflow dispatch | Wrong/moving branch could execute unreviewed code. | Full SHA approval plus local/remote equality immediately before dispatch. |
| GitHub run/artifacts to local evidence | Ambiguous, expired, substituted, or path-unsafe artifacts could impersonate authority. | Exact post-dispatch selector, cardinality, API digest, identity, safe path, and content hash checks. |
| Logs/manifests | Successful job metadata could still bind failing tests or declarative coverage. | Passing-log assertions and complete executed-case/branch/policy/digest validation. |
| Secrets/resources | Workflow credentials or downloads could leak or exhaust resources. | GitHub-managed token only, no token output, exact two artifacts, bounded manifests/logs. |

ASVS L1: any high-severity integrity/authentication finding blocks completion. This plan makes no tracked change or compatibility claim.
</threat-model>

<verification>
All local gates must already pass before publication. This no-code checkpoint performs clean-ref, human approval, `gh run watch --exit-status`, job/artifact cardinality, passing-log, manifest, identity, and payload hash checks. No commit is created and no compatibility file is edited.
</verification>

<success-criteria>
- A new human-approved immutable SHA—not `a87f84b...`—owns the evidence run.
- Canonical and sanitizer artifacts both prove passing tests and complete executable comparison coverage.
- Any failed/missing/duplicate/mismatched/expired artifact leaves Phase 9 unpromoted.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-23-SUMMARY.md` with the exact approval/run/job/artifact/log/manifest/hash evidence.</output>
