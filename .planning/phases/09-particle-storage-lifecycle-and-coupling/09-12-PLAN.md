---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 12
type: execute
wave: 11
depends_on: [09-07, 09-09, 09-10, 09-11]
files_modified:
  - crates/liquidfun-test-protocol/src/schema/rigid_world/phase9.rs
  - crates/liquidfun-test-protocol/src/schema/rigid_world.rs
  - crates/liquidfun-differential/src/rigid_world/phase9.rs
  - crates/liquidfun-differential/src/rigid_world.rs
  - crates/liquidfun-differential/tests/particle_protocol.rs
autonomous: true
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "The existing rigid-world protocol accepts bounded Phase 9 particle definitions/actions/checkpoints and stable semantic records."
    - "The phase9-v1 declaration/policy registry fails closed on unknown observations and all Phase 10 fields."
    - "Phase 6-8 request/trace behavior remains byte-compatible."
  artifacts:
    - path: crates/liquidfun-test-protocol/src/schema/rigid_world/phase9.rs
      provides: "Bounded additive Phase 9 schema and validation"
    - path: crates/liquidfun-differential/src/rigid_world/phase9.rs
      provides: "Native execution, collection, declaration, and named comparator policies"
  key_links:
    - from: crates/liquidfun-differential/src/rigid_world/phase9.rs
      to: crates/liquidfun-test-protocol/src/schema/rigid_world/phase9.rs
      via: "stable semantic actions/checkpoints"
      pattern: "phase9-v1"
---
<objective>
Extend the existing long-lived rigid-world protocol/native comparator with a closed bounded Phase 9 surface.

Purpose: Make every implemented branch observable without a second harness or wildcard numerical policy.
Output: Additive schema, native adapter, closed witness/policy registry, and codec/fail-closed tests.
</objective>
<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-07-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-09-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-10-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-11-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add bounded additive Phase 9 schema</name>
  <files>crates/liquidfun-test-protocol/src/schema/rigid_world/phase9.rs, crates/liquidfun-test-protocol/src/schema/rigid_world.rs, crates/liquidfun-differential/tests/particle_protocol.rs</files>
  <read_first>Phase 9 D-27 through D-30; existing rigid_world bounded schema and Phase 8 child-module pattern; semantic records from plans 05-11.</read_first>
  <action>Add bounded particle-system/particle definitions, owned-buffer modes, actions, checkpoints, callbacks, lifecycle occurrences, contacts/body contacts, statistics, AABB/ray observations, and mixed rigid/particle state to the existing request/trace version through a phase-specific child module. Validate stable IDs and exact f32 bit inputs. Reject unknown families, excess bounds, group topology, pair/triad generation, and baseline/flag solver observations. Preserve all earlier serialized forms.</action>
  <verify><automated>cargo test -p liquidfun-test-protocol && cargo test -p liquidfun-differential --test particle_protocol codec</automated></verify>
  <acceptance_criteria>The schema is bounded, additive, stable-ID based, and explicitly rejects every Phase 10 observation class.</acceptance_criteria>
  <done>The schema is bounded, additive, stable-ID based, and explicitly rejects every Phase 10 observation class.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Execute native scenarios and close declarations/policies</name>
  <files>crates/liquidfun-differential/src/rigid_world/phase9.rs, crates/liquidfun-differential/src/rigid_world.rs, crates/liquidfun-differential/tests/particle_protocol.rs</files>
  <read_first>Phase 9 D-28 through D-31; Phase 8 native adapter/declaration registry/comparator; implemented public semantics from summaries.</read_first>
  <action>Map bounded actions to the public native APIs and collect semantic checkpoints at source points. Define phase9-v1 declarations for every storage/capacity/permutation/lifetime/zombie/contact/strict/filter/listener/force/stat/query/culling/coupling branch. Compare identity/order/multiplicity/branch structure exactly and assign named exact-bit/ULP/absolute-relative/dimensioned policies only to reviewed float paths. Unknown observations and missing policies are harness failures; retain Phase 6-8 families unchanged.</action>
  <verify><automated>cargo test -p liquidfun-differential --test particle_protocol</automated></verify>
  <acceptance_criteria>Every Phase 9 observation is declared and policy-covered, while wildcard tolerances and Phase 10 claims fail closed.</acceptance_criteria>
  <done>Every Phase 9 observation is declared and policy-covered, while wildcard tolerances and Phase 10 claims fail closed.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| JSONL request/trace to native execution/comparator | Malformed, oversized, undeclared, or self-blessed observations can falsify evidence. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-12-01 | D | schema | mitigate | Hard bounds and typed validation before execution. |
| T-09-12-02 | R | declaration/policy registry | mitigate | Fail closed on unknown/missing/Phase 10 observations and wildcard policies. |
</threat-model>
<verification>
Run protocol/differential tests, then before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Also run earlier rigid protocol regressions and review serialized compatibility.
</verification>
<success-criteria>
- One existing long-lived protocol covers Phase 9; no second harness exists.
- phase9-v1 is bounded and closed over every scoped branch.
- Earlier rigid witnesses remain unchanged and Phase 10 observations are rejected.
</success-criteria>
<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-12-SUMMARY.md`.</output>
