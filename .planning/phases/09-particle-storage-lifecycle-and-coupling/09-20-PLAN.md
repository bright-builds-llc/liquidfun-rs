---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 20
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs
  - crates/liquidfun-differential/tests/particle_protocol.rs
  - tools/reference/src/rigid_world_phase9_decode.hpp
  - tools/reference/src/rigid_world_phase9_execute.hpp
  - tools/reference/adapter-inputs.txt
  - crates/liquidfun-differential/tests/particle_oracle.rs
autonomous: true
requirements: [PART-01, PART-02, PART-03, PART-07, PART-08, PART-15, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T23:45:08Z
must_haves:
  truths:
    - "Validated Phase 9 requests reject duplicate creation, use-before-create, use-after-destroy, wrong owner, cross-system ranges, and unknown IDs before either engine runs."
    - "Every Phase 9 action accepts exactly its action-specific observation shape, IDs, order, lengths, and lifecycle transition."
    - "Every finite lifetime bit pattern is accepted, including negative and zero infinite-lifetime values; NaN and infinities remain rejected."
    - "The C++ boundary mirrors request acceptance and emits live body IDs in the same declaration order as Rust."
  artifacts:
    - path: crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs
      provides: "Declaration/creation/liveness/ownership state machine for Phase 9 requests"
    - path: crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs
      provides: "Action-specific Phase 9 result contract and semantic replay"
    - path: crates/liquidfun-differential/tests/particle_protocol.rs
      provides: "Mutation matrix for request and result rejection plus negative lifetime acceptance"
    - path: tools/reference/src/rigid_world_phase9_execute.hpp
      provides: "C++ request parity and declaration-ordered mixed body identity"
  key_links:
    - from: crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs
      to: crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs
      via: "typed owner/lifecycle state applied in action order"
      pattern: "CreateSystem|CreateParticle|DestroySystem|MarkForDestruction"
    - from: crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs
      to: crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs
      via: "one required observation matcher per particle action"
      pattern: "Phase9ParticleObservation"
---

<objective>
Make the Phase 9 request/result boundary lifecycle-aware, action-specific, and consistent with production lifetime semantics.

Purpose: Prevent malformed or fabricated particle traces from reaching or crossing the validated protocol boundary differently in Rust and C++.
Output: Closed request state validation, closed result replay validation, and focused negative fixtures.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-REVIEW.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-12-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate Phase 9 request ownership and lifecycle order</name>
  <files>crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs, crates/liquidfun-differential/tests/particle_protocol.rs</files>
  <read-first>crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs body/fixture lifecycle state machine; crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs action definitions/bounds; crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs; crates/liquidfun-differential/tests/particle_protocol.rs; production `ParticleDef::with_lifetime`; 09-REVIEW WR-07/WR-08; 09-VERIFICATION G09-PROTOCOL-VALIDATION.</read-first>
  <action>Extend request validation with declaration-order maps `particle -> owning system`, and created/live/pending/destroyed sets for systems and particles. Apply every Phase 9 action sequentially: creation requires a declared but not previously created ID and live owner; inspection/edit/mark/compact/statistics require the correct live or explicitly permitted pending state; system destruction invalidates all owned particles; ranges must be nonempty, unique, live, and share one owner; optional query/ray owners must be live. Reject use-before-create, duplicate create, recreate-after-destroy, use-after-system-destroy, wrong owner, cross-system range, repeated mark if the wire contract does not explicitly model pending access, and unknown IDs as `InvalidParticleAction` before execution. Replace nonnegative lifetime validation with finite-bit validation so `-1.0`, `-0.0`, and `0.0` are accepted as infinite while NaN and infinities fail. Add one-concern mutation tests for every rejection and acceptance case.</action>
  <verify><automated>cargo test -p liquidfun-test-protocol phase9 && cargo test -p liquidfun-differential --test particle_protocol request</automated></verify>
  <acceptance-criteria>Each invalid lifecycle/owner mutation fails during `decode_rigid_world_request_jsonl`; negative finite lifetime decodes and preserves exact bits; no executor is invoked by these tests; earlier Phase 6-8 fixtures remain byte-compatible.</acceptance-criteria>
  <done>Both request-validation commands exit zero, proving every invalid lifecycle/owner mutation fails before execution, negative finite lifetime bits survive decode, and retained Phase 6-8 fixtures remain byte-compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mirror protocol acceptance and mixed identity in the pinned C++ adapter</name>
  <files>tools/reference/src/rigid_world_phase9_decode.hpp, tools/reference/src/rigid_world_phase9_execute.hpp, tools/reference/adapter-inputs.txt, crates/liquidfun-differential/tests/particle_oracle.rs</files>
  <read-first>the Task 1 Rust request state machine; tools/reference/src/rigid_world_phase9_decode.hpp; tools/reference/src/rigid_world_phase9_execute.hpp `observe_mixed_state`; tools/reference/adapter-inputs.txt; crates/liquidfun-differential/src/rigid_world/phase9.rs MixedState collector; 09-REVIEW WR-06/WR-07/WR-08.</read-first>
  <action>Mirror the exact declared/created/live/pending/destroyed and particle-owner acceptance matrix in C++ before allocation. Replace nonnegative lifetime rejection with finite-bit validation and preserve negative bits through the pinned lifetime call. Replace the hard-coded empty mixed `body_ids` array with currently live body IDs filtered in timeline declaration order, never unordered-map order. Register changed adapter inputs. Add cross-boundary tests for each invalid lifecycle/ownership mutation, negative finite lifetime as infinite, and live mixed body/particle identity equality.</action>
  <verify><automated>cargo xtask upstream configure --preset oracle-debug && cargo xtask upstream build --preset oracle-debug && cargo test -p liquidfun-differential --test particle_oracle decode && cargo test -p liquidfun-differential --test particle_oracle mixed_identity</automated></verify>
  <acceptance-criteria>Rust and C++ accept/reject the same request matrix before effects; `-1.0f` lifetime succeeds as infinite; mixed body IDs match exact native declaration order; provenance includes all changed adapter sources.</acceptance-criteria>
  <done>The debug oracle build and focused oracle tests exit zero, proving Rust/C++ request parity, infinite `-1.0f` lifetime handling, declaration-ordered mixed body identity, and complete adapter provenance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Match every Phase 9 action to one exact result observation contract</name>
  <files>crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs, crates/liquidfun-differential/tests/particle_protocol.rs</files>
  <read-first>crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs `validate_checkpoint_observations` and `phase9_observation_exceeds_bounds`; crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase8.rs pattern; crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs observation fields; 09-REVIEW WR-05.</read-first>
  <action>Add a dedicated Phase 9 result validator that replays the same system/particle lifecycle and ownership state at checkpoint action windows. For each `Phase9ParticleAction`, consume exactly the documented source-timed particle observation(s): require the nested variant, referenced system/particle IDs, stable declaration order, live/pending membership, query/ray owner and parallel-array lengths, lifecycle occurrence identity/multiplicity, statistics owner/stuck IDs, contact endpoint owners, and `MixedState` live body/particle identities. Reject wrong nested variants, unknown or stale IDs, reordered exact-order arrays, duplicates, missing/extra observations, mismatched parallel lengths, and observations from future actions. Keep hard aggregate bounds before allocation and preserve Phase 8 validation unchanged.</action>
  <verify><automated>cargo test -p liquidfun-differential --test particle_protocol result</automated></verify>
  <acceptance-criteria>Mutating a valid result at any action to the wrong observation variant, ID, owner, order, multiplicity, or length yields `ResultObservationMismatch` (or the existing narrower typed result error); an unmodified native result validates; Phase 8 results remain unchanged.</acceptance-criteria>
  <done>The result-validation test exits zero and proves every wrong variant/ID/owner/order/multiplicity/length mutation fails closed while an unmodified native result and retained Phase 8 results validate.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Untrusted JSONL to typed request/result | Unknown IDs, lifecycle abuse, oversized arrays, and fabricated observations could diverge engines or exhaust resources. | Strict records, fixed existing maxima, checked maps/sets, sequential state replay, fail-before-effects validation. |
| Process oracle | Only validated requests may be serialized to the child; malformed results fail before semantic comparison. | Harness failure remains distinct from physics mismatch. |
| Artifact/workflow integrity | This plan changes no evidence authority. | Existing promoted evidence is not reused; a new exact-ref run is required later. |
| Resource-bounded decoding | Attacker-controlled collections remain bounded by `PHASE9_MAXIMUM_*` and aggregate limits before map/vector construction. | No unbounded recursion, wildcard records, or arbitrary paths. |

ASVS L1 review finds no secrets, authentication, unsafe memory, or high-severity threat introduced.
</threat-model>

<verification>
Run strict codec/result mutation tests and retained-family regressions. Before every implementation commit run exactly in this order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Regenerate schema only if the wire shape changes; if non-GSD Markdown changes unexpectedly, run `just markdown-check`.
</verification>

<success-criteria>
- WR-05 through WR-08 and G09-PROTOCOL-VALIDATION are closed across Rust and C++.
- Every accepted Phase 9 request/result is lifecycle-consistent, owner-consistent, bounded, and action-specific.
- Earlier rigid protocol bytes and validations remain unchanged.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-20-SUMMARY.md`.</output>
