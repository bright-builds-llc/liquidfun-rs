---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 31
type: execute
wave: 2
depends_on: [09-30]
gap_closure: true
files_modified:
  - TESTING.md
  - reference/compatibility.json
  - tools/xtask/src/inventory/validation.rs
  - tools/xtask/tests/inventory_cli.rs
  - COMPATIBILITY.md
  - .planning/phases/09-particle-storage-lifecycle-and-coupling/09-31-SUMMARY.md
  - .planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-18T21:00:00Z
must_haves:
  truths:
    - "The Phase-09-only user exception authorizes autonomous exact-SHA dispatch without manual SHA approval and does not alter any global policy or configuration."
    - "One clean immutable local SHA equals the remote branch head, has zero prior Oracle CI Phase 9 dispatches, and receives exactly one evidence_phase=phase9 oracle.yml dispatch with no retry."
    - "One successful run at that exact SHA supplies exactly one successful canonical job and one successful sanitizer job; both safe artifacts are independently downloaded, hash-recomputed, and exact-ref validated without cross-run mixing."
    - "Runs 29439515367, 29583793056, 29625083184, and 29652578231 and artifacts 8423580554, 8431920189, and 8431922578 can never authorize Phase 9 promotion."
    - "The fresh authority proves 7 cases, exactly 58 unique semantic bindings, identical complete 22-policy arrays, retained rigid Match, canonical/sanitizer equality, safe paths, and all payload, binding, manifest, trace, API, and archive hashes."
    - "Only four scoped Phase 9 platform rows are promoted; all five Phase 10 rows remain not_evidenced."
  artifacts:
    - path: TESTING.md
      provides: "Immutable exact-ref authority, single-dispatch procedure, artifact identities, and maturity boundary"
    - path: reference/compatibility.json
      provides: "Exactly four Phase 9 platform claims bound to the fresh schema-v4 authority"
    - path: tools/xtask/src/inventory/validation.rs
      provides: "Fail-closed run/artifact authority allowlist and Phase 10 denial"
    - path: tools/xtask/tests/inventory_cli.rs
      provides: "Denylisted run/artifact, mixed-run, incomplete-proof, and Phase 10 promotion regressions"
    - path: COMPATIBILITY.md
      provides: "Deterministically generated public compatibility report"
    - path: .planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
      provides: "Fresh verifier verdict for both final Phase 9 gaps"
  key_links:
    - from: target/phase9-evidence/run.json
      to: reference/compatibility.json
      via: "independent GitHub API, archive, hash, schema-v4, and exact-ref validation before promotion"
      pattern: "run_id|head_sha|canonical|sanitizer"
    - from: tools/xtask/src/inventory/validation.rs
      to: reference/compatibility.json
      via: "exact fresh run/SHA/job/artifact/digest authority and denylist"
      pattern: "platform_validated|not_evidenced"
    - from: reference/compatibility.json
      to: COMPATIBILITY.md
      via: "inventory generate/check only"
      pattern: "@generated"
    - from: .planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
      to: .planning/STATE.md
      via: "fresh passed verifier result before phase tracking completion"
      pattern: "status: passed|Phase 9"
---

<objective>
Close G09-EXACT-REF-AUTHORITY with one immutable reviewed SHA, one dispatch, paired fresh artifacts, and a narrowly scoped compatibility promotion.

Purpose: Establish current platform authority without manual approval, retry, historical artifact reuse, mixed runs, Phase 10 leakage, or global policy changes.
Output: Fresh exact-ref evidence, four truthful Phase 9 platform promotions, generated reports, and a passed Phase 9 verifier/tracking state.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-30-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
@TESTING.md
@reference/compatibility.json
@tools/xtask/src/inventory/validation.rs
@tools/xtask/tests/inventory_cli.rs
@COMPATIBILITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seal one clean SHA and dispatch Oracle CI exactly once</name>
  <files>TESTING.md</files>
  <read-first>AGENTS.md exact Rust pre-commit order; 09-30-SUMMARY exact topology/schema result; current TESTING authority procedure; oracle.yml dispatch inputs without modifying the workflow; current local branch and origin branch.</read-first>
  <action>Apply the explicit user exception only to Phase 09: do not pause for manual SHA approval. Never edit GSD settings, approval policy, workflow configuration, repository rules, or any global/local policy file. Update TESTING.md with the reviewed schema-v4 single-dispatch protocol and all four denylisted run IDs plus all three denylisted artifact IDs, but do not claim a new run yet. Immediately before committing this pre-dispatch documentation, with no intervening file mutation, run exactly and in this order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Commit, require a clean worktree, record the full 40-character HEAD_SHA, push that exact commit once, fetch origin, and require local HEAD equals the remote branch head. Query GitHub before dispatch and require zero Oracle CI `workflow_dispatch` runs for HEAD_SHA with Phase 9 evidence; any existing dispatch blocks execution. Dispatch `.github/workflows/oracle.yml` exactly once on the branch whose remote head equals HEAD_SHA with `evidence_phase=phase9`. Do not retry the dispatch command under any failure or ambiguity. Re-query by API and require exactly one matching run; record its RUN_ID without changing the sealed SHA.</action>
  <verify><automated>test -z "$(git status --porcelain)" && HEAD_SHA=$(git rev-parse HEAD) && test ${#HEAD_SHA} -eq 40 && BRANCH=$(git branch --show-current) && REMOTE_SHA=$(git rev-parse "origin/$BRANCH") && test "$HEAD_SHA" = "$REMOTE_SHA" && MATCHING_RUNS=$(gh run list --workflow oracle.yml --event workflow_dispatch --limit 100 --json databaseId,headSha | jq --arg sha "$HEAD_SHA" '[.[] | select(.headSha==$sha)] | length') && test "$MATCHING_RUNS" -eq 1</automated></verify>
  <acceptance-criteria>The exact gated commit is clean and immutable locally/remotely, there were zero matching dispatches before the action, exactly one exists afterward, no retry occurred, and no approval/global policy/configuration file changed.</acceptance-criteria>
  <done>One and only one Phase 9 Oracle CI run is in flight for the exact reviewed schema-v4 SHA.</done>
</task>

<task type="auto">
  <name>Task 2: Independently acquire and validate the one-run canonical and sanitizer authority</name>
  <files>TESTING.md</files>
  <read-first>The sealed HEAD_SHA and sole RUN_ID from Task 1; live run, job, artifact, and workflow APIs; schema-v4 exact-ref validator; safe extraction contract; denylisted runs 29439515367, 29583793056, 29625083184, 29652578231 and artifacts 8423580554, 8431920189, 8431922578.</read-first>
  <action>Watch only the sole RUN_ID to terminal completion; never dispatch or retry another run. Require workflow `oracle.yml`, event `workflow_dispatch`, conclusion success, exact HEAD_SHA, and the branch still at the same local/remote SHA. Require exactly one successful canonical job and exactly one successful sanitizer job in that same run and SHA. Query artifact metadata independently; require one current canonical artifact and one current sanitizer artifact, both unexpired, from those jobs/run/SHA, and reject every denylisted run/artifact identity. Preserve any prior evidence only under a gitignored forensic directory, clear only the two fixed current download directories, inspect archive entry names and sizes before extraction, reject absolute/traversal paths, symlinks, hardlinks, duplicates, unbounded files, and unexpected entries, then extract each artifact into its own fixed directory. Never copy or combine files between artifacts or runs. Build run.json from independently queried API identities and recompute archive/API/payload/trace/binding/manifest hashes. Before exact-ref validation, use jq to require `.artifacts.canonical.id` and `.artifacts.sanitizer.id` are distinct and neither is 8423580554, 8431920189, or 8431922578, and require `.live_artifacts` to match those two recorded ID/digest pairs exactly. Then run exact-ref validation with all four denylisted run IDs; preserve denial of every stale artifact marker in inventory validation and its regression tests. Require schema-v4 exact role topology, 7 cases, exactly 58 unique semantic bindings, complete identical 22-policy arrays, retained rigid Match before particle comparison, positive collision-energy and nonempty stuck witnesses, passing logs, canonical/sanitizer byte and semantic equality, safe exact file sets, and every recomputed hash/identity. Only after success, update TESTING.md with immutable run/job/artifact URLs, exact SHA, verified counts/hashes, one-dispatch/no-retry fact, and independent-download procedure.</action>
  <verify><automated>RUN_ID=$(jq -r .run_id target/phase9-evidence/run.json) && HEAD_SHA=$(jq -r .head_sha target/phase9-evidence/run.json) && test "$RUN_ID" != 29439515367 && test "$RUN_ID" != 29583793056 && test "$RUN_ID" != 29625083184 && test "$RUN_ID" != 29652578231 && test "$HEAD_SHA" = "$(git rev-parse HEAD)" && gh run view "$RUN_ID" --json conclusion,headSha,event,jobs,workflowName | jq -e --arg sha "$HEAD_SHA" '.conclusion=="success" and .headSha==$sha and .event=="workflow_dispatch" and .workflowName=="Oracle CI" and ([.jobs[] | select(.name=="Canonical Linux oracle" and .conclusion=="success")] | length==1) and ([.jobs[] | select(.name=="Scheduled fail-fast sanitizer and reset corpus" and .conclusion=="success")] | length==1)' && jq -e '.artifacts.canonical as $canonical | .artifacts.sanitizer as $sanitizer | ($canonical.id != $sanitizer.id) and ([$canonical.id, $sanitizer.id] | all(. != 8423580554 and . != 8431920189 and . != 8431922578)) and (([.live_artifacts[] | {id, digest}] | sort_by(.id)) == ([{id: $canonical.id, digest: $canonical.digest}, {id: $sanitizer.id, digest: $sanitizer.digest}] | sort_by(.id)))' target/phase9-evidence/run.json && cargo xtask phase9-evidence validate --mode exact-ref --run-json target/phase9-evidence/run.json --canonical-dir target/phase9-evidence/phase9-canonical --sanitizer-dir target/phase9-evidence/phase9-sanitizer --deny-run-id 29439515367 --deny-run-id 29583793056 --deny-run-id 29625083184 --deny-run-id 29652578231 && cargo xtask provenance check</automated></verify>
  <acceptance-criteria>One successful run/SHA supplies both independently safe artifacts; exact-ref validation proves all counts, policies, retained rigid state, topology, equality, logs, and hashes; no denylisted or mixed-run material is accepted.</acceptance-criteria>
  <done>The sole fresh run is sufficient exact-ref authority and TESTING.md records only independently revalidated immutable facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Promote four Phase 9 rows, verify the full matrix, and finalize tracking</name>
  <files>TESTING.md, reference/compatibility.json, tools/xtask/src/inventory/validation.rs, tools/xtask/tests/inventory_cli.rs, COMPATIBILITY.md, .planning/phases/09-particle-storage-lifecycle-and-coupling/09-31-SUMMARY.md, .planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read-first>The independently validated Task 2 authority; exactly four currently pending Phase 9 platform rows and five Phase 10 rows; generated-report ownership; inventory authority tests; Phase 09 roadmap/tracking state; verifier gap truths.</read-first>
  <action>First add Arrange/Act/Assert inventory tests that reject each denylisted run 29439515367, 29583793056, 29625083184, and 29652578231; each denylisted artifact 8423580554, 8431920189, and 8431922578; any canonical/sanitizer mixed-run, mixed-SHA, mixed-job, or substituted pair; missing schema-v4 topology; baseline or forbidden alias topology; missing retained rigid comparator; non-Match retained outcome; wrong or incomplete 7-case/58-binding/22-policy data; failed logs; digest/hash mismatch; and promotion of any of the five Phase 10 rows. Pin validation to the fresh full SHA, sole RUN_ID, canonical/sanitizer job and artifact identities, API/archive/payload/trace/binding/manifest hashes, semantic manifest digest, TESTING anchor, and 09-31 summary. Promote platform evidence for exactly the four scoped Phase 9 rows and no other evidence dimension/row. Keep all five Phase 10 implementation, unit, differential, and platform fields `not_evidenced` with empty authority. Generate COMPATIBILITY.md only through `cargo xtask inventory generate`; generate/check twice and require byte identity. Run the complete Rust, targeted corpus/protocol/evidence/inventory, local canonical/sanitizer paired, exact-ref, provenance, dependency, schema-drift, actionlint, Markdown, upstream read-only, security, and diff matrix. Perform ASVS L1 review across GitHub identity, archive/path safety, JSONL bounds, shell injection, secrets, hashes, replay topology, and generated claims; high severity blocks. Run a fresh gsd-verifier goal-backward audit and require 09-VERIFICATION.md closes G09-PROOF-TOPOLOGY and G09-EXACT-REF-AUTHORITY with no regression or Phase 10 promotion. Update 09-31-SUMMARY.md, STATE.md, and ROADMAP.md through standard GSD tracking mechanics only after the passed verifier. Immediately before each commit, with no intervening file mutation, run exactly and in this order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Commit the ledger/report/test/docs changes, and separately commit verifier/summary/tracking changes only if the execution workflow requires it; rerun the exact ordered Rust gate immediately before each commit. Push the final commits once, fetch origin, require a clean worktree, and require final local HEAD equals the remote branch head. Never dispatch another oracle run.</action>
  <verify><automated>cargo test -p xtask --test inventory_cli && cargo test -p xtask --test phase9_evidence_cli && cargo test -p liquidfun-differential --test phase9_corpus && cargo test -p liquidfun-differential --test particle_oracle && cargo test -p liquidfun-differential --test particle_protocol && cargo xtask inventory generate && cargo xtask inventory check && cargo xtask inventory generate && cargo xtask inventory check && LIQUIDFUN_PHASE9_ORACLE_MODE=canonical LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/canonical/phase9-manifest.json bash scripts/phase9-evidence.sh canonical target/phase9-evidence-local/canonical && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 LIQUIDFUN_PHASE9_ORACLE_MODE=sanitizer LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/sanitizer/phase9-manifest.json bash scripts/phase9-evidence.sh sanitizer target/phase9-evidence-local/sanitizer && cargo xtask phase9-evidence validate --mode local --canonical-dir target/phase9-evidence-local/canonical --sanitizer-dir target/phase9-evidence-local/sanitizer --deny-run-id 29439515367 --deny-run-id 29583793056 --deny-run-id 29625083184 --deny-run-id 29652578231 && jq -e '.artifacts.canonical as $canonical | .artifacts.sanitizer as $sanitizer | ($canonical.id != $sanitizer.id) and ([$canonical.id, $sanitizer.id] | all(. != 8423580554 and . != 8431920189 and . != 8431922578)) and (([.live_artifacts[] | {id, digest}] | sort_by(.id)) == ([{id: $canonical.id, digest: $canonical.digest}, {id: $sanitizer.id, digest: $sanitizer.digest}] | sort_by(.id)))' target/phase9-evidence/run.json && cargo xtask phase9-evidence validate --mode exact-ref --run-json target/phase9-evidence/run.json --canonical-dir target/phase9-evidence/phase9-canonical --sanitizer-dir target/phase9-evidence/phase9-sanitizer --deny-run-id 29439515367 --deny-run-id 29583793056 --deny-run-id 29625083184 --deny-run-id 29652578231 && cargo xtask provenance check && cargo deny check && node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 09 | jq -e '.blocking==false' && actionlint .github/workflows/oracle.yml && just markdown-check && git diff --exit-code -- third_party/liquidfun && git diff --check && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && jq -e '["public-api.liquidfun-box2d-box2d-particle-b2particle-h","public-api.liquidfun-box2d-box2d-particle-b2particlesystem-h","subsystem.particle-contacts-and-coupling","subsystem.particle-storage-and-lifecycle"] as $phase9 | ([.entries[] | select(.id as $id | $phase9 | index($id))] | length) == 4 and all(.entries[] | select(.id as $id | $phase9 | index($id)); .evidence.platform_validated.status == "evidenced" and (.evidence.platform_validated.references | length > 0))' reference/compatibility.json && jq -e '["public-api.liquidfun-box2d-box2d-particle-b2particleassembly-h","public-api.liquidfun-box2d-box2d-particle-b2particlegroup-h","source-area.liquidfun-box2d-box2d-particle","subsystem.particle-groups-pairs-and-triads","subsystem.particle-solver-behaviors"] as $phase10 | ([.entries[] | select(.id as $id | $phase10 | index($id))] | length) == 5 and all(.entries[] | select(.id as $id | $phase10 | index($id)); ([.evidence.implemented, .evidence.unit_tested, .evidence.differentially_validated, .evidence.platform_validated] | all(.status == "not_evidenced" and .references == [])))' reference/compatibility.json && test "$(git rev-parse HEAD)" = "$(git rev-parse "origin/$(git branch --show-current)")" && test -z "$(git status --porcelain)"</automated></verify>
  <acceptance-criteria>Exactly four Phase 9 rows cite only the fresh validated authority; all five Phase 10 rows remain wholly not_evidenced; every denylist/mixing/topology regression passes; the full matrix and ASVS L1 pass; the fresh verifier closes both gaps; final local/remote SHA equality and cleanliness hold.</acceptance-criteria>
  <done>G09-EXACT-REF-AUTHORITY is closed, Phase 09 is verifier-passed and tracked complete, and no second dispatch or unsupported claim exists.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Local reviewed commit to remote dispatch | A mutable, unequal, dirty, or already-dispatched ref can break exact authority. | Mitigate with the exact Rust gate, clean full SHA, push/fetch equality, zero-before/one-after dispatch cardinality, and no retry. |
| GitHub run/jobs/artifacts to local evidence | Failed, stale, expired, denylisted, mixed-run, or unsafe archives can impersonate current evidence. | Mitigate with independent API queries, same-run/SHA job cardinality, explicit denysets, safe bounded extraction, and recomputed identities/hashes. |
| Exact-ref evidence to compatibility ledger | Incomplete particle-only proof or Phase 10 leakage can overstate maturity. | Mitigate with retained rigid Match, 7/58/22 exact counts, topology/equality checks, four-row allowlist, and five-row Phase 10 denial tests. |
| Workflow dispatch and credentials | Retries, secret exposure, or policy edits can bypass the reviewed procedure. | Mitigate with one command/one run, no retry, no secret output, Phase-09-only exception, and no workflow/global configuration mutation. |
| Generated report and tracking | Hand edits or premature completion can publish unsupported claims. | Mitigate with JSON authority, double idempotent generation, fresh verifier pass, exact pre-commit gates, and final remote equality. |

ASVS L1 is mandatory. Any high-severity finding blocks promotion and completion.
</threat-model>

<verification>
Require the exact ordered Rust gate immediately before every commit; clean local/remote SHA equality; zero-before and exactly-one-after dispatch cardinality; one successful same-run/SHA canonical and sanitizer pair; safe independent exact-ref validation; all denylist and mixed-run tests; four-row-only promotion; five Phase 10 rows wholly not_evidenced; double generated-report idempotence; full local/exact-ref/security matrix; fresh passed verifier; final push/equality.
</verification>

<success-criteria>
- One clean immutable SHA receives exactly one Phase 9 oracle.yml dispatch without manual approval or retry.
- The same successful run/SHA supplies independently validated safe canonical and sanitizer artifacts.
- All 7 cases, 58 bindings, 22 policies, retained rigid records, topology rules, equality checks, and hashes pass.
- All four denied runs and all three denied artifacts remain unusable, and mixed-run evidence fails closed.
- Exactly four Phase 9 platform rows are promoted; five Phase 10 rows remain wholly not_evidenced.
- Full matrix, ASVS L1, fresh verifier, tracking, final push, cleanliness, and local/remote equality all pass.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-31-SUMMARY.md`.</output>
