---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 16
type: execute
wave: 14
depends_on: [09-14]
files_modified: []
autonomous: false
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "A human confirms the exact published commit containing the Phase 9 workflow and corpus before dispatch."
    - "Canonical and sanitizer jobs succeed for that exact head SHA and their identity-bound artifacts are downloaded and validated."
  artifacts:
    - path: target/phase9-evidence/run.json
      provides: "Repository, branch, dispatch time, exact run/head, artifact names, and GitHub artifact digests"
    - path: target/phase9-evidence/phase9-canonical/identity.json
      provides: "Downloaded exact-ref canonical identity"
    - path: target/phase9-evidence/phase9-sanitizer/identity.json
      provides: "Downloaded exact-ref sanitizer identity"
  key_links:
    - from: .planning/phases/09-particle-storage-lifecycle-and-coupling/09-14-SUMMARY.md
      to: .github/workflows/oracle.yml
      via: "published exact commit confirmed before workflow_dispatch"
      pattern: "phase9"
---
<objective>
Publish and human-confirm the exact Phase 9 evidence commit, then dispatch, watch, download, and identity-check both remote evidence jobs.

Purpose: Bind canonical D1 and sanitizer evidence to the exact reviewed workflow/corpus commit before any compatibility promotion.
Output: Successful run ID, exact commit, downloaded artifacts, validated identities, and a summary consumed by 09-17.
</objective>
<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-14-SUMMARY.md
@.github/workflows/oracle.yml
@TESTING.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Publish and prove the exact evidence commit</name>
  <files></files>
  <read_first>09-14 summary and current branch/upstream status.</read_first>
  <action>Require a clean worktree and execute `REPO=$(gh repo view --json nameWithOwner --jq .nameWithOwner)`, `BRANCH=$(git branch --show-current)`, and `HEAD_SHA=$(git rev-parse HEAD)`. Confirm `HEAD_SHA` contains the 09-14 workflow/corpus changes, require a nonempty branch, push that branch without rewriting history using `git push origin "$BRANCH"`, then execute `REMOTE_SHA=$(git ls-remote --heads origin "refs/heads/$BRANCH" | awk 'NR == 1 { print $1 }')` and require `REMOTE_SHA == HEAD_SHA`. Also require the `origin` GitHub slug to equal `REPO`. Print repository, branch, full SHA, and `https://github.com/$REPO/commit/$HEAD_SHA` for human confirmation. Do not dispatch on any mismatch.</action>
  <verify><automated>REPO=$(gh repo view --json nameWithOwner --jq .nameWithOwner) && BRANCH=$(git branch --show-current) && HEAD_SHA=$(git rev-parse HEAD) && REMOTE_SHA=$(git ls-remote --heads origin "refs/heads/$BRANCH" | awk 'NR == 1 { print $1 }') && test -z "$(git status --porcelain)" && test -n "$REPO" && test -n "$BRANCH" && test "$REMOTE_SHA" = "$HEAD_SHA"</automated></verify>
  <acceptance_criteria>The reviewed Phase 9 workflow/corpus commit is immutable and reachable on the intended GitHub remote.</acceptance_criteria>
  <done>The reviewed Phase 9 workflow/corpus commit is immutable and reachable on the intended GitHub remote.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the exact published commit</name>
  <files></files>
  <action>Pause after printing the immutable commit URL and full SHA. Require the user to inspect that published commit and explicitly approve the exact SHA before dispatch can continue.</action>
  <what-built>The clean, published commit containing the Phase 9 corpus and canonical/sanitizer workflow jobs.</what-built>
  <how-to-verify>Open the printed immutable commit URL, verify the full SHA matches local HEAD and that the diff contains the intended Phase 9 workflow/corpus files only. Confirm this exact commit is approved as the evidence target.</how-to-verify>
  <verify><automated>BRANCH=$(git branch --show-current) && HEAD_SHA=$(git rev-parse HEAD) && REMOTE_SHA=$(git ls-remote --heads origin "refs/heads/$BRANCH" | awk 'NR == 1 { print $1 }') && test -z "$(git status --porcelain)" && test -n "$BRANCH" && test "$REMOTE_SHA" = "$HEAD_SHA"</automated></verify>
  <resume-signal>Type `approved <full-commit-sha>` to authorize exact-ref dispatch, or describe the mismatch.</resume-signal>
  <acceptance_criteria>The user explicitly approves the full published commit SHA and no different ref may be dispatched.</acceptance_criteria>
  <done>The user explicitly approves the full published commit SHA and no different ref may be dispatched.</done>
</task>
<task type="auto">
  <name>Task 3: Dispatch, watch, download, and validate exact-ref evidence</name>
  <files></files>
  <read_first>Human-approved full SHA; Phase 9 artifact/identity contract in TESTING.md and oracle.yml.</read_first>
  <action>Copy the full SHA from the human resume signal into `APPROVED_SHA` verbatim, then execute `REPO=$(gh repo view --json nameWithOwner --jq .nameWithOwner)`, `BRANCH=$(git branch --show-current)`, `HEAD_SHA=$(git rev-parse HEAD)`, and `REMOTE_SHA=$(git ls-remote --heads origin "refs/heads/$BRANCH" | awk 'NR == 1 { print $1 }')`; require `APPROVED_SHA == HEAD_SHA == REMOTE_SHA` and a clean worktree. GitHub workflow dispatch accepts a branch or tag, not a raw commit, so record `DISPATCHED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)` and run `gh workflow run oracle.yml -R "$REPO" --ref "$BRANCH" -f evidence_phase=phase9` only after the remote branch equality check. Poll `gh run list -R "$REPO" --workflow oracle.yml --event workflow_dispatch --branch "$BRANCH" --limit 30 --json databaseId,createdAt,event,headBranch,headSha,status,conclusion,workflowName` at most 30 times with two-second intervals. Filter with `jq` for `event == "workflow_dispatch"`, `workflowName == "Oracle CI"`, exact `headBranch`, exact `headSha`, and `createdAt >= DISPATCHED_AT`; require exactly one match, set `RUN_ID` from its `databaseId`, and run `gh run watch "$RUN_ID" -R "$REPO" --exit-status`.

Save `gh run view "$RUN_ID" -R "$REPO" --json conclusion,headSha,event,jobs,workflowName,url` as `target/phase9-evidence/run-view.json`. Require conclusion `success`, event `workflow_dispatch`, workflow `Oracle CI`, exact head SHA, exactly one successful `Canonical Linux oracle` job, and exactly one successful `Scheduled fail-fast sanitizer and reset corpus` job. Query `gh api "repos/$REPO/actions/runs/$RUN_ID/artifacts?per_page=100"` into `target/phase9-evidence/artifacts.json`; require exactly one unexpired artifact named `phase9-canonical-${RUN_ID}-${HEAD_SHA}` and exactly one named `phase9-sanitizer-${RUN_ID}-${HEAD_SHA}`, with each API `digest` matching `^sha256:[0-9a-f]{64}$`. Record repository, branch, dispatch timestamp, run ID/URL, head SHA, both artifact names, archive URLs, and GitHub artifact digests in `target/phase9-evidence/run.json`.

Delete only the two download directories, then download those exact names using `gh run download "$RUN_ID" -R "$REPO" -n "$CANONICAL_NAME" -D target/phase9-evidence/phase9-canonical` and the equivalent sanitizer command. For each `identity.json`, use `jq -e` to require the numeric run ID; exact head SHA; expected job; upstream `7f20402173fd143a3988c921bc384459c6a858f2`; target `x86_64-unknown-linux-gnu`; Rust 1.97.0; Clang 22.1.8; CMake 4.3.3; Ninja 1.13.2; policy `phase9-v1`; safe artifact-relative `trace.path`/`manifest.path` values; and lowercase 64-hex digests. Recompute both named file hashes from inside each artifact directory with `shasum -a 256 -c`. Missing, duplicate, expired, failed, unsafe-path, or mismatched evidence blocks completion.</action>
  <verify><automated>REPO=$(jq -r .repository target/phase9-evidence/run.json) && BRANCH=$(jq -r .branch target/phase9-evidence/run.json) && HEAD_SHA=$(jq -r .head_sha target/phase9-evidence/run.json) && RUN_ID=$(jq -r .run_id target/phase9-evidence/run.json) && CANONICAL_NAME=$(jq -r .artifacts.canonical.name target/phase9-evidence/run.json) && CANONICAL_DIGEST=$(jq -r .artifacts.canonical.digest target/phase9-evidence/run.json) && SANITIZER_NAME=$(jq -r .artifacts.sanitizer.name target/phase9-evidence/run.json) && SANITIZER_DIGEST=$(jq -r .artifacts.sanitizer.digest target/phase9-evidence/run.json) && test "$(git ls-remote --heads origin "refs/heads/$BRANCH" | awk 'NR == 1 { print $1 }')" = "$HEAD_SHA" && gh run view "$RUN_ID" -R "$REPO" --json conclusion,headSha,event,jobs,workflowName | jq -e --arg head_sha "$HEAD_SHA" '.conclusion == "success" and .headSha == $head_sha and .event == "workflow_dispatch" and .workflowName == "Oracle CI" and ([.jobs[] | select(.name == "Canonical Linux oracle" and .conclusion == "success")] | length == 1) and ([.jobs[] | select(.name == "Scheduled fail-fast sanitizer and reset corpus" and .conclusion == "success")] | length == 1)' && gh api "repos/$REPO/actions/runs/$RUN_ID/artifacts?per_page=100" | jq -e --arg canonical "$CANONICAL_NAME" --arg canonical_digest "$CANONICAL_DIGEST" --arg sanitizer "$SANITIZER_NAME" --arg sanitizer_digest "$SANITIZER_DIGEST" '([.artifacts[] | select(.name == $canonical and .expired == false and .digest == $canonical_digest and (.digest | test("^sha256:[0-9a-f]{64}$")))] | length == 1) and ([.artifacts[] | select(.name == $sanitizer and .expired == false and .digest == $sanitizer_digest and (.digest | test("^sha256:[0-9a-f]{64}$")))] | length == 1)' && for SPEC in "canonical:canonical-linux" "sanitizer:sanitizer-linux"; do KIND=${SPEC%%:*}; JOB=${SPEC#*:}; DIR="target/phase9-evidence/phase9-$KIND"; IDENTITY="$DIR/identity.json"; jq -e --argjson run_id "$RUN_ID" --arg head_sha "$HEAD_SHA" --arg job "$JOB" --arg upstream 7f20402173fd143a3988c921bc384459c6a858f2 '.run_id == $run_id and .head_sha == $head_sha and .job == $job and .upstream_revision == $upstream and .target == "x86_64-unknown-linux-gnu" and .rust == "1.97.0" and .clang == "22.1.8" and .cmake == "4.3.3" and .ninja == "1.13.2" and .policy_profile == "phase9-v1" and (.trace.path | test("^[A-Za-z0-9._/-]+$") and startswith("/") == false and contains("..") == false) and (.manifest.path | test("^[A-Za-z0-9._/-]+$") and startswith("/") == false and contains("..") == false) and (.trace.sha256 | test("^[0-9a-f]{64}$")) and (.manifest.sha256 | test("^[0-9a-f]{64}$"))' "$IDENTITY" && TRACE_CHECK=$(jq -r '.trace.sha256 + "  " + .trace.path' "$IDENTITY") && MANIFEST_CHECK=$(jq -r '.manifest.sha256 + "  " + .manifest.path' "$IDENTITY") && (cd "$DIR" && printf '%s\n%s\n' "$TRACE_CHECK" "$MANIFEST_CHECK" | shasum -a 256 -c -); done</automated></verify>
  <acceptance_criteria>Both jobs pass for the human-approved exact commit and downloaded identities/digests satisfy the full canonical contract.</acceptance_criteria>
  <done>Both jobs pass for the human-approved exact commit and downloaded identities/digests satisfy the full canonical contract.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Published commit and GitHub run to local evidence | Wrong refs, ambiguous runs, or substituted artifacts could impersonate canonical evidence. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-16-01 | S | workflow dispatch | mitigate | Human-confirm immutable full SHA and require run headSha equality. |
| T-09-16-02 | T | downloaded artifacts | mitigate | Exact names plus run/SHA/tool/upstream/policy/content digest validation. |
</threat-model>
<verification>
The published-ref check, human checkpoint, `gh run watch --exit-status`, both exact downloads, and all identity/digest checks must pass. This plan changes no tracked files and makes no compatibility promotion.
</verification>
<success-criteria>
- Human confirmation binds dispatch to the intended immutable commit.
- Canonical and sanitizer jobs both succeed for that exact SHA.
- Downloaded identity-bound evidence is ready for independent revalidation; otherwise the plan remains incomplete.
</success-criteria>
<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-16-SUMMARY.md` with repository, branch, dispatch timestamp, approved SHA, run ID/URL, job results, artifact names/URLs/GitHub digests, identity fields, and recomputed trace/manifest digests.</output>
