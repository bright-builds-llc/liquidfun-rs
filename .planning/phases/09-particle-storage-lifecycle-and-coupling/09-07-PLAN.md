---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 07
type: execute
wave: 7
depends_on: [09-05, 09-06]
files_modified:
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/world/particle_lifecycle.rs
  - crates/liquidfun/src/world/step.rs
  - crates/liquidfun/tests/particle_lifecycle.rs
autonomous: true
requirements: [PART-01, PART-02, PART-08, PART-14]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "World::step performs lifetime advancement and zombie compaction newest-system-first before the paused early return."
    - "Particle destruction occurrences share the authoritative rigid/particle lifecycle journal and precede ID invalidation."
  artifacts:
    - path: crates/liquidfun/src/world/particle_lifecycle.rs
      provides: "World effect adapter for lifetime, zombie, teardown, and shared journal ordering"
  key_links:
    - from: crates/liquidfun/src/world.rs
      to: crates/liquidfun/src/world/particle_lifecycle.rs
      via: "private world child declaration"
      pattern: "mod particle_lifecycle"
    - from: crates/liquidfun/src/world/step.rs
      to: crates/liquidfun/src/world/particle_lifecycle.rs
      via: "source-timed pre-pause particle lifecycle slice"
      pattern: "particle_lifecycle"
---

<objective>
Integrate lifetime/zombie/system teardown into the transactional World step and shared lifecycle journal.

Purpose: Make callback timing, pause behavior, rollback, and poisoning coherent with the established rigid contract.
Output: Source-timed lifecycle step slice and integration regression coverage.
</objective>
<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-05-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-06-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Place lifecycle maintenance in World::step</name>
  <files>crates/liquidfun/src/world/particle_lifecycle.rs, crates/liquidfun/src/world/step.rs</files>
  <read_first>Phase 9 D-11, D-14, D-15, D-18, D-21; existing World step transaction/rollback/poison envelope; 09-06 ordered effects.</read_first>
  <action>Declare the new World child module from world.rs and keep it behind the established public World surface. Add particle state to the same step backup/limit rollback envelope, traverse systems newest-first, advance lifetimes and compact zombies before checking pause, and append destruction occurrences directly into the existing cross-domain lifecycle journal at their returned source points. Preserve complete snapshots and callback-before-ID-invalidation. A paused system performs this maintenance but skips the later contact/coupling sub-iteration and fabricates no transitions. System teardown follows the same source-timed journal rather than reconstructing events afterward.</action>
  <verify><automated>cargo test -p liquidfun --test particle_lifecycle world_step</automated></verify>
  <acceptance_criteria>Step ordering, pause semantics, journal order, and rollback are observable and source-equivalent.</acceptance_criteria>
  <done>Step ordering, pause semantics, journal order, and rollback are observable and source-equivalent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Guard rollback, panic, and rigid regressions</name>
  <files>crates/liquidfun/tests/particle_lifecycle.rs</files>
  <read_first>Phase 8 panic poisoning and lifecycle tests; Phase 9 D-18/D-20/D-21.</read_first>
  <action>Add retry-equivalence tests for step-limit/capacity failure, paused expiration/zombie maintenance, requested/unrequested callbacks, explicit/system destruction, mixed rigid-particle occurrence order, immediate capacity compaction, and catch_unwind poisoning. Prove the lock is restored, unapplied commands are discarded, coherent state remains inspectable only under the established poison contract, and all rigid lifecycle regression tests stay unchanged.</action>
  <verify><automated>cargo test -p liquidfun --test particle_lifecycle && cargo test -p liquidfun rigid</automated></verify>
  <acceptance_criteria>Failure and panic cannot leave partial particle mutation, a locked world, reordered journal entries, or altered rigid behavior.</acceptance_criteria>
  <done>Failure and panic cannot leave partial particle mutation, a locked world, reordered journal entries, or altered rigid behavior.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Particle lifecycle effects to shared World transaction | Partial effects could corrupt rigid state or falsify callback order. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-07-01 | T | step transaction | mitigate | Snapshot particle state with rigid state and commit source-ordered effects once. |
| T-09-07-02 | D | hook panic | mitigate | Restore lock, discard commands, poison coherently, and resume unwind. |
</threat-model>
<verification>
Run `cargo check -p liquidfun --all-features` to verify child-module wiring.
Run lifecycle plus rigid regressions, then before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Diff-review the exact particle insertion point and shared journal projection.
</verification>
<success-criteria>
- Lifecycle maintenance is inside the normal World step and transaction.
- Pause does not freeze lifetime/zombie maintenance or fabricate contact events.
- Destruction occurrences remain source-timed across rigid and particle domains.
</success-criteria>
<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-07-SUMMARY.md`.</output>
