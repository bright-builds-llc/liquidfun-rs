---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 19
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - crates/liquidfun/src/particle/lifetime.rs
  - crates/liquidfun/src/particle/storage/permutation.rs
  - crates/liquidfun/src/particle/storage/permutation/tests.rs
  - crates/liquidfun/src/world/particle_object.rs
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/src/particle.rs
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/association.rs
  - crates/liquidfun/src/particle/editor.rs
  - crates/liquidfun/src/particle/view.rs
  - crates/liquidfun-differential/src/rigid_world/phase9.rs
  - crates/liquidfun/tests/object_model.rs
  - crates/liquidfun/tests/particle_body_contacts.rs
  - crates/liquidfun/tests/particle_buffers.rs
  - crates/liquidfun/tests/particle_contacts.rs
  - crates/liquidfun/tests/particle_creation_eviction.rs
  - crates/liquidfun/tests/particle_forces_statistics.rs
  - crates/liquidfun/tests/particle_identity.rs
  - crates/liquidfun/tests/particle_lifecycle.rs
  - crates/liquidfun/tests/particle_lifetimes.rs
  - crates/liquidfun/tests/particle_objects.rs
  - crates/liquidfun/tests/particle_permutation_coherence.rs
  - crates/liquidfun/tests/particle_queries.rs
  - crates/liquidfun/tests/particle_views.rs
autonomous: true
requirements: [API-09, PART-02, PART-04, PART-05, PART-08, PART-14]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T23:45:08Z
must_haves:
  truths:
    - "Both public particle-creation methods return one owned, must-use receipt containing the created ParticleId and the committed capacity eviction's ordered destruction occurrences."
    - "Capacity eviction returns the pre-existing requested destruction-listener occurrence synchronously and exactly once; no hidden queue or later step redelivers it."
    - "Every committed compaction or rotation publishes weights recomputed from its retained remapped particle/body contacts."
    - "Creation, eviction, and permutation failures leave identities, supplied allocations, lanes, contacts, weights, and occurrences unchanged."
  artifacts:
    - path: crates/liquidfun/src/world/particle_object.rs
      provides: "Owned ParticleCreationReceipt definition and checked creation transaction carrying exact immediate eviction occurrences"
    - path: crates/liquidfun/src/world/object.rs
      provides: "Default-definition creation convenience wrapper migrated to the receipt contract"
    - path: crates/liquidfun/src/particle.rs
      provides: "ParticleCreationReceipt public particle-module export"
    - path: crates/liquidfun/src/lib.rs
      provides: "ParticleCreationReceipt crate-root export"
    - path: crates/liquidfun/src/particle/storage/permutation.rs
      provides: "Contact/weight-coherent permutation candidate"
    - path: crates/liquidfun/tests/particle_creation_eviction.rs
      provides: "Listener-flagged oldest eviction exactly-once regression"
  key_links:
    - from: crates/liquidfun/src/particle/lifetime.rs
      to: crates/liquidfun/src/world/particle_object.rs
      via: "committed ParticleCompactionOutcome carried into creation receipt"
      pattern: "prepare_capacity_for_creation|requested_listener_occurrences"
    - from: crates/liquidfun/src/world/object.rs
      to: crates/liquidfun/src/world/particle_object.rs
      via: "create_particle delegates to create_particle_with_def without projecting away its receipt"
      pattern: "create_particle|create_particle_with_def|ParticleCreationReceipt"
    - from: crates/liquidfun/src/world/particle_object.rs
      to: crates/liquidfun/src/particle.rs
      via: "particle-domain re-export of the owned public receipt"
      pattern: "ParticleCreationReceipt"
    - from: crates/liquidfun/src/particle.rs
      to: crates/liquidfun/src/lib.rs
      via: "crate-root re-export used by downstream callers"
      pattern: "ParticleCreationReceipt"
    - from: crates/liquidfun/src/particle/storage/permutation.rs
      to: crates/liquidfun/src/particle/storage.rs
      via: "remapped contacts followed by source-equivalent weight recomputation before commit"
      pattern: "particle_contacts|body_contacts|weights"
---

<objective>
Close immediate capacity-eviction occurrence loss and permutation-derived weight incoherence.

Purpose: Preserve the locked callback-before-invalidation contract and guarantee that aggregate views never expose retained contacts beside stale weights.
Output: An explicit creation receipt, exactly-once eviction evidence, and contact-coherent permutation commits.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-REVIEW.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-06-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry immediate eviction occurrences through particle creation</name>
  <files>crates/liquidfun/src/particle/lifetime.rs, crates/liquidfun/src/world/particle_object.rs, crates/liquidfun/src/world/object.rs, crates/liquidfun/src/particle.rs, crates/liquidfun/src/lib.rs, crates/liquidfun/src/association.rs, crates/liquidfun/src/particle/editor.rs, crates/liquidfun/src/particle/view.rs, crates/liquidfun-differential/src/rigid_world/phase9.rs, crates/liquidfun/tests/object_model.rs, crates/liquidfun/tests/particle_body_contacts.rs, crates/liquidfun/tests/particle_buffers.rs, crates/liquidfun/tests/particle_contacts.rs, crates/liquidfun/tests/particle_creation_eviction.rs, crates/liquidfun/tests/particle_forces_statistics.rs, crates/liquidfun/tests/particle_identity.rs, crates/liquidfun/tests/particle_lifecycle.rs, crates/liquidfun/tests/particle_lifetimes.rs, crates/liquidfun/tests/particle_objects.rs, crates/liquidfun/tests/particle_queries.rs, crates/liquidfun/tests/particle_views.rs</files>
  <read-first>Run `rg -n 'create_particle(_with_def)?\(' crates --glob '*.rs'` and retain the complete before-edit inventory. Then read crates/liquidfun/src/particle/lifetime.rs `ParticleCompactionOutcome` and `prepare_capacity_for_creation`; crates/liquidfun/src/world/particle_object.rs `create_particle_with_def`, `commit_preflighted_particle`, and particle snapshot conversion; crates/liquidfun/src/world/object.rs `create_particle` plus its unit-test callers; crates/liquidfun/src/particle.rs and crates/liquidfun/src/lib.rs export lists; crates/liquidfun/src/association.rs; the rustdoc callers in crates/liquidfun/src/particle/editor.rs and crates/liquidfun/src/particle/view.rs; crates/liquidfun-differential/src/rigid_world/phase9.rs creation executor; and every integration caller in crates/liquidfun/tests/object_model.rs, particle_body_contacts.rs, particle_buffers.rs, particle_contacts.rs, particle_forces_statistics.rs, particle_identity.rs, particle_lifecycle.rs, particle_lifetimes.rs, particle_objects.rs, particle_queries.rs, and particle_views.rs. Also read crates/liquidfun/src/world/particle_lifecycle.rs occurrence-to-record projection; pinned `CreateParticle`, `DestroyOldestParticle`, and `SolveZombie`; Phase 9 D-11/D-12/D-18; 09-REVIEW WR-03; and 09-VERIFICATION G09-EVICTION-OCCURRENCE.</read-first>
  <action>Write the failing full-system regression first: a maximum-one destroy-by-age system contains an oldest live particle whose existing flags include `DESTRUCTION_LISTENER`, and creating its replacement evicts it. Make an explicit in-repository breaking migration. Define `#[must_use = "particle creation can synchronously evict particles; inspect destruction_occurrences"] pub struct ParticleCreationReceipt` in crates/liquidfun/src/world/particle_object.rs as an owned value with private fields and exact public accessors `created_particle(&self) -> ParticleId` and `destruction_occurrences(&self) -> &[ParticleDestructionOccurrence]`; derive the ordinary owned debug/equality traits needed by tests. Publicly re-export it from crates/liquidfun/src/particle.rs and crates/liquidfun/src/lib.rs; do not add a duplicate world-domain definition or hidden side channel. Change both `World::create_particle_with_def` in crates/liquidfun/src/world/particle_object.rs and its `World::create_particle` wrapper in crates/liquidfun/src/world/object.rs to return `Result<ParticleCreationReceipt, CreateObjectError>`. The cloned preflight remains validation-only and publishes nothing. During immediate commit, retain only the real `ParticleCompactionOutcome` returned by `prepare_capacity_for_creation`, move its already source-ordered `requested_listener_occurrences` into the receipt, then create and initialize the replacement and set `created_particle`; never synthesize occurrences from the clone, queue them on `World`, defer them to `World::step`, or deliver them through a later callback. Mechanically migrate every match from the initial `rg` inventory: production/unit-test callers in world/object.rs and association.rs; rustdoc examples in particle/editor.rs and particle/view.rs; the differential executor in liquidfun-differential/src/rigid_world/phase9.rs; and the integration callers in object_model.rs, particle_body_contacts.rs, particle_buffers.rs, particle_contacts.rs, particle_forces_statistics.rs, particle_identity.rs, particle_lifecycle.rs, particle_lifetimes.rs, particle_objects.rs, particle_queries.rs, and particle_views.rs, plus the new particle_creation_eviction.rs regression. Callers that need identity must explicitly obtain `receipt.created_particle()`; lifecycle/eviction assertions must inspect `receipt.destruction_occurrences()` in order, and existing non-evicting setup helpers should explicitly establish or assert the expected empty occurrence contract where that is the behavior under test. Rerun the exact `rg -n 'create_particle(_with_def)?\(' crates --glob '*.rs'` inventory after migration and account for every remaining match, including wrappers, helper definitions, helper invocations, doctests, and the new regression. Prove the synchronous successful receipt contains the listener-flagged victim exactly once even though that ID is stale by return; the replacement ID is live; repeated receipt inspection and the next eligible step produce no additional occurrence; unrequested eviction produces zero; and failed replacement creation produces neither mutation nor occurrence.</action>
  <verify><automated>rg -n 'create_particle(_with_def)?\(' crates --glob '*.rs' && cargo test -p liquidfun --test particle_creation_eviction && cargo test -p liquidfun --test particle_objects creation && cargo test --all-features</automated></verify>
  <acceptance-criteria>`ParticleCreationReceipt` is defined once in world/particle_object.rs, exported through particle.rs and lib.rs, and both public creation methods return it. Its `created_particle()` accessor yields the committed live replacement and `destruction_occurrences()` yields only the real commit's source-ordered occurrences. The before/after `rg` inventories are reconciled with no caller still typed as `ParticleId` without an explicit receipt projection. The listener-flagged victim appears exactly once in the synchronous successful receipt; no queue, callback registration, or later step repeats it; unrequested and failed paths contain no occurrence; and `cargo test --all-features` passes.</acceptance-criteria>
  <done>The call inventory is fully reconciled and all automated tests exit zero, proving the single exported receipt API returns the live replacement and only committed, synchronous, source-ordered eviction occurrences exactly once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recompute weights from remapped contacts before permutation commit</name>
  <files>crates/liquidfun/src/particle/storage/permutation.rs, crates/liquidfun/src/particle/storage/permutation/tests.rs, crates/liquidfun/tests/particle_permutation_coherence.rs</files>
  <read-first>crates/liquidfun/src/particle/storage.rs `recompute_weights`; crates/liquidfun/src/particle/storage/permutation.rs candidate preparation/commit; crates/liquidfun/src/particle/storage/permutation/tests.rs; crates/liquidfun/src/particle/view.rs; Phase 9 D-03/D-04; 09-REVIEW WR-04; 09-VERIFICATION G09-PERMUTATION-WEIGHTS.</read-first>
  <action>Keep the single validate-prepare-commit authority. After remapping all surviving particle and body contacts into candidate row indices, compute each candidate weight from exactly those remapped contact records using the same source addition order documented by `ParticleStorage::recompute_weights`; do not copy stale old weights and do not publish zero placeholders beside retained contacts. Add one compaction case that removes a contacted middle row and one rotation case that reorders every contacted row; in each, compare stable-ID contacts and per-particle weights before/after. Add an invalid-permutation case proving contacts and weights remain unchanged byte-for-byte/semantically.</action>
  <verify><automated>cargo test -p liquidfun particle::storage::permutation::tests -- weights && cargo test -p liquidfun --test particle_permutation_coherence</automated></verify>
  <acceptance-criteria>For every survivor, published weight equals the ordered sum of retained particle-contact and body-contact contributions immediately after compaction/rotation; no valid contact-bearing permutation publishes all-zero weights; invalid maps have no effect.</acceptance-criteria>
  <done>Both weight/permutation test commands exit zero and prove compaction and rotation publish source-ordered contact-derived weights while invalid maps leave contacts and weights unchanged.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Public creation input to capacity eviction | A crafted full-capacity request could duplicate/drop callbacks or partially replace state. | Candidate validation plus one committed receipt; occurrence cardinality tests. |
| Internal permutation map to semantic views | Invalid indices could corrupt identities, contacts, or derived weights. | Total-map validation, bounded vectors, candidate-only accumulation, one commit. |
| JSONL/process oracle | No decoder changes occur here; native adapter call sites only project the checked receipt. | Bounded protocol validation remains in the later protocol plan. |
| Artifact/workflow integrity | Runtime tests cannot promote compatibility. | New exact-ref evidence remains mandatory after all gap plans. |

Resource use remains bounded by the declared Phase 9 capacity and contact limits. No new unsafe code, filesystem input, network path, secret, or high-severity ASVS L1 threat is introduced.
</threat-model>

<verification>
Run the creation-call inventory before and after the breaking migration, the focused lifecycle/permutation tests, the full all-features suite, and allocation-preservation regressions. Before every implementation commit run exactly in this order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Review that the receipt is synchronous and owns only committed outcomes, every caller/export is migrated, and no hidden lifecycle queue, delayed callback, pair/triad generation, or solver behavior was added.
</verification>

<success-criteria>
- G09-EVICTION-OCCURRENCE and WR-03 are closed with a full-system exactly-once receipt regression.
- The receipt API is publicly exported, both creation entrypoints and every Rust caller compile against it, and the reconciled before/after call inventory plus `cargo test --all-features` prove the breaking migration is complete.
- G09-PERMUTATION-WEIGHTS and WR-04 are closed for both compaction and rotation.
- Fixed/growable capacity transactionality, owned allocation identity, stable IDs, and invalid-permutation no-effect guarantees remain green.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-19-SUMMARY.md`.</output>
