---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 30
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - crates/liquidfun-differential/src/rigid_world/phase9/evidence.rs
  - crates/liquidfun-differential/tests/phase9_corpus.rs
  - tools/xtask/src/phase9_evidence.rs
  - tools/xtask/tests/phase9_evidence_cli.rs
autonomous: true
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-18T21:00:00Z
must_haves:
  truths:
    - "Every cross-run proof payload has an exact normalized role path below cases/<case-id>/proofs/ and no proof role can reference a baseline request, result, or comparison path."
    - "Replay-native/replay-oracle, debug/release, and minimized/copied are independently persisted distinct pairs; the only permitted cross-role path reuse is replay with its corresponding D0 role and minimized or copied with its corresponding first-divergence role."
    - "Digest- and identity-recomputed mutations cannot disguise baseline substitution, any required-distinct pair alias, or a semantically wrong first-divergence path."
    - "Schema-v4 local canonical and sanitizer evidence contains 7 cases, 58 semantic bindings, 22 policies, retained rigid comparison, and identical paired semantics."
    - "Production physics, public APIs, CMake inputs, and the oracle workflow remain unchanged."
  artifacts:
    - path: crates/liquidfun-differential/src/rigid_world/phase9/evidence.rs
      provides: "Schema-v4 typed proof-role topology and fail-closed semantic validation"
    - path: crates/liquidfun-differential/tests/phase9_corpus.rs
      provides: "Canonical schema-v4 proof payload generation under each case proof directory"
    - path: tools/xtask/src/phase9_evidence.rs
      provides: "Shared local and exact-ref proof-path normalization and relationship enforcement"
    - path: tools/xtask/tests/phase9_evidence_cli.rs
      provides: "Digest- and identity-recomputed topology corruption regressions"
  key_links:
    - from: crates/liquidfun-differential/tests/phase9_corpus.rs
      to: crates/liquidfun-differential/src/rigid_world/phase9/evidence.rs
      via: "schema-v4 role paths and proof identities"
      pattern: "proofs/|cross_run_proofs"
    - from: tools/xtask/src/phase9_evidence.rs
      to: crates/liquidfun-differential/src/rigid_world/phase9/evidence.rs
      via: "shared topology validation before exact-file-set deduplication"
      pattern: "validate.*proof|proof.*path"
    - from: tools/xtask/tests/phase9_evidence_cli.rs
      to: tools/xtask/src/phase9_evidence.rs
      via: "recomputed adversarial manifests"
      pattern: "baseline|alias|first.divergence"
---

<objective>
Close G09-PROOF-TOPOLOGY by making independently persisted proof roles structurally unforgeable.

Purpose: Ensure valid bytes and recomputed digests cannot impersonate independent replay, build-mode, minimization, or divergence proof.
Output: A schema-v4 generator/validator contract, adversarial topology regressions, and fresh paired local evidence.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
@crates/liquidfun-differential/src/rigid_world/phase9/evidence.rs
@crates/liquidfun-differential/tests/phase9_corpus.rs
@tools/xtask/src/phase9_evidence.rs
@tools/xtask/tests/phase9_evidence_cli.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify schema-v4 proof-role topology with adversarial regressions</name>
  <files>crates/liquidfun-differential/src/rigid_world/phase9/evidence.rs, tools/xtask/tests/phase9_evidence_cli.rs</files>
  <read-first>The schema-v3 cross-run proof types and evaluator; the CLI mutation helpers that recompute payload digests, manifest digests, and artifact identity; G09-PROOF-TOPOLOGY in 09-VERIFICATION.md.</read-first>
  <action>Write failing Arrange/Act/Assert tests before implementation. Bump the evidence schema contract from v3 to v4 and define one normalized logical-role topology rooted exactly at `cases/<case-id>/proofs/`. Require the independent persisted filenames `replay-native.json`, `replay-oracle.json`, `debug.json`, `release.json`, `minimized.json`, and `copied.json`; resolve D0 roles only through their corresponding replay-native/replay-oracle payload and resolve first-divergence only through the semantically designated minimized or copied payload. Normalize separators and dot components before comparison, reject absolute paths, parent traversal, noncanonical spellings, wrong case IDs, and every manifest baseline request/result/comparison path even when bytes are semantically valid. Encode an explicit equality allowlist: replay↔corresponding-D0 and minimized-or-copied↔corresponding-first-divergence only. Encode explicit inequality requirements for replay-native↔replay-oracle, debug↔release, and minimized↔copied; all other unlisted cross-role equality fails closed. Add digest- and identity-recomputed negative tests for baseline substitution, replay-native/replay-oracle aliasing, debug/release aliasing, minimized/copied aliasing, and a semantic-path-only first-divergence mutation whose referenced bytes, payload digests, manifest digest, and artifact identity are all recomputed. Also test that the two allowed reuse families pass and that the same bytes at distinct required paths still satisfy independent persistence. Do not weaken semantic predicates or exact-file checking.</action>
  <verify><automated>cargo test -p xtask --test phase9_evidence_cli -- proof_topology</automated></verify>
  <acceptance-criteria>Each forbidden mutation reaches topology validation with internally consistent digests/identity and is rejected for its exact structural reason; only the two reviewed reuse families pass.</acceptance-criteria>
  <done>Schema-v4 topology tests fail before implementation and pass only when normalized role paths, baseline denial, pair distinctness, and the equality allowlist are enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate and validate canonical schema-v4 proof paths</name>
  <files>crates/liquidfun-differential/src/rigid_world/phase9/evidence.rs, crates/liquidfun-differential/tests/phase9_corpus.rs, tools/xtask/src/phase9_evidence.rs, tools/xtask/tests/phase9_evidence_cli.rs</files>
  <read-first>The failing Task 1 contract; current proof writer and `cross_run_payload_refs`; exact-file-set construction; local/exact-ref shared validation path; all existing schema/content mutation tests.</read-first>
  <action>Implement the schema-v4 role-path contract in the shared evidence evaluator and call it from xtask before referenced-path collection or exact-file-set deduplication. Update corpus generation so every case persists the six independent payloads at the exact canonical filenames under that case's `proofs/` directory and records only the reviewed D0/replay and first-divergence/minimized-or-copied reuse. Keep payload semantic evaluation, digest recomputation, stable identities, retained rigid comparison, 7-case/58-binding/22-policy counts, and canonical/sanitizer agreement intact. Reject schema v3 and unknown schema versions with an actionable regeneration error. Run the full evidence CLI and Phase 9 corpus suites, including all existing corruption tests, to prove the topology check is shared by local and exact-ref validation. Make no production-physics, public-API, CMake, oracle source, workflow, or global policy/configuration change.</action>
  <verify><automated>cargo test -p xtask --test phase9_evidence_cli && cargo test -p liquidfun-differential --test phase9_corpus</automated></verify>
  <acceptance-criteria>The generator emits only canonical schema-v4 role paths, validation occurs before deduplication, all old and new corruption tests pass, and no unrelated subsystem or workflow file changes.</acceptance-criteria>
  <done>Both shared validation modes fail closed on invalid topology while the complete native Phase 9 corpus remains green.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild paired local evidence and pass the complete closure gate</name>
  <files>crates/liquidfun-differential/src/rigid_world/phase9/evidence.rs, crates/liquidfun-differential/tests/phase9_corpus.rs, tools/xtask/src/phase9_evidence.rs, tools/xtask/tests/phase9_evidence_cli.rs</files>
  <read-first>AGENTS.md exact Rust pre-commit order; the completed schema-v4 contract; Phase 9 local evidence commands and denylisted historical authority in 09-VERIFICATION.md.</read-first>
  <action>Build debug and release oracle presets without modifying their CMake inputs. Generate a fresh canonical schema-v4 artifact, build the ASan/UBSan preset and protocol target, run fail-fast protocol tests, then generate a fresh sanitizer schema-v4 artifact. Validate the pair locally with all four stale/failed runs denylisted: 29439515367, 29583793056, 29625083184, and 29652578231. Require 7 cases, exactly 58 unique semantic bindings, identical complete 22-policy arrays, retained rigid Match before particle comparison, safe exact file sets, canonical role topology, recomputed hashes, and byte/semantic agreement. Run targeted corpus/CLI, inventory, provenance, dependency, schema-drift, workflow lint, Markdown, upstream read-only, and diff checks. Perform an ASVS L1 review of path traversal, symlink/archive escape, resource bounds, JSONL validation, shell arguments, and identity/digest binding; any high-severity finding blocks completion. Review the diff for simpler fail-closed topology logic and verify no production physics/public API/CMake/oracle workflow/global configuration change. Immediately before each commit, with no intervening file mutation, run exactly and in this order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Commit the topology implementation only after that contiguous gate passes. If the standard summary is committed separately, rerun the same exact ordered Rust gate immediately before that commit.</action>
  <verify><automated>cargo test -p xtask --test phase9_evidence_cli && cargo test -p liquidfun-differential --test phase9_corpus && cargo xtask upstream configure --preset oracle-debug && cargo xtask upstream build --preset oracle-debug && cargo xtask upstream configure --preset oracle-release && cargo xtask upstream build --preset oracle-release && LIQUIDFUN_PHASE9_ORACLE_MODE=canonical LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/canonical/phase9-manifest.json bash scripts/phase9-evidence.sh canonical target/phase9-evidence-local/canonical && cargo xtask upstream configure --preset oracle-asan-ubsan && cargo xtask upstream build --preset oracle-asan-ubsan && cmake --build target/reference/oracle-asan-ubsan --target liquidfun-reference-protocol-tests && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 ctest --test-dir target/reference/oracle-asan-ubsan --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$' && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 LIQUIDFUN_PHASE9_ORACLE_MODE=sanitizer LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/sanitizer/phase9-manifest.json bash scripts/phase9-evidence.sh sanitizer target/phase9-evidence-local/sanitizer && cargo xtask phase9-evidence validate --mode local --canonical-dir target/phase9-evidence-local/canonical --sanitizer-dir target/phase9-evidence-local/sanitizer --deny-run-id 29439515367 --deny-run-id 29583793056 --deny-run-id 29625083184 --deny-run-id 29652578231 && cargo xtask inventory check && cargo xtask provenance check && cargo deny check && node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 09 | jq -e '.blocking==false' && actionlint .github/workflows/oracle.yml && just markdown-check && git diff --exit-code -- third_party/liquidfun && git diff --check && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>All commands pass; fresh local canonical and sanitizer schema-v4 artifacts agree; topology attacks fail despite recomputed integrity metadata; ASVS L1 has no high finding; the final diff is limited to the four declared evidence files.</acceptance-criteria>
  <done>G09-PROOF-TOPOLOGY is closed locally and the reviewed schema-v4 implementation is ready for one fresh exact-ref authority run.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Manifest proof path to repository artifact | Traversal, absolute paths, baseline substitution, or aliases can impersonate independent persistence. | Mitigate with normalized case-rooted exact role paths, baseline denial, an equality allowlist, required pair inequality, and pre-deduplication validation. |
| Proof bytes to manifest identity | An attacker can repair digests after redirecting a semantic role. | Mitigate with typed role/path topology and digest- plus identity-recomputed adversarial tests. |
| JSONL/process evidence | Malformed or unbounded input can affect native tooling. | Mitigate with closed schemas, bounded records, hard failures, timeouts, and ASan/UBSan execution. |
| Local output paths and shell arguments | Unsafe paths or symlinks can escape the evidence root or inject commands. | Mitigate with fixed arguments, canonical containment, exact safe file sets, symlink rejection, and no secret output. |

ASVS L1 is mandatory. Any high-severity finding blocks completion.
</threat-model>

<verification>
Run topology-focused and full mutation suites, the seven-case corpus, debug/release canonical generation, fail-fast sanitizer generation, paired local validation, inventory/provenance/dependency/schema/workflow/Markdown/read-only gates, and the exact ordered Rust gate immediately before every commit.
</verification>

<success-criteria>
- Schema-v4 enforces canonical case-local proof-role paths before reference deduplication.
- Baseline substitution and every forbidden alias fail even after all digests and artifact identity are recomputed.
- Only replay↔corresponding-D0 and minimized-or-copied↔corresponding-first-divergence reuse is accepted.
- Fresh local canonical and sanitizer evidence agrees across 7 cases, 58 bindings, 22 policies, and retained rigid comparison.
- No production physics, public API, CMake, oracle workflow, or global policy/configuration file changes.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-30-SUMMARY.md`.</output>
