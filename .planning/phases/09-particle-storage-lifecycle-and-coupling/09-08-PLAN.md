---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 08
type: execute
wave: 7
depends_on: [09-05, 09-06]
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/particle.rs
  - crates/liquidfun/src/particle/proxy.rs
  - crates/liquidfun/src/particle/contact.rs
  - crates/liquidfun/tests/particle_contacts.rs
autonomous: true
requirements: [PART-07, PART-15]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "Proxy tags, sorting, neighborhoods, and particle contacts preserve pinned order and stable semantic identity."
    - "Particle filter/listener flags gate decisions and begin/end occurrences at the pinned source points."
  artifacts:
    - path: crates/liquidfun/src/particle/proxy.rs
      provides: "Source-faithful proxy tags, sorting, bounds, and neighborhood enumeration"
    - path: crates/liquidfun/src/particle/contact.rs
      provides: "Particle contact generation, filtering, listener diff, and semantic records"
  key_links:
    - from: crates/liquidfun/src/lib.rs
      to: crates/liquidfun/src/particle.rs
      via: "public particle child-module re-exports"
      pattern: "pub use"
    - from: crates/liquidfun/src/particle/contact.rs
      to: crates/liquidfun/src/particle/proxy.rs
      via: "ordered neighborhood candidates"
      pattern: "neighborhood|proxy"
---

<objective>
Implement particle spatial proxies, neighborhood enumeration, particle contacts, and flag-gated contact decisions/events.

Purpose: Establish the first half of the pinned per-sub-iteration contact pipeline as pure, testable kernels.
Output: Proxy/contact modules and ordering/filter/listener tests.
</objective>
<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-05-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-06-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-RESEARCH.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Port proxy tags and ordered neighborhood generation</name>
  <files>crates/liquidfun/src/particle/proxy.rs, crates/liquidfun/tests/particle_contacts.rs</files>
  <read_first>Pinned proxy tag/bounds/enumeration routines cited by research; Phase 9 D-03, D-05, D-16.</read_first>
  <action>Declare the new particle child module from particle.rs, re-export supported public types through particle.rs and lib.rs, and prove external-crate reachability. Implement finite checked proxy-tag construction, source-equivalent sort/tie behavior, per-system bounds, and deterministic candidate enumeration over private rows. Return stable ParticleId pairs at public/evidence seams. Route proxy reorders through the authoritative permutation/reference remap rules and add focused edge/cell/property tests.</action>
  <verify><automated>cargo test -p liquidfun --test particle_contacts proxy</automated></verify>
  <acceptance_criteria>Candidate enumeration and ordering match source-derived fixtures without exposing row tags as public identity.</acceptance_criteria>
  <done>Candidate enumeration and ordering match source-derived fixtures without exposing row tags as public identity.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Generate, filter, and diff particle contacts</name>
  <files>crates/liquidfun/src/particle/contact.rs, crates/liquidfun/tests/particle_contacts.rs</files>
  <read_first>Phase 9 D-16, D-17, D-19; pinned UpdateContacts snapshot/generate/sort/filter/diff sequence.</read_first>
  <action>Generate semantic particle contacts from ordered candidates, compute source fields, sort in pinned order, invoke a borrowed decision adapter only when filter flags require it, and diff listener-visible old/new contacts only when listener flags require it. Preserve repeated occurrences, exact multiplicity, begin/end source order, and stable IDs. Return ordered decisions/effects for later World hook/journal wiring; do not register persistent callbacks or generate Phase 10 pairs/triads.</action>
  <verify><automated>cargo test -p liquidfun --test particle_contacts contact</automated></verify>
  <acceptance_criteria>Flag gates, filter decisions, contact ordering, and begin/end diffs are individually covered and stable-ID based.</acceptance_criteria>
  <done>Flag gates, filter decisions, contact ordering, and begin/end diffs are individually covered and stable-ID based.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Spatial candidates to callback-visible contacts | Invalid math or row leakage could corrupt identity and callback decisions. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-08-01 | T | proxy tags | mitigate | Finite checked calculations and source-order fixtures/property tests. |
| T-09-08-02 | R | listener diff | mitigate | Snapshot and emit ordered multiplicity at the pinned source point. |
</threat-model>
<verification>
Run `cargo check -p liquidfun --all-features` to verify entrypoint wiring.
Run proxy/contact tests, then before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Review that no persistent callback or Phase 10 generation path was added.
</verification>
<success-criteria>
- Proxy/neighborhood/contact ordering is source-faithful.
- Listener/filter flags gate exactly the intended work.
- All public/evidence contacts use stable IDs and preserve multiplicity.
</success-criteria>
<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-08-SUMMARY.md`.</output>
