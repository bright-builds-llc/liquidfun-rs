---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 09
type: execute
wave: 8
depends_on: [09-07, 09-08, 09-15]
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/particle.rs
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/particle/body_contact.rs
  - crates/liquidfun/src/world/particle_coupling.rs
  - crates/liquidfun/src/world/step.rs
  - crates/liquidfun/tests/particle_body_contacts.rs
autonomous: true
requirements: [PART-07, PART-15]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "Fixture/body contacts carry stable identities, weight, normal, and effective mass in pinned order."
    - "Strict pruning, stuck tracking, filter/listener gates, and scoped rigid reaction run at the pinned World::step location."
  artifacts:
    - path: crates/liquidfun/src/particle/body_contact.rs
      provides: "Fixture query, body-contact generation/sort/prune/diff, and stuck tracking"
    - path: crates/liquidfun/src/world/particle_coupling.rs
      provides: "Phase 9-only rigid reaction coupling shell"
  key_links:
    - from: crates/liquidfun/src/world.rs
      to: crates/liquidfun/src/particle.rs
      via: "world child declarations and public particle records"
      pattern: "mod particle|pub use"
    - from: crates/liquidfun/src/world/step.rs
      to: crates/liquidfun/src/world/particle_coupling.rs
      via: "after rigid contact update and before rigid island solve"
      pattern: "particle_coupling"
---
<objective>
Complete fixture/body contacts, strict pruning, hooks, and the source-timed Phase 9 rigid reaction slice.

Purpose: Finish contact/coupling foundations without executing or claiming Phase 10 particle solvers.
Output: Body-contact kernel, extended single hook, shared journal integration, and coupling regressions.
</objective>
<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-07-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-08-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-15-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Generate and prune fixture/body contacts</name>
  <files>crates/liquidfun/src/particle/body_contact.rs, crates/liquidfun/tests/particle_body_contacts.rs</files>
  <read_first>Phase 9 D-16, D-19, D-23; pinned UpdateBodyContacts/RemoveSpuriousBodyContacts; existing collision shape-child queries.</read_first>
  <action>Declare all new particle/world child modules from particle.rs/world.rs, re-export supported public records through particle.rs/lib.rs, and prove external-crate reachability. Implement fixture culling and shape queries, stable body/fixture/particle identity records, weight/normal/effective mass fields, stuck counters/candidates, source sort, flag-gated borrowed filter decisions, strict-contact pruning, and flag-gated listener diffs in the pinned order. Capture strict-prune tie behavior from the canonical oracle before locking it. Return ordered effects rather than reconstructing them from final sets.</action>
  <verify><automated>cargo test -p liquidfun --test particle_body_contacts contacts</automated></verify>
  <acceptance_criteria>Strict/non-strict contacts, fields, stuck state, callback gates, order, and multiplicity match named source/oracle fixtures.</acceptance_criteria>
  <done>Strict/non-strict contacts, fields, stuck state, callback gates, order, and multiplicity match named source/oracle fixtures.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Wire the single hook, shared journal, and scoped rigid reaction</name>
  <files>crates/liquidfun/src/world/particle_coupling.rs, crates/liquidfun/src/world/step.rs, crates/liquidfun/tests/particle_body_contacts.rs</files>
  <read_first>Phase 9 D-15 through D-20 and D-23; Phase 8 borrowed StepHook/panic contract; 09-07 transaction and 09-08 particle-contact effects.</read_first>
  <action>Extend the existing single synchronous borrowed hook with distinct particle-pair and fixture-particle views that cannot access World or retain handles. For each newest-first, non-paused system, execute exactly `particle_iterations` sub-iterations with `sub_dt = step.dt / particle_iterations` and `sub_inv_dt = step.inv_dt * particle_iterations`; each sub-iteration increments the particle timestamp, runs `UpdateContacts(false)`, `UpdateBodyContacts()`, and `ComputeWeight()`, then applies only the body-contact branches of pinned `SolvePressure(subStep)` and `SolveDamping(subStep)`. Preserve the exact pressure equations: `critical_pressure = density * (diameter * sub_inv_dt)^2`, `pressure_per_weight = pressure_strength * critical_pressure`, `h = min(pressure_per_weight * max(0, weight - b2_minParticleWeight), b2_maxParticlePressure * critical_pressure)`, `velocity_per_pressure = sub_dt / (density * diameter)`, and body impulse `f = velocity_per_pressure * contact.weight * contact.mass * (h + pressure_per_weight * contact.weight) * contact.normal`; subtract `particle_inv_mass * f` from particle velocity and call the Rust equivalent of `body.ApplyLinearImpulse(f, position, true)`, waking the body and updating its linear velocity plus angular velocity through `cross(position - center, f)`. Preserve damping equations: `vn = dot(body_velocity_at_point - particle_velocity, normal)`; only when `vn < 0`, `damping = max(damping_strength * weight, min(-vn / (diameter * sub_inv_dt), 0.5))`, `f = damping * contact.mass * vn * normal`; add `particle_inv_mass * f` to particle velocity and apply body impulse `-f`, including wake/linear/angular mutations. Append begin/end occurrences directly to the shared lifecycle journal and retain transactional rollback/panic poisoning. Explicitly exclude the particle-particle branches of `SolvePressure`/`SolveDamping`, position integration, `SolveForce`, `SolveViscous`, `SolveRepulsive`, `SolvePowder`, `SolveTensile`, `SolveSolid`, `SolveColorMixing`, `SolveGravity`, `SolveStaticPressure`, `SolveExtraDamping`, `SolveElastic`, `SolveSpring`, `LimitVelocity`, `SolveRigidDamping`, `SolveBarrier`, `SolveCollision`, `SolveRigid`, `SolveWall`, `ComputeDepth`, and reactive pair/triad updates as Phase 10. Add focused local static/dynamic/rotating-body, off-center angular impulse, zero-weight, separating-velocity, multi-iteration, newest-first, pause, rollback, and independent 09-15 witness-backed cases. Do not reference or run the later generalized particle oracle suite.</action>
  <verify><automated>cargo test -p liquidfun --test particle_body_contacts step</automated></verify>
  <acceptance_criteria>The complete Phase 9 contact prefix and rigid reaction are source-timed, transactional, hook-safe, and bounded away from Phase 10.</acceptance_criteria>
  <done>The complete Phase 9 contact prefix and rigid reaction are source-timed, transactional, hook-safe, and bounded away from Phase 10.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Fixture geometry and borrowed hook to World step | Decisions and panics can affect rigid bodies and journal ordering. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-09-01 | T | strict pruning/coupling | mitigate | Canonical fixtures, checked fields, and source-ordered transactional application. |
| T-09-09-02 | E | StepHook | mitigate | Borrow-scoped views only; no World/contact handle access; poison on panic. |
</threat-model>
<verification>
Run `cargo check -p liquidfun --all-features` to verify entrypoint wiring.
Run body-contact/step and rigid regressions, then before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Diff-review the exact step location and Phase 10 exclusion.
</verification>
<success-criteria>
- Particle and body contacts execute in pinned phase order.
- Hook decisions/events preserve flags, identity, order, multiplicity, rollback, and panic semantics.
- Only the declared Phase 9 rigid reaction state is updated.
</success-criteria>
<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-09-SUMMARY.md`.</output>
