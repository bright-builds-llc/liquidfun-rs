---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 13
type: execute
wave: 12
depends_on: [09-12, 09-15]
files_modified:
  - tools/reference/src/rigid_world_phase9_decode.hpp
  - tools/reference/src/rigid_world_phase9_execute.hpp
  - tools/reference/src/rigid_world.cpp
  - crates/liquidfun-differential/tests/particle_oracle.rs
autonomous: true
requirements: [PART-01, PART-02, PART-03, PART-04, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "The pinned C++ oracle decodes and executes the same bounded Phase 9 request through the existing long-lived process."
    - "Oracle traces use stable scenario IDs and semantic fields, never pointers, dense indices, or raw memory."
    - "Malformed/unsupported/Phase 10 requests are hard harness failures and stdout remains protocol-only."
  artifacts:
    - path: tools/reference/src/rigid_world_phase9_decode.hpp
      provides: "Bounded source-native Phase 9 request decoding"
    - path: tools/reference/src/rigid_world_phase9_execute.hpp
      provides: "Pinned source execution and semantic trace collection"
  key_links:
    - from: tools/reference/src/rigid_world.cpp
      to: tools/reference/src/rigid_world_phase9_execute.hpp
      via: "existing request dispatch and long-lived loop"
      pattern: "phase9"
---
<objective>
Extend the existing pinned C++ oracle process with bounded source-faithful Phase 9 decode, execution, and semantic collection.

Purpose: Provide independent expected behavior for lifecycle, contacts, buffers, forces, queries, and rigid coupling.
Output: Phase-specific C++ helpers and Rust-side round-trip/harness-failure tests.
</objective>
<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-12-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-15-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-RESEARCH.md
@UPSTREAM.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Decode bounded Phase 9 requests in the existing oracle</name>
  <files>tools/reference/src/rigid_world_phase9_decode.hpp, tools/reference/src/rigid_world.cpp, crates/liquidfun-differential/tests/particle_oracle.rs</files>
  <read_first>09-12 phase9-v1 schema; existing Phase 8 decode helper and process dispatch; pinned ParticleSystem definitions/actions.</read_first>
  <action>Consume and verify the exact 09-15 semantic witness/provenance artifacts before generalized oracle execution; never replace them with Rust-derived expectations. Add a phase-specific C++ decoder that checks protocol/schema versions, all collection/action bounds, exact f32-bit inputs, stable scenario IDs, enum/flag domains, buffer/capacity constraints, and declaration family before allocation or mutation. Dispatch it from the existing rigid_world process; do not create a new executable or protocol. Keep stdout JSONL-only and route diagnostics to stderr. Reject group topology, pair/triad generation, solver observations, unknown fields, and malformed lines distinctly.</action>
  <verify><automated>cargo test -p liquidfun-differential --test particle_oracle decode</automated></verify>
  <acceptance_criteria>Valid bounded requests reach one existing process; every invalid or Phase 10 request is a classified harness failure.</acceptance_criteria>
  <done>Valid bounded requests reach one existing process; every invalid or Phase 10 request is a classified harness failure.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Execute source behavior and collect semantic traces</name>
  <files>tools/reference/src/rigid_world_phase9_execute.hpp, tools/reference/src/rigid_world.cpp, crates/liquidfun-differential/tests/particle_oracle.rs</files>
  <read_first>09-12 semantic trace fields; pinned source order audited in 09-RESEARCH; existing Phase 8 execute helper/provenance envelope.</read_first>
  <action>Execute multiple/newest-first systems, buffer/capacity paths, permutations/lifetimes/zombies, particle/body contacts, strict/filter/listener flags, forces/impulses/statistics, AABB/ray queries, and Phase 9 rigid reaction using pinned upstream public/private oracle access patterns already permitted in tools/reference. Add generalized coupling scenarios for the 09-09 body-contact `SolvePressure`/`SolveDamping` branches: static/dynamic/rotating bodies, off-center angular impulse, separating velocity, multiple configured particle iterations, newest-first systems, pause, and rollback, while retaining the explicit Phase 10 exclusions. Assign stable scenario IDs before execution and translate all callback/state records to semantic IDs. Emit source-timed occurrences and recorded build provenance. Never emit pointers, dense indices, memory dumps, or Rust-derived expected values.</action>
  <verify><automated>cargo run -p xtask -- reference build --preset oracle-debug && cargo test -p liquidfun-differential --test particle_oracle && cargo test -p liquidfun-differential --test particle_oracle coupling</automated></verify>
  <acceptance_criteria>Every declared Phase 9 family can round-trip against a pinned semantic oracle trace and failures remain distinct from physics mismatches.</acceptance_criteria>
  <done>Every declared Phase 9 family can round-trip against a pinned semantic oracle trace and failures remain distinct from physics mismatches.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| JSONL to C++ oracle process | Untrusted requests cross into crash-prone foreign tooling. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-13-01 | D | C++ decoder | mitigate | Validate hard bounds and domains before allocation/execution; classify crashes/timeouts separately. |
| T-09-13-02 | I | oracle trace | mitigate | Emit semantic stable IDs and pinned provenance only; no memory/layout data. |
</threat-model>
<verification>
Run the debug oracle build and round-trip suite. Before committing, also run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then run `cargo run -p xtask -- reference build --preset oracle-release` and `cargo run -p xtask -- reference build --preset oracle-asan-ubsan`; review stdout/stderr separation and upstream read-only status.
</verification>
<success-criteria>
- The existing oracle process independently executes every scoped Phase 9 family.
- Semantic traces retain provenance, source order, multiplicity, and stable IDs.
- Debug/release/sanitizer builds expose harness faults without changing production Cargo isolation.
</success-criteria>
<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-13-SUMMARY.md`.</output>
