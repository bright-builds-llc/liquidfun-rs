---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 25
type: execute
wave: 1
depends_on: [09-24]
gap_closure: true
files_modified:
  - crates/liquidfun-differential/src/rigid_world.rs
  - crates/liquidfun-differential/src/rigid_world/phase9/comparator.rs
  - crates/liquidfun-differential/tests/phase9_corpus.rs
autonomous: true
requirements: [API-09, API-10, PART-01, PART-02, PART-07, PART-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-17T17:19:43Z
must_haves:
  truths:
    - "Phase 9 compares every retained Phase 6 through Phase 8 rigid observation before accepting particle observations."
    - "Body, fixture, and retained numeric mutations produce stable first-divergence failures instead of Match."
    - "The exact same validated request and complete result pair reach both the inherited rigid comparator and the Phase 9 particle comparator."
  artifacts:
    - path: crates/liquidfun-differential/src/rigid_world.rs
      provides: "compare_complete_phase9_rigid_world_results and checked-in retained policy loading"
    - path: crates/liquidfun-differential/src/rigid_world/phase9/comparator.rs
      provides: "Closed particle-specific comparison layered after retained rigid comparison"
    - path: crates/liquidfun-differential/tests/phase9_corpus.rs
      provides: "Body, fixture, numeric, and particle mutation regressions"
  key_links:
    - from: crates/liquidfun-differential/src/rigid_world.rs
      to: crates/liquidfun-differential/src/rigid_evidence.rs
      via: "compare_complete_phase9_rigid_world_results composes compare_phase8_rigid_world_results"
      pattern: "compare_phase8_rigid_world_results"
    - from: crates/liquidfun-differential/src/rigid_world.rs
      to: crates/liquidfun-differential/src/rigid_world/phase9/comparator.rs
      via: "retained rigid comparison succeeds before Phase 9 particle comparison"
      pattern: "compare_phase9_rigid_world_results"
---

<objective>
Close G09-DIFFERENTIAL-COMPARISON by making Phase 9 a strict semantic superset of the retained Phase 8 comparator.

Purpose: Prevent a valid body, fixture, or retained numeric divergence from being discarded when Phase 9 filters particle observations.
Output: Composed comparator execution and mutation regressions proving inherited first-divergence behavior.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-REVIEW.md
@.planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-VERIFICATION.md
@crates/liquidfun-differential/src/rigid_world.rs
@crates/liquidfun-differential/src/rigid_evidence.rs
@crates/liquidfun-differential/src/rigid_world/phase9/comparator.rs
@protocol/tolerances/phase6-v1.toml
@protocol/tolerances/phase7-v1.toml
@protocol/tolerances/phase8-v1.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove retained rigid divergences currently escape Phase 9</name>
  <files>crates/liquidfun-differential/tests/phase9_corpus.rs</files>
  <read-first>Current Phase 9 corpus comparator mutation tests; `compare_phase8_rigid_world_results`; `RigidComparisonFailure`, `RigidComparisonOutcome`, and `RigidMismatchReport`; all three checked-in tolerance profiles; rigid result observation variants; 09-VERIFICATION G09-DIFFERENTIAL-COMPARISON.</read-first>
  <action>Write focused Arrange/Act/Assert regressions against a planned public seam named `compare_complete_phase9_rigid_world_results`, with one request, native result, and oracle result; the seam must own the canonical retained-policy loading rather than accept policy arguments. Use one valid Phase 9 result pair, mutate exactly one retained body field, one fixture field, and one retained computed numeric field in separate tests, and assert `Phase9ComparisonOutcome::RetainedRigidMismatch` carries the same typed first-divergence signature produced by the Phase 8 comparator. Keep the existing particle-statistics mutation separate. Add a structural-order regression where a retained rigid mismatch and a later particle mismatch coexist and assert the retained mismatch wins. Add a spy-free integration assertion that `run_phase9_differential` reports the retained variant for a deliberately modified process result through the same complete seam, rather than testing the particle-only helper.</action>
  <verify><automated>cargo test -p liquidfun-differential --test phase9_corpus phase9_comparator_rejects_retained -- --nocapture</automated></verify>
  <acceptance-criteria>`phase9_corpus.rs` contains distinct tests for body, fixture, retained numeric, and rigid-before-particle divergence; each test fails before comparator composition and asserts a typed non-Match first-divergence result.</acceptance-criteria>
  <done>The focused RED tests demonstrate that non-particle retained semantics can escape the current Phase 9 comparator and lock the required inherited ordering.</done>
</task>

<task type="auto">
  <name>Task 2: Compose retained Phase 8 and Phase 9 comparison exactly once</name>
  <files>crates/liquidfun-differential/src/rigid_world.rs, crates/liquidfun-differential/src/rigid_world/phase9/comparator.rs, crates/liquidfun-differential/tests/phase9_corpus.rs</files>
  <read-first>The RED tests; `run_phase9_differential`; `compare_phase8_rigid_world_results`; `RigidComparisonFailure`/`RigidComparisonOutcome`; `Phase9ComparatorError`/`Phase9ComparisonOutcome`; `protocol/tolerances/phase6-v1.toml`, `phase7-v1.toml`, and `phase8-v1.toml`; Phase 9 request/result validation and policy registry.</read-first>
  <action>Embed and parse the exact checked-in files `protocol/tolerances/phase6-v1.toml`, `phase7-v1.toml`, and `phase8-v1.toml` inside the production comparator module into `Phase6PolicyProfile`, `Phase7PolicyProfile`, and `Phase8PolicyProfile`; reject any parse or expected-content-digest mismatch as a harness error. Define `Phase9ComparatorError::RetainedRigid(RigidComparisonFailure)` and `Phase9ComparisonOutcome::RetainedRigidMismatch(Box&lt;RigidMismatchReport&gt;)`. Implement public `compare_complete_phase9_rigid_world_results(request, native, oracle)` so it accepts no policy arguments, loads the embedded canonical profiles, calls an internal profile-taking helper, and cannot be redirected to caller-supplied substitutes. The internal helper calls `compare_phase8_rigid_world_results` first; maps `RigidComparisonFailure` to the retained harness-error variant; maps `RigidComparisonOutcome::PhysicsMismatch` to `RetainedRigidMismatch`; and only on `RigidComparisonOutcome::Match` calls the particle-only `compare_phase9_rigid_world_results`, preserving its `PhysicsMismatch` and Match-with-22-consumed-paths results. Keep any profile-injection helper private to the module and use it only for focused unit tests. Make `run_phase9_differential` call the public complete seam exactly once after both roles execute the same canonical request. Do not duplicate Phase 6-8 policies in the particle helper and do not evaluate particle policies after a retained mismatch.</action>
  <verify><automated>cargo test -p liquidfun-differential --test phase9_corpus && cargo test -p liquidfun-differential --test particle_oracle && cargo test -p liquidfun-differential --test particle_protocol</automated></verify>
  <acceptance-criteria>`rigid_world.rs` defines `compare_complete_phase9_rigid_world_results` without policy parameters, embeds and digest-checks all three exact policy profiles, keeps any injectable helper private, maps retained harness failures and mismatches into named Phase 9 variants, and `run_phase9_differential` calls the complete seam exactly once; all body/fixture/numeric/order mutation tests reject while particle policy and same-request tests remain green.</acceptance-criteria>
  <done>The Phase 9 runner is a strict retained-rigid superset and every focused comparator/protocol/corpus test exits zero.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the comparator closure after mandatory Rust gates</name>
  <files>crates/liquidfun-differential/src/rigid_world.rs, crates/liquidfun-differential/src/rigid_world/phase9/comparator.rs, crates/liquidfun-differential/tests/phase9_corpus.rs</files>
  <read-first>The complete Task 1-2 diff; AGENTS.md precommit order; the Phase 9 review and verification gap wording.</read-first>
  <action>Review the diff for a single composed comparison authority, remove any redundant helper or workaround, run the exact repository precommit sequence in order, then commit only this plan's implementation and tests atomically. Do not edit compatibility authority or generated reports.</action>
  <verify><automated>cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && git diff --check</automated></verify>
  <acceptance-criteria>All four mandatory Rust commands and `git diff --check` exit zero; the commit contains only the three declared files and no compatibility/evidence promotion.</acceptance-criteria>
  <done>The comparator gap is committed with a green full Rust gate and no unrelated or promotion changes.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Native and oracle results to composed comparator | Filtering can hide retained rigid divergence. | Complete Phase 8 comparison runs before particle filtering. |
| Request identity to engine roles | Scenario substitution can create false parity. | One serialized digest-bound request is revalidated and executed by both roles. |
| Mismatch classification | Later particle checks can mask the true first failure. | Retained mismatch returns immediately with focused ordering regression. |

ASVS L1: malformed results remain harness errors, comparison work stays bounded by existing result limits, and any high-severity integrity finding blocks completion.
</threat-model>

<verification>
Run focused Phase 9 corpus/protocol/oracle tests, then the exact mandatory Rust sequence. Confirm no compatibility, workflow, fixture, manifest, or Phase 10 files changed.
</verification>

<success-criteria>
- Body, fixture, and retained numeric mutations cannot return Match.
- Retained rigid comparison precedes Phase 9 particle comparison and preserves first divergence.
- Same-request, policy-consumption, structural validation, and full Rust gates pass.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-25-SUMMARY.md`.</output>
