---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 26
type: execute
wave: 2
depends_on: [09-25]
gap_closure: true
files_modified:
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/phase9-v1.json
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/aabb-query-control-and-culling.jsonl
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/closed-evidence-contract.jsonl
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/contacts-listeners-filters-and-coupling.jsonl
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/forces-impulses-and-statistics.jsonl
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/lifetime-zombie-and-eviction.jsonl
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/ray-control-and-culling.jsonl
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/storage-systems-and-permutations.jsonl
  - crates/liquidfun-differential/tests/phase9_corpus.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase9.rs
  - tools/reference/src/rigid_world_phase9_execute.hpp
  - tools/reference/src/rigid_world.cpp
autonomous: true
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-17T17:19:43Z
must_haves:
  truths:
    - "Every one of the 58 Phase 9 branches is bound to an exact action, checkpoint, observation kind, and checkable semantic predicate."
    - "Lifetime, strict contact, listener/filter, collision-energy, and stuck-candidate witnesses observe the named behavior rather than declaration bits, zero values, or empty collections."
    - "Replay, minimization, first-divergence, D0 determinism, and debug/release witnesses use result digests or deliberate mismatch behavior instead of request or scenario ID equality."
    - "Cross-engine stable-ID rotation and all group, topology, pair, triad, source-area, and solver behavior remain Phase 10 scope."
  artifacts:
    - path: crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/phase9-v1.json
      provides: "Seven closed cases with 58 branch-specific semantic witness bindings"
    - path: crates/liquidfun-differential/tests/phase9_corpus.rs
      provides: "Executable witness evaluation and rejection tests for trivial or substituted bindings"
  key_links:
    - from: crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/phase9-v1.json
      to: crates/liquidfun-differential/tests/phase9_corpus.rs
      via: "branch ID resolves to exact action, checkpoint, observation kind, and semantic assertion"
      pattern: "reached_branches|witness"
    - from: crates/liquidfun-differential/tests/phase9_corpus.rs
      to: tools/reference/src/rigid_world_phase9_execute.hpp
      via: "the same request actions produce native and pinned-oracle observations"
      pattern: "phase9"
---

<objective>
Close G09-EXECUTABLE-COVERAGE by replacing declaration-only and trivial labels with branch-specific semantic cross-engine evidence.

Purpose: Make the 58-branch claim prove observable Phase 9 behavior, not merely a complete list of names.
Output: Updated seven-case requests, symmetric Rust/C++ execution, and a closed semantic witness evaluator.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-REVIEW.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-22-SUMMARY.md
@crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/phase9-v1.json
@crates/liquidfun-differential/tests/phase9_corpus.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and reject non-semantic branch bindings</name>
  <files>crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase9.rs, crates/liquidfun-differential/tests/phase9_corpus.rs</files>
  <read-first>Current case/witness schema; all 58 branch IDs; current per-branch witness assertions; result observation variants and hard bounds; 09-VERIFICATION G09-EXECUTABLE-COVERAGE.</read-first>
  <action>Introduce a closed, bounded witness binding that identifies `branch_id`, `action_index`, `checkpoint_index`, `observation_kind`, and a typed `semantic_assertion`. The assertion enum must represent the required comparisons without free-form expressions: lifetime finite/infinite/equal-expiration state, strict-contact enabled/disabled cardinality, listener/filter enabled/disabled event effect, collision energy positive finite, stuck candidate nonempty stable IDs, replay result-digest equality, minimized failure-signature preservation, deliberate first-divergence path, D0 repeated-result digest equality, and debug/release result-digest equality. Reject duplicate branch IDs, out-of-range action/checkpoint indices, mismatched observation kinds, zero/empty values for positive/nonempty assertions, identity-only assertions for replay/evidence branches, unknown assertions, and more than the existing reviewed limits. Add one Arrange/Act/Assert test per rejection class and a table-driven test requiring exactly the reviewed 58 branch IDs.</action>
  <verify><automated>cargo test -p liquidfun-test-protocol phase9 && cargo test -p liquidfun-differential --test phase9_corpus witness_binding -- --nocapture</automated></verify>
  <acceptance-criteria>The protocol contains a closed typed semantic assertion surface and finite limits; focused tests reject generic ID equality, zero energy, empty stuck candidates, wrong observation kinds, invalid indices, duplicates, and missing/extra branch IDs.</acceptance-criteria>
  <done>The RED-to-GREEN schema and evaluator tests prove that branch labels cannot be satisfied by declarations or unrelated trivial output.</done>
</task>

<task type="auto">
  <name>Task 2: Make all seven cases execute their named semantics in Rust and C++</name>
  <files>crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/phase9-v1.json, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/aabb-query-control-and-culling.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/closed-evidence-contract.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/contacts-listeners-filters-and-coupling.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/forces-impulses-and-statistics.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/lifetime-zombie-and-eviction.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/ray-control-and-culling.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/storage-systems-and-permutations.jsonl, crates/liquidfun-differential/tests/phase9_corpus.rs, tools/reference/src/rigid_world_phase9_execute.hpp, tools/reference/src/rigid_world.cpp</files>
  <read-first>The typed witness binding; current seven cases/actions; native executor action support; pinned C++ Phase 9 executor; upstream lifetime/contact/filter/listener/energy/stuck behavior; Phase 10 exclusions.</read-first>
  <action>Retain exactly the seven named JSONL case files and exactly 58 unique Phase 9 branch IDs, but extend each case's real request actions and checkpoints so every binding evaluates its named output in both engines. Include finite lifetime expiration, infinite lifetime survival, equal-expiration deterministic order, strict-contact enabled versus disabled contact results, listener and filter enabled versus disabled event/contact effects, positive finite collision energy from a deterministic contacted configuration, and at least one nonempty stable stuck-candidate result after the pinned threshold sequence. Build replay from the same request bytes and require equal result digests; introduce a deliberate copied-result mutation for first-divergence and minimization tests; require D0 repeated executions and debug/release executions to compare complete result digests. Recompute and replace every changed case's `request_sha256` in `phase9-v1.json`; tests must read each JSONL file and prove the bytes match its recorded digest. Preserve one-request/two-engine execution, all 22 policy paths per case, existing bounds, request/native/oracle/comparison SHA-256 fields, and the deferred stable-ID rotation/Phase 10 boundary.</action>
  <verify><automated>cargo test -p liquidfun-differential --test phase9_corpus && cargo test -p liquidfun-differential --test particle_oracle && cargo xtask upstream configure --preset oracle-debug && cargo xtask upstream build --preset oracle-debug && cargo xtask upstream configure --preset oracle-release && cargo xtask upstream build --preset oracle-release</automated></verify>
  <acceptance-criteria>The corpus test reports 7 distinct cases, exactly 58 unique branches, identical complete 22-policy sets per case, no declaration-only witness, positive energy, nonempty stuck IDs, observable lifetime/contact/listener/filter contrasts, and result-based replay/minimization/divergence/determinism/debug-release evidence.</acceptance-criteria>
  <done>All seven requests execute symmetrically in native Rust and the pinned oracle, and every one of the 58 bindings passes only from its named semantic observation.</done>
</task>

<task type="auto">
  <name>Task 3: Run complete native, debug/release, and sanitizer execution gates before committing</name>
  <files>crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/phase9-v1.json, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/aabb-query-control-and-culling.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/closed-evidence-contract.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/contacts-listeners-filters-and-coupling.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/forces-impulses-and-statistics.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/lifetime-zombie-and-eviction.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/ray-control-and-culling.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/cases/storage-systems-and-permutations.jsonl, crates/liquidfun-differential/tests/phase9_corpus.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase9.rs, tools/reference/src/rigid_world_phase9_execute.hpp, tools/reference/src/rigid_world.cpp</files>
  <read-first>The complete Task 1-2 diff; sanitizer exception scope test; repository precommit order; upstream read-only contract.</read-first>
  <action>Run the exact Rust precommit sequence, focused Phase 9 tests, the complete corpus against both oracle-debug and oracle-release with equal complete result digests, and the fail-fast ASan/UBSan protocol plus complete sanitizer corpus. Preserve the existing `shift-base` exception only for pinned `b2ParticleSystem.cpp`; do not weaken any other sanitizer. Run provenance, upstream read-only, and diff checks. Commit the semantic corpus change atomically only after every gate succeeds. Do not edit the evidence runner, compatibility authority, or generated COMPATIBILITY.md.</action>
  <verify><automated>cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && cargo xtask upstream configure --preset oracle-debug && cargo xtask upstream build --preset oracle-debug && cargo xtask upstream configure --preset oracle-release && cargo xtask upstream build --preset oracle-release && LIQUIDFUN_PHASE9_ORACLE_MODE=canonical cargo test -p liquidfun-differential --test phase9_corpus -- --nocapture && cargo xtask upstream configure --preset oracle-asan-ubsan && cargo xtask upstream build --preset oracle-asan-ubsan && cmake --build target/reference/oracle-asan-ubsan --target liquidfun-reference-protocol-tests && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 ctest --test-dir target/reference/oracle-asan-ubsan --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$' && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 LIQUIDFUN_PHASE9_ORACLE_MODE=sanitizer cargo test -p liquidfun-differential --test phase9_corpus -- --nocapture && cargo xtask provenance check && git diff --exit-code -- third_party/liquidfun && git diff --check</automated></verify>
  <acceptance-criteria>All mandatory Rust, focused corpus, debug/release, fail-fast sanitizer, provenance, read-only, and diff commands exit zero; the commit contains only declared implementation/test/runner files and no ledger promotion.</acceptance-criteria>
  <done>The executable-coverage gap is committed with fresh local canonical and sanitizer proof and no authority promotion.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Fixture labels to executed behavior | A complete label set can falsely claim unexecuted branches. | Typed exact action/checkpoint/observation/assertion bindings with rejection tests. |
| JSONL request/result boundary | Malformed or oversized witness data can allocate or index unsafely. | Closed enums, reviewed limits, index validation before execution, safe owned records. |
| C++ and Rust observation symmetry | One side can fabricate a convenient result. | Same request bytes, symmetric actions, semantic comparison, pinned upstream audit. |
| Result-based harness witnesses | Replay or minimization labels can pass from identity equality alone. | Complete result digests, deliberate mismatches, and stable failure signatures. |

ASVS L1: unsafe paths, unbounded records, shell injection, secret output, or high-severity evidence-integrity findings block completion.
</threat-model>

<verification>
Require exactly seven cases, 58 unique semantic bindings, complete 22-policy consumption per case, focused trivial-witness rejection, full Rust gates, debug/release comparison, fail-fast sanitizer corpus, provenance, and upstream read-only checks.
</verification>

<success-criteria>
- Every claimed branch is proven by its named semantic output in both engines.
- Former trivial lifetime/contact/listener/filter/energy/stuck and evidence-contract witnesses are replaced.
- Phase 10 scope remains absent and compatibility authority remains unchanged.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-26-SUMMARY.md`.</output>
