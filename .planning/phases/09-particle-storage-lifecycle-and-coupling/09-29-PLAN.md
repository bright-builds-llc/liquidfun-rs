---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 29
type: execute
wave: 5
depends_on: [09-28]
gap_closure: true
files_modified:
  - reference/compatibility.json
  - tools/xtask/src/inventory/validation.rs
  - tools/xtask/tests/inventory_cli.rs
  - COMPATIBILITY.md
  - TESTING.md
autonomous: true
requirements: [API-09, API-10, PART-01, PART-02, PART-03, PART-04, PART-05, PART-06, PART-07, PART-08, PART-14, PART-15, PART-16, PART-17]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-17T17:19:43Z
must_haves:
  truths:
    - "Freshly redownloaded artifacts independently reprove the new approved SHA, retained rigid comparison, 58 semantic witnesses, complete policies, passing logs, and all payload hashes before ledger mutation."
    - "Only supported Phase 9 rows cite the new authority; runs 29439515367, 29583793056, and 29625083184 cannot authorize the changed implementation."
    - "Generated compatibility output, Rust gates, oracle/sanitizer evidence, provenance, schema drift, dependency policy, Markdown, and ASVS L1 gates all pass."
    - "Phase 10 groups, topology, pairs, triads, source areas, solvers, and cross-engine stable-ID rotation remain not evidenced."
  artifacts:
    - path: reference/compatibility.json
      provides: "Scoped Phase 9 claims bound only to the new approved semantic authority"
    - path: tools/xtask/src/inventory/validation.rs
      provides: "Fail-closed authority contract including retained rigid and witness-binding evidence"
    - path: tools/xtask/tests/inventory_cli.rs
      provides: "Negative tests for prior runs, incomplete/trivial evidence, substitution, and Phase 10 claims"
    - path: COMPATIBILITY.md
      provides: "Deterministically generated truthful compatibility report"
  key_links:
    - from: .planning/phases/09-particle-storage-lifecycle-and-coupling/09-28-SUMMARY.md
      to: reference/compatibility.json
      via: "independently revalidated exact run, artifacts, and semantic digests"
      pattern: "phase9-canonical|phase9-sanitizer"
    - from: reference/compatibility.json
      to: COMPATIBILITY.md
      via: "inventory generate/check only"
      pattern: "@generated"
---

<objective>
Independently revalidate the new semantic evidence, promote only supported Phase 9 rows, and run final verification/security/schema gates.

Purpose: Replace historical authority with proof that closes both remaining verification gaps while preserving the Phase 10 boundary.
Output: Fail-closed ledger validation, regenerated report, final green gates, and evidence ready for the phase verifier.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-28-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-VERIFICATION.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-REVIEW.md
@reference/compatibility.json
@COMPATIBILITY.md
@TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Independently redownload and revalidate the new approved artifacts</name>
  <files>TESTING.md</files>
  <read-first>The exact SHA/run/job/artifact identities in 09-28-SUMMARY; live target run.json; workflow at approved SHA; current D1 authority contract; historical and failed run references 29439515367, 29583793056, and 29625083184.</read-first>
  <action>Cross-check every run.json field against 09-28-SUMMARY and fresh live GitHub APIs. Require the approved SHA is an ancestor and exact remote branch head and differs from the heads used by all three denylisted runs. Inspect the workflow at that SHA. Preserve existing evidence under a gitignored superseded directory, delete only the two fixed current download directories, re-query run/jobs/artifacts, validate archive entries before extraction, and redownload the exact unexpired artifact names. Re-run the reusable exact-ref validator over run.json and both newly extracted directories with runs 29439515367, 29583793056, and 29625083184 denylisted; it must reprove run/event/workflow/head/job/artifact cardinality, API digests, safe bounded file sets, no symlinks, identities, passing logs, every recomputed hash, 7 cases, exactly 58 unique semantic witness bindings, complete identical 22-policy arrays, retained Phase 8 rigid proof, and canonical/sanitizer semantic agreement. Never combine canonical artifact 8423580554 from failed run 29625083184 with any sanitizer artifact. Record immutable new URLs and procedure in TESTING.md only after all checks pass; do not mutate compatibility first.</action>
  <verify><automated>RUN_ID=$(jq -r .run_id target/phase9-evidence/run.json) && HEAD_SHA=$(jq -r .head_sha target/phase9-evidence/run.json) && test "$RUN_ID" != 29439515367 && test "$RUN_ID" != 29583793056 && test "$RUN_ID" != 29625083184 && test "$HEAD_SHA" != 7ed430c497efbaa8585ee9ef3862be1abda29ef5 && gh run view "$RUN_ID" --json conclusion,headSha,event,jobs,workflowName | jq -e --arg sha "$HEAD_SHA" '.conclusion=="success" and .headSha==$sha and .event=="workflow_dispatch" and .workflowName=="Oracle CI" and ([.jobs[]|select(.name=="Canonical Linux oracle" and .conclusion=="success")]|length==1) and ([.jobs[]|select(.name=="Scheduled fail-fast sanitizer and reset corpus" and .conclusion=="success")]|length==1)' && cargo xtask phase9-evidence validate --mode exact-ref --run-json target/phase9-evidence/run.json --canonical-dir target/phase9-evidence/phase9-canonical --sanitizer-dir target/phase9-evidence/phase9-sanitizer --deny-run-id 29439515367 --deny-run-id 29583793056 --deny-run-id 29625083184 && cargo xtask provenance check</automated></verify>
  <acceptance-criteria>Fresh downloads independently prove the new approved SHA, inherited rigid comparison, semantic 58-branch coverage, passing logs, complete policies, and all payload/binding hashes; any stale, failed, trivial, incomplete, unsafe, substituted, or prior-run value blocks all later edits.</acceptance-criteria>
  <done>The independent exact-ref validator exits zero before ledger mutation and TESTING.md records the new immutable authority procedure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote only fresh semantic authority and regenerate reports</name>
  <files>reference/compatibility.json, tools/xtask/src/inventory/validation.rs, tools/xtask/tests/inventory_cli.rs, COMPATIBILITY.md, TESTING.md</files>
  <read-first>The validated Task 1 authority; current compatibility schema and four scoped Phase 9 rows; generated report ownership; existing authority tests; current Phase 10 not-evidenced rows.</read-first>
  <action>First add Arrange/Act/Assert negative tests proving promotion rejects runs 29439515367, 29583793056, and 29625083184; their artifact IDs/digests/SHAs, including canonical-only artifact 8423580554; any mixed-run canonical/sanitizer pair; a missing retained-rigid comparator record; a retained-rigid record with the wrong policy digest or non-match outcome; a particle-only comparator; a 58-label manifest without semantic binding digest; wrong action/checkpoint/observation binding; zero positive-energy witness; empty stuck witness; failed logs; incomplete policies; canonical-as-sanitizer substitution; and every Phase 10 group/topology/pair/triad/source-area/solver/rotation claim. Then pin validation to the new canonical and sanitizer artifact IDs/API digests, approved full SHA/run, passing trace digests, complete shared semantic manifest digest, TESTING anchor, and 09-28 summary. Update only the supported API-09/API-10/PART-01..08/PART-14..17 evidence fields whose implementation/unit/differential/platform claims the new artifacts prove. Replace historical Phase 9 promotion references. Never hand-edit COMPATIBILITY.md; run inventory generate/check twice and require byte identity. Keep unsupported rows pending.</action>
  <verify><automated>cargo test -p xtask --test inventory_cli && cargo xtask inventory generate && cargo xtask inventory check && cargo xtask inventory generate && cargo xtask inventory check && just markdown-check</automated></verify>
  <acceptance-criteria>All authority-negative and Phase 10 rejection tests pass; JSON cites only the new validated authority for supported rows; generated output is byte-idempotent; none of the three prior or failed runs can satisfy any promoted evidence dimension.</acceptance-criteria>
  <done>The fail-closed inventory tests, double generation cycle, and Markdown check exit zero with only truthful scoped Phase 9 promotion.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Rust, oracle, evidence, schema, security, and diff gates</name>
  <files>reference/compatibility.json, tools/xtask/src/inventory/validation.rs, tools/xtask/tests/inventory_cli.rs, COMPATIBILITY.md, TESTING.md</files>
  <read-first>AGENTS.md exact Rust precommit order; standards verification/testing/Rust pages; current 09-VERIFICATION gaps; current 09-REVIEW warnings; Phase 9 threat models; schema-drift command; pinned source read-only contract.</read-first>
  <action>Run the exact Rust sequence below, focused comparator/corpus/protocol mutation tests, debug/release oracle comparison, fail-fast ASan/UBSan protocol and semantic corpus, provenance/inventory checks, cargo deny, schema drift, actionlint, Markdown, and upstream read-only diff. Perform an ASVS L1 review across JSONL/process bounds, semantic binding indices, artifact/archive integrity, safe paths, secrets, and shell injection; any high-severity finding blocks completion. Review the complete 09-25..29 diff and prove G09-DIFFERENTIAL-COMPARISON and G09-EXECUTABLE-COVERAGE each have named executable regressions and fresh evidence links. Confirm no Phase 10 claim and no manual generated-report edit. Run the exact mandatory Rust sequence again immediately before each implementation/docs commit, then commit Task 2 changes and final plan summary/tracking metadata atomically.</action>
  <verify><automated>cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && cargo test -p liquidfun-differential --test phase9_corpus && cargo test -p liquidfun-differential --test particle_oracle && cargo test -p liquidfun-differential --test particle_protocol && cargo xtask upstream configure --preset oracle-debug && cargo xtask upstream build --preset oracle-debug && cargo xtask upstream configure --preset oracle-release && cargo xtask upstream build --preset oracle-release && LIQUIDFUN_PHASE9_ORACLE_MODE=canonical LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/canonical/phase9-manifest.json bash scripts/phase9-evidence.sh canonical target/phase9-evidence-local/canonical && cargo xtask upstream configure --preset oracle-asan-ubsan && cargo xtask upstream build --preset oracle-asan-ubsan && cmake --build target/reference/oracle-asan-ubsan --target liquidfun-reference-protocol-tests && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 ctest --test-dir target/reference/oracle-asan-ubsan --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$' && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 LIQUIDFUN_PHASE9_ORACLE_MODE=sanitizer LIQUIDFUN_PHASE9_EVIDENCE_MANIFEST=target/phase9-evidence-local/sanitizer/phase9-manifest.json bash scripts/phase9-evidence.sh sanitizer target/phase9-evidence-local/sanitizer && cargo xtask phase9-evidence validate --mode local --canonical-dir target/phase9-evidence-local/canonical --sanitizer-dir target/phase9-evidence-local/sanitizer --deny-run-id 29439515367 --deny-run-id 29583793056 --deny-run-id 29625083184 && cargo xtask provenance check && cargo xtask inventory check && cargo deny check && node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 09 | jq -e '.blocking==false' && actionlint .github/workflows/oracle.yml && just markdown-check && git diff --exit-code -- third_party/liquidfun && git diff --check</automated></verify>
  <acceptance-criteria>Every command exits zero; schema drift is nonblocking; ASVS review has no high finding; both remaining verification gaps map to executable tests and fresh exact-ref evidence; Phase 10 remains pending; the final diff is scoped and generated output is untouched by hand.</acceptance-criteria>
  <done>The full closure matrix passes without waiver and Phase 09 is ready for a fresh gsd-verifier goal audit.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Risk | Disposition |
| --- | --- | --- |
| Downloaded GitHub evidence to compatibility claims | Old, failed, mixed-run, or incomplete artifacts can falsely authorize changed behavior. | Fresh independent API/content/hash validation and explicit denial of all three prior/failed runs before mutation. |
| Semantic witness manifest | Labels or unrelated observations can impersonate branch coverage. | Typed binding digest, exact action/checkpoint/observation assertions, positive/nonempty requirements, corruption tests. |
| JSONL/process oracle | Malformed or resource-heavy records can affect native tooling. | Closed schemas, hard limits, timeouts, strict result validation, sanitizer pass. |
| Generated reports and shell paths | Manual edits or unsafe paths can corrupt public claims. | JSON-only authority, deterministic generation, safe paths, fixed arguments, pipefail, no secret output. |

ASVS L1 is enforced and high severity blocks. Phase 10 groups, topology, pairs, triads, source areas, solvers, and stable-ID rotation remain explicitly unpromoted.
</threat-model>

<verification>
Before every commit run the exact ordered Rust gate. Independently revalidate fresh exact-ref evidence, run authority-negative tests, double inventory generation, canonical/debug/release/sanitizer evidence, provenance, dependency policy, schema drift, actionlint, Markdown, upstream read-only, diff, and ASVS checks.
</verification>

<success-criteria>
- Retained rigid and particle semantics are both compared for the exact same requests.
- All 58 branch claims are semantic, executable, digest-bound, and proven in canonical plus sanitizer artifacts.
- Only the new approved authority supports scoped Phase 9 compatibility rows.
- The fresh verifier can close both remaining gaps without waiver or Phase 10 scope.
</success-criteria>

<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-29-SUMMARY.md`.</output>
