---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/particle.rs
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/src/world/particle_object.rs
  - crates/liquidfun/tests/particle_objects.rs
autonomous: true
requirements: [PART-01, PART-02, PART-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "Each live particle system is the sole owner of its authoritative ParticleStorage."
    - "Wrong-world, wrong-system, stale, and pending particle handles fail with distinct typed outcomes."
    - "Multiple systems retain newest-first world ordering and complete destruction snapshots."
  artifacts:
    - path: crates/liquidfun/src/world/particle_object.rs
      provides: "Particle-system/particle object lifecycle adapter over authoritative storage"
  key_links:
    - from: crates/liquidfun/src/lib.rs
      to: crates/liquidfun/src/world.rs
      via: "public World particle lifecycle surface"
      pattern: "pub use.*World|pub use.*Particle"
    - from: crates/liquidfun/src/world/object.rs
      to: crates/liquidfun/src/particle/storage.rs
      via: "ParticleSystem owns ParticleStorage"
      pattern: "ParticleSystem.*storage"
---

<objective>
Converge World particle ownership on one ParticleStorage per system and remove the placeholder particle arena authority.

Purpose: Ensure every public handle, association, and destruction cascade observes the same live/pending/stale state.
Output: Production multi-system creation/configuration/inspection/destruction and stable per-system particle lifecycle APIs.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-02-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make particle systems own authoritative storage</name>
  <files>crates/liquidfun/src/lib.rs, crates/liquidfun/src/particle.rs, crates/liquidfun/src/world.rs, crates/liquidfun/src/world/object.rs, crates/liquidfun/src/world/particle_object.rs</files>
  <read_first>Phase 9 D-01, D-02, D-14; existing particle system/group/particle placeholder arenas and AssociationMap cleanup paths; 09-02 production storage contract.</read_first>
  <action>Declare particle_object from world.rs, re-export supported particle types through particle.rs and lib.rs, and prove external-crate reachability. Move per-particle authority into ParticleSystem-owned ParticleStorage, preserve newest-first system insertion/traversal, and route create/configure/pause/inspect/mark-destroy/system-destroy operations through the owning storage. Remove the separate World particle arena and redundant membership vectors as authorities. Capture complete owned particle/system snapshots and supplied-lane ownership before teardown, then clean associations and dependent references transactionally. Keep public IDs world- and system-scoped.</action>
  <verify><automated>cargo test -p liquidfun --test particle_objects</automated></verify>
  <acceptance_criteria>No live or pending particle state can disagree between World and its owning ParticleStorage because only the latter exists as authority.</acceptance_criteria>
  <done>No live or pending particle state can disagree between World and its owning ParticleStorage because only the latter exists as authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protect multi-system identity and destruction behavior</name>
  <files>crates/liquidfun/tests/particle_objects.rs</files>
  <read_first>Public handle error vocabulary, Phase 3 identity tests, and D-02/D-14 ordering requirements.</read_first>
  <action>Add one-concern Arrange/Act/Assert integration tests for multiple systems, newest-first enumeration, cross-world/cross-system rejection, stable IDs after permutations, pending-delete access, stale-after-compaction, generation retirement, safe user association cleanup, full system destruction snapshots, and teardown with no dangling rigid/group references.</action>
  <verify><automated>cargo test -p liquidfun --test particle_objects</automated></verify>
  <acceptance_criteria>All scoped identity failures are distinct and destruction leaves no resolvable particle or association behind.</acceptance_criteria>
  <done>All scoped identity failures are distinct and destruction leaves no resolvable particle or association behind.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public typed handle to owning system storage | Forged, stale, cross-world, and cross-system IDs request state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-03-01 | S | particle_object.rs | mitigate | Check world/system scope and generation before row resolution. |
| T-09-03-02 | T | system teardown | mitigate | Snapshot then commit one complete ownership/association teardown. |
</threat-model>

<verification>
Run `cargo check -p liquidfun --all-features` to verify entrypoint wiring.
Run object integration tests, then before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Review the diff to prove the placeholder particle arena is gone and unrelated rigid behavior is unchanged.
</verification>

<success-criteria>
- One storage authority exists per live particle system.
- Multi-system creation/configuration/pause/inspection/destruction uses stable scoped handles.
- Teardown preserves owned evidence and removes all associations and references coherently.
</success-criteria>

<output>
After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-03-SUMMARY.md`.
</output>
