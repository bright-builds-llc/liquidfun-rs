---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - crates/liquidfun/src/particle/storage.rs
  - crates/liquidfun/src/particle/storage/lanes.rs
  - crates/liquidfun/src/particle/storage/permutation.rs
  - crates/liquidfun/src/particle/storage/properties.rs
autonomous: true
requirements: [PART-03, PART-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "Stable ParticleId values survive every valid sort, rotation, and survivor compaction."
    - "An invalid or incomplete permutation leaves every storage lane and reference unchanged."
  artifacts:
    - path: crates/liquidfun/src/particle/storage/lanes.rs
      provides: "Production Vec2, flag, color, association, force, weight, contact, stuck, and lifetime lanes"
    - path: crates/liquidfun/src/particle/storage/permutation.rs
      provides: "Only validate-prepare-commit particle row permutation authority"
  key_links:
    - from: crates/liquidfun/src/particle/storage.rs
      to: crates/liquidfun/src/particle/storage/permutation.rs
      via: "all rotate/sort/compact entrypoints"
      pattern: "apply_permutation"
---

<objective>
Deepen the Phase 3 storage spike into production lanes and one atomic permutation authority.

Purpose: Make stable identity and complete SoA/reference remapping a proven invariant before World ownership and lifecycle build on it.
Output: Production ParticleStorage, centralized permutation transaction, and independent-model property coverage.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-01-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace representative storage with production lanes</name>
  <files>crates/liquidfun/src/particle/storage.rs, crates/liquidfun/src/particle/storage/lanes.rs</files>
  <read_first>09-01 lane inventory; current ParticleStorage identity state machine; Phase 9 D-01 through D-03 and D-08.</read_first>
  <action>Write failing focused tests, then replace representative integer lanes with purpose-built Vec2 and the complete production required/optional lane bundle while retaining live/pending/vacant/retired identities and lazy allocation only where inventoried. Keep dense rows private. Use one candidate row initialization path so failed creation cannot partially allocate identity or lanes.</action>
  <verify><automated>cargo test -p liquidfun particle::storage::lanes</automated></verify>
  <acceptance_criteria>Production storage represents every inventoried Phase 9 lane and preserves the existing generation-retirement semantics.</acceptance_criteria>
  <done>Production storage represents every inventoried Phase 9 lane and preserves the existing generation-retirement semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove atomic total permutations against an independent model</name>
  <files>crates/liquidfun/src/particle/storage/permutation.rs, crates/liquidfun/src/particle/storage/properties.rs</files>
  <read_first>Phase 9 D-03; current validate-then-commit permutation; pinned RotateBuffer and SolveZombie remaps; the new lane inventory.</read_first>
  <action>Make the permutation module the exclusive route for sort, rotation, and survivor compaction. Validate complete/bijective mappings and every referenced row, build all candidate lanes/maps/references/ranges without mutating authority, then commit once. Extend proptest operations and the independent semantic model across optional-lane allocation, contacts/body contacts, pairs/triads, expiration order, group ranges, pending deletion, invalid mappings, and generation retirement. Ban direct rotate/retain/swap-remove of authoritative lanes outside this module with a source scan test.</action>
  <verify><automated>cargo test -p liquidfun particle::storage::properties</automated></verify>
  <acceptance_criteria>Valid permutations preserve stable semantics; every invalid permutation is a byte-for-byte/no-semantic-effect rejection.</acceptance_criteria>
  <done>Valid permutations preserve stable semantics; every invalid permutation is a byte-for-byte/no-semantic-effect rejection.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Dense permutation candidate to authoritative storage | Internal row mappings can corrupt every public identity and contact reference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-02-01 | T | permutation.rs | mitigate | Validate all lanes/references and commit only a complete candidate. |
| T-09-02-02 | E | identity map | mitigate | Preserve checked system scope and generation retirement through property sequences. |
</threat-model>

<verification>
Run focused property tests, then before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Review the diff for any second permutation implementation or direct lane mutation.
</verification>

<success-criteria>
- One production ParticleStorage owns all dense state.
- Every reorder and compaction passes through one total validate-prepare-commit path.
- Stable IDs, optional lanes, derived records, and deferred references remain coherent under randomized sequences.
</success-criteria>

<output>
After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-02-SUMMARY.md`.
</output>
