---
phase: 09-particle-storage-lifecycle-and-coupling
plan: 06
type: execute
wave: 6
depends_on: [09-03, 09-04, 09-05, 09-15]
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/particle.rs
  - crates/liquidfun/src/particle/lifetime.rs
  - crates/liquidfun/src/particle/storage/properties.rs
  - crates/liquidfun/tests/particle_lifetimes.rs
autonomous: true
requirements: [PART-08, PART-14]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-07-15T02-54-51
generated_at: 2026-07-15T03:26:32Z
must_haves:
  truths:
    - "Finite/infinite lifetimes, quantization, dirty ordering, oldest selection, and maximum eviction follow pinned source behavior."
    - "Zombie marking creates pending-delete snapshots before ascending-row compaction invalidates IDs."
  artifacts:
    - path: crates/liquidfun/src/particle/lifetime.rs
      provides: "32.32 clock, i32 expiration ordering, oldest selection, zombie lifecycle"
  key_links:
    - from: crates/liquidfun/src/lib.rs
      to: crates/liquidfun/src/particle.rs
      via: "public particle child-module re-exports"
      pattern: "pub use"
    - from: crates/liquidfun/src/particle/lifetime.rs
      to: crates/liquidfun/src/particle/storage/permutation.rs
      via: "single survivor compaction transaction"
      pattern: "apply_permutation"
---

<objective>
Implement the pinned lifetime, zombie, destroy-by-age, and capacity-eviction lifecycle over stable identity.

Purpose: Establish lifecycle ordering and callback-ready source points before World step integration.
Output: Pure lifetime/eviction kernel, compaction preparation, canonical tie fixture, and property tests.
</objective>

<execution-context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-03-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-04-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-15-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-RESEARCH.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Port the lifetime clock and canonical oldest ordering</name>
  <files>crates/liquidfun/src/particle/lifetime.rs, crates/liquidfun/tests/particle_lifetimes.rs</files>
  <read_first>Phase 9 D-09, D-10, D-12, D-13; pinned SolveLifetimes/DestroyOldestParticle; canonical equal-expiration oracle witness required by research.</read_first>
  <action>Load and provenance-check `reference/artifacts/phase9/lifecycle-contact-witnesses.json` from 09-15 at pinned upstream `7f20402173fd143a3988c921bc384459c6a858f2`; never derive expected tie order from Rust output. Declare the new particle child module from particle.rs, re-export supported public types through particle.rs and lib.rs, and prove external-crate reachability. Capture the equal-quantized-expiration ordering from the pinned oracle before encoding the tie rule. Implement the 32.32 elapsed-time accumulator, source-equivalent truncation toward zero, i32 expiration values, dirty reverse expiration ordering, finite-before-infinite oldest selection, expiry marking, maximum count, destroy-by-age, and immediate capacity compaction preparation. Capacity eviction must not fabricate a requested destruction-listener occurrence. Test finite/infinite, equal ties, zero/negative semantics supported by source, and no-effect invalid inputs.</action>
  <verify><automated>cargo test -p liquidfun --test particle_lifetimes lifetime</automated></verify>
  <acceptance_criteria>All ordering is source- or oracle-derived; no Rust sort/hash accident defines a compatibility tie.</acceptance_criteria>
  <done>All ordering is source- or oracle-derived; no Rust sort/hash accident defines a compatibility tie.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove zombie snapshot and compaction sequencing</name>
  <files>crates/liquidfun/src/particle/lifetime.rs, crates/liquidfun/src/particle/storage/properties.rs, crates/liquidfun/tests/particle_lifetimes.rs</files>
  <read_first>Phase 9 D-09 through D-12; pinned SolveZombie ascending scan; 09-02 permutation authority.</read_first>
  <action>Implement mark-pending with owned snapshot, ascending-old-row compaction preparation, per-row requested-listener occurrence source point, identity invalidation/retirement, and one survivor permutation commit. Extend the independent property model across tick, expire, explicit mark, oldest eviction, immediate/full compaction, pause marker, and optional lanes. Keep occurrence emission as a returned ordered effect list for the later World journal plan.</action>
  <verify><automated>cargo test -p liquidfun --test particle_lifetimes && cargo test -p liquidfun particle::storage::properties</automated></verify>
  <acceptance_criteria>Pending access is typed, snapshots precede invalidation, occurrences are ordered before invalidation, and compaction remaps every lane/reference.</acceptance_criteria>
  <done>Pending access is typed, snapshots precede invalidation, occurrences are ordered before invalidation, and compaction remaps every lane/reference.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Time/capacity inputs to lifecycle ordering | Extreme values or ties can select the wrong public identity. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-06-01 | T | lifetime.rs | mitigate | Checked fixed-point arithmetic and canonical oracle tie fixture. |
| T-09-06-02 | R | zombie occurrences | mitigate | Produce ordered owned effects at the source point before invalidation. |
</threat-model>
<verification>
Run `cargo check -p liquidfun --all-features` to verify entrypoint wiring.
Run lifetime/property tests and the canonical witness, then before committing run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Review for accidental Phase 10 solver work.
</verification>
<success-criteria>
- Lifetime clock and expiration order match the pinned source.
- Oldest eviction and immediate compaction preserve transactionality.
- Zombie destruction order, snapshots, stable IDs, and callback source points are explicit.
</success-criteria>
<output>After completion, create `.planning/phases/09-particle-storage-lifecycle-and-coupling/09-06-SUMMARY.md`.</output>
