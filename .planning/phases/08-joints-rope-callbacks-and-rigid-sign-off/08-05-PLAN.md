---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "05"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - crates/liquidfun/src/world/joint/gear.rs
  - crates/liquidfun/src/world/joint.rs
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/tests/joint_gear.rs
autonomous: true
requirements: [JOIN-01, JOIN-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-13T22:02:38.000Z
must_haves:
  truths:
    - Gear creation atomically validates two live same-world revolute/prismatic dependencies and all four derived bodies.
    - Reverse dependency edges prevent dangling gears and drive newest-first dependent-gear destruction before source joints.
    - All four coordinate combinations and positive, negative, and zero ratios use the pinned four-body Jacobian.
  artifacts: [crates/liquidfun/src/world/joint/gear.rs, crates/liquidfun/tests/joint_gear.rs]
  key_links:
    - Source-joint and body cascades collect the complete dependent-gear closure before mutation.
---
# Plan 08-05: Gear Dependencies and Four-Body Solving
<objective>Implement safe gear dependency ownership, deterministic cascades, ratio mutation, inspection, reactions, and the four-body solver.</objective>
<execution_context>Use pinned b2GearJoint coordinate/Jacobian equations while deliberately strengthening upstream deletion preconditions through safe reverse edges and complete owned cascade evidence.</execution_context>
<decision_coverage>D-06, D-07, and gear portions of D-09.</decision_coverage>
<threat_model>High risks are dangling dependencies, cross-world identity, cyclic/invalid topology, partial four-object mutation, and unbounded cascade traversal. Validate the closed two-source topology and bounded arena graph before commit, deduplicate deterministic closures, reject non-finite ratio/Jacobian intermediates, and use no unsafe code.</threat_model>
<tasks><task id="08-05-01" type="auto" tdd="true">
  <name>Implement gear lifecycle, mutations, snapshots, reactions, and solver</name>
  <files>crates/liquidfun/src/world/joint/gear.rs, crates/liquidfun/src/world/joint.rs, crates/liquidfun/src/world/object.rs, crates/liquidfun/tests/joint_gear.rs</files>
  <read_first>
    - crates/liquidfun/src/world/joint/gear.rs
    - crates/liquidfun/src/world/joint.rs
    - crates/liquidfun/src/world/object.rs
    - crates/liquidfun/tests/joint_gear.rs
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2GearJoint.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2GearJoint.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
  </read_first>
  <action>Add forward `joint1/joint2` and reverse dependent-gear edges, atomic same-world live-kind/topology validation, initial `coordinate1 + ratio * coordinate2` constant, checked ratio setter with pinned wake behavior, dependency/body snapshots, anchors, reactions, warm-start impulse, four-body velocity Jacobian solve, and position correction for RR/RP/PR/PP combinations. Before explicit source-joint or body destruction, collect dependent gears newest-first, emit `DestructionCause::GearDependencyCascade { source }` with complete snapshots, destroy them, then continue the source/root cascade. Tests cover four combinations, zero/positive/negative ratio, ratio changes, shared bodies, stale/cross-world/wrong-kind dependencies, deterministic nested body/source cascades, and no dangling reverse edges.</action>
  <verify>
    - cargo test -p liquidfun --test joint_gear --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>The focused target exits 0 and proves RR/RP/PR/PP, all ratio signs, four-body reactions, source/body dependency cascades, deterministic newest-first order, and stale/cross-world no-effects; all ordered Rust gates exit 0.</acceptance_criteria>
</task></tasks>
<verification>Diff review confirms complete preflight before adjacency mutation and no upstream dangling-pointer precondition leaks into the safe API.</verification>
<success_criteria>Gear joints are safe, source-recognizable four-body constraints with explicit deterministic dependency lifetime.</success_criteria>

