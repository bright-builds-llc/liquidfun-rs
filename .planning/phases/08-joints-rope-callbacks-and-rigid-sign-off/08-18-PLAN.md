---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "18"
type: execute
wave: 15
depends_on: ["08-17"]
files_modified:
  - crates/liquidfun/src/world/joint.rs
  - crates/liquidfun/src/world/joint/solver.rs
  - crates/liquidfun/src/world/joint/gear.rs
  - crates/liquidfun/src/world/joint/gear/solver.rs
  - crates/liquidfun/src/world/contact_solver.rs
  - crates/liquidfun/src/world/island.rs
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/tests/joint_gear.rs
  - crates/liquidfun/tests/joint_island_solver.rs
  - crates/liquidfun/tests/joint_collision_origin.rs
autonomous: true
requirements: [JOIN-01, JOIN-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-14T03:20:00Z
must_haves:
  truths:
    - Gear uses actual A/B/C/D solver lanes for RR, RP, PR, and PP combinations.
    - All eleven variants are live in source encounter order with transactional candidate commit.
    - Mixed joint/contact ordering, collision suppression, origin behavior, and late-failure atomicity remain intact.
  artifacts: [crates/liquidfun/src/world/joint/gear.rs, crates/liquidfun/src/world/joint/gear/solver.rs, crates/liquidfun/src/world/joint/solver.rs]
  key_links:
    - Plan 08-19 may build behavioral evidence only after this plan proves the complete live call graph.
---
# Plan 08-18: Four-Body Gear and Complete Island Integration
<objective>Finish four-body gear solving and prove exhaustive source-ordered, alias-safe, atomic integration of all eleven live family variants.</objective>
<execution_context>Complete gear plus cross-family island integration; do not change protocol/evidence files.</execution_context>
<decision_coverage>D-06 through D-10 solver topology, source order, dependency state, and atomic integration; JOIN-01 and JOIN-02.</decision_coverage>
<threat_model>Reject two-body gear projection, inferred pointer identity, missing source bodies, double application on aliased lanes, sorted/kind-grouped joints, partial runtime commits, and regression of collision/origin/TOI rules.</threat_model>
<tasks><task id="08-18-01" type="auto" tdd="true">
  <name>Integrate four-body gear and close the live solver call graph</name>
  <files>crates/liquidfun/src/world/joint.rs, crates/liquidfun/src/world/joint/solver.rs, crates/liquidfun/src/world/joint/gear.rs, crates/liquidfun/src/world/joint/gear/solver.rs, crates/liquidfun/src/world/contact_solver.rs, crates/liquidfun/src/world/island.rs, crates/liquidfun/src/world/object.rs, crates/liquidfun/tests/joint_gear.rs, crates/liquidfun/tests/joint_island_solver.rs, crates/liquidfun/tests/joint_collision_origin.rs</files>
  <read_first>
    - crates/liquidfun/src/world/joint/solver.rs
    - crates/liquidfun/src/world/joint/gear.rs
    - crates/liquidfun/src/world/joint/gear/solver.rs
    - crates/liquidfun/src/world/island.rs
    - crates/liquidfun/src/world/contact_solver.rs
    - crates/liquidfun/src/world/object.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2GearJoint.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Island.cpp
  </read_first>
  <action>Expose from `GearRuntime` only the semantic source-joint/body information required by the private solver. Resolve gear A/B from the two moving source endpoints and C/D from their base endpoints while building the island; require all four semantic bodies to map to solver lanes. Convert the four lanes to `GearSolverBody`, call `initialize_velocity`, `solve_velocity`, and `solve_position`, then write candidates back without lost updates when static/base lanes alias. Preserve the pinned RR/RP/PR/PP Jacobian signs, ratio grouping, warm impulse, constant, and position semantics.

Remove every remaining pending-family branch and prove exhaustive dispatch. Add tests for all four source combinations with four actual bodies, positive/negative/zero ratios where accepted, nonzero velocity and position errors, cold/warm reactions, repeated lane identities, source-order mixtures of all eleven kinds plus contacts, pinned velocity/position ordering, early exit, no joint participation in TOI, collide-connected suppression/restoration, origin shifts, and a failure in the final joint leaving all earlier bodies/contacts/proxies/runtimes unchanged.</action>
  <verify>
    - cargo test -p liquidfun --test joint_gear --all-features
    - cargo test -p liquidfun --test joint_island_solver --all-features
    - cargo test -p liquidfun --test joint_collision_origin --all-features
    - rg -n "PendingFamily|CommonConstraint|solver_linear_impulse|solver_angular_impulse" crates/liquidfun/src/world && exit 1 || true
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>RR/RP/PR/PP gear scenarios use source-faithful four-body lanes and pass; all eleven variants are reachable with no pending/generic fallback; mixed ordering, TOI exclusion, collision/origin behavior, alias handling, and final-joint atomicity pass; the ordered Rust gate exits 0.</acceptance_criteria>
</task></tasks>
<verification>Audit the complete call graph from island input through variant dispatch to family methods and staged runtime commit, plus all gear lane mappings and source-significant orders.</verification>
<success_criteria>CR-08-14-01's implementation defect is removed and protected by consumer-visible live-step regressions before evidence work begins.</success_criteria>
