---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "19"
type: execute
wave: 16
depends_on: ["08-18"]
files_modified:
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/types.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/witness_registry.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs
  - crates/liquidfun-test-protocol/src/schema/rigid_world.rs
  - crates/liquidfun-test-protocol/src/tolerance/rigid_policy.rs
  - protocol/fixtures/accepted/rigid-world-request.jsonl
  - protocol/schemas/scenario-v1.schema.json
  - protocol/schemas/trace-v1.schema.json
  - protocol/tolerances/phase8-v1.toml
autonomous: true
requirements: [RIGD-11, JOIN-01, JOIN-02, JOIN-03, JOIN-04, JOIN-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-14T03:20:00Z
must_haves:
  truths:
    - Every step-dependent Phase 8 family is structurally required to perform meaningful positive steps before semantic observations.
    - The accepted corpus exercises every joint kind, all four gear combinations, callback timing branches, and destruction cascades.
    - The policy and schemas remain closed, bounded, and retain every Phase 6/7 family.
  artifacts: [protocol/fixtures/accepted/rigid-world-request.jsonl, protocol/tolerances/phase8-v1.toml, protocol/schemas/trace-v1.schema.json]
  key_links:
    - Plans 08-20 and 08-21 must execute this typed request independently rather than synthesizing expected observations.
---
# Plan 08-19: Step-Bearing Typed Corpus, Schemas, and Policy
<objective>Replace name-only Phase 8 witnesses with a validator-backed typed corpus that necessarily executes and observes every claimed solver, callback, and destruction behavior.</objective>
<execution_context>Change only protocol authority, schemas, accepted fixture, and policy. Adapter execution follows in Plans 08-20 and 08-21.</execution_context>
<decision_coverage>D-23, D-24, and D-26 fail-closed corpus requirements; all Phase 8 requirement IDs remain pending canonical evidence.</decision_coverage>
<threat_model>Reject `*Covered` placeholders, action-kind presence without valid ordering, zero-step solver claims, missing observations, wildcard/fallback tolerances, widened thresholds, unbounded decode, and removal or mutation of retained Phase 6/7 families.</threat_model>
<tasks><task id="08-19-01" type="auto" tdd="true">
  <name>Define and validate the complete step-bearing Phase 8 witness corpus</name>
  <files>crates/liquidfun-test-protocol/src/scenario/rigid_world/types.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/witness_registry.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs, crates/liquidfun-test-protocol/src/schema/rigid_world.rs, crates/liquidfun-test-protocol/src/tolerance/rigid_policy.rs, protocol/fixtures/accepted/rigid-world-request.jsonl, protocol/schemas/scenario-v1.schema.json, protocol/schemas/trace-v1.schema.json, protocol/tolerances/phase8-v1.toml</files>
  <read_first>
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-REVIEW.md
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/witness_registry.rs
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/types.rs
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs
    - crates/liquidfun-test-protocol/src/tolerance/rigid_policy.rs
    - protocol/fixtures/accepted/rigid-world-request.jsonl
    - protocol/tolerances/phase8-v1.toml
  </read_first>
  <action>Replace the ten one-line Phase 8 `*Covered` witness values with explicit behavior witnesses. Validation must enforce ordered create -> nonzero state/mutation -> positive `Step`/`ConfiguredStep` -> matching `InspectJoint`/`InspectBody` for joint families; directive -> eligible step -> lifecycle for callbacks; and touching step -> destructive action -> lifecycle for cascades. Reject zero timestep, observation before step, missing joint kind/gear combination, callback steps with ineligible geometry, and destruction without a preexisting touching contact or dependent gear. Preserve all nine retained Phase 6/7 family definitions, actions, witnesses, and order.

Rewrite only the ten Phase 8 fixture timelines with finite nondegenerate geometry and stable IDs. Use 1/60-second steps, eight velocity iterations, three position iterations, and a second step for warm-start claims. Include: all eleven joint kinds stepped/inspected; revolute/prismatic lower/upper/equal limits and motors; rigid/soft distance, pulley ratio, mouse target/force; wheel line/spring/motor, rigid/soft weld, friction caps, inactive/upper rope-joint, motor correction; post-step body state, coordinate, speed, branch, force, and torque; RR/RP/PR/PP gear over actual four-body topology with positive/negative/zero accepted ratios; mixed joint/contact source order and collision suppression/restoration; filter rejection then admission, FilterDecision -> ContactCreated/BeginContact -> PreSolve -> PostSolve, one-update disable, material/tangent speed, repeated occurrence; and touching explicit/source/body destruction with dependent-gear-first and EndContact/ContactDestroyed -> joint/fixture goodbye -> BodyDestroyed order while explicit fixture/joint actions fabricate no goodbye.

Strengthen result bounds and expected-observation validation for the expanded ordered records. Update witness-to-policy registration so joint families require anchor, coordinate, speed, branch, reaction-force, and reaction-torque paths; gear also requires dependency order; callbacks require filter/pre-solve exact bits plus lifecycle order/kind/identity/multiplicity; destruction requires all lifecycle structural paths. Keep exact transported bits D1 and existing named computed policies D2; add no wildcard or wider threshold. Regenerate scenario/trace schema presentations and replace the request's tolerance SHA-256 with the canonical hash of the updated `phase8-v1` profile.</action>
  <verify>
    - cargo test -p liquidfun-test-protocol rigid_world_phase8 --all-features
    - cargo test -p liquidfun-test-protocol rigid_policy_phase8 --all-features
    - cargo test -p liquidfun-test-protocol --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
    - git diff --check
  </verify>
  <acceptance_criteria>Typed validation proves ordered meaningful step/evidence sequences for all eight step-dependent families; the fixture covers every listed joint/gear/callback/destruction branch; Phase 6/7 registry order is unchanged; schemas are generated from typed authority; policy/hash are exact and fail closed; all tests and ordered Rust gates exit 0.</acceptance_criteria>
</task></tasks>
<verification>Audit each Phase 8 timeline against its explicit witnesses, each witness against ordered validation, every observation against a closed policy path, schema byte stability, and exact retention of prior families.</verification>
<success_criteria>CR-08-14-02's corpus defect is structurally impossible before either execution adapter is updated.</success_criteria>
