---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "07"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - crates/liquidfun/src/rope.rs
  - crates/liquidfun/src/rope/core.rs
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/tests/standalone_rope.rs
autonomous: true
requirements: [JOIN-03, JOIN-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-13T22:02:38.000Z
must_haves:
  truths:
    - rope::Rope is independent of World, JointId, contacts, bodies, islands, and RopeJointDef.
    - Step order is integrate, then stretch/bend/stretch per iteration, then velocity reconstruction; zero timestep is a no-op.
    - Invalid lengths, state, angle, timestep, or iteration counts fail transactionally.
  artifacts: [crates/liquidfun/src/rope.rs, crates/liquidfun/src/rope/core.rs, crates/liquidfun/tests/standalone_rope.rs]
  key_links:
    - Public borrow-scoped vertex inspection reads owned pure-core state without exposing mutable buffers.
---
# Plan 08-07: Standalone Rope
<objective>Implement the pinned standalone rope as a separate checked pure module with focused behavioral coverage.</objective>
<execution_context>Translate b2Rope source order and f32 grouping directly; use invariant-bearing owned input and a functional core.</execution_context>
<decision_coverage>D-11 and D-12.</decision_coverage>
<threat_model>Untrusted vertex/mass lengths, non-finite values, timestep, and iteration counts can cause out-of-bounds access, numeric poisoning, or resource exhaustion. Require equal nonempty bounded lanes, finite values, nonnegative masses/stiffness/damping, bounded iterations, candidate stepping, and no unsafe code.</threat_model>
<tasks><task id="08-07-01" type="auto" tdd="true">
  <name>Implement checked standalone rope initialization, stepping, angle control, and inspection</name>
  <files>crates/liquidfun/src/rope.rs, crates/liquidfun/src/rope/core.rs, crates/liquidfun/src/lib.rs, crates/liquidfun/tests/standalone_rope.rs</files>
  <read_first>
    - crates/liquidfun/src/rope.rs
    - crates/liquidfun/src/rope/core.rs
    - crates/liquidfun/src/lib.rs
    - crates/liquidfun/tests/standalone_rope.rs
    - crates/liquidfun/src/math.rs
    - .planning/phases/04-math-settings-and-numerical-policy/04-CONTEXT.md
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Rope/b2Rope.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Rope/b2Rope.cpp
  </read_first>
  <action>Create `rope::RopeDef`, `Rope`, `RopeError`, and borrow-scoped vertex slice inspection. Validate equal vertex/mass lanes, minimum source-required count, finite gravity/damping/stiffness/positions/masses, nonnegative masses with zero inverse mass for fixed vertices, finite nonnegative timestep, and a reviewed bounded iteration newtype. Preserve source rest lengths/angles, damping expression, index-order integration, stretch/bend/stretch iteration order, zero-step no-op, index-order velocity reconstruction, and source angle wrapping in `set_angle`. Step a complete candidate and replace once. Tests cover fixed/free vertices, gravity/damping, zero step, 0/1/N iterations, constraint order, angle wrap boundaries, invalid lane lengths, non-finite/overflow, and bit-identical no-effect errors.</action>
  <verify>
    - cargo test -p liquidfun --test standalone_rope --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>The focused target exits 0; `rg 'pub struct Rope|pub struct RopeDef|stretch.*bend.*stretch' crates/liquidfun/src/rope*` finds the separate contract/order; tests prove fixed/free, zero step, multiple iterations, wrapping, and transactional invalid input; all ordered Rust gates exit 0.</acceptance_criteria>
</task></tasks>
<verification>Confirm `rope.rs` imports neither World nor JointId and the public docs explicitly distinguish `rope::Rope` from `RopeJointDef`.</verification>
<success_criteria>The standalone rope is a bounded renderer/world-independent pure simulation surface with pinned behavior.</success_criteria>

