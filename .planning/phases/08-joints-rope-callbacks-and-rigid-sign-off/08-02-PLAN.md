---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - crates/liquidfun/src/world/joint/revolute.rs
  - crates/liquidfun/src/world/joint/prismatic.rs
  - crates/liquidfun/tests/joint_revolute.rs
  - crates/liquidfun/tests/joint_prismatic.rs
autonomous: true
requirements: [JOIN-01, JOIN-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-13T22:02:38.000Z
must_haves:
  truths:
    - Revolute and prismatic joints expose complete limits, motors, coordinates, speeds, anchors, reactions, setters, and solver state.
    - Equal/lower/upper/inactive limit branches, warm starts, cache resets, and setter wake behavior preserve pinned source order.
    - Their coordinates are stable semantic inputs for gear joints.
  artifacts:
    - crates/liquidfun/src/world/joint/revolute.rs
    - crates/liquidfun/src/world/joint/prismatic.rs
  key_links:
    - Both solvers consume copied island body state and return checked candidate constraints.
---

# Plan 08-02: Revolute and Prismatic Joints

<objective>Implement and independently test the two coordinate-producing limit/motor joints that establish the shared solver pattern and gear inputs.</objective>
<execution_context>Translate the pinned scalar operations and setter branches literally enough to audit expression grouping; public storage stays owned/snapshotted and solver scratch remains private.</execution_context>
<decision_coverage>D-03, D-04, D-05, and the revolute/prismatic parts of D-09.</decision_coverage>
<threat_model>Reject non-finite limits, speeds, torques/forces, axes, derived impulses, stale/cross-world IDs, lock, and poison before effects. Bound solver iterations through existing StepConfiguration. Candidate constraint initialization prevents a late non-finite value from partially mutating body or cache state; use no unsafe code.</threat_model>
<tasks>
<task id="08-02-01" type="auto" tdd="true">
  <name>Implement revolute definitions, mutations, inspection, reactions, and solver</name>
  <files>crates/liquidfun/src/world/joint/revolute.rs, crates/liquidfun/tests/joint_revolute.rs</files>
  <read_first>
    - crates/liquidfun/src/world/joint/revolute.rs
    - crates/liquidfun/tests/joint_revolute.rs
    - crates/liquidfun/src/world/joint.rs
    - crates/liquidfun/src/joint/definition.rs
    - crates/liquidfun/src/joint/snapshot.rs
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2RevoluteJoint.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2RevoluteJoint.cpp
  </read_first>
  <action>Implement local anchors/reference angle, angle/speed, lower/upper/equal limits, motor enable/speed/max torque, `motor_torque(inverse_timestep)`, anchors, reaction force/torque, and checked setters with each source branch's changed-only wake, impulse clearing, and limit-state reset. Implement private velocity/position scratch, warm-start scaling, 2x2/3x3 motor-limit solves, fixed-rotation branch, impulse accumulation, and pinned early exit. Tests cover every definition invariant, setter equal/changed/invalid behavior, all four limit states, cold/warm/zero/positive timestep, motor cap, reaction values, and no-effect failures.</action>
  <verify>
    - cargo test -p liquidfun --test joint_revolute --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>The focused target exits 0 and has named tests for inactive/lower/upper/equal limits, motor cap, wake/no-wake, cache reset, warm start, coordinate/speed, anchors, and reactions; all ordered Rust gates exit 0.</acceptance_criteria>
</task>
<task id="08-02-02" type="auto" tdd="true">
  <name>Implement prismatic definitions, mutations, inspection, reactions, and solver</name>
  <files>crates/liquidfun/src/world/joint/prismatic.rs, crates/liquidfun/tests/joint_prismatic.rs</files>
  <read_first>
    - crates/liquidfun/src/world/joint/prismatic.rs
    - crates/liquidfun/tests/joint_prismatic.rs
    - crates/liquidfun/src/world/joint.rs
    - crates/liquidfun/src/joint/definition.rs
    - crates/liquidfun/src/joint/snapshot.rs
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2PrismaticJoint.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2PrismaticJoint.cpp
  </read_first>
  <action>Implement normalized checked local axis, perpendicular/reference constraints, translation/speed, limit enable/range, motor enable/speed/max force, `motor_force(inverse_timestep)`, anchors, reactions, and source-exact setters. Implement the coupled 2x2/3x3 velocity and position solvers, warm-start caches, equal-limit branch, motor clamp, and coordinate accessor used by gear. Test invalid/zero axes, all limit states, every setter wake/cache branch, cold/warm/zero/positive timestep, anchors, reactions, and transactional derived-overflow rejection.</action>
  <verify>
    - cargo test -p liquidfun --test joint_prismatic --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>The focused target exits 0 and proves normalized-axis validation, four limit states, motor cap, wake/cache branches, gear coordinate, anchors, reactions, and atomic errors; all ordered Rust gates exit 0.</acceptance_criteria>
</task>
</tasks>
<verification>Diff review confirms per-kind exhaustive state and no shared epsilon, hash ordering, trait-object dispatch, or context-free reaction storage.</verification>
<success_criteria>Revolute and prismatic behavior is complete enough for mixed islands and all four gear combinations.</success_criteria>

