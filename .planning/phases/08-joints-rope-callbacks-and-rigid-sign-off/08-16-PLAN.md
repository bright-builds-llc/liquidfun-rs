---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "16"
type: execute
wave: 13
depends_on: ["08-15"]
files_modified:
  - crates/liquidfun/src/lib.rs
  - README.md
  - ARCHITECTURE.md
  - TESTING.md
  - COMPATIBILITY.md
  - reference/compatibility.json
  - tools/xtask/src/docs.rs
  - tools/xtask/tests/docs_contract.rs
autonomous: true
requirements: [RIGD-11, JOIN-01, JOIN-02, JOIN-03, JOIN-04, JOIN-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-13T22:02:38.000Z
must_haves:
  truths:
    - Documentation consumes only the exact successful Plan 08-15 run and artifact identities.
    - The ledger and generated compatibility documentation claim only the closed Phase 8 scalar rigid-body and joint corpus.
    - RIGD-10, particles, D3, cross-platform parity, performance, testbed, and release readiness remain explicitly pending.
  artifacts: [reference/compatibility.json, COMPATIBILITY.md, TESTING.md]
  key_links:
    - Every maturity claim names the Plan 08-15 run URL, RUN_ID, HEAD_SHA, artifact names, provenance identity, and residual limits.
---
# Plan 08-16: Post-Evidence Compatibility Sign-Off
<objective>After exact canonical evidence passes, update only documentation and compatibility surfaces with the narrowly justified Phase 8 sign-off.</objective>
<execution_context>Consume the successful Plan 08-15 evidence. Do not change physics, protocol, oracle, tolerances, workflow behavior, or planning requirements to manufacture completion.</execution_context>
<decision_coverage>D-21 and D-26 truthful sign-off for RIGD-11 and JOIN-01 through JOIN-05.</decision_coverage>
<threat_model>Prevent stale-run substitution, broader maturity claims, generated/manual ledger drift, hidden pending scope, and documentation that treats D0, D2, or configured CI as canonical D1.</threat_model>
<tasks><task id="08-16-01" type="auto" tdd="true">
  <name>Publish the scoped Phase 8 documentation and compatibility ledger</name>
  <files>crates/liquidfun/src/lib.rs, README.md, ARCHITECTURE.md, TESTING.md, COMPATIBILITY.md, reference/compatibility.json, tools/xtask/src/docs.rs, tools/xtask/tests/docs_contract.rs</files>
  <read_first>
    - target/phase8-ci-evidence/canonical/identity.json
    - target/phase8-ci-evidence/sanitizer/identity.json
    - crates/liquidfun/src/lib.rs
    - README.md
    - ARCHITECTURE.md
    - TESTING.md
    - COMPATIBILITY.md
    - reference/compatibility.json
    - tools/xtask/src/docs.rs
    - tools/xtask/tests/docs_contract.rs
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
  </read_first>
  <action>First revalidate the exact Plan 08-15 `RUN_ID`, `HEAD_SHA`, run URL, two artifact names, and both identity files. Only then update the compatibility ledger, generator/contracts, crate-level documentation, and non-GSD Markdown to use the exact phrase `canonical scalar rigid-body and joint differential sign-off for the closed Phase 8 corpus`. Include the run URL, `RUN_ID`, `HEAD_SHA`, artifact names, Rust/CMake/Ninja/Clang versions, upstream revision, policy profile, accumulated family counts, and residual limitations. Keep RIGD-10, particles, D3, cross-platform parity, performance, testbed, and release readiness pending. If evidence revalidation fails, make no sign-off change and leave RIGD-11/JOIN-05 pending. Run Markdown formatting only through `just markdown-check`; do not mdformat `.planning/**`.</action>
  <verify>
    - cargo xtask inventory generate
    - cargo xtask inventory check
    - cargo xtask docs check
    - just markdown-check
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
    - git diff --check
  </verify>
  <acceptance_criteria>Ledger, generated docs, contracts, and public maturity prose agree on the exact scoped sign-off and evidence identities; all excluded maturity areas remain pending; only `<files>` changed; all checks exit 0.</acceptance_criteria>
</task></tasks>
<verification>Review the final diff, generated ledger, evidence links and identities, accumulated family counts, every maturity sentence, and the ordered verification results.</verification>
<success_criteria>Phase 8 closes only with truthful documentation tied to actual exact-commit canonical and sanitizer evidence; no broader parity or release claim is introduced.</success_criteria>
