---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "06"
type: execute
wave: 4
depends_on: ["08-02", "08-03", "08-04", "08-05"]
files_modified:
  - crates/liquidfun/src/world/joint/solver.rs
  - crates/liquidfun/src/world/island.rs
  - crates/liquidfun/src/world/contact_solver.rs
  - crates/liquidfun/src/world/contact_manager.rs
  - crates/liquidfun/src/world/origin.rs
  - crates/liquidfun/src/world/step.rs
  - crates/liquidfun/tests/joint_island_solver.rs
  - crates/liquidfun/tests/joint_collision_origin.rs
autonomous: true
requirements: [JOIN-01, JOIN-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-13T22:02:38.000Z
must_haves:
  truths:
    - Island DFS preserves newest-first body/world joint lanes and encounter order without hashes or kind grouping.
    - Discrete phase order is contacts-init, joints-init, joints-then-contacts velocity, contacts-then-joints position, then sleep; TOI excludes ordinary joints.
    - Joint initialization and complete island solving are transactional, and origin shifting changes only pulley ground anchors and mouse targets.
  artifacts: [crates/liquidfun/src/world/joint/solver.rs, crates/liquidfun/tests/joint_island_solver.rs]
  key_links:
    - Joint solver scratch consumes the same copied body position/velocity arrays as contact constraints.
---
# Plan 08-06: Island, Collision, and Origin Integration
<objective>Wire all eleven joint solvers into source-ordered discrete islands, collision suppression/refiltering, atomic step commits, and origin shifting.</objective>
<execution_context>Preserve Phase 7 transaction, sleep, and CCD behavior; populate the reserved joint lane and add exhaustive enum dispatch rather than trait objects.</execution_context>
<decision_coverage>D-08, D-09, and D-10.</decision_coverage>
<threat_model>Late non-finite initialization must not leak earlier island results; malicious topology must not cause duplicate/unbounded traversal; collision suppression must not use stale IDs; TOI must not accidentally consume ordinary joints. Use bounded visited sets indexed by validated arenas, checked scratch allocation, one island commit, panic-free errors, and no unsafe code.</threat_model>
<tasks><task id="08-06-01" type="auto" tdd="true">
  <name>Integrate exhaustive joint dispatch, island order, refiltering, and origin shifts</name>
  <files>crates/liquidfun/src/world/joint/solver.rs, crates/liquidfun/src/world/island.rs, crates/liquidfun/src/world/contact_solver.rs, crates/liquidfun/src/world/contact_manager.rs, crates/liquidfun/src/world/origin.rs, crates/liquidfun/src/world/step.rs, crates/liquidfun/tests/joint_island_solver.rs, crates/liquidfun/tests/joint_collision_origin.rs</files>
  <read_first>
    - crates/liquidfun/src/world/joint/solver.rs
    - crates/liquidfun/src/world/island.rs
    - crates/liquidfun/src/world/contact_solver.rs
    - crates/liquidfun/src/world/contact_manager.rs
    - crates/liquidfun/src/world/origin.rs
    - crates/liquidfun/src/world/step.rs
    - crates/liquidfun/tests/joint_island_solver.rs
    - crates/liquidfun/tests/joint_collision_origin.rs
    - .planning/phases/07-rigid-solver-world-operations-and-ccd/07-CONTEXT.md
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Island.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.cpp
  </read_first>
  <action>Populate `Island::joint_ids` during DFS from each body's newest-first adjacency, marking/clearing private island flags exactly once. Add closed `JointVelocityConstraint`/`JointPositionConstraint` dispatch for all eleven kinds. Build every joint/contact constraint before committing any island; initialize contacts then joints; for each velocity iteration solve joints then contacts; for each position iteration solve contacts then joints with pinned early exit; copy impulses and body state back only after complete success; evaluate sleep last. Leave TOI islands joint-free. Refilter existing and future contacts for `collide_connected=false`, including multiple-joint last-suppressor behavior. Extend `shift_origin` candidate validation so only pulley ground anchors and mouse targets subtract the finite shift. Tests use mixed-kind/order-sensitive islands, late initialization failure, warm start, zero/positive timestep, existing/future collision pairs, multiple suppressors, TOI regression, and origin snapshots.</action>
  <verify>
    - cargo test -p liquidfun --test joint_island_solver --all-features
    - cargo test -p liquidfun --test joint_collision_origin --all-features
    - cargo test -p liquidfun --test rigid_ccd --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>Focused tests prove exact phase/order logs, all eleven dispatch branches, atomic late failure, warm caches, collision suppression/refiltering, TOI exclusion, and origin-shift selectivity; all ordered Rust gates exit 0.</acceptance_criteria>
</task></tasks>
<verification>Source scan finds no `HashMap`/`HashSet` in solver-visible order and no joint solve call in TOI island code.</verification>
<success_criteria>All joint kinds participate in the complete scalar discrete world with pinned ordering and unchanged Phase 7 CCD guarantees.</success_criteria>

