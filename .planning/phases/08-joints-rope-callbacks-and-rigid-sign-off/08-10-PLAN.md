---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "10"
type: execute
wave: 7
depends_on: ["08-06", "08-09"]
files_modified:
  - crates/liquidfun/src/world/diagnostics.rs
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/rigid_differential.rs
  - crates/liquidfun/tests/semantic_reconstruction.rs
  - crates/liquidfun/tests/world_diagnostics.rs
autonomous: true
requirements: [JOIN-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-13T22:02:38.000Z
must_haves:
  truths:
    - Semantic reconstruction records bodies/fixtures first, non-gear joints next, and gear joints last with stable reconstruction indices.
    - Mouse reconstruction is explicitly unsupported; deterministic text is secondary and not a persistence/whitespace contract.
    - Diagnostics expose exact semantic counts/tree metrics and no timing-profile or complete RIGD-10 claim.
  artifacts: [crates/liquidfun/src/world/diagnostics.rs, crates/liquidfun/tests/semantic_reconstruction.rs]
  key_links:
    - Reconstruction consumes public semantic snapshots, not arena layout or raw memory.
---
# Plan 08-10: Semantic Reconstruction and Diagnostics
<objective>Implement bounded typed world reconstruction plus exact Phase 8 counts/tree metrics without turning dump text into persistence or claiming RIGD-10.</objective>
<execution_context>Use b2World::Dump and per-joint Dump only to inventory semantic fields/order; typed records are authoritative.</execution_context>
<decision_coverage>D-19, D-20, D-21, and D-22.</decision_coverage>
<threat_model>Large worlds can exhaust reconstruction memory and internal indices can leak storage layout. Enforce reviewed record/count bounds, checked reconstruction indices, owned semantic IDs, deterministic two-pass traversal, no absolute paths/pointers/raw bytes, and no unsafe code. Text rendering must escape labels and never be parsed as authority.</threat_model>
<tasks><task id="08-10-01" type="auto" tdd="true">
  <name>Build typed reconstruction, secondary text rendering, and bounded diagnostic snapshots</name>
  <files>crates/liquidfun/src/world/diagnostics.rs, crates/liquidfun/src/world.rs, crates/liquidfun/src/rigid_differential.rs, crates/liquidfun/tests/semantic_reconstruction.rs, crates/liquidfun/tests/world_diagnostics.rs</files>
  <read_first>
    - crates/liquidfun/src/world/diagnostics.rs
    - crates/liquidfun/src/world.rs
    - crates/liquidfun/src/rigid_differential.rs
    - crates/liquidfun/tests/semantic_reconstruction.rs
    - crates/liquidfun/tests/world_diagnostics.rs
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2Joint.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/
  </read_first>
  <action>Add owned `WorldReconstruction` records for gravity, bodies, fixture shape/material/filter state, all joint definition/dependency fields, and stable reconstruction indices. Emit bodies with fixtures first, all non-gear joints second, gear joints last; represent mouse as `ReconstructionSupport::Unsupported(MouseJoint)` and never synthesize missing fields. Add deterministic diagnostic text renderer as a one-way human view. Add `WorldDiagnostics` exact body/fixture/joint/contact/manifold-point/proxy counts, tree height/balance, and tree quality float for later named policy; exclude wall-clock timings and debug-draw APIs. Tests cover empty/mixed worlds, every joint kind, gear ordering, mouse unsupported, origin shift, destroyed identities, deterministic rendering, record bounds, and exact metrics.</action>
  <verify>
    - cargo test -p liquidfun --test semantic_reconstruction --all-features
    - cargo test -p liquidfun --test world_diagnostics --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>Focused tests cover all eleven joints, bodies/fixtures→non-gear→gear order, mouse unsupported, deterministic secondary text, exact counts/tree metrics, and bounds; `rg 'RIGD-10.*Complete|timing profile'` finds no new claim; all ordered Rust gates exit 0.</acceptance_criteria>
</task></tasks>
<verification>Confirm reconstruction records contain no pointers, arena slots, raw memory, wall-clock timing, renderer callbacks, or round-trip promise.</verification>
<success_criteria>Every Phase 8 diagnostic representation has typed, bounded, renderer-neutral semantic evidence.</success_criteria>

