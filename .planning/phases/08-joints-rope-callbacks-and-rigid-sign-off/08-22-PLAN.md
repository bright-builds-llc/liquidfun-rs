---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "22"
type: execute
wave: 19
depends_on: ["08-21"]
files_modified:
  - crates/liquidfun-differential/src/rigid_evidence.rs
  - crates/liquidfun-differential/src/rigid_evidence/phase8.rs
  - crates/liquidfun-differential/tests/phase8_comparator.rs
  - tools/xtask/src/differential.rs
  - tools/xtask/tests/differential_cli.rs
  - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-REVIEW.md
  - .github/workflows/oracle.yml
autonomous: true
requirements: [RIGD-11, JOIN-01, JOIN-02, JOIN-03, JOIN-04, JOIN-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-14T03:20:00Z
must_haves:
  truths:
    - Comparator coverage fails at stable first-divergence paths for every strengthened semantic field.
    - Debug, release, replay, exactly two D0 runs, and configured/built fail-fast ASan/UBSan pass the complete corpus before review closure.
    - CR-08-14-01 and CR-08-14-02 are resolved before oracle.yml is made publication-ready.
    - This plan neither pushes nor dispatches CI nor makes compatibility claims.
  artifacts: [crates/liquidfun-differential/tests/phase8_comparator.rs, .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-REVIEW.md, .github/workflows/oracle.yml]
  key_links:
    - Plan 08-23 publishes and validates only the exact clean commit produced after this review/workflow gate.
---
# Plan 08-22: Comparator, Local Evidence, Review Closure, and Oracle Readiness
<objective>Close semantic comparison, prove the complete corpus locally across every required build mode, resolve both review blockers, and prepare exact-identity Oracle CI.</objective>
<execution_context>Run autonomously without pushing or dispatching. Update oracle.yml only after every local gate and review finding passes.</execution_context>
<decision_coverage>D-23 through D-26 evidence truthfulness and pre-publication enforcement; all Phase 8 requirements remain pending Plan 08-23 D1.</decision_coverage>
<threat_model>Reject missing comparator paths, accepted structural drift, wildcard/widened policy, skipped configure/build, stale oracle binary, fewer/more than two D0 runs, sanitizer suppression, unresolved review status, configured-only CI, retries, continue-on-error, tracked evidence writes, and premature claims.</threat_model>
<tasks><task id="08-22-01" type="auto" tdd="true">
  <name>Close comparator coverage and command contracts</name>
  <files>crates/liquidfun-differential/src/rigid_evidence.rs, crates/liquidfun-differential/src/rigid_evidence/phase8.rs, crates/liquidfun-differential/tests/phase8_comparator.rs, tools/xtask/src/differential.rs, tools/xtask/tests/differential_cli.rs</files>
  <read_first>
    - protocol/fixtures/accepted/rigid-world-request.jsonl
    - protocol/tolerances/phase8-v1.toml
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs
    - crates/liquidfun-test-protocol/src/tolerance/rigid_policy.rs
    - crates/liquidfun-differential/src/rigid_evidence/phase8.rs
    - crates/liquidfun-differential/tests/phase8_comparator.rs
    - tools/xtask/src/differential.rs
  </read_first>
  <action>Compare retained Phase 7 evidence first, then every Phase 8 observation in exact action/occurrence order. Cover body/joint identity, kind, bodies, dependencies, collide-connected, branch, coordinate, speed, anchors, reactions, all gear results, rope, callback directive bits and lifecycle order/kind/identity/multiplicity, destruction order/multiplicity, reconstruction, diagnostics, field presence, and signed zero under only the named policy path. Unknown, missing, duplicate, wildcard, or unregistered fields are harness errors.

Add one-concern mutations for every strengthened field and require a stable first-divergence semantic path; add missing/extra/reordered occurrence tests, all four gear dependency/result mutations, callback multiplicity/order/directive mutations, destruction identity/order mutations, retained-family removal, signed-zero distinction, missing policy, wildcard policy, and threshold widening. Keep xtask rigid debug/release/replay/`verify-determinism --runs 2`/ASan command shapes bound to the accepted request and phase8-v1.</action>
  <verify>
    - cargo test -p liquidfun-differential --test phase8_comparator --all-features
    - cargo test -p xtask --test differential_cli --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>Every strengthened semantic mutation fails at its named first-divergence path, retained coverage cannot disappear, policy drift fails closed, command-shape tests pass, and the ordered Rust gate exits 0.</acceptance_criteria>
</task>
<task id="08-22-02" type="auto" tdd="false">
  <name>Run complete local evidence and resolve both review findings</name>
  <files>.planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-REVIEW.md</files>
  <read_first>
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-REVIEW.md
    - crates/liquidfun/src/world/joint/solver.rs
    - protocol/fixtures/accepted/rigid-world-request.jsonl
    - crates/liquidfun-differential/tests/phase8_comparator.rs
    - tools/reference/tests/protocol_tests.cpp
    - tools/reference/CMakePresets.json
  </read_first>
  <action>Configure and build `oracle-debug`, `oracle-release`, and `oracle-asan-ubsan` from current sources before using them. Run debug protocol CTest, debug compare, release compare, debug replay, exactly one determinism command with `--runs 2`, then fail-fast sanitizer protocol CTest and sanitizer rigid compare with `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1` and `ASAN_OPTIONS=abort_on_error=1:halt_on_error=1`. Run the ordered Rust gate afterward.

Re-audit CR-08-14-01's live call graph, eleven family variants, A/B/C/D gear lanes, staged runtime commit, and focused tests; re-audit CR-08-14-02's validator-proven step sequences, fixture, native adapter, C++ adapter, and comparator mutations. Preserve original finding evidence, add resolution commits/tests/results, set each `status: resolved`, and set review frontmatter to zero blocking/open findings only when every named command exits 0. Otherwise leave the finding open and stop before Task 03.</action>
  <verify>
    - cargo xtask upstream configure --preset oracle-debug
    - cargo xtask upstream build --preset oracle-debug
    - cargo xtask upstream configure --preset oracle-release
    - cargo xtask upstream build --preset oracle-release
    - cargo xtask upstream configure --preset oracle-asan-ubsan
    - cargo xtask upstream build --preset oracle-asan-ubsan
    - cmake --build target/reference/oracle-debug --target liquidfun-reference-protocol-tests
    - ctest --test-dir target/reference/oracle-debug --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$'
    - cargo xtask differential compare --scenario rigid-world --preset oracle-debug --session-profile one-shot
    - cargo xtask differential compare --scenario rigid-world --preset oracle-release --session-profile one-shot
    - cargo xtask differential replay --scenario rigid-world --preset oracle-debug --session-profile one-shot
    - cargo xtask differential verify-determinism --scenario rigid-world --preset oracle-debug --runs 2
    - cmake --build target/reference/oracle-asan-ubsan --target liquidfun-reference-protocol-tests
    - UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 ctest --test-dir target/reference/oracle-asan-ubsan --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$'
    - UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 cargo xtask differential compare --scenario rigid-world --preset oracle-asan-ubsan --session-profile one-shot
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
    - git diff --check
  </verify>
  <acceptance_criteria>All three presets are configured and built before execution; debug/release/replay/exactly-two-run D0/protocol/sanitizer/ordered Rust evidence passes; both findings are resolved with recorded evidence and zero open blockers; only `08-REVIEW.md` changes in this task.</acceptance_criteria>
</task>
<task id="08-22-03" type="auto" tdd="true">
  <name>Prepare exact-identity Oracle CI after review closure</name>
  <files>.github/workflows/oracle.yml</files>
  <read_first>
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-REVIEW.md
    - .github/workflows/oracle.yml
    - tools/xtask/src/differential.rs
    - reference/upstream-lock.toml
    - protocol/tolerances/phase8-v1.toml
  </read_first>
  <action>Require both findings resolved, then update only `oracle.yml`. Rename stale Phase 6 rigid labels to Phase 8. `canonical-linux` must execute the accumulated phase8-v1 debug/release comparison, replay, exactly one `verify-determinism --runs 2`, provenance/policy checks, and read-only assertion. `sanitizer-linux` must use canonical Clang 22, explicitly configure/build oracle-asan-ubsan, build/run protocol CTest, and compare the same complete corpus with fail-fast ASan/UBSan. Remove duplicate setup lines.

Each successful job writes logs and exactly one `identity.json` with numeric `run_id`, exact `head_sha`, `job`, Rust 1.97.0, CMake 4.3.3, Ninja 1.13.2, Clang 22.1.8, upstream `7f20402173fd143a3988c921bc384459c6a858f2`, and policy `phase8-v1`; upload exactly `phase8-canonical-${{ github.run_id }}-${{ github.sha }}` or `phase8-sanitizer-${{ github.run_id }}-${{ github.sha }}` through the existing SHA-pinned action. Preserve prior phase jobs/failure evidence; add no retry, continue-on-error, tracked evidence write, secret artifact, push, or dispatch.</action>
  <verify>
    - cargo xtask provenance check
    - cargo test -p xtask --test differential_cli --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
    - git diff --check
  </verify>
  <acceptance_criteria>`oracle.yml` contains exact complete Phase 8 canonical/sanitizer jobs and unique identities, explicit sanitizer configure/build, no stale label/duplicate setup/retry/continue-on-error/evidence mutation, no workflow dispatch, and all checks exit 0.</acceptance_criteria>
</task></tasks>
<verification>Review comparator closure, exact local command results and preset freshness, REVIEW resolution state, workflow diff, strict task allowlists, ordered Rust gate, and `git diff --check`.</verification>
<success_criteria>Both Phase 8 review blockers are resolved by complete local evidence and Oracle CI is ready for the human-controlled exact-ref checkpoint without any sign-off claim.</success_criteria>
