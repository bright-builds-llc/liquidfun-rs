---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "04"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - crates/liquidfun/src/world/joint/wheel.rs
  - crates/liquidfun/src/world/joint/weld.rs
  - crates/liquidfun/src/world/joint/friction.rs
  - crates/liquidfun/src/world/joint/rope.rs
  - crates/liquidfun/src/world/joint/motor.rs
  - crates/liquidfun/tests/joint_wheel.rs
  - crates/liquidfun/tests/joint_weld.rs
  - crates/liquidfun/tests/joint_friction.rs
  - crates/liquidfun/tests/joint_rope.rs
  - crates/liquidfun/tests/joint_motor.rs
autonomous: true
requirements: [JOIN-01, JOIN-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-13T22:02:38.000Z
must_haves:
  truths:
    - Wheel, weld, friction, rope-joint, and motor constraints each implement complete definitions, mutations, inspection, solver caches, anchors, and reactions.
    - Spring, rigid/soft weld, capped friction, unilateral rope, and correction-factor branches remain per-kind and source ordered.
    - RopeJoint remains a World joint and cannot be confused with standalone rope.
  artifacts:
    - crates/liquidfun/src/world/joint/wheel.rs
    - crates/liquidfun/src/world/joint/weld.rs
    - crates/liquidfun/src/world/joint/friction.rs
    - crates/liquidfun/src/world/joint/rope.rs
    - crates/liquidfun/src/world/joint/motor.rs
  key_links:
    - Per-kind private scratch plugs into the later exhaustive solver dispatcher.
---

# Plan 08-04: Wheel, Weld, Friction, Rope-Joint, and Motor Joints

<objective>Complete the remaining non-gear joints, including coupled spring/limit and capped constraints, without merging their distinct mutation or solver semantics.</objective>
<execution_context>Each task translates its pinned header/cpp pair and independently closes definition, setter, snapshot, solver, reaction, and dump-record behavior.</execution_context>
<decision_coverage>D-03, D-04, D-05, D-09, and the rope-joint half of D-11.</decision_coverage>
<threat_model>All public floats and derived masses/biases are untrusted. Reject non-finite/negative caps, invalid axes/lengths/stiffness, stale IDs, lock, poison, and late arithmetic overflow before commit. Existing bounded iteration counts prevent solver work amplification. No unsafe code, raw solver storage, or cross-world identity is admitted.</threat_model>
<tasks>
<task id="08-04-01" type="auto" tdd="true">
  <name>Implement wheel and weld joints</name>
  <files>crates/liquidfun/src/world/joint/wheel.rs, crates/liquidfun/src/world/joint/weld.rs, crates/liquidfun/tests/joint_wheel.rs, crates/liquidfun/tests/joint_weld.rs</files>
  <read_first>
    - crates/liquidfun/src/world/joint/wheel.rs
    - crates/liquidfun/src/world/joint/weld.rs
    - crates/liquidfun/tests/joint_wheel.rs
    - crates/liquidfun/tests/joint_weld.rs
    - crates/liquidfun/src/world/joint.rs
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2WheelJoint.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2WheelJoint.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2WeldJoint.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2WeldJoint.cpp
  </read_first>
  <action>For wheel, implement checked axis/anchors, translation/speed, spring frequency/damping, motor enable/speed/max torque, motor torque, setters, point-to-line/spring/motor scalar constraints, warm start, position solve, anchors and reactions. For weld, implement anchors/reference angle, frequency/damping setters, rigid 3x3 and soft angular gamma/bias branches, warm start, position solve, anchors and reactions. Preserve exact setter wake/cache semantics and test each concern independently.</action>
  <verify>
    - cargo test -p liquidfun --test joint_wheel --all-features
    - cargo test -p liquidfun --test joint_weld --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>Both focused targets prove all configuration/setter branches, wheel motor/spring limits, weld rigid/soft paths, warm starts, anchors, and reactions; all ordered Rust gates exit 0.</acceptance_criteria>
</task>
<task id="08-04-02" type="auto" tdd="true">
  <name>Implement friction, rope-joint, and motor joints</name>
  <files>crates/liquidfun/src/world/joint/friction.rs, crates/liquidfun/src/world/joint/rope.rs, crates/liquidfun/src/world/joint/motor.rs, crates/liquidfun/tests/joint_friction.rs, crates/liquidfun/tests/joint_rope.rs, crates/liquidfun/tests/joint_motor.rs</files>
  <read_first>
    - crates/liquidfun/src/world/joint/friction.rs
    - crates/liquidfun/src/world/joint/rope.rs
    - crates/liquidfun/src/world/joint/motor.rs
    - crates/liquidfun/tests/joint_friction.rs
    - crates/liquidfun/tests/joint_rope.rs
    - crates/liquidfun/tests/joint_motor.rs
    - crates/liquidfun/src/world/joint.rs
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2FrictionJoint.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2FrictionJoint.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2RopeJoint.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2RopeJoint.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2MotorJoint.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2MotorJoint.cpp
  </read_first>
  <action>Implement friction local anchors, max force/torque setters, capped linear/angular impulses, warm start, anchors and reactions. Implement rope-joint anchors, positive maximum length setter, inactive/at-upper limit state, unilateral nonpositive accumulated impulse, predictive velocity term, position solve, anchors and reactions. Implement motor linear/angular offsets, max force/torque, correction factor, exact wake setters, linear/angular error, capped impulses, warm start, and reactions. Test invalid/equal/changed setters, every branch/cache, zero/positive timestep, and verify public names keep `RopeJointDef` distinct from `rope::Rope`.</action>
  <verify>
    - cargo test -p liquidfun --test joint_friction --all-features
    - cargo test -p liquidfun --test joint_rope --all-features
    - cargo test -p liquidfun --test joint_motor --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>Focused targets prove force/torque caps, rope inactive/upper states and unilateral impulse, motor correction/offset behavior, all setters, warm starts, anchors/reactions, and the rope type separation; all ordered Rust gates exit 0.</acceptance_criteria>
</task>
</tasks>
<verification>Inspect every public setter against the pinned changed/equal branch and every solver against source impulse accumulation and clamp order.</verification>
<success_criteria>All non-gear joint kinds are complete as isolated constraints and ready for source-ordered island integration.</success_criteria>
