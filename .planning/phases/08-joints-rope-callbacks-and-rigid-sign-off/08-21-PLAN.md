---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "21"
type: execute
wave: 18
depends_on: ["08-20"]
files_modified:
  - tools/reference/src/rigid_world.cpp
  - tools/reference/src/rigid_world_phase8_decode.hpp
  - tools/reference/src/rigid_world_phase8_execute.hpp
  - tools/reference/src/rigid_world_trace.hpp
  - tools/reference/tests/protocol_tests.cpp
autonomous: true
requirements: [RIGD-11, JOIN-01, JOIN-02, JOIN-03, JOIN-04, JOIN-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-14T03:20:00Z
must_haves:
  truths:
    - The pinned C++ adapter independently executes the same strengthened request and emits source-derived observations.
    - All solver, gear, callback, and destruction records come from upstream objects/listeners after real actions.
    - Decode, execution, teardown, and result bounds fail closed.
  artifacts: [tools/reference/src/rigid_world_phase8_decode.hpp, tools/reference/src/rigid_world_phase8_execute.hpp, tools/reference/tests/protocol_tests.cpp]
  key_links:
    - Plan 08-22 compares this output with Plan 08-20's native output under phase8-v1.
---
# Plan 08-21: Pinned C++ Step-Bearing Adapter and Protocol
<objective>Execute the strengthened Phase 8 corpus against the pinned upstream engine and emit independent bounded semantic records for comparison.</objective>
<execution_context>Change only private reference adapter/protocol sources and C++ tests.</execution_context>
<decision_coverage>D-23 through D-26 isolated pinned-oracle evidence; all Phase 8 requirements.</decision_coverage>
<threat_model>Reject expected-value echoing, Rust-derived fixtures as authority, uninitialized scratch reads, raw pointers/addresses in evidence, listener reconstruction after the fact, swallowed C++ failure, unbounded allocation, and incomplete teardown.</threat_model>
<tasks><task id="08-21-01" type="auto" tdd="true">
  <name>Execute and observe every strengthened witness in the pinned oracle</name>
  <files>tools/reference/src/rigid_world.cpp, tools/reference/src/rigid_world_phase8_decode.hpp, tools/reference/src/rigid_world_phase8_execute.hpp, tools/reference/src/rigid_world_trace.hpp, tools/reference/tests/protocol_tests.cpp</files>
  <read_first>
    - protocol/fixtures/accepted/rigid-world-request.jsonl
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/types.rs
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs
    - tools/reference/src/rigid_world_phase8_decode.hpp
    - tools/reference/src/rigid_world_phase8_execute.hpp
    - tools/reference/src/rigid_world_trace.hpp
    - tools/reference/tests/protocol_tests.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2WorldCallbacks.h
  </read_first>
  <action>Extend strict duplicate-aware decode for every new witness/action/order rule and keep all N/N+1 bounds. Execute the ordered actions using actual `b2World`, all eleven `b2Joint` kinds, `b2Rope`, contact filter/listener, pre-solve directives, and destruction listener. After real steps, call the pinned getters for requested body/joint state, branches, coordinates, speeds, reactions, and source joints; use the existing defined-positive-zero guard only before solver initialization and exact getter bits afterward. Create RR/RP/PR/PP over actual four-body topologies. Capture callbacks at their source listener/filter invocation points and destruction callbacks in occurrence order; do not synthesize them from the request.

Emit only stable scenario IDs and bounded semantic records. Catch and translate every adapter error, fully destroy worlds/ropes/maps between timelines and requests, and prove reset. Add C++ tests for all explicit witnesses, all eleven stepped kinds, four gear combinations, nontrivial post-step observations, exact callback arrays, explicit/cascade destruction arrays, malformed order, zero-step rejection, missing observations, duplicate IDs, every N+1 bound, defined pre-step reactions, exact post-step reactions, and repeated reset.</action>
  <verify>
    - cargo xtask upstream configure --preset oracle-debug
    - cargo xtask upstream build --preset oracle-debug
    - cmake --build target/reference/oracle-debug --target liquidfun-reference-protocol-tests
    - ctest --test-dir target/reference/oracle-debug --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$'
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
    - git diff --check
  </verify>
  <acceptance_criteria>The C++ oracle executes and observes every strengthened witness from actual pinned state/listeners, all kinds/gear/callback/destruction/malformed/bound/reset tests pass, no raw identity or synthetic expected evidence appears, debug protocol CTest and the ordered Rust gate exit 0.</acceptance_criteria>
</task></tasks>
<verification>Audit each C++ record's causal source, bounds, stable identity, defined-state rules, listener timing, teardown, and exact isolation from published crates.</verification>
<success_criteria>The pinned C++ side independently realizes the same closed evidence contract and is ready for strict comparison.</success_criteria>
