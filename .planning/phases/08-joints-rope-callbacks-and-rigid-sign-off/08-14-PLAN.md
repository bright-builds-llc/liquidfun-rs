---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "14"
type: execute
wave: 11
depends_on: ["08-13"]
files_modified:
  - crates/liquidfun/src/world/joint.rs
  - crates/liquidfun/src/world/joint/solver.rs
  - crates/liquidfun/src/world/contact_solver.rs
  - crates/liquidfun/src/world/island.rs
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/tests/joint_island_solver.rs
autonomous: true
requirements: [JOIN-01, JOIN-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-14T03:20:00Z
must_haves:
  truths:
    - The joint solver has an exhaustive staged family contract that later plans can activate one family at a time without changing the world transaction boundary.
    - A joint solution carries a complete candidate runtime and commits only after every island candidate validates.
    - The contract can represent ordinary A/B lanes and gear A/B/C/D lanes without changing source encounter order.
  artifacts: [crates/liquidfun/src/world/joint/solver.rs, crates/liquidfun/src/world/contact_solver.rs]
  key_links:
    - Later family plans fill the closed variants established here without reopening the island transaction boundary.
---
# Plan 08-14: Shared Live-Solver Staging and Dispatch Contract
<objective>Introduce the exhaustive, transactional family-staging contract needed to replace the generic two-body solver safely over Plans 08-15 through 08-18.</objective>
<execution_context>Change only shared solver/island/commit infrastructure. Preserve a compiling, passing compatibility path for not-yet-activated families; Plan 08-18 removes that path after every family variant is live.</execution_context>
<decision_coverage>D-08 and D-09 shared dispatch, ordering, and atomic-commit requirements.</decision_coverage>
<threat_model>Reject use of the compatibility path by an activated family, trait-object dispatch, kind grouping, hash iteration, direct live-runtime mutation, partial cache commit, and any ordinary-joint entry into TOI solving.</threat_model>
<tasks><task id="08-14-01" type="auto" tdd="true">
  <name>Build the exhaustive candidate-runtime solver contract</name>
  <files>crates/liquidfun/src/world/joint.rs, crates/liquidfun/src/world/joint/solver.rs, crates/liquidfun/src/world/contact_solver.rs, crates/liquidfun/src/world/island.rs, crates/liquidfun/src/world/object.rs, crates/liquidfun/tests/joint_island_solver.rs</files>
  <read_first>
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-REVIEW.md
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - crates/liquidfun/src/world/joint/solver.rs
    - crates/liquidfun/src/world/joint.rs
    - crates/liquidfun/src/world/contact_solver.rs
    - crates/liquidfun/src/world/island.rs
    - crates/liquidfun/src/world/object.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Island.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2Joint.cpp
  </read_first>
  <action>Define a closed family-staging enum with eleven typed variants; each variant owns the joint ID, exact solver-body lane descriptor, copied definition, copied candidate `JointRuntime`, and an activation state. Define ordinary A/B lanes and gear A/B/C/D lanes whose semantic IDs and optional solver indices are resolved while building source-ordered island inputs. Add exhaustive initialize/warm-start/velocity/position/finalize dispatch seams that Plans 08-15 through 08-18 fill without changing callers.

Extend `JointImpulseSolution` with an optional complete staged runtime candidate and thread it through `IslandConstraintSolution` and the existing world candidate so `object.rs` replaces `JointRecord.runtime` only after all body, contact, proxy, island, and joint candidates validate. Keep the current generic constraint/cache path explicitly isolated as `legacy_unmigrated` for variants whose activation state is false; activated variants must be statically unable to call it. Preserve current passing behavior until migration completes, initialize contacts then joints, solve joints then contacts for velocity, solve contacts then joints for position, pinned early exit, joint encounter order, and contact-only TOI. Add Arrange/Act/Assert tests for exhaustive variant construction, activation routing, source order, staged runtime round-trip, and rejection of missing/out-of-island lane indices.</action>
  <verify>
    - cargo test -p liquidfun --test joint_island_solver --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>The shared solver exposes eleven explicit activation-aware variants, ordinary and gear lane descriptors, optional staged complete-runtime solutions, and one post-validation commit; activated variants cannot use `legacy_unmigrated`; existing behavior, source ordering, lane validation, and runtime round-trip tests pass; the ordered Rust gate exits 0.</acceptance_criteria>
</task></tasks>
<verification>Review the shared diff for exhaustive activation routing, isolated temporary compatibility path, preserved iteration order, no TOI joints, complete runtime staging, and no modifications outside the task allowlist.</verification>
<success_criteria>Plans 08-15 through 08-18 can implement pinned family equations behind one safe exhaustive solver boundary without reopening world transaction semantics.</success_criteria>
