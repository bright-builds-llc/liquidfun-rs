---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "08"
type: execute
wave: 5
depends_on: ["08-06"]
files_modified:
  - crates/liquidfun/src/world/contact.rs
  - crates/liquidfun/src/world/contact_manager.rs
  - crates/liquidfun/src/world/step.rs
  - crates/liquidfun/tests/contact_hook_timing.rs
autonomous: true
requirements: [JOIN-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-13T22:02:38.000Z
must_haves:
  truths:
    - One borrowed synchronous decision hook filters at pair admission/refilter and pre-solves immediately after eligible contact update/begin/end.
    - FixturePairView has no ContactId; PreSolveView provides current and previous owned semantic manifolds but no mutable World/manifold.
    - Per-update enable/disable and only inventory-proven material/tangent controls are typed, validated, and nonpersistent where pinned.
  artifacts: [crates/liquidfun/src/world/step.rs, crates/liquidfun/tests/contact_hook_timing.rs]
  key_links:
    - ContactManager invokes the borrowed hook at source-equivalent effect points rather than a later grouped pass.
---
# Plan 08-08: Filter and Pre-Solve Timing
<objective>Move collision filter and pre-solve decisions to their pinned contact-manager timing through one safe borrowed hook.</objective>
<execution_context>Inventory b2ContactManager/b2Contact setters first, then expose only proven narrow directives; no persistent registration or arbitrary mutation.</execution_context>
<decision_coverage>D-13, D-14, and D-15.</decision_coverage>
<threat_model>Hook input is application code that may panic, retain views, enqueue too much work, or return invalid material values. Lifetimes prevent retention, closed directives prevent authority escalation, command limits bound work, finite constructors reject invalid values, and lock/poison guards restore coherence before resuming panic. No reusable contact handles or mutable World escape.</threat_model>
<tasks><task id="08-08-01" type="auto" tdd="true">
  <name>Install source-timed fixture filtering and eligible pre-solve controls</name>
  <files>crates/liquidfun/src/world/contact.rs, crates/liquidfun/src/world/contact_manager.rs, crates/liquidfun/src/world/step.rs, crates/liquidfun/tests/contact_hook_timing.rs</files>
  <read_first>
    - crates/liquidfun/src/world/contact.rs
    - crates/liquidfun/src/world/contact_manager.rs
    - crates/liquidfun/src/world/step.rs
    - crates/liquidfun/tests/contact_hook_timing.rs
    - .planning/phases/03-rust-object-model-and-storage-architecture/03-CONTEXT.md
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2ContactManager.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Contacts/b2Contact.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2WorldCallbacks.h
  </read_first>
  <action>Replace `StepHook::filter(ContactView)` with `CollisionDecisionHook::should_collide(FixturePairView)` invoked at broad-phase pair admission and flagged refilter before contact allocation/removal. Add `PreSolveView` with semantic fixture/body/child identity plus current/previous manifold snapshots; invoke after that contact's update and begin/end emission only for touching enabled non-sensor contacts. Define closed `PreSolveDirective` containing per-update enable/disable and only source-inventoried finite friction, restitution, and tangent-speed overrides; if a setter is not present in the pinned revision, omit it and add a compile-time/focused exclusion test. One hook is borrowed per operation; `NoDecisionHook` is explicit unregistration/replacement. Remove the later grouped hook pass. Tests assert admission/refilter counts, begin/end-before-pre-solve ordering, sensor exclusion, disable-for-one-update reset, material validation, repeated CCD occurrence eligibility, no ContactId, and panic poison/no-applied-command behavior.</action>
  <verify>
    - cargo test -p liquidfun --test contact_hook_timing --all-features
    - cargo test -p liquidfun --test hook_contract --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>Focused tests prove exact filter/refilter/pre-solve insertion order, sensor exclusion, one-update disable, validated supported controls, hook replacement/no-op, panic poison, and no retained contact identity; all ordered Rust gates exit 0.</acceptance_criteria>
</task></tasks>
<verification>Source scan finds no persistent listener field, `&mut World` hook argument, public ContactId, or grouped post-update pre-solve loop.</verification>
<success_criteria>Filtering and pre-solve decisions occur at source-equivalent points with strictly bounded safe authority.</success_criteria>

