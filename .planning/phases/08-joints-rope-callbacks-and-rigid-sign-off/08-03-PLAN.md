---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "03"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - crates/liquidfun/src/world/joint/distance.rs
  - crates/liquidfun/src/world/joint/pulley.rs
  - crates/liquidfun/src/world/joint/mouse.rs
  - crates/liquidfun/tests/joint_distance.rs
  - crates/liquidfun/tests/joint_pulley.rs
  - crates/liquidfun/tests/joint_mouse.rs
autonomous: true
requirements: [JOIN-01, JOIN-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-13T22:02:38.000Z
must_haves:
  truths:
    - Distance, pulley, and mouse joints implement complete checked configuration, mutations, snapshots, solver caches, anchors, and reactions.
    - Soft/rigid distance branches, pulley constant/ratio geometry, and mouse gamma/beta target behavior preserve pinned grouping.
    - Only pulley ground anchors and mouse targets are world-space origin-shift state.
  artifacts:
    - crates/liquidfun/src/world/joint/distance.rs
    - crates/liquidfun/src/world/joint/pulley.rs
    - crates/liquidfun/src/world/joint/mouse.rs
  key_links:
    - Each module returns checked solver candidates to the shared dispatcher without mutating World during initialization.
---

# Plan 08-03: Distance, Pulley, and Mouse Joints

<objective>Implement the fixed/soft distance family, pulley geometry, and world-target mouse constraint with complete public observability and source-faithful solvers.</objective>
<execution_context>Use pinned `b2DistanceJoint`, `b2PulleyJoint`, and `b2MouseJoint` definitions/setters/solvers/dumps as the authority; origin-shift wiring is finalized in Plan 06.</execution_context>
<decision_coverage>D-03, D-04, D-05, D-09, D-10, and D-20 mouse unsupported status preparation.</decision_coverage>
<threat_model>Untrusted lengths, ratios, targets, frequencies, damping, and force caps can create divide-by-zero, non-finite state, or excessive correction. Checked constructors and candidate gamma/beta/constant calculations reject before effects; existing step bounds cap work. Stale IDs, lock, poison, and cross-world input fail closed; no unsafe code.</threat_model>
<tasks>
<task id="08-03-01" type="auto" tdd="true">
  <name>Implement the distance joint</name>
  <files>crates/liquidfun/src/world/joint/distance.rs, crates/liquidfun/tests/joint_distance.rs</files>
  <read_first>
    - crates/liquidfun/src/world/joint/distance.rs
    - crates/liquidfun/tests/joint_distance.rs
    - crates/liquidfun/src/world/joint.rs
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2DistanceJoint.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2DistanceJoint.cpp
  </read_first>
  <action>Implement checked anchors, positive length, frequency, damping ratio, setters, current length, reaction force/torque, scalar impulse cache, rigid versus soft gamma/bias velocity solve, rigid-only position solve, warm start, and exact source wake/reset branches. Test default/invalid definitions, zero separation, soft and rigid paths, equal/changed setters, cold/warm start, reaction, and transactional overflow.</action>
  <verify>
    - cargo test -p liquidfun --test joint_distance --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>The focused target proves positive-length validation, soft/rigid branches, gamma/bias, warm cache, setters, current length, and reactions; all commands exit 0.</acceptance_criteria>
</task>
<task id="08-03-02" type="auto" tdd="true">
  <name>Implement the pulley joint</name>
  <files>crates/liquidfun/src/world/joint/pulley.rs, crates/liquidfun/tests/joint_pulley.rs</files>
  <read_first>
    - crates/liquidfun/src/world/joint/pulley.rs
    - crates/liquidfun/tests/joint_pulley.rs
    - crates/liquidfun/src/world/joint.rs
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2PulleyJoint.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2PulleyJoint.cpp
  </read_first>
  <action>Implement checked world ground anchors, local anchors, positive segment lengths and ratio, source constant, length A/B inspection, ratio, anchors, reaction force/torque, scalar velocity solve, position correction, warm start, and shiftable ground-anchor state. Preserve source normalization/slop branches and reject non-finite derived constant before insertion.</action>
  <verify>
    - cargo test -p liquidfun --test joint_pulley --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>The focused target proves ratio/length invariants, constant, degenerate segment branches, warm/cold solves, anchors/reactions, and shift-ready ground anchors; all commands exit 0.</acceptance_criteria>
</task>
<task id="08-03-03" type="auto" tdd="true">
  <name>Implement the mouse joint</name>
  <files>crates/liquidfun/src/world/joint/mouse.rs, crates/liquidfun/tests/joint_mouse.rs</files>
  <read_first>
    - crates/liquidfun/src/world/joint/mouse.rs
    - crates/liquidfun/tests/joint_mouse.rs
    - crates/liquidfun/src/world/joint.rs
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2MouseJoint.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2MouseJoint.cpp
  </read_first>
  <action>Implement checked world target, positive max force, frequency and damping ratio, target/max-force/frequency/damping setters with pinned wake behavior, gamma/beta initialization, angular damping term, capped 2D impulse, warm start, anchors, reactions, and target origin shifting. Mark semantic reconstruction support as `Unsupported(MouseJoint)` rather than fabricating a dump.</action>
  <verify>
    - cargo test -p liquidfun --test joint_mouse --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>The focused target proves target wake behavior, force cap, gamma/beta grouping, warm start, origin-shift candidate behavior, reactions, and explicit unsupported reconstruction status; all commands exit 0.</acceptance_criteria>
</task>
</tasks>
<verification>Review each solver against its pinned file and verify no generic softness, wake, or dump rule replaces the per-kind source branches.</verification>
<success_criteria>All three joint kinds are independently complete and ready for central island/origin dispatch.</success_criteria>

