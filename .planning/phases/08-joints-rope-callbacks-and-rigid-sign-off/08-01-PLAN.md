---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/joint.rs
  - crates/liquidfun/src/joint/definition.rs
  - crates/liquidfun/src/joint/snapshot.rs
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/world/joint.rs
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/tests/joint_contract.rs
autonomous: true
requirements: [JOIN-01, JOIN-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-13T22:02:38.000Z
must_haves:
  truths:
    - JointId remains opaque, world-scoped, generational, and incapable of resolving stale or cross-world identities.
    - One closed JointDef and JointSnapshot account for all eleven pinned joint kinds without exposing arena coordinates or trait-object solvers.
    - Create, inspect, mutate, and destroy authority validates complete candidates before adjacency, refilter, wake, or cache effects.
  artifacts:
    - crates/liquidfun/src/joint.rs
    - crates/liquidfun/src/world/joint.rs
    - crates/liquidfun/tests/joint_contract.rs
  key_links:
    - JointDef is parsed into a private JointRecord only through World::create_joint.
    - JointSnapshot is owned and reaction queries require an explicit inverse timestep.
---

# Plan 08-01: Shared Joint Contract and Lifecycle

<objective>Replace the placeholder joint seam with the complete checked public vocabulary and atomic shared lifecycle used by every Phase 8 joint kind.</objective>

<execution_context>Follow D-01 through D-05, the Phase 3 identity/cascade model, the Phase 4 finite-value policy, and the pinned b2Joint/world list behavior. Keep the module deep: public definitions and snapshots are data contracts; World owns storage and effects.</execution_context>

<decision_coverage>D-01, D-02, D-03, D-04, and D-05.</decision_coverage>

<threat_model>
ASVS L1; block high severity. Untrusted public floats/handles can inject non-finite state or stale/cross-world identity, and unchecked multi-object mutation can leave partial adjacency or collision suppression. Mitigate with checked definition constructors, full world-key/generation resolution, bounded arenas, candidate-first commits, no unsafe code, no raw pointers, and panic-free error returns. Resource exhaustion is bounded before allocation; poisoned/locked worlds reject without effects.
</threat_model>

<tasks>
<task id="08-01-01" type="auto" tdd="true">
  <name>Implement the closed public joint vocabulary and shared World lifecycle</name>
  <files>crates/liquidfun/src/lib.rs, crates/liquidfun/src/joint.rs, crates/liquidfun/src/joint/definition.rs, crates/liquidfun/src/joint/snapshot.rs, crates/liquidfun/src/world.rs, crates/liquidfun/src/world/joint.rs, crates/liquidfun/src/world/object.rs, crates/liquidfun/tests/joint_contract.rs</files>
  <read_first>
    - crates/liquidfun/src/lib.rs
    - crates/liquidfun/src/joint.rs
    - crates/liquidfun/src/joint/definition.rs
    - crates/liquidfun/src/joint/snapshot.rs
    - crates/liquidfun/src/world.rs
    - crates/liquidfun/src/world/joint.rs
    - crates/liquidfun/src/world/object.rs
    - crates/liquidfun/tests/joint_contract.rs
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - .planning/phases/03-rust-object-model-and-storage-architecture/03-CONTEXT.md
    - .planning/phases/04-math-settings-and-numerical-policy/04-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2Joint.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2Joint.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
  </read_first>
  <action>Create documented `JointKind` variants `Revolute`, `Prismatic`, `Distance`, `Pulley`, `Mouse`, `Gear`, `Wheel`, `Weld`, `Friction`, `Rope`, and `Motor`; checked per-kind `*JointDef` values; closed `JointDef`; `JointLimitState`; owned `JointSnapshot`; and per-kind snapshot payloads containing every configured and runtime field. Replace the placeholder `Joint` with private `JointRecord { kind/state, bodies, collide_connected, island_flag, reverse_gear_dependents }`. Add `World::create_joint(JointDef) -> Result<JointId, JointCreationError>`, `joint_snapshot`, `joint_reaction_force(joint, inverse_timestep)`, `joint_reaction_torque`, checked kind-specific resolution helpers, and `destroy_joint`. Validate finite values, live same-world bodies, distinct/topology rules, capacity, lock, and poison before inserting. Insert world/body lanes newest-first; creation never invents wake; `collide_connected=false` flags affected contacts for refilter; destruction removes lanes and refilters only when the last suppressing joint is gone. Define typed creation/mutation errors for invalid handle, wrong kind, invalid value/topology, capacity, locked, poisoned, and derived non-finite state. Keep each later per-kind module behind exhaustive enum dispatch, with no `unsafe`, trait objects, public storage borrows, or `unwrap()`. Write focused Arrange/Act/Assert tests for all eleven tags, stale/cross-world/wrong-kind rejection, newest-first lanes, collision suppression with multiple joints, explicit destruction, and bit-identical no-effect failures.</action>
  <verify>
    - cargo test -p liquidfun --test joint_contract --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>`cargo test -p liquidfun --test joint_contract --all-features` exits 0; `rg 'enum JointKind|enum JointDef|struct JointSnapshot|fn create_joint|fn joint_reaction_force' crates/liquidfun/src` finds the closed contract; tests prove all eleven kinds, stale/cross-world/wrong-kind, lock/poison, newest-first adjacency, changed collision admission, and no-effect errors; all four ordered Rust gates exit 0.</acceptance_criteria>
</task>
</tasks>

<verification>Review the diff for a single public joint module, private arena state, exact newest-first adjacency, no generic wake policy, and no unsafe/raw-pointer/trait-object escape.</verification>
<success_criteria>Every later joint solver can plug into one checked identity, definition, snapshot, lifecycle, and error contract without reopening public architecture.</success_criteria>

