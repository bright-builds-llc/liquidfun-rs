---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "09"
type: execute
wave: 6
depends_on: ["08-05", "08-08"]
files_modified:
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/src/world/contact_manager.rs
  - crates/liquidfun/src/world/contact_solver.rs
  - crates/liquidfun/src/world/continuous.rs
  - crates/liquidfun/src/world/step.rs
  - crates/liquidfun/tests/lifecycle_timeline.rs
  - crates/liquidfun/tests/destruction_listener.rs
autonomous: true
requirements: [JOIN-02, JOIN-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-13T22:02:38.000Z
must_haves:
  truths:
    - One owned lifecycle timeline is appended where filter, begin/end, pre-solve, discrete/TOI post-solve, and destruction effects occur.
    - Convenience event slices are projections of the timeline, never independently regrouped sources of truth.
    - Explicit versus implicit destruction listener semantics and gear/body/fixture/contact occurrence order match the locked contract.
  artifacts: [crates/liquidfun/src/world/step.rs, crates/liquidfun/tests/lifecycle_timeline.rs]
  key_links:
    - Direct mutation reports and StepReport use the same owned event vocabulary and occurrence ordering.
---
# Plan 08-09: Owned Lifecycle and Destruction Timing
<objective>Make one source-timed owned timeline authoritative across discrete/TOI solving, callbacks, direct mutation, and deterministic destruction cascades.</objective>
<execution_context>Preserve order and multiplicity at effect sites; projections may filter the final timeline but may not reconstruct order afterward.</execution_context>
<decision_coverage>D-16, D-17, and D-18.</decision_coverage>
<threat_model>Panicking hooks can strand locks or partially apply deferred commands; repeated CCD events can exhaust memory; stale snapshots can lose identity; explicit destruction can fabricate notifications. Use RAII lock restoration, bounded timeline/command capacities, owned pre-invalidation snapshots, discard-on-panic, deterministic occurrence append, and no unsafe code.</threat_model>
<tasks><task id="08-09-01" type="auto" tdd="true">
  <name>Unify source-timed lifecycle events and direct destruction reports</name>
  <files>crates/liquidfun/src/world/object.rs, crates/liquidfun/src/world/contact_manager.rs, crates/liquidfun/src/world/contact_solver.rs, crates/liquidfun/src/world/continuous.rs, crates/liquidfun/src/world/step.rs, crates/liquidfun/tests/lifecycle_timeline.rs, crates/liquidfun/tests/destruction_listener.rs</files>
  <read_first>
    - crates/liquidfun/src/world/object.rs
    - crates/liquidfun/src/world/contact_manager.rs
    - crates/liquidfun/src/world/contact_solver.rs
    - crates/liquidfun/src/world/continuous.rs
    - crates/liquidfun/src/world/step.rs
    - crates/liquidfun/tests/lifecycle_timeline.rs
    - crates/liquidfun/tests/destruction_listener.rs
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2ContactManager.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2WorldCallbacks.h
  </read_first>
  <action>Define one owned `LifecycleEvent` closed enum for filter decision, begin, end, pre-solve decision, discrete post-solve impulse, TOI post-solve impulse, joint/fixture destruction goodbye, contact destruction/end, command application, and root destruction. Thread a bounded append sink through contact admission/update, discrete solver, every repeated TOI solve, and destruction helpers. Make `StepReport` convenience accessors filter this timeline. Add `MutationReport<T>`/`DestructionReport` for direct operations. For body cascades append dependent gear goodbye then other joint goodbye before invalidation, touching contact end/destruction, implicit fixture goodbye before fixture invalidation, then body root; explicit joint/fixture destruction emits no fabricated `SayGoodbye`. Apply bounded hook commands sequentially only after unlock; panic restores lock, discards commands, poisons coherent operations, and resumes unwinding; owned post-completion consumers cannot poison World. Tests cover exact explicit/implicit sequences, repeated CCD multiplicity, projections, command order, recoverable invalid command continuation, panic, and complete owned snapshots.</action>
  <verify>
    - cargo test -p liquidfun --test lifecycle_timeline --all-features
    - cargo test -p liquidfun --test destruction_listener --all-features
    - cargo test -p liquidfun --test rigid_ccd --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>Focused tests assert exact event arrays and multiplicities for discrete, TOI, explicit fixture/joint, body+gear cascade, panic, and deferred commands; source has one authoritative timeline field; all ordered Rust gates exit 0.</acceptance_criteria>
</task></tasks>
<verification>Review every event append at its effect site and prove projections contain no independent sorting/grouping logic.</verification>
<success_criteria>Callback, solver, and destruction timing is owned, bounded, source-ordered, and usable after borrows end.</success_criteria>
