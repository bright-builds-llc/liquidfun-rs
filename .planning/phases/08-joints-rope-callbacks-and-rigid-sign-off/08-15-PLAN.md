---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "15"
type: execute
wave: 12
depends_on: ["08-14"]
files_modified: []
autonomous: false
requirements: [RIGD-11, JOIN-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-13T22:02:38.000Z
must_haves:
  truths:
    - The reviewed Plan 08-14 result is committed and published at one exact remote branch head before dispatch.
    - The watched workflow run is a successful workflow_dispatch for that exact commit and contains both required successful jobs.
    - Exactly two unexpired artifacts match the run, commit, job, toolchain, upstream revision, and phase8-v1 identities.
  artifacts: []
  key_links:
    - Plan 08-16 may consume evidence only when every run, job, artifact, and identity check in this checkpoint passes.
---
# Plan 08-15: Publish Reviewed Ref and Prove Canonical Evidence
<objective>At a human-controlled checkpoint, publish the exact committed reviewed ref, dispatch and watch Oracle CI, and validate the resulting canonical and sanitizer evidence identities without editing the repository.</objective>
<execution_context>This plan is non-autonomous because publishing the reviewed commit requires explicit human authority. It may inspect Git/GitHub state and download artifacts under `target/`, but it edits no tracked or untracked repository file.</execution_context>
<decision_coverage>D-21, D-23, D-24, D-25, and D-26 canonical D1 enforcement for RIGD-11 and JOIN-05.</decision_coverage>
<threat_model>Fail closed on dirty or uncommitted review output, remote ref mismatch, stale/other-SHA runs, skipped or missing jobs, duplicate/missing/expired artifacts, mismatched identity JSON, configured-only CI, and local D2 evidence.</threat_model>
<tasks><task id="08-15-01" type="checkpoint:human-action" tdd="false">
  <name>Publish the exact reviewed commit and validate its Oracle CI evidence</name>
  <files></files>
  <read_first>
    - .github/workflows/oracle.yml
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-REVIEW.md
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
  </read_first>
  <action>Require Plan 08-14 verification to have passed and its reviewed result to be one clean commit. Pause for the user to publish or explicitly identify the branch containing that exact commit; do not choose or push a ref without that authority. Then execute `REPO=$(gh repo view --json nameWithOwner --jq .nameWithOwner)`, `BRANCH=$(git branch --show-current)`, `HEAD_SHA=$(git rev-parse HEAD)`, `test -n "$BRANCH"`, `test -z "$(git status --porcelain)"`, and `test "$(git ls-remote origin "refs/heads/$BRANCH" | awk '{print $1}')" = "$HEAD_SHA"`. Record `DISPATCHED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)`, run `gh workflow run oracle.yml -R "$REPO" --ref "$BRANCH"`, and poll at most 30 times with two-second intervals for a `workflow_dispatch` run created at or after `DISPATCHED_AT` whose `headSha` equals `HEAD_SHA`. Watch that exact `RUN_ID` with `gh run watch "$RUN_ID" -R "$REPO" --exit-status`. Assert the run head SHA, event, and conclusion; assert jobs `Canonical Linux oracle` and `Scheduled fail-fast sanitizer and reset corpus` both exist exactly once and succeeded. Query the run artifacts and require exactly two unexpired names: `phase8-canonical-$RUN_ID-$HEAD_SHA` and `phase8-sanitizer-$RUN_ID-$HEAD_SHA`. Download only those artifacts into `target/phase8-ci-evidence/canonical` and `target/phase8-ci-evidence/sanitizer`. Validate both `identity.json` files with `jq`: numeric `run_id == RUN_ID`, exact `head_sha`, Rust `1.97.0`, CMake `4.3.3`, Ninja `1.13.2`, Clang `22.1.8`, upstream revision `7f20402173fd143a3988c921bc384459c6a858f2`, and policy `phase8-v1`; require canonical job `canonical-linux` and sanitizer job `sanitizer-linux`. Record the run URL and artifact names for Plan 08-16. Do not edit any repository file.</action>
  <verify>
    - test "$(git ls-remote origin "refs/heads/$BRANCH" | awk '{print $1}')" = "$HEAD_SHA"
    - gh run watch "$RUN_ID" -R "$REPO" --exit-status
    - test "$(gh run view "$RUN_ID" -R "$REPO" --json headSha --jq .headSha)" = "$HEAD_SHA"
    - test "$(gh run view "$RUN_ID" -R "$REPO" --json event --jq .event)" = workflow_dispatch
    - test "$(gh run view "$RUN_ID" -R "$REPO" --json conclusion --jq .conclusion)" = success
    - jq -e --argjson run_id "$RUN_ID" --arg head_sha "$HEAD_SHA" --arg upstream_revision 7f20402173fd143a3988c921bc384459c6a858f2 '.run_id == $run_id and .head_sha == $head_sha and .upstream_revision == $upstream_revision and .rust == "1.97.0" and .cmake == "4.3.3" and .ninja == "1.13.2" and .clang == "22.1.8" and .policy_profile == "phase8-v1"' target/phase8-ci-evidence/canonical/identity.json
    - jq -e --argjson run_id "$RUN_ID" --arg head_sha "$HEAD_SHA" --arg upstream_revision 7f20402173fd143a3988c921bc384459c6a858f2 '.run_id == $run_id and .head_sha == $head_sha and .upstream_revision == $upstream_revision and .rust == "1.97.0" and .cmake == "4.3.3" and .ninja == "1.13.2" and .clang == "22.1.8" and .policy_profile == "phase8-v1"' target/phase8-ci-evidence/sanitizer/identity.json
  </verify>
  <acceptance_criteria>The exact committed reviewed ref is published; the exact dispatch succeeds with both named jobs; exactly two matching unexpired artifacts pass every provenance check; repository status remains clean and unchanged.</acceptance_criteria>
</task></tasks>
<verification>Cross-check remote branch SHA, run head/event/conclusion, unique job identities, exact artifact set, both identity files, and unchanged repository status.</verification>
<success_criteria>Actual canonical D1 and sanitizer evidence is bound to the exact reviewed commit and ready for documentation consumption; otherwise the plan stops with RIGD-11 and JOIN-05 pending.</success_criteria>
