---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "15"
type: execute
wave: 12
depends_on: ["08-14"]
files_modified:
  - crates/liquidfun/src/world/joint/solver.rs
  - crates/liquidfun/src/world/joint/revolute.rs
  - crates/liquidfun/src/world/joint/prismatic.rs
  - crates/liquidfun/tests/joint_revolute.rs
  - crates/liquidfun/tests/joint_prismatic.rs
  - crates/liquidfun/tests/joint_island_solver.rs
autonomous: true
requirements: [JOIN-01, JOIN-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-14T03:20:00Z
must_haves:
  truths:
    - Live revolute and prismatic constraints use pinned anchors, lever arms, matrices, motor caps, and limit branches.
    - Cold and warm steps update their owned runtimes and public reaction observations through World::step.
  artifacts: [crates/liquidfun/src/world/joint/revolute.rs, crates/liquidfun/src/world/joint/prismatic.rs]
  key_links:
    - The two coordinate-producing families remain the source inputs for every gear combination in Plan 08-18.
---
# Plan 08-15: Revolute and Prismatic Live Solvers
<objective>Connect the pinned revolute and prismatic initialization, warm-start, velocity, position, limit, motor, and reaction equations to ordinary world stepping.</objective>
<execution_context>Implement only these two family variants behind Plan 08-14's staged contract.</execution_context>
<decision_coverage>D-08 and D-09 for revolute/prismatic behavior; JOIN-01 and JOIN-02.</decision_coverage>
<threat_model>Reject center-anchor approximations, scalar replacements for 2x2/3x3 blocks, limit epsilons, generic waking, cache writes before final commit, and reordered motor/limit branches.</threat_model>
<tasks><task id="08-15-01" type="auto" tdd="true">
  <name>Wire pinned revolute and prismatic equations into live islands</name>
  <files>crates/liquidfun/src/world/joint/solver.rs, crates/liquidfun/src/world/joint/revolute.rs, crates/liquidfun/src/world/joint/prismatic.rs, crates/liquidfun/tests/joint_revolute.rs, crates/liquidfun/tests/joint_prismatic.rs, crates/liquidfun/tests/joint_island_solver.rs</files>
  <read_first>
    - crates/liquidfun/src/world/joint/solver.rs
    - crates/liquidfun/src/world/joint/revolute.rs
    - crates/liquidfun/src/world/joint/prismatic.rs
    - crates/liquidfun/src/world/contact_solver.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2RevoluteJoint.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/b2PrismaticJoint.cpp
  </read_first>
  <action>For revolute, derive rotated anchor radii, K/Mat33, motor mass, fixed-rotation state, current angle, and exact lower/upper/equal limit classification from candidate solver bodies; call runtime initialization, apply the full cached Vec3 plus motor impulse to velocities, solve motor before the point/limit block, and recompute the pinned position block from current positions. For prismatic, derive world axis/perpendicular, s1/s2/a1/a2 levers, Mat33, motor mass, translation, and angular/perpendicular/axial errors; warm-start the full Vec3 plus motor impulse, solve motor then block, and execute pinned limit-aware position correction. Keep source expression grouping and update only the copied runtime.

Add non-center-anchor World::step tests for revolute free/lower/upper/equal and enabled/disabled motor states, prismatic lower/upper/equal and enabled/disabled motor states, cold then warm steps, explicit reaction force/torque, and late-error atomicity. Assertions must distinguish the family equations from the deleted generic center lock.</action>
  <verify>
    - cargo test -p liquidfun --test joint_revolute --all-features
    - cargo test -p liquidfun --test joint_prismatic --all-features
    - cargo test -p liquidfun --test joint_island_solver --all-features
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>Revolute and prismatic live variants have no pending/shared fallback; non-center, limit, motor, warm-start, reaction, and atomic-failure tests pass through World::step; the ordered Rust gate exits 0.</acceptance_criteria>
</task></tasks>
<verification>Compare every live branch and expression order with the two pinned C++ files and confirm only the listed family/shared-test paths changed.</verification>
<success_criteria>Both coordinate-producing limit/motor families execute their pinned equations in live source-ordered islands.</success_criteria>
