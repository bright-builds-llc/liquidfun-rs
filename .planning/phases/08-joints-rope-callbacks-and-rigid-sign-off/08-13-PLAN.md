---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "13"
type: execute
wave: 10
depends_on: ["08-12"]
files_modified:
  - tools/reference/src/rigid_world.cpp
  - tools/reference/src/rigid_world.hpp
  - tools/reference/src/rigid_world_phase8_decode.hpp
  - tools/reference/src/rigid_world_phase8_execute.hpp
  - tools/reference/src/rigid_world_trace.hpp
  - tools/reference/tests/protocol_tests.cpp
  - crates/liquidfun-differential/src/rigid_evidence.rs
  - crates/liquidfun-differential/src/rigid_evidence/phase8.rs
  - crates/liquidfun-differential/tests/phase8_comparator.rs
  - tools/xtask/src/differential.rs
  - tools/xtask/tests/differential_cli.rs
autonomous: true
requirements: [RIGD-11, JOIN-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-13T22:02:38.000Z
must_haves:
  truths:
    - The process-isolated C++ oracle strictly decodes, validates, executes, and traces every phase8-v1 branch using the pinned upstream only.
    - Comparator declarations cover every observation and reject unknown/missing policy paths before numeric comparison.
    - Debug, release, replay, D0, and sanitizer commands remain closed and preserve all Phase 6/7 witnesses.
  artifacts: [tools/reference/src/rigid_world_phase8_execute.hpp, crates/liquidfun-differential/tests/phase8_comparator.rs]
  key_links:
    - Stdout contains protocol records only; stderr remains bounded diagnostics supervised out of process.
---
# Plan 08-13: C++ Oracle, Comparator, and Witness Execution
<objective>Mirror phase8-v1 in the isolated pinned C++ oracle, compare every semantic field under the closed registry, and expose exact evidence commands.</objective>
<execution_context>Keep foreign logic in C++ files, never Rust strings; preserve fresh b2World per request/reset epochs and upstream delete-gear-before-source ordering inside oracle scenarios.</execution_context>
<decision_coverage>D-23 through D-26 oracle/comparator portions.</decision_coverage>
<threat_model>Untrusted JSONL may trigger C++ memory/resource failures; oracle crashes/sanitizers must never become physics mismatches. Strict duplicate-aware bounded decoding, finite validation, checked semantic maps, out-of-process deadlines/output caps, fresh worlds, exception containment, and fail-fast ASan/UBSan isolate the boundary. No C++/FFI enters published crates.</threat_model>
<tasks><task id="08-13-01" type="auto" tdd="true">
  <name>Implement strict C++ Phase 8 execution and semantic traces</name>
  <files>tools/reference/src/rigid_world.cpp, tools/reference/src/rigid_world.hpp, tools/reference/src/rigid_world_phase8_decode.hpp, tools/reference/src/rigid_world_phase8_execute.hpp, tools/reference/src/rigid_world_trace.hpp, tools/reference/tests/protocol_tests.cpp</files>
  <read_first>
    - tools/reference/src/rigid_world.cpp
    - tools/reference/src/rigid_world.hpp
    - tools/reference/src/rigid_world_phase8_decode.hpp
    - tools/reference/src/rigid_world_phase8_execute.hpp
    - tools/reference/src/rigid_world_trace.hpp
    - tools/reference/tests/protocol_tests.cpp
    - tools/reference/src/rigid_world_phase7_execute.hpp
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Joints/
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Rope/
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2WorldCallbacks.h
  </read_first>
  <action>Add bounded decode/validation for every new declaration/action/directive and exact f32 bits; create all eleven b2 joints and standalone b2Rope; install temporary filter/contact/destruction listeners that append source-time occurrences; execute mutations, steps, origin shifts, and safe gear-delete-before-source scenarios; emit semantic snapshots/reactions/dependencies/rope vertices/timeline/counts/tree metrics/dump-support records in deterministic order. Never expose addresses or upstream indices. Add malformed/N+1/unknown protocol tests and build/run under debug plus fail-fast sanitizer preset.</action>
  <verify>
    - cargo xtask upstream configure --preset oracle-debug
    - cargo xtask upstream build --preset oracle-debug
    - cmake --build target/reference/oracle-debug --target liquidfun-reference-protocol-tests
    - ctest --test-dir target/reference/oracle-debug --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$'
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>Protocol CTest covers all eleven joints, rope, callbacks, destruction, reconstruction, bounds, malformed input, and reset; stdout remains valid JSONL only; all ordered Rust gates exit 0.</acceptance_criteria>
</task>
<task id="08-13-02" type="auto" tdd="true">
  <name>Close comparator coverage and evidence command shapes</name>
  <files>crates/liquidfun-differential/src/rigid_evidence.rs, crates/liquidfun-differential/src/rigid_evidence/phase8.rs, crates/liquidfun-differential/tests/phase8_comparator.rs, tools/xtask/src/differential.rs, tools/xtask/tests/differential_cli.rs</files>
  <read_first>
    - crates/liquidfun-differential/src/rigid_evidence.rs
    - crates/liquidfun-differential/src/rigid_evidence/phase8.rs
    - crates/liquidfun-differential/tests/phase8_comparator.rs
    - tools/xtask/src/differential.rs
    - tools/xtask/tests/differential_cli.rs
    - protocol/tolerances/phase8-v1.toml
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-CONTEXT.md
  </read_first>
  <action>Add exhaustive exact/float comparison for every phase8-v1 result path, first-divergence identity, signed-zero/non-finite policy, and hard errors for unknown/missing/duplicate declarations. Switch rigid-world commands to phase8-v1 while retaining the same closed `compare`, `replay`, `verify-determinism --runs 2`, and sanitizer shapes. Add tests proving a mutation at every structural and numeric path is detected, wildcard/missing policies fail, all retained families remain declared, debug/release/replay/D0 run, and sanitizer markers are harness failures.</action>
  <verify>
    - cargo test -p liquidfun-differential --test phase8_comparator --all-features
    - cargo test -p xtask --test differential_cli --all-features
    - cargo xtask differential compare --scenario rigid-world --preset oracle-debug --session-profile one-shot
    - cargo xtask differential replay --scenario rigid-world --preset oracle-debug --session-profile one-shot
    - cargo xtask differential verify-determinism --scenario rigid-world --preset oracle-debug --runs 2
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
  </verify>
  <acceptance_criteria>Comparator mutation tests cover every declaration; debug comparison, replay, and exactly-two-run D0 exit 0 on the complete accumulated corpus; unknown/missing policy tests fail closed; all ordered Rust gates exit 0.</acceptance_criteria>
</task></tasks>
<verification>Run fail-fast sanitizer comparison before final sign-off; any process/schema/provenance/sanitizer failure remains a harness failure.</verification>
<success_criteria>Rust and pinned C++ execute and compare the same complete phase8-v1 corpus with closed field-level evidence.</success_criteria>

