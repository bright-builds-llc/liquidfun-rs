---
phase: 08-joints-rope-callbacks-and-rigid-sign-off
plan: "23"
type: execute
wave: 20
depends_on: ["08-22"]
files_modified: []
autonomous: false
requirements: [RIGD-11, JOIN-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-07-13T21-26-30
generated_at: 2026-07-14T03:20:00Z
must_haves:
  truths:
    - The resolved and reviewed Plan 08-22 result is one clean committed remote branch head before dispatch.
    - The watched workflow_dispatch run succeeds for that exact SHA with both required jobs.
    - Exactly two unexpired artifacts match run, commit, job, toolchain, upstream revision, and phase8-v1 identities.
  artifacts: []
  key_links:
    - Plan 08-24 may consume evidence only when every remote ref, run, job, artifact, and identity check here passes.
---
# Plan 08-23: Publish Reviewed Ref and Prove Canonical Evidence
<objective>At a human-controlled checkpoint, publish the exact committed reviewed ref, dispatch and watch Oracle CI, and validate canonical and sanitizer evidence identities without editing the repository.</objective>
<execution_context>This plan is non-autonomous because publishing the reviewed commit requires explicit human authority. It may inspect Git/GitHub state and download artifacts under `target/`, but edits no repository file.</execution_context>
<decision_coverage>D-21, D-23, D-24, D-25, and D-26 canonical D1 enforcement for RIGD-11 and JOIN-05.</decision_coverage>
<threat_model>Fail closed on dirty/uncommitted review output, remote mismatch, stale or other-SHA run, skipped/missing job, duplicate/missing/expired artifact, mismatched identity JSON, configured-only CI, or local D2 evidence.</threat_model>
<tasks><task id="08-23-01" type="checkpoint:human-action" tdd="false">
  <name>Publish the exact reviewed commit and validate Oracle CI evidence</name>
  <files></files>
  <read_first>
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-REVIEW.md
    - .planning/phases/08-joints-rope-callbacks-and-rigid-sign-off/08-22-PLAN.md
    - .github/workflows/oracle.yml
    - protocol/tolerances/phase8-v1.toml
  </read_first>
  <action>Require Plan 08-22 verification to have passed, both review findings to be resolved, and the result to be one clean commit. Pause for the user to publish or explicitly identify the branch containing that exact commit; do not choose or push a ref without authority. Then execute `REPO=$(gh repo view --json nameWithOwner --jq .nameWithOwner)`, `BRANCH=$(git branch --show-current)`, `HEAD_SHA=$(git rev-parse HEAD)`, `test -n "$BRANCH"`, `test -z "$(git status --porcelain)"`, and `test "$(git ls-remote origin "refs/heads/$BRANCH" | awk '{print $1}')" = "$HEAD_SHA"`.

Record `DISPATCHED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)`, run `gh workflow run oracle.yml -R "$REPO" --ref "$BRANCH"`, and poll at most 30 times with two-second intervals for a workflow_dispatch run created at/after that timestamp with `headSha == HEAD_SHA`. Watch that exact `RUN_ID` using `gh run watch "$RUN_ID" -R "$REPO" --exit-status`. Assert exact head SHA, event, success, and exactly one successful `Canonical Linux oracle` and `Scheduled fail-fast sanitizer and reset corpus` job.

Require exactly two unexpired artifact names: `phase8-canonical-$RUN_ID-$HEAD_SHA` and `phase8-sanitizer-$RUN_ID-$HEAD_SHA`. Download only them into `target/phase8-ci-evidence/canonical` and `target/phase8-ci-evidence/sanitizer`. Validate both identity files with `jq`: numeric run ID, exact SHA, Rust 1.97.0, CMake 4.3.3, Ninja 1.13.2, Clang 22.1.8, upstream `7f20402173fd143a3988c921bc384459c6a858f2`, policy `phase8-v1`, and respective jobs `canonical-linux`/`sanitizer-linux`. Record run URL and artifact names for Plan 08-24; do not edit any repository file.</action>
  <verify>
    - test "$(git ls-remote origin "refs/heads/$BRANCH" | awk '{print $1}')" = "$HEAD_SHA"
    - gh run watch "$RUN_ID" -R "$REPO" --exit-status
    - test "$(gh run view "$RUN_ID" -R "$REPO" --json headSha --jq .headSha)" = "$HEAD_SHA"
    - test "$(gh run view "$RUN_ID" -R "$REPO" --json event --jq .event)" = workflow_dispatch
    - test "$(gh run view "$RUN_ID" -R "$REPO" --json conclusion --jq .conclusion)" = success
    - jq -e --argjson run_id "$RUN_ID" --arg head_sha "$HEAD_SHA" --arg upstream_revision 7f20402173fd143a3988c921bc384459c6a858f2 '.run_id == $run_id and .head_sha == $head_sha and .job == "canonical-linux" and .upstream_revision == $upstream_revision and .rust == "1.97.0" and .cmake == "4.3.3" and .ninja == "1.13.2" and .clang == "22.1.8" and .policy_profile == "phase8-v1"' target/phase8-ci-evidence/canonical/identity.json
    - jq -e --argjson run_id "$RUN_ID" --arg head_sha "$HEAD_SHA" --arg upstream_revision 7f20402173fd143a3988c921bc384459c6a858f2 '.run_id == $run_id and .head_sha == $head_sha and .job == "sanitizer-linux" and .upstream_revision == $upstream_revision and .rust == "1.97.0" and .cmake == "4.3.3" and .ninja == "1.13.2" and .clang == "22.1.8" and .policy_profile == "phase8-v1"' target/phase8-ci-evidence/sanitizer/identity.json
  </verify>
  <acceptance_criteria>The exact clean reviewed ref is published; its exact dispatch succeeds with both unique jobs; exactly two matching unexpired artifacts pass every identity check; repository status remains clean and unchanged.</acceptance_criteria>
</task></tasks>
<verification>Cross-check remote SHA, run head/event/conclusion, unique job identities, exact artifact set, both identity files, and unchanged repository status.</verification>
<success_criteria>Actual canonical D1 and sanitizer evidence is bound to the exact reviewed commit and ready for documentation consumption; otherwise RIGD-11 and JOIN-05 remain pending.</success_criteria>
