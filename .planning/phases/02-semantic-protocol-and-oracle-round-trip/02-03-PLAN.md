---
phase: 02-semantic-protocol-and-oracle-round-trip
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - crates/liquidfun-test-protocol/src/lib.rs
  - crates/liquidfun-test-protocol/src/codec.rs
  - crates/liquidfun-test-protocol/src/scenario.rs
  - crates/liquidfun-test-protocol/src/trace.rs
  - crates/liquidfun-test-protocol/src/tolerance.rs
autonomous: true
requirements:
  - COMP-03
  - COMP-05
  - COMP-09
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-07-10T04-59-34
generated_at: 2026-07-10T05:24:31Z
must_haves:
  truths:
    - "A named or seeded bounded empty-world scenario value validates with ordered steps and unique checkpoint requests."
    - "Every JSONL input and output record is newline-complete, versioned, strictly shaped, and parsed into domain types before use."
    - "A trace is accepted only when handshake, begin, ordered checkpoints, end, request identity, provenance identity, counts, hashes, and reset proof form the exact state machine."
  artifacts:
    - path: "crates/liquidfun-test-protocol/src/codec.rs"
      provides: "Limit-aware UTF-8 JSONL framing and direct typed decoding"
    - path: "crates/liquidfun-test-protocol/src/scenario.rs"
      provides: "Validated ScenarioV1 named/seeded empty-world command contract"
    - path: "crates/liquidfun-test-protocol/src/trace.rs"
      provides: "Handshake and trace state-machine records"
  key_links:
    - from: "crates/liquidfun-test-protocol/src/codec.rs"
      to: "crates/liquidfun-test-protocol/src/scenario.rs"
      via: "strict raw-record to ValidatedScenario parsing"
      pattern: "ValidatedScenario"
    - from: "crates/liquidfun-test-protocol/src/trace.rs"
      to: "crates/liquidfun-test-protocol/src/provenance.rs"
      via: "identity hash repeated and checked on trace records"
      pattern: "identity_sha256"
    - from: "crates/liquidfun-test-protocol/src/trace.rs"
      to: "crates/liquidfun-test-protocol/src/tolerance.rs"
      via: "typed trace observables and exhaustive field policies"
      pattern: "FloatPolicy"
---

<objective>
Implement the bounded protocol-v1 scenario, JSONL, trace-state, and typed tolerance core.

Purpose: Make the wire boundary fail closed and engine-neutral before Rust or C++ execution exists.
Output: Strict codecs, validated scenario/trace types, typed tolerance policies, and boundary-focused tests that use in-memory records.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-RESEARCH.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-02-SUMMARY.md
@reference/upstream-lock.toml

<interfaces>
Consume Plan 02's `ProtocolVersion`, `ScenarioSchemaVersion`, `TraceSchemaVersion`, `ToleranceProfileVersion`, `FloatBits`, typed IDs, `HarnessLimits`, `BuildIdentity`, and `HarnessFailureKind` directly. Do not introduce parallel primitive representations.

Protocol-v1 state:

```text
child startup: handshake
request: scenario_request
response: trace_begin -> checkpoint* -> trace_end
```

`ScenarioV1` supports only an empty ordered entity-definition list in Phase 2. It carries named or seeded source metadata, exact gravity/timestep bits, ordered uniquely identified commands, solver iterations, uniquely identified checkpoints referencing command boundaries, and requested `world_counts`/`simulation_time` observables.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse strict bounded JSONL scenarios into domain types</name>
  <read_first>
    - `crates/liquidfun-test-protocol/src/lib.rs` — existing core exports and new module registration.
    - `crates/liquidfun-test-protocol/src/ids.rs` — typed ID authority.
    - `crates/liquidfun-test-protocol/src/limits.rs` — every byte/depth/string/collection bound.
    - `crates/liquidfun-test-protocol/src/failure.rs` — required boundary failure categories.
    - `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md` — D-01 through D-06 are locked.
    - `CREATE: crates/liquidfun-test-protocol/src/codec.rs` — strict UTF-8 newline codec.
    - `CREATE: crates/liquidfun-test-protocol/src/scenario.rs` — raw and validated ScenarioV1 types.
  </read_first>
  <files>crates/liquidfun-test-protocol/src/lib.rs, crates/liquidfun-test-protocol/src/codec.rs, crates/liquidfun-test-protocol/src/scenario.rs</files>
  <behavior>
    - One complete UTF-8 JSON object plus newline decodes; blank, invalid UTF-8, missing newline, trailing bytes, and over-1-MiB records fail distinctly.
    - Duplicate object members, unknown fields/kinds, unsupported independent versions, and nesting deeper than 32 fail before execution.
    - IDs and strings are bounded during typed visitation; ordered entity/command/checkpoint/observable collections enforce N and reject N+1.
    - Scenario validation rejects duplicate IDs, bad checkpoint references, nonempty Phase-2 entities, zero or excessive solver iterations/steps, and aggregate-limit violations.
    - Exact `u32` float bits survive parse/encode unchanged, including signed zero and NaN payload fixture cases.
  </behavior>
  <action>
Per D-01 through D-05, implement one-record UTF-8 JSONL framing with pre-parse byte/newline/depth checks and direct decoding into strict tagged structs. Use `#[serde(deny_unknown_fields)]` and bounded custom visitors for strings/collections; add duplicate-member detection at every object depth rather than first parsing into `serde_json::Value`. Parse raw scenario records once into `ValidatedScenarioV1`, whose constructors enforce uniqueness, references, creation/command/checkpoint order, aggregate limits, supported versions, and the Phase-2 empty-entity restriction. Model source as a tagged `Named`/`Seeded` enum and observables as a closed enum. Use in-memory test records with at least two distinguishable step boundaries/checkpoints so later reuse/reset tests can detect leakage. Encode all authoritative floats as numeric `u32` bit fields, never JSON decimals. Plan 05 owns checked-in fixtures and the permanent named scenario.

Root-cause choice: reject unsupported body/fixture/entity definitions in schema 1 instead of guessing Phase-3 public/storage semantics. The permanent seam is the validated command/checkpoint contract, not premature physics breadth.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-test-protocol scenario -- --nocapture &amp;&amp; cargo test -p liquidfun-test-protocol codec -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Focused Arrange/Act/Assert tests cover accepted and rejected in-memory records with their expected typed categories.
    - `rg -n 'deny_unknown_fields' crates/liquidfun-test-protocol/src/codec.rs crates/liquidfun-test-protocol/src/scenario.rs` finds all raw boundary structs.
    - `rg -n 'serde_json::Value|unwrap\(' crates/liquidfun-test-protocol/src` returns no matches.
    - `cargo test -p liquidfun-test-protocol scenario -- --nocapture &amp;&amp; cargo test -p liquidfun-test-protocol codec -- --nocapture` passes N/N+1 tests for record, nesting, ID, entity, command, checkpoint, and observable limits.
  </acceptance_criteria>
  <done>The named/seeded scenario boundary is strict, bounded, deterministic, engine-neutral, and ready for both adapters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate handshake and streamed trace state</name>
  <read_first>
    - `crates/liquidfun-test-protocol/src/codec.rs` — strict framing and record decoding from Task 1.
    - `crates/liquidfun-test-protocol/src/scenario.rs` — request/scenario/checkpoint identity contract.
    - `crates/liquidfun-test-protocol/src/provenance.rs` — build identity validation and stable hash authority.
    - `crates/liquidfun-test-protocol/src/failure.rs` — sequence, identity, provenance, size, and reset categories.
    - `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md` — D-07 through D-14 are locked.
    - `CREATE: crates/liquidfun-test-protocol/src/trace.rs` — handshake/trace record types and state validator.
    - `CREATE: crates/liquidfun-test-protocol/src/tolerance.rs` — typed field-policy profile.
  </read_first>
  <files>crates/liquidfun-test-protocol/src/lib.rs, crates/liquidfun-test-protocol/src/trace.rs, crates/liquidfun-test-protocol/src/tolerance.rs</files>
  <behavior>
    - A handshake is accepted only before a request and only when supported version sets and recomputed build identity match expected oracle provenance.
    - A trace accepts exactly begin, ordered unique checkpoints with matching request/scenario/identity, and one end with matching count/payload hash and true reset proof.
    - Out-of-order, duplicate, missing, trailing, request-ID mismatched, identity-mismatched, oversized, and false-reset records produce distinct harness failures.
    - `WorldCounts` exposes exact zero-valued body/fixture/joint/contact/particle-system/group/particle counts; simulation time remains `FloatBits`.
    - The phase2-v1 typed tolerance model declares exact bits for simulation time plus typed synthetic policies for comparator tests.
  </behavior>
  <action>
Implement strict tagged `HandshakeRecord`, `ScenarioRequestRecord`, `TraceBegin`, `CheckpointRecord`, and `TraceEnd` types plus a consuming `TraceValidator` state machine that makes invalid transitions unrepresentable internally. Validate expected pinned oracle revision before any scenario is sent, then repeat and validate the stable identity hash on the trace. Compute canonical scenario/tolerance/trace payload hashes over documented deterministic bytes. Preserve checkpoint/phase order. Require end-of-request reset epoch and `reset_verified = true`; false/missing proof is `AdapterResetFailure` and poisons later supervision. Define the closed typed tolerance profile with exact/discrete/float/collection policy enums; thresholds are `FloatBits`, not decimal floats. Keep comparison implementation out of this crate.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-test-protocol trace -- --nocapture &amp;&amp; cargo test -p liquidfun-test-protocol tolerance -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover valid zero-, one-, and multi-checkpoint traces plus every invalid state transition and request/identity/reset mismatch.
    - `rg -n 'trace_begin|checkpoint|trace_end|reset_verified|reset_epoch|identity_sha256' crates/liquidfun-test-protocol/src/trace.rs` finds all required state fields.
    - `rg -n 'Absolute|AbsoluteRelative|Ulps|ExactBits|Ordered|Set|Multiset' crates/liquidfun-test-protocol/src/tolerance.rs` finds closed typed policies.
    - `rg -n 'epsilon|DEFAULT_EPSILON' crates/liquidfun-test-protocol/src/tolerance.rs` returns no generic/global tolerance.
  </acceptance_criteria>
  <done>Only fully sequenced, provenance-checked, reset-proven semantic traces can become `ValidatedTrace` values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| JSONL bytes to typed record | Untrusted bytes, object structure, strings, collections, and versions cross into Rust. |
| Typed record to validated scenario/trace | Cross-field identity, reference, ordering, aggregate, hash, provenance, and reset invariants become trusted. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Required coverage | Disposition | Mitigation in this plan |
| --- | --- | --- | --- | --- |
| T-02-JSONL | Tampering | Untrusted JSONL input | mitigate | Precheck UTF-8/bytes/newline/depth; reject duplicates, unknowns, trailing/partial records; use bounded visitors and direct strict structs. |
| T-02-RESOURCE | Denial of service | Resource exhaustion | mitigate | Enforce N/N+1 byte, depth, string, entity, command, checkpoint, observable, record, and trace bounds from the hashed named profile. |
| T-02-PROCESS | Spoofing/Elevation | Path/process misuse | mitigate | This pure crate opens no executable or caller-supplied path; confined launch is deferred to Plan 10. |
| T-02-OUTPUT | Tampering | Malformed child output | mitigate | Require exact record kinds/sequence, request IDs, identity, hashes, counts, reset proof, newline completion, and EOF position before `ValidatedTrace`. |
| T-02-STDERR | Information disclosure/DoS | Stderr retention | mitigate | Protocol stdout contains no diagnostics and trace limits are explicit; bounded stderr draining/retention is implemented in Plan 10. |
| T-02-ARTIFACT | Tampering/Repudiation | Artifact promotion/provenance | mitigate | This plan performs no tracked fixture or artifact writes; Plans 04-05 own presentation, and Plan 11 owns staged generation, replay, deterministic review, and promotion. |
| T-02-ISOLATION | Elevation | C++/serialization leakage | mitigate | All codec dependencies remain in the unpublished protocol crate; `liquidfun` stays outside the graph. |

No high-severity threat is left unmitigated.
</threat_model>

<verification>
Run `cargo fmt --all`, `cargo clippy -p liquidfun-test-protocol --all-targets --all-features -- -D warnings`, `cargo build -p liquidfun-test-protocol --all-targets --all-features`, and `cargo test -p liquidfun-test-protocol --all-features`. Run the scenario, codec, trace, and tolerance filters as separate Cargo invocations. Run `cargo xtask package verify` to confirm serialization dependencies do not enter the published archive.
</verification>

<success_criteria>
- Named and seeded empty-world scenario values validate deterministically with explicit ordered commands/checkpoints and exact float bits.
- Unsupported versions, unknown/duplicate/trailing/partial/oversized inputs, bad references, and malformed trace sequences fail closed with typed categories.
- Typed Rust validation remains authoritative and is ready for deterministic presentation files in Plans 04-05.
- No broad physics schema, public object model, final tolerance values, concurrent RPC, or FFI is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-03-SUMMARY.md`.
</output>
