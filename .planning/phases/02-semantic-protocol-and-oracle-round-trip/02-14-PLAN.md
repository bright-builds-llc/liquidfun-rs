---
phase: 02-semantic-protocol-and-oracle-round-trip
plan: "14"
type: execute
wave: 12
depends_on:
  - 02-13
files_modified:
  - ARCHITECTURE.md
  - TESTING.md
  - tools/xtask/src/main.rs
  - tools/xtask/src/docs.rs
  - tools/xtask/tests/docs_contract.rs
  - .github/workflows/ci.yml
  - .github/workflows/oracle.yml
autonomous: true
requirements:
  - COMP-03
  - COMP-04
  - COMP-05
  - COMP-06
  - COMP-07
  - COMP-08
  - COMP-09
  - DOCS-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-07-10T04-59-34
generated_at: 2026-07-10T05:24:31Z
must_haves:
  truths:
    - "Contributors can follow TESTING.md to validate protocol versions, run one-shot/reuse/replay/minimization, classify failures, and review/promote reference evidence."
    - "TESTING.md documents the complete test-layer model and actionable current/deferred fuzzing, Miri, sanitizer, and coverage workflows with prerequisites, artifacts, and local-versus-scheduled placement."
    - "ARCHITECTURE.md makes protocol/domain, pure comparison, process shell, C++ oracle, artifact, and published-crate dependency directions enforceable."
    - "Cargo CI exercises private Rust protocol/comparator/supervisor tests without submodules or C++, while oracle CI proves the real debug round trip and a separate fail-fast sanitizer/reset lane."
    - "The complete repository-required Rust, workspace, package, provenance, oracle, differential, sanitizer, docs, and diff checks pass without overstating physics parity."
  artifacts:
    - path: "TESTING.md"
      provides: "Protocol, diagnosis, replay/minimization, reference-review, sanitizer, and CI tier contract"
    - path: "ARCHITECTURE.md"
      provides: "Permanent harness seam and dependency direction"
    - path: "tools/xtask/src/docs.rs"
      provides: "Exact DOCS-05 testing-layer contract checker"
    - path: "tools/xtask/tests/docs_contract.rs"
      provides: "Positive and per-field negative documentation contract tests"
    - path: ".github/workflows/ci.yml"
      provides: "Submodule-free private Rust harness verification"
    - path: ".github/workflows/oracle.yml"
      provides: "Real round-trip, bounded-reuse, provenance, and sanitizer verification"
  key_links:
    - from: ".github/workflows/oracle.yml"
      to: "cargo xtask differential compare"
      via: "canonical debug and fail-fast sanitizer jobs"
      pattern: "differential compare"
    - from: "TESTING.md"
      to: "reference/artifacts/manifest.toml"
      via: "stage/replay/diff/review/promote policy"
      pattern: "review"
    - from: "ARCHITECTURE.md"
      to: "crates/liquidfun/Cargo.toml"
      via: "one-way private-tool-to-native-library dependency"
      pattern: "liquidfun-test-protocol"
    - from: "tools/xtask/src/docs.rs"
      to: "TESTING.md"
      via: "required testing-layer contract table"
      pattern: "docs check"
---

<objective>
Publish the enforceable harness architecture/testing contract and wire proportionate Cargo/oracle CI verification for the completed Phase-2 seam.

Purpose: Make the protocol, failure, diagnosis, review, and CI behavior discoverable and continuously verified without claiming broad physics compatibility.
Output: Updated architecture/testing documentation, submodule-free Rust checks, canonical real round-trip checks, scheduled/manual fail-fast sanitizer evidence, and a complete verification/simplification pass.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-RESEARCH.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-13-SUMMARY.md
@ARCHITECTURE.md
@TESTING.md
@.github/workflows/ci.yml
@.github/workflows/oracle.yml
@reference/artifacts/manifest.toml
@justfile

<interfaces>
CI trust separation remains: Cargo jobs use no submodule/CMake/C++, while oracle jobs initialize and verify the pinned submodule before configure/build/run. External actions remain full-SHA pinned with read-only permissions. Failure bundles live under `target/differential/failures` and contain only bounded request/identity/report/stderr evidence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the permanent protocol, diagnosis, and evidence boundaries</name>
  <read_first>
    - `ARCHITECTURE.md` — Phase-1 dependency direction and deferred protocol section to replace with actual boundaries.
    - `TESTING.md` — current ordered verification, CI tiers, deterministic retry, and future-suite language.
    - `protocol/schemas/protocol-v1.schema.json` — actual versioned transport fields.
    - `protocol/tolerances/phase2-v1.toml` — actual field-policy profile.
    - `reference/artifacts/manifest.toml` — actual reviewed evidence metadata.
    - `justfile` — exact contributor aliases.
    - `tools/xtask/src/main.rs` — xtask dispatch surface from Plan 13.
    - `CREATE: tools/xtask/src/docs.rs` — exact read-only DOCS-05 checker.
    - `CREATE: tools/xtask/tests/docs_contract.rs` — positive contract plus per-layer and per-field negative cases.
    - `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md` — all locked D-01 through D-16 documentation obligations.
  </read_first>
  <files>ARCHITECTURE.md, TESTING.md, tools/xtask/src/main.rs, tools/xtask/src/docs.rs, tools/xtask/tests/docs_contract.rs</files>
  <action>
Replace the deferred protocol text in `ARCHITECTURE.md` with the implemented dependency graph and responsibilities: unpublished pure protocol/domain crate, unpublished differential pure core and imperative process shell, private native Phase-2 adapter, process-isolated C++ executable/CMake wrapper, reviewed artifact boundary, xtask/just shell, and unchanged published `liquidfun`. Document one-way dependencies, strict parse boundary, exact bits versus comparison policy, sequential one-in-flight lifecycle, one-shot/bounded reuse, reset/poison/reap, and no FFI/public object-model/broad physics scope.

Expand `TESTING.md` with one machine-auditable `Testing layer contract` table containing exactly these rows: unit, integration/API, doctest, upstream compatibility, differential, property, checked-in regression, fuzz, Miri/UB-aliasing, native sanitizer, benchmark, and coverage. Require columns for purpose, current/deferred status, exact current command or actionable planned command, prerequisites/toolchain/service needs, expected reports/failure artifacts, deterministic retry policy, local/pull-request/scheduled/manual-release placement, and semantic interpretation. Document runnable/deferred command shapes, corpus/artifact locations, toolchain prerequisites, and scheduled placement for `cargo fuzz`, pinned-nightly Miri, fail-fast Rust/C++ sanitizers, and `cargo llvm-cov`, including that coverage is not parity and sanitizer/fuzz/Miri findings are harness failures rather than physics mismatches.

Also document exact protocol/scenario/trace/tolerance versions, named limits/profiles, commands for one-shot/reuse/replay/minimize/stage/review/promote, typed failure taxonomy, first-divergence interpretation, ordered versus unordered comparison, special-float policy, exact request/stderr/report retention, reference-data manifest/review/atomic promotion, minimized-regression same-signature rule, no deterministic retries, and Cargo-only versus oracle prerequisites. State honestly that Phase 2 proves only empty-world harness compatibility, not rigid/particle parity, completed deferred fuzz/Miri/coverage lanes, or final subsystem tolerances. Implement and register `cargo xtask docs check` in `tools/xtask/src/docs.rs`/`main.rs`: parse the exact table, require each row once, validate every required column is nonempty, restrict status/placement values, require a command, reject `TBD`/`TODO`/placeholders, and enforce per-layer prerequisite, artifact/corpus/report, retry, and semantic content. Add tests that independently remove each layer and blank each required column. The checker is read-only and generates nothing.
  </action>
  <verify>
    <automated>cargo test -p xtask --test docs_contract &amp;&amp; cargo xtask docs check &amp;&amp; mdformat --check ARCHITECTURE.md TESTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'unit|integration|doctest|upstream compatibility|differential|property|regression|fuzz|Miri|sanitizer|benchmark|coverage|protocol version 1|scenario schema version 1|trace schema version 1|tolerance profile version 1|one-shot|bounded reuse|first divergent|harness failure|physics mismatch|stage|review|promote|Cargo-only|scheduled' TESTING.md` finds every required layer and protocol topic.
    - `cargo test -p xtask --test docs_contract` proves missing/duplicate layers, each blank required column, invalid status/placement, missing command/artifact/prerequisite/retry/semantics, and `TBD`/`TODO` placeholders fail.
    - `cargo xtask docs check` validates the actual `TESTING.md` contract and exits nonzero on every DOCS-05 violation.
    - `rg -n 'cargo fuzz|cargo miri|cargo llvm-cov|UBSAN_OPTIONS|ASAN_OPTIONS|local|pull request|scheduled|manual' TESTING.md` finds actionable commands and placement for all four expensive evidence workflows.
    - `rg -n 'liquidfun-test-protocol|liquidfun-differential|process-isolated|functional core|imperative shell|no FFI|default member' ARCHITECTURE.md` finds dependency/boundary language.
    - Documentation commands exactly match implemented xtask/just help output and no longer call the protocol "deferred".
    - Maturity text says empty-world protocol proof and does not claim broad physics parity.
  </acceptance_criteria>
  <done>Architecture and testing docs accurately explain how to use, diagnose, secure, review, and extend the implemented harness.</done>
</task>

<task type="auto">
  <name>Task 2: Add CI round-trip/sanitizer gates and perform the final simplification pass</name>
  <read_first>
    - `.github/workflows/ci.yml` — submodule-free workspace quality and package jobs.
    - `.github/workflows/oracle.yml` — pinned canonical/portability oracle jobs, permissions, tool installation, and cache keys.
    - `TESTING.md` — exact tiers/commands established by Task 1.
    - `tools/reference/CMakePresets.json` — debug and fail-fast sanitizer presets.
    - `tools/xtask/src/differential.rs` — exact CI commands and failure output paths.
    - `reference/artifacts/manifest.toml` — provenance gate and reviewed trace.
  </read_first>
  <files>.github/workflows/ci.yml, .github/workflows/oracle.yml</files>
  <action>
Keep Cargo CI submodule-free and explicitly exercise protocol fixture/schema, comparator/minimizer, supervisor failure-injection, provenance-schema fixture, workspace build/test/docs, and package isolation without invoking CMake or a real child. In canonical Linux oracle CI, after exact tool/upstream/provenance checks, build `liquidfun-reference`, run one-shot and two-request reuse compare, replay the reviewed trace, and assert read-only evidence stays unchanged. Add a scheduled/manual `oracle-asan-ubsan` job using exact canonical tools. The job must execute these exact repository-root commands: `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 cargo xtask differential compare --scenario empty-world --preset oracle-asan-ubsan --session-profile one-shot` and `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 cargo xtask differential compare --scenario empty-world --preset oracle-asan-ubsan --session-profile sanitizer`. The `sanitizer` profile exercises the bounded reset/reuse corpus; sanitizer evidence or zero-exit marker detection must fail. Upload bounded `target/differential/failures` only on failure using a full-commit-SHA pinned reviewed artifact action, with no secrets and read-only permissions. Preserve portability jobs as non-canonical and do not let them promote artifacts.

Perform the explicit simplification/root-cause pass required by repository guidance: remove duplicated validation from workflows/just in favor of xtask; keep standard-library threads rather than Tokio; keep one supervisor rather than separate one-shot/reuse implementations; keep typed comparison rather than JSON-path rules; confirm no C++/serde/build-script/default-member leakage; and remove any workaround that retries, globally sorts, widens tolerance, or hand-mutates golden data.
  </action>
  <verify>
    <automated>actionlint .github/workflows/ci.yml .github/workflows/oracle.yml &amp;&amp; UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 cargo xtask differential compare --scenario empty-world --preset oracle-asan-ubsan --session-profile one-shot &amp;&amp; UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 cargo xtask differential compare --scenario empty-world --preset oracle-asan-ubsan --session-profile sanitizer</automated>
  </verify>
  <acceptance_criteria>
    - Cargo workflow contains no `submodules: recursive`, CMake command, or real oracle compare, yet tests all private Rust packages/failure fixtures.
    - `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 cargo xtask differential compare --scenario empty-world --preset oracle-asan-ubsan --session-profile one-shot` exits zero and proves fail-fast one-shot execution.
    - `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 cargo xtask differential compare --scenario empty-world --preset oracle-asan-ubsan --session-profile sanitizer` exits zero and proves the bounded reset/reuse sanitizer corpus.
    - Oracle workflow contains one-shot, reuse, replay, provenance, and both exact fail-fast ASan/UBSan commands above, and any upload action is pinned to a 40-hex commit SHA.
    - Workflow permissions remain `contents: read`; no write permission, secret, artifact promotion, or portability publication step exists.
    - The full plan-level verification sequence below passes, `git diff --check` passes, and `git diff --exit-code -- crates/liquidfun` succeeds.
  </acceptance_criteria>
  <done>CI continuously proves the private Rust core, real process seam, reset/reuse, reviewed replay, and sanitizer classification without weakening isolation or truthfulness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Contributor/CI commands to harness | Documentation and workflows must invoke only reviewed entrypoints and profiles. |
| CI checkout/toolchain to oracle evidence | Exact submodule/tool/action identity and read-only permissions determine trust. |
| Failure bundle to CI artifact | Only bounded, non-secret request/identity/report/stderr evidence may be retained or uploaded. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Required coverage | Disposition | Mitigation in this plan |
| --- | --- | --- | --- | --- |
| T-02-JSONL | Tampering | Untrusted JSONL input | mitigate | Document and CI-test duplicate/unknown/partial/oversized rejection and strict typed parsing in both Rust and C++. |
| T-02-RESOURCE | Denial of service | Resource exhaustion | mitigate | Document named limits; CI runs boundary/failure tests, finite reuse, timeouts, sanitizer/reset corpus, and job timeouts. |
| T-02-PROCESS | Spoofing/Elevation | Path/process misuse | mitigate | CI uses exact pinned checkout/tools, allowlisted xtask presets, structured commands, read-only permissions, and no arbitrary path inputs. |
| T-02-OUTPUT | Tampering | Malformed child output | mitigate | Cargo CI runs all failure-injection states; oracle CI requires complete identity/schema/reset-validated traces before Match. |
| T-02-STDERR | Information disclosure/DoS | Stderr retention | mitigate | Docs specify bounded first/last retention; CI uploads only bounded failure directory, no secrets/raw unlimited streams. |
| T-02-ARTIFACT | Tampering/Repudiation | Artifact promotion/provenance | mitigate | CI is read-only, verifies tracked hashes/review/source identity, asserts no drift, and never promotes portability or unreviewed output. |
| T-02-SUPPLY | Spoofing | CI dependency/tool substitution | mitigate | Full-SHA actions, checked tool download hashes, exact compiler/CMake/Ninja assertions, immutable submodule, and vendor checksum validation. |

No high-severity ASVS L1 threat is accepted; documentation and CI enforce every in-scope mitigation.
</threat_model>

<verification>
Run in this exact order:

1. `cargo fmt --all`
2. `cargo clippy --all-targets --all-features -- -D warnings`
3. `cargo build --all-targets --all-features`
4. `cargo test --all-features`
5. `cargo clippy --workspace --all-targets --all-features -- -D warnings`
6. `cargo build --workspace --all-targets --all-features`
7. `cargo test --workspace --all-features`
8. `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps`
9. `cargo xtask package verify`
10. `cargo xtask upstream verify`
11. `cargo xtask provenance check`
12. `cargo xtask upstream configure --preset oracle-debug`
13. `cargo xtask upstream build --preset oracle-debug`
14. `cargo xtask differential compare --scenario empty-world --preset oracle-debug --session-profile one-shot`
15. `cargo xtask differential compare --scenario empty-world --preset oracle-debug --session-profile reuse`
16. `cargo xtask differential replay --scenario empty-world --preset oracle-debug`
17. `cargo xtask upstream configure --preset oracle-asan-ubsan`
18. `cargo xtask upstream build --preset oracle-asan-ubsan`
19. From the repository root, run `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 cargo xtask differential compare --scenario empty-world --preset oracle-asan-ubsan --session-profile one-shot`, then run `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 cargo xtask differential compare --scenario empty-world --preset oracle-asan-ubsan --session-profile sanitizer` to exercise the bounded reset/reuse corpus.
20. `cargo xtask check`, `just check`, `actionlint .github/workflows/ci.yml .github/workflows/oracle.yml`, `mdformat --check ARCHITECTURE.md TESTING.md`, and `git diff --check`.

Repeat provenance/replay/check and assert no tracked schema, scenario, trace, manifest, or report bytes change. Review all Phase-2 diffs for unintended public API, C++/serialization/default-member leakage, broad physics/schema/tolerance scope, hidden foreign code, or swallowed errors.
</verification>

<success_criteria>
- DOCS-05 is satisfied with accurate versions, the complete test-layer model, diagnosis, review, replay/minimization, and actionable fuzz/Miri/sanitizer/coverage guidance including commands, prerequisites, artifacts, status, and local/CI placement.
- Cargo CI remains C++/submodule-free; oracle CI proves real one-shot/reuse/replay and fail-fast sanitizer classification.
- All required Rust/workspace/package/provenance/oracle/differential/docs/workflow checks pass and read-only checks produce no drift.
- The final design uses the simplest root-cause seams: strict domain parsing, pure comparator/reducer, one bounded supervisor, external C++ process, and explicit reviewed promotion.
- Phase 2 claims only a trustworthy empty-world harness seam and does not overreach into Phase 3 or broad physics parity.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-14-SUMMARY.md`.
</output>
