---
phase: 02-semantic-protocol-and-oracle-round-trip
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - crates/liquidfun-test-protocol/src/lib.rs
  - crates/liquidfun-test-protocol/src/failure.rs
  - crates/liquidfun-test-protocol/src/float_bits.rs
  - crates/liquidfun-test-protocol/src/ids.rs
  - crates/liquidfun-test-protocol/src/limits.rs
  - crates/liquidfun-test-protocol/src/provenance.rs
autonomous: true
requirements:
  - COMP-03
  - COMP-05
  - COMP-09
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-07-10T04-59-34
generated_at: 2026-07-10T05:24:31Z
must_haves:
  truths:
    - "Versions, semantic IDs, exact float bits, limit profiles, provenance identities, and harness failures are invariant-bearing Rust types rather than unchecked primitives."
    - "The named reviewed profiles bound parsing, output, stderr, time, and process reuse before effectful code consumes them."
    - "Physics mismatch remains absent from the harness-failure taxonomy."
  artifacts:
    - path: "crates/liquidfun-test-protocol/src/limits.rs"
      provides: "Named phase2-default-v1 byte, count, timeout, stderr, and request-budget limits"
    - path: "crates/liquidfun-test-protocol/src/failure.rs"
      provides: "Exhaustive harness failure taxonomy kept separate from physics mismatches"
    - path: "crates/liquidfun-test-protocol/src/provenance.rs"
      provides: "Validated build identity and stable identity hash"
  key_links:
    - from: "crates/liquidfun-test-protocol/src/lib.rs"
      to: "crates/liquidfun-test-protocol/src/failure.rs"
      via: "private protocol contract exports"
      pattern: "HarnessFailure"
    - from: "crates/liquidfun-test-protocol/src/provenance.rs"
      to: "reference/upstream-lock.toml"
      via: "full pinned oracle revision validation"
      pattern: "oracle_revision"
---

<objective>
Define the invariant-bearing primitive types shared by protocol parsing, adapters, comparison, supervision, and evidence workflows.

Purpose: Establish one authoritative typed vocabulary before downstream modules depend on unchecked strings and numbers.
Output: Exact float/version/ID/provenance/limit/failure types with focused unit tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-RESEARCH.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-01-SUMMARY.md
@reference/upstream-lock.toml
@crates/liquidfun-test-protocol/Cargo.toml
@crates/liquidfun-test-protocol/src/lib.rs

<interfaces>
Create these contracts for downstream plans:

```rust
pub struct ProtocolVersion(u32);
pub struct ScenarioSchemaVersion(u32);
pub struct TraceSchemaVersion(u32);
pub struct ToleranceProfileVersion(u32);
pub struct FloatBits(u32);
pub struct RequestId(/* validated bounded ASCII */);
pub struct ScenarioId(/* validated bounded ASCII */);
pub struct CheckpointId(/* validated bounded ASCII */);
pub struct SemanticEntityId { kind: SemanticEntityKind, ordinal: u32 }
pub struct HarnessLimits { /* named immutable profile values */ }
pub enum HarnessFailureKind { /* process/protocol/provenance taxonomy */ }
pub struct BuildIdentity { /* oracle/adapter/compiler/target/flags identity */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define invariant-bearing protocol primitives and harness failures</name>
  <read_first>
    - `crates/liquidfun-test-protocol/src/lib.rs` — package export surface created by Plan 01.
    - `crates/liquidfun-test-protocol/Cargo.toml` — available dependencies and lint policy.
    - `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md` — D-02 through D-05 and D-08 through D-10 are authoritative.
    - `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-RESEARCH.md` — exact version axes, initial limits, identity fields, and failure variants.
    - `reference/upstream-lock.toml` — canonical oracle revision spelling.
    - `CREATE: crates/liquidfun-test-protocol/src/failure.rs` — harness taxonomy.
    - `CREATE: crates/liquidfun-test-protocol/src/float_bits.rs` — exact `f32` transport representation.
    - `CREATE: crates/liquidfun-test-protocol/src/ids.rs` — bounded IDs and semantic IDs.
    - `CREATE: crates/liquidfun-test-protocol/src/limits.rs` — named immutable profiles.
    - `CREATE: crates/liquidfun-test-protocol/src/provenance.rs` — build and stable identity contracts.
  </read_first>
  <files>crates/liquidfun-test-protocol/src/lib.rs, crates/liquidfun-test-protocol/src/failure.rs, crates/liquidfun-test-protocol/src/float_bits.rs, crates/liquidfun-test-protocol/src/ids.rs, crates/liquidfun-test-protocol/src/limits.rs, crates/liquidfun-test-protocol/src/provenance.rs</files>
  <behavior>
    - Version newtypes accept only the supported positive version `1` through explicit constructors and report unsupported values without coercion.
    - `FloatBits` round-trips representative finite values, signed zeros, infinities, and NaN payload bits without decimal conversion.
    - Typed IDs accept bounded lowercase ASCII identifiers and reject empty, uppercase, control, non-ASCII, invalid-leading, invalid-character, and 129-byte inputs.
    - `phase2-default-v1` exposes the researched record/trace/output/stderr/count/deadline/request-budget limits and a stable profile hash.
    - `BuildIdentity` validates full 40-hex oracle revisions, nonempty adapter/compiler/target/flags fields, and a recomputed stable identity SHA-256.
    - Every D-10 harness condition has a distinct `HarnessFailureKind`; `PhysicsMismatch` is intentionally absent from that enum.
  </behavior>
  <action>
Write focused tests first, then implement the version, `FloatBits`, ID, `HarnessLimits`, `BuildIdentity`, and failure types as the shared engine-neutral core. Use newtypes/enums and fallible constructors so invalid states cannot reach later execution. Preserve `u32` bits authoritatively and expose decimal `f32` only as diagnostic conversion. Define the named default/reuse/sanitizer request budgets from research; do not expose arbitrary weakening through CLI-shaped setters. Make failure evidence carry request/scenario hashes, maybe exit status, elapsed time, last valid record, bounded stderr metadata, killed/reaped flags, and limit/identity context using `maybe_` names for `Option` fields. Use `thiserror`; do not use `unwrap()`. Keep modules as `foo.rs`, with any future children under `foo/`.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-test-protocol --lib</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p liquidfun-test-protocol --lib` passes focused Arrange/Act/Assert tests for IDs, float bits, limits, provenance, and failure taxonomy.
    - `rg -n 'StartupTimeout|HandshakeMalformed|WrongProvenance|RequestTimeout|ChildNonZeroExit|ChildSignaled|SanitizerReport|UnexpectedEof|PartialRecord|MalformedRecord|UnknownRecordKind|RecordTooLarge|TraceTooLarge|TotalOutputExceeded|SequenceViolation|RequestIdMismatch|TraceIdentityMismatch|AdapterResetFailure' crates/liquidfun-test-protocol/src/failure.rs` finds the typed variants.
    - `rg -n 'unwrap\(' crates/liquidfun-test-protocol/src` returns no matches.
    - `rg -n '1024 \* 1024|32 \* 1024 \* 1024|256 \* 1024|64 \* 1024 \* 1024|Duration::from_secs\(5\)|Duration::from_secs\(10\)' crates/liquidfun-test-protocol/src/limits.rs` confirms the named researched boundary values.
  </acceptance_criteria>
  <done>Downstream codecs and runners can depend on strict versions, IDs, bits, limits, identities, and failures without revalidating primitives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Raw protocol value to domain type | Future untrusted JSONL fields become trusted only after fallible invariant constructors. |
| Harness configuration | Named reviewed profiles control memory, output, deadline, and process-reuse budgets. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Required coverage | Disposition | Mitigation in this plan |
| --- | --- | --- | --- | --- |
| T-02-JSONL | Tampering | Untrusted JSONL input | mitigate | Establish bounded IDs, exact versions/bits, strict identity constructors, and typed failures; Plan 03 performs byte/shape parsing. |
| T-02-RESOURCE | Denial of service | Resource exhaustion | mitigate | Define immutable named byte/count/depth/output/stderr/deadline/request profiles and stable hashes before any allocator or child uses them. |
| T-02-PROCESS | Spoofing/Elevation | Path/process misuse | mitigate | Keep process code out of the protocol crate and arbitrary executable/config paths out of these interfaces; Plan 10 adds confined allowlisted launch types. |
| T-02-OUTPUT | Tampering | Malformed child output | mitigate | Define sequence/request/identity/size failure variants; only later `ValidatedTrace` values may reach comparison. |
| T-02-STDERR | Information disclosure/DoS | Stderr retention | mitigate | Model bounded retained stderr, total/truncated counts, and maybe exit evidence without unbounded strings; Plan 10 owns concurrent draining. |
| T-02-ARTIFACT | Tampering/Repudiation | Artifact promotion/provenance | mitigate | Define validated build identity and content/profile hash primitives; Plan 11 confines staging, replay, review, and atomic promotion. |

No high-severity threat is accepted or transferred in this plan.
</threat_model>

<verification>
Run `cargo fmt --all`, `cargo clippy -p liquidfun-test-protocol --all-targets --all-features -- -D warnings`, `cargo build -p liquidfun-test-protocol --all-targets --all-features`, and `cargo test -p liquidfun-test-protocol --all-features`. Run `cargo xtask package verify` to preserve consumer isolation. Review the newtypes and enums against D-02 through D-05 and D-08 through D-10 and confirm every invalid state enters through a fallible constructor.
</verification>

<success_criteria>
- Exact versions, IDs, float bits, limits, provenance, and harness failures are validated domain types with focused tests.
- Named profiles are deterministic, hashed, and cannot be weakened through an arbitrary public configuration path.
- Harness failure evidence stays bounded and physics mismatch remains a separate later comparison outcome.
- The implementation follows the architecture, code-shape, testing, verification, and Rust standards loaded for planning.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-02-SUMMARY.md`.
</output>
