---
phase: 02-semantic-protocol-and-oracle-round-trip
plan: "13"
type: execute
wave: 11
depends_on:
  - 02-12
files_modified:
  - tools/xtask/src/main.rs
  - tools/xtask/src/differential.rs
  - tools/xtask/tests/differential_cli.rs
  - tools/xtask/tests/fixtures/fake_differential_tool.rs
  - justfile
autonomous: true
requirements:
  - COMP-05
  - COMP-08
  - COMP-09
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-07-10T04-59-34
generated_at: 2026-07-10T05:24:31Z
must_haves:
  truths:
    - "Contributors can compare, replay, minimize, stage, review, promote, and verify through allowlisted xtask commands and thin just aliases."
    - "Every oracle-dependent command uses structured arguments and repository-owned scenario, preset, profile, and artifact paths."
    - "Cargo-only aggregate checks remain useful without initializing or building the C++ oracle."
  artifacts:
    - path: "tools/xtask/src/differential.rs"
      provides: "Allowlisted repository command shell for the differential runner"
    - path: "tools/xtask/tests/differential_cli.rs"
      provides: "Command/preset/path/failure propagation coverage"
    - path: "justfile"
      provides: "Thin discoverable aliases over xtask"
  key_links:
    - from: "tools/xtask/src/differential.rs"
      to: "crates/liquidfun-differential/src/main.rs"
      via: "structured allowlisted runner invocation"
      pattern: "differential"
    - from: "justfile"
      to: "tools/xtask/src/differential.rs"
      via: "direct cargo xtask aliases"
      pattern: "cargo xtask differential"
---

<objective>
Expose the implemented differential and evidence workflows through safe contributor entrypoints.

Purpose: Keep orchestration transparent and allowlisted without duplicating validation or artifact lifecycle logic.
Output: Focused xtask dispatch, thin just aliases, and command-level fake-tool tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-12-SUMMARY.md
@crates/liquidfun-differential/src/main.rs
@tools/xtask/src/main.rs
@tools/xtask/src/upstream.rs
@justfile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add transparent xtask and just differential entrypoints</name>
  <read_first>
    - `tools/xtask/src/main.rs` — exact dispatch/error/aggregate-check pattern.
    - `tools/xtask/Cargo.toml` — private shell dependency boundary; no new dependency is expected.
    - `tools/xtask/src/upstream.rs` — preset validation and executable path conventions.
    - `tools/xtask/tests/upstream_cli.rs` — separately compiled fake-tool test pattern.
    - `justfile` — thin aliases only.
    - `crates/liquidfun-differential/src/main.rs` — runner command contract from Plans 10-11.
    - `CREATE: tools/xtask/src/differential.rs` — structured runner orchestration.
    - `CREATE: tools/xtask/tests/differential_cli.rs` — command/preset/path failure tests.
    - `CREATE: tools/xtask/tests/fixtures/fake_differential_tool.rs` — structured invocation recorder/failer.
  </read_first>
  <files>tools/xtask/src/main.rs, tools/xtask/src/differential.rs, tools/xtask/tests/differential_cli.rs, tools/xtask/tests/fixtures/fake_differential_tool.rs, justfile</files>
  <behavior>
    - xtask accepts only compare/replay/minimize/fixture subcommands, checked-in scenario names, three presets, and reviewed session profiles; unknown options fail usage.
    - Every oracle-dependent command verifies upstream identity/configuration and accepts no arbitrary executable/output/destination path.
    - `cargo xtask check` validates protocol schemas/fixtures/provenance/package read-only without requiring a submodule or building C++.
    - just aliases are direct visible xtask commands with no validation logic, loops, conditionals, or swallowed status.
  </behavior>
  <action>
Add a focused `differential` xtask module and `XtaskError` variant. Resolve only allowlisted commands/values, call existing upstream verification/build seams where explicitly requested, and invoke the runner with structured arguments through an injectable test executable seam. Add compare, reuse, replay, minimize, fixture stage/review/promote aliases to `justfile`; do not embed C++, JSON, shell programs, or duplicated validation. Extend aggregate check with read-only protocol fixture/schema and artifact provenance checks while preserving labeled Cargo-only behavior and never silently regenerating evidence.
  </action>
  <verify>
    <automated>cargo test -p xtask --test differential_cli &amp;&amp; cargo xtask differential compare --scenario empty-world --preset oracle-debug --session-profile one-shot &amp;&amp; cargo xtask differential compare --scenario empty-world --preset oracle-debug --session-profile reuse</automated>
  </verify>
  <acceptance_criteria>
    - Command tests assert exact structured arguments and reject arbitrary preset/scenario/profile/path/extra options and propagated child failures.
    - `just --list` exposes new commands and each recipe body is a direct `cargo xtask differential ...` line.
    - One-shot/reuse commands return Match and replay validates the reviewed trace.
    - `cargo xtask check` passes with the initialized submodule and in a temporary Cargo-only copy without `third_party/liquidfun`.
  </acceptance_criteria>
  <done>Contributors have transparent safe commands for the complete workflow without duplicated harness logic or weakened Cargo-only mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CLI/xtask input to filesystem/process | Contributor arguments select only named scenarios, presets, profiles, and artifact operations. |
| just alias to xtask | Human-facing convenience must not bypass or duplicate checked validation. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Required coverage | Disposition | Mitigation in this plan |
| --- | --- | --- | --- | --- |
| T-02-PROCESS | Spoofing/Elevation | Arbitrary executable or path input | mitigate | Closed command/preset/profile/scenario enums and structured process arguments reject arbitrary paths. |
| T-02-OUTPUT | Tampering | Child failure hidden by wrappers | mitigate | xtask and just propagate nonzero status and typed failure output without retries or swallowed errors. |
| T-02-ARTIFACT | Tampering/Repudiation | Bypassed promotion gates | mitigate | Entry points delegate to Plan 11 lifecycle APIs; aliases contain no mutation logic. |
| T-02-ISOLATION | Elevation | C++ requirements in Cargo-only check | mitigate | Aggregate check performs read-only Rust/data verification and does not configure or build C++. |

No high-severity threat is accepted.
</threat_model>

<verification>
Run `cargo fmt --all`, `cargo clippy -p xtask --all-targets --all-features -- -D warnings`, `cargo build -p xtask --all-targets --all-features`, and `cargo test -p xtask --all-features`. Run one-shot/reuse/replay through xtask, inspect `just --list`, run `cargo xtask check` in full and Cargo-only contexts, and confirm all commands propagate failures.
</verification>

<success_criteria>
- Contributor commands cover compare, replay, minimize, fixture lifecycle, and verification through allowlisted values.
- just remains a thin facade and all validation stays in typed runner/xtask modules.
- Cargo-only check remains submodule/C++ independent.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-13-SUMMARY.md`.
</output>
