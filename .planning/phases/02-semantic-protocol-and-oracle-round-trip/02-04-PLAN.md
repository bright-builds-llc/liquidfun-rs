---
phase: 02-semantic-protocol-and-oracle-round-trip
plan: "04"
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - crates/liquidfun-test-protocol/src/lib.rs
  - crates/liquidfun-test-protocol/src/schema.rs
  - protocol/schemas/protocol-v1.schema.json
  - protocol/schemas/scenario-v1.schema.json
  - protocol/schemas/trace-v1.schema.json
  - protocol/tolerances/phase2-v1.toml
autonomous: true
requirements:
  - COMP-03
  - COMP-05
  - COMP-09
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-07-10T04-59-34
generated_at: 2026-07-10T05:24:31Z
must_haves:
  truths:
    - "Checked-in schemas and the Phase-2 tolerance profile are deterministic presentation derived from stricter typed Rust authority."
    - "Independent protocol, scenario, trace, and tolerance version axes stay explicit and unsupported values remain rejected."
    - "Read-only presentation checks never rewrite tracked files."
  artifacts:
    - path: "crates/liquidfun-test-protocol/src/schema.rs"
      provides: "Deterministic schema and tolerance presentation checker"
    - path: "protocol/schemas/scenario-v1.schema.json"
      provides: "Reviewable closed scenario schema presentation"
    - path: "protocol/tolerances/phase2-v1.toml"
      provides: "Reviewed versioned Phase-2 field-policy presentation"
  key_links:
    - from: "crates/liquidfun-test-protocol/src/schema.rs"
      to: "crates/liquidfun-test-protocol/src/scenario.rs"
      via: "deterministic presentation of typed scenario variants"
      pattern: "ScenarioV1"
    - from: "protocol/tolerances/phase2-v1.toml"
      to: "crates/liquidfun-test-protocol/src/tolerance.rs"
      via: "strict versioned typed tolerance profile"
      pattern: "phase2-v1"
---

<objective>
Add deterministic schema and tolerance presentation for the typed protocol core.

Purpose: Make versioned protocol shapes and reviewed field policies inspectable without weakening typed cross-field validation.
Output: Three stable JSON schemas, one strict TOML tolerance profile, and read-only byte-stability checks.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-03-SUMMARY.md
@crates/liquidfun-test-protocol/src/scenario.rs
@crates/liquidfun-test-protocol/src/trace.rs
@crates/liquidfun-test-protocol/src/tolerance.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Check the reviewed tolerance profile presentation</name>
  <read_first>
    - `crates/liquidfun-test-protocol/src/tolerance.rs` — typed field-policy authority from Plan 03.
    - `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md` — D-04, D-06, D-12, and D-13 are locked.
    - `CREATE: crates/liquidfun-test-protocol/src/schema.rs` — shared deterministic presentation checks.
    - `CREATE: protocol/tolerances/phase2-v1.toml` — reviewed profile presentation.
  </read_first>
  <files>crates/liquidfun-test-protocol/src/lib.rs, crates/liquidfun-test-protocol/src/schema.rs, protocol/tolerances/phase2-v1.toml</files>
  <behavior>
    - Strict TOML parsing rejects unknown fields, unsupported profile versions, duplicate policies, decimal thresholds, and a mismatched profile hash.
    - Simulation time uses exact-bit policy; synthetic absolute, absolute-relative, and ULP policies exist only for comparator coverage and do not claim future subsystem tolerances.
    - Re-rendering the profile to memory yields byte-identical newline-terminated content.
  </behavior>
  <action>
Implement the smallest deterministic profile renderer/checker over the typed `ToleranceProfile`. Keep thresholds as exact `FloatBits` fields and require the reviewed profile identity/hash. Check the tracked file read-only; never regenerate it during ordinary tests. Make descriptions state that Phase 2 sets no broad rigid-body, joint, or particle tolerance values.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-test-protocol tolerance_profile_presentation -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - The focused test proves strict parsing, version/hash checks, exact-bit threshold representation, and byte-stable in-memory rendering.
    - `rg -n 'epsilon|f32\s*=|f64\s*=' protocol/tolerances/phase2-v1.toml` returns no generic/global decimal tolerance.
    - `git diff --exit-code -- protocol/tolerances/phase2-v1.toml` remains clean after the test.
  </acceptance_criteria>
  <done>The reviewed Phase-2 tolerance profile is strict, deterministic presentation of typed field policies.</done>
</task>

<task type="auto">
  <name>Task 2: Generate deterministic schema presentation</name>
  <read_first>
    - `crates/liquidfun-test-protocol/src/scenario.rs` — typed scenario authority.
    - `crates/liquidfun-test-protocol/src/trace.rs` — typed transport/trace authority.
    - `crates/liquidfun-test-protocol/src/tolerance.rs` — typed tolerance authority.
    - `crates/liquidfun-test-protocol/src/schema.rs` — presentation checker from Task 1.
    - `CREATE: protocol/schemas/protocol-v1.schema.json` — checked-in transport schema presentation.
    - `CREATE: protocol/schemas/scenario-v1.schema.json` — checked-in scenario schema presentation.
    - `CREATE: protocol/schemas/trace-v1.schema.json` — checked-in trace schema presentation.
  </read_first>
  <files>crates/liquidfun-test-protocol/src/schema.rs, protocol/schemas/protocol-v1.schema.json, protocol/schemas/scenario-v1.schema.json, protocol/schemas/trace-v1.schema.json</files>
  <action>
Per D-06, render stable, newline-terminated JSON schema presentation for protocol/scenario/trace version 1 without adding a schema framework. Make typed Rust/C++ validation authoritative for cross-field references, uniqueness, ordering, hashes, and aggregate limits, and state that limitation in schema descriptions. Regenerate only to memory in check-mode tests and compare byte-for-byte with tracked schemas. Any explicit future generator must require intentional maintainer invocation.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-test-protocol schema -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - The focused test proves all three schemas are byte-stable and newline-terminated.
    - `git diff --exit-code -- protocol/schemas` remains clean after check-mode tests.
    - Each schema contains the applicable independent version fields and `additionalProperties: false` for closed records.
    - No schema claims to enforce cross-field invariants owned by typed validation.
  </acceptance_criteria>
  <done>Reviewable schemas are deterministic checked-in presentation backed by stricter typed validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Typed protocol to presentation | Generated schemas and profile text must remain faithful but non-authoritative. |
| Checked-in presentation files | Read-only checks must detect drift without silently rewriting reviewed bytes. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Required coverage | Disposition | Mitigation in this plan |
| --- | --- | --- | --- | --- |
| T-02-JSONL | Tampering | Untrusted JSONL input | mitigate | Closed schemas mirror strict record kinds while explicitly deferring cross-field authority to typed validators. |
| T-02-RESOURCE | Denial of service | Resource exhaustion | mitigate | Schemas expose named bounds and the checker renders only fixed repository-owned documents. |
| T-02-OUTPUT | Tampering | Presentation drift | mitigate | Byte-stable in-memory rendering and hash/version checks fail on unreviewed drift. |
| T-02-ARTIFACT | Tampering/Repudiation | Silent regeneration | mitigate | Ordinary tests are read-only and require a clean diff for all tracked presentation files. |
| T-02-POLICY | Tampering | Over-broad comparison | mitigate | Exact typed policies and exact-bit thresholds prevent a global epsilon or undocumented future tolerances. |

No high-severity threat is accepted.
</threat_model>

<verification>
Run `cargo fmt --all`, `cargo clippy -p liquidfun-test-protocol --all-targets --all-features -- -D warnings`, `cargo build -p liquidfun-test-protocol --all-targets --all-features`, and `cargo test -p liquidfun-test-protocol --all-features`. Run the tolerance-presentation and schema filters as separate Cargo invocations, then assert `git diff --exit-code -- protocol/schemas protocol/tolerances`. Run `cargo xtask package verify` to preserve consumer isolation.
</verification>

<success_criteria>
- Schemas and the tolerance profile are deterministic presentation while typed validation remains authoritative.
- Independent version axes and exact-bit threshold fields are explicit and fail closed.
- Check-mode tests never mutate tracked files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-04-SUMMARY.md`.
</output>
