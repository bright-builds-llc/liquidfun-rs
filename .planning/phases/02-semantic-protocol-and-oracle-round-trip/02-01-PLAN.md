---
phase: 02-semantic-protocol-and-oracle-round-trip
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/liquidfun-test-protocol/Cargo.toml
  - crates/liquidfun-test-protocol/src/lib.rs
  - crates/liquidfun-differential/Cargo.toml
  - crates/liquidfun-differential/src/lib.rs
autonomous: true
requirements:
  - COMP-03
  - COMP-05
  - COMP-09
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-07-10T04-59-34
generated_at: 2026-07-10T05:24:31Z
must_haves:
  truths:
    - "Private protocol and differential tooling compile without changing the default Cargo consumer selection."
    - "The published liquidfun crate has no dependency, feature, build script, C++ discovery, or serialization dependency introduced by the harness."
  artifacts:
    - path: "crates/liquidfun-test-protocol/src/lib.rs"
      provides: "Unpublished engine-neutral protocol package boundary"
    - path: "crates/liquidfun-differential/Cargo.toml"
      provides: "Unpublished comparison runner package that depends inward on protocol and liquidfun"
  key_links:
    - from: "Cargo.toml"
      to: "crates/liquidfun-test-protocol/Cargo.toml"
      via: "workspace member and centralized private dependencies"
      pattern: "liquidfun-test-protocol"
    - from: "crates/liquidfun-differential/Cargo.toml"
      to: "crates/liquidfun-test-protocol/Cargo.toml"
      via: "private path dependency"
      pattern: "liquidfun-test-protocol"
    - from: "Cargo.toml"
      to: "crates/liquidfun/Cargo.toml"
      via: "unchanged sole default member"
      pattern: 'default-members = \["crates/liquidfun"\]'
---

<objective>
Create the isolated private workspace foundation for the semantic protocol and differential runner.

Purpose: Establish the private package and dependency direction before protocol types or orchestration are implemented.
Output: Two unpublished workspace crates with truthful roots and a proven unchanged Cargo-only consumer boundary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-RESEARCH.md
@.planning/phases/01-oracle-provenance-and-repository-foundation/01-02-SUMMARY.md
@.planning/phases/01-oracle-provenance-and-repository-foundation/01-04-SUMMARY.md
@Cargo.toml
@crates/liquidfun/Cargo.toml
@crates/liquidfun/src/lib.rs
@tools/xtask/Cargo.toml
@tools/xtask/src/provenance.rs

<interfaces>
The root workspace currently has `members = ["crates/liquidfun", "tools/xtask"]` and `default-members = ["crates/liquidfun"]`. Preserve the latter verbatim. The published crate has no dependencies or build script.

Plan 02 adds the invariant-bearing protocol contracts after this package boundary is verified.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isolated private workspace packages</name>
  <read_first>
    - `Cargo.toml` — workspace membership, centralized dependency, lint, and default-member authority.
    - `Cargo.lock` — existing resolved private-tool dependency graph; update only through Cargo.
    - `crates/liquidfun/Cargo.toml` — published package boundary that must remain dependency-free.
    - `tools/xtask/Cargo.toml` — established unpublished-package pattern.
    - `CREATE: crates/liquidfun-test-protocol/Cargo.toml` — new private protocol package manifest.
    - `CREATE: crates/liquidfun-test-protocol/src/lib.rs` — new protocol crate root.
    - `CREATE: crates/liquidfun-differential/Cargo.toml` — new private runner manifest.
    - `CREATE: crates/liquidfun-differential/src/lib.rs` — new runner crate root.
  </read_first>
  <files>Cargo.toml, Cargo.lock, crates/liquidfun-test-protocol/Cargo.toml, crates/liquidfun-test-protocol/src/lib.rs, crates/liquidfun-differential/Cargo.toml, crates/liquidfun-differential/src/lib.rs</files>
  <action>
Per D-01, D-06, and Phase-1 D-07/D-08, add `crates/liquidfun-test-protocol` and `crates/liquidfun-differential` to workspace `members` while leaving `default-members = ["crates/liquidfun"]` exactly unchanged. Both packages are version `0.0.0`, `publish = false`, inherit workspace edition/MSRV/lints, forbid unsafe code, and contain only truthful crate documentation at this task boundary. Add centralized `thiserror = "2.0.18"`; reuse the exact existing `serde`, `serde_json`, `sha2`, and `toml` workspace pins. Give the protocol crate only private serialization/hash/error dependencies. Give the differential crate path dependencies on the protocol crate and `liquidfun`, plus only the already-reviewed private dependencies it needs in later plans. Do not add Tokio, an approximate-equality crate, schema framework, FFI crate, RNG, feature, or build script. Regenerate `Cargo.lock` with Cargo.

Root-cause/simplification choice: use two cohesive private crates because wire/domain types must not depend on process effects, while one combined crate would make strict parsing and pure comparison depend on orchestration. Do not split the protocol into smaller packages.
  </action>
  <verify>
    <automated>cargo check -p liquidfun-test-protocol -p liquidfun-differential --all-targets --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '^default-members = \["crates/liquidfun"\]$' Cargo.toml` returns exactly one line.
    - `rg -n 'publish = false' crates/liquidfun-test-protocol/Cargo.toml crates/liquidfun-differential/Cargo.toml` returns both manifests.
    - `cargo tree -p liquidfun --edges normal` contains neither `liquidfun-test-protocol`, `liquidfun-differential`, `serde`, nor `serde_json`.
    - `rg -n 'build =|\[features\]|cmake|liquidfun-test-protocol|liquidfun-differential' crates/liquidfun/Cargo.toml` returns no matches.
  </acceptance_criteria>
  <done>Both private packages compile, the lockfile is Cargo-generated, and ordinary Cargo consumers still select only the unchanged native library.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Workspace dependency boundary | Private parsing/process crates must never enter the published `liquidfun` graph. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Required coverage | Disposition | Mitigation in this plan |
| --- | --- | --- | --- | --- |
| T-02-JSONL | Tampering | Untrusted JSONL input | mitigate | Keep all parsing dependencies private; Plan 03 adds the strict byte-to-domain gate. |
| T-02-RESOURCE | Denial of service | Resource exhaustion | mitigate | Keep this plan effect-light; Plan 02 defines immutable reviewed limits before allocation-heavy parsing or child execution. |
| T-02-PROCESS | Spoofing/Elevation | Path/process misuse | mitigate | Keep process code and arbitrary executable/config paths out of both crate roots. |
| T-02-OUTPUT | Tampering | Malformed child output | mitigate | Expose no raw-output acceptance path at this scaffold boundary. |
| T-02-STDERR | Information disclosure/DoS | Stderr retention | mitigate | Add no stderr retention or process-effect surface here. |
| T-02-ARTIFACT | Tampering/Repudiation | Artifact promotion/provenance | mitigate | Add no artifact write surface here. |
| T-02-ISOLATION | Elevation | Published-crate contamination | mitigate | Preserve the sole default member and prove the `liquidfun` dependency graph contains no harness, serde, C++, or build-script edge. |

No high-severity threat is accepted or transferred in this plan.
</threat_model>

<verification>
Run `cargo fmt --all`, then `cargo clippy -p liquidfun-test-protocol -p liquidfun-differential --all-targets --all-features -- -D warnings` and `cargo build -p liquidfun-test-protocol -p liquidfun-differential --all-targets --all-features`. Run `cargo build -p liquidfun`, `cargo test -p liquidfun`, and `cargo xtask package verify` to prove default-member and package isolation. Review `git diff -- Cargo.toml Cargo.lock crates/liquidfun-test-protocol crates/liquidfun-differential crates/liquidfun` and confirm no `crates/liquidfun` file changed.
</verification>

<success_criteria>
- Both private crates compile under workspace lints and remain unpublished/non-default.
- `liquidfun` remains Cargo-only, dependency-free, and unaffected by C++ or serialization tooling.
- The implementation follows the architecture, code-shape, testing, verification, and Rust standards loaded for planning.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-01-SUMMARY.md`.
</output>
