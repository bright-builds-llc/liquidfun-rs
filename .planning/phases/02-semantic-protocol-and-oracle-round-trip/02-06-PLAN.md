---
phase: 02-semantic-protocol-and-oracle-round-trip
plan: "06"
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - crates/liquidfun-differential/src/lib.rs
  - crates/liquidfun-differential/src/canonical.rs
  - crates/liquidfun-differential/src/comparator.rs
  - crates/liquidfun-differential/src/report.rs
  - crates/liquidfun-differential/src/minimizer.rs
  - crates/liquidfun-differential/tests/comparison.rs
  - crates/liquidfun-differential/tests/minimizer.rs
autonomous: true
requirements:
  - COMP-06
  - COMP-07
  - COMP-08
  - COMP-09
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-07-10T04-59-34
generated_at: 2026-07-10T05:24:31Z
must_haves:
  truths:
    - "Only two complete validated traces with compatible schema, scenario, tolerance, and provenance can reach semantic comparison."
    - "Discrete observables compare exactly, floating observables use exhaustive field-specific policies, and order is preserved unless a typed field explicitly declares set or multiset semantics."
    - "The primary report stops at the first divergent checkpoint/phase/path and yields a stable failure signature suitable for replay and reduction."
    - "A validity-preserving bounded reducer retains the same failure signature and persists the minimized scenario value rather than relying only on a seed."
  artifacts:
    - path: "crates/liquidfun-differential/src/comparator.rs"
      provides: "Pure exhaustive typed semantic comparison"
    - path: "crates/liquidfun-differential/src/canonical.rs"
      provides: "Explicit set/multiset canonicalization only"
    - path: "crates/liquidfun-differential/src/report.rs"
      provides: "Machine-readable first-divergence and concise human report"
    - path: "crates/liquidfun-differential/src/minimizer.rs"
      provides: "Deterministic same-signature hierarchical delta debugging"
  key_links:
    - from: "crates/liquidfun-differential/src/comparator.rs"
      to: "crates/liquidfun-test-protocol/src/tolerance.rs"
      via: "exhaustive match over typed field policies"
      pattern: "FloatPolicy"
    - from: "crates/liquidfun-differential/src/report.rs"
      to: "crates/liquidfun-differential/src/minimizer.rs"
      via: "stable FailureSignature identity"
      pattern: "FailureSignature"
---

<objective>
Build the pure comparison, canonicalization, first-divergence reporting, and scenario minimization core.

Purpose: Make physics mismatch a typed result of validated semantic comparison, never a fallback for protocol, process, provenance, or schema failure.
Output: Exhaustive comparator policies, constrained canonicalization, machine/human reports, stable failure signatures, bounded typed reduction, and synthetic regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-RESEARCH.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-04-SUMMARY.md
@crates/liquidfun-test-protocol/src/trace.rs
@crates/liquidfun-test-protocol/src/tolerance.rs
@crates/liquidfun-test-protocol/src/scenario.rs

<interfaces>
Use the validated types produced by Plan 03; never accept raw JSON or generic paths:

```rust
pub fn compare(
    expected: &ValidatedTrace,
    actual: &ValidatedTrace,
    policy: &ToleranceProfile,
) -> Result<DifferentialOutcome, HarnessFailure>;

pub enum DifferentialOutcome { Match, PhysicsMismatch(MismatchReport) }
pub enum MismatchKind { Missing, Unexpected, Exact, Numeric, Order, Multiplicity }
pub struct FailureSignature { checkpoint_id: CheckpointId, phase: PhaseName, semantic_path: SemanticPath, kind: MismatchKind }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exhaustive typed comparison and narrow canonicalization</name>
  <read_first>
    - `crates/liquidfun-differential/src/lib.rs` — runner library export surface.
    - `crates/liquidfun-test-protocol/src/trace.rs` — complete validated trace and typed observable variants.
    - `crates/liquidfun-test-protocol/src/tolerance.rs` — exact/numeric/collection policy authority.
    - `crates/liquidfun-test-protocol/src/float_bits.rs` — exact bits and diagnostic conversion.
    - `protocol/tolerances/phase2-v1.toml` — reviewed Phase-2 policy.
    - `CREATE: crates/liquidfun-differential/src/canonical.rs` — typed set/multiset handling.
    - `CREATE: crates/liquidfun-differential/src/comparator.rs` — pure comparator.
    - `CREATE: crates/liquidfun-differential/tests/comparison.rs` — focused policy tests.
  </read_first>
  <files>crates/liquidfun-differential/src/lib.rs, crates/liquidfun-differential/src/canonical.rs, crates/liquidfun-differential/src/comparator.rs, crates/liquidfun-differential/tests/comparison.rs</files>
  <behavior>
    - Incompatible protocol/scenario/trace/tolerance versions, scenario hashes, or engine identities return harness failure before observables are examined.
    - IDs, kinds, flags, counts, membership, record kinds, and Phase-2 world counts compare exactly.
    - ExactBits, Absolute, AbsoluteRelative, and Ulps policies handle ordinary values and thresholds deterministically.
    - NaN mismatches unless exact payload comparison is declared; infinities require matching sign; signed zero remains distinct unless explicitly reviewed otherwise.
    - Ordered values report order differences; only typed Set/Multiset values canonicalize by stable semantic key and deterministic tie-breaker, retaining multiplicity for multisets.
  </behavior>
  <action>
Per D-12 and D-13, write tests first and implement a pure comparator with exhaustive matches over every typed observable and field policy. Compatibility/provenance checks precede checkpoint comparison. Keep Phase-2 simulation time exact bits; exercise other numeric modes only through synthetic unit values without setting future physics tolerances. Put stable-key sorting only behind typed `Set`/`Multiset` wrappers. Never sort checkpoints, phases, callbacks, destruction events, or other ordered sequences. Do not use a generic epsilon, approximate-equality dependency, JSON path, stringly field name, or `serde_json::Value`. Return harness failure for invalid inputs and `PhysicsMismatch` only for two complete compatible traces.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-differential --test comparison -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - The focused test target covers every float and collection policy, threshold boundaries, special floats, exact discrete values, order, and multiplicity.
    - `rg -n 'serde_json::Value|epsilon|approx|sort_unstable' crates/liquidfun-differential/src/comparator.rs` returns no matches.
    - `rg -n 'Ordered|Set|Multiset' crates/liquidfun-differential/src/canonical.rs` shows explicit typed collection branches.
    - A provenance/schema incompatibility test asserts `HarnessFailure`, while a value difference between compatible traces asserts `PhysicsMismatch`.
  </acceptance_criteria>
  <done>The comparison core cannot silently widen numeric policy, erase order, or reinterpret a harness failure as physics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Produce stable first-divergence reports</name>
  <read_first>
    - `crates/liquidfun-differential/src/comparator.rs` — mismatch traversal and typed outcomes from Task 1.
    - `crates/liquidfun-differential/tests/comparison.rs` — policy test builders to extend.
    - `crates/liquidfun-test-protocol/src/ids.rs` — stable checkpoint/semantic IDs.
    - `crates/liquidfun-test-protocol/src/trace.rs` — ordered checkpoint and phase structure.
    - `CREATE: crates/liquidfun-differential/src/report.rs` — report/signature model and renderers.
  </read_first>
  <files>crates/liquidfun-differential/src/comparator.rs, crates/liquidfun-differential/src/report.rs, crates/liquidfun-differential/tests/comparison.rs</files>
  <behavior>
    - The primary mismatch is the earliest checkpoint ordinal, then named phase, then deterministic typed semantic path.
    - Reports include expected/actual exact bits and diagnostic decimals for floats, adjacent checkpoint IDs, mismatch kind, scenario/request hashes, and policy identity.
    - Machine rendering is deterministic and parseable; human rendering is concise and never replaces machine evidence.
    - Equivalent first divergences produce the same `FailureSignature`; later or different-kind mismatches do not.
  </behavior>
  <action>
Per D-14, implement typed semantic path segments and `FailureSignature`, then stop primary traversal at the first ordered divergence while retaining bounded neighboring context. Render a deterministic machine report and concise human report from the same typed structure. Distinguish `HarnessFailureReport` from `MismatchReport` at the type level. Include exact float bits plus derived decimal diagnostics; diagnostics never feed comparison. Ensure a newly added observable cannot compile without a semantic path and policy branch.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-differential --test comparison first_divergence -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Tests with multiple differences prove the report selects the first checkpoint/phase/path deterministically.
    - `rg -n 'FailureSignature|HarnessFailureReport|MismatchReport|semantic_path|expected_bits|actual_bits' crates/liquidfun-differential/src/report.rs` finds the separate typed report surfaces.
    - Running the deterministic machine-render test twice yields byte-identical expected output.
  </acceptance_criteria>
  <done>Every mismatch has one stable first-divergence signature usable for diagnosis, replay, and minimization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Minimize valid scenarios while preserving the failure signature</name>
  <read_first>
    - `crates/liquidfun-differential/src/report.rs` — `FailureSignature` equality authority from Task 2.
    - `crates/liquidfun-test-protocol/src/scenario.rs` — validated scenario constructors, references, and stable ordering.
    - `crates/liquidfun-test-protocol/src/limits.rs` — attempt/time/output budgets.
    - `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md` — D-15 is locked.
    - `CREATE: crates/liquidfun-differential/src/minimizer.rs` — pure deterministic reducer.
    - `CREATE: crates/liquidfun-differential/tests/minimizer.rs` — injected synthetic mismatch tests.
  </read_first>
  <files>crates/liquidfun-differential/src/lib.rs, crates/liquidfun-differential/src/minimizer.rs, crates/liquidfun-differential/tests/minimizer.rs</files>
  <behavior>
    - Candidate transforms remove checkpoint groups, steps/commands, and dependency-closed entity groups in stable hierarchical order.
    - Every candidate is revalidated before evaluation and retained only when it reproduces the exact original `FailureSignature`.
    - A different earlier divergence is rejected even if the candidate still mismatches.
    - Attempt and deadline budgets stop deterministically with an explicit incomplete status and best valid candidate.
    - Result contains canonical serialized scenario bytes plus original named/seeded source metadata; it is not seed-only.
  </behavior>
  <action>
Implement deterministic hierarchical delta debugging over `ValidatedScenario` values with an injected evaluator returning a typed signature. Use the smallest clear reducer: stable transform lists, revalidation, same-signature predicate, and named budgets. Do not add property-generation/RNG scope. Keep file I/O and artifact promotion out of this pure module; Plan 11 owns staging and reviewed writes. Test with synthetic ordered commands/checkpoints because the real empty-world Rust/C++ traces should match and must not require a fake checked-in physics failure.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-differential --test minimizer -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove successful reduction, invalid-reference candidate rejection, changed-signature rejection, deterministic transform order, attempt exhaustion, and deadline exhaustion.
    - `rg -n 'seed.*only|rand|proptest|unwrap\(' crates/liquidfun-differential/src/minimizer.rs` returns no matches.
    - The test asserts minimized canonical scenario bytes reparse to the same valid scenario and retain original source/seed metadata.
  </acceptance_criteria>
  <done>The reducer yields the smallest found valid serialized scenario with the same first-divergence signature under explicit budgets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Validated trace to comparator | Comparison may trust only protocol-validated, compatible, provenance-checked traces. |
| Policy to numeric decision | Reviewed typed policy controls whether exact, absolute/relative, or ULP comparison is permitted. |
| Evaluator to minimizer | Potentially expensive/failing evaluations are bounded and accepted only for the exact signature. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Required coverage | Disposition | Mitigation in this plan |
| --- | --- | --- | --- | --- |
| T-02-JSONL | Tampering | Untrusted JSONL input | mitigate | Public comparison accepts `ValidatedTrace`, not raw JSON; protocol parsing remains Plan 03's strict gate. |
| T-02-RESOURCE | Denial of service | Resource exhaustion | mitigate | Bound report context and reducer attempts/deadlines; operate within validated collection/trace limits. |
| T-02-PROCESS | Spoofing/Elevation | Path/process misuse | mitigate | Pure core launches no process and accepts no path; evaluator is injected as a typed function. |
| T-02-OUTPUT | Tampering | Malformed child output | mitigate | Malformed/incompatible traces cannot construct comparison input; provenance/schema errors remain harness failures. |
| T-02-STDERR | Information disclosure/DoS | Stderr retention | mitigate | Pure reports consume already-bounded failure evidence and never append unbounded stderr. |
| T-02-ARTIFACT | Tampering/Repudiation | Artifact promotion/provenance | mitigate | Reducer returns canonical bytes only and performs no write; Plan 11 replays, hashes, reviews, confines, and atomically promotes. |
| T-02-POLICY | Tampering | Over-broad comparison | mitigate | Exhaustive typed field policies, special-float rules, and canonicalization wrappers prevent global epsilon and global sorting. |

No high-severity threat is accepted.
</threat_model>

<verification>
Run `cargo fmt --all`, `cargo clippy -p liquidfun-differential --all-targets --all-features -- -D warnings`, `cargo build -p liquidfun-differential --all-targets --all-features`, and `cargo test -p liquidfun-differential --tests --all-features`. Review the comparator/canonicalizer for an exhaustive branch per typed observable, confirm the reducer performs no filesystem/process I/O, and run `cargo xtask package verify` to preserve consumer isolation.
</verification>

<success_criteria>
- Exact and field-specific numeric policies are exhaustively tested without global tolerance.
- Ordered semantic sequences remain ordered; only typed unordered collections canonicalize.
- First-divergence reports and signatures are deterministic and distinguish harness from physics outcomes.
- Synthetic failures minimize to valid serialized scenarios with the identical signature and bounded work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-06-SUMMARY.md`.
</output>
