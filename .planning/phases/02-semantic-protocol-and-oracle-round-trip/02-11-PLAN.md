---
phase: 02-semantic-protocol-and-oracle-round-trip
plan: "11"
type: execute
wave: 9
depends_on:
  - 02-10
files_modified:
  - crates/liquidfun-differential/src/lib.rs
  - crates/liquidfun-differential/src/main.rs
  - crates/liquidfun-differential/src/fixtures.rs
  - crates/liquidfun-differential/tests/fixture_workflow.rs
  - scenarios/regressions/README.md
autonomous: true
requirements:
  - COMP-05
  - COMP-08
  - COMP-09
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-07-10T04-59-34
generated_at: 2026-07-10T05:24:31Z
must_haves:
  truths:
    - "Reference generation stages outside tracked paths, replays and validates candidates, shows a deterministic diff, and atomically promotes only an explicitly reviewed candidate."
    - "Read-only checks never regenerate or rewrite tracked evidence."
  artifacts:
    - path: "crates/liquidfun-differential/src/fixtures.rs"
      provides: "Confined stage/replay/diff/review/atomic promotion workflow"
  key_links:
    - from: "crates/liquidfun-differential/src/fixtures.rs"
      to: "reference/artifacts/manifest.toml"
      via: "validated metadata and atomic promotion"
      pattern: "review_status"
---

<objective>
Add the confined staged fixture lifecycle for accepted traces and minimized regressions.

Purpose: Convert successful round trips and minimized failures into auditable evidence without permitting tests, untrusted paths, wrong identities, or unreviewed bytes to mutate accepted artifacts.
Output: Confined staging/replay/diff/review/promotion core, regression format guidance, and security-focused fixture lifecycle tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-RESEARCH.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-10-SUMMARY.md
@.planning/phases/01-oracle-provenance-and-repository-foundation/01-04-SUMMARY.md
@.planning/phases/01-oracle-provenance-and-repository-foundation/01-05-SUMMARY.md
@reference/upstream-lock.toml
@reference/artifacts/manifest.toml
@reference/source-map.toml
@tools/xtask/src/provenance.rs
@tools/xtask/src/main.rs
@justfile

<interfaces>
Tracked evidence is immutable under check/test/replay. Explicit generation writes candidates below `target/differential/staging`; promotion derives the destination from validated artifact kind/scenario ID, validates exact hashes and identities, requires explicit review metadata, and atomically renames within a confined filesystem boundary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement confined staged fixture generation and promotion</name>
  <read_first>
    - `crates/liquidfun-differential/src/main.rs` — existing allowlisted compare/replay/minimize dispatch.
    - `crates/liquidfun-differential/src/minimizer.rs` — minimized scenario/signature result.
    - `crates/liquidfun-differential/src/supervisor.rs` — complete trace and bounded failure evidence.
    - `crates/liquidfun-test-protocol/src/provenance.rs` — build identity/hash authority.
    - `reference/artifacts/manifest.toml` — existing Phase-1 reviewed-artifact model.
    - `.gitignore` — target output must remain ignored.
    - `CREATE: crates/liquidfun-differential/src/fixtures.rs` — staged artifact workflow.
    - `CREATE: crates/liquidfun-differential/tests/fixture_workflow.rs` — temp-repository security/replay tests.
    - `CREATE: scenarios/regressions/README.md` — exact serialized-regression format and review contract.
  </read_first>
  <files>crates/liquidfun-differential/src/lib.rs, crates/liquidfun-differential/src/main.rs, crates/liquidfun-differential/src/fixtures.rs, crates/liquidfun-differential/tests/fixture_workflow.rs, scenarios/regressions/README.md</files>
  <behavior>
    - Generate creates a new candidate only under canonical `target/differential/staging`, with request/trace/report/identity/stderr files and hashes; no tracked file changes.
    - Review replays candidate bytes through strict validation/comparison, verifies same signature for regressions, and renders a deterministic diff against any accepted artifact.
    - Promote rejects traversal, symlink, wrong root, overwrite race, missing replay, wrong hash/identity/schema/profile/scenario, dirty candidate, or absent explicit review metadata.
    - Successful promotion uses a destination derived from typed artifact kind/scenario ID, writes manifest metadata, fsyncs where supported, and atomically renames without partial accepted state.
    - Minimized regression promotion stores canonical scenario bytes and original source/seed/signature, never only a seed.
  </behavior>
  <action>
Per D-15/D-16, implement a typed `ArtifactCandidate` and `PromotionReceipt`. Confine staging and accepted directories by component-wise canonical/symlink checks; do not accept caller-provided destination paths. Use create-new temporary files, size limits, hashes, replay verification, deterministic diff, and same-signature validation. Promotion requires explicit reviewer identity/timestamp/status supplied as metadata (not inferred from successful generation), rejects an existing unexpected destination, and atomically renames a complete candidate/metadata unit. Tests/check/replay are read-only. Extend the CLI only with exact `fixture stage`, `fixture review`, and `fixture promote` subcommands using scenario/preset/profile/artifact IDs rather than paths. Document the regression value/source/signature contract without claiming a real physics mismatch exists in Phase 2.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-differential --test fixture_workflow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Temp-repository tests cover successful stage/replay/diff/promote and rejection of traversal, symlink, wrong identity/hash/signature, unreviewed, partial, oversized, and overwrite/race candidates.
    - `rg -n 'canonicalize|symlink_metadata|create_new|rename|review_status|FailureSignature' crates/liquidfun-differential/src/fixtures.rs` finds confinement, atomicity, review, and signature controls.
    - `git diff --exit-code -- reference scenarios` remains clean after all read-only fixture tests.
    - `scenarios/regressions/README.md` requires exact serialized scenario content, original source/seed, and first-divergence signature.
  </acceptance_criteria>
  <done>Artifact bytes can move from generated to accepted only through confined replay, deterministic review, full provenance, and atomic promotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Generated output to staged candidate | Untrusted child/request/report bytes become a bounded candidate only after validation and hashing. |
| Staged candidate to tracked artifact | Replay, identity, diff, explicit review, path confinement, and atomicity gate repository mutation. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Required coverage | Disposition | Mitigation in this plan |
| --- | --- | --- | --- | --- |
| T-02-JSONL | Tampering | Untrusted JSONL input | mitigate | Stage/replay reparses with strict protocol validation; invalid/duplicate/oversized bytes never become candidates or manifest records. |
| T-02-RESOURCE | Denial of service | Resource exhaustion | mitigate | Enforce candidate/trace/stderr/total/attempt/deadline limits before hashing, diffing, replaying, or writing. |
| T-02-PROCESS | Spoofing/Elevation | Path/process misuse | mitigate | xtask allowlists names/presets/profiles, uses structured invocations, and fixture code derives canonical roots/destinations with symlink/traversal rejection. |
| T-02-OUTPUT | Tampering | Malformed child output | mitigate | Only complete validated trace/failure bundles may stage; request/identity/hash/reset mismatch blocks review/promotion. |
| T-02-STDERR | Information disclosure/DoS | Stderr retention | mitigate | Persist only supervisor-bounded stderr windows with total/truncation metadata; manifest/report never accepts unbounded raw stderr. |
| T-02-ARTIFACT | Tampering/Repudiation | Artifact promotion/provenance | mitigate | Create-new staging, full hashes/identities/notices, replay, deterministic diff, explicit reviewer metadata, confined atomic rename, strict read-only provenance checks. |
| T-02-RACE | Tampering | Promotion race/partial state | mitigate | Reject existing destinations/unexpected inode changes and atomically rename complete validated bytes/metadata; tests cover overwrite/race attempts. |

No high-severity threat is left unmitigated; tracked mutation has the strongest gate in the phase.
</threat_model>

<verification>
Run focused fixture, provenance, and xtask command tests; then the required Rust sequence and full-workspace sequence. Build `oracle-debug`; run xtask one-shot, reuse, replay, staged review diff, and provenance check twice; assert tracked evidence is byte-identical after read-only commands. Run `cargo xtask package verify`, `just --list`, and `just check`, plus a Cargo-only temporary-copy `cargo xtask check`. Review the diff for no arbitrary path, shell string, silent retry/regeneration, unreviewed artifact, or `crates/liquidfun` change.
</verification>

<success_criteria>
- Minimized regression candidates must preserve validity, exact signature, serialized value, source/seed, and full identity before promotion.
- Tests/checks never rewrite evidence, and malicious paths/bytes/identities/review states fail closed.
- The lifecycle API is ready for Plan 12 to promote and validate the reviewed empty-world trace.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-11-SUMMARY.md`.
</output>
