---
phase: 02-semantic-protocol-and-oracle-round-trip
plan: "08"
type: execute
wave: 6
depends_on:
  - 02-05
  - 02-07
files_modified:
  - tools/reference/CMakeLists.txt
  - tools/reference/src/main.cpp
  - tools/reference/src/protocol.hpp
  - tools/reference/src/protocol.cpp
  - tools/reference/src/oracle_adapter.hpp
  - tools/reference/src/oracle_adapter.cpp
  - tools/reference/tests/protocol_tests.cpp
  - reference/source-map.toml
autonomous: true
requirements:
  - COMP-04
  - COMP-05
  - COMP-09
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-07-10T04-59-34
generated_at: 2026-07-10T05:24:31Z
must_haves:
  truths:
    - "The repository builds a process-only C++ oracle executable outside the read-only submodule and published crate."
    - "The C++ adapter accepts only the strict bounded empty-world protocol, emits semantic records only, flushes each record, and proves a complete reset between reused requests."
  artifacts:
    - path: "tools/reference/src/main.cpp"
      provides: "Protocol-only stdout process loop with stderr diagnostics"
    - path: "tools/reference/src/protocol.cpp"
      provides: "Strict bounded C++ JSONL parser/encoder"
    - path: "tools/reference/src/oracle_adapter.cpp"
      provides: "Fresh b2World empty-world execution and reset proof"
  key_links:
    - from: "tools/reference/CMakeLists.txt"
      to: "third_party/liquidfun/liquidfun/Box2D"
      via: "private link from liquidfun-reference to Box2D target"
      pattern: "liquidfun-reference"
    - from: "tools/reference/src/oracle_adapter.cpp"
      to: "crates/liquidfun-test-protocol protocol fixtures"
      via: "same version-1 semantic field contract"
      pattern: "reset_verified"
---

<objective>
Implement and test the strict process-isolated C++ empty-world oracle adapter with reset proof.

Purpose: Prove the permanent C++ side of the semantic seam without modifying upstream, exposing memory/layout, adding FFI, or leaking C++ into consumer Cargo.
Output: Strict C++ protocol/domain code, registered `liquidfun-reference` and test targets, and focused native tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-RESEARCH.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-05-SUMMARY.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-07-SUMMARY.md
@.planning/phases/01-oracle-provenance-and-repository-foundation/01-01-SUMMARY.md
@.planning/phases/01-oracle-provenance-and-repository-foundation/01-03-SUMMARY.md
@reference/upstream-lock.toml
@protocol/fixtures/accepted/empty-world-request.jsonl
@protocol/fixtures/rejected/duplicate-member.jsonl

<interfaces>
The C++ executable emits exactly one handshake at startup, then processes sequential complete scenario-request lines and emits `trace_begin`, ordered `checkpoint` records, and `trace_end`. No pointer, C++ index, class memory, exception, or STL object crosses the process boundary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the strict C++ protocol loop and empty-world adapter</name>
  <read_first>
    - `protocol/schemas/protocol-v1.schema.json` — reviewable transport spelling.
    - `protocol/schemas/scenario-v1.schema.json` — reviewable scenario spelling.
    - `protocol/schemas/trace-v1.schema.json` — reviewable trace spelling.
    - `protocol/fixtures/accepted/empty-world-request.jsonl` — cross-language accepted bytes.
    - `protocol/fixtures/rejected/duplicate-member.jsonl` — duplicate-key rejection contract.
    - `scenarios/phase-02/empty-world.json` — named scenario semantics.
    - `reference/upstream-lock.toml` — pinned oracle identity.
    - `tools/reference/vendor/nlohmann/json.hpp` — checksummed parser dependency from Plan 07.
    - `reference/source-map.toml` — derivation/alteration authority for upstream-informed local adapter files.
    - `tools/reference/CMakeLists.txt` — register executable and test targets before any target build is attempted.
    - `CREATE: tools/reference/src/main.cpp` — process loop and exception boundary.
    - `CREATE: tools/reference/src/protocol.hpp` — strict C++ domain interfaces.
    - `CREATE: tools/reference/src/protocol.cpp` — SAX/visitor decode and deterministic encode.
    - `CREATE: tools/reference/src/oracle_adapter.hpp` — empty-world adapter interface.
    - `CREATE: tools/reference/src/oracle_adapter.cpp` — b2World execution/reset.
    - `CREATE: tools/reference/tests/protocol_tests.cpp` — focused parser/sequence/reset tests.
  </read_first>
  <files>tools/reference/CMakeLists.txt, tools/reference/src/main.cpp, tools/reference/src/protocol.hpp, tools/reference/src/protocol.cpp, tools/reference/src/oracle_adapter.hpp, tools/reference/src/oracle_adapter.cpp, tools/reference/tests/protocol_tests.cpp, reference/source-map.toml</files>
  <behavior>
    - Accepted Rust fixture decodes and re-encodes with the same semantic values and exact float bits.
    - Invalid UTF-8, missing newline, duplicate/unknown members/kinds, unsupported versions, excess depth/string/collection counts, bad references, and nonempty Phase-2 entities fail to stderr/nonzero without protocol-looking success output.
    - Every request constructs a fresh `b2World`, executes ordered steps/checkpoints, emits exact zero counts/time bits, destroys world/mappings, verifies reset, increments epoch, and flushes each complete record.
    - Two distinguishable requests in one process produce isolated checkpoint counts and reset epochs 1 then 2.
  </behavior>
  <action>
Immediately after the existing `add_subdirectory(...)` declaration in `tools/reference/CMakeLists.txt`, register `liquidfun-reference` and `liquidfun-reference-protocol-tests`, compile repository adapter sources as C++17, link Box2D privately, and register the test with CTest before any target build is attempted. Then implement small C++17 domain structs mirroring protocol-v1 and a SAX/event parser that tracks duplicate member names per object and enforces bytes/depth/strings/collections while parsing; do not deserialize untrusted input into an unrestricted mutable JSON tree. Validate cross-field invariants before constructing `b2World`. Keep one request in flight, semantic IDs request-scoped, stdout protocol-only, and diagnostics on stderr. Buffer each record to a bounded string, append exactly one newline, write, and flush. Catch `std::exception` only at `main`, report to stderr, and exit nonzero. Never serialize pointers, dense indices, raw object bytes, or private layout. Add source-map records for local files derived from pinned API behavior with alteration/notice classification.
  </action>
  <verify>
    <automated>cd tools/reference &amp;&amp; cmake --preset oracle-debug &amp;&amp; cmake --build --preset oracle-debug --target liquidfun-reference-protocol-tests &amp;&amp; ctest --test-dir ../../target/reference/oracle-debug --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - C++ tests exercise the accepted fixture, every rejected fixture class, exact bit round-trip, two-request reset isolation, and protocol-only stdout.
    - `rg -n 'reinterpret_cast|memcpy|void\s*\*|printf\(' tools/reference/src` returns no protocol/raw-layout shortcuts.
    - `rg -n 'std::cout.*(error|diagnostic)|cerr.*(trace_begin|checkpoint|trace_end)' tools/reference/src` returns no stream mixing.
    - `git -C third_party/liquidfun status --short` remains empty.
  </acceptance_criteria>
  <done>The C++ process is a strict semantic adapter over a fresh empty world, not a serialization or binding surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Stdin JSONL to C++ domain structs | Untrusted child input crosses into native code and upstream API calls. |
| C++ process stdout/stderr | Machine protocol and bounded diagnostics must remain distinct for the supervisor. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Required coverage | Disposition | Mitigation in this plan |
| --- | --- | --- | --- | --- |
| T-02-JSONL | Tampering | Untrusted JSONL input | mitigate | SAX/event duplicate detection, strict kinds/fields/versions, typed validation, UTF-8/newline checks, and no unrestricted deep tree. |
| T-02-RESOURCE | Denial of service | Resource exhaustion | mitigate | Enforce the shared record/depth/string/collection/output bounds before and during allocation/emission. |
| T-02-PROCESS | Spoofing/Elevation | Path/process misuse | mitigate | CMake registers fixed repository-owned targets and links only the pinned out-of-tree Box2D target; Plan 10 owns confined supervisor launch and executable-path resolution. |
| T-02-OUTPUT | Tampering | Malformed child output | mitigate | Deterministic complete newline records, flush-after-record, exact sequence/IDs/hashes/reset proof, exceptions to stderr/nonzero. |
| T-02-STDERR | Information disclosure/DoS | Stderr retention | mitigate | Adapter emits diagnostics only to stderr and never mirrors raw request secrets; Plan 10 drains/caps it concurrently. |
| T-02-ARTIFACT | Tampering/Repudiation | Artifact promotion/provenance | mitigate | Source-map and vendor hashes/notices are checked in; immutable build identity is added in Plan 09. |
| T-02-NATIVE | Elevation/DoS | Native parser or adapter fault | mitigate | Native focused tests reject malformed input and require nonzero termination without protocol-looking success output. |

No high-severity threat is accepted; native parsing and process identity are directly mitigated.
</threat_model>

<verification>
Checksum the vendored files, configure `oracle-debug`, build `liquidfun-reference-protocol-tests`, and run CTest. Verify `git -C third_party/liquidfun status --short` is empty and run `cargo xtask package verify`. Plan 09 owns immutable build identity, allowlisted xtask integration, and sanitizer configuration.
</verification>

<success_criteria>
- The same strict protocol fixtures are accepted/rejected in C++ with bounded parsing and no raw memory/layout exposure.
- One process handles two requests with reset epochs 1 and 2 and no state leakage.
- The registered debug executable and native test targets build outside the submodule while the published/default Cargo path stays unchanged.
- Immutable identity and fail-fast sanitizer behavior remain explicitly owned by Plan 09.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-08-SUMMARY.md`.
</output>
