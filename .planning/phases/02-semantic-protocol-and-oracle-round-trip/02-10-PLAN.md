---
phase: 02-semantic-protocol-and-oracle-round-trip
plan: "10"
type: execute
wave: 8
depends_on:
  - 02-05
  - 02-06
  - 02-09
files_modified:
  - crates/liquidfun-differential/src/lib.rs
  - crates/liquidfun-differential/src/main.rs
  - crates/liquidfun-differential/src/rust_adapter.rs
  - crates/liquidfun-differential/src/supervisor.rs
  - crates/liquidfun-differential/src/supervisor/stdio.rs
  - crates/liquidfun-differential/tests/rust_adapter.rs
  - crates/liquidfun-differential/tests/supervisor_failures.rs
  - crates/liquidfun-differential/tests/round_trip.rs
  - crates/liquidfun-differential/tests/fixtures/fake_oracle.rs
autonomous: true
requirements:
  - COMP-04
  - COMP-05
  - COMP-08
  - COMP-09
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-07-10T04-59-34
generated_at: 2026-07-10T05:24:31Z
must_haves:
  truths:
    - "The same validated empty-world scenario produces complete semantic traces through a private native Rust adapter and a separately spawned pinned C++ oracle."
    - "One supervisor configuration supports both one-shot isolation and finite bounded reuse with exactly one sequential request in flight."
    - "Timeouts, exits/signals, sanitizer evidence, EOF/partial/malformed/oversized output, sequence/request/identity/provenance mismatch, and reset failure poison, kill, wait/reap, and never reuse a child."
    - "Stdout and stderr are drained concurrently; retained stderr and total output are bounded without allowing a pipe deadlock."
  artifacts:
    - path: "crates/liquidfun-differential/src/rust_adapter.rs"
      provides: "Private native Rust empty-world trace adapter"
    - path: "crates/liquidfun-differential/src/supervisor.rs"
      provides: "One-shot/bounded-reuse child state machine and failure taxonomy"
    - path: "crates/liquidfun-differential/src/supervisor/stdio.rs"
      provides: "Concurrent bounded stdout/stderr drains"
    - path: "crates/liquidfun-differential/src/main.rs"
      provides: "Thin compare/replay/minimize command dispatch over library APIs"
  key_links:
    - from: "crates/liquidfun-differential/src/supervisor.rs"
      to: "target/reference/<preset>/liquidfun-reference"
      via: "confined allowlisted OracleExecutable resolved from preset"
      pattern: "OracleExecutable"
    - from: "crates/liquidfun-differential/src/rust_adapter.rs"
      to: "crates/liquidfun-test-protocol/src/scenario.rs"
      via: "same ValidatedScenario consumed by C++"
      pattern: "ValidatedScenario"
    - from: "crates/liquidfun-differential/src/supervisor.rs"
      to: "crates/liquidfun-test-protocol/src/trace.rs"
      via: "incremental TraceValidator before comparison"
      pattern: "TraceValidator"
---

<objective>
Complete the private Rust adapter and process supervisor, then prove the real one-shot and bounded-reuse Rust/C++ empty-world round trip.

Purpose: Turn the typed protocol and C++ executable into one trustworthy isolated differential execution path with strict failures and no premature public engine model.
Output: Native empty-world adapter, hardened sequential child supervisor, failure-injection suite, thin differential CLI, and real compare/replay tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-RESEARCH.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-05-SUMMARY.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-06-SUMMARY.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-09-SUMMARY.md
@reference/upstream-lock.toml
@scenarios/phase-02/empty-world.json
@tools/reference/CMakePresets.json

<interfaces>
Use the validated scenario/trace types and pure comparator from Plans 02/03. Model the session as an enum with legal transitions only:

```text
Starting -> Handshaking -> Ready -> InFlight -> Ready
                         \-> Poisoned -> Reaped
                         \-> Exited
```

Use one supervisor API with `SessionProfile::OneShot`, `SessionProfile::Reuse`, and `SessionProfile::Sanitizer`, each resolving to a reviewed named `HarnessLimits`/request budget. Exactly one request may be active.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute the bounded scenario through a private native Rust adapter</name>
  <read_first>
    - `crates/liquidfun-differential/src/lib.rs` — existing comparator/minimizer exports and new adapter registration.
    - `crates/liquidfun-test-protocol/src/scenario.rs` — validated commands/checkpoints.
    - `crates/liquidfun-test-protocol/src/trace.rs` — trace construction and reset proof.
    - `crates/liquidfun-test-protocol/src/provenance.rs` — native Rust build identity spelling.
    - `crates/liquidfun/src/lib.rs` — published scaffold boundary that must not gain a public World/object model.
    - `CREATE: crates/liquidfun-differential/src/rust_adapter.rs` — private Phase-2 executor.
    - `CREATE: crates/liquidfun-differential/tests/rust_adapter.rs` — deterministic adapter tests.
  </read_first>
  <files>crates/liquidfun-differential/src/lib.rs, crates/liquidfun-differential/src/rust_adapter.rs, crates/liquidfun-differential/tests/rust_adapter.rs</files>
  <behavior>
    - The named empty-world scenario emits begin, exact ordered checkpoints, and end with exact zero counts and deterministic simulation-time bits.
    - Native identity uses `engine_kind = native_rust`, current crate/build identity, scenario/tolerance hashes, and the selected comparison-oracle revision without pretending Rust was built from upstream.
    - Invalid or nonempty Phase-2 scenarios cannot enter execution; arithmetic/time accumulation follows exact scenario bit operations defined for this slice.
    - Per-request state is destroyed and reset-proven; two executions produce independent epoch/state evidence.
  </behavior>
  <action>
Implement the smallest private `EmptyWorldAdapter` required for protocol proof. It may track only gravity, accumulated simulation time, ordered step/checkpoint execution, and always-zero semantic counts. It must consume `ValidatedScenario` and emit typed validated trace records through shared constructors. Keep it entirely inside the unpublished differential crate; do not modify `crates/liquidfun`, define public handles, callbacks, storage, bodies, fixtures, or broad physics. Use explicit bit conversions and typed errors, no `unwrap()`.

Simplification choice: the adapter proves the permanent harness seam while deliberately avoiding a disposable public `World` design that Phase 3 must decide from identity/invalidation evidence.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-differential --test rust_adapter -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Tests run the checked-in scenario twice and assert exact ordered checkpoint IDs, zero counts, time bits, identity/hash fields, and reset evidence.
    - `git diff --exit-code -- crates/liquidfun` succeeds.
    - `rg -n 'pub struct (World|Body|Fixture)|unsafe|unwrap\(' crates/liquidfun-differential/src/rust_adapter.rs` returns no matches.
  </acceptance_criteria>
  <done>The private Rust side produces a complete protocol trace without preempting Phase-3 engine API/storage design.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Supervise one-shot and bounded-reuse child sessions safely</name>
  <read_first>
    - `crates/liquidfun-test-protocol/src/codec.rs` — bounded stdout record framing.
    - `crates/liquidfun-test-protocol/src/trace.rs` — handshake and incremental sequence validation.
    - `crates/liquidfun-test-protocol/src/limits.rs` — output/stderr/deadline/request profiles.
    - `crates/liquidfun-test-protocol/src/failure.rs` — exhaustive failure evidence.
    - `reference/upstream-lock.toml` — expected oracle revision.
    - `tools/reference/CMakePresets.json` — allowlisted preset/output paths.
    - `CREATE: crates/liquidfun-differential/src/supervisor.rs` — session state/lifecycle.
    - `CREATE: crates/liquidfun-differential/src/supervisor/stdio.rs` — bounded concurrent drain workers.
    - `CREATE: crates/liquidfun-differential/tests/supervisor_failures.rs` — failure taxonomy tests.
    - `CREATE: crates/liquidfun-differential/tests/fixtures/fake_oracle.rs` — injectable child behaviors.
  </read_first>
  <files>crates/liquidfun-differential/src/lib.rs, crates/liquidfun-differential/src/supervisor.rs, crates/liquidfun-differential/src/supervisor/stdio.rs, crates/liquidfun-differential/tests/supervisor_failures.rs, crates/liquidfun-differential/tests/fixtures/fake_oracle.rs</files>
  <behavior>
    - One-shot budget spawns/handshakes/runs/reaps one request; reuse handles exactly the finite budget, cycles afterward, and never has two requests in flight.
    - Stdout and stderr workers start immediately and drain concurrently; stderr retains bounded first/last windows plus total/truncation counts while continuing to drain.
    - Startup/request timeout, nonzero/signal, sanitizer marker, EOF/partial/malformed/oversized output, total overflow, request/identity/provenance/sequence mismatch, and reset failure return their exact typed kinds.
    - Every poisoned path closes stdin, kills if needed, waits/reaps, joins drain workers, and preserves request, identity, exit, elapsed, last-record, and bounded stderr evidence; no poisoned child is reused or silently retried.
    - Executable resolution accepts only an allowlisted preset and a regular non-symlink executable confined to canonical `target/reference/&lt;preset&gt;`.
  </behavior>
  <action>
Implement the explicit session enum and one controlling thread using `std::process::Command`, `std::thread`, channels, and deadlines; do not add Tokio. Resolve the executable from repository root plus an allowlisted preset, canonicalize each existing component, reject traversal/symlinks/non-files/out-of-tree paths, and use structured `Command` arguments without a shell. Start stdout/stderr drains before handshake, enforce record/trace/total limits incrementally, and inspect recognized ASan/UBSan markers even if exit status is zero. On any poison condition, preserve evidence before continuation, then kill/wait/reap and join; deterministic requests are never auto-retried. Prove periodic cycling and reset epochs in reuse mode. Test all branches with a separately compiled fake child fixture, including large stderr concurrent with valid stdout to detect deadlocks.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-differential --test supervisor_failures -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Named tests cover every D-10 failure class, one-shot, finite reuse/cycling, reset leakage, kill/reap/join evidence, stderr truncation, and pipe-deadlock pressure.
    - `rg -n 'Command::new|Stdio::piped|thread::spawn|kill\(|wait\(|join\(' crates/liquidfun-differential/src/supervisor.rs crates/liquidfun-differential/src/supervisor/stdio.rs` finds structured lifecycle primitives.
    - `rg -n 'sh -c|bash -c|Command::new\([^)]*(args|path)' crates/liquidfun-differential/src/supervisor.rs` returns no shell/arbitrary-path launch.
    - Tests assert `was_killed`, `was_reaped`, bounded retained bytes, total stderr bytes, and truncation count on poisoned sessions.
  </acceptance_criteria>
  <done>The single supervisor safely supports focused one-shot and finite sequential reuse while failing closed with preserved bounded evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove compare, replay, and real two-request round trips</name>
  <read_first>
    - `crates/liquidfun-differential/src/rust_adapter.rs` — native trace source from Task 1.
    - `crates/liquidfun-differential/src/supervisor.rs` — C++ trace source from Task 2.
    - `crates/liquidfun-differential/src/comparator.rs` — typed comparison core from Plan 06.
    - `crates/liquidfun-differential/src/report.rs` — machine/human outcome rendering.
    - `crates/liquidfun-differential/src/minimizer.rs` — same-signature reducer API.
    - `scenarios/phase-02/empty-world.json` — shared scenario.
    - `CREATE: crates/liquidfun-differential/src/main.rs` — thin CLI dispatch.
    - `CREATE: crates/liquidfun-differential/tests/round_trip.rs` — real/debug oracle integration tests.
  </read_first>
  <files>crates/liquidfun-differential/src/lib.rs, crates/liquidfun-differential/src/main.rs, crates/liquidfun-differential/tests/round_trip.rs</files>
  <behavior>
    - `compare --scenario empty-world --preset oracle-debug --session-profile one-shot` yields a semantic Match only after both traces validate.
    - Reuse mode sends two distinguishable requests through one child, validates epochs 1/2 and no checkpoint/state leakage, then reports both matches.
    - `replay` accepts only a checked-in scenario name or exact serialized validated scenario plus seed/source metadata and reproduces the same outcome/signature.
    - Harness failures and physics mismatches have distinct machine result kinds and exit codes.
  </behavior>
  <action>
Create a thin binary dispatching `compare`, `replay`, and `minimize` into library functions with exact allowlisted subcommands/preset/session profiles. Scenario name lookup maps stable names to confined checked-in paths; exact-value replay reads a single validated record and never accepts an output/promote path. The command must emit a deterministic machine report and concise stderr/human summary without confusing them with child protocol stdout. Add real integration tests gated by an explicit repository/oracle environment check so ordinary full-workspace tests compile and skip with a labeled prerequisite rather than initiating CMake. Run real debug one-shot and two-request reuse locally after explicit xtask configure/build.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-differential --test round_trip -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - With `oracle-debug` built, the real test produces Match in one-shot and two-request reuse and validates reset epochs 1/2.
    - Without the submodule/executable, full-workspace tests do not spawn CMake/C++ and report the explicit integration prerequisite.
    - `rg -n 'compare|replay|minimize|one-shot|reuse' crates/liquidfun-differential/src/main.rs` finds allowlisted command/profile spelling.
    - A fake malformed child produces a harness-failure result/exit code; a synthetic compatible value divergence produces a physics-mismatch result/exit code.
  </acceptance_criteria>
  <done>The permanent private harness seam produces a validated semantic Match through both engines and reproduces classified failures deterministically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Repository scenario to native adapter | Validated engine-neutral commands drive a private Rust implementation. |
| Parent to child process | A confined executable receives one bounded request and returns untrusted stdout/stderr. |
| Validated traces to comparator | Only complete compatible Rust/C++ traces may become match/mismatch outcomes. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Required coverage | Disposition | Mitigation in this plan |
| --- | --- | --- | --- | --- |
| T-02-JSONL | Tampering | Untrusted JSONL input | mitigate | Reuse Plan-02 strict codec/domain validation for requests and incremental child records; no raw JSON reaches adapters/comparator. |
| T-02-RESOURCE | Denial of service | Resource exhaustion | mitigate | Named record/trace/total/stderr/deadline/request budgets enforced during I/O; finite reuse cycles children. |
| T-02-PROCESS | Spoofing/Elevation | Path/process misuse | mitigate | Allowlisted preset, canonical target confinement, symlink/non-file rejection, structured `Command`, one child/request state machine, no shell. |
| T-02-OUTPUT | Tampering | Malformed child output | mitigate | Concurrent incremental parser checks newline, size, sequence, request/identity/provenance/hash/reset; invalid output poisons and reaps. |
| T-02-STDERR | Information disclosure/DoS | Stderr retention | mitigate | Always drain; retain bounded first/last window with total/truncation count; include only bounded evidence in reports/artifacts. |
| T-02-ARTIFACT | Tampering/Repudiation | Artifact promotion/provenance | mitigate | This plan writes failures only beneath gitignored target paths with hashes/identity; tracked promotion remains Plan 11's replay/review/atomic path. |
| T-02-RETRY | Repudiation | Silent deterministic retry | mitigate | No automatic retry; every poison records exact request/signature/identity/exit before independent continuation. |

All high-severity process and resource threats are explicitly mitigated and tested.
</threat_model>

<verification>
Run the Rust requirements in order: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`; then workspace variants and docs. Run focused protocol/comparator/supervisor tests, `cargo xtask upstream verify`, configure/build `oracle-debug`, execute one-shot compare, two-request reuse compare, and replay. Configure/build `oracle-asan-ubsan` and run the one-shot/reset corpus with fail-fast variables. Finish with `cargo xtask package verify` and inspect `git diff -- crates/liquidfun` for no changes.
</verification>

<success_criteria>
- Native Rust and process-isolated C++ consume the same validated scenario and produce provenance-checked semantic traces.
- One-shot and bounded reuse are configurations of one sequential supervisor, with proven reset and periodic cycling.
- Every failure taxonomy branch is tested, preserves bounded evidence, poisons/reaps, and remains distinct from physics mismatch.
- Default Cargo/workspace tests never build or start C++ implicitly, and `liquidfun` remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-10-SUMMARY.md`.
</output>
