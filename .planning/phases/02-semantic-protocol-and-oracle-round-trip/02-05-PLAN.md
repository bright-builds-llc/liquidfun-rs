---
phase: 02-semantic-protocol-and-oracle-round-trip
plan: "05"
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - protocol/fixtures/accepted/empty-world-request.jsonl
  - protocol/fixtures/accepted/empty-world-trace.jsonl
  - protocol/fixtures/rejected/duplicate-member.jsonl
  - protocol/fixtures/rejected/unknown-record-kind.jsonl
  - protocol/fixtures/rejected/partial-record.jsonl
  - protocol/fixtures/rejected/unsupported-version.jsonl
  - protocol/fixtures/rejected/oversized-id.jsonl
  - scenarios/phase-02/empty-world.json
  - crates/liquidfun-test-protocol/tests/fixtures.rs
autonomous: true
requirements:
  - COMP-03
  - COMP-05
  - COMP-09
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-07-10T04-59-34
generated_at: 2026-07-10T05:24:31Z
must_haves:
  truths:
    - "A contributor can load the permanent bounded empty-world scenario with ordered steps and unique checkpoint requests."
    - "Accepted and rejected protocol fixtures deterministically exercise the typed validators and schema presentation."
    - "Fixture checks decode, classify, and canonicalize in memory without rewriting tracked files."
  artifacts:
    - path: "scenarios/phase-02/empty-world.json"
      provides: "Permanent bounded empty-world scenario shared by both engines"
    - path: "protocol/fixtures/accepted/empty-world-request.jsonl"
      provides: "Canonical request record for the named scenario"
    - path: "protocol/fixtures/accepted/empty-world-trace.jsonl"
      provides: "Representative complete reset-proven trace stream"
    - path: "crates/liquidfun-test-protocol/tests/fixtures.rs"
      provides: "Owned read-only fixture corpus contract tests"
  key_links:
    - from: "scenarios/phase-02/empty-world.json"
      to: "crates/liquidfun-test-protocol/src/scenario.rs"
      via: "strict named-scenario decoding and validation"
      pattern: "scenario_id"
    - from: "protocol/fixtures/accepted/empty-world-trace.jsonl"
      to: "crates/liquidfun-test-protocol/src/trace.rs"
      via: "complete handshake/begin/checkpoint/end validation"
      pattern: "reset_verified"
    - from: "crates/liquidfun-test-protocol/tests/fixtures.rs"
      to: "protocol/fixtures"
      via: "exact accepted and rejected corpus paths"
      pattern: "empty-world-request"
---

<objective>
Add the permanent empty-world scenario and representative accepted/rejected protocol fixtures.

Purpose: Turn the typed protocol seam into reviewable, deterministic regression inputs shared by Rust and C++ adapters.
Output: One named scenario, two accepted fixtures, five classified rejection fixtures, and clean read-only checks.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-04-SUMMARY.md
@crates/liquidfun-test-protocol/src/codec.rs
@crates/liquidfun-test-protocol/src/scenario.rs
@crates/liquidfun-test-protocol/src/trace.rs
@crates/liquidfun-test-protocol/src/schema.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Check the named scenario and protocol fixture corpus</name>
  <read_first>
    - `crates/liquidfun-test-protocol/src/codec.rs` — strict framing and duplicate/unknown/partial rejection authority.
    - `crates/liquidfun-test-protocol/src/scenario.rs` — cross-field scenario validator.
    - `crates/liquidfun-test-protocol/src/trace.rs` — streamed trace state validator.
    - `protocol/schemas/*.schema.json` — presentation shapes created by Plan 04.
    - `CREATE: scenarios/phase-02/empty-world.json` — named bounded scenario value.
    - `CREATE: protocol/fixtures/accepted/empty-world-request.jsonl` — accepted request bytes.
    - `CREATE: protocol/fixtures/accepted/empty-world-trace.jsonl` — accepted complete stream bytes.
    - `CREATE: protocol/fixtures/rejected/duplicate-member.jsonl` — duplicate-key rejection.
    - `CREATE: protocol/fixtures/rejected/unknown-record-kind.jsonl` — tagged-kind rejection.
    - `CREATE: protocol/fixtures/rejected/partial-record.jsonl` — missing-newline rejection.
    - `CREATE: protocol/fixtures/rejected/unsupported-version.jsonl` — version rejection.
    - `CREATE: protocol/fixtures/rejected/oversized-id.jsonl` — typed string-limit rejection.
    - `CREATE: crates/liquidfun-test-protocol/tests/fixtures.rs` — focused integration tests that own every checked-in fixture path.
  </read_first>
  <files>protocol/fixtures/accepted/empty-world-request.jsonl, protocol/fixtures/accepted/empty-world-trace.jsonl, protocol/fixtures/rejected/duplicate-member.jsonl, protocol/fixtures/rejected/unknown-record-kind.jsonl, protocol/fixtures/rejected/partial-record.jsonl, protocol/fixtures/rejected/unsupported-version.jsonl, protocol/fixtures/rejected/oversized-id.jsonl, scenarios/phase-02/empty-world.json, crates/liquidfun-test-protocol/tests/fixtures.rs</files>
  <behavior>
    - The named scenario has an empty entity list, exact gravity/timestep bit fields, bounded solver settings, at least two distinguishable step/checkpoint boundaries, and requested world-count/simulation-time observables.
    - The accepted request and trace decode to the same scenario/request/identity hashes and re-encode byte-identically.
    - Each rejection fixture maps to its intended duplicate, unknown-kind, partial, unsupported-version, or oversized-ID harness category.
    - Fixture bytes remain newline-complete where the case is not intentionally partial.
  </behavior>
  <action>
Author deterministic canonical JSON/JSONL fixtures from the already-implemented types and presentations. Keep authoritative floats as numeric `u32` bit fields. Make the complete trace include handshake, begin, ordered checkpoints, end, matching identity/payload counts and hashes, a reset epoch, and `reset_verified = true`. Create the owned `crates/liquidfun-test-protocol/tests/fixtures.rs` integration target to consume every tracked corpus path through the public protocol validation surface, assert exact categories, and compare canonical in-memory re-encoding. Do not modify unlisted Rust source files. Checks never rewrite the corpus.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-test-protocol fixtures -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Every listed accepted/rejected fixture is asserted by a focused Arrange/Act/Assert test with its expected typed category.
    - `rg -n 'gravity_.*_bits|timestep_bits' scenarios/phase-02/empty-world.json` finds exact bit-pattern fields and no decimal authority fields.
    - `rg -n 'trace_begin|checkpoint|trace_end|reset_verified|reset_epoch|identity_sha256' protocol/fixtures/accepted/empty-world-trace.jsonl` finds all required state fields.
    - Running the fixture test twice leaves `git diff --exit-code -- protocol/fixtures scenarios/phase-02/empty-world.json` clean.
  </acceptance_criteria>
  <done>The checked-in scenario and fixture corpus is canonical, classified, bounded, deterministic, and read-only under verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Checked-in fixture bytes to typed records | Human-reviewable inputs remain untrusted until strict protocol validation succeeds. |
| Named scenario to adapter input | Stable source metadata, hashes, ordering, and bounds must agree before either engine executes it. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Required coverage | Disposition | Mitigation in this plan |
| --- | --- | --- | --- | --- |
| T-02-JSONL | Tampering | Fixture drift or malformed records | mitigate | Decode through the strict codec and assert one typed category for each rejected file. |
| T-02-RESOURCE | Denial of service | Oversized fixture fields | mitigate | Include a boundary rejection fixture and enforce the named profile before execution. |
| T-02-OUTPUT | Tampering | Successful-looking incomplete traces | mitigate | Accepted trace requires exact sequence, matching hashes/counts/identity, newline completion, and reset proof. |
| T-02-ARTIFACT | Tampering/Repudiation | Silent fixture regeneration | mitigate | Canonicalization occurs in memory and two consecutive checks must leave tracked paths unchanged. |
| T-02-ISOLATION | Elevation | Runtime-oracle leakage | mitigate | Fixtures are private test data and do not change the published crate or consumer dependency graph. |

No high-severity threat is accepted.
</threat_model>

<verification>
Run `cargo fmt --all`, `cargo clippy -p liquidfun-test-protocol --all-targets --all-features -- -D warnings`, `cargo build -p liquidfun-test-protocol --all-targets --all-features`, and `cargo test -p liquidfun-test-protocol --all-features`. Run the fixture test twice, assert a clean diff for `protocol/fixtures` and `scenarios/phase-02`, and run `cargo xtask package verify` to prove the corpus stays outside the published archive.
</verification>

<success_criteria>
- The permanent empty-world scenario and accepted stream are deterministic, exact-bit, and reset-proven.
- Every rejected fixture maps to a distinct typed failure category.
- Verification is read-only and leaves tracked scenario/fixture bytes unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-05-SUMMARY.md`.
</output>
