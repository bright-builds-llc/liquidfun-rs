---
phase: 02-semantic-protocol-and-oracle-round-trip
plan: "09"
type: execute
wave: 7
depends_on:
  - 02-08
files_modified:
  - tools/reference/CMakeLists.txt
  - tools/reference/CMakePresets.json
  - tools/reference/src/build_identity.hpp.in
  - tools/xtask/src/upstream.rs
  - tools/xtask/tests/upstream_cli.rs
  - tools/xtask/tests/fixtures/fake_upstream_tool.rs
autonomous: true
requirements:
  - COMP-04
  - COMP-05
  - COMP-09
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-07-10T04-59-34
generated_at: 2026-07-10T05:24:31Z
must_haves:
  truths:
    - "The child startup identity binds the pinned oracle, adapter digest, preset, compiler, target, flags, and sanitizer mode before requests are accepted."
    - "xtask configures and builds only allowlisted presets and the registered liquidfun-reference target with structured arguments."
    - "Sanitizer builds fail fast and cannot emit a successful-looking trace after undefined behavior."
  artifacts:
    - path: "tools/reference/src/build_identity.hpp.in"
      provides: "Configured immutable handshake provenance"
    - path: "tools/xtask/src/upstream.rs"
      provides: "Allowlisted identity-bound reference configure/build shell"
    - path: "tools/reference/CMakePresets.json"
      provides: "Out-of-tree debug/release/fail-fast sanitizer configurations"
  key_links:
    - from: "tools/xtask/src/upstream.rs"
      to: "tools/reference/CMakeLists.txt"
      via: "structured revision/digest arguments and liquidfun-reference target"
      pattern: "liquidfun-reference"
    - from: "tools/reference/CMakeLists.txt"
      to: "tools/reference/src/build_identity.hpp.in"
      via: "configured immutable build fields"
      pattern: "build_identity"
---

<objective>
Bind immutable build identity, fail-fast sanitizer settings, and allowlisted xtask orchestration to the registered native oracle targets.

Purpose: Make every child handshake reproducibly attributable without mixing process orchestration into native protocol implementation.
Output: Configured identity header, exact CMake preset controls, structured xtask arguments, and command-level tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-08-SUMMARY.md
@reference/upstream-lock.toml
@tools/reference/CMakeLists.txt
@tools/reference/CMakePresets.json
@tools/xtask/src/upstream.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind immutable identity and allowlisted reference builds</name>
  <read_first>
    - `tools/reference/CMakeLists.txt` — registered executable/test targets from Plan 08.
    - `tools/reference/CMakePresets.json` — debug/release/sanitizer preset authority.
    - `tools/xtask/src/upstream.rs` — existing verified allowlisted configure/build shell.
    - `tools/xtask/tests/upstream_cli.rs` — command-level fixture pattern.
    - `tools/xtask/tests/fixtures/fake_upstream_tool.rs` — structured fake CMake behavior.
    - `reference/upstream-lock.toml` — revision passed independently into CMake.
    - `CREATE: tools/reference/src/build_identity.hpp.in` — configured immutable handshake fields.
  </read_first>
  <files>tools/reference/CMakeLists.txt, tools/reference/CMakePresets.json, tools/reference/src/build_identity.hpp.in, tools/xtask/src/upstream.rs, tools/xtask/tests/upstream_cli.rs, tools/xtask/tests/fixtures/fake_upstream_tool.rs</files>
  <behavior>
    - The configured identity includes oracle revision, adapter digest, preset, compiler ID/full version, target/system/architecture, build type, effective flags, and sanitizer mode.
    - xtask passes expected revision/digest as separate `-D` arguments, builds `liquidfun-reference`, and rejects unknown presets or extra path inputs.
    - Generated headers and binaries remain under `target/reference/<preset>` and never enter the submodule.
    - ASan/UBSan settings abort/halt and disable undefined-behavior recovery.
  </behavior>
  <action>
Compute a deterministic adapter source digest from checked-in adapter sources and configure the identity header for the targets registered in Plan 08. Have xtask read the lock, pass expected revision/digest as structured `-D` configure arguments, and build `liquidfun-reference` so Box2D remains transitive. Extend fake-tool tests for exact arguments and unknown-preset rejection. Add `-fno-sanitize-recover=undefined`; set/document `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1` and `ASAN_OPTIONS=abort_on_error=1:halt_on_error=1`. Preserve out-of-tree outputs and do not duplicate target registration after verification.
  </action>
  <verify>
    <automated>cargo test -p xtask --test upstream_cli &amp;&amp; cargo xtask upstream configure --preset oracle-debug &amp;&amp; cargo xtask upstream build --preset oracle-debug</automated>
  </verify>
  <acceptance_criteria>
    - Command tests prove revision/digest configure arguments, the `liquidfun-reference` target, and fail-closed preset/path validation.
    - `rg -n 'liquidfun-reference|CXX_STANDARD 17|build_identity|SHA256' tools/reference/CMakeLists.txt tools/xtask/src/upstream.rs` finds target and identity binding.
    - `rg -n 'fno-sanitize-recover=undefined|halt_on_error=1|abort_on_error=1' tools/reference/CMakePresets.json tools/reference/CMakeLists.txt` finds fail-fast controls.
    - `find third_party/liquidfun -path '*build*' -o -name build_identity.hpp` produces no generated output inside the submodule.
  </acceptance_criteria>
  <done>The oracle binary is reproducibly identity-bound, allowlisted, out-of-tree, and sanitizer-fail-fast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CMake/xtask to executable identity | Build inputs determine which oracle code and compiler produced traces. |
| Contributor preset input to process invocation | Only reviewed preset names and repository-owned paths may reach CMake. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Required coverage | Disposition | Mitigation in this plan |
| --- | --- | --- | --- | --- |
| T-02-PROCESS | Spoofing/Elevation | Path/process misuse | mitigate | xtask accepts only named presets, structured arguments, lock/digest identity, fixed paths, and no shell command strings. |
| T-02-OUTPUT | Tampering | Wrong successful oracle identity | mitigate | Startup identity is generated from independently checked build inputs and later validated before comparison. |
| T-02-ARTIFACT | Tampering/Repudiation | Unattributed traces | mitigate | Handshake binds revision, adapter digest, compiler/target/flags/preset/sanitizer mode. |
| T-02-NATIVE | Elevation/DoS | Sanitizer finding hidden by recovery | mitigate | Fail-fast ASan/UBSan flags/options and nonzero termination prevent successful traces after evidence. |

No high-severity threat is accepted.
</threat_model>

<verification>
Run `cargo fmt --all`, `cargo clippy -p xtask --all-targets --all-features -- -D warnings`, and `cargo test -p xtask --test upstream_cli`. Run upstream verify/configure/build for debug, build/run the native protocol tests, then configure/build the sanitizer preset and run focused tests with fail-fast variables. Confirm the submodule is clean and `cargo xtask package verify` passes.
</verification>

<success_criteria>
- Startup identity is immutable, complete, and tied to independently checked inputs.
- xtask uses structured allowlisted configuration/build arguments only.
- Sanitizer findings fail fast and generated outputs remain outside the submodule.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-09-SUMMARY.md`.
</output>
