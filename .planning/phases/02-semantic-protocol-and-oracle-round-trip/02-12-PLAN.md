---
phase: 02-semantic-protocol-and-oracle-round-trip
plan: "12"
type: execute
wave: 10
depends_on:
  - 02-11
files_modified:
  - tools/xtask/src/provenance.rs
  - tools/xtask/tests/provenance_cli.rs
  - reference/artifacts/manifest.toml
  - reference/artifacts/traces/empty-world-v1.jsonl
  - reference/source-map.toml
autonomous: true
requirements:
  - COMP-05
  - COMP-08
  - COMP-09
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-07-10T04-59-34
generated_at: 2026-07-10T05:24:31Z
must_haves:
  truths:
    - "Every accepted trace records complete content, scenario, protocol, policy, oracle, adapter, compiler, target, flags, notice, source, and review provenance."
    - "The reviewed empty-world trace is promoted only after replay and deterministic review, and read-only checks never rewrite it."
  artifacts:
    - path: "reference/artifacts/manifest.toml"
      provides: "Strict versioned provenance records for semantic evidence"
    - path: "reference/artifacts/traces/empty-world-v1.jsonl"
      provides: "Reviewed replay-validated Phase-2 oracle trace"
  key_links:
    - from: "tools/xtask/src/provenance.rs"
      to: "reference/artifacts/traces/empty-world-v1.jsonl"
      via: "strict schema/path/hash/oracle/adapter/review validation"
      pattern: "artifact_kind"
---

<objective>
Extend strict provenance and promote the reviewed empty-world trace through Plan 11's lifecycle.

Purpose: Convert a successful round trip into auditable, replayable evidence without hand-editing or unreviewed mutation.
Output: Manifest v2 validation, negative provenance tests, source mapping, and one reviewed trace.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-RESEARCH.md
@.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-11-SUMMARY.md
@reference/upstream-lock.toml
@reference/artifacts/manifest.toml
@reference/source-map.toml

<interfaces>
Only Plan 11's typed stage/replay/diff/review/promote flow may create the accepted trace; provenance checks are strictly read-only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend strict provenance and promote the reviewed empty-world trace</name>
  <read_first>
    - `tools/xtask/src/provenance.rs` — fail-closed Phase-1 validator.
    - `tools/xtask/tests/provenance_cli.rs` — strict negative fixture patterns.
    - `reference/artifacts/manifest.toml` — manifest schema to version deliberately.
    - `reference/source-map.toml` — required derivation/notice mapping.
    - `reference/upstream-lock.toml` — selected revision authority.
    - `protocol/tolerances/phase2-v1.toml` — profile version/hash authority.
    - `scenarios/phase-02/empty-world.json` — source/canonical scenario hashes.
    - `CREATE: reference/artifacts/traces/empty-world-v1.jsonl` — reviewed trace destination.
  </read_first>
  <files>tools/xtask/src/provenance.rs, tools/xtask/tests/provenance_cli.rs, reference/artifacts/manifest.toml, reference/artifacts/traces/empty-world-v1.jsonl, reference/source-map.toml</files>
  <behavior>
    - Manifest v2 requires artifact kind/path/content hash, generator commit, scenario hashes, all four versions/profile hash, oracle/adapter/compiler/target/flags identity, source/seed, trace hash, notices, reviewer/timestamp, and reviewed status.
    - Unknown/duplicate/missing fields, unsafe paths, bad hashes/revisions/identities, unreviewed status, or absent notices fail.
    - The accepted trace replays through the same validator and remains byte-identical under read-only checks.
  </behavior>
  <action>
Version the manifest deliberately and extend `ProvenanceError` with strict `deny_unknown_fields` trace/regression variants. Validate all identities and hashes against scenario, tolerance, trace, source map, lock, and generator commit with inspect-before-use confinement. Use Plan 11's explicit stage/review/promote flow to generate, replay, diff, and promote the real `oracle-debug` trace with explicit review metadata. Do not hand-edit trace bytes, accept portability output as canonical, or mark reviewed before replay/diff succeeds.
  </action>
  <verify>
    <automated>cargo test -p xtask --test provenance_cli &amp;&amp; cargo xtask provenance check</automated>
  </verify>
  <acceptance_criteria>
    - Tests reject every missing/unknown/wrong/unreviewed/path-confined variant and accept complete trace/regression records.
    - `cargo xtask provenance check` reports one reviewed Phase-2 trace at the selected revision.
    - `cargo run -p liquidfun-differential -- replay --scenario empty-world --preset oracle-debug` returns Match.
    - Re-running provenance/replay leaves all five owned files byte-identical.
  </acceptance_criteria>
  <done>The first semantic trace is provenance-complete, replayable, reviewed, and governed by a strict manifest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Reviewed candidate to tracked evidence | Replay, identity, diff, explicit review, confinement, and atomic promotion gate mutation. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Required coverage | Disposition | Mitigation in this plan |
| --- | --- | --- | --- | --- |
| T-02-ARTIFACT | Tampering/Repudiation | Artifact promotion/provenance | mitigate | Full strict metadata, replay, deterministic diff, explicit reviewer, confinement, and read-only validation. |
| T-02-RACE | Tampering | Promotion race/partial state | mitigate | Reuse Plan 11's create-new and atomic promotion controls. |
| T-02-OUTPUT | Tampering | Malformed accepted trace | mitigate | Reparse complete trace and validate request/identity/hash/reset before acceptance. |

No high-severity threat is accepted.
</threat_model>

<verification>
Run focused provenance tests/check twice, replay the promoted trace, assert all owned files are byte-identical under read-only commands, and review the diff for no hand-edited trace or weakened review/path validation.
</verification>

<success_criteria>
- One reviewed empty-world trace is reproducible, provenance-complete, replayed, and atomically promoted.
- Strict provenance checks fail closed and never rewrite tracked evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-protocol-and-oracle-round-trip/02-12-SUMMARY.md`.
</output>
