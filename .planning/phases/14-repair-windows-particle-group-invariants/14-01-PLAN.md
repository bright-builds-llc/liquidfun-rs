---
phase: 14-repair-windows-particle-group-invariants
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun/tests/particle_group_properties.rs
  - crates/liquidfun/tests/particle_group_properties/model.rs
  - crates/liquidfun/tests/particle_group_properties/snapshot.rs
  - crates/liquidfun/src/world/particle_object/group_lifecycle_tests.rs
autonomous: true
requirements:
  - PART-03
  - PART-04
  - PART-09
  - PART-10
  - TEST-02
  - TEST-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 14-2026-07-27T16-15-14
generated_at: 2026-07-27T16:17:54.200Z
must_haves:
  truths:
    - "The audited seed 4149329052036581951 and its complete minimized public operation input are checked in as one readable named regression."
    - "The regression reaches the same group-creation candidate failure recorded by the milestone audit before production behavior is changed."
    - "The first invalid candidate transition and exact ParticleStorageError category are identified without mutating authoritative live storage."
    - "Public and private snapshots can prove stable IDs, topology, optional lanes, derived structures, identity allocation, and lifecycle output before and after a rejected operation."
  artifacts:
    - path: crates/liquidfun/tests/particle_group_properties.rs
      provides: "Named exact-input public regression and versioned replay entrypoint"
    - path: crates/liquidfun/tests/particle_group_properties/model.rs
      provides: "Deterministic public operation replay and rejection snapshot helpers"
    - path: crates/liquidfun/tests/particle_group_properties/snapshot.rs
      provides: "Comprehensive public semantic rollback comparison"
    - path: crates/liquidfun/src/world/particle_object/group_lifecycle_tests.rs
      provides: "Private candidate/storage invariant localization test"
  key_links:
    - from: crates/liquidfun/tests/particle_group_properties.rs
      to: crates/liquidfun/tests/particle_group_properties/model.rs
      via: "Exact seed plus fully persisted controls replay the audited public operation sequence"
      pattern: "4149329052036581951|3995_60c9_ead9_4a3f"
    - from: crates/liquidfun/tests/particle_group_properties/model.rs
      to: crates/liquidfun/tests/particle_group_properties/snapshot.rs
      via: "Every rejected operation compares the complete semantic before/after snapshot"
      pattern: "typed rejection must be effect-free"
---

<objective>
Turn the audited Windows failure into a complete, checked-in, diagnostic regression before touching production behavior.

Purpose: Preserve the exact failure signal and locate the first invalid candidate transition so the repair addresses the root cause rather than hiding the panic.
Output: A named exact-input public replay, comprehensive rollback snapshots, and a private localization test that identifies the failing storage invariant.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/14-repair-windows-particle-group-invariants/14-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
@.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-CONTEXT.md
@crates/liquidfun/tests/particle_group_properties.rs
@crates/liquidfun/tests/particle_group_properties/model.rs
@crates/liquidfun/tests/particle_group_properties/snapshot.rs
@crates/liquidfun/src/world/particle_object.rs
@crates/liquidfun/src/particle/storage.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recover and persist the complete audited operation input</name>
  <read-first>
    <file>.planning/v1.0-MILESTONE-AUDIT.md</file>
    <file>crates/liquidfun/tests/particle_group_properties.rs</file>
    <file>crates/liquidfun/tests/particle_group_properties/model.rs</file>
    <file>crates/liquidfun/tests/particle_group_properties/snapshot.rs</file>
    <file>.github/workflows/ci.yml</file>
  </read-first>
  <files>crates/liquidfun/tests/particle_group_properties.rs, crates/liquidfun/tests/particle_group_properties/model.rs, crates/liquidfun/tests/particle_group_properties/snapshot.rs</files>
  <action>
    Implement D-07, D-08, and D-10. Recover the exact minimized controls or complete operation sequence paired with audited seed `4149329052036581951` (`0x3995_60c9_ead9_4a3f`) from run `30070539790`, Proptest replay, or deterministic local reconstruction. Add a named non-random regression whose input includes both the seed and every control/operation required to reproduce the audited path; do not rely on a Proptest persistence file or the generator strategy alone. Keep the existing different seed `0x7d7b_4a19_10c2_3023` as separate vocabulary coverage. Extend the semantic snapshot only where needed so before/after comparison includes ordered live group/member IDs and metadata, particle snapshots, system statistics, optional public lanes/views, lifecycle output, and next observable identity/diagnostic allocation behavior. If the current defect panics, capture that exact panic only in this diagnostic plan with `catch_unwind` or a private expected-panic assertion and document that Plan 14-02 must replace the temporary expectation with successful no-panic behavior. This diagnostic remains D2 and cannot promote compatibility evidence.
  </action>
  <verify>
    <automated>cargo test -p liquidfun --test particle_group_properties persisted_audited_windows_seed -- --exact --nocapture &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bun scripts/bright-builds-check.ts all</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`crates/liquidfun/tests/particle_group_properties.rs` contains decimal seed `4149329052036581951` or hex `0x3995_60c9_ead9_4a3f` and an explicit minimized controls/operations constant.</criterion>
    <criterion>The named regression does not generate its operation input from `any`, a changing strategy, or seed-only framework persistence.</criterion>
    <criterion>The original vocabulary test using `0x7d7b_4a19_10c2_3023` remains present and distinct.</criterion>
    <criterion>The focused command exits 0 while preserving the audited failure as an explicit temporary diagnostic expectation.</criterion>
  </acceptance-criteria>
  <done>The exact audited public operation sequence is readable, stable, reproducible, and no longer dependent on a transient CI seed report.</done>
</task>

<task type="auto">
  <name>Task 2: Localize the first invalid candidate transition</name>
  <read-first>
    <file>crates/liquidfun/src/world/particle_object.rs</file>
    <file>crates/liquidfun/src/particle/storage.rs</file>
    <file>crates/liquidfun/src/particle/storage/mutation.rs</file>
    <file>crates/liquidfun/src/particle/storage/validation.rs</file>
    <file>crates/liquidfun/src/world/particle_object/group_lifecycle_tests.rs</file>
    <file>crates/liquidfun/tests/particle_group_properties.rs</file>
  </read-first>
  <files>crates/liquidfun/src/world/particle_object/group_lifecycle_tests.rs, crates/liquidfun/tests/particle_group_properties/model.rs</files>
  <action>
    Implement D-01, D-02, and D-06. Add one focused crate-private Arrange/Act/Assert localization test that replays the exact candidate construction stages used by `World::plan_particle_group`: sampled particle append and identity allocation, lifetime initialization, contact refresh, `ParticleStorage::plan_group`, topology candidate commit, solid-depth computation, and final invariant validation. Record the first stage returning `InvalidPermutation`, `LaneLengthMismatch`, `InvalidDerivedReference`, or `InvalidLaneBundle` for the audited sequence. Assert that the authoritative live `ParticleSystem` and group arena remain byte-for-byte or structurally equal to their pre-plan clones and that no lifecycle output or diagnostic allocation is published. Do not add production logging, a `cfg(windows)` branch, a second storage owner, or a fallback topology rebuild. Summarize the proven transition and error category in `14-01-SUMMARY.md` for Plan 14-02.
  </action>
  <verify>
    <automated>cargo test -p liquidfun particle_object::group_lifecycle_tests::audited_group_creation_localizes_first_invalid_candidate_transition -- --exact --nocapture &amp;&amp; cargo test -p liquidfun --test particle_group_properties persisted_audited_windows_seed -- --exact --nocapture &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bun scripts/bright-builds-check.ts all</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The private localization test names the first failing candidate stage and exact `ParticleStorageError` category.</criterion>
    <criterion>The test proves the live system, group arena, diagnostic counter, and lifecycle journal are unchanged.</criterion>
    <criterion>No production `cfg(windows)`, platform-specific branch, logging side effect, or parallel storage path is introduced.</criterion>
    <criterion>Plan summary records the exact failure stage and error category for downstream implementation.</criterion>
  </acceptance-criteria>
  <done>The repair target is a demonstrated candidate transition with an exact invariant failure, not a guessed error mapper change.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Historical CI log to checked regression | Seed-only diagnostics are incomplete until bound to the exact minimized operation input. |
| Generated public operations to physics state | Bounded input must not cause panic, unbounded work, or partial world mutation. |
| Candidate storage to authoritative world | Diagnostic observation must not publish candidate lanes, identities, topology, or lifecycle effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-14-01-01 | Tampering | Regression input | mitigate | Persist seed and full minimized controls together; reject seed-only evidence. |
| T-14-01-02 | Denial of Service | Operation replay | mitigate | Retain existing 24-operation, 8-group, and 32-particle bounds. |
| T-14-01-03 | Repudiation | Failure localization | mitigate | Name the exact candidate stage and storage error in focused test output and the plan summary. |
| T-14-01-04 | Elevation of Privilege | Evidence authority | mitigate | Treat local/macOS and supported-platform replay as D2 regression evidence only. |
</threat-model>

<verification>
Run the exact named public regression and private localization test. Review the diff to confirm production behavior is unchanged and the checked input includes the complete minimized sequence. Before every task commit run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; `bun scripts/bright-builds-check.ts all`.
</verification>

<success-criteria>
1. The exact audited seed and minimized operation input are checked in together.
1. The regression reliably reaches the audited failure path without generator drift.
1. The first invalid candidate transition and storage error category are proven.
1. Authoritative live storage, stable identities, group shells, diagnostics, and lifecycle output remain unchanged during diagnosis.
1. No production workaround or compatibility evidence promotion occurs.
</success-criteria>

<output>
After completion, create `.planning/phases/14-repair-windows-particle-group-invariants/14-01-SUMMARY.md`.
</output>
