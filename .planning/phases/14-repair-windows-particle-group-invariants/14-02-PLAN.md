---
phase: 14-repair-windows-particle-group-invariants
plan: '02'
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - crates/liquidfun/src/world/particle_object.rs
  - crates/liquidfun/src/particle/storage.rs
  - crates/liquidfun/src/particle/storage/mutation.rs
  - crates/liquidfun/src/particle/storage/validation.rs
  - crates/liquidfun/src/world/particle_object/group_lifecycle_tests.rs
  - crates/liquidfun/tests/particle_group_properties.rs
autonomous: true
requirements:
  - PART-03
  - PART-04
  - PART-09
  - PART-10
  - TEST-02
  - TEST-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 14-2026-07-27T16-15-14
generated_at: 2026-07-27T16:17:54.200Z
must_haves:
  truths:
    - "The exact audited sequence no longer reaches the authoritative-storage panic."
    - "The diagnosed transition is repaired at its root inside one clone-plan-commit transaction."
    - "Legitimate isolated-candidate rejection returns CreateObjectError::InvalidParticleGroupTopology without observable effects."
    - "Private storage equality proves identities, lanes, topology, caches, lifecycle state, and next allocations remain transactional."
  artifacts:
    - path: crates/liquidfun/src/world/particle_object.rs
      provides: "Root-cause group-creation repair and typed candidate-side error mapping"
    - path: crates/liquidfun/src/particle/storage/mutation.rs
      provides: "Storage-owned prepared mutation when the diagnosed transition requires it"
    - path: crates/liquidfun/tests/particle_group_properties.rs
      provides: "Passing exact audited regression"
    - path: crates/liquidfun/src/world/particle_object/group_lifecycle_tests.rs
      provides: "Private full-state transactionality proof"
  key_links:
    - from: crates/liquidfun/src/world/particle_object.rs
      to: crates/liquidfun/src/particle/storage/mutation.rs
      via: "Owned candidate preparation validates storage and topology before publication"
      pattern: "plan_particle_group|plan_group|commit_group"
---

<objective>
Repair the exact invalid particle-group candidate transition and prove the implementation remains one atomic storage transaction.

Purpose: Remove the reachable Windows panic without a platform workaround, second storage authority, topology approximation, or leaked private error contract.
Output: Minimal production repair plus exact-seed and crate-private full-state regressions.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/14-repair-windows-particle-group-invariants/14-CONTEXT.md
@.planning/phases/14-repair-windows-particle-group-invariants/14-01-SUMMARY.md
@.planning/phases/09-particle-storage-lifecycle-and-coupling/09-CONTEXT.md
@.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-CONTEXT.md
@crates/liquidfun/src/world/particle_object.rs
@crates/liquidfun/src/particle/storage.rs
@crates/liquidfun/src/particle/storage/mutation.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair the demonstrated candidate transition</name>
  <read-first>
    <file>.planning/phases/14-repair-windows-particle-group-invariants/14-01-SUMMARY.md</file>
    <file>crates/liquidfun/src/world/particle_object.rs</file>
    <file>crates/liquidfun/src/particle/storage.rs</file>
    <file>crates/liquidfun/src/particle/storage/mutation.rs</file>
    <file>crates/liquidfun/src/particle/storage/validation.rs</file>
    <file>crates/liquidfun/src/particle/lifetime.rs</file>
    <file>crates/liquidfun/src/particle/contact.rs</file>
  </read-first>
  <files>crates/liquidfun/src/world/particle_object.rs, crates/liquidfun/src/particle/storage.rs, crates/liquidfun/src/particle/storage/mutation.rs, crates/liquidfun/src/particle/storage/validation.rs</files>
  <action>
    Implement D-01 through D-05. Use the exact stage/error evidence from `14-01-SUMMARY.md`; do not begin with the error mapper. Preserve `World::plan_particle_group` as an owned clone-plan-commit transaction by default. Reorder or consolidate the diagnosed preparation so every required and allocated optional lane, dense/ID map, group range, pair, triad, proxy, contact, body contact, lifetime entry, solver cache, shell effect, and diagnostic allocation is valid before final invariant checking. Introduce one storage-owned prepared payload only if the diagnosis proves incremental preparation cannot maintain those invariants, and never add a second authority. Map the demonstrated legitimate isolated-candidate validation category to `CreateObjectError::InvalidParticleGroupTopology`; retain loud assertions only for corruption of authoritative live storage. Add no `cfg(windows)`, hash-order dependency, topology regeneration, ID substitution, or best-effort repair.
  </action>
  <verify>
    <automated>cargo test -p liquidfun --test particle_group_properties persisted_audited_windows_seed -- --exact --nocapture &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bun scripts/bright-builds-check.ts all</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The exact regression no longer expects or observes the audited panic.</criterion>
    <criterion>The repaired transition constructs a valid complete candidate or returns `InvalidParticleGroupTopology` before authoritative commit.</criterion>
    <criterion>The touched production path contains no `cfg(windows)` or second storage owner.</criterion>
    <criterion>The full ordered Rust gate and Bright Builds checker exit 0 before the task commit.</criterion>
  </acceptance-criteria>
  <done>The audited public sequence cannot reach the impossible-state panic and the root cause is repaired inside the locked transaction boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Make the exact replay and private rollback proof permanent</name>
  <read-first>
    <file>crates/liquidfun/tests/particle_group_properties.rs</file>
    <file>crates/liquidfun/src/world/particle_object/group_lifecycle_tests.rs</file>
    <file>crates/liquidfun/src/particle/storage.rs</file>
    <file>crates/liquidfun/src/particle/storage/validation.rs</file>
  </read-first>
  <files>crates/liquidfun/tests/particle_group_properties.rs, crates/liquidfun/src/world/particle_object/group_lifecycle_tests.rs</files>
  <action>
    Implement D-06 and D-07. Replace the temporary Plan 14-01 panic expectation with a permanent no-panic regression for seed `4149329052036581951` and its explicit minimized controls. At the private boundary compare stable particle/group IDs and next allocations; dense/ID maps; ordered memberships and group records; required and optional lanes; pairs/triads and rest values; proxies, contacts, weights, and solver caches; pending-delete, zombie, expiration, free, and retired identity state; group shells; diagnostic allocation; and zero lifecycle output. Keep this task focused on the exact replay and private implementation state; Plan 14-03 expands the public operation matrix.
  </action>
  <verify>
    <automated>cargo test -p liquidfun --test particle_group_properties &amp;&amp; cargo test -p liquidfun particle_object::group_lifecycle_tests &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bun scripts/bright-builds-check.ts all</automated>
  </verify>
  <acceptance-criteria>
    <criterion>No Phase 14 exact-seed regression contains `should_panic` or asserts the audited panic.</criterion>
    <criterion>The exact seed and minimized controls remain explicit and unchanged.</criterion>
    <criterion>The crate-private test compares every storage category named in D-06 and proves zero published lifecycle effects.</criterion>
    <criterion>The full ordered Rust gate and Bright Builds checker exit 0 before the task commit.</criterion>
  </acceptance-criteria>
  <done>The production repair is guarded by exact-input and complete private transactionality regressions.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Safe group input to candidate storage | Bounded input must not panic or partially mutate live state. |
| Candidate storage to authoritative world | Every lane, identity, topology, cache, shell, and diagnostic effect validates before commit. |
| Local regression to compatibility authority | Local passing output remains D2 and cannot rewrite canonical fixtures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-14-02-01 | Denial of Service | Public group creation | mitigate | Eliminate reachable panic and retain bounded sampling/topology limits. |
| T-14-02-02 | Tampering | Candidate commit | mitigate | Validate the complete owned candidate and publish only through the short commit boundary. |
| T-14-02-03 | Repudiation | Regression result | mitigate | Bind the test to exact seed and controls and retain before/after state assertions. |
| T-14-02-04 | Elevation of Privilege | Evidence authority | mitigate | Forbid fixture or compatibility promotion in this plan. |
</threat-model>

<verification>
Before every task commit run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; `bun scripts/bright-builds-check.ts all`. Inspect the diff for a platform workaround, second storage authority, canonical evidence edit, or Phase 15 scope.
</verification>

<success-criteria>
1. The audited sequence passes without panic.
1. The demonstrated invalid transition is repaired at its root.
1. Legitimate candidate rejection is typed and effect-free.
1. Exact private state equality proves transactionality.
1. No platform workaround, second authority, self-blessed evidence, or Phase 15 work is introduced.
</success-criteria>

<output>
After completion, create `.planning/phases/14-repair-windows-particle-group-invariants/14-02-SUMMARY.md`.
</output>
