---
phase: 14-repair-windows-particle-group-invariants
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - crates/liquidfun/src/world/particle_object.rs
  - crates/liquidfun/src/world/particle_object/group.rs
  - crates/liquidfun/src/particle/storage/solver_state.rs
  - crates/liquidfun/src/particle/storage/solver_state/tests.rs
  - crates/liquidfun/tests/particle_group_properties.rs
  - crates/liquidfun/src/world/object/tests.rs
  - crates/liquidfun/src/world/object/tests/particle_group_transactions.rs
  - crates/liquidfun/src/particle/storage.rs
  - crates/liquidfun/src/particle/storage/transaction_test_support.rs
  - crates/liquidfun/src/arena.rs
autonomous: true
requirements: [PART-03, PART-04, PART-09, PART-10]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 14-2026-07-27T16-15-14
generated_at: 2026-09-14T02:44:00.339924Z
must_haves:
  truths:
    - "Valid public group creation and the audited append succeed without panic on the repaired path."
    - "Legitimate isolated-candidate topology rejection returns InvalidParticleGroupTopology without publishing any state."
    - "Private rollback covers complete storage, lifetime, shell/identity allocation and diagnostic state."
  artifacts:
    - path: crates/liquidfun/src/particle/storage/solver_state.rs
      provides: "Scratch reservation scales with candidate particle count while preserving logical-capacity validation"
    - path: crates/liquidfun/src/world/particle_object/group.rs
      provides: "One clone-plan-commit group authority with legitimate candidate rejection preserved"
    - path: crates/liquidfun/tests/particle_group_properties.rs
      provides: "Original audited replay requires successful final append on every platform"
    - path: crates/liquidfun/src/world/object/tests/particle_group_transactions.rs
      provides: "Exact private transaction-state comparison for late candidate rejection"
  key_links:
    - from: crates/liquidfun/src/world/particle_object/group.rs
      to: crates/liquidfun/src/particle/storage/mutation.rs
      via: "Preserves GroupPlan validation before commit_particle_group publishes"
    - from: crates/liquidfun/src/world/object/tests/particle_group_transactions.rs
      to: crates/liquidfun/src/particle/storage.rs
      via: "Structural PartialEq covers every storage field without unsafe memory inspection"
---

<objective>
Repair the demonstrated candidate transition and prove exact private rollback.

Purpose: Preserve valid behavior while making legitimate rejection typed and effect-free; changing panic mapping alone cannot satisfy this plan.
Output: Narrow native-Rust repair, permanent original-seed success regression, and complete private transaction tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-repair-windows-particle-group-invariants/14-CONTEXT.md
@.planning/phases/14-repair-windows-particle-group-invariants/14-RESEARCH.md

@.planning/phases/14-repair-windows-particle-group-invariants/14-DIAGNOSIS.md
@.planning/phases/14-repair-windows-particle-group-invariants/14-01-SUMMARY.md
@crates/liquidfun/src/world/particle_object/group.rs
@crates/liquidfun/src/world/particle_object.rs
@crates/liquidfun/src/particle/storage/solver_state.rs
@crates/liquidfun/src/particle/storage/solver_state/tests.rs
@crates/liquidfun/src/particle/storage/lifecycle.rs
@crates/liquidfun/src/particle/storage/mutation.rs
@crates/liquidfun/src/world/object/tests.rs
<interfaces>
Execution evidence from Plan 01: Windows operation 14 reaches valid source nine-row and candidate ten-row state, then `zeroed_lane` requests 8,589,934,588 bytes from `try_reserve_exact(declared_capacity)` where declared_capacity is 2,147,483,647. `TryReserveError::AllocError` maps to `InvalidLaneBundle`. No invalid source/candidate invariant or float was observed. `zeroed_lane<T: Copy>(particle_count, declared_capacity, zero)` already calls `preflight_count`; clone/append, permutation and optional-lane initialization reuse it. The existing transaction is sufficient (D-03).
`ParticleStorage` derives `Clone, PartialEq` over identities (including free/retired state), dense maps, every required/optional lane, groups, proxies, particle/body contacts, pairs/triads, group records and solver state.
`ParticleLifetimeState` derives `Clone, PartialEq` separately.
`ParticleSystem` is Clone and carries `diagnostic_id, definition, groups, storage, lifetime, timestamp`.
`World::plan_particle_group(&self, ...) -> Result<ParticleGroupCreationPlan, CreateObjectError>` must finish before `commit_particle_group(&mut self, plan) -> ParticleGroupId`.
`ParticleGroupCreationPlan` owns `system, system_candidate, result_group, maybe_shell, next_diagnostic_id`.
World's `next_diagnostic_id` is private to `world::object`; place complete world tests under `world/object/tests.rs` to inspect it without a public accessor. `Arena::next_handle()` supplies preflighted next identity.
Storage `check_invariants` lives in `storage/lifecycle.rs` and is private: a world test must use accessible comparison/validation seams rather than widening production visibility.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Repair the proven transition and require original-seed success</name>
  <read-first>
    <file>.planning/phases/14-repair-windows-particle-group-invariants/14-DIAGNOSIS.md</file>
    <file>crates/liquidfun/src/world/particle_object.rs</file>
    <file>crates/liquidfun/src/world/particle_object/group.rs</file>
    <file>crates/liquidfun/src/particle/storage/solver_state.rs</file>
    <file>crates/liquidfun/src/particle/storage/solver_state/tests.rs</file>
    <file>crates/liquidfun/tests/particle_group_properties.rs</file>
  </read-first>
  <files>crates/liquidfun/src/world/particle_object.rs, crates/liquidfun/src/world/particle_object/group.rs, crates/liquidfun/src/particle/storage/solver_state.rs, crates/liquidfun/src/particle/storage/solver_state/tests.rs, crates/liquidfun/tests/particle_group_properties.rs</files>
  <behavior>
    - The exact original seed/controls still expand to all 14 persisted operations.
    - Model initialization and every original operation complete without panic; the final valid append yields Outcome::Applied { created: 0, lifecycle: 0 }, retains its existing target, adds one particle and preserves prior live identities.
    - A demonstrated late invalid topology candidate returns CreateObjectError::InvalidParticleGroupTopology; a blanket rejection of valid creation/append fails the test.
  </behavior>
  <action>
Per D-01 through D-05 and D-07, first replace only the original regression's temporary cfg(windows) panic expectation with the same explicit valid success contract on all platforms; preserve every original constant and the separate vocabulary regression. Record a real RED signal from the supported-Windows baseline or the diagnosis's focused root-cause reproducer, never claim local macOS passing behavior proves RED. Implement the demonstrated sizing repair in solver_state.rs::zeroed_lane: reserve the actual `particle_count`, not the declared logical maximum, after the existing preflight_count guard. Preserve count-limit validation, zero initialization, optional-lane absence, copied/permuted values, and source ordering. Add behavior regressions in solver_state/tests.rs proving small scratch lanes stay bounded when declared capacity is larger, and that append/permutation preserve values and invalid counts still reject without effects. Use bounded declared capacities for a local RED resource-budget test so regression testing does not itself require multi-gigabyte allocations; the retained Windows trace supplies the real default-maximum RED evidence. Do not weaken logical-capacity limits or introduce allocation-dependent physics behavior. Preserve legitimate typed topology rejection at the existing group boundary; no mapper-only change can satisfy the valid append test. Other listed world files are a narrow envelope only if the existing candidate boundary actually needs a correction demonstrated by the tests. The listed current preparation seams are an ownership envelope, not a claim all need edits; if diagnosis proves another file is responsible, first amend this task/file ownership narrowly and split any growth beyond five files per task. Keep sampling, identity/lifetime/contact preparation, topology, final validation, shell reservation and diagnostic arithmetic owned by the candidate until immediate commit. Use one storage-owned prepared payload only when D-03's evidence condition is met, carrying the complete state; never add a parallel owner. For legitimate candidate failures use existing InvalidParticleGroupTopology at the candidate boundary. Do not convert every shared storage_object_creation_error arm into a recoverable error: its ordinary creation/lifecycle callers must retain justified authoritative-corruption assertions. No platform branch, tolerance relaxation, clamping, rebuild, validation bypass or identity replacement can stand in for the repair. Remove diagnostic probes once their evidence is retained; stage only tested final behavior. Re-run the scratch-lane regression module, focused success case and related creation/lifetime suites before the ordered commit gates.
  </action>
  <verify><automated>cargo test -p liquidfun --lib particle::storage::solver_state::tests &amp;&amp; cargo test -p liquidfun --test particle_group_properties persisted_audited_windows_seed -- --exact --nocapture &amp;&amp; cargo test -p liquidfun --test particle_groups --test particle_group_mutation</automated></verify>
  <acceptance-criteria>
    <criterion>Small scratch lanes allocate in proportion to actual particle count while declared-capacity and i32 count limits still reject invalid requests; append/permutation preserve lane values.</criterion>
    <criterion>The original exact command executes one test; valid final append is Applied { created: 0, lifecycle: 0 }, preserves target identity and adds its intended member.</criterion>
    <criterion>The full original sequence includes initialization and all prefix operations without an expected panic or platform-specific acceptance.</criterion>
    <criterion>The diff repairs the diagnosed transition before adjusting candidate-specific failure mapping, preserves one owner and changes no public error contract.</criterion>
    <criterion>Successful local checks are recorded honestly; final actual Windows/Linux/macOS proof is supplied by Plan 04.</criterion>
  </acceptance-criteria>
  <done>The demonstrated transition is repaired, and mapper-only blanket rejection cannot satisfy the permanent original regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove complete private no-effect rejection and allocation continuity</name>
  <read-first>
    <file>crates/liquidfun/src/world/object.rs</file>
    <file>crates/liquidfun/src/world/object/tests.rs</file>
    <file>crates/liquidfun/src/particle/storage.rs</file>
    <file>crates/liquidfun/src/particle/lifetime.rs</file>
    <file>crates/liquidfun/src/world/particle_object/group.rs</file>
  </read-first>
  <files>crates/liquidfun/src/world/object/tests.rs, crates/liquidfun/src/world/object/tests/particle_group_transactions.rs, crates/liquidfun/src/particle/storage.rs, crates/liquidfun/src/particle/storage/transaction_test_support.rs, crates/liquidfun/src/arena.rs</files>
  <behavior>
    - Rejection after candidate particle/lifetime/contact preparation preserves the entire live ParticleStorage and ParticleLifetimeState exactly.
    - Rejected new-group and append requests preserve live group shells, arena next handles/generations, system group order, definition/timestamp and next diagnostic allocation.
    - A following successful create after rejection gets the exact preflighted next particle/group/diagnostic identities and publishes no extra lifecycle output.
  </behavior>
  <action>
Per D-02, D-04 through D-06, register a test child module in object/tests.rs and keep new tests in object/tests/particle_group_transactions.rs. Build populated valid fixtures exercising required and optional lanes, pair/triad rest data, proxies/contacts/weights/caches and pending/zombie/expiration/free/retired identities; use existing public setup/lifecycle operations and narrow existing test seams. Capture live storage and lifetime clones, all system fields outside storage, ordered group shells and diagnostic IDs, arena next identity state and world next_diagnostic_id. Force real late candidate validation failure after the demonstrated preparation stage with bounded checked input (for example the existing non-finite-derived topology fixture), not an invalid request rejected before preparation. Compare full ParticleStorage and lifetime structurally with assert!(after == before) where Debug is unavailable, plus the separately captured world fields. Verify exact InvalidParticleGroupTopology, not any error. Prove next allocations by comparing preflighted IDs/diagnostics within the same world and a no-rejection control trace normalized only for world identity; do not compare raw pointers or serialized memory. Add separate new/append rejection tests and an allocation-continuity test with explicit Arrange/Act/Assert. Capture lifecycle events/receipts before/after and the next step to prove no immediate or deferred output from rejection. Do not expose private state through public APIs or add production equality/debug solely for testing. If a inaccessible field prevents exact proof, add a narrowly scoped cfg(test) comparison helper in its owning module, first updating files_modified and keeping this task bounded.
  </action>
  <verify><automated>cargo test -p liquidfun --lib -- --list &amp;&amp; cargo test -p liquidfun --lib world::object::tests::particle_group_transactions -- --nocapture</automated></verify>
  <acceptance-criteria>
    <criterion>Listed and executed transaction tests include separate new, append and next-allocation concerns; the module filter runs at least three tests and zero selected tests is a failure.</criterion>
    <criterion>Tests actually exercise late candidate preparation and InvalidParticleGroupTopology without asserting a panic.</criterion>
    <criterion>Every D-06 state category is mapped to full-storage equality or a named extra assertion; populated optional and derived state make equality meaningful.</criterion>
    <criterion>Success after rejection proves exact identity/diagnostic allocation continuity and no leaked immediate/deferred lifecycle output.</criterion>
  </acceptance-criteria>
  <done>Late rejection has complete private rollback proof beyond a count-only or normalized public snapshot.</done>
</task>
</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Checked recipe to isolated candidate | Reachable valid input must succeed; legitimate invalid topology must reject without panic. |
| Candidate to live world | Only complete validated candidate state may publish identities, lanes, topology or output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-14-02-01 | Denial of Service | create_particle_group | mitigate | Repair demonstrated reachable panic and preserve sampling/topology limits. |
| T-14-02-02 | Tampering | commit_particle_group | mitigate | Validate full owned candidate before publication; exact private rollback and next-allocation tests. |
| T-14-02-03 | Repudiation | Error classification | mitigate | Assert exact legitimate rejection type and valid success; preserve RED trace and cause. |
| T-14-02-04 | Information Disclosure | Public API | mitigate | Keep private storage categories and test snapshots private; retain existing stable errors. |
</threat-model>

<verification>
Before each commit, including documentation commits, run in this order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then run `bun scripts/bright-builds-check.ts all` and `git diff --check`, inspect the scoped diff, and commit only after all applicable checks pass. Run `just markdown-check` for non-GSD Markdown; never format `.planning/**` with mdformat. Keep touched Rust files below 629 physical lines by cohesive child modules. Standing authorization in AGENTS.md permits ordinary non-force publication to verified `bright-builds-llc/liquidfun-rs:main`, validation workflow inspection/dispatch, diagnosis and retries; missing final platform proof blocks phase completion, not the locally verified publication needed to obtain that proof.
Require actual nonzero filtered test counts. Read the full diff against 14-DIAGNOSIS.md and reject a mapper-only patch, newly weakened invariant, platform workaround or changed canonical evidence.
</verification>

<success-criteria>
1. Original valid replay and append succeed with stable identity and intended member count.
1. Legitimate isolated-candidate failure uses the existing typed topology boundary.
1. Complete private state, allocations and lifecycle remain unchanged on rejection.
1. One authoritative clone-plan-commit path and all source ordering contracts remain intact.
</success-criteria>

<output>
After completion, create `.planning/phases/14-repair-windows-particle-group-invariants/14-02-SUMMARY.md`, including D-03's evidence-based disposition and the D-06 assertion inventory.
</output>
