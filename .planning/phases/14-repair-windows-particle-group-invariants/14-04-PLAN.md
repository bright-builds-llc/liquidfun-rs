---
phase: 14-repair-windows-particle-group-invariants
plan: "04"
type: execute
wave: 4
depends_on: ["14-03"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/14-repair-windows-particle-group-invariants/14-PLATFORM-EVIDENCE.md
autonomous: true
requirements: [PART-03, PART-04, PART-09, PART-10, TEST-02, TEST-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 14-2026-07-27T16-15-14
generated_at: 2026-09-13T19:42:47Z
must_haves:
  truths:
    - "The original and current fixed regressions, focused integration/property suite and complete liquidfun package pass on one identical full SHA on Windows, Linux and macOS."
    - "Failed or stale attempts remain separately retained and never satisfy platform closure."
    - "Platform results are D2 only; Phase 15 release-candidate aggregation and canonical evidence remain unchanged."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Existing supported-OS Cargo CI matrix with visible fixed-regression selection"
    - path: .planning/phases/14-repair-windows-particle-group-invariants/14-PLATFORM-EVIDENCE.md
      provides: "Actual same-SHA platform results, test counts, toolchain/runner and run/job identities"
  key_links:
    - from: .github/workflows/ci.yml
      to: crates/liquidfun/tests/particle_group_properties.rs
      via: "Explicit named regressions plus complete property target on existing matrix"
    - from: .planning/phases/14-repair-windows-particle-group-invariants/14-PLATFORM-EVIDENCE.md
      to: .github/workflows/ci.yml
      via: "Validated full-SHA run and actual Windows/Linux/macOS job logs"
---

<objective>
Obtain actual supported-platform proof through ordinary authorized publication and the existing Cargo CI matrix.

Purpose: Close the Windows implementation defect without the old pre-push approval deadlock or Phase 15 evidence expansion.
Output: Minimal existing-matrix wiring and 14-PLATFORM-EVIDENCE.md with truthful same-SHA D2 results.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-repair-windows-particle-group-invariants/14-CONTEXT.md
@.planning/phases/14-repair-windows-particle-group-invariants/14-RESEARCH.md

@.planning/phases/14-repair-windows-particle-group-invariants/14-01-SUMMARY.md
@.planning/phases/14-repair-windows-particle-group-invariants/14-02-SUMMARY.md
@.planning/phases/14-repair-windows-particle-group-invariants/14-03-SUMMARY.md
@.planning/phases/14-repair-windows-particle-group-invariants/14-DIAGNOSIS.md
@.github/workflows/ci.yml
<interfaces>
Cargo CI workflow path: `.github/workflows/ci.yml`; triggers pull_request, push on main, workflow_dispatch.
Existing default-features matrix: `ubuntu-24.04, macos-15, windows-2025`; Rust 1.97.0; `cargo build -p liquidfun`, then `cargo test -p liquidfun`.
The full package is the explicit superset of the complete relevant particle suite; no selected particle target may be skipped because an earlier target failed.
Keep existing Linux workspace quality/isolation lane, action SHA pins, cache keys, submodules:false, read-only permissions and fail-fast:false.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Make focused regression execution visible in the existing matrix</name>
  <read-first>
    <file>.github/workflows/ci.yml</file>
    <file>crates/liquidfun/tests/particle_group_properties.rs</file>
    <file>.planning/phases/14-repair-windows-particle-group-invariants/14-03-SUMMARY.md</file>
    <file>.planning/phases/14-repair-windows-particle-group-invariants/14-RESEARCH.md</file>
  </read-first>
  <files>.github/workflows/ci.yml</files>
  <action>
Per D-07 through D-10, reuse the existing default-features OS matrix. After build and before the full package step, add focused commands for both exact named fixed regressions and the three integration targets particle_group_properties, particle_groups, particle_group_mutation. Existing full cargo test -p liquidfun remains mandatory afterward. Include --list for the fixed target and retain normal harness output so each exact command demonstrably runs one test; never accept a zero-test success or --skip workaround. Keep commands shell-portable on Windows and the Unix runners, using separate step entries rather than shell-specific error masking. Retain Rust 1.97.0, supported runner labels, action pins, isolation, feature/cache conventions and the Linux quality lane. Do not add a second matrix, new producer/attestation format, release workflow, fixture generation or continue-on-error. Validate YAML with the installed actionlint and run exact local commands before the full ordered commit gates.
  </action>
  <verify><automated>actionlint .github/workflows/ci.yml &amp;&amp; cargo test -p liquidfun --test particle_group_properties persisted_audited_windows_seed -- --exact --nocapture &amp;&amp; cargo test -p liquidfun --test particle_group_properties persisted_current_windows_seed -- --exact --nocapture &amp;&amp; cargo test -p liquidfun --test particle_group_properties --test particle_groups --test particle_group_mutation</automated></verify>
  <acceptance-criteria>
    <criterion>Every existing default-features runner selects each exact regression, all three focused targets and the complete liquidfun package.</criterion>
    <criterion>Exact fixed regression commands execute one test each; full package testing and existing quality/isolation gates are retained.</criterion>
    <criterion>Actionlint and local ordered gates pass before committing; workflow wiring alone is not platform proof.</criterion>
  </acceptance-criteria>
  <done>The existing matrix runs the exact repair regressions visibly without weakening full-suite coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the locally verified candidate and retain exact-SHA D2 results</name>
  <read-first>
    <file>AGENTS.md</file>
    <file>standards-overrides.md</file>
    <file>.github/workflows/ci.yml</file>
    <file>.planning/phases/14-repair-windows-particle-group-invariants/14-DIAGNOSIS.md</file>
    <file>.planning/phases/14-repair-windows-particle-group-invariants/14-03-SUMMARY.md</file>
  </read-first>
  <files>.planning/phases/14-repair-windows-particle-group-invariants/14-PLATFORM-EVIDENCE.md</files>
  <action>
Per D-08 through D-10, apply AGENTS.md standing authorization from 2026-09-13; do not request a new per-candidate token or require remote evidence before the publication needed to obtain it. Run ordered local gates, inspect the complete scoped diff, commit, then verify repository slug bright-builds-llc/liquidfun-rs, origin URL, default/main branch, clean worktree, full candidate SHA, remote base and ancestry immediately before ordinary non-force push. Fetch/integrate compatible drift autonomously; changed code requires renewed checks and a fresh candidate. After push, resolve the Cargo CI run from exact repository/workflow/event/ref/SHA identity, retaining run and actual job IDs; polling a run-list result alone is not proof of identity. Prefer the push-triggered run. If dispatch is needed, record intent/result once, resolve uncertain dispatch before retrying, and verify returned run identity. Inspect failures under standing authorization, preserve each failed attempt in a distinct target/phase14-platform/&lt;full-sha&gt;/attempt-&lt;unique-id&gt;/ directory and cite it. Diagnose, correct and repeat locally gated publication as needed; do not waive failures, silently rerun deterministic tests until green or overwrite records. A fixed-SHA infrastructure rerun must retain its distinct attempt and rationale.

Wait for real completed successful non-skipped Windows, Linux and macOS default-feature jobs at one identical full source SHA. Validate both exact named counts are one, the full property/focused target counts match inventory, and cargo test -p liquidfun completes all relevant targets. Record runner/architecture, rustc -vV/toolchain, command argv, exit/outcome and run/job/attempt URLs with retained log digests. Inspect the Linux quality result: a failed unrelated lane is a separate blocker to a clean Cargo CI claim, never hidden as success; diagnose ordinary in-scope failures autonomously and surface material out-of-scope work if necessary. Write 14-PLATFORM-EVIDENCE.md with before/after Windows provenance, same-SHA platform table, failed-attempt history and explicit D2 classification. Missing, failed, skipped or different-SHA results leave Phase 14 unverified. Documentation commits after the tested source must be labeled as such; if claiming a later HEAD is tested, obtain all three jobs at that exact HEAD rather than silently rebinding older evidence. Do not update canonical fixtures, compatibility statuses, release aggregation or frozen-source attestation.
  </action>
  <verify><automated>gh run view "$PHASE14_RUN_ID" -R bright-builds-llc/liquidfun-rs --json databaseId,headSha,event,status,conclusion,jobs,url &amp;&amp; gh run view "$PHASE14_RUN_ID" -R bright-builds-llc/liquidfun-rs --log &amp;&amp; git diff --check</automated></verify>
  <acceptance-criteria>
    <criterion>PHASE14_RUN_ID is set from the verified run identity, and live metadata plus logs prove one full SHA for all supported OS results.</criterion>
    <criterion>Both exact regressions run one test each; all focused targets and complete liquidfun package finish successfully, without skips or tolerated errors.</criterion>
    <criterion>The record names runner/toolchain, run/job/attempt IDs, commands, counts, log digests, and original failed Windows evidence; failed attempts remain separate.</criterion>
    <criterion>No historical approval fiction, one-shot limit, forced push, canonical promotion or Phase 15 artifact is introduced.</criterion>
    <criterion>Absent evidence blocks phase completion while routine authorized diagnosis/republication continues.</criterion>
  </acceptance-criteria>
  <done>The repaired source has actual same-SHA Windows/Linux/macOS D2 success, preserved failure history, and truthful remaining-scope boundaries.</done>
</task>
</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Local commit to remote runner | Publication and test selection must target the intended repository/ref/SHA. |
| Workflow output to phase acceptance | Results must prove executed tests rather than configuration or stale runs. |
| D2 results to canonical authority | Platform success cannot promote fixtures or release acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-14-04-01 | Spoofing | Run identity | mitigate | Verify repository/workflow/ref/full SHA and actual OS job IDs before acceptance. |
| T-14-04-02 | Tampering | Test selection | mitigate | Require exact count one for each named test, complete target inventory, successful full package and no skips. |
| T-14-04-03 | Repudiation | Attempts | mitigate | Retain separate candidate/attempt logs, digests and dispatch reconciliation; never overwrite failed records. |
| T-14-04-04 | Elevation of Privilege | Evidence tier | mitigate | Classify D2 only; leave canonical fixtures, compatibility rows and Phase 15 aggregation untouched. |
</threat-model>

<verification>
Before each commit, including documentation commits, run in this order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then run `bun scripts/bright-builds-check.ts all` and `git diff --check`, inspect the scoped diff, and commit only after all applicable checks pass. Run `just markdown-check` for non-GSD Markdown; never format `.planning/**` with mdformat. Keep touched Rust files below 629 physical lines by cohesive child modules. Standing authorization in AGENTS.md permits ordinary non-force publication to verified `bright-builds-llc/liquidfun-rs:main`, validation workflow inspection/dispatch, diagnosis and retries; missing final platform proof blocks phase completion, not the locally verified publication needed to obtain that proof.
The automated gh commands retrieve evidence; the task's identity/count/outcome criteria must also pass. They do not become successful platform proof merely because gh exits zero.
</verification>

<success-criteria>
1. One identical tested source SHA passes both named cases, focused suites and complete package on Windows/Linux/macOS.
1. Actual counts and terminal job logs are retained, independently checked against the declared commands and inventory.
1. Failed attempts and original failure stay available and distinct.
1. All Phase 14 platform proof remains D2; final release-candidate evidence remains Phase 15.
</success-criteria>

<output>
After completion, create `.planning/phases/14-repair-windows-particle-group-invariants/14-04-SUMMARY.md` referencing 14-PLATFORM-EVIDENCE.md and the exact tested source SHA.
</output>
