---
phase: 14-repair-windows-particle-group-invariants
plan: '03'
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - crates/liquidfun/tests/particle_group_properties/model.rs
  - crates/liquidfun/tests/particle_group_properties/snapshot.rs
  - crates/liquidfun/tests/particle_groups.rs
  - crates/liquidfun/tests/particle_group_mutation.rs
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - PART-03
  - PART-04
  - PART-09
  - PART-10
  - TEST-02
  - TEST-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 14-2026-07-27T16-15-14
generated_at: 2026-07-27T16:17:54.200Z
must_haves:
  truths:
    - "Public create, append, join, split, flag, lifecycle, and property failures preserve complete semantic state."
    - "The exact named regression and complete relevant particle suite execute successfully on one exact commit under supported Windows, Linux, and macOS toolchains."
    - "Missing any platform result blocks phase verification and every push."
    - "All platform results remain D2 and Phase 15 candidate aggregation remains deferred."
  artifacts:
    - path: crates/liquidfun/tests/particle_group_properties/snapshot.rs
      provides: "Complete public semantic rollback oracle"
    - path: crates/liquidfun/tests/particle_groups.rs
      provides: "Focused public creation and typed-rejection regressions"
    - path: crates/liquidfun/tests/particle_group_mutation.rs
      provides: "Focused join, split, and flag transaction regressions"
    - path: .github/workflows/ci.yml
      provides: "Exact named regression and relevant suite execution on the supported OS matrix"
  key_links:
    - from: crates/liquidfun/tests/particle_group_properties/snapshot.rs
      to: crates/liquidfun/tests/particle_group_properties/model.rs
      via: "Every rejected public operation compares complete before/after semantics"
      pattern: "SemanticSnapshot|typed rejection must be effect-free"
    - from: .github/workflows/ci.yml
      to: crates/liquidfun/tests/particle_group_properties.rs
      via: "Exact named regression runs on Windows, Linux, and macOS at one full SHA"
      pattern: "particle_group_properties|cargo test"
---

<objective>
Close Phase 14 with complete public rollback coverage and actual exact-commit Windows/Linux/macOS execution evidence.

Purpose: Prove the repair is cross-platform behavior, not a local or static-CI assumption, while preserving D2 evidence authority.
Output: Public regression expansion, supported-platform wiring, exact platform results, and a blocking evidence checkpoint when those results cannot be obtained without a prohibited pre-verification push.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/14-repair-windows-particle-group-invariants/14-CONTEXT.md
@.planning/phases/14-repair-windows-particle-group-invariants/14-02-SUMMARY.md
@crates/liquidfun/tests/particle_group_properties.rs
@crates/liquidfun/tests/particle_group_properties/model.rs
@crates/liquidfun/tests/particle_group_properties/snapshot.rs
@crates/liquidfun/tests/particle_groups.rs
@crates/liquidfun/tests/particle_group_mutation.rs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand permanent public rollback and CI-matrix coverage</name>
  <read-first>
    <file>crates/liquidfun/tests/particle_group_properties.rs</file>
    <file>crates/liquidfun/tests/particle_group_properties/model.rs</file>
    <file>crates/liquidfun/tests/particle_group_properties/snapshot.rs</file>
    <file>crates/liquidfun/tests/particle_groups.rs</file>
    <file>crates/liquidfun/tests/particle_group_mutation.rs</file>
    <file>.github/workflows/ci.yml</file>
  </read-first>
  <files>crates/liquidfun/tests/particle_group_properties/model.rs, crates/liquidfun/tests/particle_group_properties/snapshot.rs, crates/liquidfun/tests/particle_groups.rs, crates/liquidfun/tests/particle_group_mutation.rs, .github/workflows/ci.yml</files>
  <action>
    Implement D-06 through D-09. Extend the public semantic snapshot and focused Arrange/Act/Assert tests so legitimate create, append, topology, join, split, and flag rejection compares stable IDs and next allocations; ordered membership and metadata; optional public lanes/views; topology/rest observations; statistics and contacts; shell validity; and zero lifecycle output. Keep one concern per test. Inspect `.github/workflows/ci.yml`; modify it only if the existing Windows/Linux/macOS jobs do not execute the exact named regression, focused integrations, and complete relevant particle suite. Preserve existing toolchain, cache, feature, and isolation conventions and add no new evidence schema or Phase 15 workflow.
  </action>
  <verify>
    <automated>cargo test -p liquidfun --test particle_group_properties &amp;&amp; cargo test -p liquidfun --test particle_groups &amp;&amp; cargo test -p liquidfun --test particle_group_mutation &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bun scripts/bright-builds-check.ts all</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Focused public tests cover create, append, topology, join, split, and flag rejection as separate concerns.</criterion>
    <criterion>The exact named seed regression and relevant particle suite are selected by every supported OS job.</criterion>
    <criterion>The full ordered Rust gate and Bright Builds checker exit 0 before the task commit.</criterion>
    <criterion>No canonical fixture, compatibility row, D1/D3 promotion, or Phase 15 artifact changes.</criterion>
  </acceptance-criteria>
  <done>Public rollback behavior and the supported-platform execution surface are complete and locally clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Supply exact-commit supported-platform execution evidence</name>
  <action>
    Implement D-08 through D-10. Stop before any push. Present the exact local full commit SHA and the commands that must execute on Windows, Linux, and macOS: the named audited regression, focused particle-group property/groups/mutation integrations, and the complete relevant particle suite selected by the existing CI jobs. Accept either user-supplied native command transcripts bound to that exact SHA/toolchain or explicit user authorization for a temporary evidence ref push and existing CI dispatch. Do not infer authorization from the wrapper, because its strict gate forbids all pushes before clean verification. Require successful results for all three platforms; missing, skipped, stale, different-SHA, or failed evidence blocks continuation and final push.
  </action>
  <resume-signal>Provide exact-SHA Windows, Linux, and macOS passing evidence, or explicitly authorize one temporary evidence-ref push for the existing CI matrix.</resume-signal>
  <acceptance-criteria>
    <criterion>Evidence names one identical full commit SHA across Windows, Linux, and macOS.</criterion>
    <criterion>Each platform record names toolchain/runner, exact commands or run/job IDs, and successful outcomes.</criterion>
    <criterion>No platform evidence is accepted from a different commit, skipped job, static workflow inspection, or self-authored fixture.</criterion>
  </acceptance-criteria>
  <done>Actual supported-platform execution evidence exists at the implementation commit without violating the strict pre-verification push gate.</done>
</task>

<task type="auto">
  <name>Task 3: Validate and record Phase 14 platform closure</name>
  <read-first>
    <file>.planning/phases/14-repair-windows-particle-group-invariants/14-CONTEXT.md</file>
    <file>.planning/phases/14-repair-windows-particle-group-invariants/14-02-SUMMARY.md</file>
    <file>.github/workflows/ci.yml</file>
    <file>.planning/v1.0-MILESTONE-AUDIT.md</file>
  </read-first>
  <files>.planning/phases/14-repair-windows-particle-group-invariants/14-03-SUMMARY.md</files>
  <action>
    Validate the supplied evidence against D-08 through D-10: same exact full SHA, supported Windows/Linux/macOS identities, named regression, focused integrations, complete relevant particle suite, and successful non-skipped outcomes. Record run/job IDs or native command transcripts, toolchains, commit identity, and D2-only classification in `14-03-SUMMARY.md`. Record Phase 15 release-candidate aggregation as deferred. Re-run the complete local ordered Rust gate and Bright Builds checker; review the diff for `cfg(windows)`, canonical fixture changes, compatibility promotion, or Phase 15 scope. If any platform evidence or local gate is missing, stop and leave verification non-passed.
  </action>
  <verify>
    <automated>cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bun scripts/bright-builds-check.ts all</automated>
  </verify>
  <acceptance-criteria>
    <criterion>`14-03-SUMMARY.md` records one exact full SHA and passing Windows, Linux, and macOS evidence with run/job IDs or native command transcripts.</criterion>
    <criterion>Every platform record is classified D2 and no compatibility promotion is claimed.</criterion>
    <criterion>The final ordered Rust gate and Bright Builds checker exit 0.</criterion>
    <criterion>Missing evidence leaves Phase 14 verification non-passed and blocks commit/push finalization.</criterion>
  </acceptance-criteria>
  <done>Phase 14 has actual exact-commit supported-platform proof and a truthful D2 closure record.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Public operation to semantic snapshot | Rejected actions must not hide identity, topology, lane, cache, or lifecycle drift. |
| Local commit to platform runner | Evidence must execute the identical full SHA and exact selected suites. |
| D2 platform result to canonical authority | Platform success cannot rewrite fixtures or promote D1/D3 compatibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-14-03-01 | Spoofing | Platform identity | mitigate | Require full SHA, runner/toolchain identity, and run/job ID or native transcript. |
| T-14-03-02 | Tampering | Evidence selection | mitigate | Reject skipped, different-SHA, stale, static-only, or incomplete results. |
| T-14-03-03 | Repudiation | Phase closure | mitigate | Record exact commands/outcomes and D2 classification in the plan summary. |
| T-14-03-04 | Elevation of Privilege | Compatibility authority | mitigate | Implement D-10 and forbid fixture or status promotion. |
</threat-model>

<verification>
Before Task 1 and Task 3 commits run the exact ordered Rust and Bright Builds gates. Task 2 is a blocking evidence gate: do not continue, mark phase passed, or push when any supported-platform result is missing. After evidence is supplied, confirm one exact full SHA and successful named/focused/full relevant particle execution on Windows, Linux, and macOS.
</verification>

<success-criteria>
1. Public failure paths preserve complete semantic state.
1. The exact audited regression and complete relevant suite pass on one exact commit on Windows, Linux, and macOS.
1. Exact platform identities and outcomes are recorded as D2.
1. Missing evidence blocks phase verification and all push finalization.
1. Phase 15 aggregation remains deferred.
</success-criteria>

<output>
After completion, create `.planning/phases/14-repair-windows-particle-group-invariants/14-03-SUMMARY.md`.
</output>
