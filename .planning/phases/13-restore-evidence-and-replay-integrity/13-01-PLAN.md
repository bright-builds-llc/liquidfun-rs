---
phase: 13-restore-evidence-and-replay-integrity
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/reference/phase9-lifecycle-contact-witness.materials.json
  - tools/xtask/src/provenance/phase9_witness.rs
  - tools/xtask/tests/phase9_witness_provenance.rs
  - reference/source-map.toml
  - reference/artifacts/manifest.toml
autonomous: true
requirements:
  - FND-04
  - COMP-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-07-25T18-25-02
generated_at: 2026-07-25T18:25:02.901Z
must_haves:
  truths:
    - "Maintainers can mechanically enumerate every material that affects the Phase 9 lifecycle/contact witness target."
    - "The declared manifest is checked against an independently derived CMake codemodel and compiler dependency closure, so an undeclared target input fails validation."
    - "An unrelated oracle adapter change cannot invalidate the target-scoped witness identity."
    - "A missing, added, reordered, or changed declared material fails provenance validation before any oracle build or comparison proceeds."
    - "Every new Phase 13 evidence class has an upstream revision, derivation/alteration classification, and applicable notice reference before canonical production."
  artifacts:
    - path: tools/reference/phase9-lifecycle-contact-witness.materials.json
      provides: "Versioned target-scoped inventory of source files, wrappers, build rules, flags, and generated inputs"
    - path: tools/xtask/src/provenance/phase9_witness.rs
      provides: "Canonical materials resolution, scoped digest calculation, and fail-closed provenance validation"
    - path: tools/xtask/tests/phase9_witness_provenance.rs
      provides: "Regression coverage for complete closure, unrelated-file isolation, and tamper detection"
    - path: reference/source-map.toml
      provides: "FND-04 source, derivation, alteration, and notice mapping for the materials manifest and Phase 13 evidence classes"
    - path: reference/artifacts/manifest.toml
      provides: "Artifact-ledger schema for canonical bundle, witness, replay, and promotion records"
  key_links:
    - from: tools/reference/phase9-lifecycle-contact-witness.materials.json
      to: tools/xtask/src/provenance/phase9_witness.rs
      via: "Typed manifest parsing and deterministic path/metadata normalization"
      pattern: "phase9-lifecycle-contact-witness\\.materials\\.json"
    - from: tools/xtask/src/provenance/phase9_witness.rs
      to: reference/artifacts/phase9/lifecycle-contact-witnesses.provenance.json
      via: "Scoped materials identity validation without rewriting the reviewed artifact"
      pattern: "materials"
    - from: tools/xtask/src/provenance/phase9_witness.rs
      to: tools/reference/CMakeLists.txt
      via: "CMake File API codemodel plus compiler-generated depfile closure compared bidirectionally with the declared manifest"
      pattern: "codemodel|depfile"
    - from: reference/artifacts/manifest.toml
      to: reference/source-map.toml
      via: "Every Phase 13 artifact class resolves to source/revision, alteration classification, and notice policy"
      pattern: "phase13"
---

<objective>
Replace the Phase 9 witness's broad aggregate adapter digest with a complete, mechanically enumerated target-scoped materials identity and regression-protected validator.

Purpose: Restore source-bound provenance without coupling the witness to unrelated later oracle changes.
Output: A versioned materials manifest, deterministic scoped digest implementation, and focused fail-closed tests. This plan does not regenerate or promote canonical artifacts.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/13-restore-evidence-and-replay-integrity/13-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@tools/xtask/src/provenance/phase9_witness.rs
@tools/reference/src/phase9_lifecycle_contact_witness.cpp
@reference/artifacts/phase9/lifecycle-contact-witnesses.provenance.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define and resolve the target-scoped Phase 9 materials closure</name>
  <read-first>
    <file>.planning/phases/13-restore-evidence-and-replay-integrity/13-CONTEXT.md</file>
    <file>tools/xtask/src/provenance/phase9_witness.rs</file>
    <file>tools/reference/src/phase9_lifecycle_contact_witness.cpp</file>
    <file>tools/reference/CMakeLists.txt</file>
    <file>reference/artifacts/phase9/lifecycle-contact-witnesses.provenance.json</file>
  </read-first>
  <files>tools/reference/phase9-lifecycle-contact-witness.materials.json, tools/xtask/src/provenance/phase9_witness.rs, tools/xtask/tests/phase9_witness_provenance.rs</files>
  <behavior>
    - "The resolver produces one canonical, stable ordering for every target-affecting source, wrapper, CMake/build rule, compiler flag, preset value, and generated input."
    - "The resolver compares the declaration bidirectionally with a mechanically derived CMake target codemodel, evaluated compile/link fragments, and compiler-generated header depfiles."
    - "Adding an undeclared target source, included header, compile definition, compiler option, link input, or generated input fails with the exact unexpected material."
    - "Changing any scoped material changes the scoped identity; changing an unrelated oracle source does not."
    - "Missing files, duplicate paths, path traversal, unknown material kinds, incomplete generated-input declarations, and noncanonical flags fail explicitly."
  </behavior>
  <action>
    Implement D-01 as a versioned typed materials manifest and deterministic resolver. Independently derive the actual `phase9-lifecycle-contact-witness` closure from the configured CMake File API target codemodel (target sources, transitive target/link inputs, generated inputs, evaluated include paths/definitions/options/link fragments, preset values, and build-rule backtraces) plus compiler-generated dependency files for the configured translation units. Normalize repository-relative paths and build metadata, compare derived and declared sets in both directions, hash each material with its kind and canonical identity, and hash the canonical ordered record into one scoped materials digest. Add table-driven Arrange/Act/Assert negative fixtures that inject an undeclared `target_sources` input, an included header, and a compile definition/option and prove each is rejected by its exact path or flag; also prove declared-but-not-derived entries fail. Focused tests may be observed failing locally before implementation, but no failing state may be committed. Do not reuse the broad aggregate adapter digest, accept a manifest as proof of its own completeness, permit globs/implicit directory hashing, or regenerate either reviewed Phase 9 artifact.
  </action>
  <verify>
    <automated>cargo test -p xtask --test phase9_witness_provenance target_scoped_materials -- --exact &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The manifest explicitly covers every source, wrapper, build rule, flag, preset-derived value, and generated input that can affect the witness target.</criterion>
    <criterion>An added undeclared CMake source, compiler-discovered header, and evaluated flag/definition each fail the bidirectional closure check.</criterion>
    <criterion>The canonical resolver is deterministic across repository path spellings and rejects incomplete or ambiguous declarations.</criterion>
    <criterion>An unrelated adapter change is proven not to alter the target-scoped identity.</criterion>
  </acceptance-criteria>
  <done>The target-scoped dependency closure is explicit, mechanically resolved, deterministically hashed, and protected against omissions and false coupling.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Phase 9 provenance validation to the scoped identity</name>
  <read-first>
    <file>tools/reference/phase9-lifecycle-contact-witness.materials.json</file>
    <file>tools/xtask/src/provenance/phase9_witness.rs</file>
    <file>tools/xtask/tests/phase9_witness_provenance.rs</file>
    <file>reference/artifacts/phase9/lifecycle-contact-witnesses.json</file>
    <file>reference/artifacts/phase9/lifecycle-contact-witnesses.provenance.json</file>
  </read-first>
  <files>tools/xtask/src/provenance/phase9_witness.rs, tools/xtask/tests/phase9_witness_provenance.rs, reference/source-map.toml, reference/artifacts/manifest.toml</files>
  <behavior>
    - "Validation checks witness bytes and scoped provenance as one pair and reports the exact missing or changed material."
    - "A syntactically edited digest that is not bound to the resolved materials is rejected."
    - "Validation is read-only and cannot bless current output."
    - "Phase 13 materials and evidence schemas fail provenance validation when upstream source/revision, derivation/alteration classification, or notice references are absent."
  </behavior>
  <action>
    Per D-01, D-03, and FND-04, make the existing Phase 9 provenance check consume the resolved materials record and validate the recorded schema, repository/upstream identities, target/compiler/preset metadata, probe digest, scoped materials digest, and witness digest before declaring success. Extend `reference/source-map.toml` for the target-scoped materials record and define Phase 13 witness, replay-evidence, staged-bundle, and promotion-receipt derivation/alteration and notice classes; extend `reference/artifacts/manifest.toml` with the Phase 13 record schema that requires source revision/path, derivation/alteration classification, and `THIRD_PARTY_NOTICES.md` references. Add negative fixtures proving missing source-map, alteration, or notice fields fail and that editing a recorded hash to match current bytes is insufficient. Focused tests may be observed failing locally before implementation, but no failing state may be committed. Keep canonical witness and provenance artifact bytes unchanged; Plan 13-04 alone adds their regenerated reviewed records.
  </action>
  <verify>
    <automated>cargo test -p xtask --test phase9_witness_provenance &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The validator fails closed with actionable material-level diagnostics for every integrity dimension in D-03.</criterion>
    <criterion>The validator never writes reviewed artifacts and exposes no self-blessing mode.</criterion>
    <criterion>The existing `cargo xtask provenance check` entrypoint uses the scoped validation path.</criterion>
    <criterion>FND-04 validation rejects every new Phase 13 evidence class lacking source revision/path, derivation/alteration classification, or notice references.</criterion>
  </acceptance-criteria>
  <done>Phase 9 provenance validation is target-scoped, pair-aware, read-only, and resistant to edited-hash substitution.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Repository files to materials resolver | Tracked paths and manifest fields are untrusted until normalized and hashed. |
| Materials resolver to reviewed provenance | Computed identity must not be replaced by a user-supplied digest. |
| Local checkout to canonical evidence | Local state is D2-only and cannot promote reviewed evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13-01-01 | Tampering | Materials manifest | mitigate | Reject traversal, duplicates, missing files, unknown kinds, and noncanonical ordering; bind kind, path, metadata, and bytes into the digest. |
| T-13-01-02 | Spoofing | Provenance digest fields | mitigate | Recompute every identity from resolved inputs instead of trusting edited recorded hashes. |
| T-13-01-03 | Repudiation | Provenance diagnostics | mitigate | Report the exact declared material and integrity field that diverged. |
| T-13-01-04 | Elevation of Privilege | Validation command | mitigate | Keep validation read-only; expose no artifact-write or promotion path in this plan. |
| T-13-01-05 | Denial of Service | Manifest expansion | mitigate | Require finite explicit tracked paths and reject recursive globs or unbounded generated input discovery. |
</threat-model>

<verification>
Run the focused xtask provenance test target, then run `cargo xtask provenance check` against the unchanged reviewed pair and confirm any failure names the exact scoped mismatch rather than the former aggregate adapter hash. Before the plan commit, run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not run promotion or rewrite canonical artifacts during this plan.
</verification>

<success-criteria>
1. The complete Phase 9 target-affecting material closure is explicit and deterministic.
1. Unrelated oracle changes no longer invalidate the witness identity.
1. Missing or changed scoped materials fail validation before downstream oracle work.
1. An undeclared CMake source, compiler-discovered header, or evaluated target flag fails the mechanical closure check.
1. Phase 13 source-map and artifact schemas require source revision, derivation/alteration classification, and notice references.
1. No reviewed artifact is regenerated or promoted.
1. No Rust-produced output can become an expected value through this validation path.
1. Phase 14 Windows repair and Phase 15 candidate aggregation remain out of scope.
</success-criteria>

<output>
After completion, create `.planning/phases/13-restore-evidence-and-replay-integrity/13-01-SUMMARY.md`.
</output>
