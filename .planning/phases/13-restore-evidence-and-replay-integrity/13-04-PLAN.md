---
phase: 13-restore-evidence-and-replay-integrity
plan: '04'
type: execute
wave: 3
depends_on:
  - 13-03
files_modified:
  - tools/xtask/src/phase13_evidence.rs
  - tools/xtask/src/phase13_evidence/promotion.rs
  - tools/xtask/src/main.rs
  - tools/xtask/tests/phase13_promotion_contract.rs
  - reference/artifacts/phase9/lifecycle-contact-witnesses.json
  - reference/artifacts/phase9/lifecycle-contact-witnesses.provenance.json
  - reference/artifacts/catalog/rigid-stack-v1.replay-evidence.json
  - reference/artifacts/phase13/promotion-receipt.json
  - crates/liquidfun-differential/src/fixtures/replay/catalog.rs
  - reference/source-map.toml
  - reference/artifacts/manifest.toml
autonomous: false
requirements:
  - FND-04
  - COMP-04
  - COMP-05
  - COMP-08
  - TEST-07
  - EXMP-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-07-25T18-25-02
generated_at: 2026-07-25T18:25:02.901Z
must_haves:
  truths:
    - "Reviewed promotion consumes only the immutable canonical bundle identified by producer SHA P and bundle digest B."
    - "The actual clean promotion base R may include required GSD metadata and promoter commits after P, but P is its ancestor and every producer-affecting closure recomputed at R is byte-identical to P."
    - "The Phase 9 witness/provenance pair, replay evidence/identity, promotion receipt, source map, and artifact ledger are promoted as one validated transaction."
    - "An independently identified human reviewer acknowledges the exact deterministic seven-file diff digest before any tracked replacement."
    - "The tracked receipt records P, B, R, promoted file-set digest, acquisition metadata, the independently identified reviewer, and Q's required first-parent/trailer contract without claiming a future Q SHA or A."
    - "The promotion commit Q has first parent R and exact P/B/R trailers; final acceptance remains a later identity."
  artifacts:
    - path: reference/artifacts/phase9/lifecycle-contact-witnesses.provenance.json
      provides: "Reviewed scoped-materials witness provenance produced at P"
    - path: reference/artifacts/catalog/rigid-stack-v1.replay-evidence.json
      provides: "Reviewed replay evidence binding sealed input, two D0 runs, and pinned-oracle D1"
    - path: reference/artifacts/phase13/promotion-receipt.json
      provides: "Non-circular P/B/R/file-set/reviewer/acquisition identity and Q relationship contract"
    - path: reference/source-map.toml
      provides: "Source revision, derivation/alteration, and notice mappings for every promoted artifact"
    - path: reference/artifacts/manifest.toml
      provides: "Reviewed hashes, generator/source identities, build metadata, and notice refs for every promoted artifact"
    - path: tools/xtask/src/phase13_evidence/promotion.rs
      provides: "Transactional prepare/validate/promote implementation with seven-file staging, exact-diff review binding, and atomic replacement"
    - path: tools/xtask/tests/phase13_promotion_contract.rs
      provides: "Contract tests for P/R closure checks, exact seven-file diff, independent acknowledgement, rollback, and Q readiness"
  key_links:
    - from: reference/artifacts/phase13/promotion-receipt.json
      to: reference/artifacts/manifest.toml
      via: "Promoted file-set entries and artifact hashes share P and B"
      pattern: "producer_sha|bundle_sha256"
    - from: crates/liquidfun-differential/src/fixtures/replay/catalog.rs
      to: reference/artifacts/catalog/rigid-stack-v1.replay-evidence.json
      via: "Accepted identity is authorized by reviewed D0/D1 evidence"
      pattern: "replay-evidence"
    - from: reference/artifacts/manifest.toml
      to: reference/source-map.toml
      via: "Every artifact row resolves an upstream source/revision, alteration classification, and notice reference"
      pattern: "notice_refs"
    - from: tools/xtask/src/phase13_evidence/promotion.rs
      to: reference/artifacts/phase13/promotion-receipt.json
      via: "Prepare stages all seven replacements at R; promote consumes the independent acknowledgement for the same exact diff digest before atomic replacement"
      pattern: "promotion_base_sha|review_diff_sha256|review_ack"
---

<objective>
Consume, review, and atomically promote the canonical staged bundle while establishing a non-circular promotion identity.

Purpose: Turn independently produced evidence into tracked witness/replay authority with complete FND-04 traceability, independent review, and a verifiable P → R → Q promotion relationship.
Output: Tested transactional promoter, independently acknowledged exact seven-file diff, regenerated reviewed artifacts, corrected replay authority when diagnosis permits it, promotion receipt, source map, and artifact ledger.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/13-restore-evidence-and-replay-integrity/13-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-restore-evidence-and-replay-integrity/13-03-SUMMARY.md
@tools/xtask/src/phase13_evidence.rs
@tools/xtask/src/phase13_evidence/bundle.rs
@reference/source-map.toml
@reference/artifacts/manifest.toml
@crates/liquidfun-differential/src/fixtures/replay/catalog.rs

<interfaces>
Promotion identity contract:

- P is the producer SHA recorded inside the validated bundle.
- B is recomputed from downloaded bytes and must equal the expected internal digest and the canonical workflow artifact metadata.
- R is the actual clean HEAD after every required GSD metadata commit and the promoter implementation task commit. Promotion requires P to be an ancestor of R and mechanically recomputed witness/replay producer-affecting closures at R to be byte-identical to the closures recorded at P.
- The tracked receipt records P, B, R, acquisition IDs/provider digest, the independently identified reviewer, promoted file-set digest, and Q's required first-parent/trailer relationship. It MUST NOT record a Q SHA or A because neither exists when the receipt bytes are reviewed. The exact diff digest and signed/text acknowledgement remain in the untracked review packet and acknowledgement inputs so adding the acknowledgement cannot change the reviewed seven-file diff.
- The executor commits the complete seven-file transaction as Q whose first parent is R and whose message has exact `Phase13-Producer-SHA: P`, `Phase13-Bundle-SHA256: B`, and `Phase13-Promotion-Base-SHA: R` trailers. Plan 13-05 discovers Q from ancestry, verifies P → R → Q and the promoted paths, and emits final acceptance at descendant SHA A.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the transactional promoter and prepare the exact seven-file review diff at R</name>
  <read-first>
    <file>.planning/phases/13-restore-evidence-and-replay-integrity/13-CONTEXT.md</file>
    <file>.planning/phases/13-restore-evidence-and-replay-integrity/13-03-SUMMARY.md</file>
    <file>tools/xtask/src/phase13_evidence.rs</file>
    <file>tools/xtask/src/phase13_evidence/bundle.rs</file>
    <file>reference/artifacts/phase9/lifecycle-contact-witnesses.json</file>
    <file>reference/artifacts/phase9/lifecycle-contact-witnesses.provenance.json</file>
    <file>crates/liquidfun-differential/src/fixtures/replay/catalog.rs</file>
    <file>reference/source-map.toml</file>
    <file>reference/artifacts/manifest.toml</file>
    <file>THIRD_PARTY_NOTICES.md</file>
  </read-first>
  <files>tools/xtask/src/phase13_evidence.rs, tools/xtask/src/phase13_evidence/promotion.rs, tools/xtask/src/main.rs, tools/xtask/tests/phase13_promotion_contract.rs</files>
  <behavior>
    - "`cargo xtask phase13 evidence prepare` downloads/acquires by immutable identifiers, validates provider metadata and B, stages exactly seven final replacements in a temporary directory, and never changes tracked files."
    - "Preparation captures the clean full HEAD as R, proves P is an ancestor of R, and requires the mechanically recomputed producer-affecting witness/replay closures at R to be byte-identical to P."
    - "The staged receipt records P, B, R, the independently identified reviewer, and Q's required first-parent/trailer relationship without a Q SHA or A; every staged artifact and ledger row has complete source/revision, derivation/alteration, notice, generator, oracle, build, and promoted-byte identities."
    - "The promoter emits one canonical seven-file diff and SHA-256 digest; extras, omissions, symlinks, path escapes, stale hashes, acknowledgement mismatch, or any pre-acknowledgement tracked write fail closed."
    - "`cargo xtask phase13 evidence promote` revalidates the same temporary tree, exact diff digest, and acknowledgement, then atomically replaces exactly seven tracked files or rolls all seven back."
  </behavior>
  <action>
    Implement the Plan 13-04-owned transactional promoter in the listed xtask files; Plan 13-03 producer/check commands remain write-free. Separate `prepare` from `promote`. `prepare` acquires the canonical bundle by exact run and artifact ID/name, validates provider digest, P, B, canonical identities, D0/D1 results, and the complete seven-path set, then builds all final replacement bytes under a newly created temporary directory. Require the independently identified reviewer ID as preparation input so the final receipt can contain it before the diff is reviewed. Per D-04 through D-07, update accepted identity only when diagnosis and pinned-oracle D1 authorize it; otherwise preserve the historical physics projection and validate expanded checkpoint fields separately. Per FND-04 and D-03, stage complete source-map and artifact-manifest rows plus a receipt containing P/B/R, acquisition metadata, promoted-set digest, reviewer identity, and Q's required first-parent/trailer contract but no Q SHA or A. Before preparation, the promoter requires a clean full R, proves P is an ancestor of R, and mechanically recomputes the same producer-affecting closures recorded at P with byte-identical results. Render one deterministic diff over exactly the seven tracked replacement paths and emit its digest plus a review packet; keep the acknowledgement external so it cannot alter the reviewed bytes. `promote` must require a recorded independent-review acknowledgement for that exact digest, revalidate all staged bytes/ledgers/digests immediately before replacement, atomically replace exactly seven tracked files, and roll back all seven on any failure. Add focused Arrange/Act/Assert tests for P-not-ancestor-of-R, closure drift, path-set mismatch, stale ledger hashes, diff mismatch, missing/wrong acknowledgement, partial replacement failure, and successful rollback; focused failures may be observed locally but no failing state may be committed. Run the full ordered Rust checks before committing the promoter implementation task. After that ordinary task commit and any required GSD metadata commits, capture the resulting clean HEAD as R and run `prepare`; the review packet must therefore contain the actual committed R that will be Q's first parent.
  </action>
  <verify>
    <automated>cargo test -p xtask --test phase13_promotion_contract &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; cargo xtask phase13 evidence prepare --bundle target/phase13/acquired --expected-producer-sha "$PHASE13_PRODUCER_SHA" --expected-bundle-sha256 "$PHASE13_BUNDLE_SHA256" --reviewer-id "$PHASE13_INDEPENDENT_REVIEWER_ID" --review-packet target/phase13/promotion-review.json &amp;&amp; git diff --quiet</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The tested promoter owns the transactional write path; producer/check commands remain write-free.</criterion>
    <criterion>The clean actual R is a descendant of P and its producer-affecting closures are byte-identical to P.</criterion>
    <criterion>Preparation leaves tracked files untouched and produces exactly seven staged replacements plus one deterministic diff digest.</criterion>
    <criterion>The staged receipt records P/B/R and the Q relationship contract without a Q SHA or A, and every staged source-map/artifact row is complete and matches staged bytes.</criterion>
  </acceptance-criteria>
  <done>The transactional promoter is committed, R is established, and one exact validated seven-file diff is ready for independent review without tracked replacement.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Obtain independent acknowledgement of the exact prepared diff digest</name>
  <files>target/phase13/promotion-review.json, target/phase13/independent-review-ack.json</files>
  <action>An independently identified human reviewer—not the implementing agent and not an auto-approval mechanism—must inspect the complete deterministic seven-file diff from `target/phase13/promotion-review.json`, verify the displayed P/B/R identities and producer-closure equality, and provide a recorded acknowledgement bound to that exact diff SHA-256. Store the reviewer identity, exact digest, acknowledgement text or signature, and review time in the review-ack input consumed by Task 3. If any byte changes, discard the acknowledgement, regenerate the review packet, and obtain a new independent review.</action>
  <verify>
    <automated>cargo xtask phase13 evidence review-ack check --review-packet target/phase13/promotion-review.json --ack target/phase13/independent-review-ack.json &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated>
  </verify>
  <done>An independently identified reviewer has acknowledged the exact prepared seven-file diff digest, and the acknowledgement validates against the unchanged review packet.</done>
</task>

<task type="auto">
  <name>Task 3: Consume the acknowledgement, atomically promote seven files, and commit Q on R</name>
  <read-first>
    <file>target/phase13/promotion-review.json</file>
    <file>target/phase13/independent-review-ack.json</file>
    <file>tools/xtask/src/phase13_evidence/promotion.rs</file>
    <file>tools/xtask/tests/phase13_promotion_contract.rs</file>
  </read-first>
  <files>reference/artifacts/phase9/lifecycle-contact-witnesses.json, reference/artifacts/phase9/lifecycle-contact-witnesses.provenance.json, reference/artifacts/catalog/rigid-stack-v1.replay-evidence.json, reference/artifacts/phase13/promotion-receipt.json, crates/liquidfun-differential/src/fixtures/replay/catalog.rs, reference/source-map.toml, reference/artifacts/manifest.toml</files>
  <behavior>
    - "Promotion refuses a dirty or changed R, a non-descendant R, producer-closure drift, a changed seven-file diff digest, or an acknowledgement from any identity other than the recorded independent reviewer."
    - "All seven tracked replacements appear together with no eighth path, and all seven revert to their pre-promotion bytes if any replacement or post-write validation fails."
    - "Q has first parent R and exact P/B/R trailers; the receipt remains non-circular and records no Q or A."
  </behavior>
  <action>
    Re-run `prepare` from the immutable acquisition tuple at the unchanged clean R and same independently identified reviewer, require its exact seven-file diff digest to match both the prior review packet and the independent acknowledgement, and invoke the tested transactional `promote` command. Immediately validate scoped provenance, replay evidence, source map, artifact manifest, receipt, promoted hashes, and absence of unrelated paths. Run the focused promotion checks followed by the full ordered Rust sequence, then run `promotion-ready` to prove formatting/tests changed no reviewed byte and the exact diff digest and acknowledgement still match before committing. Commit exactly the seven promoted paths once as Q with first parent R and exact trailers `Phase13-Producer-SHA: P`, `Phase13-Bundle-SHA256: B`, and `Phase13-Promotion-Base-SHA: R`; do not include promoter code or GSD metadata in Q. After commit, mechanically verify Q's first parent and trailers, but do not amend the reviewed receipt with Q SHA or A. Do not run a local/D2 producer, copy assertion output, manually edit a hash, accept native-only evidence, or split authority and ledger updates across commits.</action>
  <verify>
    <automated>cargo xtask phase13 evidence promote --review-packet target/phase13/promotion-review.json --review-ack target/phase13/independent-review-ack.json &amp;&amp; cargo xtask provenance check &amp;&amp; cargo xtask phase13 evidence check --tracked --require-reviewed &amp;&amp; cargo test -p xtask --test phase13_promotion_contract &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; cargo xtask phase13 evidence promotion-ready --review-packet target/phase13/promotion-review.json --review-ack target/phase13/independent-review-ack.json</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Acquired metadata, P, B, R, canonical identities, D0/D1 results, exact diff digest, and independent acknowledgement all validate before replacement.</criterion>
    <criterion>All seven authority and ledger files update in Q or all remain unchanged; no unrelated path is committed.</criterion>
    <criterion>Every new or regenerated artifact has complete source/revision, derivation/alteration, and notice traceability, and manifest hashes equal tracked bytes.</criterion>
    <criterion>Q has first parent R and exact P/B/R trailers without embedding Q or A in pre-commit evidence.</criterion>
    <criterion>No expected value originates from Rust assertion, native-only, or D2 output.</criterion>
  </acceptance-criteria>
  <done>The independently reviewed witness, replay authority, receipt, source map, and artifact ledger are promoted from P/B at R as one non-circular commit Q.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Workflow artifact service to local acquisition | Provider metadata and bytes are untrusted until matched to P and B. |
| Staged bundle to tracked artifacts | Review and complete-set validation precede atomic replacement. |
| Promoted evidence to source/notice ledgers | Every artifact must retain derivation, alteration, and notice obligations. |
| P to promotion base R | Required GSD/promoter commits may intervene, but producer-affecting closures must remain byte-identical. |
| Review packet to tracked replacement | An independently identified human acknowledgement must bind the exact seven-file diff digest. |
| Pre-commit receipt to Q | Q cannot be self-recorded; P → R → Q ancestry, tree digest, and trailers bind it afterward. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13-04-01 | Spoofing | Acquired artifact | mitigate | Require exact run/artifact IDs, provider digest, P, B, and canonical producer metadata. |
| T-13-04-02 | Tampering | Atomic promotion | mitigate | Stage in a temporary directory, revalidate exactly seven replacements and both ledgers immediately before replacement, and roll back all seven on any failure. |
| T-13-04-03 | Repudiation | Review and Q | mitigate | Require an independently identified human acknowledgement bound to the exact diff digest, then bind Q through first parent R, tree digest, and P/B/R trailers. |
| T-13-04-04 | Elevation of Privilege | Native output | mitigate | Require reviewed pinned-oracle D1 and reject native/Rust/D2 authority. |
| T-13-04-05 | Information Disclosure | Receipt/ledgers | mitigate | Record reproducibility metadata only and exclude secrets or raw environment dumps. |
| T-13-04-06 | Denial of Service | Partial promotion | mitigate | Use a bounded staged transaction with rollback and deterministic failure reports. |
</threat-model>

<verification>
Implement and test the transactional promoter, commit it, then establish the actual clean R and prove P is its ancestor and both producer-affecting closures are byte-identical to P. Prepare one exact seven-file diff, obtain independent human acknowledgement bound to its digest, and only then atomically promote. Before each task commit—including Q—run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Commit the promoted set once as Q with first parent R and exact P/B/R trailers, then verify no unrelated tracked paths changed.
</verification>

<success-criteria>
1. Promotion consumes the immutable canonical P/B bundle through the tested acquisition path at an actual clean R where P is an ancestor and producer-affecting closures equal P.
1. Witness/provenance, replay authority, receipt, source map, and manifest validate as one reviewed transaction.
1. An independently identified human reviewer acknowledges the exact deterministic seven-file diff digest before replacement.
1. Every new/regenerated artifact satisfies FND-04 source, alteration, and notice traceability.
1. Q has first parent R and is bound by P/B/R ancestry, tree, and trailers without a circular self-SHA claim.
1. No Rust/native/D2 output or manual hash edit can bless expected output.
1. Final exact-head acceptance identity is deferred only to Plan 13-05, after Q exists.
</success-criteria>

<output>
After completion, create `.planning/phases/13-restore-evidence-and-replay-integrity/13-04-SUMMARY.md`.
</output>
