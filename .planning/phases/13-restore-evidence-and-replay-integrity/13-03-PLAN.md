---
phase: 13-restore-evidence-and-replay-integrity
plan: '03'
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified:
  - tools/xtask/src/phase13_evidence.rs
  - tools/xtask/src/phase13_evidence/bundle.rs
  - tools/xtask/src/main.rs
  - tools/xtask/tests/phase13_evidence_contract.rs
  - .github/workflows/phase13-evidence-producer.yml
autonomous: true
requirements:
  - FND-04
  - COMP-04
  - COMP-05
  - COMP-08
  - TEST-07
  - EXMP-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-07-25T18-25-02
generated_at: 2026-07-25T18:25:02.901Z
must_haves:
  truths:
    - "A dedicated canonical Linux workflow executes the Phase 13 producer at one clean full producer SHA."
    - "The producer emits an immutable staged bundle only after two byte-identical pinned-oracle witness runs, two byte-identical native D0 runs over one sealed input, and a passing pinned-oracle D1 comparison."
    - "The staged bundle has one canonical digest over its manifest and ordered file set and contains no tracked-artifact promotion side effect."
    - "Every staged evidence item carries upstream source/revision, derivation/alteration classification, and notice references."
    - "The bundle records mechanically recomputable witness and replay producer-affecting closures at P so a later clean promotion base R can prove byte identity without requiring R to equal P."
  artifacts:
    - path: tools/xtask/src/phase13_evidence.rs
      provides: "Typed canonical producer, acquisition, and read-only bundle validator; tracked replacement remains unavailable"
    - path: tools/xtask/src/phase13_evidence/bundle.rs
      provides: "Canonical staged-bundle manifest, digest, source/notice metadata, and identity validation"
    - path: tools/xtask/tests/phase13_evidence_contract.rs
      provides: "Producer, bundle, acquisition, and no-promotion contract tests"
    - path: .github/workflows/phase13-evidence-producer.yml
      provides: "Executable canonical Linux producer and immutable artifact publication path"
  key_links:
    - from: .github/workflows/phase13-evidence-producer.yml
      to: tools/xtask/src/phase13_evidence.rs
      via: "One aggregate producer invocation at github.sha with pinned canonical tools"
      pattern: "cargo xtask phase13 evidence produce"
    - from: tools/xtask/src/phase13_evidence.rs
      to: tools/xtask/src/provenance/phase9_witness.rs
      via: "Mechanically closed materials identity and staged pair validation"
      pattern: "phase9"
    - from: tools/xtask/src/phase13_evidence/bundle.rs
      to: reference/source-map.toml
      via: "Each bundle entry resolves source revision/path, derivation/alteration class, and notice refs"
      pattern: "notice_refs|derivation"
---

<objective>
Create the canonical Phase 13 evidence producer and dedicated Linux workflow that emits, but never promotes, an immutable staged bundle.

Purpose: Establish independent D0/D1 evidence at a source-bound producer SHA before any reviewed repository artifact changes.
Output: Typed producer/bundle tooling, contract tests, and a dedicated canonical workflow with a durable artifact acquisition path.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/13-restore-evidence-and-replay-integrity/13-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-restore-evidence-and-replay-integrity/13-01-SUMMARY.md
@.planning/phases/13-restore-evidence-and-replay-integrity/13-02-SUMMARY.md
@tools/xtask/src/provenance/phase9_witness.rs
@tools/reference/phase9-lifecycle-contact-witness.materials.json
@crates/liquidfun-differential/src/fixtures/replay/diagnosis.rs
@crates/liquidfun-differential/src/runner/catalog.rs
@.github/workflows/oracle.yml

<interfaces>
The identity chain is intentionally non-circular:

- `producer_sha` (P): the clean full Git SHA whose mechanically derived witness and replay input closures are executed by canonical Linux.
- `bundle_sha256` (B): SHA-256 over the versioned canonical bundle manifest plus ordered `{path, sha256}` entries; the digest field itself is excluded from its preimage.
- This plan emits P, B, and the canonical producer-affecting closures at P only. It does not claim a promotion base, promotion commit, or final acceptance SHA.
- Plan 13-04 establishes the actual clean promotion base R after required GSD metadata and promoter commits, requires P to be an ancestor of R, and requires the same closures recomputed at R to be byte-identical to P before creating Q with first parent R.
- Plan 13-05 verifies the complete P → R → Q → A ancestry and emits the final acceptance identity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the canonical producer and immutable staged-bundle contract</name>
  <read-first>
    <file>.planning/phases/13-restore-evidence-and-replay-integrity/13-CONTEXT.md</file>
    <file>.planning/phases/13-restore-evidence-and-replay-integrity/13-01-SUMMARY.md</file>
    <file>.planning/phases/13-restore-evidence-and-replay-integrity/13-02-SUMMARY.md</file>
    <file>tools/xtask/src/main.rs</file>
    <file>tools/xtask/src/provenance/phase9_witness.rs</file>
    <file>crates/liquidfun-differential/src/fixtures/replay/diagnosis.rs</file>
    <file>crates/liquidfun-differential/src/runner/catalog.rs</file>
    <file>reference/source-map.toml</file>
    <file>reference/artifacts/manifest.toml</file>
  </read-first>
  <files>tools/xtask/src/phase13_evidence.rs, tools/xtask/src/phase13_evidence/bundle.rs, tools/xtask/src/main.rs, tools/xtask/tests/phase13_evidence_contract.rs</files>
  <behavior>
    - "Producer mode requires clean canonical Linux, exact P, pinned upstream SHA, canonical compiler/target/preset, and sealed resolved scenario bytes."
    - "Witness generation runs the pinned C++ oracle twice and rejects non-byte-identical output."
    - "Replay generation runs native D0 twice over the same sealed bytes, rejects non-byte-identical output, then requires pinned-oracle D1 over those exact bytes."
    - "The canonical B changes for any manifest, evidence, source/notice metadata, or staged file change and rejects traversal, duplicates, extras, omissions, and symlinks."
    - "Produce and check modes cannot write repository-tracked artifact paths."
  </behavior>
  <action>
    Implement D-02, D-03, D-05, D-06, and D-07 with a pure typed bundle/identity core and a thin effectful xtask shell, following the repository architecture and Rust standards. Record P, upstream SHA, both mechanically derived producer-affecting input closures at P, scoped materials/probe/input digests, exact invocations, compiler/build identity, schema/tolerance identities, two witness digests, two D0 digests, D1 oracle identity/result, first-divergence diagnosis, and per-item upstream path/revision, derivation/alteration class, and notice refs. Define those closures narrowly but mechanically around every input capable of changing witness or replay evidence, while excluding GSD metadata and the separate promotion-only implementation; Plan 13-04 must recompute the same closures at R and require byte identity. Reuse Plan 13-02's diagnosis to choose expanded identity versus versioned legacy projection. Write only beneath a caller-selected staging root, compute B over a canonical ordered manifest/file set, and expose only producer, acquisition, and read-only `check` validators for Plan 13-04. The transactional promoter and its tests belong exclusively to Plan 13-04; no command in this plan may replace tracked files. Add one-concern Arrange/Act/Assert tests before implementation for repeat mismatch, D1 failure, wrong P/environment, malformed FND-04 metadata, tampering, path escape, closure drift, and any write attempt to tracked artifact paths; focused failures may be observed locally but no failing state may be committed. Never accept Rust assertion output, native-only output, or D2 output as independent authority.
  </action>
  <verify>
    <automated>cargo test -p xtask --test phase13_evidence_contract producer_ &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated>
  </verify>
  <acceptance-criteria>
    <criterion>Tests prove the two-repeat, same-input D0/D1, canonical-environment, and no-self-blessing gates.</criterion>
    <criterion>P and B are unambiguous full identities, and B covers every staged byte without hashing itself.</criterion>
    <criterion>Every staged evidence item satisfies the FND-04 source/revision, derivation/alteration, and notice contract.</criterion>
    <criterion>No command in this plan can mutate tracked witness, replay, source-map, or artifact-manifest paths.</criterion>
  </acceptance-criteria>
  <done>The producer can create and validate one immutable, independently evidenced staged bundle without promotion.</done>
</task>

<task type="auto">
  <name>Task 2: Add the executable canonical Linux producer workflow and acquisition contract</name>
  <read-first>
    <file>tools/xtask/src/phase13_evidence.rs</file>
    <file>tools/xtask/src/phase13_evidence/bundle.rs</file>
    <file>tools/xtask/tests/phase13_evidence_contract.rs</file>
    <file>.github/workflows/oracle.yml</file>
    <file>.github/workflows/platform.yml</file>
  </read-first>
  <files>.github/workflows/phase13-evidence-producer.yml, tools/xtask/tests/phase13_evidence_contract.rs</files>
  <behavior>
    - "Workflow dispatch checks out exactly github.sha with the pinned oracle and canonical Linux toolchain, then invokes one producer command."
    - "The workflow uploads a uniquely named immutable bundle and publishes run ID, artifact ID/name, provider digest, P, and B for later acquisition."
    - "Contract tests reject mutable actions, ambiguous checkout identity, missing canonical tool checks, tracked promotion, or missing artifact metadata."
  </behavior>
  <action>
    Create the dedicated canonical producer workflow before any promotion. Reuse the exact pinned checkout/upload actions and canonical Rust/CMake/Ninja/Clang identities from Oracle CI; assert `git rev-parse HEAD == github.sha` and a clean worktree before production. Run the aggregate producer into `target/phase13/staged`, validate it, upload it with a run/P-qualified immutable name and nonzero retention, and expose the upload action's artifact ID, URL/provider digest plus internal B in the job summary/output. The later acquisition contract is `producer run id + artifact id/name + expected P + expected B`; Plan 13-04 must download by those immutable identifiers and validate both provider metadata and internal bytes. Do not edit tracked reference artifacts or add cross-workflow promotion.
  </action>
  <verify>
    <automated>cargo test -p xtask --test phase13_evidence_contract workflow_ &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The dedicated workflow is executable before promotion and produces a downloadable immutable artifact.</criterion>
    <criterion>Action pins, exact checkout identity, canonical tools, unique artifact identity, retention, and no-promotion behavior are mechanically tested.</criterion>
    <criterion>The acquisition tuple is sufficient for Plan 13-04 to fetch and reject a substituted bundle.</criterion>
  </acceptance-criteria>
  <done>Canonical Linux can publish an immutable staged bundle bound to P and B through a tested acquisition path.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Canonical workflow checkout to producer | P and tool identities must be exact and source state clean. |
| Native D0 to pinned-oracle D1 | Native output is observed data; the independent oracle is the cross-implementation gate. |
| Staging root to uploaded artifact | Every file and metadata record must be confined, canonicalized, and covered by B. |
| Workflow artifact metadata to later acquisition | Run/artifact identifiers and provider digest are untrusted until cross-checked with P and B. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13-03-01 | Spoofing | Canonical producer | mitigate | Verify clean full P, upstream SHA, OS/target, and pinned tool identities before effects. |
| T-13-03-02 | Tampering | Staged bundle | mitigate | Canonically hash the manifest and ordered file set; reject extras, omissions, traversal, symlinks, and digest mismatch. |
| T-13-03-03 | Repudiation | Producer run | mitigate | Publish run/artifact IDs, provider digest, P, B, exact invocations, and tool identities. |
| T-13-03-04 | Elevation of Privilege | Native output | mitigate | Require pinned-oracle D1 and prohibit native/Rust/D2 output from becoming independent expected values. |
| T-13-03-05 | Information Disclosure | Uploaded bundle | mitigate | Include semantic/reproducibility records only; reject secrets, raw memory, and environment dumps. |
| T-13-03-06 | Denial of Service | Repeats/oracle | mitigate | Reuse bounded scenario/oracle timeouts and retain the exact failing request. |
</threat-model>

<verification>
Run the focused producer/workflow tests and inspect the workflow contract. Before the plan commit, run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then dispatch the canonical workflow at P and confirm it publishes one immutable staged bundle with P, B, run/artifact identifiers, provider digest, and no tracked repository diff.
</verification>

<success-criteria>
1. A canonical Linux producer exists and runs before any tracked promotion.
1. Witness and replay evidence satisfy the two-repeat, same-input, and pinned-oracle D1 gates.
1. P, B, and the canonical producer-affecting closures bind source/tool/input/evidence identities without claiming R, Q, or A.
1. The workflow exposes a tested immutable artifact acquisition path.
1. Every staged evidence item has FND-04 source, alteration, and notice traceability.
1. No tracked artifact is promoted and no Rust/native/D2 output self-blesses an expectation.
</success-criteria>

<output>
After completion, create `.planning/phases/13-restore-evidence-and-replay-integrity/13-03-SUMMARY.md`.
</output>
