---
phase: 13-restore-evidence-and-replay-integrity
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun-differential/src/fixtures/replay/diagnosis.rs
  - crates/liquidfun-differential/src/fixtures/replay/catalog.rs
  - crates/liquidfun-differential/src/runner/catalog.rs
  - crates/liquidfun-differential/tests/catalog_regressions.rs
autonomous: true
requirements:
  - COMP-08
  - TEST-07
  - EXMP-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-07-25T18-25-02
generated_at: 2026-07-25T18:25:02.901Z
must_haves:
  truths:
    - "A `rigid-stack-v1` replay failure reports the first changed semantic path and both compared values."
    - "Maintainers can distinguish physics drift, resolved-scenario drift, and checkpoint/capture-schema drift before changing reviewed identity."
    - "The diagnosis uses the sealed resolved scenario bytes as input authority and does not edit the regression manifest."
  artifacts:
    - path: crates/liquidfun-differential/src/fixtures/replay/diagnosis.rs
      provides: "Typed first-divergence diagnosis and three-way drift classification"
    - path: crates/liquidfun-differential/src/fixtures/replay/catalog.rs
      provides: "Replay integration that preserves resolved bytes and emits structured diagnosis"
    - path: crates/liquidfun-differential/tests/catalog_regressions.rs
      provides: "Regression tests for changed semantic paths and drift classification"
  key_links:
    - from: crates/liquidfun-differential/src/runner/catalog.rs
      to: crates/liquidfun-differential/src/fixtures/replay/diagnosis.rs
      via: "Sealed resolved bytes plus current and reviewed semantic checkpoints"
      pattern: "diagnos"
    - from: crates/liquidfun-differential/src/fixtures/replay/catalog.rs
      to: crates/liquidfun-differential/tests/catalog_regressions.rs
      via: "Structured replay failure asserted by semantic path and classification"
      pattern: "rigid-stack-v1"
---

<objective>
Build root-cause diagnosis for the reviewed `rigid-stack-v1` replay failure before any identity or manifest change.

Purpose: Establish whether exact-head drift is a physics defect, resolved-scenario drift, or intentional capture-contract expansion, with the first changed semantic path as reviewable evidence.
Output: Typed diagnosis tooling and focused tests. This plan must not edit the accepted replay identity or promote evidence.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/13-restore-evidence-and-replay-integrity/13-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@scenarios/catalog/rigid-stack-v1.json
@crates/liquidfun-differential/src/runner/catalog.rs
@crates/liquidfun-differential/src/fixtures/replay/catalog.rs
@crates/liquidfun-differential/src/failure_bundle/catalog.rs
@crates/liquidfun-differential/tests/catalog_regressions.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add semantic-path replay diagnosis and drift taxonomy</name>
  <read-first>
    <file>.planning/phases/13-restore-evidence-and-replay-integrity/13-CONTEXT.md</file>
    <file>scenarios/catalog/rigid-stack-v1.json</file>
    <file>crates/liquidfun-differential/src/runner/catalog.rs</file>
    <file>crates/liquidfun-differential/src/fixtures/replay/catalog.rs</file>
    <file>crates/liquidfun-differential/src/failure_bundle/catalog.rs</file>
    <file>crates/liquidfun-differential/tests/catalog_regressions.rs</file>
  </read-first>
  <files>crates/liquidfun-differential/src/fixtures/replay/diagnosis.rs, crates/liquidfun-differential/src/fixtures/replay/catalog.rs, crates/liquidfun-differential/src/runner/catalog.rs, crates/liquidfun-differential/tests/catalog_regressions.rs</files>
  <behavior>
    - "Resolved byte drift is reported before checkpoint comparison and classified as resolved-scenario drift."
    - "With identical resolved bytes, parity-bearing state/event divergence is classified as physics drift."
    - "With identical resolved bytes and legacy physics projection, newly added checkpoint/debug fields are classified as capture-schema drift."
    - "Every mismatch includes the first semantic JSON path, reviewed value, current value, schema identities, and resolved input digest."
  </behavior>
  <action>
    Implement D-04 as a typed diagnostic record and deterministic classifier. Reuse the existing exact resolved-byte runner, comparator path ordering, replay manifest, and failure-bundle semantics; do not create a second scenario resolver. Compare input identity first, then versioned semantic projections, then current expanded checkpoints so the category is evidence-derived rather than guessed. Persist or print enough structured data to replay the exact request and identify the first changed semantic path. Add focused Arrange/Act/Assert tests before implementation for all three drift classes and malformed/incomparable schema cases; those tests may fail locally before implementation but no failing state may be committed.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-differential --test catalog_regressions diagnosis_ &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated>
  </verify>
  <acceptance-criteria>
    <criterion>All three root-cause categories are mutually exclusive, evidence-backed, and covered by tests.</criterion>
    <criterion>Diagnosis retains the exact sealed resolved bytes/digest and first divergent semantic path.</criterion>
    <criterion>Unknown or incompatible schema versions fail as harness/schema errors rather than being mislabeled physics drift.</criterion>
  </acceptance-criteria>
  <done>The replay path can explain the first divergence and classify its authority boundary without changing reviewed identity.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the exact-head rigid-stack cause without re-baselining</name>
  <read-first>
    <file>crates/liquidfun-differential/src/fixtures/replay/diagnosis.rs</file>
    <file>crates/liquidfun-differential/src/fixtures/replay/catalog.rs</file>
    <file>crates/liquidfun-differential/src/runner/catalog.rs</file>
    <file>crates/liquidfun-differential/tests/catalog_regressions.rs</file>
    <file>scenarios/catalog/rigid-stack-v1.json</file>
  </read-first>
  <files>crates/liquidfun-differential/src/fixtures/replay/diagnosis.rs, crates/liquidfun-differential/tests/catalog_regressions.rs</files>
  <behavior>
    - "The exact `rigid-stack-v1` failure produces a stable diagnosis record that names its first changed semantic path."
    - "Unchanged resolved bytes do not by themselves authorize a new identity."
    - "The existing reviewed manifest value remains byte-for-byte unchanged."
  </behavior>
  <action>
    Exercise D-04, D-06, and D-07 against `rigid-stack-v1` and encode the proven classification as regression expectations over diagnosis behavior, not as a new accepted digest. If the evidence shows capture-schema expansion, test both the versioned legacy physics projection and the expanded checkpoint separately while retaining every parity-bearing field. If it shows physics or scenario drift, make that failure explicit for Plan 13-03 instead of masking it. Remove or replace any helper that treats the native catalog backend or Rust assertion output as independent evidence. Do not edit the tracked accepted identity, do not promote a fixture, and do not copy `0bcba508...d1d2372` or any Rust-emitted value into an expectation.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-differential --test catalog_regressions rigid_stack_v1_diagnosis -- --exact &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The exact-head failure is explained by a stable first semantic path and one evidence-supported drift class.</criterion>
    <criterion>No reviewed replay identity or manifest entry changes in this plan.</criterion>
    <criterion>No expected value originates from Rust assertion output, the native catalog backend, or a D2 local run.</criterion>
  </acceptance-criteria>
  <done>The root cause is reproducibly diagnosed and ready for canonical independent evidence generation without self-blessing.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Scenario source to resolved bytes | The source must resolve once and the exact bytes must be sealed before replay. |
| Native trace to diagnosis | Native output is untrusted observed data, never an independent expected value. |
| Reviewed identity to current checkpoint | Schema evolution must not silently redefine historical physics authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13-02-01 | Tampering | Resolved scenario bytes | mitigate | Compare and record exact resolved-byte digest before semantic diagnosis. |
| T-13-02-02 | Spoofing | Native output as independent oracle | mitigate | Ban native/self-reported values from accepted expectations and label all evidence origins. |
| T-13-02-03 | Repudiation | Root-cause classification | mitigate | Record first semantic path, both values, schema versions, input digest, and deterministic category rationale. |
| T-13-02-04 | Information Disclosure | Failure bundles | mitigate | Persist only engine-neutral scenario/checkpoint data and diagnostics; exclude pointers, raw memory, and environment secrets. |
| T-13-02-05 | Denial of Service | Semantic diff | mitigate | Stop at the deterministic first divergence while retaining the exact request for focused replay. |
</threat-model>

<verification>
Run the focused catalog diagnosis tests and inspect the diff to confirm no accepted manifest identity changed. Before the plan commit, run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. The result must explain the exact-head `rigid-stack-v1` mismatch through resolved bytes, versioned semantic projection, and first-divergence evidence.
</verification>

<success-criteria>
1. `rigid-stack-v1` has a deterministic first-divergence diagnosis.
1. The diagnosis distinguishes physics, scenario, and capture-schema drift.
1. Exact resolved bytes remain the single scenario authority.
1. The reviewed manifest is untouched.
1. Rust output, native catalog output, and D2 runs cannot self-bless an expected identity.
1. Phase 14 Windows repair and Phase 15 release-candidate work remain out of scope.
</success-criteria>

<output>
After completion, create `.planning/phases/13-restore-evidence-and-replay-integrity/13-02-SUMMARY.md`.
</output>
