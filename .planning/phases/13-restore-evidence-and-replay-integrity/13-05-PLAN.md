---
phase: 13-restore-evidence-and-replay-integrity
plan: '05'
type: execute
wave: 4
depends_on:
  - 13-04
files_modified:
  - tools/xtask/src/phase13_acceptance.rs
  - tools/xtask/src/main.rs
  - tools/xtask/tests/phase13_acceptance_contract.rs
  - .github/workflows/phase13-acceptance.yml
  - justfile
autonomous: true
requirements:
  - FND-04
  - COMP-04
  - COMP-05
  - COMP-08
  - TEST-07
  - EXMP-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-07-25T18-25-02
generated_at: 2026-07-25T18:25:02.901Z
must_haves:
  truths:
    - "One dedicated canonical Linux gate validates the final checked-out full acceptance SHA A before and after every required step."
    - "The gate derives P, B, promotion base R, and promotion commit Q from tracked evidence/history; proves P is an ancestor of R, producer-affecting closures at R equal P, Q has first parent R with matching tree/trailers, and A descends from Q."
    - "Producer-affecting witness/replay input closures at A equal the closures recorded at P; no circular claim requires promoted evidence to contain Q or A."
    - "A skipped, reordered, missing, or failed provenance, replay, regression, oracle, or comparison step prevents acceptance identity publication."
    - "A mismatch retains the exact failing request and first divergent semantic path as CI artifacts."
  artifacts:
    - path: tools/xtask/src/phase13_acceptance.rs
      provides: "Fail-closed ordered P/B/R/Q/A acceptance state machine and terminal identity record"
    - path: tools/xtask/tests/phase13_acceptance_contract.rs
      provides: "Tests for identity relationships, ordering, completeness, SHA stability, and publication gating"
    - path: .github/workflows/phase13-acceptance.yml
      provides: "Dedicated canonical Linux P-to-R promotion-descendant exact-head acceptance gate"
    - path: justfile
      provides: "Thin contributor entrypoint for the same xtask contract"
  key_links:
    - from: tools/xtask/src/phase13_acceptance.rs
      to: reference/artifacts/phase13/promotion-receipt.json
      via: "Resolve P/B/R/promoted-set identity, then discover and validate Q from Git history"
      pattern: "producer_sha|bundle_sha256|promotion_base_sha"
    - from: .github/workflows/phase13-acceptance.yml
      to: tools/xtask/src/phase13_acceptance.rs
      via: "Single aggregate xtask invocation at exact A"
      pattern: "cargo xtask phase13 acceptance"
    - from: justfile
      to: tools/xtask/src/phase13_acceptance.rs
      via: "Thin recipe with no orchestration logic"
      pattern: "cargo xtask"
---

<objective>
Add the final promotion-descendant exact-head Phase 13 Linux acceptance gate.

Purpose: Revalidate tracked provenance, replay, regressions, and the current oracle surface at A while proving the non-circular P/B evidence and P → R → Q → A ancestry relationship.
Output: Typed acceptance state machine, dedicated workflow, thin contributor command, contract tests, and success-only terminal identity.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/13-restore-evidence-and-replay-integrity/13-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-restore-evidence-and-replay-integrity/13-04-SUMMARY.md
@reference/artifacts/phase13/promotion-receipt.json
@reference/source-map.toml
@reference/artifacts/manifest.toml
@tools/xtask/src/phase13_evidence.rs
@.github/workflows/ci.yml
@.github/workflows/oracle.yml
@justfile

<interfaces>
Final identity contract:

- P: canonical producer SHA from the tracked receipt and reviewed artifact rows.
- B: canonical staged-bundle digest from the tracked receipt and reviewed artifact rows.
- R: actual clean promotion-base SHA from the tracked receipt; P must be an ancestor of R, and the mechanically recomputed witness/replay producer-affecting closures at R must be byte-identical to those recorded at P.
- Q: promotion commit discovered from A's history; Q must have first parent R, exact P/B/R trailers, and the receipt's promoted file-set digest.
- A: clean full SHA checked out by the final gate; A must descend from Q and remain unchanged throughout execution.
- The gate verifies P → R → Q → A and recomputes producer-affecting witness and replay input closures at A, requiring equality with P. Only the successful terminal gate artifact records Q and A; tracked evidence does not self-record future SHAs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the ordered P/B/R/Q/A exact-head acceptance contract</name>
  <read-first>
    <file>.planning/phases/13-restore-evidence-and-replay-integrity/13-CONTEXT.md</file>
    <file>.planning/phases/13-restore-evidence-and-replay-integrity/13-04-SUMMARY.md</file>
    <file>reference/artifacts/phase13/promotion-receipt.json</file>
    <file>reference/source-map.toml</file>
    <file>reference/artifacts/manifest.toml</file>
    <file>tools/xtask/src/main.rs</file>
    <file>tools/xtask/src/phase13_evidence.rs</file>
    <file>.github/workflows/ci.yml</file>
    <file>.github/workflows/oracle.yml</file>
  </read-first>
  <files>tools/xtask/src/phase13_acceptance.rs, tools/xtask/src/main.rs, tools/xtask/tests/phase13_acceptance_contract.rs</files>
  <behavior>
    - "The contract validates P/B/R agreement across receipt, source/artifact ledgers, and evidence; proves P is an ancestor of R and producer-affecting closures at R equal P; discovers Q and verifies first parent R, P/B/R trailers, and promoted-tree digest; and proves A descends from Q."
    - "A is captured at start, checked before every authority transition, and required unchanged and clean at completion."
    - "Required order is identity relationships, scoped provenance/FND-04 ledgers, reviewed catalog replay, diagnosis tests, tracked regressions, canonical oracle configure/build, then current native/oracle comparisons."
    - "Missing, skipped, duplicate, reordered, or failed steps prevent terminal identity publication."
  </behavior>
  <action>
    Implement D-08 and D-09 as a typed ordered state machine around existing commands, with pure identity/transition logic and a thin effectful shell. Parse P/B/R from the tracked promotion receipt and manifest; prove P is an ancestor of R; recompute at R the mechanically defined witness and replay producer-affecting closures recorded at P and require byte identity. Discover Q from Git ancestry, validate Q's first parent is R, validate its exact P/B/R trailers and promoted-path tree, capture exact A, prove A descends from Q, and recompute both producer-affecting closures at A to prove they still equal P's recorded inputs. Run the locked fail-closed sequence without duplicating physics, scenario, comparator, provenance, or oracle-build logic. Each step returns structured completion evidence; absence/skip is failure. Emit the terminal identity only after success with P, B, R, Q, A, upstream/build identity, reviewed evidence digests, ordered step identities, and invocation. Add one-concern Arrange/Act/Assert tests before implementation for P not ancestor of R, closure drift at R/A, wrong Q first parent, wrong P/B/R trailers/tree, A drift/dirty state, ordering, skipped work, failure short-circuit, and publication suppression; focused failures may be observed locally but no failing state may be committed.
  </action>
  <verify>
    <automated>cargo test -p xtask --test phase13_acceptance_contract &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The P/B/R/Q/A relationships and producer-closure equality at R and A are mechanically proven without circular stored SHA claims.</criterion>
    <criterion>The required step sequence is a closed typed contract; every missing/skipped/failed step suppresses publication.</criterion>
    <criterion>Failure artifacts retain exact request and first semantic divergence without secrets.</criterion>
  </acceptance-criteria>
  <done>The aggregate acceptance command is promotion-aware, exact-A-bound, ordered, fail-closed, and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the dedicated final canonical Linux workflow and thin contributor command</name>
  <read-first>
    <file>tools/xtask/src/phase13_acceptance.rs</file>
    <file>tools/xtask/tests/phase13_acceptance_contract.rs</file>
    <file>.github/workflows/ci.yml</file>
    <file>.github/workflows/oracle.yml</file>
    <file>justfile</file>
  </read-first>
  <files>.github/workflows/phase13-acceptance.yml, justfile, tools/xtask/tests/phase13_acceptance_contract.rs</files>
  <behavior>
    - "The workflow checks out exact A with full history and pinned oracle, validates canonical Linux tools, and invokes exactly one Phase 13 aggregate command."
    - "The workflow uploads failure diagnostics and uploads the P/B/R/Q/A acceptance identity only from terminal success."
    - "Workflow tests reject mutable actions, shallow/ambiguous checkout, cross-workflow joins, missing failure artifacts, or partial-success identity publication."
    - "The just recipe delegates to the same xtask command with no orchestration logic."
  </behavior>
  <action>
    Create the dedicated workflow allowed by D-08 using pinned full-SHA actions and the exact canonical Linux compiler/target/preset from Oracle CI. Check out full history so P → R → Q → A ancestry is provable, assert `git rev-parse HEAD == github.sha`, initialize the pinned oracle, and invoke one `cargo xtask phase13 acceptance` command. Upload deterministic exact-request/first-divergence diagnostics with `if: failure()` and the terminal P/B/R/Q/A identity only after successful completion. Add a thin `just phase13-acceptance` wrapper. Extend workflow/recipe contract tests to prove exact checkout and history, immutable action pins, canonical identities, one aggregate invocation, artifact conditions, and absence of Phase 14/15 aggregation; focused failures may be observed locally but no failing state may be committed.
  </action>
  <verify>
    <automated>cargo test -p xtask --test phase13_acceptance_contract workflow_ &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated>
  </verify>
  <acceptance-criteria>
    <criterion>The workflow has one exact-A authority, full ancestry, canonical Linux, and one aggregate invocation.</criterion>
    <criterion>Failure evidence is retained and terminal identity cannot upload on partial, skipped, or failed execution.</criterion>
    <criterion>The contributor command and workflow execute the same tested contract.</criterion>
  </acceptance-criteria>
  <done>CI and contributor entrypoints execute the final promotion-descendant Phase 13 acceptance contract.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Tracked receipt/ledgers to Git history | P/B/R claims must resolve to one valid P → R → Q ancestry/tree/trailer relationship. |
| GitHub checkout to acceptance state machine | Exact A and clean source state must remain immutable across the gate. |
| Individual commands to aggregate success | Each command is untrusted until structured completion evidence is recorded in order. |
| Gate completion to terminal identity | P/B/R/Q/A identity publication is privileged and success-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13-05-01 | Spoofing | P/B/R/Q/A identity | mitigate | Cross-check receipt/manifest, P ancestry to R, closure equality at R, Q first parent/trailers/tree, exact A, and closure equality at A. |
| T-13-05-02 | Tampering | Ordered gate | mitigate | Encode a closed typed state machine and reject missing, duplicate, reordered, skipped, or failed records. |
| T-13-05-03 | Repudiation | Acceptance publication | mitigate | Emit P/B/R/Q/A, evidence/build digests, invocation, and ordered results only after terminal success. |
| T-13-05-04 | Information Disclosure | Failure artifact | mitigate | Redact environment/credentials and retain only exact requests, semantic divergence, and reproducibility metadata. |
| T-13-05-05 | Denial of Service | Canonical gate | mitigate | Reuse bounded commands and preserve focused failure artifacts. |
| T-13-05-06 | Elevation of Privilege | Workflow success conditions | mitigate | Treat missing/skipped steps as failure and condition identity upload on the tested terminal record. |
</threat-model>

<verification>
Run the focused acceptance/workflow contract tests. Before every task commit, run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then execute the dedicated canonical Linux workflow at A and confirm it validates P/B/R/Q/A, P ancestry to R, unchanged producer closures at R and A, scoped provenance/FND-04 ledgers, reviewed replay, regressions, oracle build/comparisons, failure retention, and success-only terminal identity publication.
</verification>

<success-criteria>
1. One dedicated final Linux gate proves P/B evidence and P → R → Q → A ancestry without circular stored SHA claims.
1. A is asserted before and after the complete fail-closed sequence.
1. P is an ancestor of R, Q has first parent R, and producer-affecting witness and replay closures at R and A equal the canonical P evidence.
1. Scoped provenance, FND-04 ledgers, replay, diagnosis, regressions, oracle build, and comparisons all pass in order.
1. Missing, skipped, reordered, or failed work cannot publish the terminal identity.
1. Exact requests and first divergence are retained on mismatch.
1. Phase 14 repair and Phase 15 release aggregation/attestation remain absent.
</success-criteria>

<output>
After completion, create `.planning/phases/13-restore-evidence-and-replay-integrity/13-05-SUMMARY.md`.
</output>
