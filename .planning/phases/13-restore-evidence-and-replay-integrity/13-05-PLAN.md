---
phase: 13-restore-evidence-and-replay-integrity
plan: '05'
type: execute
wave: 4
depends_on:
  - 13-04
files_modified:
  - crates/liquidfun-differential/src/failure_bundle/catalog.rs
  - crates/liquidfun-differential/tests/catalog_failures.rs
  - tools/xtask/src/phase13_acceptance.rs
  - tools/xtask/src/phase13_evidence.rs
  - tools/xtask/src/phase13_evidence/promotion.rs
  - tools/xtask/src/phase13_evidence/promotion/transaction.rs
  - tools/xtask/tests/phase13_acceptance_contract.rs
  - tools/xtask/tests/phase13_evidence_contract.rs
  - tools/xtask/tests/phase13_promotion_contract.rs
  - .github/workflows/phase13-acceptance.yml
  - reference/artifacts/phase13/promotion-receipt.json
  - reference/artifacts/catalog/rigid-stack-v1.replay-evidence.json
  - reference/catalog.toml
  - reference/source-map.toml
  - reference/artifacts/manifest.toml
autonomous: false
requirements:
  - FND-04
  - COMP-04
  - COMP-05
  - COMP-08
  - TEST-07
  - EXMP-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-07-25T18-25-02
generated_at: 2026-07-25T18:25:02.901Z
revised_at: 2026-07-26
must_haves:
  truths:
    - "Canonical acceptance uses a dedicated reviewed live replay whose physics projection must match D1 while expanded-capture drift remains exactly the reviewed capture_schema_drift."
    - "Tracked replay evidence and the live result agree exactly on authority, sealed input, D0 repeats, D1 identity/result, drift diagnosis, projections, first path, and bounded values."
    - "Projected physics and evidence-contract failures retain bounded, replayable, typed diagnostics in their designated failure directories."
    - "Incremental promotion reviews all seven Phase 13 replacements, changes only the mechanically different subset, and preserves every unchanged reviewed member byte-for-byte."
    - "One new human acknowledgment is bound to the complete schema-v2 review subject; previous acknowledgments do not authorize this recovery."
    - "The exact-head acceptance state machine suppresses terminal identity for a missing, failed, duplicated, skipped, or reordered live check."
  artifacts:
    - path: tools/xtask/src/phase13_evidence.rs
      provides: "Shared replay acquisition and strict reviewed live-check command"
    - path: crates/liquidfun-differential/src/failure_bundle/catalog.rs
      provides: "Replayable projected-comparison failure evidence"
    - path: tools/xtask/src/phase13_evidence/promotion.rs
      provides: "Incremental schema-v2 prepare, review, acknowledgment, receipt, and promotion contract"
    - path: tools/xtask/src/phase13_acceptance.rs
      provides: "Schema-v2 exact-head acceptance and ordered live-check enforcement"
    - path: .github/workflows/phase13-acceptance.yml
      provides: "Canonical Linux acceptance with both failure evidence roots"
  key_links:
    - from: tools/xtask/src/phase13_evidence.rs
      to: crates/liquidfun-differential/src/runner/catalog.rs
      via: "Production and live acceptance share acquisition and compare_catalog_physics_projection"
      pattern: "live-check|compare_catalog_physics_projection"
    - from: tools/xtask/src/phase13_evidence/promotion.rs
      to: reference/artifacts/phase13/promotion-receipt.json
      via: "Schema-v2 full-set and changed-set digests"
      pattern: "review_sha256|changed_paths|unchanged_paths"
    - from: tools/xtask/src/phase13_acceptance.rs
      to: tools/xtask/src/phase13_evidence.rs
      via: "Final ordered acceptance step invokes reviewed live-check"
      pattern: "phase13 evidence live-check --tracked --require-reviewed"
---

<objective>
Recover the still-open Plan 13-05 by replacing raw expanded-capture acceptance with projection-aware reviewed live replay and honest incremental repromotion.

Purpose: Preserve the original P/B/R/Q/A exact-head design while making acceptance compare the reviewed physics surface, retaining actionable failure evidence, and binding the recovery promotion to one fresh human-reviewed schema-v2 subject.
Output: Shared live replay, typed failure evidence, incremental promotion v2, acceptance v2, canonical workflow proof, and an append-only recovery record.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/13-restore-evidence-and-replay-integrity/13-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-restore-evidence-and-replay-integrity/13-04-SUMMARY.md
@reference/artifacts/phase13/promotion-receipt.json
@reference/artifacts/catalog/rigid-stack-v1.replay-evidence.json
@reference/source-map.toml
@reference/artifacts/manifest.toml
@tools/xtask/src/phase13_evidence.rs
@tools/xtask/src/phase13_evidence/promotion.rs
@tools/xtask/src/phase13_acceptance.rs
@crates/liquidfun-differential/src/runner/catalog.rs
@crates/liquidfun-differential/src/failure_bundle/catalog.rs
@.github/workflows/phase13-evidence-producer.yml
@.github/workflows/phase13-acceptance.yml

<recovery-history>
Preserve these failed canonical acceptance runs and their commits as audit history:

- Run 30211150612 at `ced2134`: stale temporary-repository negative tests omitted the reviewed replay evidence file.
- Run 30211470256 at `32e2c93`: the test-only repair altered the producer-affecting replay closure after promotion.
- Run 30211674242 at `d15191e`: identity, ancestry, closures, provenance, reviewed replay, diagnosis, regressions, and oracle build passed; the final raw expanded `catalog compare` failed at checkpoint 0001 because reviewed debug capture schema drift was treated as a physics mismatch.

Do not erase, rewrite, revert, or reuse the acknowledgments, Q commit, or evidence from those runs. This recovery creates P2/B2/R2/Q2/A2 and requires a new acknowledgment.
</recovery-history>

<interfaces>
Reviewed live replay:

- Exact command: `cargo xtask phase13 evidence live-check --tracked --require-reviewed`.
- Production and live acceptance call one acquisition function and the existing `compare_catalog_physics_projection`.
- The tracked record is parsed strictly and matched against the live record for upstream revision, sealed input, both D0 digests, D1 oracle identity/result, drift class, reviewed/current projections, first divergence path, and bounded values.
- Pass only when `legacy_physics_v1` matches D1 and the expanded difference is exactly reviewed `capture_schema_drift`.

Incremental promotion v2:

- P2 is the producer commit; B2 is the independently validated canonical bundle; R2 is the clean constants-only review base.
- Preparation stages and validates all seven replacement paths, replacing existing Phase 13 ledger rows instead of duplicating them.
- `changed_paths` is the nonempty subset whose staged bytes differ from R2; `unchanged_paths` is its byte-identical complement.
- The review subject binds P2/B2/R2, all seven replacement hashes, both classifications, and deterministic Git diff to one `review_sha256`.
- Q2 has first parent R2, exact P2/B2/R2 trailers, and a Git diff exactly equal to `changed_paths`.
- The tracked receipt and terminal identity record schema v2 full promoted-set and changed-set digests.
- All seven final bytes remain unchanged from Q2 through A2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared projection-aware live replay and typed failure evidence</name>
  <read-first>
    <file>tools/xtask/src/phase13_evidence.rs</file>
    <file>crates/liquidfun-differential/src/runner/catalog.rs</file>
    <file>crates/liquidfun-differential/src/failure_bundle/catalog.rs</file>
    <file>reference/artifacts/catalog/rigid-stack-v1.replay-evidence.json</file>
    <file>tools/xtask/tests/phase13_evidence_contract.rs</file>
    <file>crates/liquidfun-differential/tests/catalog_failures.rs</file>
  </read-first>
  <files>tools/xtask/src/phase13_evidence.rs, tools/xtask/tests/phase13_evidence_contract.rs, crates/liquidfun-differential/src/failure_bundle/catalog.rs, crates/liquidfun-differential/tests/catalog_failures.rs</files>
  <behavior>
    - "Debug-only expanded capture drift passes when legacy_physics_v1 matches D1."
    - "Body, world, or numeric physics drift fails with its projected semantic path."
    - "Changed authority, sealed input, D0 repeat, D1 identity/result, drift class, projection, first path, or bounded value fails closed."
    - "Missing, extra, or malformed live-check arguments are usage failures."
    - "Failure replay validates hashes, confinement, comparison surface, exact request, and first divergence."
  </behavior>
  <action>
    Refactor the current replay producer into one acquisition function used by both production and the exact `phase13 evidence live-check --tracked --require-reviewed` command. Keep generic `catalog compare` unchanged. Parse tracked replay evidence into a strict typed record and compare every reviewed field. Reuse `compare_catalog_physics_projection`; reject unknown projections, changed schema drift, authority drift, or physics divergence. Add a bounded evidence-contract failure record under `target/phase13-acceptance/failures` with exact request identity, expected and observed replay records, and first JSON pointer. Extend catalog failure bundles under `target/differential/catalog-failures` with exact request, captures, comparison surface, checkpoint, first semantic path, states, and bounded values; permit category and stderr for harness failures without comparable captures.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-differential --test catalog_failures &amp;&amp; cargo test -p xtask --test phase13_evidence_contract</automated>
  </verify>
  <done>The reviewed live-check shares producer acquisition, compares the locked physics projection, and emits replayable bounded failures.</done>
</task>

<task type="auto">
  <name>Task 2: Implement incremental promotion v2 and acceptance v2</name>
  <read-first>
    <file>tools/xtask/src/phase13_evidence/promotion.rs</file>
    <file>tools/xtask/src/phase13_evidence/promotion/transaction.rs</file>
    <file>tools/xtask/src/phase13_acceptance.rs</file>
    <file>tools/xtask/tests/phase13_promotion_contract.rs</file>
    <file>tools/xtask/tests/phase13_acceptance_contract.rs</file>
    <file>.github/workflows/phase13-acceptance.yml</file>
  </read-first>
  <files>tools/xtask/src/phase13_evidence/promotion.rs, tools/xtask/src/phase13_evidence/promotion/transaction.rs, tools/xtask/src/phase13_acceptance.rs, tools/xtask/tests/phase13_promotion_contract.rs, tools/xtask/tests/phase13_acceptance_contract.rs, .github/workflows/phase13-acceptance.yml</files>
  <behavior>
    - "An already promoted Phase 13 tree prepares without duplicate ledger rows or artificial byte changes."
    - "Any replacement byte changes review_sha256; stale or wrong acknowledgment fails."
    - "Any write or post-write validation failure rolls back all seven paths."
    - "Acceptance rejects wrong changed subset, modified unchanged member, wrong Q parent/trailers, head drift, or missing live comparison."
    - "Workflow tests require both failure directories and success-only terminal identity upload."
  </behavior>
  <action>
    Upgrade preparation, review acknowledgment, receipt, and terminal identity to schema v2. Replace existing Phase 13 catalog/source-map/manifest/receipt rows and preserve identical artifacts. Stage and transactionally validate all seven replacements; derive changed and unchanged subsets against R2. Hash a complete canonical review subject containing P2/B2/R2, every replacement hash, both classifications, and deterministic Git diff. Bind the acknowledgment to `review_sha256`. Keep atomic replacement and rollback across all seven paths while allowing Q2 to alter only the changed subset. Require Q2's actual Git paths to equal `changed_paths`, every unchanged path to equal R2, and all seven paths to remain unchanged through A2. Replace the final raw comparison step with the exact reviewed live-check and make skipped, failed, duplicated, or reordered live checking suppress identity. Upload both failure directories in the workflow.
  </action>
  <verify>
    <automated>cargo test -p xtask --test phase13_promotion_contract &amp;&amp; cargo test -p xtask --test phase13_acceptance_contract</automated>
  </verify>
  <done>Promotion and acceptance enforce schema-v2 reviewed-set identity without manufactured churn.</done>
</task>

<task type="auto">
  <name>Task 3: Produce P2/B2, establish R2, and prepare the review subject</name>
  <read-first>
    <file>.github/workflows/phase13-evidence-producer.yml</file>
    <file>tools/xtask/src/phase13_evidence/promotion.rs</file>
    <file>reference/artifacts/phase13/promotion-receipt.json</file>
  </read-first>
  <files>tools/xtask/src/phase13_evidence/promotion.rs</files>
  <action>
    Run the repository-required Rust checks in order and commit the implementation as producer P2. Push P2, dispatch `phase13-evidence-producer.yml` at that exact SHA, wait for success, download canonical B2 plus provider metadata, and independently validate hashes, confinement, provenance, closures, and replay. Update only promotion-side P2/B2 acquisition constants, rerun the required checks, commit them, and capture the resulting clean head as R2. Prove producer-affecting closures at R2 equal P2. Run prepare at R2; it must write only untracked review/staging outputs and produce the schema-v2 review packet without changing tracked files.
  </action>
  <verify>
    <automated>cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated>
  </verify>
  <done>P2 is pushed, B2 is independently validated, R2 is clean and closure-equivalent, and one exact review_sha256 is ready.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Obtain fresh schema-v2 review acknowledgment</name>
  <action>
    Stop and present the exact `review_sha256`, review packet path, P2/B2/R2 identities, changed/unchanged classification, and deterministic diff summary. Require a new acknowledgment explicitly naming this exact digest. Do not reuse any prior Phase 13 acknowledgment and do not promote before the new acknowledgment is supplied.
  </action>
  <resume-signal>I, pRizz, reviewed and acknowledge Phase 13 review SHA-256 &lt;review_sha256&gt;</resume-signal>
  <done>The user supplies a fresh acknowledgment bound to the exact schema-v2 review subject.</done>
</task>

<task type="auto">
  <name>Task 5: Promote Q2, establish A2, rerun canonical acceptance, and close Phase 13</name>
  <read-first>
    <file>reference/artifacts/phase13/promotion-receipt.json</file>
    <file>.planning/phases/13-restore-evidence-and-replay-integrity/13-04-SUMMARY.md</file>
    <file>.github/workflows/phase13-acceptance.yml</file>
  </read-first>
  <files>reference/catalog.toml, reference/source-map.toml, reference/artifacts/manifest.toml, reference/artifacts/catalog/rigid-stack-v1.json, reference/artifacts/catalog/rigid-stack-v1.replay-evidence.json, reference/artifacts/provenance/liquidfun-v1.1.0.txt, reference/artifacts/phase13/promotion-receipt.json, .planning/phases/13-restore-evidence-and-replay-integrity/13-04-SUMMARY.md, .planning/phases/13-restore-evidence-and-replay-integrity/13-05-SUMMARY.md</files>
  <action>
    Validate the new acknowledgment, transactionally promote all seven replacements, run the required Rust checks, and commit Q2 with first parent R2 and exact P2/B2/R2 trailers. Prove Q2's Git diff equals the recorded changed subset. Append recovery details to the Plan 13-04 summary without erasing original promotion history and commit that GSD-only update as A2. Push A2, dispatch the exact-head acceptance workflow, wait for success, download and validate the schema-v2 terminal identity, complete objective UAT and phase verification, create 13-05-SUMMARY.md with all failed runs and recovery deviations, and advance to Phase 14.
  </action>
  <verify>
    <automated>cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated>
  </verify>
  <done>Q2/A2 are accepted by canonical Linux, terminal identity v2 is validated, and Phase 13 is complete.</done>
</task>

</tasks>

<retry-policy>
- Acceptance/workflow-only fixes outside producer-affecting closures may create a new A descendant without repromotion.
- Any fix touching producer-affecting closure inputs or promoted evidence restarts P/B/R/Q and requires a new human acknowledgment.
- Preserve all failed runs, commits, packets, and acknowledgments as append-only audit history.
</retry-policy>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Tracked replay to live replay | Reviewed authority and physics projection must match fresh acquisition exactly. |
| Bundle to review subject | All seven replacements and their changed/unchanged classification must be hash-bound. |
| Acknowledgment to promotion | Only a fresh acknowledgment of the exact review_sha256 authorizes Q2. |
| R2/Q2/A2 Git history | Changed paths, unchanged bytes, ancestry, trailers, closures, and final trees must agree. |
| Acceptance effects to terminal identity | The closed ordered state machine must complete at one exact clean A2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13-05-R01 | Spoofing | Tracked/live replay authority | mitigate | Strict typed equality across revision, sealed input, D0, D1, drift, projections, path, and values. |
| T-13-05-R02 | Tampering | Incremental replacement set | mitigate | Review all seven hashes and classifications; validate changed Git subset and unchanged bytes. |
| T-13-05-R03 | Repudiation | Human acknowledgment | mitigate | Bind signer statement to one complete review_sha256 and preserve prior history. |
| T-13-05-R04 | Information Disclosure | Failure artifacts | mitigate | Store bounded request/comparison evidence and redact unrelated environment or credentials. |
| T-13-05-R05 | Denial of Service | Replay and canonical workflow | mitigate | Reuse bounded acquisition/comparison paths and deterministic artifacts. |
| T-13-05-R06 | Elevation of Privilege | Terminal publication | mitigate | Suppress identity for missing, skipped, duplicate, reordered, or failed live-check evidence. |
</threat-model>

<verification>
Before every Rust-project commit, run in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.

Final proof is a successful canonical Linux exact-A2 workflow whose downloaded schema-v2 terminal identity validates P2/B2/R2/Q2/A2, producer closures, all seven final bytes, the exact changed subset, ordered reviewed live replay, and success-only publication.
</verification>

<success-criteria>
1. `phase13 evidence live-check --tracked --require-reviewed` is strict, projection-aware, and shared with production acquisition.
1. Projected physics and tracked/live contract failures produce bounded replayable evidence in the correct directories.
1. Incremental promotion v2 reviews all seven members, changes only the real subset, and preserves unchanged bytes.
1. One fresh acknowledgment authorizes the complete review subject.
1. Acceptance v2 proves P2/B2/R2/Q2/A2, exact changed paths, unchanged members, immutable final bytes, and ordered live checking.
1. Canonical Linux acceptance succeeds at exact A2 and the terminal identity artifact validates independently.
1. Failed historical runs and original promotion history remain intact.
</success-criteria>

<output>
After successful canonical acceptance, create `.planning/phases/13-restore-evidence-and-replay-integrity/13-05-SUMMARY.md`.
</output>
