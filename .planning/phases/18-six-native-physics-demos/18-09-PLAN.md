---
phase: 18-six-native-physics-demos
plan: "09"
type: execute
wave: 4
depends_on: [18-01, 18-02, 18-03, 18-04, 18-05, 18-06, 18-07, 18-08]
files_modified:
  - web/src/catalog/scenes.ts
  - web/src/components/SceneControls.tsx
  - web/src/components/SceneCredits.tsx
  - web/src/components/PlayerPanel.tsx
  - web/src/components/FallbackPanel.tsx
  - web/src/physics/session.ts
  - web/src/physics/loader.ts
  - web/src/App.tsx
  - web/src/player/runtime.ts
  - web/src/app.css
  - web/tests/scenes.test.ts
  - web/tests/session.test.ts
  - web/tests/controls.test.ts
  - web/tests/credits.test.ts
autonomous: true
requirements: [WEB-01, WEB-04, WEB-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-09-18T03-36-16
generated_at: 2026-09-18T04:15:00Z
must_haves:
  truths:
    - "Any ready scene id constructs one WASM world, and switching or resetting disposes the prior session."
    - "Recreating presets show Changing this setting recreates the scene from its documented initial state. and require Apply setting; runtime presets apply on change."
    - "Each player view shows Scene source with host-locked implementation, inspiration, and Third-party notices links beside the existing SiteFooter."
    - "All six catalog ids are ready:true and App uses isReadySceneRoute instead of isDamBreakRoute."
  artifacts:
    - path: web/src/components/SceneControls.tsx
      provides: "Labeled presets/actions plus Apply setting"
    - path: web/src/components/SceneCredits.tsx
      provides: "Implementation / inspiration / notices aside"
    - path: web/src/player/runtime.ts
      provides: "startScene/abandonScene extracted from App.tsx"
    - path: web/src/physics/session.ts
      provides: "applyControl/applyAction on the TypeScript owner"
  key_links:
    - from: web/src/App.tsx
      to: web/src/physics/loader.ts
      via: "loadSceneSession(id) for every ready id"
      pattern: "loadSceneSession"
    - from: web/src/components/SceneControls.tsx
      to: "Apply setting"
      via: "Noun-bearing construction CTA"
      pattern: "Apply setting"
    - from: web/src/components/SceneCredits.tsx
      to: web/src/catalog/links.ts
      via: "sceneBlobUrl / noticesBlobUrl"
      pattern: "sceneBlobUrl"
---

<objective>
Mark all six scenes ready, generalize the player to any allowlisted id, and mount labeled controls plus per-scene credits.

Purpose: WEB-04 and WEB-08 become visitor-visible. D-08 one-session teardown stays. Construction CTA is `Apply setting` (UI-SPEC FLAG).
Output: Wired App, SceneControls, SceneCredits, all-ready catalog.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/code-shape.md
@standards/core/frontend-ui.md
@.planning/phases/18-six-native-physics-demos/18-CONTEXT.md
@.planning/phases/18-six-native-physics-demos/18-UI-SPEC.md
@web/src/App.tsx
@web/src/physics/session.ts
@web/src/components/PlayerPanel.tsx

<interfaces>
```typescript
export interface GeneratedProofSession {
  advance(stepCount: number): void;
  captureFrame(): RawProofFrame;
  applyControl(name: string, value: string): boolean;
  applyAction(name: string): void;
  free(): void;
}

export interface SceneSession {
  nextFrame(stepCount?: number): RenderFrame;
  applyControl(name: string, value: string): boolean;
  applyAction(name: string): void;
  dispose(): void;
}
```

Construction CTA (FLAG overrides 18-UI-SPEC `Apply`): `Apply setting`.
Reset hint (visible before Apply setting): `Changing this setting recreates the scene from its documented initial state.`
Credits: aside `Scene source`, terms `Implementation` / `Inspiration` / `Notices`, link texts `View scene source` / FEATURES labels / `Third-party notices`. `target="_blank"` `rel="noopener noreferrer"`.
`PAGE_SUMMARY`: `Play experimental Rust physics scenes in the browser. All six demos run this repository's engine through WebAssembly.`
Player heading/status/canvas name use the scene title, not hardcoded Dam Break.
Generalize `isDamBreakRoute` → `isReadySceneRoute`. Extract lifecycle if `App.tsx` would exceed ~628 lines.

Do not add canvas pointer handlers (Phase 19). Do not change Pages workflow or Vite base.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the TS session owner and catalog ready flags</name>
  <files>web/src/physics/session.ts, web/tests/session.test.ts, web/src/catalog/scenes.ts, web/tests/scenes.test.ts, web/src/components/SceneControls.tsx, web/src/components/SceneCredits.tsx, web/tests/controls.test.ts, web/tests/credits.test.ts</files>
  <read_first>18-CONTEXT.md D-03, D-04, D-06, D-12, D-13; 18-UI-SPEC.md Scene controls and Scene Credits; web/src/physics/session.ts; web/src/catalog/links.ts</read_first>
  <behavior>
    - Test: `applyControl` forwards one `applyControl` call and returns the boolean; invalid names poison like other WASM failures with the fixed `FAILED_MESSAGE`.
    - Test: `applyAction` forwards one call; generated throw poisons and frees.
    - Test: every `SCENE_IDS` entry is `ready: true`; `isReadySceneId` is true for all six.
    - Test: a helper `constructionHintVisible(control)` is true only when `recreates` is true.
    - Test: `implementationHref(path, sha)` never contains `google/liquidfun`.
  </behavior>
  <action>Per D-03, D-04, D-06, and D-13, write failing tests first. Add `applyControl`/`applyAction` to the owner with the same poison/`finally` free rules as `nextFrame`. Set all six `ready: true`. Add presentational `SceneControls` and `SceneCredits`: recreating presets are label+select+`Apply setting` with the reset sentence visible before the button; runtime presets apply on `change`; actions are `type="button"` with locked verb+noun labels. Credits use `sceneBlobUrl`/`noticesBlobUrl` and `readBuildInfo().maybeCommitUrl` SHA. No `innerHTML`. Do not mount them in App yet if that keeps this task under the file budget — mounting is Task 2. Prefer creating the components here so Task 2 only wires.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/session.test.ts tests/scenes.test.ts tests/controls.test.ts tests/credits.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "applyControl|applyAction" web/src/physics/session.ts` matches.
    - `rg -n "ready: true" web/src/catalog/scenes.ts` matches six records.
    - `rg -n "Apply setting" web/src/components/SceneControls.tsx` matches.
    - `rg -n "Changing this setting recreates the scene from its documented initial state." web/src/components/SceneControls.tsx` matches.
    - `rg -n "View scene source|Third-party notices|noopener noreferrer" web/src/components/SceneCredits.tsx` matches.
    - `rg -n "innerHTML|google/liquidfun" web/src/components/SceneCredits.tsx` is empty.
    - Unit tests listed above exit 0.
  </acceptance_criteria>
  <done>Session apply_* exists; all six ids are ready; control/credit components render locked copy.</done>
</task>

<task type="auto">
  <name>Task 2: Wire any ready id through one-session App lifecycle</name>
  <files>web/src/App.tsx, web/src/player/runtime.ts, web/src/components/PlayerPanel.tsx, web/src/app.css, web/src/physics/loader.ts</files>
  <read_first>web/src/App.tsx; 18-CONTEXT.md D-08, D-12, D-14, D-15; 18-UI-SPEC.md State and Interaction, Player heading; standards/core/code-shape.md</read_first>
  <action>Per D-08 and D-12, replace `isDamBreakRoute` / `startDamBreak` / `abandonDamBreak` with `isReadySceneRoute` / `startScene(id)` / `abandonScene`. Construct via `loadSceneSession(id)`. Keep generation tokens, hidden-tab cap, copied frames, and dispose-on-switch. Generalize PlayerPanel title, loading/failed strings, and canvas accessible name to the current scene title. Slot SceneControls and SceneCredits inside the player (credits 24px below controls). Construction Apply setting may pass through Loading then Playing (or Paused if reduced motion). Update `PAGE_SUMMARY`. Extract `web/src/player/runtime.ts` if App.tsx would exceed ~628 lines. Do not add pointer handlers, Tailwind, or Pages workflow edits. Do not run a new live Pages deploy.</action>
  <verify>
    <automated>rg -n "isReadySceneRoute|loadSceneSession|Apply setting|SceneCredits|isDamBreakRoute" web/src/App.tsx web/src/player/runtime.ts web/src/components/PlayerPanel.tsx &amp;&amp; test -z "$(rg -n "isDamBreakRoute|startDamBreak|innerHTML|onPointer|pointerdown" web/src/App.tsx web/src/player/runtime.ts || true)" &amp;&amp; cd web &amp;&amp; bun run test:unit &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "isDamBreakRoute|startDamBreak|loadProofSession" web/src/App.tsx` is empty.
    - `rg -n "loadSceneSession" web/src/App.tsx web/src/player/runtime.ts` matches.
    - `rg -n "All six demos run this repository's engine" web/src/App.tsx` matches.
    - `rg -n "innerHTML|onPointer|pointerdown" web/src/App.tsx web/src/components` is empty.
    - `wc -l web/src/App.tsx` is under 628.
    - `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Any ready hash constructs one world, shows locked controls/credits, and disposes on switch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hash id → WASM constructor | Only ready allowlisted ids construct a world. |
| Control values → applyControl | Only catalog ids/values cross the WASM boundary. |
| Credit hrefs → new tab | Only host-locked helpers may build URLs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09-01 | T | Hash → factory | mitigate | `isReadySceneRoute` + `parse_scene_id`; unknown/empty stay fallback. |
| T-18-09-02 | T | Control apply | mitigate | Forward catalog ids only; Rust allowlists again; poison on generated throw. |
| T-18-09-03 | D | Scene switch leak | mitigate | `abandonScene` increments generation, cancels rAF, `dispose()`s the prior owner. |
| T-18-09-04 | S | Credit URLs | mitigate | `sceneBlobUrl` / `noticesBlobUrl`; `rel="noopener noreferrer"`; never hash-derived hrefs. |
| T-18-09-05 | T | XSS from scene/error strings | mitigate | Fixed UI-SPEC text nodes; DEV `Details:` sliced text only. |
| T-18-09-06 | D | Unbounded emission | mitigate | Engine caps from scene modules; no extra JS emitters. |
| T-18-09-07 | E | Pages / PAT | accept | No workflow or permission changes; no deploy gate. |
</threat_model>

<verification>
Run `cd web && bun run test:unit && bun run typecheck`. After `just web-wasm`, `just web-build` should still write `/liquidfun-rs/assets/`. Do not run the six-scene pointer matrix. Do not deploy Pages.
</verification>

<success_criteria>
- All six ids are ready and constructible.
- Apply setting is the construction CTA; reset hint is visible first.
- Credits sit in the player, not in place of the footer.
- App.tsx no longer special-cases Dam Break.
</success_criteria>

<output>
After completion, create `.planning/phases/18-six-native-physics-demos/18-09-SUMMARY.md`
</output>
