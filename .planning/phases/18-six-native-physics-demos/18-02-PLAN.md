---
phase: 18-six-native-physics-demos
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - web/src/catalog/scenes.ts
  - web/src/catalog/links.ts
  - web/tests/scenes.test.ts
  - web/tests/links.test.ts
autonomous: true
requirements: [WEB-04, WEB-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-09-18T03-36-16
generated_at: 2026-09-18T04:15:00Z
must_haves:
  truths:
    - "Each of the six catalog records carries the locked one-sentence description, two or three labeled controls with recreates flags, and credit paths."
    - "Implementation and notices hrefs are host-locked to https://github.com/bright-builds-llc/liquidfun-rs/blob/{sha|main}/… and never use google/liquidfun as implementation."
    - "Only dam-break stays ready:true so App cannot construct unimplemented stub worlds from catalog metadata."
  artifacts:
    - path: web/src/catalog/scenes.ts
      provides: "Descriptions, preview ids, control metadata, credits, still-honest ready flags"
      exports: ["SCENES", "SceneControl", "SceneCredits"]
    - path: web/src/catalog/links.ts
      provides: "Host-locked blob URL helper"
      exports: ["sceneBlobUrl", "REPO_BLOB_ORIGIN"]
    - path: web/tests/links.test.ts
      provides: "SHA vs main fallback and rejected hosts"
  key_links:
    - from: web/src/catalog/links.ts
      to: web/src/build-info.ts
      via: "Reuse the 40-hex SHA gate; do not accept hash-route strings"
      pattern: "\\^\\[0-9a-f\\]\\{40\\}\\$"
    - from: web/src/catalog/scenes.ts
      to: "crates/liquidfun-wasm/src/scene/"
      via: "implementationPath per UI-SPEC"
      pattern: "crates/liquidfun-wasm/src/scene/dam_break.rs"
---

<objective>
Expand catalog records with locked descriptions, control contracts, and host-locked credit URLs while leaving readiness as Dam Break-only.

Purpose: WEB-04 and WEB-08 need a functional-core data module before chrome. D-03 forbids marking stub scenes ready.
Output: Tested `SceneRecord` extras and `sceneBlobUrl`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/frontend-ui.md
@.planning/phases/18-six-native-physics-demos/18-CONTEXT.md
@.planning/phases/18-six-native-physics-demos/18-UI-SPEC.md
@.planning/phases/18-six-native-physics-demos/18-RESEARCH.md
@web/src/catalog/scenes.ts
@web/src/build-info.ts
@web/tests/scenes.test.ts

<interfaces>
Extend `SceneRecord` in `web/src/catalog/scenes.ts`. Keep `ready: true` only on `dam-break`.

```typescript
export type SceneControl =
  | {
      readonly id: string;
      readonly label: string;
      readonly kind: "preset";
      readonly recreates: boolean;
      readonly values: readonly { readonly id: string; readonly label: string }[];
    }
  | {
      readonly id: string;
      readonly label: string;
      readonly kind: "action";
      readonly recreates: false;
    };

export type SceneCredits = {
  readonly implementationPath: string;
  readonly inspiration: readonly { readonly label: string; readonly href: string }[];
};

export type SceneRecord = {
  readonly id: SceneId;
  readonly title: string;
  readonly ready: boolean;
  readonly description: string;
  readonly previewId: SceneId;
  readonly controls: readonly SceneControl[];
  readonly credits: SceneCredits;
};

export function sceneBlobUrl(
  path: string,
  maybeSha: string | undefined,
): string;
```

Exact descriptions from 18-UI-SPEC.md Catalog card descriptions. Exact control labels, option labels, and recreates flags from 18-UI-SPEC.md Scene control labels. Control ids (machine):

| Scene | Control id | Kind | Recreates |
|-------|------------|------|-----------|
| dam-break | water-amount | preset | true |
| dam-break | gravity | preset | true |
| dam-break | drop-obstacle | action | false |
| dam-break | reset-obstacle | action | false |
| fountain | emission-rate | preset | false |
| fountain | launch-speed | preset | false |
| fountain | aim-angle | preset | false |
| float-or-sink | body | preset | false |
| float-or-sink | drop-body | action | false |
| color-mixer | mix-strength | preset | true |
| color-mixer | stir-speed | preset | false |
| jelly-drop | shape | preset | true |
| jelly-drop | softness | preset | true |
| water-wheel | jet-strength | preset | false |
| water-wheel | emission | preset | false |

Dam Break has three UI groups: Water amount, Gravity, and Obstacle (two actions). Encode the two obstacle actions as two `kind: "action"` controls.

Implementation paths (D-13):
- dam-break → `crates/liquidfun-wasm/src/scene/dam_break.rs`
- fountain → `crates/liquidfun-wasm/src/scene/fountain.rs`
- float-or-sink → `crates/liquidfun-wasm/src/scene/float_or_sink.rs`
- color-mixer → `crates/liquidfun-wasm/src/scene/color_mixer.rs`
- jelly-drop → `crates/liquidfun-wasm/src/scene/jelly_drop.rs`
- water-wheel → `crates/liquidfun-wasm/src/scene/water_wheel.rs`

Inspiration hrefs (host-locked constants, not hash input):
- LiquidFun showcase: `https://google.github.io/liquidfun/`
- Particle guide: `https://google.github.io/liquidfun/Programmers-Guide/html/md__chapter11__particles.html`
- Pinned Faucet: `https://github.com/google/liquidfun/blob/7f20402173fd143a3988c921bc384459c6a858f2/liquidfun/Box2D/Testbed/Tests/Faucet.h`

Assign at least: Dam Break and Water Wheel → showcase; Fountain → Faucet + showcase; Float or Sink, Color Mixer, Jelly Drop → particle guide + showcase.
</interfaces>
</context>

<feature>
  <name>Catalog control and credit metadata</name>
  <files>web/src/catalog/scenes.ts, web/src/catalog/links.ts, web/tests/scenes.test.ts, web/tests/links.test.ts</files>
  <behavior>
    Data-in/data-out catalog extras. No Solid, no WASM.
    Cases:
    - Every scene has the exact UI-SPEC description
    - Dam Break controls include recreating `water-amount` and `gravity` plus two obstacle actions
    - `isReadySceneId` remains true only for `dam-break`
    - `sceneBlobUrl("crates/liquidfun-wasm/src/scene/dam_break.rs", valid40hex)` → `https://github.com/bright-builds-llc/liquidfun-rs/blob/{sha}/crates/liquidfun-wasm/src/scene/dam_break.rs`
    - missing/short/non-hex SHA → `.../blob/main/{path}`
    - `javascript:`, `https://evil.example/`, and `https://github.com/google/liquidfun/...` as SHA/path inputs never become the returned implementation href
  </behavior>
  <implementation>
    Pure modules. Reuse the 40-hex pattern from `readBuildInfo`. Reject path strings that contain `..`, start with `/`, or are not under `crates/liquidfun-wasm/src/scene/`.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add descriptions, controls, and credits to SCENES</name>
  <files>web/src/catalog/scenes.ts, web/tests/scenes.test.ts</files>
  <read_first>18-CONTEXT.md D-01, D-04, D-05, D-12, D-13; 18-UI-SPEC.md Copywriting Contract and Scene control labels; web/src/catalog/scenes.ts; web/tests/scenes.test.ts</read_first>
  <behavior>
    - Test: each `SCENES` description equals the UI-SPEC sentence.
    - Test: control labels and option labels match the UI-SPEC table; recreates is true only for water-amount, gravity, mix-strength, shape, and softness.
    - Test: only `dam-break` is `ready: true`.
    - Test: `maybeSceneById("dam-break")` includes the new fields; unknown id is still undefined.
  </behavior>
  <action>Per D-04, D-05, and D-13, write failing Vitest cases first, then extend `SceneRecord`. Do not add preview SVG components, cards, or `ready: true` on the five stub scenes. Do not add search, filters, or a scene editor. Keep `maybe_` naming on lookups.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/scenes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Release a block of water into a basin" web/src/catalog/scenes.ts` matches.
    - `rg -n "recreates: true" web/src/catalog/scenes.ts` matches only the five construction presets listed above.
    - `rg -n "ready: true" web/src/catalog/scenes.ts` has exactly one match on `dam-break`.
    - `rg -n "google/liquidfun" web/src/catalog/scenes.ts` appears only inside `inspiration` hrefs, never `implementationPath`.
    - `cd web && bun run test:unit -- tests/scenes.test.ts` exits 0.
  </acceptance_criteria>
  <done>Catalog data names every locked control and credit path without claiming the five stubs are ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Host-lock implementation and notices blob URLs</name>
  <files>web/src/catalog/links.ts, web/tests/links.test.ts</files>
  <read_first>web/src/build-info.ts; 18-CONTEXT.md D-12, D-13; 18-RESEARCH.md Pitfall 6; 18-UI-SPEC.md Scene Credits Chrome</read_first>
  <behavior>
    - Test: valid 40-hex SHA yields the blob/{sha}/path URL on `bright-builds-llc/liquidfun-rs`.
    - Test: undefined, empty, short, and non-hex SHA yield blob/main/{path}.
    - Test: path `../LICENSE` or `liquidfun/Box2D/Testbed/Tests/Faucet.h` is rejected (throw or return undefined — pick throw with a fixed message `Scene source path is not allowlisted`) and never produces a google/liquidfun implementation URL.
    - Test: notices path `THIRD_PARTY_NOTICES.md` is allowed as a second exported helper `noticesBlobUrl(maybeSha)` using the same host lock.
  </behavior>
  <action>Per D-13, implement `sceneBlobUrl` and `noticesBlobUrl` as pure functions. Copy the SHA pattern from `readBuildInfo`. Do not read `window.location.hash`. Do not accept caller-supplied origins. Notices may use repo-root `THIRD_PARTY_NOTICES.md`; scene paths must stay under `crates/liquidfun-wasm/src/scene/` and end in `.rs`.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/scenes.test.ts tests/links.test.ts &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "bright-builds-llc/liquidfun-rs/blob" web/src/catalog/links.ts` matches.
    - `rg -n "google/liquidfun" web/src/catalog/links.ts` is empty.
    - `rg -n "location\\.hash|innerHTML" web/src/catalog/links.ts` is empty.
    - `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Implementation and notices URLs are host-locked and independent of hash-route input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build SHA / path → credit href | Provenance strings become public links. |
| Catalog metadata → later player | Control ids must match Rust allowlists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | S | `sceneBlobUrl` | mitigate | Host-lock `bright-builds-llc/liquidfun-rs`; never use hash tokens or `google/liquidfun` as implementation. |
| T-18-02-02 | T | Scene path | mitigate | Allow only `crates/liquidfun-wasm/src/scene/*.rs` or `THIRD_PARTY_NOTICES.md`; reject `..`. |
| T-18-02-03 | T | XSS from scene/error strings | mitigate | Metadata is constant TS; later UI uses text nodes. |
| T-18-02-04 | I | Ready flags | mitigate | Only Dam Break is ready so App cannot construct stubs. |
| T-18-02-05 | E | Pages / PAT | accept | No workflow changes. |
</threat_model>

<verification>
Run `cd web && bun run test:unit -- tests/scenes.test.ts tests/links.test.ts && bun run typecheck`. Do not edit App.tsx or Rust scenes.
</verification>

<success_criteria>
- Locked descriptions and D-05 controls exist as data.
- Credit URL helper is host-locked.
- Five later scenes remain `ready: false`.
</success_criteria>

<output>
After completion, create `.planning/phases/18-six-native-physics-demos/18-02-SUMMARY.md`
</output>
