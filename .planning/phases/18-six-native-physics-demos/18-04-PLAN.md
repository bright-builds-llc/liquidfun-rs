---
phase: 18-six-native-physics-demos
plan: "04"
type: tdd
wave: 2
depends_on: [18-01]
files_modified:
  - crates/liquidfun-wasm/src/scene/jelly_drop.rs
autonomous: true
requirements: [DEMO-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-09-18T03-36-16
generated_at: 2026-09-18T04:15:00Z
must_haves:
  truths:
    - "Jelly Drop builds an ELASTIC|SPRING filled group over two rigid bars using a public ParticleGroupRecipe."
    - "Circle/Square and Soft/Medium/Firm recreate the world; poke-jelly applies a labeled impulse without reset."
    - "After a poke and recovery steps, particle count stays bounded and the group remains a connected blob (bbox does not explode past the camera box)."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/jelly_drop.rs
      provides: "Elastic group builder, shape/softness construction, poke action"
  key_links:
    - from: crates/liquidfun-wasm/src/scene/jelly_drop.rs
      to: crates/liquidfun/src/particle/group/recipe.rs
      via: "ParticleGroupRecipe + ELASTIC|SPRING"
      pattern: "ParticleFlags::ELASTIC"
---

<objective>
Replace the Jelly Drop stub with a bounded elastic particle shape that deforms under a labeled poke and recovers without expanding the public engine API.

Purpose: D-10 spike for DEMO-05. Softness is recipe strength on reset (D-06). Pointer poking stays Phase 19.
Output: Working `jelly_drop.rs` with coherence tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/18-six-native-physics-demos/18-CONTEXT.md
@.planning/phases/18-six-native-physics-demos/18-RESEARCH.md
@crates/liquidfun/src/particle/group/recipe.rs
@crates/liquidfun/src/world/particle_object/group.rs
@crates/liquidfun/src/lib.rs

<interfaces>
`ParticleGroupRecipe` is not re-exported from the crate root. Import `liquidfun::particle::*`.

```rust
use liquidfun::collision::{CircleShape, PolygonShape, Shape};
use liquidfun::particle::{ParticleGroupDestination, ParticleGroupRecipe, ParticleGroupSource};
use liquidfun::{ParticleColor, ParticleFlags};

let source = ParticleGroupSource::filled_shapes(vec![Shape::from(CircleShape::new(Vec2::ZERO, 1.2)?)]);
let recipe = ParticleGroupRecipe::new(source, ParticleGroupDestination::New)
    .with_particle_flags(ParticleFlags::ELASTIC | ParticleFlags::SPRING)
    .with_strength(softness)?
    .with_color(ParticleColor::new(244, 114, 182, 255));
```

Defaults: Circle shape, Medium strength `1.0`, Soft `0.4`, Firm `2.0`. Two static obstacle bars near `y=1`. Group particle count ≤ 220. Poke = `World::apply_particle_linear_impulse_range` on a contiguous group range, impulse roughly `(0, -8)` or `(4, 0)` — pick one and keep it documented.

Construction controls `shape` (`circle`|`square`) and `softness` (`soft`|`medium`|`firm`) return `ControlEffect::Recreated`. Plan 01 already stores construction pairs on `SessionCore` and rebuilds via `build_scene(id, presets)` — do **not** edit `session.rs`. Read presets in `build(presets: &[(String, String)])`. Default when a key is missing: circle, medium.
</interfaces>
</context>

<feature>
  <name>Bounded elastic jelly group</name>
  <files>crates/liquidfun-wasm/src/scene/jelly_drop.rs</files>
  <behavior>
    - Create succeeds; flags include ELASTIC and SPRING; count ≤ 220.
    - Softness medium strength is 1.0 at construct.
    - `apply_control("shape", "square")` returns Recreated and the new world uses a filled polygon.
    - `apply_action("poke-jelly")` changes particle positions without changing particle count; after 60 more steps count is unchanged and max particle |x| or |y| stays inside (-7,9).
  </behavior>
  <implementation>
    If the group shreds, reduce count or strength and record the limitation in SUMMARY. Do not add a custom JS spring. Do not enable a revolute motor.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construct a coherent elastic group on two bars</name>
  <files>crates/liquidfun-wasm/src/scene/jelly_drop.rs</files>
  <read_first>18-CONTEXT.md D-09, D-10, D-11; 18-RESEARCH.md DEMO-05, elastic example, Assumption A2; crates/liquidfun/src/particle/group/recipe.rs</read_first>
  <behavior>
    - Test: `SessionCore::create(SceneId::JellyDrop)` succeeds with 8..=220 particles and at least two rigid segments.
    - Test: group flags include `ELASTIC` and `SPRING`.
    - Test: 30 steps keep particle count constant (no emit).
  </behavior>
  <action>Per D-10, write failing tests first, then implement `build()` with `ParticleGroupRecipe` and two static obstacle bars. Stay under frame caps. Do not import diagnostic group recipes. Color may be primary-text pink `(244, 114, 182, 255)` so it is not the water teal identity.</action>
  <verify>
    <automated>cargo test -p liquidfun-wasm --lib jelly_drop -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ParticleFlags::ELASTIC" crates/liquidfun-wasm/src/scene/jelly_drop.rs` matches.
    - `rg -n "SceneUnimplemented" crates/liquidfun-wasm/src/scene/jelly_drop.rs` is empty.
    - `rg -n "liquidfun-test-protocol" crates/liquidfun-wasm/src/scene/jelly_drop.rs` is empty.
    - `cargo test -p liquidfun-wasm --lib jelly_drop` exits 0.
  </acceptance_criteria>
  <done>Jelly Drop constructs a bounded elastic group on rigid bars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recreate on shape/softness and poke without reset</name>
  <files>crates/liquidfun-wasm/src/scene/jelly_drop.rs</files>
  <read_first>18-CONTEXT.md D-05, D-06; 18-RESEARCH.md control table; crates/liquidfun/src/world/particle_object/system.rs apply_particle_linear_impulse_range</read_first>
  <behavior>
    - Test: `shape=square` and `softness=firm` return `Recreated` and the next create uses those presets.
    - Test: `poke-jelly` returns Ok, particle count unchanged, some positions change.
    - Test: after poke + 60 steps, all particle positions remain finite and inside (-7,-2)..(7,9).
  </behavior>
    <action>Per D-05 and D-06, return `ControlEffect::Recreated` for `shape` and `softness` so Plan 01’s SessionCore rebuilds with the stored pairs. Apply poke as `apply_particle_linear_impulse_range` on group member ids. Do not show pointer handling. Do not recreate on poke. Do not edit `session.rs` or `scene.rs`.</action>
  <verify>
    <automated>cargo test -p liquidfun-wasm --lib -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "poke-jelly|softness|shape" crates/liquidfun-wasm/src/scene/jelly_drop.rs` matches.
    - `rg -n "apply_particle_linear_impulse_range" crates/liquidfun-wasm/src/scene/jelly_drop.rs` matches.
    - Full `liquidfun-wasm` lib tests pass.
  </acceptance_criteria>
  <done>Shape and softness reset the jelly; poke deforms it in-engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Control strings → group recipe | Only circle/square and soft/medium/firm rebuild the group. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | T | Shape/softness values | mitigate | Allowlist three+two tokens; unknown → `UnknownControl`. |
| T-18-04-02 | D | Elastic particle count | mitigate | Recipe stays ≤ 220; frame cap 512. |
| T-18-04-03 | S | Fake jelly | mitigate | Impulse + solver recovery; no JS position writes. |
| T-18-04-04 | E | Pages / PAT | accept | No workflow changes. |
</threat_model>

<verification>
Run `cargo test -p liquidfun-wasm --lib`. Do not add canvas pointer tests.
</verification>

<success_criteria>
- Elastic group constructs and stays bounded after poke.
- Shape/softness recreate; poke does not.
</success_criteria>

<output>
After completion, create `.planning/phases/18-six-native-physics-demos/18-04-SUMMARY.md`
</output>
