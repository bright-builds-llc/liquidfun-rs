---
phase: 18-six-native-physics-demos
plan: "08"
type: execute
wave: 2
depends_on: [18-02]
files_modified:
  - web/src/catalog/previews.tsx
  - web/src/components/CatalogNav.tsx
  - web/src/components/FallbackPanel.tsx
  - web/src/app.css
  - web/tests/scenes.test.ts
autonomous: true
requirements: [WEB-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-09-18T03-36-16
generated_at: 2026-09-18T04:15:00Z
must_haves:
  truths:
    - "The catalog is a six-card grid with static inline SVG, the caption Static preview, the locked description, and one Open hash link per approved id."
    - "Cards never construct a WASM session or animate fake physics."
    - "Empty and unknown fallback copy matches 18-UI-SPEC; Ready / Not ready yet chips are gone from shipped card chrome even while five ids stay ready:false until Plan 09."
  artifacts:
    - path: web/src/catalog/previews.tsx
      provides: "Static inline SVG per scene id"
      exports: ["ScenePreview"]
    - path: web/src/components/CatalogNav.tsx
      provides: "Six-card nav with hash-only Open"
    - path: web/src/app.css
      provides: "Three/two/one column card grid on existing tokens"
  key_links:
    - from: web/src/components/CatalogNav.tsx
      to: "#/scene/{id}"
      via: "Open href from SCENE_IDS only"
      pattern: 'href={`#/scene/${scene.id}`}'
    - from: web/src/catalog/previews.tsx
      to: web/src/catalog/scenes.ts
      via: "Preview keyed by id, no ProofSession"
      pattern: "ScenePreview"
---

<objective>
Replace the honest name list with six static SVG cards and update empty/unknown fallback copy, without marking stub-ready flags true yet.

Purpose: WEB-01 needs cards. D-02 forbids live thumbnails. D-03 forbids not-ready chips on the shipped card chrome; Plan 09 flips ready and wires construction.
Output: Card grid, previews, fallback copy, evolved CSS.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/frontend-ui.md
@standards-overrides.md
@.planning/phases/18-six-native-physics-demos/18-CONTEXT.md
@.planning/phases/18-six-native-physics-demos/18-UI-SPEC.md
@web/src/components/CatalogNav.tsx
@web/src/components/FallbackPanel.tsx
@web/src/app.css
@web/src/catalog/scenes.ts

<interfaces>
18-UI-SPEC page structure and catalog cards. Exact copy:

- Card CTA `Open`
- Caption `Static preview`
- Empty heading `Choose a scene`
- Empty body `Pick a demo card to open it in the shared player. Each scene is a live Rust simulation, not the static card preview.`
- Unknown heading `Scene not found`
- Unknown body `This playground link does not match a known scene. Open Dam Break, or pick a demo from the scene cards.`
Keep not-ready fallback implementation as unused defensive code. Do not edit `PAGE_SUMMARY` in this plan (Plan 09 flips it when all six ids become ready). Do not render Ready / Not ready yet chips. Current ready scene (still only dam-break until Plan 09) gets `aria-current="page"` and a 4px accent bar.

Preview art (tokens only, `aria-hidden="true"`):
- Dam Break: teal rectangle, three-segment basin, one circle
- Fountain: teal dotted arc into a U bowl
- Float or Sink: teal band, one small high rect, one large low rect
- Color Mixer: teal blob + `#F87171` blob, no animated blend
- Jelly Drop: rounded square/circle over two bars
- Water Wheel: hub, four paddles, teal jet dash
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author static SVG previews and card CSS</name>
  <files>web/src/catalog/previews.tsx, web/src/app.css</files>
  <read_first>18-CONTEXT.md D-01, D-02, D-15; 18-UI-SPEC.md Catalog Cards, Color, Spacing, Narrow-Viewport Minimum; web/src/app.css</read_first>
  <action>Per D-02 and D-15, add `ScenePreview` that switches on `SceneId` and returns inline SVG (not `img`, not `canvas`). Use only declared tokens `#39D3C7`, `#F87171`, `#CBD5E1`, `#F4F7FA`, `#071018`. Evolve `app.css`: three-column grid ≥721px, two columns 481–720, one column ≤480; card border `#2A3441`, 12px radius, padding ≥16px; preview 16:9; 44px min-block-size on Open; 4px accent bar for current. Do not add Tailwind, MysticUI, shadcn, or an icon package. Do not start WASM in this module.</action>
  <verify>
    <automated>rg -n "ScenePreview|aria-hidden" web/src/catalog/previews.tsx &amp;&amp; test ! -f web/tailwind.config.ts &amp;&amp; test ! -f web/components.json &amp;&amp; rg -n "ProofSession|loadSceneSession|requestAnimationFrame" web/src/catalog/previews.tsx; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/catalog/previews.tsx` exports `ScenePreview` and contains inline `<svg`.
    - `rg -n "ProofSession|innerHTML|tailwind" web/src/catalog/previews.tsx` is empty.
    - `web/src/app.css` contains a catalog grid and 44px min-block-size for Open.
    - No Tailwind/MysticUI/shadcn config files exist under `web/`.
  </acceptance_criteria>
  <done>Static token-only previews and card layout exist without a design-system migration.</done>
</task>

<task type="auto">
  <name>Task 2: Render six cards and update fallback/summary copy</name>
  <files>web/src/components/CatalogNav.tsx, web/src/components/FallbackPanel.tsx, web/tests/scenes.test.ts</files>
  <read_first>18-UI-SPEC.md Page Structure, Copywriting, Navigation; web/src/components/CatalogNav.tsx; web/src/components/FallbackPanel.tsx</read_first>
  <action>Per D-01, D-03, and D-14, replace the name-and-chip list with a `&lt;ul&gt;` of six `&lt;li&gt;` cards in locked order: preview, `Static preview`, title as `&lt;strong&gt;` or styled paragraph (not `h2`), description, `&lt;a href="#/scene/{id}"&gt;Open&lt;/a&gt;`. Mark current only when `maybeCurrentSceneId` matches a ready id. Remove `Ready` / `Not ready yet` chips. Update FallbackPanel empty/unknown strings to the UI-SPEC. Keep not-ready branch as dead defensive code. Do not edit `App.tsx` or `PAGE_SUMMARY`. Do not construct WASM from the catalog. Do not flip the five `ready` flags (Plan 09). Do not add pointer handlers.</action>
  <verify>
    <automated>rg -n "Static preview|Open|Choose a scene" web/src/components/CatalogNav.tsx web/src/components/FallbackPanel.tsx &amp;&amp; test -z "$(rg -n "Not ready yet|innerHTML|Dispose session|pigment chemistry" web/src/components/CatalogNav.tsx web/src/components/FallbackPanel.tsx || true)" &amp;&amp; cd web &amp;&amp; bun run test:unit -- tests/scenes.test.ts &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `CatalogNav.tsx` contains `Static preview`, `Open`, and `#/scene/${scene.id}`.
    - `rg -n "scene-chip|Not ready yet" web/src/components/CatalogNav.tsx` is empty.
    - Fallback empty/unknown bodies match 18-UI-SPEC.
    - `web/src/App.tsx` is unchanged in this plan.
    - `cd web && bun run typecheck` exits 0.
    - `web/src/catalog/scenes.ts` still has exactly one `ready: true`.
  </acceptance_criteria>
  <done>Visitors see six static cards; empty/unknown copy is honest; chips are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog Open → hash | Only allowlisted ids become hrefs. |
| Copy → DOM | Scene strings must stay text nodes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08-01 | S | Card hrefs | mitigate | `#/scene/${scene.id}` from `SCENE_IDS` only; never `/#/scene/` or hash interpolation from raw input. |
| T-18-08-02 | T | XSS from scene/error strings | mitigate | Fixed copy and catalog fields as text; no `innerHTML`. |
| T-18-08-03 | D | Live catalog WASM | mitigate | Previews are SVG; no `ProofSession` in catalog code. |
| T-18-08-04 | I | Ready flags | mitigate | Five scenes stay `ready: false` until Plan 09 wires construction. |
| T-18-08-05 | E | Pages / PAT | accept | No workflow or permission changes. |
</threat_model>

<verification>
Run the ripgrep checks, `cd web && bun run test:unit -- tests/scenes.test.ts && bun run typecheck`. Do not run Playwright against all six constructions yet.
</verification>

<success_criteria>
- Six static cards with locked copy and Open hash links.
- No design-system packages, chips, or live thumbnails.
- Ready flags still Dam Break-only.
</success_criteria>

<output>
After completion, create `.planning/phases/18-six-native-physics-demos/18-08-SUMMARY.md`
</output>
