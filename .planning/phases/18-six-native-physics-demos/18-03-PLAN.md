---
phase: 18-six-native-physics-demos
plan: "03"
type: tdd
wave: 2
depends_on: [18-01]
files_modified:
  - crates/liquidfun-wasm/src/scene/float_or_sink.rs
autonomous: true
requirements: [DEMO-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-09-18T03-36-16
generated_at: 2026-09-18T04:15:00Z
must_haves:
  truths:
    - "Float or Sink builds a water pool of about 180 particles plus reusable basin geometry and accepts Cork 0.3, Wood 0.6, and Stone 2.0 density presets."
    - "After the same step count, a cork body’s world y is above a stone body’s world y using only engine particle-body coupling."
    - "drop-body creates one dynamic fixture at the locked density; body preset changes do not recreate the world."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/float_or_sink.rs
      provides: "Native Float or Sink builder, density presets, drop-body action"
  key_links:
    - from: crates/liquidfun-wasm/src/scene.rs
      to: crates/liquidfun-wasm/src/scene/float_or_sink.rs
      via: "Factory match already calls float_or_sink::build"
      pattern: "float_or_sink::"
---

<objective>
Replace the Float or Sink stub with a native pool-and-density toy that proves light versus heavy bodies separate in y without fake buoyancy.

Purpose: D-10 spikes the least-certain buoyancy composition first (DEMO-03). D-09 and D-11 forbid JS motion or silent scene substitution.
Output: Working `float_or_sink.rs` with native honesty tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/18-six-native-physics-demos/18-CONTEXT.md
@.planning/phases/18-six-native-physics-demos/18-RESEARCH.md
@crates/liquidfun-wasm/src/scene.rs
@crates/liquidfun/src/world/particle_coupling/body_coupling.rs
@crates/liquidfun/src/world/object/body_object.rs

<interfaces>
Plan 01 already routes `SceneId::FloatOrSink` to this module. Implement:

```rust
pub(crate) fn build() -> Result<BuiltScene, SessionError>;
```

Discretion defaults from 18-RESEARCH.md: pool ~180 `WATER` particles, particle density 1.0, Cork 0.3, Wood 0.6, Stone 2.0, shared camera box `(-6,-1)..(6,8)`, teal water `(57, 211, 199, 255)`. Reuse `scene.rs` basin helpers. Default body preset is Wood.

Controls (live, do not recreate):
- `body` values `cork` | `wood` | `stone`
- `drop-body` action creates one dynamic polygon or circle with that fixture density

Use public `liquidfun` only. `World::apply_body_linear_impulse_to_center` / `apply_body_force_to_center` with `WakePolicy::Wake` if a drop needs a wake. Do not add engine APIs (D-11).
</interfaces>
</context>

<feature>
  <name>Native float-or-sink y-separation</name>
  <files>crates/liquidfun-wasm/src/scene/float_or_sink.rs</files>
  <behavior>
    - `SessionCore::create(SceneId::FloatOrSink)` succeeds; particle count is in 120..=220 and ≤ 384.
    - After dropping cork then stepping N>=120 ticks, cork y &gt; pool surface-ish; after dropping stone on a fresh world and the same ticks, stone y &lt; cork y from the other run (compare body snapshot y).
    - `apply_control("body", "cork")` returns `Live`; `apply_action("drop-body")` increases dynamic body count by one.
    - Unknown control names still return `UnknownControl`.
    - No `enable_motor`, no per-frame `set_body_transform` of the dropped body.
  </behavior>
  <implementation>
    Tests live in this file under `#[cfg(test)]` so Plans 04–05 do not touch it. If y-separation fails, retune counts/densities or record an explicit D-11 scope note in the SUMMARY — do not fake upward JS motion and do not expand `liquidfun`.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the pool and prove cork versus stone y-separation</name>
  <files>crates/liquidfun-wasm/src/scene/float_or_sink.rs</files>
  <read_first>18-CONTEXT.md D-09, D-10, D-11; 18-RESEARCH.md DEMO-03, Assumptions A1, Pitfall 3; crates/liquidfun-wasm/src/scene.rs; crates/liquidfun/src/world/object/body_object.rs</read_first>
  <behavior>
    - Test: factory create for Float or Sink succeeds and captures ≥1 segment and 0 circles until a drop (or 1 if you include a rest marker — prefer 0 extra circles).
    - Test: cork y after 120 steps is greater than stone y after 120 steps on separate worlds with the same drop spawn `(0, 6)`.
    - Test: particle count never exceeds 384.
  </behavior>
  <action>Per D-10 and D-09, write failing native tests first, then implement `build()` with a water pool using `ParticleFlags::WATER` and public fixture densities. Reuse shared basin helpers. Keep particles well under the 512 frame cap. If the first composition does not separate, retune density/radius/count inside this file only. Do not call `set_body_transform` every advance. Do not import the diagnostic protocol catalog.</action>
  <verify>
    <automated>cargo test -p liquidfun-wasm --lib float_or_sink -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SceneUnimplemented" crates/liquidfun-wasm/src/scene/float_or_sink.rs` is empty.
    - `rg -n "enable_motor|set_body_transform" crates/liquidfun-wasm/src/scene/float_or_sink.rs` is empty (drop uses `create_body` + fixture density).
    - `rg -n "liquidfun-test-protocol" crates/liquidfun-wasm/src/scene/float_or_sink.rs` is empty.
    - `cargo test -p liquidfun-wasm --lib float_or_sink` exits 0.
  </acceptance_criteria>
  <done>Cork and stone separate in y from native coupling only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply body preset and drop-body without reset</name>
  <files>crates/liquidfun-wasm/src/scene/float_or_sink.rs</files>
  <read_first>18-CONTEXT.md D-05, D-06; 18-RESEARCH.md control table; crates/liquidfun-wasm/src/scene.rs SceneHooks</read_first>
  <behavior>
    - Test: `apply_control("body", "cork"|"wood"|"stone")` returns `ControlEffect::Live`.
    - Test: `apply_action("drop-body")` after cork then stone yields two dynamic bodies with fixture densities 0.3 then 2.0.
    - Test: construction controls `water-amount` / `mix-strength` return `UnknownControl`.
  </behavior>
  <action>Per D-05 and D-06, remember the density preset on the hooks struct and create the body only on `drop-body`. Do not recreate the pool. Cap dropped bodies at 4 so circles stay ≤ 8. Use `WakePolicy::Wake` on a tiny impulse if the new body sleeps immediately. Do not edit `scene.rs` or `session.rs` unless a Plan 01 hook signature bug blocks you — prefer fixing only this file.</action>
  <verify>
    <automated>cargo test -p liquidfun-wasm --lib -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "drop-body|cork|wood|stone" crates/liquidfun-wasm/src/scene/float_or_sink.rs` matches.
    - `rg -n "ControlEffect::Recreated" crates/liquidfun-wasm/src/scene/float_or_sink.rs` is empty.
    - Full `liquidfun-wasm` lib tests still pass, including Dam Break.
  </acceptance_criteria>
  <done>Visitors can choose a density and drop it live; the pool is not rebuilt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Control strings → body factory | Only cork/wood/stone and drop-body may create bodies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | T | `apply_control` values | mitigate | Allowlist `cork`/`wood`/`stone` only; unknown values return `UnknownControl`. |
| T-18-03-02 | D | Dropped bodies / particles | mitigate | Max 4 dropped bodies; pool count ≤ 384; frame cap 512. |
| T-18-03-03 | S | Fake buoyancy | mitigate | Honesty test compares native body y; no per-frame transform writes. |
| T-18-03-04 | T | XSS | accept | No web strings in this plan. |
| T-18-03-05 | E | Pages / PAT | accept | No workflow changes. |
</threat_model>

<verification>
Run `cargo test -p liquidfun-wasm --lib`. Do not mark the catalog scene ready. Do not add pointer input.
</verification>

<success_criteria>
- Float or Sink constructs through the factory.
- Cork versus stone y-separation is a native test, not animation.
- Density preset and drop-body are live controls.
</success_criteria>

<output>
After completion, create `.planning/phases/18-six-native-physics-demos/18-03-SUMMARY.md`
</output>
