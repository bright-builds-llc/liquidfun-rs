---
phase: 18-six-native-physics-demos
plan: "07"
type: tdd
wave: 3
depends_on: [18-01, 18-03, 18-04, 18-05]
files_modified:
  - crates/liquidfun-wasm/src/scene/color_mixer.rs
autonomous: true
requirements: [DEMO-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-09-18T03-36-16
generated_at: 2026-09-18T04:15:00Z
must_haves:
  truths:
    - "Color Mixer builds two COLOR_MIXING groups, teal (57,211,199,255) and destructive red (248,113,113,255)."
    - "mix-strength Off (0.0) keeps color bytes stable; Gentle 0.25 or Strong 0.5 changes captured color lanes after contact."
    - "Stir-speed applies tangential particle forces each advance and does not recreate the world."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/color_mixer.rs
      provides: "Two mixing groups, construction mix-strength, live stir"
  key_links:
    - from: crates/liquidfun-wasm/src/scene/color_mixer.rs
      to: crates/liquidfun/src/particle/solver/material.rs
      via: "COLOR_MIXING + color_mixing_strength"
      pattern: "ParticleFlags::COLOR_MIXING"
---

<objective>
Replace the Color Mixer stub with two contact-mixing groups and a labeled stir force, proven by changing color-lane bytes.

Purpose: DEMO-04 must show engine mixing, not canvas blending. Mix strength is construction-only (D-06).
Output: Working `color_mixer.rs` with Off-stable and On-changes tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@.planning/phases/18-six-native-physics-demos/18-CONTEXT.md
@.planning/phases/18-six-native-physics-demos/18-RESEARCH.md
@crates/liquidfun/src/particle/solver/material.rs
@crates/liquidfun/src/world/particle_object/system.rs

<interfaces>
```rust
ParticleSystemDef::default()
    .with_color_mixing_strength(strength)? // 0.0 off, 0.25 gentle, 0.5 strong
ParticleFlags::WATER | ParticleFlags::COLOR_MIXING
```

Two filled groups that overlap or sit in contact. Stir: `World::apply_particle_force_range` with a tangential vector each `on_advance` when stir is `slow` or `fast`. `off` applies no force. Mix-strength returns `Recreated`. Stir-speed returns `Live`. Default Strong + Slow.

Label this particle-color mixing in comments only; UI copy lands in Plan 09. Do not lerp colors in capture. Do not edit `session.rs`.
</interfaces>
</context>

<feature>
  <name>Contact-driven color mixing</name>
  <files>crates/liquidfun-wasm/src/scene/color_mixer.rs</files>
  <behavior>
    - Create succeeds with two distinct initial color byte patterns.
    - Off mix: after 120 steps with stir slow, `particle_colors()` equals the step-0 snapshot (or differs only in alpha if you never write alpha — prefer exact equality).
    - Strong mix: after 120 steps, `particle_colors()` != step-0 snapshot.
    - Particle count stays constant (no fountain emit).
  </behavior>
  <implementation>
    Both particles in a mixing pair must carry `COLOR_MIXING`. Strength 0 skips the solver pass.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build two COLOR_MIXING groups</name>
  <files>crates/liquidfun-wasm/src/scene/color_mixer.rs</files>
  <read_first>18-CONTEXT.md D-09, D-14; 18-RESEARCH.md color-mixing honesty test; crates/liquidfun/src/particle/solver/material.rs</read_first>
  <behavior>
    - Test: create succeeds; count is 40..=220; captured colors contain both teal and red bytes.
    - Test: every particle flag set includes `COLOR_MIXING`.
  </behavior>
  <action>Per D-09, write failing tests first, then place two filled groups in a bowl using shared basin helpers. Use public group or particle create APIs. Do not blend colors in `collect_*` or capture. Do not claim pigment chemistry in comments.</action>
  <verify>
    <automated>cargo test -p liquidfun-wasm --lib color_mixer -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "COLOR_MIXING" crates/liquidfun-wasm/src/scene/color_mixer.rs` matches.
    - `rg -n "SceneUnimplemented" crates/liquidfun-wasm/src/scene/color_mixer.rs` is empty.
    - `rg -n "pigment" crates/liquidfun-wasm/src/scene/color_mixer.rs` is empty.
    - `cargo test -p liquidfun-wasm --lib color_mixer` exits 0.
  </acceptance_criteria>
  <done>Two distinctly colored mixing groups construct in-engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove Off is stable and Strong changes color lanes</name>
  <files>crates/liquidfun-wasm/src/scene/color_mixer.rs</files>
  <read_first>18-CONTEXT.md D-05, D-06; 18-RESEARCH.md material.rs notes; crates/liquidfun/src/world/particle_object/system.rs</read_first>
  <behavior>
    - Test: `mix-strength=off` Recreated world keeps colors equal after 120 stirred steps.
    - Test: default Strong changes `particle_colors()` after 120 steps.
    - Test: `stir-speed` off/slow/fast returns `Live`.
  </behavior>
  <action>Per D-06, recreate on mix-strength because there is no live system-def setter. Apply stir with `apply_particle_force_range` on a contiguous range each advance. Do not implement mixing in TypeScript. Do not edit `session.rs`.</action>
  <verify>
    <automated>cargo test -p liquidfun-wasm --lib -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "with_color_mixing_strength|apply_particle_force_range" crates/liquidfun-wasm/src/scene/color_mixer.rs` matches.
    - `rg -n "lerp|mix.*canvas|innerHTML" crates/liquidfun-wasm/src/scene/color_mixer.rs` is empty.
    - Full `liquidfun-wasm` lib tests pass.
  </acceptance_criteria>
  <done>Color-lane bytes change only when the engine mix pass runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mix/stir strings → system def / forces | Only locked tokens change strength or stir. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07-01 | T | Mix/stir values | mitigate | Allowlist off/gentle/strong and off/slow/fast. |
| T-18-07-02 | S | Fake mixing | mitigate | Honesty tests compare Rust color lanes; no JS blend. |
| T-18-07-03 | D | Stir forces | mitigate | Bounded group size; no extra emission. |
| T-18-07-04 | E | Pages / PAT | accept | No workflow changes. |
</threat_model>

<verification>
Run `cargo test -p liquidfun-wasm --lib`. Do not add pointer stirring. Do not claim pigment chemistry.
</verification>

<success_criteria>
- Off keeps colors stable; Strong changes captured bytes.
- Stir is a live force, mix-strength recreates.
</success_criteria>

<output>
After completion, create `.planning/phases/18-six-native-physics-demos/18-07-SUMMARY.md`
</output>
