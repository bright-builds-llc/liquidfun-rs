---
phase: 18-six-native-physics-demos
plan: "06"
type: tdd
wave: 3
depends_on: [18-01, 18-03, 18-04, 18-05]
files_modified:
  - crates/liquidfun-wasm/src/scene/dam_break.rs
  - crates/liquidfun-wasm/src/scene/fountain.rs
autonomous: true
requirements: [DEMO-01, DEMO-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-09-18T03-36-16
generated_at: 2026-09-18T04:15:00Z
must_haves:
  truths:
    - "Dam Break Medium/Normal is still the documented 192-particle basin; Small is 8×8=64, Large is 20×14=280; gravity Low/Normal/High is (0,-6)/(0,-10)/(0,-16)."
    - "drop-obstacle raises the existing circle to (2.5, 7.2) and wakes it; reset-obstacle restores (2.5, 5.5); neither recreates the world."
    - "Fountain emits a bounded stream whose count plateaus at ≤ 320; emission-rate, launch-speed, and aim-angle apply live."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/dam_break.rs
      provides: "Evolved Dam Break with water-amount, gravity, obstacle actions"
    - path: crates/liquidfun-wasm/src/scene/fountain.rs
      provides: "Bounded fountain emitter and bowl"
  key_links:
    - from: crates/liquidfun-wasm/src/scene/dam_break.rs
      to: crates/liquidfun/src/world/object/body_object.rs
      via: "set_body_transform plus apply_body_linear_impulse_to_center Wake"
      pattern: "set_body_transform"
    - from: crates/liquidfun-wasm/src/scene/fountain.rs
      to: crates/liquidfun/src/particle/definition/particle.rs
      via: "with_lifetime + with_maximum_count + destroy_by_age"
      pattern: "with_lifetime"
---

<objective>
Evolve Dam Break into the named demo with locked construction presets and obstacle actions, and replace the Fountain stub with a lifetime-capped stream.

Purpose: D-10 says evolve the existing basin rather than replace it. DEMO-02 needs a plateauing emitter, not an unbounded list.
Output: Dam Break controls plus a working fountain module.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@.planning/phases/18-six-native-physics-demos/18-CONTEXT.md
@.planning/phases/18-six-native-physics-demos/18-RESEARCH.md
@crates/liquidfun-wasm/src/scene/dam_break.rs
@crates/liquidfun-wasm/src/scene/fountain.rs
@crates/liquidfun/src/world/object/body_object.rs

<interfaces>
Dam Break presets (recreate via Plan 01 bag):
- `water-amount`: `small` 8×8=64, `medium` 16×12=192, `large` 20×14=280
- `gravity`: `low` (0,-6), `normal` (0,-10), `high` (0,-16)

Dam Break actions (live):
- `drop-obstacle`: `set_body_transform(circle, (2.5, 7.2), 0.0)` then `apply_body_linear_impulse_to_center(..., Vec2::new(0.0, -0.1), WakePolicy::Wake)` so the body wakes
- `reset-obstacle`: transform back to `(2.5, 5.5)` with zero angle

Fountain (all live; emit in `on_advance`):
- `emission-rate`: `off` 0, `low` 1, `medium` 2, `high` 3 particles per step
- `launch-speed`: `slow` 4, `medium` 8, `fast` 12
- `aim-angle`: `left` `-τ/8`, `up` `0`, `right` `+τ/8` from world up, using `std::f32::consts::TAU`
- lifetime 3.0s, `maximum_count` 320, `destroy_by_age(true)`, bowl using shared basin helpers, teal water

Do not edit `session.rs`. Do not import `liquidfun-test-protocol`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evolve Dam Break water, gravity, and obstacle</name>
  <files>crates/liquidfun-wasm/src/scene/dam_break.rs</files>
  <read_first>18-CONTEXT.md D-05, D-06, D-10; crates/liquidfun-wasm/src/scene/dam_break.rs; crates/liquidfun-wasm/src/session.rs construction tests</read_first>
  <behavior>
    - Test: default create is still 192 particles and gravity -10.
    - Test: presets `small`/`large`/`low`/`high` return Recreated and the rebuilt world has 64 or 280 particles and the matching gravity bits.
    - Test: `drop-obstacle` changes circle y upward then downward after steps; `reset-obstacle` restores `(2.5, 5.5)` bits; both return live / Ok without Recreated.
    - Existing Dam Break advance/capture tests still pass on Medium/Normal.
  </behavior>
  <action>Per D-10, keep the current circle obstacle. Read Plan 01 presets in `build`. Recreating controls return `ControlEffect::Recreated`. Obstacle actions use public `set_body_transform` and a wake impulse. Do not replace Dam Break with canned motion. Do not use Dispose-red semantics.</action>
  <verify>
    <automated>cargo test -p liquidfun-wasm --lib dam_break -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "water-amount|drop-obstacle|reset-obstacle" crates/liquidfun-wasm/src/scene/dam_break.rs` matches.
    - `rg -n "16 \\* 12|PARTICLE_COUNT" crates/liquidfun-wasm/src/scene/dam_break.rs` still documents 192 as Medium.
    - `rg -n "liquidfun-test-protocol" crates/liquidfun-wasm/src/scene/dam_break.rs` is empty.
    - `cargo test -p liquidfun-wasm --lib dam_break` exits 0.
  </acceptance_criteria>
  <done>Dam Break is the named demo with locked presets and a droppable obstacle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build a plateauing Fountain stream</name>
  <files>crates/liquidfun-wasm/src/scene/fountain.rs</files>
  <read_first>18-CONTEXT.md D-05, D-06, D-09; 18-RESEARCH.md bounded fountain emit, Pitfall 2; crates/liquidfun/src/particle/definition/system_definition.rs</read_first>
  <behavior>
    - Test: `SessionCore::create(SceneId::Fountain)` succeeds.
    - Test: default Medium/Up/Medium after 240 steps has particle count ≤ 320 and count(step 240) ≤ count(step 210) + 2 (plateau, not climb-to-512).
    - Test: `emission-rate=off` for 60 steps does not increase count.
    - Test: the three fountain controls return `ControlEffect::Live`.
  </behavior>
  <action>Per D-09, emit in `on_advance` with `ParticleDef::with_lifetime(3.0)` and `ParticleFlags::WATER`. Aim uses TAU. Full system = no-op emit. Do not poison the session at 320. Do not edit `session.rs`.</action>
  <verify>
    <automated>cargo test -p liquidfun-wasm --lib -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SceneUnimplemented" crates/liquidfun-wasm/src/scene/fountain.rs` is empty.
    - `rg -n "with_lifetime|with_maximum_count|TAU" crates/liquidfun-wasm/src/scene/fountain.rs` matches.
    - `rg -n "512" crates/liquidfun-wasm/src/scene/fountain.rs` does not use 512 as the fountain max (use 320).
    - Full `liquidfun-wasm` lib tests pass.
  </acceptance_criteria>
  <done>Fountain aims a bounded stream that plateaus instead of hitting the frame cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preset strings → rebuild/emit | Only locked ids change gravity, counts, or jet policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06-01 | T | Dam Break / Fountain values | mitigate | Allowlist the locked tokens; unknown → `UnknownControl`. |
| T-18-06-02 | D | Fountain emission | mitigate | Lifetime 3s, max 320, destroy-by-age, no-op when full. |
| T-18-06-03 | I | Obstacle identity | mitigate | Drop/reset the same body id; do not create unbounded extra obstacles. |
| T-18-06-04 | E | Pages / PAT | accept | No workflow changes. |
</threat_model>

<verification>
Run `cargo test -p liquidfun-wasm --lib`. Do not add pointer aiming. Do not mark catalog ready.
</verification>

<success_criteria>
- Dam Break Medium/Normal remains the documented basin and gains locked controls.
- Fountain population plateaus at ≤ 320.
</success_criteria>

<output>
After completion, create `.planning/phases/18-six-native-physics-demos/18-06-SUMMARY.md`
</output>
