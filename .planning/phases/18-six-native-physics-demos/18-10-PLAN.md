---
phase: 18-six-native-physics-demos
plan: "10"
type: execute
wave: 5
depends_on: [18-09]
files_modified:
  - web/e2e/player.spec.ts
  - README.md
  - TESTING.md
  - .planning/phases/18-six-native-physics-demos/18-REVIEW.md
autonomous: true
requirements: [WEB-01, WEB-04, WEB-08, DEMO-01, DEMO-02, DEMO-03, DEMO-04, DEMO-05, DEMO-06]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-09-18T03-36-16
generated_at: 2026-09-18T04:15:00Z
must_haves:
  truths:
    - "Chromium opens each of the six cards and hash URLs, sees Playing plus the scene title and Scene source credits, and Reset scene restarts the step series."
    - "Switching scenes disposes the prior world so only one data-step-index series advances."
    - "A Dam Break construction Apply setting change shows the reset hint and returns to Playing."
    - "Independent AI review acknowledges the six-scene diff and evidence at an exact digest; the implementing agent does not approve its own work."
  artifacts:
    - path: web/e2e/player.spec.ts
      provides: "Thin six-scene open/reset/switch/credits proofs, not WEBTEST-01"
    - path: .planning/phases/18-six-native-physics-demos/18-REVIEW.md
      provides: "Independent exact-digest acknowledgment"
  key_links:
    - from: web/e2e/player.spec.ts
      to: "/liquidfun-rs/#/scene/{id}"
      via: "One loop over SCENE_IDS plus Dam Break Apply setting"
      pattern: "/liquidfun-rs/#/scene/"
    - from: .planning/phases/18-six-native-physics-demos/18-REVIEW.md
      to: "digest"
      via: "Separate reviewer identity and SHA-256"
      pattern: "digest"
---

<objective>
Prove each native scene locally in Chromium and obtain independent AI review, without a Pages deploy gate or Phase 19 pointer matrix.

Purpose: D-16 local proofs and D-17 review close the phase. Existing main-push Pages workflow stays untouched.
Output: Updated player spec, `just web-player-smoke` passing, `18-REVIEW.md`, honest README/TESTING mentions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@.planning/phases/18-six-native-physics-demos/18-CONTEXT.md
@.planning/phases/18-six-native-physics-demos/18-UI-SPEC.md
@web/e2e/player.spec.ts
@README.md
@TESTING.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-08-PLAN.md

<interfaces>
Keep Playwright `baseURL http://127.0.0.1:4173` and `/liquidfun-rs/#/scene/{id}`.

Replace the Fountain not-ready assertion. For each `SCENE_IDS` entry:
1. Click catalog `Open` or `goto` the hash.
2. Assert `Playing` (no reduced-motion emulation) and heading `{Title}`.
3. Assert `Scene source`, `View scene source`, and `Third-party notices`.
4. Click `Reset scene` and assert `data-step-index` restarts near 0.

Also:
- From Dam Break, open Fountain, assert Playing Fountain, and `data-step-index` restarts (dispose).
- Dam Break: `Gravity` select shows the reset hint; click `Apply setting`; return to Playing.
- Keep unknown-hash `Scene not found` + `Open Dam Break`.
- Keep hidden-tab max-4 proof on Dam Break only.
- Do not add canvas pointer, poke-via-pointer, or narrow-width WEB-07 acceptance.
- Do not require a new live Pages URL as a pass condition.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend player-smoke to six native scenes</name>
  <files>web/e2e/player.spec.ts, README.md, TESTING.md</files>
  <read_first>web/e2e/player.spec.ts; 18-CONTEXT.md D-16; 18-UI-SPEC.md Copywriting; README.md; TESTING.md</read_first>
  <action>Per D-16, rewrite the Fountain not-ready case into a ready-scene switch. Add a focused loop over the six ids for open / Playing / title / credits / reset. Add one Dam Break `Apply setting` construction proof. Keep unknown-hash and hidden-tab tests. Do not add WEBTEST-01 pointer coverage. In README/TESTING, say the playground has six native scenes and `just web-player-smoke` proves open/reset/switch locally; do not claim complete parity, pigment chemistry, live card previews, or a new Pages URL. Run `just web-wasm` then `just web-player-smoke` once and keep the log under ignored `target/web-build/`.</action>
  <verify>
    <automated>rg -n "Apply setting|View scene source|fountain is not ready yet" web/e2e/player.spec.ts &amp;&amp; just web-player-smoke</automated>
  </verify>
  <acceptance_criteria>
    - `web/e2e/player.spec.ts` contains `/liquidfun-rs/#/scene/` for multiple ids and `Apply setting`.
    - `rg -n "Fountain is not ready yet" web/e2e/player.spec.ts` is empty.
    - `rg -n "onPointer|pointerdown|WEBTEST-01" web/e2e/player.spec.ts` is empty.
    - `just web-player-smoke` exits 0.
    - README/TESTING mention six scenes and `web-player-smoke` without a new required Pages URL.
    - `.github/workflows/pages.yml` is unchanged in this plan.
  </acceptance_criteria>
  <done>Local Chromium proves each scene opens, steps, resets, shows credits, and disposes on switch.</done>
</task>

<task type="auto">
  <name>Task 2: Obtain independent exact-digest review</name>
  <files>.planning/phases/18-six-native-physics-demos/18-REVIEW.md</files>
  <read_first>AGENTS.md Independent review; 18-CONTEXT.md D-17; .planning/phases/17-shared-player-and-early-pages-delivery/17-REVIEW.md</read_first>
  <action>Per D-17, the implementing agent must not approve its own work. Build a review manifest over the six scene modules, factory, catalog/controls/credits, App wiring, player spec, and smoke log. Compute one lowercase SHA-256 digest. Launch a separate identified AI reviewer (not this implementing agent) to inspect the complete relevant diff and evidence and record an acknowledgment bound to that digest, the reviewer’s actual identity, and the actual review time. Write `18-REVIEW.md` identifying AI review honestly. Passing `just web-player-smoke` alone is not an acknowledgment. Do not deploy Pages, publish a crate, or treat this as WEBTEST-01.</action>
  <verify>
    <automated>rg -n "digest|reviewer|acknowled" .planning/phases/18-six-native-physics-demos/18-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - `18-REVIEW.md` contains a 64-character hex digest, a reviewer identity different from the implementing agent label, and an ISO timestamp.
    - The file states independent AI review, not human approval.
    - The acknowledgment quotes the same digest as the manifest.
    - No self-approval sentence from the implementing agent is used as the acknowledgment.
  </acceptance_criteria>
  <done>Independent review is bound to the exact six-scene digest and local proof evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preview server → Chromium | Tests load real JS/WASM under `/liquidfun-rs/`. |
| Review digest → acknowledgment | Only a separate reviewer may bind approval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10-01 | T | Production-base assets | mitigate | Spec stays on `/liquidfun-rs/` WASM URLs. |
| T-18-10-02 | S | Hash-route handling | mitigate | Unknown id still shows `Scene not found`; six allowlisted ids play. |
| T-18-10-03 | D | Session disposal | mitigate | Scene-switch assertion requires a restarted step series. |
| T-18-10-04 | T | XSS from scene/error strings | mitigate | Spec asserts fixed headings, `Apply setting`, and credit link names. |
| T-18-10-05 | R | Self-review | mitigate | Separate AI reviewer + exact digest (D-17). |
| T-18-10-06 | E | Pages / PAT | accept | No deploy; existing workflow unchanged; no PAT. |
</threat_model>

<verification>
Run `just web-player-smoke` and confirm `18-REVIEW.md` fields. Do not push to main or claim a new live Pages URL as this phase’s gate.
</verification>

<success_criteria>
- Each approved scene is proven locally: open, Playing, credits, reset, dispose-on-switch.
- Construction reset copy and Apply setting are exercised on Dam Break.
- Independent AI review is recorded.
- Phase 19 pointer/WEBTEST-01 and a new Pages deploy are not treated as pass conditions.
</success_criteria>

<output>
After completion, create `.planning/phases/18-six-native-physics-demos/18-10-SUMMARY.md`
</output>
