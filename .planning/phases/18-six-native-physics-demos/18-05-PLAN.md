---
phase: 18-six-native-physics-demos
plan: "05"
type: tdd
wave: 2
depends_on: [18-01]
files_modified:
  - crates/liquidfun-wasm/src/scene/water_wheel.rs
autonomous: true
requirements: [DEMO-06]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-09-18T03-36-16
generated_at: 2026-09-18T04:15:00Z
must_haves:
  truths:
    - "Water Wheel builds a motor-off revolute hub with four paddle segments and a bounded jet."
    - "Wheel angle changes while emission is On and jet strength is at least Medium; angle stays within 1e-3 rad while emission is Off for the same step count."
    - "Paddles are captured as transformed rigid segments from BodySnapshot::transform().apply, not CSS or scripted angles."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/water_wheel.rs
      provides: "Pinned paddle wheel, jet emit in on_advance, live jet/emission controls"
  key_links:
    - from: crates/liquidfun-wasm/src/scene/water_wheel.rs
      to: crates/liquidfun/src/joint/definition/revolute_prismatic.rs
      via: "RevoluteJointDef without with_motor"
      pattern: "RevoluteJointDef::new"
---

<objective>
Replace the Water Wheel stub with a native jet-and-joint toy whose rotation comes only from particle-body coupling.

Purpose: D-10 spike for DEMO-06. D-09 and D-11 forbid motor spin or silent substitution. The pinwheel-and-balls fallback is allowed only as an explicit SUMMARY revision.
Output: Working `water_wheel.rs` with motor-off angle proofs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/18-six-native-physics-demos/18-CONTEXT.md
@.planning/phases/18-six-native-physics-demos/18-RESEARCH.md
@crates/liquidfun/src/joint/definition/revolute_prismatic.rs
@crates/liquidfun/src/math/transform.rs
@crates/liquidfun/src/world/body.rs

<interfaces>
```rust
let joint = RevoluteJointDef::new(ground, wheel)?.with_frame(hub_local_ground, hub_local_wheel, 0.0)?;
world.create_joint(JointDef::from(joint))?;
// Do not call with_motor / with_motor_speed.

let pose = world.body_snapshot(wheel)?.transform();
for [a, b] in PADDLE_LOCAL_SEGMENTS {
    let start = pose.apply(a);
    let end = pose.apply(b);
}
```

Defaults: hub near `(0, 3)`, four local paddle segments, jet from the left, `maximum_count` 320, lifetime ~3s, `destroy_by_age(true)`. Jet strength Weak/Med/Strong maps to launch speed 4/8/12. Emission `on`|`off`. Both controls are live (`ControlEffect::Live`). `on_advance` emits only when emission is on. Treat a full particle system as a no-op emit, not a session poison.

If the wheel does not rotate after retuning jet aim, paddle area, and particle radius, stop and write an explicit D-11 revision in the SUMMARY proposing the documented pinwheel-and-dropped-balls toy. Do not enable the revolute motor. Do not edit `session.rs`.
</interfaces>
</context>

<feature>
  <name>Motor-off water wheel</name>
  <files>crates/liquidfun-wasm/src/scene/water_wheel.rs</files>
  <behavior>
    - Create succeeds; joint exists; `enable_motor` is false on the revolute definition used.
    - After 180 steps with emission On + Medium jet, `abs(angle_after - angle_before) &gt; 0.05`.
    - After 180 steps with emission Off, `abs(angle_after - angle_before) &lt; 0.01`.
    - `collect_segments` returns ≥4 paddle segments that move when the wheel rotates.
    - Particle count plateaus at ≤ 320 after 240 steps.
  </behavior>
  <implementation>
    Emit in `on_advance` with `ParticleDef::with_lifetime` + `ParticleFlags::WATER`. Capture hub as one rigid circle plus transformed paddles.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin a motor-off wheel and capture transformed paddles</name>
  <files>crates/liquidfun-wasm/src/scene/water_wheel.rs</files>
  <read_first>18-CONTEXT.md D-09, D-10, D-11; 18-RESEARCH.md DEMO-06, motor-off example, Assumption A3; crates/liquidfun/src/joint/definition/revolute_prismatic.rs; crates/liquidfun/src/math/transform.rs</read_first>
  <behavior>
    - Test: `SessionCore::create(SceneId::WaterWheel)` succeeds with ≥1 circle and ≥4 segments.
    - Test: the created revolute joint was not built with `with_motor`.
    - Test: `collect_segments` values change after a forced `set_body_transform` on the wheel in the test only (proves capture uses the live pose). Production `on_advance` must not call `set_body_transform` to spin.
  </behavior>
  <action>Per D-10 and D-09, write failing tests first, then implement the ground, hub circle, four local paddles, and motor-off revolute joint. Reuse basin helpers only if they fit; a small trough under the wheel is enough. Do not import the diagnostic joint catalog. Do not call `with_motor`.</action>
  <verify>
    <automated>cargo test -p liquidfun-wasm --lib water_wheel -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "RevoluteJointDef::new" crates/liquidfun-wasm/src/scene/water_wheel.rs` matches.
    - `rg -n "with_motor" crates/liquidfun-wasm/src/scene/water_wheel.rs` is empty.
    - `rg -n "SceneUnimplemented" crates/liquidfun-wasm/src/scene/water_wheel.rs` is empty.
    - `rg -n "liquidfun-test-protocol" crates/liquidfun-wasm/src/scene/water_wheel.rs` is empty.
    - `cargo test -p liquidfun-wasm --lib water_wheel` exits 0.
  </acceptance_criteria>
  <done>A pinned wheel exists and paddle segments follow the body transform.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the jet turns the wheel and emission stays bounded</name>
  <files>crates/liquidfun-wasm/src/scene/water_wheel.rs</files>
  <read_first>18-CONTEXT.md D-05, D-06, D-09, D-11; 18-RESEARCH.md Pitfall 2 and 3; crates/liquidfun/src/particle/definition/particle.rs</read_first>
  <behavior>
    - Test: emission On + medium jet for 180 steps changes wheel angle by more than 0.05 rad.
    - Test: emission Off for 180 steps leaves angle nearly unchanged.
    - Test: after 240 On steps, particle count ≤ 320 and is not still strictly increasing over the last 30 steps.
    - Test: `jet-strength` and `emission` return `ControlEffect::Live`.
  </behavior>
  <action>Per D-09, emit water in `on_advance` only when emission is on. Use lifetime + `maximum_count` 320 + `destroy_by_age(true)`. Full-system create is a no-op. If angle does not change after retuning, record the D-11 pinwheel-and-balls revision in SUMMARY and stop — do not enable the motor. Do not edit `session.rs`.</action>
  <verify>
    <automated>cargo test -p liquidfun-wasm --lib -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "with_lifetime|with_maximum_count" crates/liquidfun-wasm/src/scene/water_wheel.rs` matches.
    - `rg -n "with_motor" crates/liquidfun-wasm/src/scene/water_wheel.rs` is empty.
    - `rg -n "set_body_transform" crates/liquidfun-wasm/src/scene/water_wheel.rs` is empty outside `#[cfg(test)]`.
    - Full `liquidfun-wasm` lib tests pass.
  </acceptance_criteria>
  <done>The jet rotates the wheel natively and particle count plateaus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Control strings → emit policy | Only weak/medium/strong and on/off change the jet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05-01 | T | Jet/emission values | mitigate | Allowlist four tokens; unknown → `UnknownControl`. |
| T-18-05-02 | D | Unbounded emission | mitigate | `maximum_count` 320, lifetime, destroy-by-age; no-op when full. |
| T-18-05-03 | S | Scripted rotation | mitigate | Motor off; angle test requires emission On; no production `set_body_transform` spin. |
| T-18-05-04 | E | Pages / PAT | accept | No workflow changes. |
</threat_model>

<verification>
Run `cargo test -p liquidfun-wasm --lib`. Do not add pointer aiming. Do not deploy Pages.
</verification>

<success_criteria>
- Wheel rotation is native and motor-off.
- Emission is bounded.
- Failure to rotate is an explicit D-11 revision, not a motor.
</success_criteria>

<output>
After completion, create `.planning/phases/18-six-native-physics-demos/18-05-SUMMARY.md`
</output>
