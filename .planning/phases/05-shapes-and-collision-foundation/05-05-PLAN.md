---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-07-11T14-53-25
generated_at: 2026-07-11T15:12:45.000Z
phase: 05-shapes-and-collision-foundation
plan: "05"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - crates/liquidfun/src/collision/tree.rs
  - crates/liquidfun/src/collision/tree/pool.rs
  - crates/liquidfun/src/collision/tree/traversal.rs
  - crates/liquidfun/src/collision/broad_phase.rs
  - crates/liquidfun/tests/collision_broad_phase.rs
autonomous: true
requirements:
  - COLL-04
  - COLL-05
  - COLL-07
must_haves:
  truths:
    - DynamicTree proxies have opaque scoped generational identity while private node coordinates preserve pinned allocation and tie order.
    - Proxy lifecycle, fat movement, balancing, metrics, visitors, and broad-phase pairs reproduce pinned branches and bounded behavior.
    - Pair order is exact while ordinary query/ray collection order remains explicitly unspecified and set-like.
    - Pure filtering and touch/refilter reconsideration are complete without claiming Phase 6 contact-manager lifecycle.
  artifacts:
    - crates/liquidfun/src/collision/tree.rs
    - crates/liquidfun/src/collision/broad_phase.rs
    - crates/liquidfun/tests/collision_broad_phase.rs
  key_links:
    - Tree and broad phase consume Plan 05-01 AABBs but expose no world/fixture handles or private node slots.
    - Ordered pair output and pure filter results form the exact Phase 6 contact-candidate seam.
---

# Plan 05-05: Dynamic Tree, Broad Phase, and Filtering

<objective>
Implement the generic dynamic AABB tree, opaque proxy lifecycle, exact private ordering/tie behavior, visitor queries/rays, broad-phase movement/pair generation, and pure filter/refilter substrate.
</objective>

<decision_coverage>
Implements locked decisions D-13, D-14, D-15, D-16, and D-17: opaque scoped proxy identity, pinned private tree/pair order, visitor collection contracts, and pure filter/refilter behavior with the Phase 6 contact boundary intact.
</decision_coverage>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| Proxy operations | Foreign, stale, destroyed IDs and arbitrary AABBs/displacement | Tree-scope/generation validation, finite valid bounds, explicit stale/foreign errors. |
| Query/ray visitors | Caller controls traversal continuation and clipping | Closed control enum; finite clip fraction within current interval; borrow-scoped payload only. |
| Move/touch buffers | Duplicate/tombstoned occurrences | Bounded live validation, tombstone destruction, final exact sort/dedup. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T05-1 | Spoofing | `ProxyId` | Mitigate | Tree-scoped generational validation and no raw constructor/serialization/ordering. |
| T05-2 | Tampering | Private node order | Mitigate | Separate node coordinates, source-faithful free list, exact tie tests, and no public sorting key. |
| T05-3 | Denial of service | Traversal/move buffer | Mitigate | Iterative bounded stacks, checked capacity growth, stale tombstones, and finite visitor controls. |
| T05-4 | Repudiation | Query versus pair ordering | Mitigate | Types/tests declare ordered pairs and set-like ordinary results separately. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement opaque proxy identity and source-faithful node pool/tree</name>
  <files>crates/liquidfun/src/collision/tree.rs, crates/liquidfun/src/collision/tree/pool.rs, crates/liquidfun/tests/collision_broad_phase.rs</files>
  <read_first>
    - crates/liquidfun/src/arena.rs
    - crates/liquidfun/src/collision/types.rs
    - crates/liquidfun/src/math/settings.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2DynamicTree.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2DynamicTree.cpp
    - third_party/liquidfun/liquidfun/Box2D/Testbed/Tests/DynamicTreeTest.h
    - .planning/phases/03-rust-object-model-and-storage-architecture/03-CONTEXT.md
    - standards/languages/rust.md
  </read_first>
  <action>
Implement public `DynamicTree<T>` and opaque tree-scoped generational `ProxyId` with private node coordinate, generation retirement, and explicit foreign/stale/destroyed errors. Reproduce pinned pool growth/free-list/LIFO reuse, leaf/internal representation, insertion cost calculations, equal-cost descent to child 2, balancing rotations, equal-height grandchild-2 choice, removal/reinsertion, validation, height/balance/area-ratio metrics, and origin shift where applicable. Creation fattens tight bounds by `AABB_EXTENSION`; movement is a no-op when contained, otherwise rebuilds the fat bound and sign-extends `AABB_MULTIPLIER * displacement`. Provide payload access/update without leaking slots. Add invariant/property tests for lifecycle, stale/foreign IDs, free-list reuse, tie branches, balancing, fat bounds, containment no-op, displacement signs, metrics, and randomized insert/move/remove consistency. Expose these public values through `liquidfun::collision::tree` and the pair/filter surface through `liquidfun::collision::broad_phase`; wave-2 integration tests use those child-module paths until Plan 05-07 finalizes root re-exports.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_broad_phase dynamic_tree --all-features`
    - `cargo test -p liquidfun tree::pool --all-features`
    - `! rg "pub .*slot|impl Ord for ProxyId|Serialize.*ProxyId|from_raw|unsafe" crates/liquidfun/src/collision/tree.rs crates/liquidfun/src/collision/tree`
  </verify>
  <acceptance_criteria>
    - Proxy identity rejects stale/foreign access and exposes no coordinate, constructor, serialization, or `Ord` promise.
    - `liquidfun::collision::tree` and `liquidfun::collision::broad_phase` are executable public import paths for this plan's integration tests.
    - Pool, insertion, rotation, movement, and metric equality branches match pinned source and are named tests.
    - Randomized invariants pass without unsafe code.
    - Dynamic-tree/pool tests exit 0.
  </acceptance_criteria>
  <done>The tree preserves pinned private topology/order while exposing safe scoped proxy identity.</done>
</task>

<task type="auto">
  <name>Task 2: Implement borrow-scoped query and ray visitors</name>
  <files>crates/liquidfun/src/collision/tree.rs, crates/liquidfun/src/collision/tree/traversal.rs, crates/liquidfun/tests/collision_broad_phase.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/tree/pool.rs
    - crates/liquidfun/src/collision/types.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2DynamicTree.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2DynamicTree.cpp
    - third_party/liquidfun/liquidfun/Box2D/Testbed/Tests/RayCast.h
  </read_first>
  <action>
Add borrow-scoped AABB query and ray visitors using closed controls for continue/stop and ignore/terminate/clip. Preserve raw diagnostic traversal by pushing child 1 then child 2 so the LIFO stack visits child 2 first, but document ordinary consumer result order as unspecified. Validate every returned clip fraction as finite, nonnegative, and no greater than the current maximum before narrowing the segment AABB. Optional owned collection helpers may collect unique proxy IDs but must not promise traversal order. Add tests for continue/stop, ignore/terminate/clip, invalid clip rejection without state corruption, child-2-first diagnostic traversal, and set-equivalent collect-all results across balancing histories.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_broad_phase query --all-features`
    - `cargo test -p liquidfun --test collision_broad_phase ray --all-features`
    - `cargo clippy -p liquidfun --all-targets --all-features -- -D warnings`
  </verify>
  <acceptance_criteria>
    - Visitor payloads are borrow-scoped and controls are closed enums with checked clipping.
    - Diagnostic traversal proves pinned LIFO behavior while public collection docs explicitly deny order guarantees.
    - Invalid visitor fractions return typed errors and never corrupt the tree.
    - Query/ray tests and strict Clippy exit 0.
  </acceptance_criteria>
  <done>Tree queries and ray casts are safe, bounded, controllable, and correctly classified as unordered consumer observations.</done>
</task>

<task type="auto">
  <name>Task 3: Implement move/touch pair generation and pure filtering/refiltering</name>
  <files>crates/liquidfun/src/collision/broad_phase.rs, crates/liquidfun/tests/collision_broad_phase.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/tree.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2BroadPhase.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2BroadPhase.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Fixture.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2ContactManager.cpp
    - third_party/liquidfun/liquidfun/Box2D/Testbed/Tests/CollisionFiltering.h
    - .planning/phases/05-shapes-and-collision-foundation/05-CONTEXT.md
  </read_first>
  <action>
Implement broad-phase proxy creation/movement/destruction/touch with duplicate move entries, tombstoning every destroyed occurrence, querying each live occurrence, and sorting candidates by private `(min_node_slot,max_node_slot)` before adjacent deduplication. Emit pair callbacks in this exact order while exposing only proxy/payload identities. Implement `FilterData` defaults (`category=0x0001`, `mask=0xffff`, `group=0`) and the pure rule: equal nonzero group overrides masks, positive collides, negative does not; otherwise both symmetric category/mask intersections must be nonzero. Model refilter as dirty filter replacement plus proxy touch/reconsideration; prove newly eligible candidates reappear and duplicates collapse. Do not create/persist/destroy contacts, wake bodies, suppress joint pairs, or call listeners. Add tests for duplicate moves/touches, destruction tombstones, pair private-order/dedup, move containment, every filter branch, refilter reconsideration, metrics forwarding, and explicit Phase 6 boundary.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_broad_phase pairs --all-features`
    - `cargo test -p liquidfun --test collision_broad_phase filter --all-features`
    - `! rg "ContactManager|wake|listener|joint" crates/liquidfun/src/collision/broad_phase.rs`
  </verify>
  <acceptance_criteria>
    - Duplicate/tombstone/sort/dedup behavior matches pinned broad-phase source and pair order is exact.
    - Filter defaults/group override/symmetric masks and refilter touch are fully tested.
    - No world contact lifecycle, wake, joint suppression, or callback behavior is implemented or claimed.
    - Pair/filter tests exit 0.
  </acceptance_criteria>
  <done>Broad-phase candidate ordering and pure filtering/refiltering are complete and ready for Phase 6 contact management.</done>
</task>

</tasks>

<verification>
- Run focused tree/broad-phase/property tests, then `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Run warning-denied rustdoc and package isolation.
- Review private/public ordering, stale identity, traversal controls, and the Phase 6 boundary; do not mark contact creation/persistence portions of `COLL-05` evidenced.
</verification>

<success_criteria>
- `COLL-04` proxy lifecycle, movement, queries, rays, metrics, and tie behavior is implemented and tested.
- Phase 5's pair/filter/refilter substrate of `COLL-05` is complete, exact, and truthfully scoped; world contact lifecycle remains explicitly pending Phase 6.
- `COLL-07` has focused/property evidence for identity, topology, ordering, deduplication, and controls.
</success_criteria>

<must_haves>
- Private node order is never a public identity/order promise.
- Pair sequences are ordered; ordinary query/ray result collections are set-like.
- Refiltering only reconsiders candidates and does not overreach into world contact lifecycle.
</must_haves>
