---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-07-11T14-53-25
generated_at: 2026-07-11T15:12:45.000Z
phase: 05-shapes-and-collision-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - crates/liquidfun/src/collision/distance.rs
  - crates/liquidfun/src/collision/distance/proxy.rs
  - crates/liquidfun/src/collision/distance/simplex.rs
  - crates/liquidfun/tests/collision_distance.rs
autonomous: true
requirements:
  - COLL-03
  - COLL-07
must_haves:
  truths:
    - One source-ordered GJK path computes witnesses and distance for validated shape children with radii selected explicitly.
    - Reusable cache state is initialized, topology-safe, semantically observable, and preserves pinned flush/tie/iteration behavior.
    - Overlap uses the exact strict pinned threshold and tests both sides.
  artifacts:
    - crates/liquidfun/src/collision/distance.rs
    - crates/liquidfun/src/collision/distance/proxy.rs
    - crates/liquidfun/src/collision/distance/simplex.rs
    - crates/liquidfun/tests/collision_distance.rs
  key_links:
    - Shape child topology from Plan 05-02 maps into one bounded distance proxy without borrowed raw C++ storage.
    - Exact cache/support/branch diagnostics feed Plan 05-07 collision-probe evidence while public cache representation stays initialized and private.
---

# Plan 05-03: GJK Distance, Cache, and Overlap

<objective>
Implement the pinned shape-child proxy, simplex solver, reusable checked cache, GJK witness/distance output, radii behavior, strict overlap predicate, and branch-focused tests.
</objective>

<decision_coverage>
Implements locked decisions D-07 and D-08: one source-ordered GJK/overlap path and initialized topology-safe reusable cache with semantic observability.
</decision_coverage>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| Shape plus child index to proxy | Caller-selected topology | Validate child index and finite invariant-bearing shape before any support lookup. |
| Reused distance cache | Cache produced for another topology/child | Validate private topology identity and index bounds; explicitly reject or reset before indexing. |
| Iterative GJK | Degenerate/tie geometry | Fixed 20-iteration cap, duplicate-support termination, finite diagnostics, no allocation growth. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T03-1 | Tampering | `DistanceCache` | Mitigate | Private fields, `empty()` constructor, returned updates, and topology validation prevent forged support arrays. |
| T03-2 | Denial of service | GJK loop | Mitigate | Preserve 20 support-call cap and duplicate/near-zero termination with cap-boundary tests. |
| T03-3 | Repudiation | Cache fallback | Mitigate | Return typed reuse disposition and private branch diagnostics so reset/reject behavior is explicit evidence. |
| T03-4 | Information disclosure | Support indices | Mitigate | Public cache snapshot exposes semantic ordered pairs only, not pointers/layout/private topology keys. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build validated shape-child proxies and initialized cache</name>
  <files>crates/liquidfun/src/collision/distance.rs, crates/liquidfun/src/collision/distance/proxy.rs, crates/liquidfun/tests/collision_distance.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/shape.rs
    - crates/liquidfun/src/collision/shape/chain.rs
    - crates/liquidfun/src/collision/types.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2Distance.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2Distance.cpp
    - .planning/phases/05-shapes-and-collision-foundation/05-CONTEXT.md
  </read_first>
  <action>
Implement a private validated distance proxy over circle, polygon, edge, and selected chain child with owned/safely borrowed invariant data, exact radius, bounded vertex count, and support selection using strict `>` so ties keep the first vertex. Define public initialized `DistanceCache::empty()` and read-only semantic `DistanceCacheSnapshot` with count, ordered support-index pairs, and metric. Bind reusable cache privately to compatible proxy topology/child identities; before accessing indices, either return a typed incompatible-cache error or explicitly reset according to the documented safe policy. Reproduce the pinned cache metric flush window `metric2 < 0.5 * metric1 || 2.0 * metric1 < metric2 || metric2 < EPSILON`. Add tests for every proxy kind/child, support ties, cold cache, compatible reuse, ratio boundaries, epsilon flush, and incompatible cross-topology behavior. Expose the public cache and distance results through `liquidfun::collision::distance` during this wave; integration tests use that executable child-module path until Plan 05-07 adds curated root re-exports.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_distance proxy --all-features`
    - `cargo test -p liquidfun --test collision_distance cache --all-features`
    - `! rg "get_unchecked|unsafe|pub .*\[.*3\]|from_raw" crates/liquidfun/src/collision/distance.rs crates/liquidfun/src/collision/distance`
  </verify>
  <acceptance_criteria>
    - Support ties retain the first vertex and every chain proxy uses a checked child.
    - `liquidfun::collision::distance` publicly exposes the initialized cache and distance result surface used by this plan's integration tests.
    - Cache cannot be forged with unchecked arrays and incompatible reuse cannot index another topology.
    - Count/index-pair order/metric are semantically observable without raw layout.
    - Proxy/cache tests exit 0.
  </acceptance_criteria>
  <done>Validated proxies and cache semantics are safe, source-faithful, and probeable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement source-ordered simplex solve, GJK output, and overlap</name>
  <files>crates/liquidfun/src/collision/distance.rs, crates/liquidfun/src/collision/distance/simplex.rs, crates/liquidfun/tests/collision_distance.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/distance/proxy.rs
    - crates/liquidfun/src/math/scalar.rs
    - crates/liquidfun/src/math/settings.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2Distance.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2Collision.cpp
    - standards/core/code-shape.md
    - standards/core/testing.md
  </read_first>
  <action>
Translate `Solve2`, `Solve3`, search direction, closest/witness points, metric, saved-support duplicate detection, near-zero direction termination, and the 20-iteration GJK loop with pinned operand/expression/branch order. Expose checked `distance(shape_a, child_a, transform_a, shape_b, child_b, transform_b, use_radii, maybe_cache)` returning witnesses, distance, exact iteration count, updated cache, and a bounded private diagnostic trace. Apply radii exactly: separated witnesses move to surfaces, overlap collapses both witnesses to midpoint and distance to zero. Implement `test_overlap` by radii-enabled distance and the strict `distance < 10.0 * EPSILON` predicate. Add focused tests/proptests for simplex sizes 1/2/3, every termination path, duplicate support, support ties, radii modes, symmetry where pinned semantics permit, witness separation consistency, warm-cache repeatability, cap behavior, and exact values immediately below/equal/above the overlap threshold.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_distance gjk --all-features`
    - `cargo test -p liquidfun --test collision_distance overlap --all-features`
    - `cargo clippy -p liquidfun --all-targets --all-features -- -D warnings`
  </verify>
  <acceptance_criteria>
    - GJK uses one source-ordered path with a fixed 20-iteration bound and exact discrete diagnostics.
    - Updated cache, iteration count, witnesses, and distance are returned for both radii modes.
    - Overlap is false at equality and true strictly below `10.0 * EPSILON`.
    - GJK/overlap/property tests and strict Clippy exit 0.
  </acceptance_criteria>
  <done>Distance, cache reuse, witnesses, radii, and overlap match the pinned kernel contract.</done>
</task>

</tasks>

<verification>
- Run focused distance tests, then `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Run `RUSTDOCFLAGS="-D warnings" cargo doc -p liquidfun --all-features --no-deps` and package isolation.
- Review every strict/equality branch, cache threshold, loop cap, and support ordering against pinned `b2Distance.cpp`; do not simplify algebra or add `mul_add`.
</verification>

<success_criteria>
- `COLL-03` distance/overlap behavior is complete for validated shape children.
- Cache and diagnostic observability support exact Phase 5 differential evidence without public raw simplex state.
- Focused and property tests establish all simplex/tie/cache/threshold branches before TOI and manifolds consume them.
</success_criteria>

<must_haves>
- One bounded source-faithful GJK implementation, not pair-specific substitutes.
- Cache state is initialized and topology-safe.
- Discrete branch/order evidence remains exact; numeric tolerance is not embedded in production API.
</must_haves>
