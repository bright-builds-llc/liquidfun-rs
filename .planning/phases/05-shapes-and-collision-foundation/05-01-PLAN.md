---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-07-11T14-53-25
generated_at: 2026-07-11T15:12:45.000Z
phase: 05-shapes-and-collision-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/collision.rs
  - crates/liquidfun/src/collision/error.rs
  - crates/liquidfun/src/collision/types.rs
  - crates/liquidfun/src/collision/shape.rs
  - crates/liquidfun/src/collision/distance.rs
  - crates/liquidfun/src/collision/narrow.rs
  - crates/liquidfun/src/collision/tree.rs
  - crates/liquidfun/src/collision/broad_phase.rs
  - crates/liquidfun/src/collision/toi.rs
  - crates/liquidfun/tests/collision_contract.rs
autonomous: true
requirements:
  - COLL-02
  - COLL-03
  - COLL-04
  - COLL-06
must_haves:
  truths:
    - Consumers have one cohesive safe collision namespace with initialized AABB, ray, mass, child-index, and semantic feature/manifold vocabulary.
    - Invalid finite-domain values and invalid child coordinates fail through typed errors before reaching kernels.
    - Future collision children have executable public module paths during their implementation waves and can be integrated into curated root re-exports after all kernels exist.
  artifacts:
    - crates/liquidfun/src/collision.rs
    - crates/liquidfun/src/collision/types.rs
    - crates/liquidfun/src/collision/error.rs
    - crates/liquidfun/tests/collision_contract.rs
  key_links:
    - liquidfun::collision consumes the existing source-ordered math/settings layer and remains independent of world ownership and private harness crates.
    - Semantic feature identity and initialized manifold values replace the packed C++ union and uninitialized inactive fields.
---

# Plan 05-01: Collision Domain and Public Seam

<objective>
Establish the deep `liquidfun::collision` module, invariant-bearing shared geometry/result types, semantic manifold identity, typed errors, and stable child module seams required by all later Phase 5 kernels.
</objective>

<decision_coverage>
Foundation for D-01 through D-22: establishes the shared deep-module, invariant-value, semantic-identity, and disjoint-file seams consumed by the explicitly mapped later plans.
</decision_coverage>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| Public collision constructors | Caller floats, bounds, fractions, and child indices | Reject non-finite values, reversed bounds, invalid fractions, and out-of-range child indices before core arithmetic. |
| Public manifold/feature observations | Kernel-produced active fields | Expose only initialized semantic values; omit packed keys, inactive storage, and impulses. |
| Production-to-tooling dependency | Private protocol/C++ types | Keep production types serde-free, pointer-free, and independent of private crates. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T01-1 | Tampering | Geometry domain values | Mitigate | Tasks use private fields and fallible constructors so callers cannot mutate bounds/topology into invalid states. |
| T01-2 | Elevation of privilege | Public collision API | Mitigate | Preserve crate-level `forbid(unsafe_code)` and expose no raw layout, pointer, unchecked index, or trait-object escape hatch. |
| T01-3 | Information disclosure | Feature/proxy identity | Mitigate | Public values contain semantic feature and checked child identity only, never C++ keys or private slots. |
| T01-4 | Denial of service | Shared result containers | Mitigate | Manifold capacity is fixed by `MAX_MANIFOLD_POINTS`; no shared type accepts unbounded allocation. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the cohesive collision module and typed errors</name>
  <files>crates/liquidfun/src/lib.rs, crates/liquidfun/src/collision.rs, crates/liquidfun/src/collision/error.rs, crates/liquidfun/src/collision/shape.rs, crates/liquidfun/src/collision/distance.rs, crates/liquidfun/src/collision/narrow.rs, crates/liquidfun/src/collision/tree.rs, crates/liquidfun/src/collision/broad_phase.rs, crates/liquidfun/src/collision/toi.rs</files>
  <read_first>
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
    - standards/core/architecture.md
    - standards/core/code-shape.md
    - standards/languages/rust.md
    - .planning/phases/05-shapes-and-collision-foundation/05-CONTEXT.md
    - .planning/phases/05-shapes-and-collision-foundation/05-RESEARCH.md
    - crates/liquidfun/src/lib.rs
    - crates/liquidfun/src/error.rs
    - crates/liquidfun/src/math.rs
  </read_first>
  <action>
Add public `collision` to `lib.rs` and create `collision.rs` with private `error`/`types` children plus public documented `shape`, `distance`, `narrow`, `tree`, `broad_phase`, and `toi` child modules using the required `foo.rs` plus `foo/` layout. Root-re-export the completed shared types/errors now; keep each later kernel reachable through its public child module so that wave's public integration tests compile without another plan editing `collision.rs`. Create compilable documented child entrypoints now so later wave-2/3/4 plans can own disjoint files. Reserve the final curated root re-export list for Plan 05-07 after every concrete public type exists. Do not expose private simplex, clip vertex, node-slot, packed-key, or TOI diagnostic state through either path. Define hand-written `#[non_exhaustive]` errors for invalid geometry, invalid bounds/fractions, invalid child selection, incompatible cache/proxy use, and unsupported shape pairs. Keep errors free of private slot/index details and avoid `unwrap`, `unsafe`, public trait objects, serde, or new dependencies. Update crate-level maturity wording only enough to advertise a collision foundation in progress without claiming Phase 5 completion.
  </action>
  <verify>
    - `cargo check -p liquidfun --all-targets --all-features`
    - `RUSTDOCFLAGS="-D warnings" cargo doc -p liquidfun --all-features --no-deps`
    - `! rg "unsafe|serde|dyn Shape|repr\(C\)|from_raw" crates/liquidfun/src/collision.rs crates/liquidfun/src/collision`
  </verify>
  <acceptance_criteria>
    - `lib.rs` exposes exactly one production `collision` module and no new crate/dependency/feature.
    - `collision.rs` owns documented public child seams using no `mod.rs` files, allowing each later public integration test to import its wave's implemented types.
    - Shared types/errors are root-re-exported now and the file explicitly reserves curated concrete kernel re-exports for serialized Plan 05-07 integration.
    - Typed non-exhaustive errors cover every invalid boundary category without leaking private storage coordinates.
    - Cargo check and warning-denied rustdoc exit 0.
  </acceptance_criteria>
  <done>The collision namespace and error boundary compile as a safe cohesive deep module ready for disjoint kernel implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement initialized shared geometry and semantic manifold values</name>
  <files>crates/liquidfun/src/collision/types.rs, crates/liquidfun/tests/collision_contract.rs</files>
  <read_first>
    - crates/liquidfun/src/collision.rs
    - crates/liquidfun/src/collision/error.rs
    - crates/liquidfun/src/math/settings.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2Collision.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2Collision.cpp
    - .planning/phases/04-math-settings-and-numerical-policy/04-CONTEXT.md
    - standards/core/testing.md
  </read_first>
  <action>
Implement private-representation public `Aabb`, `MassData`, `RayCastInput`, `RayCastHit`, checked `ChildIndex`, `FeatureKind::{Vertex, Face}`, `ContactFeatureId`, `ManifoldKind`, `ManifoldPoint`, initialized `Manifold`, `PointState`, and explicit collision outcomes that distinguish unsupported, separated, and touching. Use `Vec2`/`Transform` and exact settings from `math`; reject non-finite and reversed AABBs, non-finite ray endpoints, and max fractions outside the reviewed interval. Keep maximum manifold storage fixed at two active points and expose slices/accessors rather than raw arrays. Feature identity is four semantic fields; never expose or reconstruct the C++ union `key`. Empty manifolds expose no normal/point/impulse payload, and Phase 5 manifold points carry no solver impulses. Add public integration tests with clear Arrange/Act/Assert sections for construction, invalid boundaries, child-index checking, semantic feature equality, active-point ordering, and compile-time absence of raw constructors/layout promises.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_contract --all-features`
    - `cargo clippy -p liquidfun --all-targets --all-features -- -D warnings`
    - `! rg "normal_impulse|tangent_impulse|contact.*key|uint32|u32.*key" crates/liquidfun/src/collision/types.rs`
  </verify>
  <acceptance_criteria>
    - All shared public geometry values are fully initialized and reject invalid finite-domain inputs through typed errors.
    - Feature identity is semantic and ordered; packed union/key representation is absent.
    - Empty and active manifolds cannot expose uninitialized/inactive data, and no impulse field exists in the Phase 5 value.
    - `collision_contract` and strict package Clippy exit 0.
  </acceptance_criteria>
  <done>The shared collision vocabulary makes invalid states and C++ layout leakage unrepresentable.</done>
</task>

</tasks>

<verification>
- Run focused checks, then before the plan commit run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Run `cargo package -p liquidfun --allow-dirty` and inspect the package list for C++/protocol/tooling leakage.
- Run `git diff --check` and review for `unsafe`, raw storage/layout promises, inactive manifold fields, public impulses, global tolerance APIs, and unrelated churn.
</verification>

<success_criteria>
- Later shape, distance, manifold, tree, and TOI plans share one invariant-bearing vocabulary without editing shared entrypoints in parallel.
- The public collision foundation is recognizable but safer than C++ layout/pointer interfaces.
- Cargo-only consumers remain independent of C++ and private evidence crates.
</success_criteria>

<must_haves>
- Shared values are initialized and parse invalid input at the boundary.
- Semantic feature identity replaces packed/raw identity.
- The architecture follows the Bright Builds functional-core, invariant-type, module-shape, and focused-test standards with no active override.
</must_haves>
