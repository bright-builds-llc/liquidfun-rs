---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-07-11T14-53-25
generated_at: 2026-07-11T15:12:45.000Z
phase: 05-shapes-and-collision-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - crates/liquidfun/src/collision/shape.rs
  - crates/liquidfun/src/collision/shape/circle.rs
  - crates/liquidfun/src/collision/shape/edge.rs
  - crates/liquidfun/src/collision/shape/polygon.rs
  - crates/liquidfun/src/collision/shape/chain.rs
  - crates/liquidfun/tests/collision_shapes.rs
autonomous: true
requirements:
  - COLL-02
  - COLL-07
must_haves:
  truths:
    - Consumers can construct, clone, measure, point-test, bound, and ray-cast all four immutable owned shape types.
    - Accepted polygon and chain topology preserves pinned valid-input ordering while invalid/truncated/fallback behavior becomes an explicit typed safe-Rust difference.
    - Closed-loop vertices are owned once and checked child edges preserve required adjacency.
  artifacts:
    - crates/liquidfun/src/collision/shape.rs
    - crates/liquidfun/src/collision/shape/polygon.rs
    - crates/liquidfun/src/collision/shape/chain.rs
    - crates/liquidfun/tests/collision_shapes.rs
  key_links:
    - Shape unary queries consume Plan 05-01 values and Phase 4 source-ordered math/settings.
    - Shape and child-edge topology becomes the only input boundary for later distance, manifold, and TOI proxies.
---

# Plan 05-02: Owned Shapes and Unary Queries

<objective>
Implement immutable owned circle, edge, polygon, and chain values plus exhaustive `Shape` dispatch, exact accepted-input construction behavior, unary queries, and focused property/regression coverage.
</objective>

<decision_coverage>
Implements locked decisions D-01, D-02, D-03, D-04, D-05, and D-06: owned exhaustive shapes, immutable private topology, checked construction, pinned accepted-input algorithms, explicit chain modes, and typed unary query values.
</decision_coverage>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| Shape constructors | Arbitrary finite/non-finite vertices, radii, topology, ghost vertices | Reject non-finite, negative radius, insufficient/duplicate/degenerate topology, overflow, and invalid ghost/loop combinations before committing derived state. |
| Polygon hull work | Caller-sized point list | Bound at `MAX_POLYGON_VERTICES`; reject excess rather than truncate; bounded gift-wrapping loops only. |
| Chain child selection | Caller child index | Check against open/closed child count before owned edge creation. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T02-1 | Tampering | Derived polygon/chain state | Mitigate | Private immutable fields and validate-then-commit constructors keep normals, centroid, vertices, and adjacency coherent. |
| T02-2 | Denial of service | Hull/chain input | Mitigate | Enforce reviewed vertex bounds before allocation/loops and add proptest bounds. |
| T02-3 | Repudiation | Safe-Rust invalid-input differences | Mitigate | Named errors and regression tests record rejection of upstream truncation/fallback/debug-assert behavior. |
| T02-4 | Elevation of privilege | Shape storage | Mitigate | Deep owned clones, no borrowed raw arrays, mutation setters, or allocator callbacks. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement circle and adjacency-bearing edge values</name>
  <files>crates/liquidfun/src/collision/shape.rs, crates/liquidfun/src/collision/shape/circle.rs, crates/liquidfun/src/collision/shape/edge.rs, crates/liquidfun/tests/collision_shapes.rs</files>
  <read_first>
    - .planning/phases/05-shapes-and-collision-foundation/05-CONTEXT.md
    - .planning/phases/05-shapes-and-collision-foundation/05-RESEARCH.md
    - crates/liquidfun/src/collision/types.rs
    - crates/liquidfun/src/math.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/Shapes/b2Shape.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/Shapes/b2CircleShape.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/Shapes/b2CircleShape.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/Shapes/b2EdgeShape.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/Shapes/b2EdgeShape.cpp
  </read_first>
  <action>
Define public private-representation `CircleShape`, `EdgeShape`, and exhaustive `Shape` variants with deep `Clone`, exact radii, initialized constructors, and static dispatch. Preserve accepted-input mass, centroid, point-test, AABB, distance, and ray-cast operand order. Edge point tests always return false; edge ray normal/fraction and ray-start-inside behavior follow pinned source. Model optional previous/next edge adjacency explicitly and reject equal/non-finite endpoints or invalid adjacency. For the circle-center distance case, audit the pinned result and choose one named safe finite normal result or typed outcome; document and regression-test that deliberate difference instead of allowing arithmetic NaN. Add focused integration/unit tests for exact mass/AABB bits, ordinary point/ray behavior, inside-start no-hit, two-sided edge normals, center-distance behavior, clone independence, and invalid inputs. Public integration tests in this plan import through the executable `liquidfun::collision::shape::{CircleShape, EdgeShape, Shape}` seam established by Plan 05-01; they must not assume the final Plan 05-07 root re-export exists yet.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_shapes circle --all-features`
    - `cargo test -p liquidfun --test collision_shapes edge --all-features`
    - `! rg "unsafe|Box<dyn|&\[Vec2\].*struct|pub .*vertex" crates/liquidfun/src/collision/shape.rs crates/liquidfun/src/collision/shape`
  </verify>
  <acceptance_criteria>
    - Circle and edge values own immutable initialized geometry and deep clone without raw layout or trait objects.
    - `liquidfun::collision::shape::{CircleShape, EdgeShape, Shape}` is public and compiles for external integration tests in wave 2.
    - Edge point tests are always false and ray-start-inside/no-hit semantics are regression-protected.
    - Circle-center distance behavior is explicit, finite or typed, documented, and never masked by tolerance.
    - Named circle/edge test filters exit 0.
  </acceptance_criteria>
  <done>Circle and edge values provide safe exact unary behavior and reusable adjacency state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pinned polygon hull, centroid, normals, and mass</name>
  <files>crates/liquidfun/src/collision/shape/polygon.rs, crates/liquidfun/tests/collision_shapes.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/shape.rs
    - crates/liquidfun/src/collision/shape/circle.rs
    - crates/liquidfun/src/math/settings.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/Shapes/b2PolygonShape.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/Shapes/b2PolygonShape.cpp
    - standards/core/testing.md
  </read_first>
  <action>
Implement `PolygonShape` construction with the pinned valid-input sequence: finite input and count validation; the exact upstream weld comparison using squared distance against `0.5 * LINEAR_SLOP`; rightmost-lowest start tie; gift-wrapping selection; strict cross-product and farthest-collinear branch; final winding, normalized outward normals, centroid, and mass/inertia expression grouping. Reject more than `MAX_POLYGON_VERTICES`, fewer than three retained hull vertices, failed hulls, zero-length normals, and non-finite results with named errors; never truncate or substitute upstream's fallback unit box. Provide explicit box/oriented-box convenience constructors through the same invariant path. Implement point, ray, AABB, mass, centroid/vertex/normal accessors, validation, and deep clone. Add one-concern Arrange/Act/Assert tests and proptests for deterministic hull order, weld/tie boundaries, convex winding, unit normals, centroid containment, AABB containment, mass relationships, box constructors, input permutation invariants where compatible, and every safe-Rust rejection.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_shapes polygon --all-features`
    - `cargo test -p liquidfun shape::polygon --all-features`
    - `! rg "truncate|SetAsBox\(1|fallback|sort_unstable" crates/liquidfun/src/collision/shape/polygon.rs`
  </verify>
  <acceptance_criteria>
    - Accepted polygons preserve pinned weld, start/tie, gift-wrapping, normal, centroid, and mass order.
    - Excess/degenerate/failed input returns typed errors instead of truncation, assertion dependence, or fallback geometry.
    - Property tests prove bounded convex topology and deterministic derived state.
    - Polygon integration and module tests exit 0.
  </acceptance_criteria>
  <done>Polygon construction is source-faithful for accepted geometry and explicitly safer for rejected geometry.</done>
</task>

<task type="auto">
  <name>Task 3: Implement open/closed chains and exhaustive shape dispatch</name>
  <files>crates/liquidfun/src/collision/shape.rs, crates/liquidfun/src/collision/shape/chain.rs, crates/liquidfun/tests/collision_shapes.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/shape.rs
    - crates/liquidfun/src/collision/shape/edge.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/Shapes/b2ChainShape.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/Shapes/b2ChainShape.cpp
    - .planning/phases/03-rust-object-model-and-storage-architecture/03-CONTEXT.md
  </read_first>
  <action>
Implement explicit open and closed `ChainShape` construction. Own canonical semantic vertices exactly once; closed loops derive the closing child rather than publishing the duplicated upstream storage vertex. Permit ghost previous/next vertices only for open chains, validate all vertices/adjacency and consecutive spacing, expose checked child count/index and return an owned adjacency-bearing `EdgeShape` from `child_edge`. Chain point test remains false; child AABB/ray/distance delegates to the exact owned edge. Complete exhaustive static `Shape` dispatch for radius, child count, point test, ray cast, AABB, mass, and clone without public virtual traits. Add tests for open endpoints, ghosts, loop closure, every child adjacency tuple, invalid child access, no duplicated loop vertex, deep clone, false point tests, and enum dispatch equivalence.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_shapes chain --all-features`
    - `cargo test -p liquidfun --test collision_shapes shape_dispatch --all-features`
    - `cargo clippy -p liquidfun --all-targets --all-features -- -D warnings`
  </verify>
  <acceptance_criteria>
    - Open and closed chains are distinct invariant-bearing states and ghost vertices are impossible on loops.
    - Loop public vertices contain no duplicated closing element while the final child closes correctly.
    - Every child lookup is checked and produces owned adjacency matching the pinned source.
    - Chain, dispatch, and strict Clippy commands exit 0.
  </acceptance_criteria>
  <done>All four immutable shape types and their complete unary query surface are implemented and tested.</done>
</task>

</tasks>

<verification>
- Run all shape/unit/property tests, then `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Run `RUSTDOCFLAGS="-D warnings" cargo doc -p liquidfun --all-features --no-deps` and `cargo package -p liquidfun --allow-dirty`.
- Review accepted-input operation order against pinned sources and verify every invalid-input departure has a named error, test, and rustdoc note.
</verification>

<success_criteria>
- `COLL-02` shape construction, cloning, mass, point, AABB, and ray behavior is fully represented for all four shapes.
- Unit/property tests protect hull, adjacency, and safe-Rust difference boundaries.
- Later distance/manifold/TOI code receives only validated immutable shape-child state.
</success_criteria>

<must_haves>
- No mutable public topology or borrowed allocator-owned storage.
- Accepted geometry follows pinned expression/tie order; rejected geometry fails explicitly.
- Tests follow focused Arrange/Act/Assert and bounded property strategies required by repository standards.
</must_haves>
