---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-07-11T14-53-25
generated_at: 2026-07-11T15:12:45.000Z
phase: 05-shapes-and-collision-foundation
plan: "08"
type: execute
wave: 6
depends_on:
  - "05-07"
files_modified:
  - crates/liquidfun/src/lib.rs
  - tools/xtask/src/docs.rs
  - tools/xtask/tests/docs_contract.rs
  - reference/compatibility.json
  - COMPATIBILITY.md
  - ARCHITECTURE.md
  - TESTING.md
  - README.md
autonomous: true
requirements:
  - COLL-02
  - COLL-03
  - COLL-04
  - COLL-05
  - COLL-06
  - COLL-07
must_haves:
  truths:
    - Public collision contracts, ordering, invalid-input differences, numerical evidence, and exact commands are discoverable and machine-checked.
    - Compatibility rows promote only evidence actually produced by Phase 5 and preserve independent evidence dimensions/tier limits.
    - The Phase 5 portion of COLL-05 is reported precisely while world contact lifecycle remains pending Phase 6.
    - Package, docs, inventory, provenance, full Rust gates, and fixed collision probes all pass without a maturity overclaim.
  artifacts:
    - ARCHITECTURE.md
    - TESTING.md
    - reference/compatibility.json
    - COMPATIBILITY.md
    - tools/xtask/tests/docs_contract.rs
  key_links:
    - Human compatibility claims derive from the authoritative machine ledger and executable Phase 5 commands.
    - Phase 6 receives documented immutable shape snapshots, semantic manifold identity, ordered pairs, and pure filter/refilter seams without pretending contacts already exist.
---

# Plan 05-08: Collision Documentation and Evidence Sign-Off

<objective>
Document the complete safe collision contract and reproducible evidence workflow, update the compatibility ledger truthfully, and run the full Phase 5 completion gate.
</objective>

<decision_coverage>
Signs off D-01 through D-22 only to executed evidence scope, with special enforcement of D-17's deferred contact lifecycle and D-20 through D-22's protocol, policy, and witness claims.
</decision_coverage>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| Machine evidence to public claims | Test/probe/profile/tier results | Inventory/docs/provenance checks, exact references, independent evidence dimensions, no configured-only promotion. |
| Generated compatibility report | Authoritative JSON ledger | Update ledger first, regenerate deterministically, reject hand-edited drift. |
| Contributor commands | Copied docs | Exact registered allowlisted commands and prerequisites; no hidden paths/flags. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T08-1 | Spoofing | Compatibility status | Mitigate | Promote only rows with executable evidence references and correct D-tier. |
| T08-2 | Repudiation | Safe-Rust differences/order policy | Mitigate | Docs name invalid-input departures, ordered/set semantics, thresholds/caps, and source refs. |
| T08-3 | Tampering | Generated report | Mitigate | Modify `reference/compatibility.json` first and require byte-identical regeneration/check. |
| T08-4 | Information disclosure | Docs/evidence | Mitigate | Reject local absolute paths, raw identities, secrets, pointers, and private slots. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Document public contracts, private diagnostics, ordering, and reproducible evidence</name>
  <files>crates/liquidfun/src/lib.rs, tools/xtask/src/docs.rs, tools/xtask/tests/docs_contract.rs, ARCHITECTURE.md, TESTING.md, README.md</files>
  <read_first>
    - .planning/phases/05-shapes-and-collision-foundation/05-CONTEXT.md
    - .planning/phases/05-shapes-and-collision-foundation/05-RESEARCH.md
    - .planning/phases/04-math-settings-and-numerical-policy/04-CONTEXT.md
    - crates/liquidfun/src/collision.rs
    - tools/xtask/src/docs.rs
    - ARCHITECTURE.md
    - TESTING.md
    - README.md
    - standards/core/architecture.md
    - standards/core/testing.md
    - standards/core/verification.md
  </read_first>
  <action>
Update crate/root architecture/testing/readme documentation with the exact Phase 5 public types and invariants: immutable owned shapes, checked child edges, safe invalid-input differences, AABB/ray/mass conventions, GJK/cache/overlap semantics, semantic manifold identity, supported pair/reversal table, tree/proxy identity, ordered pair versus set-like query/ray policy, filter/refilter seam, checked TOI states, and private diagnostic exclusions. Document `differential-internals` explicitly as a non-default, `#[doc(hidden)]`, development-only bridge enabled solely by the unpublished workspace differential crate; state that it carries only owned bounded typed diagnostics, is not a stable consumer API, and exposes no raw storage, mutation, packed keys, or constructors. Document exact-bit transport separately from field comparison; exact structural/order/branch fields; named numeric policies; operation/phase-local horizons; arithmetic NaN/signed-zero rules; D0-D3 authority; and exact debug/release/replay plus `cargo xtask differential verify-determinism --scenario collision-probes --preset oracle-debug --runs 2` commands/prerequisites/evidence limits. Explicitly state that Phase 5 has no bodies/fixtures/contact manager, impulses, contact persistence/destruction, waking, joint suppression, or listeners. Extend read-only docs contract tests to require these headings/tables/commands and reject treating the diagnostic feature as default/stable, a competing `d0` subcommand, global epsilon, query-order promises, packed keys, public iteration APIs, local paths, or Phase 6/full-parity claims. Acknowledge that local guidance plus Bright Builds architecture/code-shape/testing/verification/Rust standards drove the deep-module, invariant-type, focused-test, and verification structure; no substantive active override applies.
  </action>
  <verify>
    - `cargo test -p xtask --test docs_contract phase5 --all-features`
    - `cargo xtask docs check`
    - `cargo test -p liquidfun --doc --no-default-features`
    - `cargo test -p liquidfun --test differential_feature --features differential-internals`
    - `! cargo tree -p liquidfun -e features | rg 'differential-internals'`
    - `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps`
    - `! rg -i "full parity|production ready|all platforms validated|query order is guaranteed|global epsilon" README.md ARCHITECTURE.md TESTING.md`
  </verify>
  <acceptance_criteria>
    - Public contracts, safe differences, ordering classes, thresholds/caps, D0-D3 rules, and exact commands are discoverable and docs-tested.
    - Phase 6 exclusions and COLL-05 partial boundary are stated unambiguously.
    - Documentation identifies `differential-internals` as non-default, doc-hidden, private-development-only, owned/typed, and unstable; default docs do not expose its module.
    - No local path, raw storage identity, packed key, or unsupported maturity claim appears.
    - Docs tests/check and warning-denied rustdoc exit 0.
  </acceptance_criteria>
  <done>Phase 5 architecture, API, numerical, ordering, and evidence contracts are precise and executable.</done>
</task>

<task type="auto">
  <name>Task 2: Promote only proven compatibility dimensions and execute the completion gate</name>
  <files>reference/compatibility.json, COMPATIBILITY.md, tools/xtask/tests/docs_contract.rs, TESTING.md</files>
  <read_first>
    - reference/compatibility.json
    - COMPATIBILITY.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/05-shapes-and-collision-foundation/05-CONTEXT.md
    - .planning/phases/05-shapes-and-collision-foundation/05-RESEARCH.md
    - .planning/phases/05-shapes-and-collision-foundation/05-07-PLAN.md
  </read_first>
  <action>
Update the authoritative ledger first for only the shape/collision public APIs, source areas, and subsystems directly evidenced by Plans 05-01 through 05-07; preserve investigated/planned/implemented/unit/differential/platform/documented-difference dimensions independently and cite exact code/tests/probe commands/profile. Record safe invalid-input differences explicitly. Mark shape, narrow phase, tree/broad-phase substrate, TOI, and focused evidence only to actual passing scope/tier. Treat `differential-internals` as private evidence plumbing, never as a public API compatibility row or consumer feature claim. For `COLL-05`, evidence pair generation and pure filter/refilter reconsideration but leave world contact creation/persistence/destruction/waking/joint/listener rows pending Phase 6; do not mark the aggregate requirement fully satisfied if the ledger cannot express partial scope. Regenerate `COMPATIBILITY.md` through xtask and extend docs tests to enforce ledger/report agreement and expected pending rows. Run all Phase 5 focused suites, fixed oracle commands, default/no-feature docs and package isolation, feature-on private diagnostic tests, package/provenance/docs/inventory checks, mandatory Rust gates, and diff/overclaim scans; record exact results in TESTING without promoting configured-but-unexecuted D1/platform evidence.
  </action>
  <verify>
    - `cargo xtask inventory generate`
    - `cargo xtask inventory check`
    - `cargo xtask provenance check`
    - `cargo xtask docs check`
    - `cargo xtask package verify`
    - `cargo check -p liquidfun --no-default-features`
    - `cargo test -p liquidfun --doc --no-default-features`
    - `cargo test -p liquidfun --test differential_feature --features differential-internals`
    - `cargo xtask differential compare --scenario collision-probes --preset oracle-debug --session-profile one-shot`
    - `cargo xtask differential compare --scenario collision-probes --preset oracle-release --session-profile one-shot`
    - `cargo xtask differential replay --scenario collision-probes --preset oracle-debug --session-profile one-shot`
    - `cargo xtask differential verify-determinism --scenario collision-probes --preset oracle-debug --runs 2`
    - `git diff --check`
  </verify>
  <acceptance_criteria>
    - Generated `COMPATIBILITY.md` is byte-identical to the authoritative ledger rendering.
    - Every promoted dimension cites passing evidence and local D2/D3 results never become D1/platform proof.
    - Contact lifecycle rows remain pending and docs tests fail any overstatement of `COLL-05` or Phase 6 capability.
    - Default/package verification proves the diagnostic module is absent without the feature, while the unpublished differential crate's feature-on tests consume only owned typed records.
    - Inventory, provenance, docs, package, debug/release/replay/`verify-determinism`, and diff commands exit 0.
  </acceptance_criteria>
  <done>Compatibility status and requirement disposition are evidence-derived, conservative, and ready for Phase 6.</done>
</task>

</tasks>

<verification>
- Run all commands named in both tasks, then before the plan commit run exactly: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Run `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps`, `cargo xtask package verify`, `cargo xtask inventory check`, `cargo xtask provenance check`, and `cargo xtask docs check` after the final generated report.
- Re-run oracle debug/release collision comparison, replay, and the exact two-run `verify-determinism` command after all documentation/ledger changes; inspect D0 tier and ensure commands are read-only.
- Review the complete diff for unintended generated churn, standalone Markdown body separators, absolute paths, false contact/platform/full-parity claims, and unrelated edits.
</verification>

<success_criteria>
- `COLL-02`, `COLL-03`, `COLL-04`, `COLL-06`, and `COLL-07` have implementation/unit/differential evidence appropriate to actual execution tier.
- Phase 5's `COLL-05` pair/filter/refilter substrate is evidenced while Phase 6 contact lifecycle remains explicitly pending.
- Every Phase 5 roadmap truth has executable repository evidence and no required local verification is failing.
</success_criteria>

<must_haves>
- Human claims derive from machine evidence and preserve independent evidence dimensions.
- Phase boundaries and D-tier promotion authority remain truthful.
- The complete mandatory Rust sequence and repo-native oracle/docs/inventory/package checks are green before commit.
</must_haves>
