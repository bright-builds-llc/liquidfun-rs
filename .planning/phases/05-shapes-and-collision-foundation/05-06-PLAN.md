---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-07-11T14-53-25
generated_at: 2026-07-11T15:12:45.000Z
phase: 05-shapes-and-collision-foundation
plan: "06"
type: execute
wave: 4
depends_on:
  - "05-03"
files_modified:
  - crates/liquidfun/src/collision/toi.rs
  - crates/liquidfun/src/collision/toi/separation.rs
  - crates/liquidfun/tests/collision_toi.rs
autonomous: true
requirements:
  - COLL-06
  - COLL-07
must_haves:
  truths:
    - Checked TOI input validates proxies, sweeps, and t_max and never mutates caller sweeps.
    - The source-ordered kernel preserves target/tolerance formulas, support ties, alternating root method, every cap, and closed output state.
    - Private bounded diagnostics make branch and termination differences exact differential observables without publishing iteration APIs.
  artifacts:
    - crates/liquidfun/src/collision/toi.rs
    - crates/liquidfun/src/collision/toi/separation.rs
    - crates/liquidfun/tests/collision_toi.rs
  key_links:
    - TOI consumes Plan 05-03 validated proxy/GJK behavior and Phase 4 checked `Sweep` values.
    - Bounded private diagnostics feed Plan 05-07 but wall-clock globals never enter compatibility evidence.
---

# Plan 05-06: Time of Impact and Termination Diagnostics

<objective>
Implement checked time-of-impact input/output, source-faithful separation and root finding, bounded diagnostic traces, and edge/cap witness coverage.
</objective>

<decision_coverage>
Implements locked decisions D-18 and D-19: checked immutable TOI inputs/closed outputs and exact pinned formulas, ties, caps, alternating roots, and termination branches.
</decision_coverage>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| Public TOI input | Shape children, sweeps, t_max | Validate children, checked sweeps, finite `t_max` in `0..=1`; copy/normalize internally. |
| Iterative root finding | Nearly tangent/rotating geometry | Preserve fixed outer/root/push-back caps and return closed `Failed` instead of looping. |
| Diagnostics | Potentially large iteration history | Store only bounded enums/indices/counts up to pinned caps; no wall clock or raw pointers. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T06-1 | Tampering | Caller sweeps | Mitigate | Copy and normalize validated sweeps; tests prove originals unchanged. |
| T06-2 | Denial of service | TOI loops | Mitigate | Outer 20, root 50, and push-back `MAX_POLYGON_VERTICES` caps with reachable cap tests. |
| T06-3 | Repudiation | Failed/terminated state | Mitigate | Closed state plus exact bounded termination cause and branch trace. |
| T06-4 | Information disclosure | Global profiling data | Mitigate | Exclude upstream wall-clock/call globals and private support storage from public output. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement checked TOI contract and separation functions</name>
  <files>crates/liquidfun/src/collision/toi.rs, crates/liquidfun/src/collision/toi/separation.rs, crates/liquidfun/tests/collision_toi.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/distance.rs
    - crates/liquidfun/src/math/sweep.rs
    - crates/liquidfun/src/math/settings.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2TimeOfImpact.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2TimeOfImpact.cpp
    - .planning/phases/04-math-settings-and-numerical-policy/04-CONTEXT.md
  </read_first>
  <action>
Define checked public `TimeOfImpactInput` over two validated shape children, two checked `Sweep` copies, and finite `t_max` in `0.0..=1.0`; define public `TimeOfImpactOutput { state: Overlapped|Touching|Separated|Failed, time }`. Keep separation kind, support indices, root method, branch path, iteration counts, and termination cause in a bounded private diagnostic record. Implement points/face-A/face-B separation initialization, minimum separation, and evaluation with strict first-support ties and source expression order. Normalize copied sweeps internally and prove caller values remain unchanged. Add tests for invalid child/t_max, large-angle copied normalization, every separation kind, support ties, and no public diagnostic/raw index API. Expose only the checked input/output/state/function through `liquidfun::collision::toi`; wave-4 integration tests use that child-module path before Plan 05-07's curated root integration.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_toi input --all-features`
    - `cargo test -p liquidfun --test collision_toi separation --all-features`
    - `! rg "pub .*iterations|pub .*support|Timer|clock|unsafe" crates/liquidfun/src/collision/toi.rs crates/liquidfun/src/collision/toi`
  </verify>
  <acceptance_criteria>
    - Invalid child/sweep/t_max input fails before kernel execution and caller sweeps are immutable.
    - `liquidfun::collision::toi` is an executable public import path while separation/iteration diagnostics remain private.
    - All separation kinds and strict support ties match pinned source.
    - Public output contains only closed state/time; diagnostics remain bounded/private.
    - Input/separation tests exit 0.
  </acceptance_criteria>
  <done>TOI domain and separation state are checked, source-ordered, and safely observable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement capped alternating root finding and witness matrix</name>
  <files>crates/liquidfun/src/collision/toi.rs, crates/liquidfun/src/collision/toi/separation.rs, crates/liquidfun/tests/collision_toi.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/toi/separation.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2TimeOfImpact.cpp
    - third_party/liquidfun/liquidfun/Box2D/Testbed/Tests/TimeOfImpact.h
    - standards/core/code-shape.md
    - standards/core/testing.md
  </read_first>
  <action>
Translate the complete TOI loop with target `max(LINEAR_SLOP, total_radius - 3.0 * LINEAR_SLOP)`, tolerance `0.25 * LINEAR_SLOP`, outer cap 20, root cap 50, push-back cap `MAX_POLYGON_VERTICES`, bisection on the first/even root iteration and secant on alternating iterations, and exact overlap/touch/separated/failed termination branches. Do not algebraically reassociate or introduce adaptive tolerances. Record exact bounded diagnostics for each root method/termination. Add deterministic witnesses for initial overlap, touching at zero, separated through `t_max`, translation impact, rotation, near tangent, zero translation with rotation, edge/chain children, support ties, large angles from the pinned testbed case, and every reachable cap/failure path. Add property checks that successful time remains in range and evaluating immediately around the reported time respects the fixed tolerance without converting the property into production policy.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_toi states --all-features`
    - `cargo test -p liquidfun --test collision_toi caps --all-features`
    - `cargo test -p liquidfun --test collision_toi testbed_witness --all-features`
    - `cargo clippy -p liquidfun --all-targets --all-features -- -D warnings`
  </verify>
  <acceptance_criteria>
    - Target/tolerance and all three caps have exact-value/boundary tests.
    - Root diagnostics prove bisection-first/secant-alternating order and exact termination causes.
    - All required motion/shape/tie/large-angle witnesses return bounded closed states.
    - TOI state/cap/testbed tests and strict Clippy exit 0.
  </acceptance_criteria>
  <done>TOI matches the pinned bounded algorithm and exposes sufficient private evidence for first-divergence diagnosis.</done>
</task>

</tasks>

<verification>
- Run TOI tests, then `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Run warning-denied rustdoc and package isolation.
- Diff the kernel against pinned source for formulas, strict inequalities, support direction, root alternation, and caps; scan for wall-clock globals and adaptive tolerance.
</verification>

<success_criteria>
- `COLL-06` supports checked shape sweeps and all named edge/cap outcomes within Phase 4 numerical policy.
- Branch/termination diagnostics are exact and bounded for differential evidence.
- TOI remains a pure native Rust kernel with no world state or caller mutation.
</success_criteria>

<must_haves>
- Fixed formulas, caps, support ties, and root alternation are source-faithful.
- Public output is closed and small; diagnostic internals remain private.
- No wall-clock or iteration-scaled tolerance enters compatibility evidence.
</must_haves>
