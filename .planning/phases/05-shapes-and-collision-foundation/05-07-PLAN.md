---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-07-11T14-53-25
generated_at: 2026-07-11T15:12:45.000Z
phase: 05-shapes-and-collision-foundation
plan: "07"
type: execute
wave: 5
depends_on:
  - "05-04"
  - "05-05"
  - "05-06"
files_modified:
  - crates/liquidfun/Cargo.toml
  - crates/liquidfun/src/collision.rs
  - crates/liquidfun/src/collision/differential.rs
  - crates/liquidfun/src/collision/differential/cache_replay.rs
  - crates/liquidfun/src/collision/distance.rs
  - crates/liquidfun/src/collision/distance/proxy.rs
  - crates/liquidfun/src/collision/distance/replay.rs
  - crates/liquidfun/src/collision/distance/simplex.rs
  - crates/liquidfun/src/collision/shape/edge.rs
  - crates/liquidfun/src/collision/shape/polygon.rs
  - crates/liquidfun/tests/collision_distance.rs
  - crates/liquidfun/tests/collision_shapes.rs
  - crates/liquidfun/tests/differential_feature.rs
  - crates/liquidfun-differential/Cargo.toml
  - crates/liquidfun-test-protocol/Cargo.toml
  - Cargo.lock
  - crates/liquidfun-differential/native-math-sources.txt
  - crates/liquidfun-test-protocol/src/scenario.rs
  - crates/liquidfun-test-protocol/src/scenario/collision_probe.rs
  - crates/liquidfun-test-protocol/src/scenario/collision_probe/result.rs
  - crates/liquidfun-test-protocol/src/scenario/collision_probe/tests.rs
  - crates/liquidfun-test-protocol/src/scenario/collision_probe/types.rs
  - crates/liquidfun-test-protocol/src/scenario/collision_probe/validation.rs
  - crates/liquidfun-test-protocol/src/scenario/collision_probe/validation/shape_validation.rs
  - crates/liquidfun-test-protocol/src/scenario/collision_probe/validation/tree_validation.rs
  - crates/liquidfun-test-protocol/src/scenario/collision_probe/witness_registry.rs
  - crates/liquidfun-test-protocol/src/tolerance.rs
  - crates/liquidfun-test-protocol/src/tolerance/collision_policy.rs
  - crates/liquidfun-test-protocol/src/schema.rs
  - crates/liquidfun-test-protocol/src/schema/tests.rs
  - crates/liquidfun-differential/src/lib.rs
  - crates/liquidfun-differential/src/collision_probe.rs
  - crates/liquidfun-differential/src/collision_probe/support.rs
  - crates/liquidfun-differential/src/collision_evidence.rs
  - crates/liquidfun-differential/src/supervisor.rs
  - crates/liquidfun-differential/src/supervisor/collision_probe.rs
  - crates/liquidfun-differential/tests/collision_probe.rs
  - protocol/fixtures/accepted/collision-probe-request.jsonl
  - protocol/schemas/protocol-v1.schema.json
  - protocol/schemas/scenario-v1.schema.json
  - protocol/tolerances/phase5-v1.toml
  - tools/reference/CMakeLists.txt
  - tools/reference/adapter-inputs.txt
  - tools/reference/src/generate_build_identity.cmake.in
  - tools/reference/src/collision_probe.hpp
  - tools/reference/src/collision_probe.cpp
  - tools/reference/src/collision_probe_shapes.hpp
  - tools/reference/src/collision_probe_spatial.hpp
  - tools/reference/src/protocol.hpp
  - tools/reference/src/protocol.cpp
  - tools/reference/src/main.cpp
  - tools/reference/tests/protocol_tests.cpp
  - tools/xtask/src/differential.rs
  - tools/xtask/tests/differential_cli.rs
  - justfile
  - .github/workflows/oracle.yml
autonomous: true
requirements:
  - COLL-02
  - COLL-03
  - COLL-04
  - COLL-05
  - COLL-06
  - COLL-07
must_haves:
  truths:
    - One bounded closed collision-probe family executes identical semantic operations through native Rust and the existing pinned C++ subprocess.
    - Every float remains exact-bit transport while every authoritative field resolves to an explicit reviewed Phase 5 comparison policy.
    - Structural/order/branch mismatches, physics numeric mismatches, and harness/provenance/process failures remain distinct and first-divergence reproducible.
    - The fixed corpus covers every D-22 witness family in debug/release and proves D0 two-run byte identity.
    - Expected invalid circle, edge, polygon, and chain candidates reach both executors as bounded raw values and compare as exact typed rejections without weakening ordinary accepted-case decoding.
    - Cold, topology-matched warm, source-reset, and invalid cache seeds produce explicit semantic cache outcomes without exposing or reconstructing unchecked storage state.
  artifacts:
    - crates/liquidfun-test-protocol/src/scenario/collision_probe.rs
    - protocol/tolerances/phase5-v1.toml
    - crates/liquidfun-differential/src/collision_probe.rs
    - tools/reference/src/collision_probe.cpp
    - protocol/fixtures/accepted/collision-probe-request.jsonl
  key_links:
    - Closed probe operations map one-to-one onto Plans 05-02 through 05-06 and pinned C++ functions without a second executable or FFI path.
    - Existing supervisor, provenance, evidence-tier, exact-bit, and first-divergence machinery remains authoritative for Phase 5.
---

# Plan 05-07: Closed Collision Differential Evidence

<objective>
Extend the existing protocol, native runner, C++ adapter, comparator, xtask, and oracle CI with one bounded collision-probe contract and complete fixed Phase 5 witness corpus.
</objective>

<decision_coverage>
Implements locked decisions D-20, D-21, and D-22 and differentially exercises D-01 through D-19 through the existing protocol/oracle path with exact structural evidence and the complete fixed witness matrix.
</decision_coverage>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| JSONL collision requests | Record kind, shapes, children, floats, operations, case counts | Strict closed decoding, exact bits, aggregate/resource bounds, invariant reconstruction, unknown/duplicate rejection. |
| C++ subprocess output | Child-controlled result/order/identity | Validate handshake, case echo, operation, field order/count, reset, terminal record, and build identity before physics comparison. |
| Policy/profile files | Field paths and thresholds | Closed identity, no wildcard/fallback, finite nonnegative thresholds, fixed horizon/tier/collection semantics. |
| Contributor CLI | Scenario/preset/session arguments | Closed allowlists only; no executable/path/compiler/flag injection. |
| Expected-rejection and cache-seed cases | Bounded raw shape fields, expected error category/field, semantic proxy fingerprints, support pairs, metric | Defer only expected shape validity to both executors; keep framing/resource checks at decode; validate fingerprint/topology/count/indices before private cache construction. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T07-1 | Spoofing | Oracle/case identity | Mitigate | Reuse exact upstream/build identity and require one-to-one case/operation/path echoes. |
| T07-2 | Tampering | Numeric policy | Mitigate | Hashed closed `phase5-v1` profile with no wildcard, runtime widening, or iteration-scaled thresholds. |
| T07-3 | Denial of service | Protocol/child | Mitigate | Reviewed case/shape/node/trace/output/time bounds and existing concurrent drain/kill/reap supervisor. |
| T07-4 | Information disclosure | C++ diagnostics | Mitigate | Stdout protocol-only, bounded stderr, no environment/secrets/raw pointers in results. |
| T07-5 | Elevation of privilege | Probe dispatch | Mitigate | Closed operation enums and fixed named CLI commands carry no paths, commands, or code. |
| T07-6 | Tampering | Expected rejection/cache replay | Mitigate | Typed expected outcomes, exact rejection category/field comparison, semantic proxy fingerprints, checked support indices, and fail-closed Used/Reset/Rejected outcomes prevent malformed seeds from becoming trusted private state. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Finalize curated public exports and the gated diagnostic seam</name>
  <files>crates/liquidfun/Cargo.toml, crates/liquidfun/src/collision.rs, crates/liquidfun/src/collision/differential.rs, crates/liquidfun/tests/differential_feature.rs</files>
  <read_first>
    - crates/liquidfun/Cargo.toml
    - crates/liquidfun/src/collision.rs
    - crates/liquidfun/src/collision/shape.rs
    - crates/liquidfun/src/collision/distance.rs
    - crates/liquidfun/src/collision/narrow.rs
    - crates/liquidfun/src/collision/tree.rs
    - crates/liquidfun/src/collision/broad_phase.rs
    - crates/liquidfun/src/collision/toi.rs
    - crates/liquidfun/tests/collision_shapes.rs
    - crates/liquidfun/tests/collision_distance.rs
    - crates/liquidfun/tests/collision_manifolds.rs
    - crates/liquidfun/tests/collision_broad_phase.rs
    - crates/liquidfun/tests/collision_toi.rs
    - crates/liquidfun-differential/Cargo.toml
    - crates/liquidfun-differential/src/math_probe.rs
    - .planning/phases/03-rust-object-model-and-storage-architecture/03-CONTEXT.md
    - .planning/phases/04-math-settings-and-numerical-policy/04-CONTEXT.md
  </read_first>
  <action>
After every production kernel plan has completed, add the explicit curated root re-export list promised by Plan 05-01 for `CircleShape`, `EdgeShape`, `PolygonShape`, `ChainShape`, `Shape`, public distance/cache results and operations, semantic collision/manifold operations, `DynamicTree`, `ProxyId`, broad-phase/filter types, and checked TOI input/output/state/function. Preserve the already executable public child-module paths for compatibility, but do not wildcard-export or expose private proxies, simplex/cache arrays, clip vertices, node coordinates/pool, packed keys, TOI separation/iteration diagnostics, or harness-only trace types. Add/adjust a public integration assertion so each promised `liquidfun::collision::{...}` root import compiles before the protocol adapters are written.

In `crates/liquidfun/Cargo.toml`, add a non-default empty `differential-internals` feature and keep the default feature set unchanged. In `collision.rs`, declare only `#[cfg(feature = "differential-internals")] #[doc(hidden)] pub mod differential;`. Implement `collision/differential.rs` as a doc-hidden adapter over completed kernels that returns owned typed, bounded diagnostic records needed by the private workspace differential executor: support/cache index pairs and termination enums; clip outputs and feature identity; reference/flip/point-state branches; private node-order/pair diagnostics expressed as semantic owned records; and TOI separation/root/termination enums/counts. The adapter may invoke private pure kernels through narrow crate-private helpers but must never expose references into storage, mutable access, raw node/proxy/simplex constructors, packed keys, raw pointers, arbitrary indices without typed context, or stability/public-API promises. Add a compile-fail rustdoc/default-feature assertion proving `liquidfun::collision::differential` cannot be imported without the feature and focused feature-on integration tests proving every exact typed diagnostic family is accessible and owned. Add scans/tests that reject forbidden raw/storage/mutation vocabulary and prove ordinary default package/docs remain feature-free.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_contract public_root_exports --all-features`
    - `cargo test -p liquidfun --test differential_feature --features differential-internals`
    - `cargo test -p liquidfun --doc --no-default-features`
    - `cargo check -p liquidfun --no-default-features`
    - `RUSTDOCFLAGS="-D warnings" cargo doc -p liquidfun --all-features --no-deps`
    - `! rg "pub use .*\*|&mut|from_raw|raw_(slot|proxy|node|simplex)|packed.*key|ContactID|NodeSlot" crates/liquidfun/src/collision.rs crates/liquidfun/src/collision/differential.rs`
  </verify>
  <acceptance_criteria>
    - Every promised public collision type/function has an explicit `liquidfun::collision` root re-export and remains reachable through its child module.
    - Public integration tests compile only after the concrete export exists; no wave depended on a nonexistent root export.
    - Private simplex, clipping, node-coordinate, packed-key, and TOI diagnostic state remains unexported.
    - `differential-internals` is non-default and the diagnostic module is both cfg-gated and `#[doc(hidden)]`.
    - Feature-on tests access all required owned typed diagnostic records; default/no-feature compile-fail rustdoc proves the module cannot be imported.
    - No diagnostic adapter exposes raw storage, borrowed mutable state, packed keys, raw constructors, or stable public-API claims.
    - Root-export, feature-on, default-feature, compile-fail rustdoc, and warning-denied rustdoc commands exit 0.
  </acceptance_criteria>
  <done>The complete collision API has one executable curated root surface and one narrowly gated owned diagnostic bridge available only to private differential tooling.</done>
</task>

<task type="auto">
  <name>Task 2: Define bounded collision requests, semantic results, schemas, and policies</name>
  <files>crates/liquidfun-test-protocol/src/scenario.rs, crates/liquidfun-test-protocol/src/scenario/collision_probe.rs, crates/liquidfun-test-protocol/src/tolerance.rs, crates/liquidfun-test-protocol/src/tolerance/collision_policy.rs, crates/liquidfun-test-protocol/src/schema.rs, crates/liquidfun-test-protocol/src/schema/tests.rs, protocol/fixtures/accepted/collision-probe-request.jsonl, protocol/schemas/protocol-v1.schema.json, protocol/schemas/scenario-v1.schema.json, protocol/tolerances/phase5-v1.toml</files>
  <read_first>
    - .planning/phases/02-semantic-protocol-and-oracle-round-trip/02-CONTEXT.md
    - .planning/phases/04-math-settings-and-numerical-policy/04-CONTEXT.md
    - .planning/phases/05-shapes-and-collision-foundation/05-CONTEXT.md
    - crates/liquidfun-test-protocol/src/scenario/math_probe.rs
    - crates/liquidfun-test-protocol/src/tolerance/policy.rs
    - protocol/tolerances/phase4-v1.toml
    - protocol/fixtures/accepted/math-probe-request.jsonl
  </read_first>
  <action>
Add a separately tagged but existing-version collision-probe request/result family with bounded semantic shape definitions, transforms/sweeps, checked child indices, cache snapshots, tree commands, stable payload IDs, case IDs, exact `FloatBits`, and closed operations for: shape construction/unary query; distance/overlap; clipping; manifold/pair/feature transition; tree lifecycle/query/ray/metrics; broad-phase move/touch/pair/filter/refilter; and TOI. Results must separate ordered discrete fields, ordered numeric fields, and explicitly set-like query/ray results; no raw pointers, C++ keys, tree slots, public simplex structs, impulses, or wall clocks. Enforce case/vertex/node/command/output aggregate bounds and strict operation/input/output field mappings. Create `Phase5PolicyProfile` in a new cohesive child using the existing `FieldPolicy` vocabulary and exact profile hash; require explicit semantic paths for every authoritative result, operation/phase-local horizons, D1/D2 tier, signed-zero/non-finite policy, and ordered/set semantics with no wildcard/fallback. Begin exact-bit for canonical finite probes; any non-exact threshold must cite measured evidence, not headroom. Add schema/fixture tests for byte stability, all operations, unknown/duplicate fields, invalid child/topology, oversized collections, missing policy, wildcard rejection, and collection/horizon mismatch.
  </action>
  <verify>
    - `cargo test -p liquidfun-test-protocol collision_probe --all-features`
    - `cargo test -p liquidfun-test-protocol collision_policy --all-features`
    - `cargo test -p liquidfun-test-protocol schema --all-features`
    - `! rg 'semantic_path = ".*(\*|default|fallback|any|all)' protocol/tolerances/phase5-v1.toml`
  </verify>
  <acceptance_criteria>
    - Every collision operation/result field is closed, bounded, exact-bit transported, and validates before adapter use.
    - Every authoritative semantic path has exactly one explicit policy and no fallback/wildcard exists.
    - Ordered versus set-like collections and operation/phase-local horizons are encoded, not inferred.
    - Protocol policy/schema/fixture tests exit 0.
  </acceptance_criteria>
  <done>The Phase 5 collision evidence contract is bounded, versioned, semantic, and policy-complete.</done>
</task>

<task type="auto">
  <name>Task 3: Implement native execution, typed comparison, evidence, supervised capture, and source identity</name>
  <files>crates/liquidfun-differential/Cargo.toml, crates/liquidfun-differential/native-math-sources.txt, crates/liquidfun-differential/src/lib.rs, crates/liquidfun-differential/src/collision_probe.rs, crates/liquidfun-differential/src/collision_evidence.rs, crates/liquidfun-differential/src/supervisor.rs, crates/liquidfun-differential/src/supervisor/collision_probe.rs, crates/liquidfun-differential/tests/collision_probe.rs</files>
  <read_first>
    - crates/liquidfun-differential/Cargo.toml
    - crates/liquidfun-differential/src/math_probe.rs
    - crates/liquidfun-differential/src/phase4_evidence.rs
    - crates/liquidfun-differential/src/comparator.rs
    - crates/liquidfun-differential/src/canonical.rs
    - crates/liquidfun-differential/src/supervisor/math_probe.rs
    - crates/liquidfun-differential/native-math-sources.txt
    - crates/liquidfun-test-protocol/src/scenario/collision_probe.rs
    - crates/liquidfun/src/collision.rs
    - crates/liquidfun/src/collision/differential.rs
  </read_first>
  <action>
In `crates/liquidfun-differential/Cargo.toml`, enable `features = ["differential-internals"]` only on its private path dependency on `liquidfun`; do not add the feature to workspace defaults, the published crate defaults, or any consumer-facing command. Implement a stateless/bounded native executor that maps every validated operation onto the ordinary public collision surface plus the doc-hidden typed diagnostic adapter, preserving request/result order. The differential crate consumes owned diagnostic records only and must not depend on raw storage or crate-private implementation types. Extend `native-math-sources.txt` (the existing native build-identity manifest) with the collision executor, diagnostic adapter, and every production collision source/child file that can affect probe results; preserve stable repo-relative ordering and include no generated/build outputs. Update native identity tests so changing either `collision_probe.rs`, `collision/differential.rs`, or a production collision source changes the reported source digest. Implement pure first-divergence comparison: reject identity/profile/case/operation/field/count/horizon/tier/collection mismatches as typed harness failures; compare IDs, tags, branches, support/cache indices, feature/pair order, iteration/termination data exactly; use `float_values_match_with_policy` only after class/sign and exact field alignment; canonicalize only explicit query/ray sets by stable semantic payload ID. Add collision-specific numeric/discrete/order/harness reports containing request/scenario/profile/build hashes, exact bits/decimals/differences, tier/horizon, branch context, and stable neighboring case IDs/signature. Extend the existing one-shot process supervisor for collision result records, reusing handshake, concurrent bounded drains, timeout/sanitizer classification, reset epoch, kill/reap, and output reconciliation. Add tests that mutate every structural echo, collection order, exact branch, numeric value, policy/tier/horizon, child/cache identity, terminal/reset record, stderr/timeout/crash path, and verify stable first signature/replay bytes.
  </action>
  <verify>
    - `cargo test -p liquidfun-differential --test collision_probe native --all-features`
    - `cargo test -p liquidfun-differential --test collision_probe comparison --all-features`
    - `cargo test -p liquidfun-differential --test collision_probe supervisor --all-features`
    - `cargo test -p liquidfun-differential collision_source_changes_native_identity --all-features`
    - `cargo tree -p liquidfun-differential -e features | rg 'liquidfun feature "differential-internals"'`
    - `! cargo tree -p liquidfun -e features | rg 'differential-internals'`
    - `cargo clippy -p liquidfun-differential --all-targets --all-features -- -D warnings`
  </verify>
  <acceptance_criteria>
    - Native execution covers every closed operation and returns exact ordered typed results.
    - Comparator canonicalizes only declared sets and fails at the first exact or policy-bound divergence.
    - Every malformed child record/process outcome remains a harness failure, never physics mismatch.
    - Native identity hashes the collision executor and all production collision sources; focused tests prove changing either class of source changes identity.
    - The private differential crate alone enables `differential-internals` and compiles against the exact owned typed diagnostic vocabulary.
    - Default `liquidfun` dependency/docs/package resolution does not enable or expose the diagnostic feature/module.
    - Native/comparison/supervisor tests and strict Clippy exit 0.
  </acceptance_criteria>
  <done>Native collision evidence is executable, first-divergence typed, and safely supervised.</done>
</task>

<task type="auto">
  <name>Task 4: Extend the pinned C++ adapter and register fixed debug/release/determinism workflows</name>
  <files>tools/reference/CMakeLists.txt, tools/reference/src/generate_build_identity.cmake.in, tools/reference/src/collision_probe.hpp, tools/reference/src/collision_probe.cpp, tools/reference/src/protocol.hpp, tools/reference/src/protocol.cpp, tools/reference/src/main.cpp, tools/reference/tests/protocol_tests.cpp, tools/xtask/src/differential.rs, tools/xtask/tests/differential_cli.rs, justfile, .github/workflows/oracle.yml</files>
  <read_first>
    - tools/reference/src/math_probe.hpp
    - tools/reference/src/math_probe.cpp
    - tools/reference/src/protocol.hpp
    - tools/reference/src/protocol.cpp
    - tools/reference/src/main.cpp
    - tools/reference/CMakeLists.txt
    - tools/reference/src/generate_build_identity.cmake.in
    - tools/xtask/src/differential.rs
    - .github/workflows/oracle.yml
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2Collision.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2Distance.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2DynamicTree.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2BroadPhase.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2TimeOfImpact.cpp
  </read_first>
  <action>
Mirror the collision contract in external C++ using small handler functions grouped by subsystem rather than one oversized switch. Strictly decode bounded records, reconstruct only validated upstream shapes, execute pinned APIs, and serialize semantic active fields plus bounded private diagnostics: convert contact IDs to four typed features; omit packed key/inactive/impulse fields; map private node coordinates only into pair-order diagnostics; exclude wall-clock globals. Keep exact bits via existing `protocol_bits`, stdout protocol-only, stderr diagnostic-only, and a single long-lived executable/request-kind loop. Update CMake source lists and `generate_build_identity.cmake.in` so the compile-command filter accepts `math_probe.cpp`, `protocol_bits.cpp`, and `collision_probe.cpp`, requires exactly those three normalized commands, and hashes all three under the same canonical FP flags. Update xtask's compile-database calculation and tests to use the identical three-file filter/count and prove changing `collision_probe.cpp` changes the C++ identity. Extend xtask/just with the named `collision-probes` scenario for debug, release, replay, and the existing `verify-determinism` two-run path; extend its accepted scenario set/tests rather than creating a competing `d0` subcommand. Validate profile/preset/session allowlists before effects. The checked-in corpus must cover D-22 completely: accepted/rejected shape boundaries, cold/warm/invalid cache, simplex sizes/ties/termination, overlap threshold, clipping branches, all pair/reversal/adjacency/feature transitions, tree allocation/ties/lifecycle/query/ray controls, duplicate move/touch/filter/refilter pairs, and all TOI states/motions/ties/caps. Run it in oracle CI debug/release plus determinism verification, without rewriting accepted artifacts or claiming D1 when the actual identity is D2/D3.
  </action>
  <verify>
    - `cargo test -p xtask --test differential_cli collision --all-features`
    - `cargo xtask upstream configure --preset oracle-debug`
    - `cargo xtask upstream build --preset oracle-debug`
    - `cargo xtask differential compare --scenario collision-probes --preset oracle-debug --session-profile one-shot`
    - `cargo xtask differential compare --scenario collision-probes --preset oracle-release --session-profile one-shot`
    - `cargo xtask differential replay --scenario collision-probes --preset oracle-debug --session-profile one-shot`
    - `cargo xtask differential verify-determinism --scenario collision-probes --preset oracle-debug --runs 2`
    - `cargo test -p xtask --test differential_cli collision_compile_database_identity --all-features`
  </verify>
  <acceptance_criteria>
    - C++ executes every bounded operation through pinned sources and emits the identical semantic result vocabulary.
    - Adapter identity requires and hashes exactly the three effective probe compile commands, and a test proves collision C++ source changes alter identity.
    - Fixed corpus explicitly contains every D-22 witness family and debug/release/replay/two-run `verify-determinism` commands exit 0.
    - No second executable, FFI path, arbitrary CLI path/flag, raw key/pointer, artifact rewrite, or false D1 promotion exists.
  </acceptance_criteria>
  <done>The complete Phase 5 collision substrate is differentially executable through one pinned, reproducible, fail-closed oracle path.</done>
</task>

<task type="auto">
  <name>Task 5: Close rejected-shape, cache-replay, and required-family D-22 evidence gaps</name>
  <scope_extension>
Add a private workspace path dependency from `crates/liquidfun-test-protocol/Cargo.toml` to `liquidfun` so accepted cases call the actual production constructors and validators instead of duplicating their rules; update `Cargo.lock` only for the resulting workspace dependency metadata. Add every touched collision adapter input to the authoritative `tools/reference/adapter-inputs.txt` manifest and keep CMake consuming that manifest so the C++ adapter-content identity changes whenever any result-affecting collision adapter source changes.

The code-shape simplification pass may split `collision/differential.rs` into `collision/differential/cache_replay.rs`, distance replay support into `collision/distance/replay.rs`, the native adapter into `collision_probe/support.rs`, and `scenario/collision_probe.rs` into cohesive `scenario/collision_probe/{result,tests,types,validation,witness_registry}.rs` children with shape/tree validation grandchildren. It may also split the C++ adapter into included `collision_probe_shapes.hpp` and `collision_probe_spatial.hpp` helpers while retaining the exact three-translation-unit compile identity and adding those headers to `adapter-inputs.txt`. These files are owned by Task 5 and must preserve existing public paths through thin entrypoint re-exports.
  </scope_extension>
  <files>crates/liquidfun/src/collision/differential.rs, crates/liquidfun/src/collision/distance.rs, crates/liquidfun/src/collision/distance/proxy.rs, crates/liquidfun/src/collision/distance/simplex.rs, crates/liquidfun/src/collision/shape/edge.rs, crates/liquidfun/src/collision/shape/polygon.rs, crates/liquidfun/tests/collision_distance.rs, crates/liquidfun/tests/collision_shapes.rs, crates/liquidfun/tests/differential_feature.rs, crates/liquidfun-differential/Cargo.toml, crates/liquidfun-test-protocol/Cargo.toml, Cargo.lock, crates/liquidfun-differential/native-math-sources.txt, crates/liquidfun-differential/src/lib.rs, crates/liquidfun-differential/src/collision_probe.rs, crates/liquidfun-differential/src/collision_evidence.rs, crates/liquidfun-differential/src/supervisor.rs, crates/liquidfun-differential/src/supervisor/collision_probe.rs, crates/liquidfun-differential/tests/collision_probe.rs, crates/liquidfun-test-protocol/src/scenario.rs, crates/liquidfun-test-protocol/src/scenario/collision_probe.rs, crates/liquidfun-test-protocol/src/tolerance.rs, crates/liquidfun-test-protocol/src/tolerance/collision_policy.rs, crates/liquidfun-test-protocol/src/schema.rs, crates/liquidfun-test-protocol/src/schema/tests.rs, protocol/fixtures/accepted/collision-probe-request.jsonl, protocol/schemas/protocol-v1.schema.json, protocol/schemas/scenario-v1.schema.json, protocol/tolerances/phase5-v1.toml, tools/reference/CMakeLists.txt, tools/reference/adapter-inputs.txt, tools/reference/src/generate_build_identity.cmake.in, tools/reference/src/collision_probe.hpp, tools/reference/src/collision_probe.cpp, tools/reference/src/protocol.hpp, tools/reference/src/protocol.cpp, tools/reference/src/main.cpp, tools/reference/tests/protocol_tests.cpp, tools/xtask/src/differential.rs, tools/xtask/tests/differential_cli.rs, justfile, .github/workflows/oracle.yml</files>
  <read_first>
    - .planning/phases/05-shapes-and-collision-foundation/05-CONTEXT.md
    - .planning/phases/05-shapes-and-collision-foundation/05-RESEARCH.md
    - .planning/phases/05-shapes-and-collision-foundation/05-01-SUMMARY.md
    - .planning/phases/05-shapes-and-collision-foundation/05-02-SUMMARY.md
    - .planning/phases/05-shapes-and-collision-foundation/05-03-SUMMARY.md
    - .planning/phases/05-shapes-and-collision-foundation/05-04-SUMMARY.md
    - .planning/phases/05-shapes-and-collision-foundation/05-05-SUMMARY.md
    - .planning/phases/05-shapes-and-collision-foundation/05-06-SUMMARY.md
    - crates/liquidfun/src/collision/error.rs
    - crates/liquidfun/src/collision/differential.rs
    - crates/liquidfun/src/collision/distance.rs
    - crates/liquidfun/src/collision/distance/proxy.rs
    - crates/liquidfun/src/collision/distance/simplex.rs
    - crates/liquidfun/src/collision/shape/edge.rs
    - crates/liquidfun/src/collision/shape/polygon.rs
    - crates/liquidfun/src/collision/shape/chain.rs
    - crates/liquidfun/tests/collision_distance.rs
    - crates/liquidfun/tests/collision_shapes.rs
    - crates/liquidfun/tests/differential_feature.rs
    - crates/liquidfun-test-protocol/src/scenario.rs
    - crates/liquidfun-test-protocol/Cargo.toml
    - Cargo.lock
    - crates/liquidfun-test-protocol/src/scenario/collision_probe.rs
    - crates/liquidfun-test-protocol/src/tolerance/collision_policy.rs
    - crates/liquidfun-test-protocol/src/schema.rs
    - crates/liquidfun-test-protocol/src/schema/tests.rs
    - protocol/fixtures/accepted/collision-probe-request.jsonl
    - protocol/schemas/protocol-v1.schema.json
    - protocol/schemas/scenario-v1.schema.json
    - protocol/tolerances/phase5-v1.toml
    - crates/liquidfun-differential/Cargo.toml
    - crates/liquidfun-differential/native-math-sources.txt
    - crates/liquidfun-differential/src/lib.rs
    - crates/liquidfun-differential/src/collision_probe.rs
    - crates/liquidfun-differential/src/collision_evidence.rs
    - crates/liquidfun-differential/src/supervisor.rs
    - crates/liquidfun-differential/src/supervisor/collision_probe.rs
    - crates/liquidfun-differential/tests/collision_probe.rs
    - tools/reference/CMakeLists.txt
    - tools/reference/adapter-inputs.txt
    - tools/reference/src/generate_build_identity.cmake.in
    - tools/reference/src/collision_probe.hpp
    - tools/reference/src/collision_probe.cpp
    - tools/reference/src/protocol.hpp
    - tools/reference/src/protocol.cpp
    - tools/reference/src/main.cpp
    - tools/reference/tests/protocol_tests.cpp
    - tools/xtask/src/differential.rs
    - tools/xtask/tests/differential_cli.rs
    - justfile
    - .github/workflows/oracle.yml
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2Collision.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2Distance.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2DynamicTree.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2BroadPhase.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2TimeOfImpact.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/Shapes/b2CircleShape.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/Shapes/b2EdgeShape.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/Shapes/b2PolygonShape.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/Shapes/b2ChainShape.cpp
  </read_first>
  <action>
Close the active checkpoint without changing protocol versions or adding a second harness. Extend each `CollisionProbeCase` with a required typed `ExpectedOutcome`: `Accepted` or `Rejected { category, field }`. Use closed rejection categories `NonFiniteValue`, `InvalidGeometry`, and `InvalidChildIndex`, and closed fields `CircleCenter`, `CircleRadius`, `EdgeStart`, `EdgeEnd`, `EdgePrevious`, `EdgeNext`, `PolygonVertices`, `ChainVertices`, and `ChildIndex`. Refactor `CollisionProbeResult` to carry a closed outcome payload: `Accepted { numeric, discrete, payload_ids }` or `Rejected { category, field }`; keep case/operation/policy/horizon/collection echoes outside the payload. Strict JSON framing, unknown/duplicate-field rejection, ID and aggregate bounds, exact `FloatBits`, operation/input mapping, and collection/policy/horizon validation remain decode-time requirements. Only a `ShapeConstruction` case whose expected outcome is `Rejected` may retain a bounded raw circle/edge/polygon/chain candidate past decoding; every other expected-rejected operation, invalid accepted case, malformed record, invalid child, or operation/input mismatch fails ordinary scenario decoding.

Accepted shape cases must pass the same production constructors/validators used by ordinary `liquidfun` calls, not a reduced protocol-only approximation. Cover every implemented invariant explicitly: circles require finite center/radius and nonnegative radius; edges require finite distinct endpoints, finite optional ghosts, ghosts distinct from the endpoint they extend, and nondegenerate adjacent segments; polygons require finite input, the `3..=8` limit, the pinned weld threshold, a valid source-ordered hull with at least three non-collinear points, nonzero area, convexity, and no invalid hull; open and closed chains require finite vertices, minimum open/loop counts, valid optional endpoint ghosts with the implemented duplication/spacing rules, every adjacent segment above `LINEAR_SLOP`, the closed last-to-first segment above `LINEAR_SLOP`, and checked child indices. `ChainShape` does not currently reject general self-intersection, so Task 5 must not add a chain self-intersection rule or modify the `ChainShape` constructor; D-22 rejected-chain boundaries cover only the implemented finite/count/ghost/adjacent/closing/child rules. If a currently shared edge or polygon production validator does not encode one of its listed locked safe-shape invariants, add it once in the corresponding shape module and make both public construction and differential execution consume it; do not create a harness-only accepted-shape rule. Both Rust and C++ executors validate expected-rejected raw candidates in the same deterministic field order before invoking a production/upstream kernel and emit the exact semantic rejection category and field.

Before comparing Rust to C++, the comparator must validate the Rust result against `case.expected_outcome`, then independently validate the C++ result against `case.expected_outcome`, and only then compare the aligned engine results. A declaration mismatch is `CollisionDivergence::Order` with field `expected_outcome` and side context `rust` or `cpp`; it is never accepted merely because both engines agree. Add the explicit negative witness `comparison_rejects_agreeing_engines_when_expected_outcome_disagrees`: construct identical Rust/C++ accepted results for a case declared rejected, and identical rejected results for a case declared accepted, and require both comparisons to fail before numeric/engine-to-engine comparison. Category/field mismatches are also exact ordered semantic divergences, never process failures or numeric mismatches.

Add a semantic cache seed to pair inputs with `proxy_a` and `proxy_b` fingerprints, ordered support pairs, and metric bits. Each fingerprint contains only `shape_kind`, checked `child_index`, radius bits, and source-ordered vertex bits; it contains no pointer, allocation address, generation, tree/node coordinate, or private identity. Under the already non-default `differential-internals` feature, add a `#[doc(hidden)]` topology-safe replay API whose input is the two public shapes/children/transforms, `use_radii`, and that owned semantic seed. Return exactly `DistanceCacheReplayOutcome::Used { result }`, `Reset { result, reason }`, or `Rejected { reason }`; `Used` and `Reset` carry the ordinary owned public distance result.

Apply this exact source-ordered validation/replay precedence before constructing private `DistanceCache` or filling `b2SimplexCache`: (1) compare proxy A fingerprint, then proxy B fingerprint; first mismatch returns `Rejected(ProxyAFingerprintMismatch)` or `Rejected(ProxyBFingerprintMismatch)`; (2) require support count `1..=3`, otherwise `Rejected(SupportCountOutOfRange)`; (3) scan support pairs in source order and, for each pair, check index A bounds, then index B bounds, then whether the ordered pair duplicates any earlier pair, returning respectively `Rejected(SupportIndexAOutOfRange)`, `Rejected(SupportIndexBOutOfRange)`, or `Rejected(DuplicateSupportPair)` at the first failure; (4) reject a non-finite metric as `Rejected(NonFiniteMetric)`; (5) if the checked support count is exactly one, use the seed immediately and return `Used` without applying any metric reset predicate; (6) only for checked counts two or three, reconstruct `metric2` from the checked pairs and, if `metric2 < 0.5 * metric1 || 2.0 * metric1 < metric2`, execute cold and return `Reset { reason: MetricRatio }`; (7) otherwise, if `metric2 < EPSILON`, execute cold and return `Reset { reason: MetricTooSmall }`; (8) otherwise use the seed and return `Used`. This matches the source expression order in `b2Simplex::ReadCache` and the Rust `cache_metric_requires_flush`/`Simplex::read_cache`: ratio predicates precede the epsilon predicate and the complete flush predicate is evaluated only when simplex count is greater than one. Rust and C++ must use this identical ordering. Add an exact one-point warm replay whose finite metric would satisfy a reset predicate for a multi-point seed and prove it is still `Used`. Add compound-invalid tests for A+B fingerprint mismatch (A wins), B mismatch plus invalid count (B wins), invalid count plus bad indices (count wins), A+B index failure in one pair (A wins), duplicate pair plus non-finite/too-small metric (duplicate wins), non-finite metric plus reset conditions (NonFiniteMetric wins), and, for a two/three-point seed, metric simultaneously below epsilon and outside the ratio window (`MetricRatio` wins).

Do not add raw constructors, expose `DistanceProxy`, return `DistanceCache` mutation, accept mutable references, or leak `ProxyIdentity`/storage coordinates. Modify the private distance/simplex seam only enough to report whether a validated seed was used or source-reset; ordinary public `distance` behavior and cache construction remain unchanged. Mirror the same semantic fingerprint, precedence, reset predicates, and Used/Reset/Rejected vocabulary in C++.

Extend the Rust executor, C++ handlers, request/result schemas, Phase 5 policy, comparator, supervisor decoder, fixed fixture, native/C++ identity manifests and mutation tests, xtask commands/tests, just recipes, and oracle debug/release CI to exercise these new outcomes. Replace inferred corpus completeness with a serialized closed `CollisionWitnessFamily` enum and a fail-closed `CollisionWitnessFamily::REQUIRED` registry. The registry must enumerate at least these exact families: `ShapeAcceptedCircle`, `ShapeAcceptedEdge`, `ShapeAcceptedPolygon`, `ShapeAcceptedChain`, `ShapeAcceptedEdgeGhosts`, `ShapeAcceptedPolygonWeldHull`, `ShapeAcceptedChainTopology`, `ShapeRejectedCircle`, `ShapeRejectedEdge`, `ShapeRejectedPolygon`, `ShapeRejectedChain`, `ShapeRejectedEdgeGhost`, `ShapeRejectedPolygonWeldHull`, `ShapeRejectedChainAdjacentClosing`, `DistanceCold`, `DistanceWarmUsed`, `DistanceWarmSinglePointUsed`, `DistanceCacheReset`, `DistanceCacheRejected`, `DistanceCacheRejectedPrecedence`, `DistanceCacheResetPrecedence`, `DistanceSimplexOne`, `DistanceSimplexTwo`, `DistanceSimplexThree`, `DistanceSupportTie`, `DistanceNearZeroTermination`, `DistanceDuplicateSupportTermination`, `DistanceIterationLimitTermination`, `OverlapBelowThreshold`, `OverlapAtThreshold`, `OverlapAboveThreshold`, `ClipInsideInside`, `ClipOutsideOutside`, `ClipCrossingForward`, `ClipCrossingReverse`, `ClipOnPlane`, `PairCircleCircle`, `PairPolygonCircle`, `PairPolygonPolygon`, `PairEdgeCircle`, `PairEdgePolygon`, `PairChainCircle`, `PairChainPolygon`, `PairCirclePolygonReversed`, `PairCircleEdgeReversed`, `PairPolygonEdgeReversed`, `PairCircleChainReversed`, `PairPolygonChainReversed`, `EdgeIsolated`, `EdgeConvexAdjacency`, `EdgeConcaveAdjacency`, `EdgeFront`, `EdgeBack`, `EdgeEndpointOwnership`, `FeatureAdded`, `FeaturePersisted`, `FeatureRemoved`, `TreeInsertionTie`, `TreeRotationTie`, `TreeLifecycleReuse`, `TreeQueryContinueStop`, `TreeRayIgnoreTerminateClip`, `TreeMetrics`, `BroadPhaseDuplicateMoveTouch`, `BroadPhasePairOrderDedup`, `BroadPhaseFilterGroupsMasks`, `BroadPhaseRefilter`, `ToiOverlapped`, `ToiTouching`, `ToiSeparated`, `ToiTranslation`, `ToiRotation`, `ToiTangent`, `ToiSupportTie`, `ToiCapFailed`, `ToiLargeAngle`, and `ToiEdgeChainChildren`. Every required variant must occur in the fixed request; duplicates are allowed, missing variants fail decoding/xtask before spawning the oracle, and unrecognized variants fail closed through the schema. Add explicit tests that delete each required family in turn and prove rejection, rather than checking only that every operation appears once. The declaration-mismatch comparator test remains an intentionally failing mutation test and is not serialized as an accepted fixed family.

Complete the still-placeholder C++ collision handlers while adding the new evidence: construct all four bounded shape kinds, select checked chain children, execute all seven manifold families/reversals, real dynamic-tree and broad-phase commands/controls, all TOI shape-child combinations, and exact cache replay. Preserve semantic payload IDs and typed feature identities only. Update `native-math-sources.txt`, CMake adapter-content inputs, the exact three-translation-unit compile-command identity, source-mutation tests, schemas, and policy hash for every touched result-affecting source. The fixed corpus and fail-closed family registry are authoritative inputs to debug/release compare, replay, and two-run determinism; those commands must refuse to run if any required family, expected outcome, policy path, schema identity, native source, or C++ compile-command identity is missing.
  </action>
  <verify>
    - `cargo test -p liquidfun --test differential_feature cache_replay --features differential-internals`
    - `cargo test -p liquidfun collision::distance::tests::cache_replay --features differential-internals`
    - `cargo test -p liquidfun --test collision_shapes production_validation_boundaries --all-features`
    - `cargo check -p liquidfun --no-default-features`
    - `cargo test -p liquidfun --doc --no-default-features`
    - `RUSTDOCFLAGS="-D warnings" cargo doc -p liquidfun --no-deps`
    - `! cargo tree -p liquidfun -e features | rg 'differential-internals'`
    - `cargo test -p liquidfun --test differential_feature default_feature_import_is_compile_fail --no-default-features`
    - `cargo test -p liquidfun --test differential_feature package_surface_excludes_enabled_replay --no-default-features`
    - `cargo package -p liquidfun --allow-dirty --list`
    - `! cargo package -p liquidfun --allow-dirty --list | rg 'liquidfun-differential|tools/reference|third_party/liquidfun'`
    - `cargo test -p liquidfun-test-protocol collision_probe_expected_rejection --all-features`
    - `cargo test -p liquidfun-test-protocol collision_probe_required_families_are_complete --all-features`
    - `cargo test -p liquidfun-test-protocol collision_probe_required_family_deletion_fails_closed --all-features`
    - `cargo test -p liquidfun-test-protocol collision_policy --all-features`
    - `cargo test -p liquidfun-test-protocol schema --all-features`
    - `cargo test -p liquidfun-differential --test collision_probe rejected_shape_boundaries --all-features`
    - `cargo test -p liquidfun-differential --test collision_probe cache_replay_used_reset_rejected --all-features`
    - `cargo test -p liquidfun-differential --test collision_probe cache_replay_count_one_is_used --all-features`
    - `cargo test -p liquidfun-differential --test collision_probe cache_replay_compound_invalid_precedence --all-features`
    - `cargo test -p liquidfun-differential --test collision_probe comparison_rejects_agreeing_engines_when_expected_outcome_disagrees --all-features`
    - `cargo test -p liquidfun-differential --test collision_probe comparison --all-features`
    - `cargo test -p liquidfun-differential --test collision_probe supervisor --all-features`
    - `cargo test -p liquidfun-differential collision_source_changes_native_identity --all-features`
    - `cargo test -p xtask --test differential_cli collision_required_families --all-features`
    - `cargo test -p xtask --test differential_cli collision_compile_database_identity --all-features`
    - `cargo xtask upstream configure --preset oracle-debug`
    - `cargo xtask upstream build --preset oracle-debug`
    - `ctest --test-dir target/reference/oracle-debug --output-on-failure`
    - `cargo xtask differential compare --scenario collision-probes --preset oracle-debug --session-profile one-shot`
    - `cargo xtask differential compare --scenario collision-probes --preset oracle-release --session-profile one-shot`
    - `cargo xtask differential replay --scenario collision-probes --preset oracle-debug --session-profile one-shot`
    - `cargo xtask differential verify-determinism --scenario collision-probes --preset oracle-debug --runs 2`
    - `! rg "from_raw|raw_(slot|proxy|node|simplex)|ProxyIdentity|NodeSlot|&mut DistanceCache|pub .*DistanceProxy" crates/liquidfun/src/collision/differential.rs crates/liquidfun/src/collision/distance.rs crates/liquidfun-differential/src/collision_probe.rs`
  </verify>
  <acceptance_criteria>
    - `CollisionProbeCase` serializes one typed `ExpectedOutcome`; only expected-rejected `ShapeConstruction` retains bounded raw candidates, while accepted cases and every other operation run production-equivalent validation for edge ghosts/degeneracy, polygon limit/weld/hull/area/convexity, and the currently implemented chain finite/count/ghost/adjacent/closing/child rules.
    - Rust and C++ emit `Rejected { category, field }` for every invalid circle/edge/polygon/chain boundary. The comparator validates Rust versus the declaration, then C++ versus the declaration, before engine comparison; the named agreeing-engines/declaration-disagrees test fails with exact `expected_outcome` evidence.
    - The doc-hidden feature-gated cache API accepts only semantic proxy fingerprints plus ordered support pairs/metric and applies the exact precedence A fingerprint, B fingerprint, count, per-pair A index/B index/duplicate, non-finite metric, then count-one Used; only counts two/three evaluate ratio reset, metric-too-small reset, then Used.
    - The exact one-point warm witness returns Used without metric reset checks, and compound-invalid tests prove the first deterministic reason wins, including `MetricRatio` over `MetricTooSmall` for counts two/three; Rust and C++ return identical outcomes and reasons.
    - Ordinary public `distance`, default/no-default feature checks, compile-fail import, warning-denied default docs, default cargo feature tree, and packaged consumer/API scan expose no enabled replay seed constructor, private proxy/cache storage, raw identity, mutable access, or `differential-internals` activation.
    - Rust and C++ handlers execute all four shapes, seven pair families and reversals, tree/broad-phase controls, TOI child combinations, rejected-shape cases, and cold/warm/reset/rejected cache cases through identical semantic records.
    - `CollisionWitnessFamily::REQUIRED` contains every enumerated family above, including implemented chain boundaries, one-point warm-cache use, production accepted-shape invariants, and cache precedence; the fixed fixture contains all of them, deleting any one makes protocol/xtask validation fail before process spawn, and schema unknown-family tests fail closed.
    - Native source identity covers every touched Rust result source, C++ adapter identity covers every touched adapter input, and compile-command identity still requires exactly `collision_probe.cpp`, `math_probe.cpp`, and `protocol_bits.cpp`.
    - Focused production validation, protocol, feature-on/feature-off, compile-fail, docs, package, differential, precedence, supervisor, identity, xtask, CTest, debug/release compare, replay, and two-run determinism commands exit 0.
  </acceptance_criteria>
  <done>D-22 is closed by fail-closed fixed evidence for rejected shape boundaries, topology-safe cache replay, every required witness family, and complete native/C++ collision execution without widening the public engine API.</done>
</task>

</tasks>

<verification>
- Run all focused protocol/differential/C++/xtask commands, then `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Run oracle debug/release configure/build/CTest, fixed collision compare in both profiles, replay, and the exact two-run `verify-determinism` command; inspect D0 evidence tier and compile-command identity.
- Assert the fixed request contains every `CollisionWitnessFamily::REQUIRED` variant; delete each variant in a focused test and prove protocol/xtask rejection occurs before native or C++ execution.
- Exercise production-equivalent accepted validation and expected-rejected shape cases plus cold, Used, Reset, and Rejected cache cases through native/C++ comparison; verify exact rejection category/field, declaration-first comparison, deterministic compound-invalid precedence, and cache outcome/reason alignment.
- Run no-feature check, compile-fail import, warning-denied default docs, default feature-tree exclusion, and package/API absence tests to prove the replay seam remains disabled outside private differential tooling.
- Run `cargo xtask package verify`, `cargo xtask provenance check`, and `git diff --check`; verify no generated fixture/artifact changed during read-only compare/replay/determinism verification.
</verification>

<success_criteria>
- `COLL-07` has bounded native, differential, replay, and D0 evidence spanning every Phase 5 subsystem.
- All exact order/identity/branch and numeric policies are explicit and first-divergence diagnosable.
- Expected invalid shape boundaries and semantic cache replay are typed, topology-safe, and differentially exact without weakening ordinary decode or public cache invariants.
- Both result streams must satisfy the declared expected outcome independently before matching each other, so shared executor mistakes cannot satisfy a contradictory case declaration.
- The enumerated required-family registry fails closed on every missing D-22 witness before oracle process spawn.
- C++ remains isolated private tooling and Cargo-only package use remains unaffected.
</success_criteria>

<must_haves>
- One existing protocol/supervisor/oracle path, not a parallel harness.
- Complete D-22 fixed witness coverage with exact structural/order evidence.
- Typed expected rejection and Used/Reset/Rejected cache evidence with no raw storage or mutable replay surface.
- Fail-closed enumerated required-family assertions, not inferred operation-count coverage.
- Policy and evidence authority follow Phase 4 D0-D3 rules without global or adaptive tolerance.
</must_haves>
