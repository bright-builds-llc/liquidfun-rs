---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-07-11T14-53-25
generated_at: 2026-07-11T15:12:45.000Z
phase: 05-shapes-and-collision-foundation
plan: "04"
type: execute
wave: 4
depends_on:
  - "05-03"
files_modified:
  - crates/liquidfun/src/collision/narrow.rs
  - crates/liquidfun/src/collision/narrow/clipping.rs
  - crates/liquidfun/src/collision/narrow/circle.rs
  - crates/liquidfun/src/collision/narrow/polygon.rs
  - crates/liquidfun/src/collision/narrow/edge.rs
  - crates/liquidfun/tests/collision_manifolds.rs
autonomous: true
requirements:
  - COLL-03
  - COLL-07
must_haves:
  truths:
    - Private clipping preserves pinned output and feature order and is independently probeable.
    - Every registered manifold pair and reversal produces semantic ordered feature identity without packed keys or impulses.
    - Unsupported, separated, and touching outcomes remain distinct and feature-state transitions are exact.
  artifacts:
    - crates/liquidfun/src/collision/narrow.rs
    - crates/liquidfun/src/collision/narrow/clipping.rs
    - crates/liquidfun/src/collision/narrow/polygon.rs
    - crates/liquidfun/src/collision/narrow/edge.rs
    - crates/liquidfun/tests/collision_manifolds.rs
  key_links:
    - Plan 05-01 semantic manifold values carry Plan 05-04 kernel output without inactive C++ fields.
    - Canonical pair orientation mirrors pinned `b2Contact::Create` and feeds future Phase 6 contact identity/persistence.
---

# Plan 05-04: Clipping, Manifolds, and Pair Dispatch

<objective>
Implement private clipping, semantic feature transitions, all seven pinned manifold pair families, canonical reversal handling, and exact branch/order regression coverage.
</objective>

<decision_coverage>
Implements locked decisions D-09, D-10, D-11, and D-12: private ordered clipping, semantic manifold identity, exact supported-pair/reversal dispatch, feature transitions, and no Phase 5 impulses.
</decision_coverage>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| Generic pair dispatch | Arbitrary shape variants/child indices/order | Closed registry, checked chain child, explicit unsupported/separated/touching result, canonical primary orientation. |
| Clip kernel | Segment endpoints, feature IDs, plane | Invariant finite inputs from validated shapes; fixed two-point output only. |
| Manifold persistence | Previous/current feature lists | Compare semantic typed identity and bounded active points only. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T04-1 | Spoofing | Contact identity | Mitigate | Typed four-field features replace packed keys and exact transition tests prevent identity aliasing. |
| T04-2 | Tampering | Pair orientation | Mitigate | One closed registry records canonical order and explicit reversal; tests cover every reverse input. |
| T04-3 | Denial of service | Clipping/manifold output | Mitigate | Fixed two-point arrays/slices and bounded polygon counts prevent growth. |
| T04-4 | Repudiation | Inactive/impulse fields | Mitigate | Output types omit inactive fields and Phase 5 impulses; tests reject accidental serialization/exposure. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement ordered clipping and feature-state transitions</name>
  <files>crates/liquidfun/src/collision/narrow.rs, crates/liquidfun/src/collision/narrow/clipping.rs, crates/liquidfun/tests/collision_manifolds.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/types.rs
    - crates/liquidfun/src/collision/distance.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2Collision.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2Collision.cpp
    - .planning/phases/05-shapes-and-collision-foundation/05-CONTEXT.md
    - .planning/phases/04-math-settings-and-numerical-policy/04-CONTEXT.md
  </read_first>
  <action>
Implement private `ClipVertex` and `clip_segment_to_line` with the exact Sutherland-Hodgman order: compute both signed distances in source order; retain each `<= 0.0` input in input order; if their product is `< 0.0`, append the interpolated crossing; assign crossing feature A from the supplied vertex index and feature B from input zero exactly. Expose no public clip primitive, but return a bounded diagnostic form for Plan 05-07. Implement pure point-state transition over ordered semantic feature IDs, returning null/add/persist/remove exactly like `b2GetPointStates` without the union key. Add tests for inside/inside, outside/outside, each crossing orientation, on-plane endpoints, near-parallel segments, reversed input order, crossing identity, output count/order, and one/two-point persistence transitions.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_manifolds clipping --all-features`
    - `cargo test -p liquidfun --test collision_manifolds point_state --all-features`
    - `! rg "contact.*key|\.key|union|HashSet" crates/liquidfun/src/collision/narrow.rs crates/liquidfun/src/collision/narrow`
  </verify>
  <acceptance_criteria>
    - Clip count, retained/crossing order, and feature identity exactly follow the pinned function.
    - Point transitions use ordered semantic identities with no packed key or unordered collection.
    - Clip and transition outputs remain bounded to `MAX_MANIFOLD_POINTS`.
    - Named clipping/point-state tests exit 0.
  </acceptance_criteria>
  <done>Clipping and point persistence are pure, ordered, semantic, and independently testable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement circle and polygon manifold kernels</name>
  <files>crates/liquidfun/src/collision/narrow.rs, crates/liquidfun/src/collision/narrow/circle.rs, crates/liquidfun/src/collision/narrow/polygon.rs, crates/liquidfun/tests/collision_manifolds.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/narrow/clipping.rs
    - crates/liquidfun/src/collision/shape/polygon.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2CollideCircle.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2CollidePolygon.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2Collision.cpp
  </read_first>
  <action>
Translate circle-circle, polygon-circle, polygon-polygon, and world-manifold conversion using pinned operand/loop order. Preserve first-edge ties via strict comparisons, polygon incident-edge selection, point order, and reference-face hysteresis exactly at `separationB > separationA + 0.1 * LINEAR_SLOP`; preserve A/B feature swap when B becomes reference. Initialize only semantic active fields and never add impulses. Add tests for separated/tangent/deep-overlap cases, polygon vertex/face regions, coincident circles, all reference-face boundary sides, two-point clip order, flipped identity, world normal A-to-B orientation, and active separation/point values.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_manifolds circle --all-features`
    - `cargo test -p liquidfun --test collision_manifolds polygon --all-features`
    - `cargo test -p liquidfun --test collision_manifolds world_manifold --all-features`
  </verify>
  <acceptance_criteria>
    - Circle and polygon kernels reproduce every pinned region/reference/flip branch for valid shapes.
    - The hysteresis equality selects face A and strict-above selects face B.
    - Active feature IDs and point order are exact and inactive/impulse fields remain absent.
    - Named kernel tests exit 0.
  </acceptance_criteria>
  <done>Circle/polygon manifold generation and world conversion preserve source branches and semantic identity.</done>
</task>

<task type="auto">
  <name>Task 3: Implement adjacency-aware edge kernels and closed pair registry</name>
  <files>crates/liquidfun/src/collision/narrow.rs, crates/liquidfun/src/collision/narrow/edge.rs, crates/liquidfun/tests/collision_manifolds.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/shape/edge.rs
    - crates/liquidfun/src/collision/shape/chain.rs
    - crates/liquidfun/src/collision/narrow/circle.rs
    - crates/liquidfun/src/collision/narrow/polygon.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Collision/b2CollideEdge.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Contacts/b2Contact.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Contacts/b2ChainAndCircleContact.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Contacts/b2ChainAndPolygonContact.cpp
  </read_first>
  <action>
Translate edge-circle region/adjacency rejection and edge-polygon EPCollider classification, normal limits, separation axes, hysteresis (`0.98`, `0.001`), clipping, front/back selection, and identity swap in pinned order. Implement one closed dispatcher for exactly circle-circle, polygon-circle, polygon-polygon, edge-circle, edge-polygon, chain-child-circle, and chain-child-polygon. Canonicalize reversed inputs as the pinned contact registry does and retain explicit orientation in the outcome; reject invalid chain children and distinguish unsupported from supported-separated. Add table-driven tests for every primary/reversed pair, every unsupported pair, isolated/convex/concave adjacency, front/back collisions, endpoint ownership, edge/polygon axis hysteresis, chain child delegation, exact point order, and feature transitions across nearby poses. Keep private clip primitives private, but expose supported collision dispatch and point-state/world-manifold operations through `liquidfun::collision::narrow` so wave-4 public integration tests compile before final root integration.
  </action>
  <verify>
    - `cargo test -p liquidfun --test collision_manifolds edge --all-features`
    - `cargo test -p liquidfun --test collision_manifolds pair_registry --all-features`
    - `cargo clippy -p liquidfun --all-targets --all-features -- -D warnings`
  </verify>
  <acceptance_criteria>
    - All seven and only seven supported pair families dispatch in canonical primary order.
    - `liquidfun::collision::narrow` publicly exposes only semantic collision/state operations and no private clip vertex/kernel.
    - Every reversed input reports its orientation and yields correctly mapped semantic A/B features.
    - Edge adjacency/front/back/tie behavior is covered by named branch witnesses.
    - Edge/registry tests and strict Clippy exit 0.
  </acceptance_criteria>
  <done>All pinned narrow-phase manifold pairs, reversals, adjacency branches, and persistence identities are implemented.</done>
</task>

</tasks>

<verification>
- Run all manifold tests, then `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Run warning-denied rustdoc and package isolation.
- Review every pair against the pinned registry and scan for packed keys, impulses, unordered point handling, invented pairs, or inactive-field comparison.
</verification>

<success_criteria>
- `COLL-03` clipping, manifold, feature transition, world conversion, and supported pair behavior is complete.
- Exact identity/order branches are regression-protected before Phase 6 contact storage consumes them.
- No packed C++ key, impulse, or unsupported manifold behavior enters the public contract.
</success_criteria>

<must_haves>
- Clipping and manifold points remain exact ordered observables.
- Pair support and reversal semantics are closed and source-audited.
- Numeric policies stay in the evidence layer; production kernels do not use a global comparison epsilon.
</must_haves>
