---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 4-2026-07-11T04-16-20
generated_at: 2026-07-11T04:42:00.000Z
phase: 04-math-settings-and-numerical-policy
plan: "04"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - crates/liquidfun-test-protocol/src/scenario.rs
  - crates/liquidfun-test-protocol/src/scenario/math_probe.rs
  - crates/liquidfun-test-protocol/src/trace.rs
  - crates/liquidfun-differential/src/math_probe.rs
  - crates/liquidfun-differential/src/rust_adapter.rs
  - protocol/fixtures/accepted/math-probe-request.jsonl
  - scenarios/phase-04/math-probes.json
autonomous: true
requirements:
  - COLL-01
  - COLL-08
must_haves:
  truths:
    - A bounded pure math request executes through native Rust without world or solver state.
    - Every f32 input and output is exact bits with class/sign metadata and an explicit policy path.
    - The corpus covers every D-14 exceptional class, rounding witness, operation family, and repeated-composition horizon.
  artifacts:
    - crates/liquidfun-test-protocol/src/scenario/math_probe.rs
    - crates/liquidfun-differential/src/math_probe.rs
    - scenarios/phase-04/math-probes.json
  key_links:
    - Closed probe operation IDs map one-to-one to public Rust math kernels.
    - Typed results are compared only through the phase4-v1 policy created by plan 04-03.
---

# Plan 04-04: Bounded Native Math Probe Contract

<objective>
Define the bounded exact-bit probe contract, implement its native Rust executor, and check in the complete deterministic Phase 4 corpus that the later C++ adapter must match.
</objective>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| JSONL math-probe request | Operation IDs, bit operands, case count, horizons | Closed enum, strict schema, duplicate rejection, named limits before allocation/execution. |
| Native dispatch | Validated enum cases | Exhaustive direct dispatch to `liquidfun::math`; no names, paths, commands, or dynamic calls. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T04-1 | Tampering | Probe request | Mitigate | Task 1 rejects unknown/duplicate cases and preserves exact input bits through deterministic validation. |
| T04-2 | Elevation of privilege | Native dispatch | Mitigate | Task 1 uses a closed enum and accepts no executable, path, compiler flag, pointer, or function-name field. |
| T04-3 | Denial of service | Corpus/horizon execution | Mitigate | Task 1 applies existing record limits plus explicit maximum case and `ScenarioSteps` counts before allocation. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement exact-bit protocol, native dispatch, and complete witness corpus</name>
  <files>crates/liquidfun-test-protocol/src/scenario.rs, crates/liquidfun-test-protocol/src/scenario/math_probe.rs, crates/liquidfun-test-protocol/src/trace.rs, crates/liquidfun-differential/src/math_probe.rs, crates/liquidfun-differential/src/rust_adapter.rs, protocol/fixtures/accepted/math-probe-request.jsonl, scenarios/phase-04/math-probes.json</files>
  <read_first>
    - crates/liquidfun-test-protocol/src/scenario.rs
    - crates/liquidfun-test-protocol/src/trace.rs
    - crates/liquidfun-test-protocol/src/limits.rs
    - crates/liquidfun-differential/src/rust_adapter.rs
    - crates/liquidfun/src/math.rs
    - protocol/fixtures/accepted/empty-world-request.jsonl
    - scenarios/phase-02/empty-world.json
    - .planning/phases/04-math-settings-and-numerical-policy/04-CONTEXT.md
  </read_first>
  <action>
Add a closed `MathProbeOperation` enum with named variants for `IsValid`, ordered `Abs/Min/Max/Clamp`, `InvSqrt`, `VecLength`, `VecNormalize`, `Dot`, `Cross`, `Mat22Solve`, `Mat33Solve`, `Mat22Inverse`, `Mat33SymInverse`, `Rotation`, `Transform`, `SweepTransform`, `SweepAdvance`, and `SweepNormalize`, plus numeric witnesses `Cancellation` (`(1.0e20 + -1.0e20) + 3.14` versus `1.0e20 + (-1.0e20 + 3.14)`), `HalfwayRounding` (exact adjacent-f32 midpoint cases around `1.0`), `Overflow` (`f32::MAX * 2.0`), `Underflow` (`f32::MIN_POSITIVE * 0.25`), and `FmaWitness` (record separate `a*b+c` using fixed bit operands known to differ from fused evaluation, never call `mul_add`). Requests use exact `FloatBits`, stable case IDs, bounded structured scalar/vector/matrix/sweep inputs, and `Operation` or bounded `ScenarioSteps`. Add ordered results containing operation ID, exact output bits, class/sign, and discrete branch/predicate values. Implement exhaustive pure native dispatch to `liquidfun::math` without world creation. Check in cases for ordinary values, both zeros, min/max subnormal, min normal, max finite, both infinities, several NaN payloads, epsilon neighbors, singular/near-singular matrices, tau fractions, large angles, transform composition, sweep endpoints, all five numeric witnesses, and repeated transform/sweep composition. Add accepted/rejected codec tests for unknown operations, oversize case/step counts, duplicates, malformed bits, and every witness's exact-bit output.
  </action>
  <verify>
    - `cargo test -p liquidfun-test-protocol math_probe --all-features`
    - `cargo test -p liquidfun-test-protocol math_probe_scenario_is_byte_stable --all-features`
    - `cargo test -p liquidfun-differential native_math_probe_executes_complete_witness_corpus --all-features`
    - `cargo test -p liquidfun-differential native_math_probe_witness_bits_are_exact --all-features`
  </verify>
  <acceptance_criteria>
    - The enum contains all listed math operations and all five named numeric witness variants.
    - Checked-in cases contain signed zero, subnormal, infinity, NaN, epsilon, singular, angle, transform, sweep, cancellation, halfway, overflow, underflow, and FMA witnesses.
    - Tests assert exact output bits for every witness and byte-stable scenario re-encoding.
    - Payloads contain no executable, path, raw pointer, compiler flag, or arbitrary function name.
  </acceptance_criteria>
  <done>The bounded native probe contract and complete exact-bit witness corpus pass all focused tests.</done>
</task>

</tasks>

<verification>
- Run focused protocol/native checks, then `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Review generated JSON and exact-bit assertions; do not generate or promote canonical C++ evidence in this plan.
</verification>

<success_criteria>
- Every locked D-14 pure-probe witness has a stable named case and exact-bit assertion.
- Native execution is closed, bounded, deterministic, and independent of world/solver state.
- The C++ plan can implement against one complete validated wire contract.
</success_criteria>

<must_haves>
- Probe dispatch cannot execute arbitrary input.
- All exceptional and rounding cases remain exact bits on the wire.
- Scenario stability and bounded horizons are executable tests.
</must_haves>
