---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 4-2026-07-11T04-16-20
generated_at: 2026-07-11T04:42:00.000Z
phase: 04-math-settings-and-numerical-policy
plan: "06"
type: execute
wave: 5
depends_on:
  - "04-05"
files_modified:
  - tools/xtask/src/differential.rs
  - justfile
  - .github/workflows/ci.yml
  - .github/workflows/oracle.yml
  - protocol/schemas/scenario-v1.schema.json
  - protocol/schemas/trace-v1.schema.json
autonomous: true
requirements:
  - COLL-01
  - COLL-08
must_haves:
  truths:
    - Local and CI entrypoints execute the same named Phase 4 checks without arbitrary command/path input.
    - Canonical Linux runs debug/release probes, D0 repetition, identity validation, and read-only evidence checks under pinned tools.
  artifacts:
    - tools/xtask/src/differential.rs
    - .github/workflows/oracle.yml
    - .github/workflows/ci.yml
  key_links:
    - Thin just recipes call allowlisted xtask commands.
    - CI uses the exact scenario and policy verified by the local commands.
---

# Plan 04-06: Verification Entry Points and Canonical CI

<objective>
Expose transparent allowlisted Phase 4 verification, deterministic schemas, and canonical/supported CI lanes without changing documentation or compatibility claims yet.
</objective>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| xtask CLI | Scenario/profile/preset/action strings | Closed allowlists; reject arbitrary paths, executables, flags, and unknown values. |
| CI supply chain | Actions and downloaded tools | Full action SHAs, exact archive checksums, read-only permissions, no secrets. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T06-1 | Elevation of privilege | xtask routing | Mitigate | Task 1 accepts only `math-probes`, reviewed presets/profiles, and fixed subcommands. |
| T06-2 | Tampering | CI tools/actions | Mitigate | Task 1 retains full-SHA actions, checksum verification, and `contents: read`. |
| T06-3 | Repudiation | Evidence mutation | Mitigate | Task 1 ends canonical runs with an explicit read-only `git diff --exit-code`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire allowlisted commands, schemas, and CI lanes</name>
  <files>tools/xtask/src/differential.rs, justfile, .github/workflows/ci.yml, .github/workflows/oracle.yml, protocol/schemas/scenario-v1.schema.json, protocol/schemas/trace-v1.schema.json</files>
  <read_first>
    - tools/xtask/src/differential.rs
    - justfile
    - .github/workflows/ci.yml
    - .github/workflows/oracle.yml
    - crates/liquidfun-test-protocol/src/schema.rs
    - protocol/schemas/scenario-v1.schema.json
    - protocol/schemas/trace-v1.schema.json
    - scenarios/phase-04/math-probes.json
  </read_first>
  <action>
Allow `--scenario math-probes` to resolve only `scenarios/phase-04/math-probes.json`; add fixed compare, replay, and D0 repeated-byte-stability paths without accepting arbitrary executable/compiler/path input. Add thin just recipes that print/call exact cargo commands. Regenerate scenario/trace schemas deterministically. In canonical Linux retain pinned Rust 1.97.0, Clang 22.1.8, CMake 4.3.3, Ninja 1.13.2, checksums, SHA-pinned actions, `contents: read`, and no secrets; run oracle-debug and oracle-release compare plus D0 repetition, identity rejection tests, and `git diff --exit-code -- protocol scenarios reference COMPATIBILITY.md`. Add Rust-only math-contract/policy tests to supported OS CI. Compare/replay never promotes fixtures; randomized/sanitizer/exploratory modes remain scheduled/manual.
  </action>
  <verify>
    - `cargo test -p xtask differential --all-features`
    - `cargo test -p liquidfun-test-protocol --lib schema::tests --all-features`
    - `cargo test -p liquidfun-test-protocol --test fixtures --all-features`
    - `cargo xtask differential compare --scenario math-probes --preset oracle-debug --session-profile one-shot`
    - `cargo xtask differential compare --scenario math-probes --preset oracle-release --session-profile one-shot`
    - `cargo xtask differential replay --scenario math-probes --preset oracle-debug --session-profile one-shot`
    - `cargo xtask differential verify-determinism --scenario math-probes --preset oracle-debug --runs 2`
  </verify>
  <acceptance_criteria>
    - xtask/just expose only named Phase 4 values and every listed command exits 0.
    - Canonical CI pins tools/actions, uses read-only permissions, runs debug/release/D0 checks, and asserts evidence is unchanged.
    - Supported OS CI runs public math and policy tests without the C++ submodule.
    - Generated schema fixture tests exit 0 and no compare path calls fixture promotion.
  </acceptance_criteria>
  <done>Phase 4 local/CI verification is allowlisted, deterministic, pinned, and read-only.</done>
</task>

</tasks>

<verification>
- Run all named commands, then `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Run `git diff --check` and inspect workflow permissions, action SHAs, tool checksums, and command allowlists.
</verification>

<success_criteria>
- Phase 4 probe/policy behavior is continuously exercised under canonical and supported assumptions.
- Commands are transparent, reproducible, and cannot execute arbitrary input.
- Evidence stays read-only unless a separate reviewed promotion command is invoked.
</success_criteria>

<must_haves>
- Canonical tool identity is asserted before probes.
- Consumer CI remains C++-free.
- D0 determinism has an executable repeated-run command.
</must_haves>
