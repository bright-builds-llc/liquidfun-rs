---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 4-2026-07-11T04-16-20
generated_at: 2026-07-11T04:42:00.000Z
phase: 04-math-settings-and-numerical-policy
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun-test-protocol/src/tolerance.rs
  - crates/liquidfun-test-protocol/src/tolerance/policy.rs
  - crates/liquidfun-differential/src/comparator.rs
  - crates/liquidfun-differential/src/report.rs
  - crates/liquidfun-differential/tests/comparison.rs
  - protocol/tolerances/phase4-v1.toml
autonomous: true
requirements:
  - COLL-08
must_haves:
  truths:
    - Every authoritative observable is compared through a closed versioned policy with explicit special-value, collection, horizon, and platform semantics.
    - Signed zero is distinct and arithmetic NaN mismatches by default; no global epsilon or time-growing tolerance exists.
    - First-divergence reports explain exact bits, numeric distances, policy identity, horizon, tier, and bounded context.
  artifacts:
    - crates/liquidfun-test-protocol/src/tolerance.rs
    - crates/liquidfun-test-protocol/src/tolerance/policy.rs
    - crates/liquidfun-differential/src/comparator.rs
    - crates/liquidfun-differential/src/report.rs
    - protocol/tolerances/phase4-v1.toml
  key_links:
    - Exact FloatBits transport remains independent from ToleranceProfile semantic equality.
    - Comparator output is derived only from validated profile and trace domain types.
---

# Plan 04-03: Versioned Numerical Policy and Diagnostics

<objective>
Extend the Phase 2 comparator into a closed field-policy registry covering exact/ULP/absolute/relative rules, IEEE special values, collection order, fixed horizons, platform tiers, and actionable first-divergence evidence.
</objective>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| Tolerance TOML/profile decoding | External fields, thresholds, paths, horizons | Deny unknown/duplicate fields; bound lengths/horizons; require finite nonnegative thresholds and valid combinations. |
| Trace comparison | Validated expected/actual float bits and collections | Require matching profile hash/version/provenance before observing values. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T03-1 | Tampering | Policy profile/hash | Mitigate | Task 1 hashes every canonical field and rejects duplicates, wildcards, invalid thresholds, and unsupported combinations. |
| T03-2 | Repudiation | Mismatch reports | Mitigate | Task 2 records stable signature, policy hash, bits, horizon, tier, and bounded provenance. |
| T03-3 | Information disclosure | Diagnostics | Mitigate | Task 2 emits allowlisted identity fields and bounded context, never environment variables or arbitrary commands. |
| T03-4 | Denial of service | Profile/collection comparison | Mitigate | Task 1 bounds semantic paths/horizons and Task 2 consumes only protocol-bounded validated collections. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Model and validate the complete policy registry</name>
  <files>crates/liquidfun-test-protocol/src/tolerance.rs, crates/liquidfun-test-protocol/src/tolerance/policy.rs, protocol/tolerances/phase4-v1.toml</files>
  <read_first>
    - crates/liquidfun-test-protocol/src/tolerance.rs
    - crates/liquidfun-test-protocol/src/float_bits.rs
    - protocol/tolerances/phase2-v1.toml
    - .planning/phases/04-math-settings-and-numerical-policy/04-CONTEXT.md
    - .planning/phases/04-math-settings-and-numerical-policy/04-RESEARCH.md
  </read_first>
  <action>
Preserve `FloatPolicy::{ExactBits, Absolute, AbsoluteRelative, Ulps}` and add invariant-bearing closed types for `ZeroPolicy::{Distinct, SignInsensitive}`, `NonFinitePolicy::{RejectArithmeticNaN, ExactBitsTransport, SameSignInfinity}`, `DivergenceHorizon::{Operation, PhaseLocal, ScenarioSteps(NonZeroU32)}`, and `EvidenceTier::{D0Replay, D1Canonical, D2Supported, D3Exploratory}`. Define one semantic-field policy record containing a bounded semantic path, float or discrete rule, zero/non-finite rule, collection policy, horizon, tier, and nonempty source justification. Validate finite nonnegative thresholds, uniqueness, nonzero bounded step horizons, allowed policy combinations, and stable sorting. Parse `protocol/tolerances/phase4-v1.toml` with unknown-field denial and hash the complete canonical field set. Seed the profile with explicit math probe paths covering exact constants/predicates/branches, exact or ULP local kernels, and absolute-relative composite outputs; do not add a wildcard/default entry. Add focused Arrange/Act/Assert tests for every rejected combination and deterministic hash change.
  </action>
  <verify>
    - `cargo test -p liquidfun-test-protocol tolerance --all-features`
    - `! rg -i "global.*epsilon|default.*tolerance|grow.*tolerance|wildcard" crates/liquidfun-test-protocol/src/tolerance.rs crates/liquidfun-test-protocol/src/tolerance protocol/tolerances/phase4-v1.toml`
  </verify>
  <acceptance_criteria>
    - Every phase4 profile row has an explicit semantic path, equality policy, zero/non-finite rule, collection semantics, horizon, tier, and justification.
    - Duplicate paths, NaN/infinite/negative thresholds, zero/unbounded step horizons, and unsupported combinations produce typed validation errors.
    - Canonical serialization and SHA-256 identity are order-stable and change when any policy field changes.
    - `cargo test -p liquidfun-test-protocol tolerance --all-features` exits 0.
  </acceptance_criteria>
  <done>The closed Phase 4 policy profile is strict, complete, deterministic, and has no global fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Apply policy and enrich first-divergence evidence</name>
  <files>crates/liquidfun-differential/src/comparator.rs, crates/liquidfun-differential/src/report.rs, crates/liquidfun-differential/tests/comparison.rs</files>
  <read_first>
    - crates/liquidfun-differential/src/comparator.rs
    - crates/liquidfun-differential/src/report.rs
    - crates/liquidfun-differential/src/canonical.rs
    - crates/liquidfun-differential/tests/comparison.rs
    - crates/liquidfun-test-protocol/src/tolerance.rs
  </read_first>
  <action>
Refactor numeric comparison to accept only validated field policies. Preserve exact bits before decoding; arithmetic NaN always mismatches, transport NaN uses exact bits, infinity matches only by sign where allowed, and signed zeros compare by the named zero policy. Calculate bounded absolute, relative, and ordered-bit ULP distances for diagnostics without changing pass/fail rules. Enforce ordered/set/multiset semantics explicitly and reject hash iteration as an ordering source. Extend mismatch reports with semantic path, expected/actual bits and decimal display, float class/sign, all three distances, policy/profile hash, horizon, evidence tier, build/request/scenario/seed/generator identity when available, and at most one previous/next checkpoint. Keep one stable primary failure signature and a bounded sibling count. Add table-driven one-concern tests for exact boundaries, signed zero, NaNs, infinities, subnormals, ULP sign crossing, absolute-relative near zero/large magnitude, collection multiplicity, fixed horizons, and unchanged Phase 2 behavior.
  </action>
  <verify>
    - `cargo test -p liquidfun-differential --test comparison --all-features`
    - `cargo test -p liquidfun-differential comparator --all-features`
    - `cargo test -p liquidfun-differential --test comparison deliberate_mismatch_reports_phase4_diagnostics --all-features`
  </verify>
  <acceptance_criteria>
    - Arithmetic NaN, same-sign infinity permissions, and signed-zero policies match the locked rules exactly.
    - No comparator code uses an unnamed epsilon or modifies thresholds based on elapsed steps.
    - Reports bound neighboring context to one record on each side and exclude environment values.
    - Existing Phase 2 comparison tests and new Phase 4 policy tests exit 0.
  </acceptance_criteria>
  <done>The comparator applies only explicit validated rules and produces bounded actionable first-divergence evidence.</done>
</task>

</tasks>

<verification>
- Run protocol and differential focused tests, then before each commit run: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Verify profile parsing is read-only and fixture promotion remains a separate reviewed command.
- Review every policy enum exhaustiveness match and ensure new variants cannot silently fall through.
</verification>

<success_criteria>
- `COLL-08` has an explicit machine-readable policy for NaN, infinity, signed zero, exact/tolerant fields, ordering, horizons, and platforms.
- Every mismatch is reproducible from a stable signature and enough bounded provenance to diagnose it.
- Phase 2 exact transport and failure-taxonomy guarantees remain intact.
</success_criteria>

<must_haves>
- No global epsilon or automatic tolerance growth exists.
- Unknown policy state fails closed.
- Transport bits and semantic comparison remain separate concerns.
</must_haves>
