---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 4-2026-07-11T04-16-20
generated_at: 2026-07-11T04:42:00.000Z
phase: 04-math-settings-and-numerical-policy
plan: "07"
type: execute
wave: 6
depends_on:
  - "04-06"
files_modified:
  - tools/xtask/src/docs.rs
  - tools/xtask/tests/docs_contract.rs
  - ARCHITECTURE.md
  - TESTING.md
  - COMPATIBILITY.md
  - README.md
autonomous: true
requirements:
  - COLL-01
  - COLL-08
must_haves:
  truths:
    - Public math conventions and numerical policy are discoverable and match executable commands/evidence.
    - Compatibility rows claim only proven Phase 4 scope and leave collision/solver/platform work pending.
  artifacts:
    - ARCHITECTURE.md
    - TESTING.md
    - COMPATIBILITY.md
    - tools/xtask/tests/docs_contract.rs
  key_links:
    - Documentation contract tests connect stated commands/policies to checked-in code and CI.
    - Compatibility claims derive from Phase 4 evidence, never from implementation presence alone.
---

# Plan 04-07: Numerical Policy Documentation and Compatibility Sign-Off

<objective>
Publish precise math, policy, command, platform, and evidence documentation and update compatibility status only to the level actually verified by Phase 4.
</objective>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| Machine evidence to human claims | Test/probe/status records | Provenance/inventory/docs checks and scoped derivation; no manual broad parity claim. |
| Documentation commands | Copied contributor input | Exact allowlisted commands already implemented by plan 04-06. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T07-1 | Spoofing | Compatibility claims | Mitigate | Task 1 updates only Phase 4 rows backed by passing evidence and adds docs contract assertions. |
| T07-2 | Repudiation | Policy/evidence description | Mitigate | Task 1 records policy hash/tier/horizon authority and executable commands. |
| T07-3 | Information disclosure | Documentation | Accept | Public docs contain no secrets; docs tests reject accidental local paths/identity dumps where applicable. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Document contracts and evidence-backed status</name>
  <files>tools/xtask/src/docs.rs, tools/xtask/tests/docs_contract.rs, ARCHITECTURE.md, TESTING.md, COMPATIBILITY.md, README.md</files>
  <read_first>
    - tools/xtask/src/docs.rs
    - tools/xtask/tests/docs_contract.rs
    - ARCHITECTURE.md
    - TESTING.md
    - COMPATIBILITY.md
    - README.md
    - .planning/phases/04-math-settings-and-numerical-policy/04-CONTEXT.md
    - .planning/phases/04-math-settings-and-numerical-policy/04-RESEARCH.md
  </read_first>
  <action>
Document every public math type, initialized/safe difference, fixed setting, MKS/radian/column-major/transform/sweep convention, and source mapping. Record exact transport versus comparison, arithmetic NaN/infinity rules, signed-zero default, exact/ULP/absolute/absolute-relative selection, ordered/set/multiset semantics, fixed Operation/PhaseLocal/ScenarioSteps horizons, D0-D3 authority, canonical compiler/feature assumptions, and debug/release/supported-platform interpretation. List exact plan 04-06 commands, prerequisites, outcomes, evidence locations, and the rule that only D1 can promote canonical fixtures. Update only `b2Math`, `b2Settings`, common math/settings, `COLL-01`, and `COLL-08` compatibility dimensions to the evidence actually proven; leave shapes, collision, solvers, particles, complete platform parity, performance parity, and production readiness pending. Extend docs checks to assert required headings/phrases, exact command strings, tier authority, special-value rules, and cross-file agreement.
  </action>
  <verify>
    - `cargo test -p xtask --test docs_contract`
    - `cargo xtask docs check`
    - `cargo xtask inventory check`
    - `cargo xtask provenance check`
    - `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps`
    - `cargo xtask package verify`
    - `! rg -i "full parity|production ready|all platforms validated" README.md ARCHITECTURE.md TESTING.md COMPATIBILITY.md`
  </verify>
  <acceptance_criteria>
    - TESTING.md contains all four float policies, signed-zero/NaN/infinity rules, all three horizons, D0-D3 tiers, canonical commands, and evidence limits.
    - ARCHITECTURE.md keeps public math, private protocol/comparator, and external C++ adapter direction explicit.
    - COMPATIBILITY.md changes only Phase 4-related rows and leaves Phase 5+ capabilities pending.
    - Docs, inventory, provenance, rustdoc, package isolation, and no-overclaim commands exit 0.
  </acceptance_criteria>
  <done>Phase 4 contracts and status are precise, discoverable, command-backed, and truthfully scoped.</done>
</task>

</tasks>

<verification>
- Run every named docs/evidence command, then `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Run `git diff --check` and review Markdown for frontmatter safety, accidental local paths, unsupported claims, and unrelated churn.
</verification>

<success_criteria>
- `COLL-01` and `COLL-08` documentation matches implementation and executable evidence.
- Contributors can reproduce the correct lane without hidden commands.
- No Phase 5+ or all-platform maturity is overstated.
</success_criteria>

<must_haves>
- Documentation is evidence-derived and contract-tested.
- Platform tiers and promotion authority are unambiguous.
- Ordinary packaged-crate use remains independent of C++.
</must_haves>
