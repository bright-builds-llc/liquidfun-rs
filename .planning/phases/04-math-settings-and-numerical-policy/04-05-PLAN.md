---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 4-2026-07-11T04-16-20
generated_at: 2026-07-11T04:42:00.000Z
phase: 04-math-settings-and-numerical-policy
plan: "05"
type: execute
wave: 4
depends_on:
  - "04-04"
files_modified:
  - crates/liquidfun-test-protocol/src/provenance.rs
  - crates/liquidfun-test-protocol/src/trace.rs
  - crates/liquidfun-test-protocol/src/trace/tests.rs
  - crates/liquidfun-differential/build.rs
  - crates/liquidfun-differential/src/rust_adapter.rs
  - tools/reference/CMakeLists.txt
  - tools/reference/src/build_identity.hpp.in
  - tools/reference/src/protocol_bits.cpp
  - tools/reference/src/math_probe.hpp
  - tools/reference/src/math_probe.cpp
  - tools/reference/src/protocol.hpp
  - tools/reference/src/protocol.cpp
  - tools/reference/src/main.cpp
autonomous: true
requirements:
  - COLL-01
  - COLL-08
must_haves:
  truths:
    - The pinned C++ adapter implements the complete native probe contract with representation-preserving bit transport.
    - Canonical floating flags and full Rust/C++ build identity are validated before comparison.
    - Missing, contradictory, or forbidden identity is a harness/provenance failure rather than a physics mismatch.
  artifacts:
    - tools/reference/src/math_probe.cpp
    - tools/reference/src/protocol_bits.cpp
    - tools/reference/src/build_identity.hpp.in
    - crates/liquidfun-test-protocol/src/provenance.rs
  key_links:
    - C++ operation IDs and result records exactly match plan 04-04's validated contract.
    - Rust and C++ identity fields map one-to-one onto the strict protocol provenance type.
---

# Plan 04-05: Pinned C++ Probes and Floating Build Identity

<objective>
Implement the external C++ math-probe backend, bit-preserving conversion, explicit canonical floating configuration, and complete strict identity required to compare it with native Rust.
</objective>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| Supervisor/JSONL to C++ adapter | Validated operation records and exact bits | Closed enum dispatch, protocol bounds, no commands/paths/flags in payload. |
| C++ stdout identity/results to Rust | Child-controlled records | Strict schema/version/hash/field validation before numeric comparison. |
| CMake/compiler environment | Compiler, target, flags, SDK, runtime FP mode | Allowlisted preset and required/forbidden flag/runtime witnesses; fail D1 closed. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T05-1 | Spoofing | Build identity | Mitigate | Task 2 records command hash and exact compiler/target/FP fields and validates them on Rust decode. |
| T05-2 | Tampering | Float transport | Mitigate | Task 1 uses `std::memcpy`, static size assertions, and exact exceptional-bit round trips. |
| T05-3 | Elevation of privilege | C++ dispatch | Mitigate | Task 1 exhaustively switches closed operation IDs and receives no executable/path/flag input. |
| T05-4 | Denial of service | Adapter records | Mitigate | Existing protocol/process bounds remain in force; probe kernels are bounded arithmetic. |
| T05-5 | Information disclosure | Identity/diagnostics | Mitigate | Identity uses allowlisted fields; environment variables and secrets are never serialized. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement bit-faithful C++ probe dispatch</name>
  <files>tools/reference/src/protocol_bits.cpp, tools/reference/src/math_probe.hpp, tools/reference/src/math_probe.cpp, tools/reference/src/protocol.hpp, tools/reference/src/protocol.cpp, tools/reference/src/main.cpp</files>
  <read_first>
    - tools/reference/src/protocol_bits.cpp
    - tools/reference/src/protocol.hpp
    - tools/reference/src/protocol.cpp
    - tools/reference/src/main.cpp
    - crates/liquidfun-test-protocol/src/scenario/math_probe.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Common/b2Math.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Common/b2Math.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Common/b2Settings.h
  </read_first>
  <action>
Mirror every `MathProbeOperation` discriminant and structured record from plan 04-04 in `math_probe.hpp/cpp`, dispatch exhaustively to pinned `b2Math`/`b2Settings`, and emit the identical ordered typed result including all cancellation/halfway/overflow/underflow/FMA witnesses. Replace arithmetic reconstruction in `protocol_bits.cpp` with `std::memcpy` between `uint32_t` and `float`, guarded by `static_assert(sizeof(float) == sizeof(uint32_t))`; do not union-pun or perform arithmetic conversion. Wire the new request/result records through `protocol.hpp/cpp` and `main.cpp` while keeping stdout protocol-only and stderr bounded diagnostics. Add adapter self-tests or Rust-driven fixtures named `cpp_protocol_bits_preserve_exceptional_classes` and `cpp_math_probe_matches_operation_contract` that cover both zeros, min/max subnormal, infinities, quiet/signaling NaNs, every operation ID, unknown-ID rejection, and reset/reuse behavior.
  </action>
  <verify>
    - `cargo test -p liquidfun-differential cpp_protocol_bits_preserve_exceptional_classes --all-features`
    - `cargo test -p liquidfun-differential cpp_math_probe_matches_operation_contract --all-features`
    - `! rg "ldexp|pow\(|reinterpret_cast.*float|union" tools/reference/src/protocol_bits.cpp`
  </verify>
  <acceptance_criteria>
    - `protocol_bits.cpp` uses `std::memcpy` and a float/u32 size assertion with no arithmetic or union conversion.
    - C++ dispatch covers every plan 04-04 operation/witness and rejects unknown IDs.
    - Exceptional-class round-trip tests assert exact `uint32_t` equality.
    - Protocol stdout remains parseable records only and reuse reset tests pass.
  </acceptance_criteria>
  <done>The C++ adapter implements the complete bit-faithful bounded probe contract.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce canonical flags and one-to-one build identity</name>
  <files>crates/liquidfun-test-protocol/src/provenance.rs, crates/liquidfun-test-protocol/src/trace.rs, crates/liquidfun-test-protocol/src/trace/tests.rs, crates/liquidfun-differential/build.rs, crates/liquidfun-differential/src/rust_adapter.rs, tools/reference/CMakeLists.txt, tools/reference/src/build_identity.hpp.in, tools/reference/src/protocol.hpp, tools/reference/src/main.cpp</files>
  <read_first>
    - crates/liquidfun-test-protocol/src/provenance.rs
    - crates/liquidfun-test-protocol/src/trace.rs
    - crates/liquidfun-test-protocol/src/trace/tests.rs
    - crates/liquidfun-differential/build.rs
    - crates/liquidfun-differential/src/rust_adapter.rs
    - tools/reference/CMakeLists.txt
    - tools/reference/src/build_identity.hpp.in
    - tools/reference/src/protocol.hpp
    - tools/reference/src/main.cpp
    - tools/reference/CMakePresets.json
  </read_first>
  <action>
For Clang canonical math/probe translation units require checked support for `-fno-fast-math`, `-ffp-model=precise`, `-ffp-contract=off`, `-fsigned-zeros`, `-fhonor-nans`, `-fhonor-infinities`, `-fdenormal-fp-math=ieee`, and `-fdenormal-fp-math-fp32=ieee`. Reject `-ffast-math`, `-Ofast`, reassociation, reciprocal approximation, unsafe-math, `-march=native`, and equivalent native CPU tuning in D1. Extend `RawBuildIdentity` decoding/validation in `trace.rs`, its focused fixtures in `trace/tests.rs`, the native constructor in `rust_adapter.rs`, the Rust compile-time values emitted by `build.rs`, and the configured C++ constructor in `build_identity.hpp.in`/`main.cpp` one-to-one with exact fields `compile_command_sha256`, `compiler_id`, `compiler_version`, `target_triple`, `target_cpu`, `target_features`, `sdk_or_sysroot`, `optimization`, `fp_model`, `fp_contract`, `denormal_mode`, `feature_set`, `os`, `libc`, `libm`, `rounding_mode`, and `gradual_underflow`. Use empty strings only where D2 permits a documented unavailable field. Require D1 values and runtime round-to-nearest/gradual-underflow witnesses before comparison. Add tests named `canonical_identity_rejects_forbidden_fp_flags`, `canonical_identity_requires_all_fields`, `raw_build_identity_decodes_all_phase4_fields`, `native_build_identity_populates_all_phase4_fields`, and `supported_identity_cannot_promote_canonical_evidence`.
  </action>
  <verify>
    - `cargo test -p liquidfun-test-protocol provenance --all-features`
    - `cargo test -p liquidfun-test-protocol raw_build_identity_decodes_all_phase4_fields --all-features`
    - `cargo test -p liquidfun-differential native_build_identity_populates_all_phase4_fields --all-features`
    - `cargo test -p liquidfun-differential canonical_identity_rejects_forbidden_fp_flags --all-features`
    - `cargo test -p liquidfun-differential canonical_identity_requires_all_fields --all-features`
    - `cargo test -p liquidfun-differential supported_identity_cannot_promote_canonical_evidence --all-features`
    - `cargo xtask upstream configure --preset oracle-debug`
    - `cargo xtask upstream build --preset oracle-debug`
  </verify>
  <acceptance_criteria>
    - Rust and C++ encode/decode the same 17 named identity fields and the protocol hash covers all of them.
    - `RawBuildIdentity`, validated provenance, native Rust construction, and configured C++ construction all carry those fields without defaults in D1.
    - Canonical configure fails on unsupported required semantics or any forbidden fast/native flag.
    - D1 comparison requires complete matching identity and runtime witnesses; D2/D3 cannot promote canonical evidence.
    - Oracle debug config/build and all named identity tests exit 0.
  </acceptance_criteria>
  <done>Compiler and runtime floating assumptions are explicit, wire-compatible, and fail closed before comparison.</done>
</task>

</tasks>

<verification>
- Run focused C++/identity checks, then `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Run `cargo xtask upstream verify`, configure/build oracle-debug and oracle-release, then compare the math-probe scenario in both profiles.
- Inspect the effective compile commands and handshake; no forbidden flag or secret/environment value may appear.
</verification>

<success_criteria>
- Pinned C++ and native Rust execute one identical bounded pure-probe contract.
- All IEEE bits cross the C++ boundary unchanged.
- Complete compatible build identity gates every numerical comparison.
</success_criteria>

<must_haves>
- C++ remains external developer tooling and never enters the publishable crate.
- D1 floating behavior cannot silently degrade to compiler defaults.
- Identity failures remain separate from physics mismatches.
</must_haves>
