---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 4-2026-07-11T04-16-20
generated_at: 2026-07-11T04:42:00.000Z
phase: 04-math-settings-and-numerical-policy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/math.rs
  - crates/liquidfun/src/math/scalar.rs
  - crates/liquidfun/src/math/vector.rs
  - crates/liquidfun/src/math/settings.rs
autonomous: true
requirements:
  - COLL-01
must_haves:
  truths:
    - Consumers can construct initialized vectors and use source-ordered scalar/vector operations with documented MKS and radians conventions.
    - Every fixed setting has the intended upstream-equivalent value and exact f32 bit pattern.
    - Exceptional IEEE values remain representable in raw math while validity predicates classify them explicitly.
  artifacts:
    - crates/liquidfun/src/math.rs
    - crates/liquidfun/src/math/scalar.rs
    - crates/liquidfun/src/math/vector.rs
    - crates/liquidfun/src/math/settings.rs
  key_links:
    - liquidfun::math publicly curates the safe Rust surface while preserving pinned b2Math operation order.
    - math::settings maps exact upstream constants without exposing mutable global C++ infrastructure.
---

# Plan 04-01: Scalar, Vector, and Settings Foundation

<objective>
Create the public initialized math foundation: pinned-order scalar helpers, `Vec2`/`Vec3`/`Vec4`, exact settings constants, MKS/radian documentation, and focused bit-level regression tests.
</objective>

<threat_model>
### Trust boundaries

| Boundary | Untrusted input | Required validation |
| --- | --- | --- |
| Public math constructors/operators | Caller-provided `f32` bit patterns | Preserve raw IEEE values; finite-only domain constructors reject NaN/infinity explicitly. |
| Public settings surface | Consumer reads/links constants | Constants are immutable compile-time values; no mutation or override API exists. |

### STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- |
| T01-1 | Tampering | `math::settings` | Mitigate | Task 2 asserts exact constant bits and exposes only `pub const`, never `static mut`, atomics, locks, or setters. |
| T01-2 | Elevation of privilege | `liquidfun::math` | Mitigate | Task 1 preserves `#![forbid(unsafe_code)]`, adds no raw constructors/unchecked indexing, and verifies forbidden patterns. |
| T01-3 | Denial of service | Scalar/vector operations | Accept | Operations are allocation-free bounded arithmetic; expected non-finite values are classified without loops or panics. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement source-ordered scalar and vector math</name>
  <files>crates/liquidfun/src/lib.rs, crates/liquidfun/src/math.rs, crates/liquidfun/src/math/scalar.rs, crates/liquidfun/src/math/vector.rs</files>
  <read_first>
    - crates/liquidfun/src/lib.rs
    - .planning/phases/04-math-settings-and-numerical-policy/04-CONTEXT.md
    - .planning/phases/04-math-settings-and-numerical-policy/04-RESEARCH.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Common/b2Math.h
    - standards/languages/rust.md
  </read_first>
  <action>
Add public `math` exports and private children using `math.rs` plus `math/`, never `math/mod.rs`. Implement initialized `Vec2 { pub x: f32, pub y: f32 }`, `Vec3`, and `Vec4` with `Copy`, `Clone`, `Debug`, `Default`, and `PartialEq`, `ZERO` constants, ordinary vector/scalar operators, dot/cross/skew, length/length-squared, normalize returning the original length and leaving values unchanged while returning `0.0` when length is below `f32::EPSILON`, and finite validity predicates. Implement pinned-branch scalar helpers `is_valid`, `abs`, `min`, `max`, `clamp`, power-of-two helpers, distance, and the approximate inverse square root with safe `to_bits`/`from_bits`. Preserve operand order and do not use `f32::min/max/clamp`, `mul_add`, `sin_cos`, unchecked indexing, `unsafe`, or a math dependency. Add rustdoc naming the upstream mapping and MKS/radian conventions. Add focused Arrange/Act/Assert unit tests for ordinary arithmetic, signed-zero helper bits, NaN operand order, subnormal validity, epsilon normalization, and inverse-square-root witnesses.
  </action>
  <verify>
    - `cargo test -p liquidfun math::scalar --all-features`
    - `cargo test -p liquidfun math::vector --all-features`
    - `! rg "unsafe|mul_add|sin_cos|f32::(min|max|clamp)|mod math" crates/liquidfun/src/math.rs crates/liquidfun/src/math`
  </verify>
  <acceptance_criteria>
    - `crates/liquidfun/src/lib.rs` publicly exports `math` and `math.rs` declares `scalar`, `vector`, and `settings` children.
    - `Vec2`, `Vec3`, and `Vec4` are initialized public value types and no float-bearing type derives `Eq`, `Hash`, or `Ord`.
    - Tests assert exact `to_bits()` results for both signed-zero helper directions and multiple NaN operand orders.
    - `cargo test -p liquidfun math::scalar --all-features` and `cargo test -p liquidfun math::vector --all-features` exit 0.
  </acceptance_criteria>
  <done>The public scalar/vector layer is initialized, documented, source-ordered, and protected by exact edge-case tests.</done>
</task>

<task type="auto">
  <name>Task 2: Translate fixed compatibility settings</name>
  <files>crates/liquidfun/src/math/settings.rs</files>
  <read_first>
    - crates/liquidfun/src/math.rs
    - crates/liquidfun/src/math/scalar.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Common/b2Settings.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Documentation/Programmers-Guide/Chapter01_Introduction.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Documentation/Programmers-Guide/Chapter03_Common.md
  </read_first>
  <action>
Create these exact settings: `MAX_FLOAT = f32::MAX`, `EPSILON = f32::EPSILON`, `PI = 3.14159265359_f32`, `TAU = 2.0 * PI`; collision `MAX_MANIFOLD_POINTS = 2`, `MAX_POLYGON_VERTICES = 8`, `AABB_EXTENSION = 0.1`, `AABB_MULTIPLIER = 2.0`, `LINEAR_SLOP = 0.005`, `ANGULAR_SLOP = 2.0 / 180.0 * PI`, `POLYGON_RADIUS = 2.0 * LINEAR_SLOP`, `MAX_SUB_STEPS = 8`; dynamics `MAX_TOI_CONTACTS = 32`, `VELOCITY_THRESHOLD = 1.0`, `MAX_LINEAR_CORRECTION = 0.2`, `MAX_ANGULAR_CORRECTION = 8.0 / 180.0 * PI`, `MAX_TRANSLATION = 2.0`, `MAX_TRANSLATION_SQUARED = MAX_TRANSLATION * MAX_TRANSLATION`, `MAX_ROTATION = 0.5 * PI`, `MAX_ROTATION_SQUARED = MAX_ROTATION * MAX_ROTATION`, `BAUMGARTE = 0.2`, `TOI_BAUMGARTE = 0.75`; particle `PARTICLE_STRIDE = 0.75`, `MIN_PARTICLE_WEIGHT = 1.0`, `MAX_PARTICLE_PRESSURE = 0.25`, `MAX_PARTICLE_FORCE = 0.5`, `MAX_TRIAD_DISTANCE = 2.0`, `MAX_TRIAD_DISTANCE_SQUARED = MAX_TRIAD_DISTANCE * MAX_TRIAD_DISTANCE`, `MIN_PARTICLE_SYSTEM_BUFFER_CAPACITY = 256`, `BARRIER_COLLISION_TIME = 2.5`; sleep `TIME_TO_SLEEP = 0.5`, `LINEAR_SLEEP_TOLERANCE = 0.01`, `ANGULAR_SLEEP_TOLERANCE = 2.0 / 180.0 * PI`. Preserve these exact expression groupings and document units/upstream spellings. They are immutable `pub const` values. Exclude allocator/free callbacks, logging/version strings, assertion/debug macros, C typedefs, `b2_invalidParticleIndex`, and `B2_USE_16_BIT_PARTICLE_INDICES`. Add one focused test per family asserting exact bits/values and squared/derived relationships.
  </action>
  <verify>
    - `cargo test -p liquidfun math::settings --all-features`
    - `! rg "pub static|Atomic|Mutex|invalid.*particle.*index|alloc.*callback|16_BIT_PARTICLE" crates/liquidfun/src/math/settings.rs`
  </verify>
  <acceptance_criteria>
    - Collision, dynamics, particle, and sleep constant families from `b2Settings.h` are represented and rustdoc names their units.
    - `PI.to_bits()` and every derived angular constant are asserted against pinned expected bits.
    - No allocator, logger, version string, assertion macro, dense-index sentinel, or particle-index-width switch is public.
    - `cargo test -p liquidfun math::settings --all-features` exits 0.
  </acceptance_criteria>
  <done>The fixed settings surface is complete, immutable, documented, and bit-verified.</done>
</task>

</tasks>

<verification>
- Run the focused task tests, then before every commit run exactly: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.
- Run `cargo package -p liquidfun --allow-dirty` and inspect the package file list to confirm no C++ or private harness dependency entered the published crate.
- Review the diff for `unsafe`, implicit layout promises, global epsilon APIs, and unrelated formatting churn.
</verification>

<success_criteria>
- `COLL-01` has a safe consumer-facing scalar/vector/settings foundation with exact upstream mapping.
- Signed-zero, NaN, subnormal, epsilon, and constant-bit behavior is regression-protected.
- Cargo-only consumers remain independent of C++ and private differential crates.
</success_criteria>

<must_haves>
- Source-order compatibility is explicit and testable.
- Settings are fixed values, not mutable policy knobs.
- Raw math can represent all IEEE bits without making non-finite physics input valid.
</must_haves>
