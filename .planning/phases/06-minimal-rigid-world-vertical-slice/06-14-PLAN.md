---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T08:10:00Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "14"
type: execute
wave: 10
depends_on: ["06-03"]
files_modified:
  - crates/liquidfun/src/world/body.rs
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/src/world/fixture.rs
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/tests/fixture_dynamics.rs
autonomous: true
requirements: [RIGD-02]
gap_closure: true
must_haves:
  truths:
    - Aggregate fixture mass, weighted center, origin inertia, normalization, and centered inertia are computed in fixture source order with every intermediate required to remain finite and valid.
    - Positive-density fixture creation validates the complete prospective aggregate before fixture allocation, proxy creation, body adjacency insertion, or body-mass mutation.
    - Explicit mass reset returns a typed error and leaves fixture topology, proxies, adjacency, and prior body mass bit-identical when aggregation fails.
  artifacts:
    - crates/liquidfun/src/world/body.rs
    - crates/liquidfun/src/world/object.rs
    - crates/liquidfun/tests/fixture_dynamics.rs
  key_links:
    - One fallible aggregate-mass function supplies both create preflight and explicit reset; neither path repeats unchecked arithmetic.
---

# Plan 06-14: Aggregate Mass Atomicity

<objective>
Close `aggregate-mass-atomicity` by replacing post-commit infallible aggregation with one source-ordered, fallible validate-before-commit transaction.
</objective>

<threat_model>

## Trust boundaries

| Threat | Boundary | Mitigation |
| --- | --- | --- |
| Partial mutation | Individually finite fixture mass values can overflow only when aggregated | Compute and validate the full prospective body mass state before any fixture/proxy/adjacency/body mutation. |
| Panic denial of service | Aggregate `f32` overflow or invalid centered inertia reaches an assertion | Return a typed create/reset error; remove the assertion from caller-controlled arithmetic. |
| Ordering drift | Refactoring changes solver-visible accumulation order | Traverse the same newest-first body fixture adjacency, placing a create candidate at its prospective source position. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make aggregate mass calculation fallible and transactional</name>
  <files>crates/liquidfun/src/world/body.rs, crates/liquidfun/src/world/object.rs, crates/liquidfun/src/world/fixture.rs, crates/liquidfun/src/world.rs, crates/liquidfun/src/lib.rs, crates/liquidfun/tests/fixture_dynamics.rs</files>
  <read_first>
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-VERIFICATION.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-REVIEW.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-CONTEXT.md
    - crates/liquidfun/src/world/body.rs
    - crates/liquidfun/src/world/object.rs
    - crates/liquidfun/src/world/fixture.rs
    - crates/liquidfun/src/world.rs
    - crates/liquidfun/src/lib.rs
    - crates/liquidfun/tests/fixture_dynamics.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.cpp
    - standards/core/architecture.md
    - standards/core/testing.md
    - standards/languages/rust.md
  </read_first>
  <action>
Replace `BodyState::reset_mass_data` with a pure fallible calculation that returns a complete candidate mass state. In existing fixture adjacency order, validate each mass addition, each `mass * center` component, weighted-center addition, inertia addition, reciprocal/normalization operation, `mass * dot(center, center)`, and centered-inertia subtraction for finiteness and the pinned non-negative/positive branch invariant. Add typed aggregate errors to fixture creation and explicit reset. For positive-density create, assemble the prospective candidate-first mass-data sequence and validate it before diagnostic-ID allocation, arena insertion, proxy creation, adjacency insertion, or body mutation; commit only after success. For explicit reset, calculate first and apply once. Keep density-edit asymmetry and static/kinematic behavior unchanged. Add Arrange/Act/Assert regressions using two individually valid high-density fixtures whose aggregate overflows; prove the rejected create and reset do not panic and preserve fixture adjacency/count, live fixture snapshots, broad-phase count, contact count, and body mass/center/inertia bits.
  </action>
  <verify>
    <automated>cargo test -p liquidfun --test fixture_dynamics aggregate_mass --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p liquidfun --test fixture_dynamics aggregate_mass --all-features` exits 0 with separate create and explicit-reset atomicity tests.
    - `rg -n "Result&lt;.*Mass|Result<.*Mass|InvalidAggregateMass" crates/liquidfun/src/world/body.rs crates/liquidfun/src/world/object.rs crates/liquidfun/src/world/fixture.rs` finds the fallible boundary and typed errors.
    - `! rg -n "checked aggregate fixture inertia must remain positive|reset_mass_data\(&mut self.*\) \{" crates/liquidfun/src/world/body.rs` exits 0.
  </acceptance_criteria>
  <done>Aggregate overflow is a typed no-effect failure on both create and reset, with exact source ordering retained.</done>
</task>

</tasks>

<verification>
Run the focused regression, then `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` in that order, followed by `git diff --check` and a simplification review confirming one aggregate implementation serves both paths.
</verification>

<success_criteria>
The critical verifier finding is closed without a panic, partial mutation, alternate accumulation order, or Phase 7 API expansion.
</success_criteria>

<output>After completion, create `06-14-SUMMARY.md` and preserve the gap ID `aggregate-mass-atomicity` in its verification evidence.</output>
