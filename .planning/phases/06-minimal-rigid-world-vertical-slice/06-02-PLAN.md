---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T02:43:09.000Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/world/body.rs
  - crates/liquidfun/src/world/fixture.rs
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/tests/object_model.rs
  - crates/liquidfun/tests/hook_contract.rs
  - crates/liquidfun/tests/rigid_world.rs
autonomous: true
requirements: [RIGD-01, RIGD-02]
must_haves:
  truths:
    - Consumers create bodies and fixtures only through complete world-scoped handle validation and checked definitions.
    - Body type, transform, active state, fixture attachment, and semantic snapshots are inspectable without durable borrows or raw storage.
    - Stale, destroyed, cross-world, or wrong-kind inputs fail before mutation and preserve prior state.
  artifacts:
    - crates/liquidfun/src/world/object.rs
    - crates/liquidfun/tests/rigid_world.rs
  key_links:
    - World arenas store BodyDef/FixtureDef-derived private records while public methods return owned snapshots.
    - Existing destruction records and Phase 3 identity checks remain the only invalidation authority.
---

# Plan 06-02: Handle-Oriented Rigid World State

<objective>
Replace the Phase 3 placeholder body/fixture records with checked rigid state and granular World operations while preserving identity and destruction contracts.
</objective>

<decision_coverage>
D-02 and the storage/operation portions of D-01, D-03, and D-04 are fully implemented. Proxy/contact side effects in D-05 are completed by Plans 06-03 and 06-04.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Typed handle to arena authority | Opaque caller handles must be checked for kind, world, slot, and generation before record access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T06-02-01 | Spoofing | BodyId/FixtureId lookup | mitigate | Reuse complete Phase 3 identity validation before every read or mutation. |
| T06-02-02 | Tampering | Create/mutate transitions | mitigate | Validate all operands and capture required state before commit; add no-partial-mutation tests. |
| T06-02-03 | Elevation of privilege | Public API | mitigate | Keep arenas, records, mutable borrows, and object-style mutators private; authority remains on World. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store checked bodies and expose granular World operations</name>
  <files>crates/liquidfun/src/world/object.rs, crates/liquidfun/src/world/body.rs, crates/liquidfun/src/world.rs, crates/liquidfun/src/lib.rs, crates/liquidfun/tests/rigid_world.rs</files>
  <read_first>
    - .planning/phases/03-rust-object-model-and-storage-architecture/03-02-SUMMARY.md
    - .planning/phases/03-rust-object-model-and-storage-architecture/03-05-SUMMARY.md
    - crates/liquidfun/src/world/object.rs
    - crates/liquidfun/src/world/body.rs
    - crates/liquidfun/src/arena.rs
    - crates/liquidfun/src/identity.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.cpp
  </read_first>
  <behavior>
    - `World::create_body(&BodyDef)` returns a stable BodyId and snapshot with exact type/transform/active state.
    - Granular inspect/type/transform/active operations reject invalid handles before changing any live body.
    - Destruction retains owned pre-invalidation body state while the handle becomes stale immediately.
  </behavior>
  <action>
Deepen the private Body record with the checked definition state plus internal transform/sweep/velocity fields required later by Plan 06-05, without exporting those velocity fields. Add handle-oriented create, snapshot, type, transform, and active methods on World. Centralize lookup/validation and validate complete candidate state before commit. Extend body destruction snapshots with semantic Phase 6 state while preserving the established joint-before-fixture-before-body cascade category order.
  </action>
  <verify><automated>cargo test -p liquidfun --test rigid_world body --all-features</automated></verify>
  <acceptance_criteria>
    - `cargo test -p liquidfun --test rigid_world body --all-features` exits 0 for all types, mutations, stale reuse, cross-world rejection, and no-partial-mutation cases.
    - `rg -n "pub fn (create_body|body_snapshot|set_body_type|set_body_transform|set_body_active)" crates/liquidfun/src/world/object.rs` finds granular World methods.
    - `! rg -n "pub fn .*body_mut|pub struct Body \{|pub .*velocity" crates/liquidfun/src/world/object.rs crates/liquidfun/src/world/body.rs` exits 0.
  </acceptance_criteria>
  <done>Body authority remains on World and complete typed identity protects every rigid state operation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store immutable fixtures and preserve Phase 3 consumers</name>
  <files>crates/liquidfun/src/world/object.rs, crates/liquidfun/src/world/fixture.rs, crates/liquidfun/tests/object_model.rs, crates/liquidfun/tests/hook_contract.rs, crates/liquidfun/tests/rigid_world.rs</files>
  <read_first>
    - crates/liquidfun/src/world/fixture.rs
    - crates/liquidfun/tests/object_model.rs
    - crates/liquidfun/tests/hook_contract.rs
    - crates/liquidfun/src/world/step.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Fixture.cpp
  </read_first>
  <behavior>
    - `World::create_fixture(body, &FixtureDef)` validates the body then clones immutable fixture state.
    - Fixture snapshots expose semantic owner/shape/material/filter/sensor state without storage references.
    - Existing Phase 3 identity, hook, command, poison, and cascade tests retain their behavior after adopting checked definitions.
  </behavior>
  <action>
Deepen Fixture storage with an owned Shape and definition state, add granular fixture inspection, and migrate Phase 3 test setup to explicit checked definitions. Preserve newest-first body fixture adjacency and existing deferred-command behavior. Do not add proxy/contact behavior yet; this plan establishes the validated storage seam consumed by Plan 06-03.
  </action>
  <verify><automated>cargo test -p liquidfun --test rigid_world fixture --all-features &amp;&amp; cargo test -p liquidfun --test object_model --all-features &amp;&amp; cargo test -p liquidfun --test hook_contract --all-features</automated></verify>
  <acceptance_criteria>
    - All three automated test targets exit 0.
    - `rg -n "pub fn (create_fixture|fixture_snapshot)" crates/liquidfun/src/world/object.rs` finds handle-oriented methods.
    - `! rg -n "shape_mut|&mut Shape|pub .*ProxyId" crates/liquidfun/src/world crates/liquidfun/src/lib.rs` exits 0.
  </acceptance_criteria>
  <done>Checked body/fixture storage replaces placeholders without weakening Phase 3 identity, hook, poison, or destruction guarantees.</done>
</task>

</tasks>

<verification>Run the focused targets, crate doctests, strict Clippy, and `git diff --check`; review all signature migrations for accidental public authority.</verification>
<success_criteria>Consumers can perform the Phase 6 body/fixture lifecycle through safe handles, and every invalid operation is explicit and atomic.</success_criteria>
<output>After completion, create `.planning/phases/06-minimal-rigid-world-vertical-slice/06-02-SUMMARY.md`.</output>
