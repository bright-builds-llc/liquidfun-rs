---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T02:43:09.000Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "04"
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/src/world/proxy.rs
  - crates/liquidfun/src/world/contact.rs
  - crates/liquidfun/src/world/contact_manager.rs
  - crates/liquidfun/src/world/step.rs
  - crates/liquidfun/tests/rigid_contacts.rs
autonomous: true
requirements: [RIGD-04]
must_haves:
  truths:
    - Broad-phase pairs automatically create at most one ordered contact occurrence for each eligible fixture-child pair.
    - Contacts persist by semantic feature identity, update touching state, defer refiltering, and destroy through one manager path.
    - Sensors emit begin/end touching transitions but retain no manifold and never enter pre-solve or solver work.
    - Friction/restitution edits do not rewrite values already mixed into an authoritative existing contact; recreation uses the new fixture values.
    - Filtering, deactivation, fixture destruction, and body cascades emit end/destruction evidence before dependent invalidation.
  artifacts:
    - crates/liquidfun/src/world/contact.rs
    - crates/liquidfun/src/world/contact_manager.rs
    - crates/liquidfun/tests/rigid_contacts.rs
  key_links:
    - ContactManager consumes BroadPhase ordered pairs and `collide_shapes` canonical manifolds.
    - Destruction transitions call the same manager removal path before proxy/arena invalidation.
---

# Plan 06-04: Automatic Contact Lifecycle

<objective>
Replace caller-supplied representative contacts with one private world-owned contact manager covering creation, persistence, sensors, filtering, and ordered destruction.
</objective>

<decision_coverage>
D-08, D-09, D-10, D-11, D-13, and D-15 are fully implemented here. D-12 solving and D-14 enriched reports complete in Plan 06-05.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Broad-phase candidate to durable private contact | Potential pairs are admitted only after owner, duplicate, active, same-body, and filter checks. |
| Contact lifecycle to public evidence | Private occurrence identity is converted to borrow-scoped views and owned semantic events only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T06-04-01 | Spoofing | Contact pair identity | mitigate | Use private oriented fixture-child keys plus checked fixture ownership; never accept caller-created contacts. |
| T06-04-02 | Tampering | Adjacency/destruction | mitigate | Centralize validate-then-commit contact removal and assert body/fixture/manager adjacency coherence. |
| T06-04-03 | Repudiation | Event order | mitigate | Preserve manager occurrence order and exact transition/destruction records in focused tests. |
| T06-04-04 | Information disclosure | Contact API | mitigate | No public durable contact ID, raw contact list, proxy ID, or mutable manifold access. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admit, create, update, and persist automatic contacts</name>
  <files>crates/liquidfun/src/world/contact.rs, crates/liquidfun/src/world/contact_manager.rs, crates/liquidfun/src/world/proxy.rs, crates/liquidfun/src/world/object.rs, crates/liquidfun/src/world.rs, crates/liquidfun/tests/rigid_contacts.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/broad_phase.rs
    - crates/liquidfun/src/collision/narrow.rs
    - crates/liquidfun/src/collision/types.rs
    - crates/liquidfun/src/world/proxy.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2ContactManager.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Contacts/b2Contact.cpp
  </read_first>
  <behavior>
    - Ordered eligible pairs create one newest-first manager occurrence and duplicates are suppressed.
    - Non-sensor updates compute canonical manifolds and transfer point correspondence by ContactFeatureId.
    - Friction is square-root product and restitution is max at creation; later fixture edits leave authoritative existing-contact values unchanged while recreation uses the edits.
    - Sensor updates use overlap only and retain no manifold.
  </behavior>
  <action>
Create private contact and manager modules with oriented fixture-child identity, creation ordinal, body/fixture adjacency, touching/enabled/filter flags, optional manifold, mixed material, and point impulse slots reserved for Plan 06-05. Consume ordered `BroadPhase::update_pairs`, reject same-body/duplicate/inactive/filter pairs, and insert in pinned occurrence order. Update non-sensors through `collide_shapes`, match old/new points by semantic feature ID, and route sensors through overlap only. Keep occurrence ordinals and all contact storage private.
  </action>
  <verify><automated>cargo test -p liquidfun --test rigid_contacts lifecycle --all-features &amp;&amp; cargo test -p liquidfun --test rigid_contacts sensor --all-features</automated></verify>
  <acceptance_criteria>
    - Both test filters exit 0 for create/begin/persist/duplicate/feature/sensor cases and include a real contact witness proving old mixed material persists until recreation.
    - `rg -n "ContactFeatureId|collide_shapes|update_pairs|sqrt|max" crates/liquidfun/src/world/contact.rs crates/liquidfun/src/world/contact_manager.rs` finds the required wiring.
    - `! rg -n "pub (struct|enum|type).*Contact(Id|Handle)|pub fn .*contact_by" crates/liquidfun/src/world crates/liquidfun/src/lib.rs` exits 0.
  </acceptance_criteria>
  <done>The world automatically owns exact ordered contact creation and update state with no consumer contact identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Centralize refilter and destruction event ordering</name>
  <files>crates/liquidfun/src/world/contact_manager.rs, crates/liquidfun/src/world/object.rs, crates/liquidfun/src/world/step.rs, crates/liquidfun/tests/rigid_contacts.rs</files>
  <read_first>
    - crates/liquidfun/src/world/step.rs
    - crates/liquidfun/src/world/object.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2ContactManager.cpp
    - .planning/phases/03-rust-object-model-and-storage-architecture/03-03-SUMMARY.md
  </read_first>
  <behavior>
    - Filter flags are reevaluated during contact update and rejected/non-overlapping contacts use the same destruction path.
    - Deactivation, fixture destruction, and body cascade emit contact end before fixture/body destruction records.
    - Activation recreates proxies but does not create a contact until the next step.
  </behavior>
  <action>
Add one manager destruction transaction that unlinks both bodies/fixtures, records end evidence when touching, removes the occurrence, and returns owned semantic evidence before any dependent invalidation. Route refilter, separation, deactivate, fixture destroy, body cascade, and type-change teardown through it. Evolve StepReport internals to accumulate automatic transition/destruction evidence but retain the Phase 3 lock, poison, and post-unlock command rules.
  </action>
  <verify><automated>cargo test -p liquidfun --test rigid_contacts destruction --all-features &amp;&amp; cargo test -p liquidfun --test rigid_contacts filtering --all-features</automated></verify>
  <acceptance_criteria>
    - Both filters exit 0 and assert exact end-before-fixture-before-body ordering plus activation-next-step timing.
    - `rg -n "destroy_contact|remove_contact" crates/liquidfun/src/world/contact_manager.rs crates/liquidfun/src/world/object.rs` shows one authoritative removal seam.
    - A duplicate-event test proves multiplicity/order are preserved rather than deduplicated or hash-ordered.
  </acceptance_criteria>
  <done>Every contact removal cause uses one coherent ordered transaction and exposes owned evidence before handle invalidation.</done>
</task>

</tasks>

<verification>Run the full rigid_contacts target, existing hook/object targets, strict Clippy, and invariant scans for public contact identity and hash-derived ordering.</verification>
<success_criteria>Automatic contacts create, persist, filter, update, and destroy in pinned order, with sensor and lifecycle behavior observable through safe evidence.</success_criteria>
<output>After completion, create `.planning/phases/06-minimal-rigid-world-vertical-slice/06-04-SUMMARY.md`.</output>
