---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T08:10:00Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "15"
type: execute
wave: 10
depends_on: ["06-04", "06-13"]
files_modified:
  - crates/liquidfun/src/world/contact_manager.rs
  - crates/liquidfun/tests/rigid_contacts.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/witness_registry.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs
  - protocol/fixtures/accepted/rigid-world-request.jsonl
  - tools/reference/src/rigid_world_decode.hpp
  - reference/compatibility.json
  - COMPATIBILITY.md
  - tools/xtask/src/docs.rs
  - tools/xtask/tests/docs_contract.rs
autonomous: true
requirements: [RIGD-04]
gap_closure: true
must_haves:
  truths:
    - Contact admission rejects every fixture pair whose two owning bodies are both non-dynamic, including static/kinematic and kinematic/kinematic overlaps.
    - The declaration-first non-colliding oracle family contains overlapping non-dynamic fixtures and requires zero contacts, so a shared omission or erroneous admission fails before cross-engine equality can pass.
    - Contact-manager differential ledger claims cite the new branch witness rather than the earlier separated-body corpus.
  artifacts:
    - crates/liquidfun/src/world/contact_manager.rs
    - protocol/fixtures/accepted/rigid-world-request.jsonl
    - reference/compatibility.json
  key_links:
    - The native admission predicate mirrors pinned `b2Body::ShouldCollide`; the accepted request independently constrains both native and C++ traces to the declared zero-contact result.
---

# Plan 06-15: Non-Dynamic Contact Admission Evidence

<objective>
Close `non-dynamic-contact-admission` in production code, focused tests, declaration-first oracle evidence, and the compatibility ledger.
</objective>

<threat_model>

## Trust boundaries

| Threat | Boundary | Mitigation |
| --- | --- | --- |
| False contact creation | Broad-phase overlap enters private manager admission | Require at least one owning body to be `Dynamic` before filters or contact allocation. |
| Shared omission | Both engines can agree on zero contacts because fixtures never overlap | Author overlapping static/kinematic and kinematic/kinematic declarations with explicit zero-contact checkpoints/witnesses. |
| Evidence spoofing | Ledger claims validation from an insufficient corpus | Bind promoted rows and docs contracts to the updated fixture and named admission witnesses. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct admission and add focused overlap regressions</name>
  <files>crates/liquidfun/src/world/contact_manager.rs, crates/liquidfun/tests/rigid_contacts.rs</files>
  <read_first>
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-VERIFICATION.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-REVIEW.md
    - crates/liquidfun/src/world/contact_manager.rs
    - crates/liquidfun/tests/rigid_contacts.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2ContactManager.cpp
    - standards/core/testing.md
  </read_first>
  <action>
Change `pair_is_eligible` to return false when neither owning body has `BodyType::Dynamic`; keep same-body, active, duplicate, and fixture-filter gates unchanged. Add focused one-concern Arrange/Act/Assert tests with truly overlapping circle fixtures for static/kinematic and kinematic/kinematic pairs, step the world, and assert zero contacts and zero begin/pre-solve/post-solve evidence. Retain static/dynamic admission and the Phase 6 one-contact solver boundary.
  </action>
  <verify><automated>cargo test -p liquidfun --test rigid_contacts non_dynamic --all-features</automated></verify>
  <acceptance_criteria>
    - Both named non-dynamic overlap tests pass and a static/dynamic control still creates the existing supported contact.
    - `rg -n "BodyType::Dynamic" crates/liquidfun/src/world/contact_manager.rs` finds the positive dynamic admission predicate.
  </acceptance_criteria>
  <done>Rust contact creation matches the pinned no-dynamic-body predicate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the oracle corpus and ledger prove the missing branch</name>
  <files>crates/liquidfun-test-protocol/src/scenario/rigid_world/witness_registry.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs, protocol/fixtures/accepted/rigid-world-request.jsonl, tools/reference/src/rigid_world_decode.hpp, reference/compatibility.json, COMPATIBILITY.md, tools/xtask/src/docs.rs, tools/xtask/tests/docs_contract.rs</files>
  <read_first>
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-CONTEXT.md
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/witness_registry.rs
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs
    - protocol/fixtures/accepted/rigid-world-request.jsonl
    - tools/reference/src/rigid_world_decode.hpp
    - reference/compatibility.json
    - COMPATIBILITY.md
    - tools/xtask/src/docs.rs
    - tools/xtask/tests/docs_contract.rs
    - tools/reference/src/rigid_world.cpp
  </read_first>
  <action>
Extend `non_colliding_body_fixture_lifecycle`—do not add a third family—with overlapping static/kinematic and kinematic/kinematic fixture declarations/actions. After configuring the overlapping static/kinematic pair, execute an exact fixed `step` action with timestep bits `0x3c888889`, velocity iterations `8`, and position iterations `3`, then capture a checkpoint whose counts require zero contacts, zero manifold points, and zero contact events and whose required witness is exactly `static_kinematic_overlap_rejected`. Repeat the same exact fixed step and post-step zero-contact/manifold/event checkpoint for the overlapping kinematic/kinematic pair with witness exactly `kinematic_kinematic_overlap_rejected`. Update Rust completeness/deletion tests and the byte-stable accepted JSONL. Extend the C++ `required_witnesses` registry in `tools/reference/src/rigid_world_decode.hpp` with those same two exact names so C++ declaration validation cannot accept a weaker request. Run real debug and release rigid comparison to prove both engines satisfy both post-step declarations. Update the authoritative compatibility references for `b2ContactManager.h` and `subsystem.contacts-and-filtering`, regenerate `COMPATIBILITY.md`, and strengthen docs contracts so the differential claim requires the two exact witness names. Keep platform evidence false and broad Phase 7 solver rows pending.
  </action>
  <verify><automated>cargo test -p liquidfun-test-protocol rigid_world --all-features &amp;&amp; cargo xtask differential compare --scenario rigid-world --preset oracle-debug --session-profile one-shot &amp;&amp; cargo xtask inventory generate &amp;&amp; cargo xtask inventory check</automated></verify>
  <acceptance_criteria>
    - `rg -n "static_kinematic_overlap_rejected|kinematic_kinematic_overlap_rejected" crates/liquidfun-test-protocol/src/scenario/rigid_world/witness_registry.rs protocol/fixtures/accepted/rigid-world-request.jsonl tools/reference/src/rigid_world_decode.hpp` finds both exact witness names in Rust, the accepted request, and the C++ required-witness registry.
    - The accepted request contains one `0x3c888889`/8/3 step after the static/kinematic overlap and a separate `0x3c888889`/8/3 step after the kinematic/kinematic overlap; each immediately referenced checkpoint asserts `contacts: 0`, `manifold_points: 0`, and `events: 0` and carries only its matching rejection witness.
    - A fixture-mutation regression that removes either post-overlap step or changes either post-step checkpoint to permit one contact fails declaration-first validation, and the pre-fix Rust admission predicate fails the post-step expected counts.
    - Debug and release rigid compares report both required families matched; deletion of either admission witness fails declaration validation.
    - The two promoted ledger rows reference the updated accepted fixture and no platform row changes to evidenced.
  </acceptance_criteria>
  <done>The previously omitted admission branch is fail-closed differential evidence and the ledger claim is truthful.</done>
</task>

</tasks>

<verification>
Run both task checks, release compare, and docs/inventory checks. Then run `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` in that exact order, followed by `git diff --check`. Confirm Phase 7 forces, islands, sleeping, CCD, and queries remain untouched.
</verification>

<success_criteria>
Every non-dynamic pair is rejected before contact creation and the exact branch is covered by focused and declaration-first Rust/C++ evidence.
</success_criteria>

<output>After completion, create `06-15-SUMMARY.md` with the gap ID `non-dynamic-contact-admission` and updated ledger row IDs.</output>
