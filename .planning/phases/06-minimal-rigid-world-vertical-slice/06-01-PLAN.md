---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T02:43:09.000Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/world/body.rs
  - crates/liquidfun/src/world/fixture.rs
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/tests/rigid_definitions.rs
autonomous: true
requirements: [RIGD-01, RIGD-02]
must_haves:
  truths:
    - Consumers can construct reusable checked body definitions for all three body types without creating a world object.
    - Consumers can construct reusable fixture definitions that own immutable shape snapshots and checked material/filter state.
    - Invalid non-finite transforms, negative material values, and invalid centered inertia fail through typed errors before world mutation is possible.
  artifacts:
    - crates/liquidfun/src/world/body.rs
    - crates/liquidfun/src/world/fixture.rs
    - crates/liquidfun/tests/rigid_definitions.rs
  key_links:
    - BodyDef and FixtureDef are curated through world.rs and lib.rs while their fields remain private.
    - FixtureDef owns Shape by value and uses the existing FilterData contract rather than duplicating collision filtering.
---

# Plan 06-01: Checked Body and Fixture Contracts

<objective>
Define the invariant-bearing consumer inputs and snapshots that every later Phase 6 world operation will consume.

Purpose: Establish safe, reusable API contracts before storage, proxy, contact, or solver code depends on unchecked primitives.
Output: Checked body/fixture definitions, closed body type, typed validation errors, curated exports, and black-box tests.
</objective>

<decision_coverage>
D-01, D-03, and the definition-time portion of D-04 are fully implemented here. D-02 world authority and D-05 through D-20 are assigned to later plans.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Consumer values to physics domain | Floats, transforms, mass data, shapes, and filters become trusted definition state only after typed construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T06-01-01 | Tampering | BodyDef/FixtureDef constructors | mitigate | Reject non-finite and negative values without clamping; keep fields private after construction. |
| T06-01-02 | Denial of service | Shape ownership | mitigate | Accept only already bounded validated Shape values and clone owned topology without unbounded external storage. |
| T06-01-03 | Information disclosure | Public Debug/API | mitigate | Expose semantic values only; no raw handle, arena, proxy, or mutable-shape representation. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define checked body state contracts</name>
  <files>crates/liquidfun/src/world/body.rs, crates/liquidfun/src/world.rs, crates/liquidfun/src/lib.rs, crates/liquidfun/tests/rigid_definitions.rs</files>
  <read_first>
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.cpp
    - crates/liquidfun/src/world.rs
    - crates/liquidfun/src/world/object.rs
    - crates/liquidfun/src/math/transform.rs
    - crates/liquidfun/src/collision/types.rs
    - standards/core/architecture.md
    - standards/languages/rust.md
  </read_first>
  <behavior>
    - Static, kinematic, and dynamic BodyType values round-trip through BodyDef accessors.
    - Finite transforms and active state are retained exactly; non-finite transform components are rejected.
    - Checked custom mass data rejects negative mass or centered inertia and remains a distinct invariant-bearing value.
  </behavior>
  <action>
Create `world/body.rs` with the closed `BodyType`, private-field `BodyDef`, public semantic body snapshot/mass values, and non-exhaustive typed validation errors. Preserve accepted f32 bits and source expression order; reject invalid values rather than clamping. Keep Phase 7 force, damping, sleep, bullet, fixed-rotation, gravity-scale, and public velocity controls absent. Export only the consumer contract through `world.rs` and `lib.rs`, with rustdoc covering units and the Phase 6 boundary.
  </action>
  <verify>
    <automated>cargo test -p liquidfun --test rigid_definitions body --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub enum BodyType|pub struct BodyDef|pub enum BodyDefError" crates/liquidfun/src/world/body.rs` finds all three contracts.
    - `cargo test -p liquidfun --test rigid_definitions body --all-features` exits 0 and includes rejection witnesses for every non-finite transform lane and invalid centered inertia.
    - `! rg -n "force|torque|damping|gravity_scale|bullet|sleep|fixed_rotation|set_velocity" crates/liquidfun/src/world/body.rs` exits 0.
  </acceptance_criteria>
  <done>Body definitions are reusable, checked, semantically inspectable, and contain no Phase 7 consumer controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define immutable fixture and material contracts</name>
  <files>crates/liquidfun/src/world/fixture.rs, crates/liquidfun/src/world.rs, crates/liquidfun/src/lib.rs, crates/liquidfun/tests/rigid_definitions.rs</files>
  <read_first>
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Fixture.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Fixture.cpp
    - crates/liquidfun/src/collision/shape.rs
    - crates/liquidfun/src/collision/broad_phase.rs
    - crates/liquidfun/src/world/body.rs
    - crates/liquidfun/tests/collision_shapes.rs
  </read_first>
  <behavior>
    - FixtureDef owns a deep immutable Shape snapshot plus density, friction, restitution, sensor, and FilterData values.
    - Negative or non-finite density/friction/restitution is rejected without clamping.
    - Cloning or reusing a definition cannot create mutable shape aliasing.
  </behavior>
  <action>
Create `world/fixture.rs` with private-field `FixtureDef`, semantic fixture snapshot/material accessors, and typed validation errors. Store `Shape` by value, reuse `FilterData`, and expose no mutable topology or raw proxy state. Add focused black-box tests and compile-fail rustdoc for field mutation/raw storage access; curate explicit exports without wildcard re-exports.
  </action>
  <verify>
    <automated>cargo test -p liquidfun --test rigid_definitions fixture --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub struct FixtureDef|pub enum FixtureDefError" crates/liquidfun/src/world/fixture.rs` finds the checked contracts.
    - `cargo test -p liquidfun --test rigid_definitions fixture --all-features` and `cargo test -p liquidfun --doc --all-features` exit 0.
    - `! rg -n "pub .*shape:|shape_mut|ProxyId|&mut Shape" crates/liquidfun/src/world/fixture.rs crates/liquidfun/src/world.rs` exits 0.
  </acceptance_criteria>
  <done>Fixture definitions own validated immutable shapes and complete Phase 6 material/filter state without exposing storage.</done>
</task>

</tasks>

<verification>
Run the two focused test filters, warning-denied rustdoc, strict crate Clippy, and `git diff --check`.
</verification>

<success_criteria>
The checked definitions compile as a curated public contract, invalid input is rejected before any World exists, and no Phase 7 or raw-storage API leaks into the surface.
</success_criteria>

<output>After completion, create `.planning/phases/06-minimal-rigid-world-vertical-slice/06-01-SUMMARY.md`.</output>
