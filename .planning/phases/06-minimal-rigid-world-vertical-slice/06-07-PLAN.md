---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T02:43:09.000Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "07"
type: execute
wave: 4
depends_on: ["06-06"]
files_modified:
  - crates/liquidfun-test-protocol/src/tolerance.rs
  - crates/liquidfun-test-protocol/src/tolerance/rigid_policy.rs
  - crates/liquidfun-test-protocol/src/schema.rs
  - crates/liquidfun-test-protocol/src/schema/tests.rs
  - protocol/schemas/protocol-v1.schema.json
  - protocol/schemas/scenario-v1.schema.json
  - protocol/schemas/trace-v1.schema.json
  - protocol/tolerances/phase6-v1.toml
autonomous: true
requirements: [RIGD-01, RIGD-02, RIGD-04]
must_haves:
  truths:
    - Every rigid-world float path resolves to one explicit phase6-v1 policy and every structural/order path remains exact.
    - Wildcards, fallbacks, invalid thresholds, wrong horizons/tiers, and unclassified collections fail closed.
    - Protocol, scenario, and trace schemas describe the rigid records and render byte-stably from typed authority.
  artifacts:
    - crates/liquidfun-test-protocol/src/tolerance/rigid_policy.rs
    - protocol/tolerances/phase6-v1.toml
    - protocol/schemas/trace-v1.schema.json
  key_links:
    - Phase6PolicyProfile uses the existing FieldPolicy vocabulary and the semantic paths defined by Plan 06-06.
    - Schema renderers import the closed rigid-world record model and expose no filesystem write path in ordinary protocol builds.
---

# Plan 06-07: Rigid Policies and Schemas

<objective>
Classify every rigid-world observable with a closed numeric/order policy and deterministic schema presentation.
</objective>

<decision_coverage>
Completes D-19 numeric, horizon, tier, and collection policy. D-16 through D-18 remain authoritative from Plan 06-06; D-20 consumes these artifacts downstream.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Policy TOML to comparator authority | Untrusted paths and thresholds become trusted only after complete closed validation and hashing. |
| Typed records to schema presentations | Generated schema bytes must derive only from reviewed types and remain read-only during checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T06-07-01 | Tampering | phase6-v1 policy | mitigate | Require one explicit path per field, stable hash, finite thresholds, fixed horizon/tier, and no wildcard or widening. |
| T06-07-02 | Spoofing | Schema/version identity | mitigate | Keep protocol/scenario/trace version axes explicit and reject unknown record shapes/additional fields. |
| T06-07-03 | Repudiation | Generated bytes | mitigate | Render in memory, compare byte-for-byte, and prove repeated checks leave tracked files unchanged. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add phase6-v1 field policies and deterministic schemas</name>
  <files>crates/liquidfun-test-protocol/src/tolerance.rs, crates/liquidfun-test-protocol/src/tolerance/rigid_policy.rs, crates/liquidfun-test-protocol/src/schema.rs, crates/liquidfun-test-protocol/src/schema/tests.rs, protocol/schemas/protocol-v1.schema.json, protocol/schemas/scenario-v1.schema.json, protocol/schemas/trace-v1.schema.json, protocol/tolerances/phase6-v1.toml</files>
  <read_first>
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-06-PLAN.md
    - crates/liquidfun-test-protocol/src/scenario/rigid_world.rs
    - crates/liquidfun-test-protocol/src/tolerance/policy.rs
    - crates/liquidfun-test-protocol/src/tolerance/collision_policy.rs
    - protocol/tolerances/phase4-v1.toml
    - protocol/tolerances/phase5-v1.toml
    - crates/liquidfun-test-protocol/src/schema.rs
    - .planning/phases/04-math-settings-and-numerical-policy/04-CONTEXT.md
  </read_first>
  <behavior>
    - Every structural/order/count/feature/lifecycle path is exact and every float path resolves to one explicit policy.
    - Wildcards, fallback paths, invalid thresholds, wrong horizons/tiers, and unclassified collections are rejected.
    - Schema/policy rendering is byte-stable and read-only.
  </behavior>
  <action>
Create `Phase6PolicyProfile` using the existing FieldPolicy vocabulary with exact profile identity. Name explicit paths for transforms, mass/center/inertia, material, manifold geometry, and normal/tangent impulses; begin exact and permit only source/evidence-justified ULP or absolute-relative thresholds. Encode exact ordered versus declaration-ordered semantics, local horizons, zero/non-finite policy, and D1/D2 authority with no global epsilon or iteration widening. Extend all three schemas and byte-stability tests from typed renderers without adding write paths to ordinary protocol builds.
  </action>
  <verify><automated>cargo test -p liquidfun-test-protocol rigid_policy --all-features &amp;&amp; cargo test -p liquidfun-test-protocol schema --all-features</automated></verify>
  <acceptance_criteria>
    - Both commands exit 0 and negative tests reject missing, duplicate, wildcard, fallback, invalid-threshold, horizon, tier, and collection policies.
    - `! rg 'semantic_path = ".*(\*|default|fallback|any|all)' protocol/tolerances/phase6-v1.toml` exits 0.
    - Running schema/policy presentation tests twice leaves `git diff -- protocol/schemas protocol/tolerances` empty.
  </acceptance_criteria>
  <done>The rigid-world contract has exact-bit transport, complete field-specific policy, and deterministic schemas.</done>
</task>

</tasks>

<verification>Run rigid policy, schema, fixture decode, and byte-stability tests plus strict protocol Clippy and `git diff --check`.</verification>
<success_criteria>Every rigid result field is classified before adapter comparison, and every wire presentation is closed and byte-stable.</success_criteria>
<output>After completion, create `.planning/phases/06-minimal-rigid-world-vertical-slice/06-07-SUMMARY.md`.</output>
