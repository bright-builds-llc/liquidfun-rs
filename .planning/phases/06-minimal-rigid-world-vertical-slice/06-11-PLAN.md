---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T02:43:09.000Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "11"
type: execute
wave: 5
depends_on: ["06-10"]
files_modified:
  - tools/reference/adapter-inputs.txt
  - tools/reference/CMakeLists.txt
  - tools/reference/src/generate_build_identity.cmake.in
  - tools/reference/tests/protocol_tests.cpp
autonomous: true
requirements: [RIGD-01, RIGD-02, RIGD-04]
must_haves:
  truths:
    - Every result-affecting rigid C++ source/header participates in adapter identity.
    - The rigid translation unit compiles under the existing reviewed scalar warning and floating-point flags.
    - Compile-database identity fails closed when rigid units are missing, duplicated, or flag-divergent.
  artifacts:
    - tools/reference/adapter-inputs.txt
    - tools/reference/src/generate_build_identity.cmake.in
  key_links:
    - CMake compiles rigid_world.cpp in the existing liquidfun-reference target and identity generation hashes its command plus included inputs.
    - Xtask and CMake calculate the same fixed-source adapter digest before the child may report successful provenance.
---

# Plan 06-11: Rigid Oracle Build Identity

<objective>
Bind the new rigid adapter translation unit and headers into the existing reproducible C++ build and provenance identity.
</objective>

<decision_coverage>
Completes the oracle build/provenance portion of D-20 without changing the process boundary or evidence authority.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Repository sources to generated build identity | Missing or differently flagged result sources could spoof oracle provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T06-11-01 | Spoofing | Adapter digest | mitigate | Hash every reviewed rigid source/header and independently verify expected repository-relative inputs. |
| T06-11-02 | Tampering | Compile commands | mitigate | Require exact translation-unit set and canonical scalar flags; reject missing, duplicate, or divergent commands. |
| T06-11-03 | Repudiation | Generated identity | mitigate | Mutation tests prove `.cpp` and included `.hpp` changes alter the reported digest. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Bind rigid adapter sources into reproducible build identity</name>
  <files>tools/reference/adapter-inputs.txt, tools/reference/CMakeLists.txt, tools/reference/src/generate_build_identity.cmake.in, tools/reference/tests/protocol_tests.cpp</files>
  <read_first>
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-10-PLAN.md
    - tools/reference/adapter-inputs.txt
    - tools/reference/CMakeLists.txt
    - tools/reference/src/generate_build_identity.cmake.in
    - tools/xtask/src/upstream.rs
    - .planning/phases/05-shapes-and-collision-foundation/05-07-SUMMARY.md
  </read_first>
  <action>
Add every result-affecting rigid source/header to the adapter-input digest and compile `rigid_world.cpp` as a named translation unit under the existing scalar warning/FP rules. Update compile-database identity expectations to require the rigid translation unit alongside the existing result units; do not relax flags or hash generated output. Add mutation tests proving rigid source changes alter adapter/build identity.
  </action>
  <verify><automated>cargo test -p xtask rigid_compile_database_identity --all-features &amp;&amp; cargo xtask upstream configure --preset oracle-debug &amp;&amp; cargo xtask upstream build --preset oracle-debug</automated></verify>
  <acceptance_criteria>
    - The automated command exits 0 and generated compile identity includes exactly the reviewed result translation units.
    - `rg -n "rigid_world\.(cpp|hpp)|rigid_world_trace\.hpp" tools/reference/adapter-inputs.txt tools/reference/CMakeLists.txt` finds every new input.
    - Mutation tests prove both a `.cpp` and included `.hpp` change alter adapter identity.
  </acceptance_criteria>
  <done>The rigid oracle is reproducibly built, content-addressed, and bound to the existing provenance contract.</done>
</task>

</tasks>

<verification>Run compile-identity positive/negative tests, debug configure/build, strict CTest, and adapter-input completeness checks.</verification>
<success_criteria>No rigid result source can change without changing the child build identity, and all units use the reviewed scalar flags.</success_criteria>
<output>After completion, create `.planning/phases/06-minimal-rigid-world-vertical-slice/06-11-SUMMARY.md`.</output>
