---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T02:43:09.000Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "10"
type: execute
wave: 4
depends_on: ["06-06"]
files_modified:
  - tools/reference/src/main.cpp
  - tools/reference/src/protocol.hpp
  - tools/reference/src/protocol.cpp
  - tools/reference/src/rigid_world.hpp
  - tools/reference/src/rigid_world.cpp
  - tools/reference/src/rigid_world_trace.hpp
  - tools/reference/tests/protocol_tests.cpp
autonomous: true
requirements: [RIGD-01, RIGD-02, RIGD-04]
must_haves:
  truths:
    - The pinned C++ adapter executes the same two validated timeline families through one fresh b2World per request.
    - C++ output contains semantic IDs, ordered active state, exact float bits, lifecycle evidence, and reset proof but no pointers/layout.
    - Strict duplicate/unknown/bounds errors occur before any b2World effect and stdout remains protocol-only.
  artifacts:
    - tools/reference/src/rigid_world.cpp
    - tools/reference/src/rigid_world_trace.hpp
    - tools/reference/tests/protocol_tests.cpp
  key_links:
    - protocol.cpp strict decoding feeds rigid_world.cpp only validated domain records.
    - rigid_world.cpp uses pinned b2Body/b2Fixture/b2World/contact APIs and protocol_bits exact transport.
---

# Plan 06-10: Pinned C++ Rigid-World Adapter

<objective>
Mirror the closed timeline contract in the existing process-isolated oracle with strict parsing, semantic tracing, and reset proof.
</objective>

<decision_coverage>
D-16 through D-19 are implemented on the C++ execution side; both D-17 witness families run without a second oracle path or FFI. D-20 build identity completes in Plan 06-11.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| JSONL bytes to C++ objects | Duplicate-aware bounded decoding must finish before upstream allocation or mutation. |
| Upstream memory to semantic trace | Pointers, list nodes, padding, and private layout must be mapped to explicit IDs/order only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T06-10-01 | Elevation of privilege | C++ dispatch | mitigate | Closed record/action enums and no filesystem, command, dynamic-code, or caller-selected target paths. |
| T06-10-02 | Information disclosure | Trace/stderr | mitigate | Protocol-only stdout, bounded stderr, semantic IDs only, no addresses/environment. |
| T06-10-03 | Denial of service | Native request | mitigate | Enforce protocol aggregate bounds before world creation; sanitizer-compatible fail-fast exceptions. |
| T06-10-04 | Spoofing | Reset identity | mitigate | Use one fresh world/mapping scope per request and require monotonic reset epoch after complete destruction. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strictly decode and execute rigid timelines in pinned C++</name>
  <files>tools/reference/src/protocol.hpp, tools/reference/src/protocol.cpp, tools/reference/src/rigid_world.hpp, tools/reference/src/rigid_world.cpp, tools/reference/src/rigid_world_trace.hpp, tools/reference/src/main.cpp, tools/reference/tests/protocol_tests.cpp</files>
  <read_first>
    - tools/reference/src/protocol.hpp
    - tools/reference/src/protocol.cpp
    - tools/reference/src/collision_probe.cpp
    - tools/reference/src/oracle_adapter.cpp
    - tools/reference/src/protocol_bits.cpp
    - crates/liquidfun-test-protocol/src/scenario/rigid_world.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Fixture.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Contacts/b2Contact.cpp
  </read_first>
  <behavior>
    - Both fixed requests decode and emit ordered complete semantic traces with exact reset epochs.
    - Duplicate/unknown/oversized/out-of-order actions fail before world construction.
    - Contact/material/manifold/impulse/event/destruction output omits pointers, packed keys, inactive payload, and wall clocks.
  </behavior>
  <action>
Extend the existing duplicate-aware SAX protocol with rigid-world records and bounded typed C++ domain values. Execute declarations/actions through pinned APIs with semantic ID maps, explicit contact occurrence ordinals, source list order, active manifold features, material/impulses, lifecycle events, and destruction snapshots. Split trace encoding from execution into the named files to keep foreign logic reviewable. Destroy the b2World and all maps before emitting terminal reset proof; catch exceptions at the process boundary and keep stdout protocol-only.
  </action>
  <verify><automated>cmake --build --preset oracle-debug --target liquidfun-reference-tests &amp;&amp; ctest --preset oracle-debug --output-on-failure</automated></verify>
  <acceptance_criteria>
    - CTest exits 0 for both accepted families and all strict rejection/reset/ordering cases.
    - `! rg -n "reinterpret_cast|0x.*pointer|uintptr|ContactID|\.key|std::filesystem|system\(" tools/reference/src/rigid_world*` exits 0.
    - `rg -n "non_colliding_body_fixture_lifecycle|single_contact_lifecycle" tools/reference/tests/protocol_tests.cpp` finds both complete oracle witnesses.
  </acceptance_criteria>
  <done>The existing oracle process executes the complete fixed rigid timeline with semantic, bounded, reset-proven output.</done>
</task>

</tasks>

<verification>Run debug CTest, strict C++ compilation through the existing target, and forbidden pointer/path/output scans.</verification>
<success_criteria>The pinned subprocess accepts and emits the complete rigid contract with exact reset proof and no new runtime boundary.</success_criteria>
<output>After completion, create `.planning/phases/06-minimal-rigid-world-vertical-slice/06-10-SUMMARY.md`.</output>
