---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T08:10:00Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "18"
type: execute
wave: 13
depends_on: ["06-14", "06-17"]
files_modified:
  - .github/workflows/oracle.yml
  - tools/xtask/tests/differential_cli.rs
  - tools/xtask/src/docs.rs
  - tools/xtask/tests/docs_contract.rs
  - TESTING.md
  - ARCHITECTURE.md
  - README.md
autonomous: true
requirements: [RIGD-02, RIGD-04]
gap_closure: true
must_haves:
  truths:
    - The scheduled ASan/UBSan job executes the rigid reference protocol tests and a fail-fast `rigid-world` comparison under the sanitizer preset before its read-only assertion.
    - Any sanitizer/protocol/rigid failure terminates the job and uploads only the existing bounded `target/differential/failures` artifact with seven-day retention and fail-if-missing policy.
    - All seven Phase 6 verifier gaps have direct focused evidence, full Rust gates pass, debug/release/replay/D0 pass, and formal re-verification can mark RIGD-02 and RIGD-04 verified without widening Phase 7 scope.
  artifacts:
    - .github/workflows/oracle.yml
    - TESTING.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-VERIFICATION.md
  key_links:
    - CI executes the same rigid command and C++ protocol target used locally; final docs and verifier evidence cite exact commands rather than inferred compilation coverage.
---

# Plan 06-18: Sanitizer Execution and Phase 6 Re-Signoff

<objective>
Close `rigid-sanitizer-not-executed`, consolidate all gap evidence, and run the final goal-backward Phase 6 verification without overstating authority.
</objective>

<threat_model>

## Trust boundaries

| Threat | Boundary | Mitigation |
| --- | --- | --- |
| Compile-only sanitizer claim | Rigid C++ code builds but is never executed | Run CTest/reference protocol tests and the rigid comparison under fail-fast ASan/UBSan options. |
| Unbounded diagnostics | Native failures upload arbitrary workspace data | Reuse only bounded failure bundles, fixed artifact path, seven-day retention, and fail-if-missing behavior. |
| False sign-off | Passing fixed cases obscure remaining source contradictions | Re-run every gap-specific assertion plus formal goal-backward verification and require zero unresolved review findings. |
| Scope creep | Gap work becomes Phase 7 solver/configuration | Docs/tests continue to reject forces, general islands, sleeping, CCD, queries, and public step configuration claims. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute rigid protocol and comparison under ASan/UBSan</name>
  <files>.github/workflows/oracle.yml, tools/xtask/tests/differential_cli.rs, tools/xtask/src/docs.rs, tools/xtask/tests/docs_contract.rs, TESTING.md</files>
  <read_first>
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-VERIFICATION.md
    - .github/workflows/oracle.yml
    - tools/xtask/tests/differential_cli.rs
    - tools/xtask/src/docs.rs
    - tools/xtask/tests/docs_contract.rs
    - TESTING.md
    - tools/reference/CMakePresets.json
    - tools/reference/CMakeLists.txt
    - tools/reference/tests/protocol_tests.cpp
    - standards/core/verification.md
  </read_first>
  <action>
After configuring/building `oracle-asan-ubsan`, run `liquidfun-reference-protocol-tests`/CTest from `target/reference/oracle-asan-ubsan` with `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1` and `ASAN_OPTIONS=abort_on_error=1:halt_on_error=1`, then run `cargo xtask differential compare --scenario rigid-world --preset oracle-asan-ubsan --session-profile one-shot` under the same options before the read-only assertion. Preserve empty-world sanitizer/reuse coverage. Keep the existing failure-only upload restricted to `target/differential/failures`, `if-no-files-found: error`, and seven-day retention; ensure rigid harness failures persist bounded bundles there. Add workflow/docs tests requiring exact order, fail-fast environment, rigid command, CTest, bounded path, and retention.
  </action>
  <verify><automated>cargo test -p xtask --test differential_cli sanitizer_rigid --all-features &amp;&amp; cargo test -p xtask --test docs_contract oracle_workflow --all-features</automated></verify>
  <acceptance_criteria>
    - `rg -n "ctest.*oracle-asan-ubsan|compare --scenario rigid-world --preset oracle-asan-ubsan|target/differential/failures|retention-days: 7" .github/workflows/oracle.yml` finds all execution and artifact controls.
    - Workflow-order tests prove protocol tests and rigid comparison occur after sanitizer build and before read-only assertion.
    - No `continue-on-error`, swallowed status, unbounded workspace upload, or sanitizer retry is introduced.
  </acceptance_criteria>
  <done>The complete Phase 6 C++ boundary actually executes under fail-fast sanitizers with bounded failure evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Run final gap closure and formal goal verification</name>
  <files>tools/xtask/src/docs.rs, tools/xtask/tests/docs_contract.rs, TESTING.md, ARCHITECTURE.md, README.md</files>
  <read_first>
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-VERIFICATION.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-REVIEW.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-14-SUMMARY.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-15-SUMMARY.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-16-SUMMARY.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-17-SUMMARY.md
    - tools/xtask/src/docs.rs
    - tools/xtask/tests/docs_contract.rs
    - TESTING.md
    - ARCHITECTURE.md
    - README.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
Document the exact fixed step/action/mass boundary, non-dynamic overlap witness, aggregate atomicity guarantee, real D1-only fixture lifecycle, and sanitizer execution. Strengthen docs contracts against regression of all seven gap IDs while retaining local D2/D0 labels and Phase 7 deferrals. Run focused tests for each gap; debug and release compare; debug replay; exact two-run D0; docs/inventory/provenance/package checks; warning-denied rustdoc; and the mandatory Rust sequence. Then hand the completed artifacts to the GSD formal verifier to regenerate `06-VERIFICATION.md` goal-backward; require roadmap criteria 4/4, RIGD-01 still verified, RIGD-02/RIGD-04 verified, seven original gaps closed, repository completion gate passed, and no human-verification dependency. Do not manually claim canonical D1 execution unless the pinned lane actually produced it.
  </action>
  <verify><automated>cargo xtask docs check &amp;&amp; cargo xtask inventory check &amp;&amp; cargo xtask provenance check &amp;&amp; cargo xtask package verify &amp;&amp; cargo xtask differential compare --scenario rigid-world --preset oracle-debug --session-profile one-shot &amp;&amp; cargo xtask differential compare --scenario rigid-world --preset oracle-release --session-profile one-shot &amp;&amp; cargo xtask differential replay --scenario rigid-world --preset oracle-debug --session-profile one-shot &amp;&amp; cargo xtask differential verify-determinism --scenario rigid-world --preset oracle-debug --runs 2</automated></verify>
  <acceptance_criteria>
    - Every automated command exits 0, followed by `cargo fmt --all`, strict Clippy, all-target build, and all-feature tests in exact order.
    - `git diff --check`, Markdown checks, unsafe/raw-identity/absolute-path scans, and Phase 7 overclaim scans pass.
    - Formal GSD re-verification reports `status: passed`, RIGD-02 and RIGD-04 verified, and none of the seven original gap IDs unresolved.
  </acceptance_criteria>
  <done>Phase 6 is re-verified from code and executed evidence, with all seven gaps closed and the next-phase boundary intact.</done>
</task>

</tasks>

<verification>
Run task checks, the exact Rust pre-commit sequence, all fixed rigid workflows, repo-owned aggregate checks, and formal GSD verification after the last file change. Review the full diff for generated drift and false D1/platform or Phase 7 claims.
</verification>

<success_criteria>
ASan/UBSan executes rigid decode/world/contact/teardown/trace paths, bounded failure artifacts remain enforced, and the formal verifier turns the Phase 6 report green with RIGD-01/02/04 satisfied.
</success_criteria>

<output>After completion, create `06-18-SUMMARY.md`; the execution workflow then regenerates `06-VERIFICATION.md` through the formal verifier rather than hand-editing it.</output>
