---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T02:43:09.000Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "05"
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/world/body.rs
  - crates/liquidfun/src/world/contact.rs
  - crates/liquidfun/src/world/contact_manager.rs
  - crates/liquidfun/src/world/contact_solver.rs
  - crates/liquidfun/src/world/step.rs
  - crates/liquidfun/tests/rigid_contact_solver.rs
  - crates/liquidfun/tests/hook_contract.rs
autonomous: true
requirements: [RIGD-04]
must_haves:
  truths:
    - One active static/dynamic contact with up to two points consumes and writes finite normal/tangent impulses in pinned order.
    - Persistent feature IDs carry warm-start impulses; new, recreated, and sensor points start at exact zero.
    - World stepping discovers, updates, solves, reports, unlocks, and applies deferred commands in the documented Phase 6 order.
    - Unsupported multi-contact, dynamic/dynamic, joint, sleeping, or CCD solver topology fails before any solver-state, body-velocity, or impulse write-back; already committed contact lifecycle updates remain coherent and observable.
  artifacts:
    - crates/liquidfun/src/world/contact_solver.rs
    - crates/liquidfun/tests/rigid_contact_solver.rs
  key_links:
    - Solver consumes private contact manifolds/world manifolds and internal body velocity/sweep state, then writes impulses back by feature ID.
    - Enriched ContactView and StepReport are generated from manager state without exposing references or durable IDs.
---

# Plan 06-05: One-Contact Solve and Step Reports

<objective>
Complete the Phase 6 world step with one deterministic discrete static/dynamic contact solve, warm-start persistence, restricted hooks, and owned ordered reports.
</objective>

<decision_coverage>
D-12 and D-14 are fully implemented, and D-09/D-10/D-13 are protected at the solver/report boundary. Phase 7 solver families remain explicit errors or absent controls.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Contact state to numerical solver | Only finite supported static/dynamic manifolds enter the fixed-capacity constraint kernel. |
| Locked step to application commands | Hooks see read-only transient views; commands remain owned, bounded, and applied only after RAII unlock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T06-05-01 | Tampering | Warm-start cache | mitigate | Match only semantic feature IDs, zero new points, validate finite write-back, and test recreation resets. |
| T06-05-02 | Denial of service | Solver topology | mitigate | Fixed two-point capacity and fail-closed rejection of more than one supported solving contact. |
| T06-05-03 | Elevation of privilege | StepHook | mitigate | Preserve borrow-scoped views, no &mut World, bounded one-command-per-occurrence, and poison on panic. |
| T06-05-04 | Repudiation | Phase ordering | mitigate | Encode phase/event kinds in StepReport and assert exact occurrence order in integration tests. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Solve and persist one bounded static/dynamic contact</name>
  <files>crates/liquidfun/src/world/body.rs, crates/liquidfun/src/world/contact.rs, crates/liquidfun/src/world/contact_solver.rs, crates/liquidfun/src/world/contact_manager.rs, crates/liquidfun/tests/rigid_contact_solver.rs</files>
  <read_first>
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Contacts/b2ContactSolver.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/Contacts/b2ContactSolver.h
    - crates/liquidfun/src/collision/narrow.rs
    - crates/liquidfun/src/world/contact.rs
    - crates/liquidfun/src/world/body.rs
    - crates/liquidfun/src/math/settings.rs
  </read_first>
  <behavior>
    - Cold contact impulses start at zero, are consumed normal-then-tangent in source order, and finite results write back to matching points.
    - Persistent points reuse prior normal/tangent impulses by ContactFeatureId; removed/new/recreated points do not inherit them.
    - Sensors bypass constraint creation; unsupported solver topology returns a typed StepError before body velocity or contact impulse mutation, without rolling back coherent lifecycle discovery/update state.
  </behavior>
  <action>
Create a fixed-capacity private constraint kernel for exactly one touching non-sensor static/dynamic contact with up to two points. Preserve pinned initialization, warm-start scaling/application, normal then tangent point order, friction clamping, restitution branch, and impulse write-back. Use the closed Phase 6 witness timestep bit pattern and one reviewed solve pass as an explicit internal slice contract; do not expose timestep/iteration/force/velocity configuration publicly. Preflight solver eligibility after coherent contact discovery/update but before constructing or applying constraints. On unsupported topology or numeric failure, retain the lifecycle result while leaving pre-step body velocities and all contact impulses bit-identical.
  </action>
  <verify><automated>cargo test -p liquidfun --test rigid_contact_solver --all-features</automated></verify>
  <acceptance_criteria>
    - The complete solver target exits 0 for cold, persistent, feature-change, recreated, sensor, two-point, friction/restitution, and unsupported-topology cases.
    - An explicit pre/post-state test proves unsupported topology may add/update lifecycle contacts but leaves body velocity state and every pre-existing/new contact impulse bit-identical at zero or its pre-step value.
    - `rg -n "ContactFeatureId|normal_impulse|tangent_impulse" crates/liquidfun/src/world/contact.rs crates/liquidfun/src/world/contact_solver.rs` finds semantic carry/write-back.
    - `! rg -n "pub .*velocity|pub .*impulse|pub .*iteration|pub .*time_step" crates/liquidfun/src/world/body.rs crates/liquidfun/src/world/contact_solver.rs crates/liquidfun/src/world.rs` exits 0.
  </acceptance_criteria>
  <done>The bounded solver witness consumes, carries, and refreshes impulses without claiming the Phase 7 island solver.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrate automatic stepping and enrich safe reports</name>
  <files>crates/liquidfun/src/world/step.rs, crates/liquidfun/src/world.rs, crates/liquidfun/tests/rigid_contact_solver.rs, crates/liquidfun/tests/hook_contract.rs</files>
  <read_first>
    - crates/liquidfun/src/world/step.rs
    - crates/liquidfun/tests/hook_contract.rs
    - crates/liquidfun/src/world/contact_manager.rs
    - .planning/phases/03-rust-object-model-and-storage-architecture/03-CONTEXT.md
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2WorldCallbacks.h
  </read_first>
  <behavior>
    - `World::step` no longer accepts caller-supplied contacts and instead runs find-pairs, update/refilter, hooks, solve, unlock, commands.
    - ContactView exposes semantic fixture/manifold/material/warm-start state read-only; StepReport owns begin/persist/end, solve, command, and destruction evidence.
    - Hook panic restores lock, discards commands, poisons coherent operations, and resumes the original panic.
  </behavior>
  <action>
Replace the representative contact slice argument with manager-owned iteration. Split the phase order into named helpers, invoke pre-solve only for awake supported non-sensor touching contacts, and retain filtered/sensor timing. Evolve ContactView and StepReport with bounded owned semantic event variants and snapshots while preserving no durable contact identity, exact multiplicity/order, RAII lock restoration, post-unlock sequential command application, and persistent poison behavior. Update compile-fail and runtime hook tests.
  </action>
  <verify><automated>cargo test -p liquidfun --test hook_contract --all-features &amp;&amp; cargo test -p liquidfun --test rigid_contact_solver step_order --all-features</automated></verify>
  <acceptance_criteria>
    - Both commands exit 0 and include exact phase order, sensor no-pre-solve, duplicate occurrence, deferred command, stale command, and panic poison witnesses.
    - `rg -n "find.*pairs|update.*contacts|solve.*contact|apply_commands" crates/liquidfun/src/world/step.rs` finds named step seams in source order.
    - `! rg -n "contacts: &\[ContactSnapshot\]|pub .*Contact(Id|Handle)" crates/liquidfun/src/world/step.rs crates/liquidfun/src/lib.rs` exits 0.
  </acceptance_criteria>
  <done>World stepping owns the complete Phase 6 lifecycle and emits safe ordered evidence without weakening Phase 3 hook rules.</done>
</task>

</tasks>

<verification>Run solver, contact, hook, and object targets; strict Clippy; warning-denied docs; and scan for Phase 7 controls or public durable contact identity.</verification>
<success_criteria>A non-colliding world and one supported colliding world step execute end to end inside native World with finite warm-start state and exact owned reports.</success_criteria>
<output>After completion, create `.planning/phases/06-minimal-rigid-world-vertical-slice/06-05-SUMMARY.md`.</output>
