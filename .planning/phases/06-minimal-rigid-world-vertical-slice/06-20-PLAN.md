---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T20:20:38Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "20"
type: execute
wave: 15
depends_on: ["06-19"]
files_modified:
  - crates/liquidfun/src/world/body.rs
  - crates/liquidfun/tests/rigid_definitions.rs
  - crates/liquidfun/tests/fixture_dynamics.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs
  - crates/liquidfun-test-protocol/tests/fixtures.rs
  - protocol/fixtures/rejected/rigid-world-zero-centered-inertia.jsonl
  - crates/liquidfun-differential/src/rigid_world.rs
  - crates/liquidfun-differential/tests/rigid_world.rs
  - tools/reference/src/rigid_world_decode.hpp
  - tools/reference/tests/protocol_tests.cpp
autonomous: true
requirements: [RIGD-02, RIGD-04]
gap_closure: true
must_haves:
  truths:
    - BodyMassData accepts origin inertia zero through the pinned no-inertia branch, but when origin inertia is positive it requires the source-ordered centered inertia to be finite and strictly positive.
    - Rust protocol validation, the native executor defense, and C++ decode reject the exact equality boundary before either engine mutates a world or reaches the pinned debug assertion/divide-by-zero path.
    - A checked-in request differing from the accepted rigid request only at custom mass `(1, (1,0), 1)` fails with stable Rust InvalidGeometry and matching C++ centered-inertia rejection.
    - Existing negative/non-finite rejections and real debug/release comparison plus replay remain green without widening Phase 6 solver configuration.
  artifacts:
    - crates/liquidfun/src/world/body.rs
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs
    - protocol/fixtures/rejected/rigid-world-zero-centered-inertia.jsonl
    - tools/reference/src/rigid_world_decode.hpp
  key_links:
    - BodyMassData::new and validate_custom_mass use the same origin-inertia branch and source-ordered dot, product, subtraction, finite, and strict-positive rules.
    - The C++ decoder rejects equality before constructing SetCustomMassData, so b2Body::SetMassData never sees the unsafe boundary.
    - The native executor still reconstructs BodyMassData, providing a defense even if a typed request were constructed outside JSON decoding.
---

# Plan 06-20: Zero Centered-Inertia Boundary

<objective>
Close `zero-centered-inertia-boundary` with one branch-faithful invariant across the public Rust type, protocol decoder, native defense, and C++ oracle decoder.
</objective>

<threat_model>

## Trust boundaries

| Threat | Boundary | Mitigation |
| --- | --- | --- |
| Native assertion or divide by zero | Positive origin inertia becomes exactly zero after the parallel-axis shift | Require strictly positive centered inertia before BodyMassData construction or C++ action construction. |
| Over-rejection | Origin inertia zero is incorrectly treated like the positive-inertia branch | Preserve the pinned zero-origin no-inertia branch and prove it with dedicated Rust and C++ controls. |
| Cross-language parser drift | Rust rejects equality but C++ accepts it, or vice versa | Use the same source order and matched equality/negative/non-finite tests plus one checked-in malformed JSONL request. |
| Schema overclaim | JSON Schema appears to prove a relational IEEE-754 bit invariant it cannot express | Keep schemas structural and byte-stable; document in tests that typed decoding is authoritative for the relational check. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce the branch-faithful Rust domain and protocol invariant</name>
  <files>crates/liquidfun/src/world/body.rs, crates/liquidfun/tests/rigid_definitions.rs, crates/liquidfun/tests/fixture_dynamics.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs, crates/liquidfun-test-protocol/tests/fixtures.rs, protocol/fixtures/rejected/rigid-world-zero-centered-inertia.jsonl, crates/liquidfun-differential/src/rigid_world.rs, crates/liquidfun-differential/tests/rigid_world.rs</files>
  <read_first>
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
    - standards/core/architecture.md
    - standards/core/testing.md
    - standards/core/verification.md
    - standards/languages/rust.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-CONTEXT.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-VERIFICATION.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-REVIEW.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-16-PLAN.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-16-SUMMARY.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-19-SUMMARY.md
    - crates/liquidfun/src/world/body.rs
    - crates/liquidfun/tests/rigid_definitions.rs
    - crates/liquidfun/tests/fixture_dynamics.rs
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs
    - crates/liquidfun-test-protocol/tests/fixtures.rs
    - protocol/fixtures/accepted/rigid-world-request.jsonl
    - protocol/fixtures/rejected/rigid-world-negative-centered-inertia.jsonl
    - crates/liquidfun-differential/src/rigid_world.rs
    - crates/liquidfun-differential/tests/rigid_world.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.cpp
  </read_first>
  <action>
Use `apply_patch`. In `BodyMassData::new`, keep input finiteness and negative-mass checks, but branch on the original `rotational_inertia`: if it is exactly zero, store centered inertia zero without performing or requiring a positive parallel-axis result; if it is positive, compute effective mass, `center.x * center.x`, `center.y * center.y`, their source-ordered sum, the mass product, and the subtraction with explicit finite checks, then reject `centered_rotational_inertia <= 0.0`. Rename `BodyMassDataError::NegativeCenteredRotationalInertia` to the truthful `NonPositiveCenteredRotationalInertia`, update Display/rustdoc, and update downstream matches. Preserve exact accepted bits and the effective-unit-mass rule when authored mass is zero.

Mirror the same original-inertia branch and strict-positive rule in protocol `validate_custom_mass`; keep `RigidWorldErrorKind::InvalidGeometry`. Create `protocol/fixtures/rejected/rigid-world-zero-centered-inertia.jsonl` by copying the accepted request and changing only `nc-custom-mass` to mass `1.0`, center `(1.0, 0.0)`, and origin inertia `1.0`; preserve the final newline. Add separate Arrange/Act/Assert tests for public equality rejection, origin-inertia-zero acceptance with nonzero center, protocol equality rejection, checked-in fixture classification, and all earlier negative/non-finite cases. Keep the schema presentations byte-identical because cross-field f32 bit arithmetic is not expressible by the current JSON Schema; add an assertion/comment at the schema-facing test seam that strict relational validity belongs to typed decode.

Retain the native defense in `execute_action`: it must continue reconstructing `BodyMassData` before `World::set_body_mass_data`; add a direct typed-request/native test proving equality fails before the world action even if JSON decode is bypassed. Do not use `unwrap()`. Before committing this task, run these exact commands in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Commit only if all four pass.
  </action>
  <verify>
    <automated>cargo test -p liquidfun --test rigid_definitions centered_inertia --all-features &amp;&amp; cargo test -p liquidfun-test-protocol rigid_world --all-features &amp;&amp; cargo test -p liquidfun-test-protocol --test fixtures rigid_world_zero_centered_inertia --all-features &amp;&amp; cargo test -p liquidfun-differential --test rigid_world centered_inertia --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "NonPositiveCenteredRotationalInertia|centered_rotational_inertia <= 0.0" crates/liquidfun/src/world/body.rs` finds the public strict-positive branch.
    - The public tests separately prove `(1, (1,0), 1)` rejects and positive mass with nonzero center plus origin inertia zero accepts the no-inertia branch.
    - `cargo test -p liquidfun-test-protocol --test fixtures rigid_world_zero_centered_inertia --all-features` returns exactly `Some(RigidWorldErrorKind::InvalidGeometry)`.
    - Schema byte-stability tests pass without adding a misleading relational schema claim.
    - A native typed-request regression proves the equality boundary cannot reach world mutation even if JSON decoding is bypassed.
  </acceptance_criteria>
  <done>The Rust public, protocol, and native boundaries reject zero centered inertia exactly when the pinned positive-origin branch requires strict positivity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject equality at the C++ boundary and prove real workflows</name>
  <files>tools/reference/src/rigid_world_decode.hpp, tools/reference/tests/protocol_tests.cpp</files>
  <read_first>
    - tools/reference/src/rigid_world_decode.hpp
    - tools/reference/tests/protocol_tests.cpp
    - crates/liquidfun/src/world/body.rs
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs
    - protocol/fixtures/rejected/rigid-world-zero-centered-inertia.jsonl
    - protocol/schemas/scenario-v1.schema.json
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.cpp
  </read_first>
  <action>
Use `apply_patch`. In the C++ custom-mass decoder, retain finite/nonnegative input checks, branch on the decoded origin inertia before parallel-axis validation, allow exact origin inertia zero through the no-inertia path, and for positive origin inertia compute center dot, product, and subtraction in the same source order as Rust and reject non-finite values or `centered_inertia <= 0.0F` before returning `SetCustomMassData`. Keep the diagnostic stable and bounded with the phrase `centered inertia` and no raw request dump.

Extend `protocol_tests.cpp` with one-concern controls for equality rejection, origin-inertia-zero acceptance, existing negative/non-finite rejection, and decode-before-execution behavior using the checked-in equality fixture where practical. Freshly configure and build the debug and release targets after the adapter change, run CTest, then run real rigid debug/release compare and debug replay to prove valid corpus behavior remains unchanged and no debug assertion/divide-by-zero occurs. Before committing this task, run these exact commands in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Commit only if all four pass.
  </action>
  <verify>
    <automated>cargo xtask upstream configure --preset oracle-debug &amp;&amp; cargo xtask upstream build --preset oracle-debug &amp;&amp; cmake --build target/reference/oracle-debug --target liquidfun-reference-protocol-tests &amp;&amp; ctest --test-dir target/reference/oracle-debug --output-on-failure &amp;&amp; cargo xtask differential compare --scenario rigid-world --preset oracle-debug --session-profile one-shot</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "centered_inertia <= 0\.0F|centered inertia" tools/reference/src/rigid_world_decode.hpp tools/reference/tests/protocol_tests.cpp` finds the strict C++ boundary and stable diagnostic test.
    - CTest proves equality rejects before adapter execution and origin inertia zero remains accepted.
    - Debug and release compare plus debug replay match both required families with truthful local authority labels.
    - No debug assertion, signal, non-finite inverse inertia, or unbounded diagnostic is observed.
  </acceptance_criteria>
  <done>Rust and C++ admit the same branch-faithful custom mass domain and malformed equality cannot reach either world.</done>
</task>

</tasks>

<verification>
Run both focused task suites, fresh C++ protocol tests, debug/release rigid comparison, debug replay, schema byte-stability, the exact four-command Rust gate in order, warning-denied rustdoc, and `git diff --check`. Do not modify GSD reports or run `mdformat` on GSD artifacts.
</verification>

<success_criteria>
`zero-centered-inertia-boundary` is closed by matched typed rejection while origin inertia zero remains valid, and all existing Phase 6 real comparison/replay evidence stays green.
</success_criteria>

<output>After completion, create `06-20-SUMMARY.md` naming the equality fixture, public error rename, matched Rust/C++ evidence, and gap ID `zero-centered-inertia-boundary`.</output>
