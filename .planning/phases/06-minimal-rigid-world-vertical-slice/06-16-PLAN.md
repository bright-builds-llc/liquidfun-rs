---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T08:10:00Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "16"
type: execute
wave: 11
depends_on: ["06-15"]
files_modified:
  - crates/liquidfun-test-protocol/src/scenario/rigid_world.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/types.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs
  - crates/liquidfun-test-protocol/src/schema/rigid_world.rs
  - crates/liquidfun-test-protocol/src/schema/tests.rs
  - crates/liquidfun-test-protocol/tests/fixtures.rs
  - protocol/schemas/protocol-v1.schema.json
  - protocol/schemas/scenario-v1.schema.json
  - protocol/fixtures/rejected/rigid-world-negative-centered-inertia.jsonl
  - crates/liquidfun-differential/src/rigid_world.rs
  - crates/liquidfun-differential/tests/rigid_world.rs
  - tools/reference/src/rigid_world.hpp
  - tools/reference/src/rigid_world_decode.hpp
  - tools/reference/tests/protocol_tests.cpp
autonomous: true
requirements: [RIGD-02, RIGD-04]
gap_closure: true
must_haves:
  truths:
    - Phase 6 accepts exactly timestep bits `0x3c888889`, 8 velocity iterations, and 3 position iterations; every alternate lane is rejected by schema, Rust validation/native defense, and C++ decode before execution.
    - Rust, generated schemas, and C++ accept at most exactly 128 rigid actions and reject 129 before world effects.
    - Custom mass data is rejected at Rust and C++ protocol boundaries when source-ordered centered inertia is non-finite or negative.
  artifacts:
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs
    - protocol/schemas/scenario-v1.schema.json
    - tools/reference/src/rigid_world_decode.hpp
  key_links:
    - Named Rust/C++ contract constants drive validation and tests; the native executor no longer ignores the authored Step fields.
---

# Plan 06-16: Close the Rigid Protocol Contract

<objective>
Close `ignored-step-parameters`, `rigid-action-bound-mismatch`, and `invalid-centered-inertia-boundary` without expanding the fixed Phase 6 solver into Phase 7 configuration.
</objective>

<threat_model>

## Trust boundaries

| Threat | Boundary | Mitigation |
| --- | --- | --- |
| Contract smuggling | Valid JSON authors unsupported solver inputs | Schema and both decoders admit only the exact fixed tuple. |
| Bound disagreement | Rust accepts work the C++ child rejects | One reviewed value, 128, is mirrored in generated schema and C++ with max/max+1 tests. |
| Native assertion | Malformed mass data reaches `b2World::SetMassData` | Validate source-ordered centered inertia completely before either engine effect. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define one exact Rust/schema contract</name>
  <files>crates/liquidfun-test-protocol/src/scenario/rigid_world.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/types.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs, crates/liquidfun-test-protocol/src/schema/rigid_world.rs, crates/liquidfun-test-protocol/src/schema/tests.rs, crates/liquidfun-test-protocol/tests/fixtures.rs, protocol/schemas/protocol-v1.schema.json, protocol/schemas/scenario-v1.schema.json, protocol/fixtures/rejected/rigid-world-negative-centered-inertia.jsonl, crates/liquidfun-differential/src/rigid_world.rs, crates/liquidfun-differential/tests/rigid_world.rs</files>
  <read_first>
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-VERIFICATION.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-CONTEXT.md
    - crates/liquidfun-test-protocol/src/scenario/rigid_world.rs
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/types.rs
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/tests.rs
    - crates/liquidfun-test-protocol/src/schema/rigid_world.rs
    - crates/liquidfun-test-protocol/src/schema/tests.rs
    - crates/liquidfun-test-protocol/tests/fixtures.rs
    - protocol/schemas/protocol-v1.schema.json
    - protocol/schemas/scenario-v1.schema.json
    - protocol/fixtures/rejected/rigid-world-negative-centered-inertia.jsonl (create from the accepted rigid request with only the custom-mass action changed)
    - crates/liquidfun-differential/src/rigid_world.rs
    - crates/liquidfun-differential/tests/rigid_world.rs
    - crates/liquidfun/src/world/body.rs
    - crates/liquidfun/src/world/contact_solver.rs
  </read_first>
  <action>
Define reviewed contract constants for action limit 128 and fixed step tuple `0x3c888889`/8/3. Make Rust validation require exact equality, retaining `RigidWorldErrorKind::InvalidActionOrder` for an unsupported fixed-step tuple and using the existing `RigidWorldErrorKind::InvalidGeometry` category for invalid centered mass geometry. Replace the schema step ranges with `const` values and keep `actions.maxItems` at 128. Validate custom mass with the same source order as `BodyMassData::new`: effective mass, center dot product, multiplication, and `inertia - mass * dot`, rejecting non-finite or negative centered inertia. In the native executor destructure and check the exact Step fields before calling the fixed solver instead of matching `Step { .. }`. Add one-concern tests for each alternate step lane, action counts 128/129, and mass `(1, (2,0), 1)` plus non-finite intermediate cases. Create `protocol/fixtures/rejected/rigid-world-negative-centered-inertia.jsonl` by changing only a custom-mass action in the accepted rigid request, and extend the ordinary `fixtures` integration suite to decode it through `decode_rigid_world_request_jsonl` and assert exactly `Some(RigidWorldErrorKind::InvalidGeometry)` before native or C++ execution. Regenerate schemas read-only from typed authority.
  </action>
  <verify><automated>cargo test -p liquidfun-test-protocol rigid_world --all-features &amp;&amp; cargo test -p liquidfun-test-protocol schema --all-features &amp;&amp; cargo test -p liquidfun-test-protocol --test fixtures rigid_world_negative_centered_inertia --all-features &amp;&amp; cargo test -p liquidfun-differential --test rigid_world contract --all-features</automated></verify>
  <acceptance_criteria>
    - `rg -n "1015580809|0x3c88_8889|128" crates/liquidfun-test-protocol/src/scenario/rigid_world crates/liquidfun-test-protocol/src/schema/rigid_world.rs` finds the exact contract.
    - `! rg -n "RigidWorldAction::Step \{ \.\. \}" crates/liquidfun-differential/src/rigid_world.rs` exits 0.
    - Generated scenario schema uses exact step constants and `maxItems: 128`; negative centered inertia is a boundary validation error.
    - `cargo test -p liquidfun-test-protocol --test fixtures rigid_world_negative_centered_inertia --all-features` exits 0 and asserts `RigidWorldErrorKind::InvalidGeometry` for the checked-in rejected fixture.
  </acceptance_criteria>
  <done>Rust/schema/native acceptance is one closed fixed Phase 6 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror the exact contract at the C++ boundary</name>
  <files>tools/reference/src/rigid_world.hpp, tools/reference/src/rigid_world_decode.hpp, tools/reference/tests/protocol_tests.cpp</files>
  <read_first>
    - tools/reference/src/rigid_world.hpp
    - tools/reference/src/rigid_world_decode.hpp
    - tools/reference/tests/protocol_tests.cpp
    - crates/liquidfun-test-protocol/src/scenario/rigid_world/validation.rs
    - protocol/schemas/scenario-v1.schema.json
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.cpp
  </read_first>
  <action>
Mirror constants 128 and `0x3c888889`/8/3 in the reviewed C++ domain header, replace both 64-action checks, and reject every alternate step tuple during decode. Decode custom mass center once, compute dot, product, and centered inertia in source order, and reject non-finite or negative results before `RigidWorldAdapter` constructs a world. Add C++ protocol tests that accept a lifecycle-valid 128-action request, reject 129, reject each alternate step lane, and reject invalid centered inertia without an assertion or adapter execution. Preserve duplicate-aware bounded parsing and protocol-only stdout.
  </action>
  <verify><automated>cmake --build target/reference/oracle-debug --target liquidfun-reference-protocol-tests &amp;&amp; ctest --test-dir target/reference/oracle-debug --output-on-failure</automated></verify>
  <acceptance_criteria>
    - `! rg -n "actions\.size\(\) > 64|timeline\.actions\.size\(\) > 64" tools/reference/src/rigid_world_decode.hpp` exits 0.
    - CTest proves 128 accepted, 129 rejected, three step-lane mutations rejected, and centered-inertia rejection occurs before execution.
  </acceptance_criteria>
  <done>C++ accepts exactly the same bounded inputs as Rust and cannot assert on malformed custom mass data.</done>
</task>

</tasks>

<verification>
Run both task checks, debug/release rigid compare, and schema byte-stability. Then run `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` in that exact order, followed by `git diff --check`. No public timestep/iteration configuration is added; that remains Phase 7.
</verification>

<success_criteria>
All three protocol findings are closed with exact accepted values and matched Rust/C++ rejection behavior.
</success_criteria>

<output>After completion, create `06-16-SUMMARY.md` mapping all three verifier gap IDs to their max/max+1 and invalid-input evidence.</output>
