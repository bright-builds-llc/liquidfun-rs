---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T08:10:00Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "17"
type: execute
wave: 12
depends_on: ["06-15", "06-16"]
files_modified:
  - crates/liquidfun-differential/src/main.rs
  - crates/liquidfun-differential/src/lib.rs
  - crates/liquidfun-differential/src/rigid_fixtures.rs
  - crates/liquidfun-differential/src/fixtures/lifecycle.rs
  - crates/liquidfun-differential/src/fixtures/replay.rs
  - crates/liquidfun-differential/src/fixtures/domain.rs
  - crates/liquidfun-differential/tests/rigid_fixture_workflow.rs
  - crates/liquidfun-differential/tests/fixtures/fake_oracle.rs
  - tools/xtask/src/differential.rs
  - tools/xtask/tests/differential_cli.rs
  - justfile
autonomous: true
requirements: [RIGD-04]
gap_closure: true
must_haves:
  truths:
    - The actual `liquidfun-differential fixture stage --scenario rigid-world` command executes typed request decode, native execution, supervised oracle capture, identity validation, declaration-first comparison, and D1 authority validation before its first candidate write.
    - Rigid candidate replay and promotion re-run the rigid transaction and validate D1 authority before any accepted artifact or manifest mutation.
    - A real-binary end-to-end test accepts a canonical D1 fixture and rejects D2 with no staging directory or accepted-path mutation.
  artifacts:
    - crates/liquidfun-differential/src/rigid_fixtures.rs
    - crates/liquidfun-differential/tests/rigid_fixture_workflow.rs
    - crates/liquidfun-differential/src/main.rs
  key_links:
    - Rigid-specific typed validation feeds the existing confined create-new/no-clobber storage primitives; generic empty-world behavior remains unchanged.
---

# Plan 06-17: Real Rigid Fixture Lifecycle

<objective>
Close `rigid-staging-not-integrated` by making the advertised command a real typed D1-only stage/replay/promotion transaction.
</objective>

<threat_model>

## Trust boundaries

| Threat | Boundary | Mitigation |
| --- | --- | --- |
| Noncanonical promotion | Local D2 output enters reviewed storage | Call `validate_rigid_promotion_authority` before candidate directory creation and again before accepted destination/manifest mutation. |
| Partial artifact | Validation fails after some files are written | Complete decode, identity, comparison, tier, size, and path validation before invoking existing atomic storage primitives. |
| Replay substitution | Reviewed bytes or build identity change after review | Replay exact candidate hashes through the rigid comparator and require unchanged D1 identity before review/promotion. |
| Path injection | CLI candidate metadata selects arbitrary paths | Retain fixed scenario/preset/profile enums, normalized bounded IDs, confined paths, create-new/no-clobber semantics. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement typed rigid stage, replay, and promotion transactions</name>
  <files>crates/liquidfun-differential/src/main.rs, crates/liquidfun-differential/src/lib.rs, crates/liquidfun-differential/src/rigid_fixtures.rs, crates/liquidfun-differential/src/fixtures/lifecycle.rs, crates/liquidfun-differential/src/fixtures/replay.rs, crates/liquidfun-differential/src/fixtures/domain.rs</files>
  <read_first>
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-VERIFICATION.md
    - crates/liquidfun-differential/src/main.rs
    - crates/liquidfun-differential/src/lib.rs
    - crates/liquidfun-differential/src/rigid_fixtures.rs (create as the cohesive rigid fixture transaction module)
    - crates/liquidfun-differential/src/rigid_evidence.rs
    - crates/liquidfun-differential/src/rigid_world.rs
    - crates/liquidfun-differential/src/supervisor/rigid_world.rs
    - crates/liquidfun-differential/src/fixtures/lifecycle.rs
    - crates/liquidfun-differential/src/fixtures/replay.rs
    - crates/liquidfun-differential/src/fixtures/domain.rs
    - protocol/fixtures/accepted/rigid-world-request.jsonl
    - protocol/tolerances/phase6-v1.toml
    - standards/core/architecture.md
    - standards/core/verification.md
  </read_first>
  <action>
Create a cohesive `rigid_fixtures` domain transaction rather than embedding it in CLI parsing. For stage: decode the fixed rigid request and phase6 policy, execute native, capture the reviewed subprocess result/build identity, validate request/profile/build provenance, independently validate declarations, require exact comparison success for reviewed traces (or a stable matching signature for minimized regressions), enforce artifact bounds, then call `validate_rigid_promotion_authority` before the first staging-directory write and reuse existing confined create-new storage. Extend replay dispatch by recorded scenario kind so rigid candidates re-decode and recompare instead of entering the empty-world adapter. At review and promotion, replay exact hashes and call the D1 guard again before review receipt or accepted destination/manifest mutation. Keep failures typed as harness/fixture errors and leave generic empty-world lifecycle behavior unchanged.
  </action>
  <verify><automated>cargo test -p liquidfun-differential --test rigid_fixture_workflow transaction --all-features</automated></verify>
  <acceptance_criteria>
    - `rg -n "validate_rigid_promotion_authority" crates/liquidfun-differential/src/rigid_fixtures.rs crates/liquidfun-differential/src/fixtures/lifecycle.rs crates/liquidfun-differential/src/fixtures/replay.rs` finds pre-write and pre-promotion callers.
    - `! rg -n 'scenario != "empty-world"' crates/liquidfun-differential/src/main.rs` exits 0.
    - D2 rejection leaves no `target/differential/staging/<artifact-id>`, accepted artifact, or manifest diff.
  </acceptance_criteria>
  <done>The production fixture lifecycle is typed, replayable, D1-only, and atomic for rigid evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the actual binary and xtask surface end to end</name>
  <files>crates/liquidfun-differential/tests/rigid_fixture_workflow.rs, crates/liquidfun-differential/tests/fixtures/fake_oracle.rs, tools/xtask/src/differential.rs, tools/xtask/tests/differential_cli.rs, justfile</files>
  <read_first>
    - crates/liquidfun-differential/tests/fixture_workflow.rs
    - crates/liquidfun-differential/tests/rigid_fixture_workflow.rs
    - crates/liquidfun-differential/tests/fixtures/fake_oracle.rs
    - tools/xtask/src/differential.rs
    - tools/xtask/tests/differential_cli.rs
    - justfile
    - crates/liquidfun-differential/src/main.rs
  </read_first>
  <action>
Invoke the compiled `liquidfun-differential` binary—not a command-capture fake—against isolated repository fixtures. Extend `tests/fixtures/fake_oracle.rs` to decode the fixed rigid-world request in addition to its existing generic empty-world request handling, execute or load the deterministic declaration-valid rigid result, and emit protocol-correct canonical-D1 handshake, rigid result, terminal reset proof, and monotonic reset epoch bytes that pass the complete real-binary rigid comparator. Preserve every existing generic empty-world fake-oracle branch and output unchanged. Prove rigid stage then replay/review succeeds without path escape; separately make the fake emit a truthful D2 identity and prove exit failure occurs before candidate creation. Add dirty-candidate, comparison mismatch, authority, no-clobber, and status-propagation cases. Keep xtask and just as thin fixed-argument delegates, but update their tests so at least one path executes the real child and observes canonical acceptance plus D2 rejection.
  </action>
  <verify><automated>cargo test -p liquidfun-differential --test rigid_fixture_workflow real_binary --all-features &amp;&amp; cargo test -p xtask --test differential_cli rigid_fixture_real_binary --all-features</automated></verify>
  <acceptance_criteria>
    - The real binary stages/replays a D1 candidate and rejects D2 with a nonzero status and zero filesystem mutation.
    - `cargo test -p liquidfun-differential --test fixture_workflow --all-features` still passes the unchanged generic empty-world fake-oracle scenarios, while the rigid real-binary test consumes a decoded rigid request and validates result plus reset output.
    - Unknown scenario paths, executable paths, compiler flags, and arbitrary request paths remain rejected before effects.
    - `cargo xtask differential fixture stage --scenario rigid-world ...` delegates to the now-supported actual binary usage.
  </acceptance_criteria>
  <done>The advertised rigid fixture command is executable evidence, not argument-shape simulation.</done>
</task>

</tasks>

<verification>
Run the focused lifecycle and CLI tests, all existing fixture workflow tests, debug rigid compare/replay, and package isolation. Then run `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` in that exact order, followed by `git diff --check`. Review the transaction for a single validation-before-write seam.
</verification>

<success_criteria>
Rigid staging, replay, review, and promotion are real, confined, comparison-validated, and D1-authorized before every mutation.
</success_criteria>

<output>After completion, create `06-17-SUMMARY.md` with the real-binary commands and filesystem no-effect proof for D2.</output>
