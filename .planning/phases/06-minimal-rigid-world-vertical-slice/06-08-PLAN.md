---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T02:43:09.000Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "08"
type: execute
wave: 6
depends_on: ["06-05", "06-06", "06-07"]
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/rigid_differential.rs
  - crates/liquidfun-differential/native-math-sources.txt
  - crates/liquidfun-differential/src/lib.rs
  - crates/liquidfun-differential/src/main.rs
  - crates/liquidfun-differential/src/rigid_world.rs
  - crates/liquidfun-differential/tests/rigid_world.rs
autonomous: true
requirements: [RIGD-01, RIGD-02, RIGD-04]
must_haves:
  truths:
    - The validated timeline executes through native World and produces only bounded semantic results in declared/manager order.
    - Each native result is checked against scenario declarations before it becomes an accepted native trace.
  artifacts:
    - crates/liquidfun/src/rigid_differential.rs
    - crates/liquidfun-differential/src/rigid_world.rs
    - crates/liquidfun-differential/tests/rigid_world.rs
  key_links:
    - Native executor maps semantic IDs to opaque handles and uses only public World methods plus a doc-hidden owned diagnostic bridge.
---

# Plan 06-08: Native Rigid-World Adapter

<objective>
Execute the closed timeline in native Rust and produce a declaration-validated semantic trace for downstream comparison.
</objective>

<decision_coverage>
D-16 through D-19 are implemented on the native execution side; D-09 is enforced by keeping occurrence ordinals behind the existing non-default diagnostic feature. D-20 comparison/evidence lifecycle completes in Plan 06-09.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Validated scenario to native effects | Closed actions resolve semantic IDs to checked world handles before each mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T06-08-01 | Spoofing | Semantic ID mapping | mitigate | Closed maps reject missing/duplicate/cross-owner IDs and never serialize engine handles. |
| T06-08-02 | Tampering | Native trace | mitigate | Validate every result against declarations/counts/transitions before returning an accepted trace. |
| T06-08-03 | Information disclosure | Diagnostics | mitigate | Owned semantic records only; redact raw identities, pointers, and private storage. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute rigid timelines through native World</name>
  <files>crates/liquidfun/src/lib.rs, crates/liquidfun/src/rigid_differential.rs, crates/liquidfun-differential/native-math-sources.txt, crates/liquidfun-differential/src/lib.rs, crates/liquidfun-differential/src/main.rs, crates/liquidfun-differential/src/rigid_world.rs, crates/liquidfun-differential/tests/rigid_world.rs</files>
  <read_first>
    - crates/liquidfun-differential/src/collision_probe.rs
    - crates/liquidfun-differential/src/rust_adapter.rs
    - crates/liquidfun-differential/native-math-sources.txt
    - crates/liquidfun-test-protocol/src/scenario/rigid_world.rs
    - crates/liquidfun/src/world.rs
    - crates/liquidfun/src/world/step.rs
    - crates/liquidfun/src/collision/differential.rs
  </read_first>
  <behavior>
    - Both fixed families execute deterministically and every action/checkpoint echoes exact semantic IDs and phase.
    - Invalid semantic resolution or unexpected World failure is a typed native harness error with no partial accepted trace.
    - Private occurrence/impulse diagnostics are owned and available only with differential-internals enabled by the unpublished harness.
  </behavior>
  <action>
Add a doc-hidden feature-gated owned rigid diagnostic bridge that converts private manager state to bounded semantic records without exposing handles/storage publicly. Implement a native executor that creates one World per request, maintains declaration-ID-to-handle maps, dispatches every closed action, captures ordered checkpoints/reports, validates expected transitions/counts inline, and resets completely before terminal proof. Extend the existing native source identity manifest with every result-affecting rigid source. Add CLI dispatch under the existing differential binary rather than a second executable.
  </action>
  <verify><automated>cargo test -p liquidfun-differential --test rigid_world native --all-features</automated></verify>
  <acceptance_criteria>
    - The native filter exits 0 for both families, ID/owner rejection, declaration disagreement, exact ordering, and reset proof.
    - `cargo tree -p liquidfun-differential -e features | rg 'liquidfun feature "differential-internals"'` succeeds and `cargo tree -p liquidfun -e features | rg 'differential-internals'` fails.
    - `! rg -n '^\s*(pub(\([^)]*\))?\s+)?[a-z_][A-Za-z0-9_]*\s*:\s*(BodyId|FixtureId|ProxyId|ContactId|ContactHandle)\b' crates/liquidfun-test-protocol/src/scenario/rigid_world/types.rs crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs` and `! rg -n '^\s*pub\s+[a-z_][A-Za-z0-9_]*\s*:\s*(BodyId|FixtureId|ProxyId|ContactId|ContactHandle)\b' crates/liquidfun/src/rigid_differential.rs crates/liquidfun-differential/src/rigid_world.rs` both exit 0; private executor imports and semantic-ID-to-handle maps remain permitted.
  </acceptance_criteria>
  <done>The native adapter executes exact bounded timelines without leaking engine authority into the protocol.</done>
</task>

</tasks>

<verification>Run native execution, declaration-negative, source-identity mutation, strict differential Clippy, and default-feature isolation checks.</verification>
<success_criteria>Native Phase 6 execution produces reproducible declaration-validated semantic traces without exposing engine authority.</success_criteria>
<output>After completion, create `.planning/phases/06-minimal-rigid-world-vertical-slice/06-08-SUMMARY.md`.</output>
