---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T02:43:09.000Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "03"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/src/world/body.rs
  - crates/liquidfun/src/world/fixture.rs
  - crates/liquidfun/src/world/proxy.rs
  - crates/liquidfun/tests/rigid_world.rs
  - crates/liquidfun/tests/fixture_dynamics.rs
autonomous: true
requirements: [RIGD-01, RIGD-02, RIGD-04]
must_haves:
  truths:
    - Active fixtures own checked broad-phase proxies; deactivation removes them and activation recreates them deterministically.
    - Transform, type, sensor, and filter changes produce the pinned touch/synchronize/defer side effects without directly inventing contacts.
    - Body mass and inertia follow fixture density, explicit reset, custom override, type, and destruction trigger asymmetries exactly.
    - Fixture friction and restitution edits update only fixture state and the values used by contacts created afterward; existing-contact persistence is verified after the contact manager exists in Plan 06-04.
  artifacts:
    - crates/liquidfun/src/world/proxy.rs
    - crates/liquidfun/tests/fixture_dynamics.rs
  key_links:
    - World fixture children map to BroadPhase proxies using private typed payloads and checked AABBs.
    - Fixture mutation methods update private proxy/contact flags while mass calculations reuse Shape::mass_data.
---

# Plan 06-03: Proxy Lifecycle and Fixture Side Effects

<objective>
Connect checked rigid objects to Phase 5 broad phase and implement the pinned mass/material/filter/activation side effects that contact management relies on.
</objective>

<decision_coverage>
D-05 and D-06 are fully implemented here. D-07 fixture state and pending wake/refilter/touch side effects are implemented here, while its existing-contact material-persistence evidence completes in Plan 06-04 after authoritative contacts exist. D-04 mutation validation is extended to every new boundary.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Fixture topology to broad phase | Shape children and transforms become private proxies only after checked child/AABB construction. |
| Mutation to derived state | One public mutation may affect body mass, proxies, and pending contact flags atomically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T06-03-01 | Tampering | Derived proxy/mass state | mitigate | Validate all candidates, capture affected proxies, then commit one centralized transition with invariant tests. |
| T06-03-02 | Denial of service | Multi-child fixtures | mitigate | Reuse bounded Shape child counts and reviewed arena/tree capacities; reject overflow explicitly. |
| T06-03-03 | Spoofing | Proxy payload | mitigate | Keep fixture-child payload private and validate owning fixture/body before every proxy operation. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Own fixture proxies and activation/type/transform transitions</name>
  <files>crates/liquidfun/src/world/proxy.rs, crates/liquidfun/src/world/object.rs, crates/liquidfun/src/world/body.rs, crates/liquidfun/src/world/fixture.rs, crates/liquidfun/src/world.rs, crates/liquidfun/tests/fixture_dynamics.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/broad_phase.rs
    - crates/liquidfun/src/collision/shape.rs
    - crates/liquidfun/src/world/object.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Fixture.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.cpp
  </read_first>
  <behavior>
    - Active fixture creation creates one proxy per shape child; inactive creation creates none.
    - Transform synchronizes proxy AABBs but contact discovery waits for step.
    - Deactivation removes proxies and marks attached contacts for ordered destruction; activation recreates proxies; type change touches proxies and marks contacts.
  </behavior>
  <action>
Add private `world/proxy.rs` with semantic fixture-child payloads and a World-owned `BroadPhase`. Centralize child AABB creation, proxy synchronize/create/destroy/touch operations and keep ProxyId private. Wire exact transform/type/active transition effects, including finite displacement/AABB validation before commit. Expose only semantic counts/snapshots needed by tests, not proxy identities or traversal order.
  </action>
  <verify><automated>cargo test -p liquidfun --test fixture_dynamics proxy --all-features</automated></verify>
  <acceptance_criteria>
    - The focused proxy tests exit 0 for active/inactive creation, transform deferral, deactivate/reactivate, type touch, chain children, and failure atomicity.
    - `rg -n "BroadPhase&lt;|BroadPhase<|FixtureProxy" crates/liquidfun/src/world/proxy.rs crates/liquidfun/src/world/object.rs` finds private integration.
    - `! rg -n "pub .*ProxyId|pub fn .*proxy" crates/liquidfun/src/world crates/liquidfun/src/lib.rs` exits 0.
  </acceptance_criteria>
  <done>Fixture proxies are world-owned, deterministic, private, and updated by source-faithful lifecycle transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exact mass and fixture mutation asymmetries</name>
  <files>crates/liquidfun/src/world/object.rs, crates/liquidfun/src/world/body.rs, crates/liquidfun/src/world/fixture.rs, crates/liquidfun/tests/rigid_world.rs, crates/liquidfun/tests/fixture_dynamics.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/types.rs
    - crates/liquidfun/src/collision/shape.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Fixture.cpp
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-RESEARCH.md
  </read_first>
  <behavior>
    - Positive-density create resets mass; fixture destruction always resets; density edit does not reset until explicit reset.
    - Custom mass changes dynamic bodies only and is replaced by every later reset trigger.
    - Sensor/filter edits set the required pending wake/refilter/touch state; material edits change fixture snapshots and future contact-creation inputs only.
  </behavior>
  <action>
Implement source-ordered aggregate mass/center/inertia calculation, explicit reset, checked custom mass override, and fixture density/friction/restitution/sensor/filter methods. Preserve no-op custom mass behavior for static/kinematic bodies and validate centered inertia before mutation. Record private pending refilter/wake/touch flags for Plan 06-04, and keep fixture material changes confined to fixture state plus future contact-creation inputs because no authoritative contact manager exists yet. Extend destruction to remove proxies before invalidation and always reset the parent mass.
  </action>
  <verify><automated>cargo test -p liquidfun --test fixture_dynamics mass --all-features &amp;&amp; cargo test -p liquidfun --test fixture_dynamics mutation --all-features</automated></verify>
  <acceptance_criteria>
    - Both focused filters exit 0 and name create/destroy/density/reset/override/type plus sensor/filter/material witnesses.
    - `rg -n "pub fn (set_fixture_density|reset_body_mass_data|set_body_mass_data|set_fixture_sensor|set_fixture_filter|set_fixture_friction|set_fixture_restitution)" crates/liquidfun/src/world/object.rs` finds every handle method.
    - A regression assertion proves density edit preserves prior mass bits until explicit reset, and fixture snapshot tests prove material edits preserve exact accepted bits for later contact creation.
  </acceptance_criteria>
  <done>Mass, sensor, filter, and material side effects match the pinned asymmetries and remain atomic.</done>
</task>

</tasks>

<verification>Run both focused suites, strict Clippy, warning-denied docs, and inspect that no proxy identity or Phase 7 control became public.</verification>
<success_criteria>Every active fixture has coherent proxies and mass/material/filter mutations produce the exact deferred side effects required by automatic contacts.</success_criteria>
<output>After completion, create `.planning/phases/06-minimal-rigid-world-vertical-slice/06-03-SUMMARY.md`.</output>
