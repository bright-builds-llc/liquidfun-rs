---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T02:43:09.000Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "12"
type: execute
wave: 8
depends_on: ["06-09", "06-11"]
files_modified:
  - tools/xtask/src/differential.rs
  - tools/xtask/tests/differential_cli.rs
  - justfile
  - .github/workflows/oracle.yml
autonomous: true
requirements: [RIGD-01, RIGD-02, RIGD-04]
must_haves:
  truths:
    - Named debug and release commands compare both fixed families through native Rust and the pinned subprocess.
    - Replay and two independent debug runs preserve exact ordered bytes and first-divergence identity.
    - Stage/review/minimize workflows accept rigid artifacts while canonical promotion remains D1-only and no-clobber.
    - Cargo-only consumers and CI remain free of C++, the upstream checkout, and rigid reference execution.
  artifacts:
    - tools/xtask/src/differential.rs
    - tools/xtask/tests/differential_cli.rs
    - .github/workflows/oracle.yml
  key_links:
    - Xtask maps the closed `rigid-world` scenario name to the fixed request/profile and existing differential binary.
    - Oracle CI alone initializes C++, then runs compare/replay/determinism read-only under provenance checks.
---

# Plan 06-12: Closed Rigid Evidence Workflows

<objective>
Wire the native and C++ adapters into allowlisted contributor/CI compare, replay, determinism, minimization, and reviewed fixture-lifecycle commands.
</objective>

<decision_coverage>
D-20 is fully implemented here, including D0 two-run identity, D2 local authority, D1-only promotion, and reuse of all established evidence lifecycle paths. D-17 is exercised as one fixed corpus, not a general DSL.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Contributor CLI to subprocess/filesystem | Arguments may select only reviewed scenario, preset, session, action, and ignored staging identities. |
| Local evidence to accepted artifacts | Promotion authority must distinguish D0/D2 execution from canonical D1 review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T06-12-01 | Elevation of privilege | Xtask invocation | mitigate | Closed enums/fixed paths/structured arguments; no executable, compiler, flag, or external scenario injection. |
| T06-12-02 | Tampering | Fixture lifecycle | mitigate | Existing candidate-bound review, exact content hashes, create-new/no-clobber, atomic manifest rules, and D1 gate. |
| T06-12-03 | Repudiation | Evidence tier | mitigate | Print and validate build identity/tier; tests reject local D2 promotion or platform claims. |
| T06-12-04 | Denial of service | CI child | mitigate | Existing one-shot/reuse limits, concurrency cancellation policy, bounded artifacts, kill/reap. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register closed compare, replay, determinism, and fixture commands</name>
  <files>tools/xtask/src/differential.rs, tools/xtask/tests/differential_cli.rs, justfile</files>
  <read_first>
    - tools/xtask/src/differential.rs
    - tools/xtask/tests/differential_cli.rs
    - justfile
    - crates/liquidfun-differential/src/main.rs
    - protocol/fixtures/accepted/rigid-world-request.jsonl
    - protocol/tolerances/phase6-v1.toml
    - crates/liquidfun-differential/src/fixtures/lifecycle.rs
  </read_first>
  <behavior>
    - Only `rigid-world` selects the fixed request and phase6-v1 profile; external paths/unknown scenarios fail before effects.
    - Debug/release compare, replay, and exactly two-run determinism invoke canonical argument shapes and propagate status.
    - Rigid stage/review/minimize uses ignored bounded candidates; D2 promotion is rejected.
  </behavior>
  <action>
Extend the existing scenario enum and command parser with one `rigid-world` mapping to fixed repository paths. Reuse compare, replay, minimize, failure bundle, stage, review, promote, and verify-determinism machinery; do not create a competing command family. Add thin just aliases containing one visible xtask command. Add command-capture and negative tests for exact arguments, unknown/path injection, nonzero propagation, no-clobber, and D1 promotion authority.
  </action>
  <verify><automated>cargo test -p xtask --test differential_cli rigid --all-features</automated></verify>
  <acceptance_criteria>
    - The focused CLI suite exits 0 and covers every workflow plus injection/status/tier rejection.
    - `rg -n "rigid-world" tools/xtask/src/differential.rs justfile` finds only fixed scenario mappings/aliases.
    - `! rg -n "rigid.*(path|executable|compiler|flags)|--scenario-file" tools/xtask/src/differential.rs tools/xtask/tests/differential_cli.rs` exits 0.
  </acceptance_criteria>
  <done>Contributors have one closed, auditable rigid-world command surface reusing all permanent evidence machinery.</done>
</task>

<task type="auto">
  <name>Task 2: Run full rigid comparisons and isolate oracle CI</name>
  <files>.github/workflows/oracle.yml, tools/xtask/tests/differential_cli.rs</files>
  <read_first>
    - .github/workflows/oracle.yml
    - .github/workflows/cargo.yml
    - tools/xtask/src/differential.rs
    - .planning/phases/02-semantic-protocol-and-oracle-round-trip/02-14-SUMMARY.md
    - .planning/phases/05-shapes-and-collision-foundation/05-08-SUMMARY.md
  </read_first>
  <action>
Add rigid debug/release compare, debug replay, and exact two-run determinism to the oracle workflow after identity/build checks. Keep Cargo CI submodule-free and add no C++ dependency to consumer jobs. Locally run both presets and the complete workflows; exercise minimize plus stage/review in ignored storage without promoting D2 output. Verify read-only commands leave protocol/reference/compatibility tracked paths unchanged.
  </action>
  <verify><automated>cargo xtask differential compare --scenario rigid-world --preset oracle-debug --session-profile one-shot &amp;&amp; cargo xtask differential compare --scenario rigid-world --preset oracle-release --session-profile one-shot &amp;&amp; cargo xtask differential replay --scenario rigid-world --preset oracle-debug --session-profile one-shot &amp;&amp; cargo xtask differential verify-determinism --scenario rigid-world --preset oracle-debug --runs 2</automated></verify>
  <acceptance_criteria>
    - All four commands exit 0 and report both required families under phase6-v1 with truthful local D2 and D0 labels.
    - `git diff --exit-code -- protocol reference/compatibility.json COMPATIBILITY.md` exits 0 after compare/replay/determinism.
    - `! rg -n "submodules: recursive|cmake|oracle|rigid-world" .github/workflows/cargo.yml` exits 0, while oracle.yml contains the four closed commands.
    - An ignored stage/review test succeeds and an attempted local promotion fails before accepted-path mutation.
  </acceptance_criteria>
  <done>Both worlds match through debug/release/replay/D0 workflows and CI preserves the consumer/oracle trust split.</done>
</task>

</tasks>

<verification>Run focused CLI tests, debug/release CTest, all four fixed workflows, package isolation, provenance check, and tracked-path cleanliness checks.</verification>
<success_criteria>The full Rust/C++ pipeline proves both lifecycle families, permanent diagnosis/regression paths work, and no local result is overstated as canonical D1.</success_criteria>
<output>After completion, create `.planning/phases/06-minimal-rigid-world-vertical-slice/06-12-SUMMARY.md`.</output>
