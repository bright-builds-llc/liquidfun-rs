---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-07-12T02-22-53
generated_at: 2026-07-12T20:20:38Z
phase: 06-minimal-rigid-world-vertical-slice
plan: "22"
type: execute
wave: 16
depends_on: ["06-19", "06-20", "06-21"]
files_modified:
  - ARCHITECTURE.md
  - TESTING.md
  - README.md
  - reference/compatibility.json
  - COMPATIBILITY.md
  - tools/xtask/src/docs.rs
  - tools/xtask/tests/docs_contract.rs
autonomous: true
requirements: [RIGD-01, RIGD-02, RIGD-04]
gap_closure: true
must_haves:
  truths:
    - Public and evidence documentation truthfully records typed atomic type/fixture transitions, strict-positive centered inertia only for the positive-origin branch, and current-checkout adapter/compile provenance before every rigid fixture mutation.
    - The final matrix passes focused regressions, the mandatory Rust gate, fresh C++ protocol tests, debug/release/replay D2, two-run D0, fail-fast sanitizer D2, real rigid stage no-effect controls, docs/inventory/provenance/package checks, and lifecycle/completeness checks.
    - Local results remain D0/D2 only, canonical D1 is not claimed, and forces, general islands, sleeping, CCD, configurable stepping, queries, ray casts, origin shifting, and joints remain deferred.
    - Execution does not hand-edit REVIEW.md or VERIFICATION.md; after the matrix passes, the orchestrator must run a fresh GSD code review and formal verifier to replace both reports from actual code/evidence.
  artifacts:
    - ARCHITECTURE.md
    - TESTING.md
    - reference/compatibility.json
    - tools/xtask/tests/docs_contract.rs
  key_links:
    - Docs contracts require the three second-round gap IDs and exact focused evidence rather than relying on the happy-path corpus.
    - Compatibility body/fixture rows cite the new atomicity and centered-inertia tests, while fixture-lifecycle evidence cites the shared checkout validator and stale-digest real-binary tests.
    - The final executor handoff leaves 06-REVIEW.md and 06-VERIFICATION.md solely owned by subsequent formal review/verifier runs.
---

# Plan 06-22: Final Completion Matrix and Re-Signoff Preparation

<objective>
Consolidate the three second-round fixes into truthful contracts, run the complete Phase 6 completion matrix, and hand a clean evidence set to formal GSD review and verification without self-signing the phase.
</objective>

<threat_model>

## Trust boundaries

| Threat | Boundary | Mitigation |
| --- | --- | --- |
| False completion claim | Happy-path compare passes while source atomicity or provenance regresses | Require each gap's focused negative/no-effect test in docs contracts and the final matrix. |
| Evidence inflation | Local Apple Clang/CMake execution is described as canonical D1 or platform validation | Preserve explicit D0/D2 labels and leave all platform evidence unchanged. |
| Stale generated reporting | Compatibility presentation diverges from authoritative JSON | Update JSON first, regenerate presentation, and require byte-stable inventory check. |
| Unbounded or secret diagnostics | Failure checks expose checkout files or compiler commands | Require bounded harness bundles and redaction/path scans; never print adapter bytes or compile databases. |
| Self-approved verification | Executor edits review/verifier status to passed | Prohibit edits to 06-REVIEW.md and 06-VERIFICATION.md; invoke formal agents only after execution completes. |
| Scope creep | Gap closure absorbs Phase 7/8 work | Scan public API/docs for forces, general solver, sleeping, CCD, queries, configurable stepping, or joints claims. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile public contracts and compatibility evidence</name>
  <files>ARCHITECTURE.md, TESTING.md, README.md, reference/compatibility.json, COMPATIBILITY.md, tools/xtask/src/docs.rs, tools/xtask/tests/docs_contract.rs</files>
  <read_first>
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
    - standards/core/architecture.md
    - standards/core/testing.md
    - standards/core/verification.md
    - standards/languages/rust.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-CONTEXT.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-VERIFICATION.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-REVIEW.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-19-SUMMARY.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-20-SUMMARY.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-21-SUMMARY.md
    - crates/liquidfun/src/world/body.rs
    - crates/liquidfun/src/world/fixture.rs
    - crates/liquidfun/src/world/object.rs
    - crates/liquidfun-differential/src/oracle_identity.rs
    - crates/liquidfun-differential/src/rigid_fixtures.rs
    - ARCHITECTURE.md
    - TESTING.md
    - README.md
    - reference/compatibility.json
    - COMPATIBILITY.md
    - tools/xtask/src/docs.rs
    - tools/xtask/tests/docs_contract.rs
  </read_first>
  <action>
Use `apply_patch`. Update the Phase 6 architecture/API text to name the exact typed `BodyTypeChangeError` and `FixtureDestructionError` no-effect behavior, including body cascade's no-reset rationale. Correct custom-mass wording: origin inertia zero is the pinned no-inertia branch; positive origin inertia must remain finite and strictly positive after source-ordered centering. Correct fixture evidence wording: D1 authority alone is insufficient; adapter-source and effective compile-command digests are independently recomputed from the current checkout/preset at stage and every replay used by review/promotion before mutation. Preserve local D0/D2 and canonical D1 distinctions and all Phase 7/8 deferrals.

Extend the Phase 6 verification-gap table with exact rows for `implicit-aggregate-mass-atomicity`, `zero-centered-inertia-boundary`, and `rigid-fixture-checkout-provenance`, citing the new focused commands and no-effect surfaces. Strengthen `tools/xtask/src/docs.rs` and negative tests so deleting any second-round gap ID, typed error name, positive-origin/strict-positive clause, current-checkout adapter/compile clause, or D0/D2 qualifier fails `cargo xtask docs check`. Update authoritative compatibility rows for bodies/fixtures/world and rigid fixture evidence to cite the new production seams, equality fixture/tests, shared validator, stale-digest real-binary tests, and 06-19..06-21 summaries. Do not mark platform validation or broader solver rows evidenced. Regenerate `COMPATIBILITY.md` only through `cargo xtask inventory generate`, then require byte stability.

Do not edit `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `06-REVIEW.md`, or `06-VERIFICATION.md`; GSD tooling/formal agents own those status changes. Do not run `mdformat` on any GSD artifact. Before committing this task, run these exact commands in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Commit only if all four pass.
  </action>
  <verify>
    <automated>cargo test -p xtask --test docs_contract phase6 --all-features &amp;&amp; cargo xtask docs check &amp;&amp; cargo xtask inventory generate &amp;&amp; cargo xtask inventory check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "implicit-aggregate-mass-atomicity|zero-centered-inertia-boundary|rigid-fixture-checkout-provenance" TESTING.md tools/xtask/src/docs.rs tools/xtask/tests/docs_contract.rs` finds all three second-round gap IDs in prose authority and fail-closed checks.
    - Architecture and README name typed no-effect transitions, the positive-origin strict-positive rule, and current-checkout adapter plus compile digest validation without claiming local D1.
    - `cargo xtask inventory check` reports no generated drift, and affected rows cite plans/summaries 06-19 through 06-21 while platform evidence remains false.
    - `git diff --exit-code -- .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/phases/06-minimal-rigid-world-vertical-slice/06-REVIEW.md .planning/phases/06-minimal-rigid-world-vertical-slice/06-VERIFICATION.md` exits 0 for this task.
  </acceptance_criteria>
  <done>Documentation and compatibility evidence describe the fixed contracts exactly and fail closed against reopening any second-round gap.</done>
</task>

<task type="auto">
  <name>Task 2: Run the completion matrix and hand off formal re-signoff</name>
  <files>ARCHITECTURE.md, TESTING.md, README.md, reference/compatibility.json, COMPATIBILITY.md, tools/xtask/src/docs.rs, tools/xtask/tests/docs_contract.rs</files>
  <read_first>
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-19-SUMMARY.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-20-SUMMARY.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-21-SUMMARY.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-VERIFICATION.md
    - .planning/phases/06-minimal-rigid-world-vertical-slice/06-REVIEW.md
    - .github/workflows/oracle.yml
    - tools/reference/CMakeLists.txt
    - tools/reference/tests/protocol_tests.cpp
    - crates/liquidfun/tests/fixture_dynamics.rs
    - crates/liquidfun/tests/rigid_definitions.rs
    - crates/liquidfun-test-protocol/tests/fixtures.rs
    - crates/liquidfun-differential/tests/rigid_fixture_workflow.rs
    - tools/xtask/tests/differential_cli.rs
    - ARCHITECTURE.md
    - TESTING.md
    - README.md
    - reference/compatibility.json
  </read_first>
  <action>
Perform a read-only final matrix after the last implementation/documentation change. Run the focused implicit-mass, zero-centered-inertia, protocol fixture, shared checkout identity, stale real-binary, docs, and workflow tests. Run the mandatory Rust sequence in the exact order below. Freshly configure/build debug and release; build and run C++ protocol tests; run rigid debug compare, release compare, debug replay, and exactly two-run D0 determinism. Freshly configure/build `oracle-asan-ubsan`, build/run protocol CTest with `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1` and `ASAN_OPTIONS=abort_on_error=1:halt_on_error=1`, and run the one-shot rigid compare under the same environment; classify local output only as D2. Run docs, inventory, provenance, package isolation, warning-denied rustdoc, schema drift/byte-stability, GSD lifecycle, phase completeness, and `git diff --check`. Check that read-only commands produce no tracked source, protocol, compatibility, accepted artifact, manifest, or GSD-report mutation and that diagnostics remain bounded.

If any command fails, fix only the owning implementation/documentation contract, rerun its focused check, then restart the affected matrix; do not soften assertions, swallow status, widen tolerances, or edit REVIEW/VERIFICATION. Run one explicit simplification pass over the final diff: prefer shared candidate/identity functions, remove redundant branches, and confirm this is the root-cause solution. Before any executor commit, run these exact commands in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Commit only if all four pass.

After every matrix command passes, create the plan summary and return control to the phase orchestrator. The orchestrator—not this executor—must run a fresh GSD code reviewer and formal goal-backward verifier to overwrite `06-REVIEW.md` and `06-VERIFICATION.md`. Formal acceptance requires no critical/warning findings for these gaps, roadmap criteria 4/4, RIGD-01 retained verified, RIGD-02/RIGD-04 verified, repository completion gate passed, and no human-verification dependency. If formal review or verification finds a contradiction, leave the phase incomplete and route back to gap planning.
  </action>
  <verify>
    <automated>cargo test -p liquidfun --test fixture_dynamics implicit_aggregate_mass --all-features &amp;&amp; cargo test -p liquidfun --test rigid_definitions centered_inertia --all-features &amp;&amp; cargo test -p liquidfun-test-protocol --test fixtures rigid_world_zero_centered_inertia --all-features &amp;&amp; cargo test -p liquidfun-differential --test oracle_identity --all-features &amp;&amp; cargo test -p liquidfun-differential --test rigid_fixture_workflow stale --all-features &amp;&amp; cargo xtask docs check &amp;&amp; cargo xtask inventory check &amp;&amp; cargo xtask provenance check &amp;&amp; cargo xtask package verify</automated>
  </verify>
  <acceptance_criteria>
    - Focused implicit-mass, equality-boundary, protocol/C++, shared identity, and stale real-binary suites all pass with exact typed/no-effect assertions.
    - `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` pass in that exact order after the final change.
    - Fresh C++ debug protocol tests, debug/release compare, debug replay, D0 two-run determinism, and fail-fast sanitizer protocol/rigid compare pass with truthful D0/D2 labels.
    - Docs/inventory/provenance/package/rustdoc/schema/lifecycle/phase-completeness/diff checks pass and no accepted artifact, manifest, source, protocol, report, ROADMAP, or REQUIREMENTS file changes during read-only verification.
    - Executor does not modify `06-REVIEW.md` or `06-VERIFICATION.md`; its summary explicitly requests fresh formal code review and verification.
  </acceptance_criteria>
  <done>The repository is cleanly prepared for formal Phase 6 re-signoff with all three critical contradictions covered by direct evidence.</done>
</task>

</tasks>

<verification>
Run the complete Task 2 matrix after the last file change. The formal code reviewer and verifier run afterward and own the report replacements; execution alone must not mark the phase passed.
</verification>

<success_criteria>
All second-round gaps have focused and full-matrix evidence, documentation remains truthful, no Phase 7/8 scope is claimed, and formal review/verifier receive a clean immutable evidence set for the only valid Phase 6 completion decision.
</success_criteria>

<output>After completion, create `06-22-SUMMARY.md` with every matrix command/result, local authority limits, unchanged-report proof, and explicit handoff to fresh GSD code review and formal verification.</output>
