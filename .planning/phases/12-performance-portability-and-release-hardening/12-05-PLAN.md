---
phase: 12-performance-portability-and-release-hardening
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun/src/world/observation/profile.rs
  - crates/liquidfun/src/world/step.rs
  - crates/liquidfun/tests/phase12_profiles.rs
autonomous: true
requirements: ["PERF-03", "API-11"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "Consumers can inspect versioned common parent phases while Rust-only children remain optional diagnostics."
    - "Profile durations remain non-comparable and outside StepReport/checkpoints."
  artifacts:
    - path: "crates/liquidfun/src/world/observation/profile.rs"
      provides: "Versioned storage-neutral parent/child profile vocabulary"
      contains: "DiagnosticProfileSchema"
    - path: "crates/liquidfun/tests/phase12_profiles.rs"
      provides: "Public profile structure and non-authority tests"
      contains: "profile"
  key_links:
    - from: "crates/liquidfun/src/world/step.rs"
      to: "DiagnosticStepProfiler"
      via: "source-ordered phase boundaries"
      pattern: "profiler.record"
---
<objective>
Extend the diagnostic profile vocabulary for comparable parent phases without exposing storage or promoting durations into parity.

Purpose: Implement D-03 and PERF-03 on the established public boundary.
Output: `phase12-profile-v1` parent/child phases and focused public API evidence.
</objective>
<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@standards/core/testing.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@crates/liquidfun/src/world/observation/profile.rs
@crates/liquidfun/src/world/step.rs
@crates/liquidfun-testbed/src/capability/report.rs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add versioned comparable parent and diagnostic child phases</name>
  <files>crates/liquidfun/src/world/observation/profile.rs, crates/liquidfun/src/world/step.rs, crates/liquidfun/tests/phase12_profiles.rs</files>
  <read-first>Read every modified file, existing step-profile call sites/tests, Phase 11 capability profile assertions, and D-03 before editing.</read-first>
  <behavior>
    - "Common parents are contact_update, rigid_solve, continuous_solve, particle_prepare, particle_solve, and finalize."
    - "Optional Rust-only children identify existing solver clusters but do not change parent elapsed totals or semantic reports."
    - "Disabled profiling remains allocation-free and successful profiled/unprofiled steps have identical StepReport semantics."
  </behavior>
  <action>Per D-03, add public documented `DiagnosticProfileSchema::Phase12V1`, closed common-parent identifiers, and optional Rust-only child identifiers with explicit parent mapping. Preserve `DiagnosticStepPhaseTiming`'s lack of PartialEq/Hash/serialization and keep all durations out of `StepReport`. Instrument existing source boundaries without reordering operations, add checked maximum phase count 32, and prove unprofiled/profiled semantic equality through public observations rather than timing equality. Document units as `Duration`, diagnostic-only meaning, and storage-neutral parent intent. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>cargo test -p liquidfun --test phase12_profiles && RUSTDOCFLAGS="-D warnings" cargo doc -p liquidfun --all-features --no-deps && cargo clippy -p liquidfun --all-targets --all-features -- -D warnings</automated></verify>
  <acceptance-criteria>All six parent tokens appear; the public test proves identical StepReport/observations; rustdoc and Clippy exit 0; profile timing types still have no PartialEq, Hash, serde, or checkpoint conversion.</acceptance-criteria>
  <done>The profile boundary is versioned, comparable by structure, storage-neutral, and non-authoritative by type.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| instrumented production step to diagnostics | Timing instrumentation must not alter solver order or expose private storage. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-05-01 | Tampering | solver order | mitigate | Insert timers only around existing contiguous source regions and assert semantic equality. |
| T-12-05-02 | Information disclosure | child phases | mitigate | Expose closed semantic names, never private indices/caches. |
| T-12-05-03 | Denial of service | phase collection | mitigate | Fixed maximum of 32 records. |
</threat-model>
<verification>Run profile integration tests, public rustdoc with warning denial, Clippy, and determinism comparisons.</verification>
<success-criteria>Consumers receive useful parent phases while parity, safety, and deterministic behavior remain unchanged.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-05-SUMMARY.md`.</output>
