---
phase: 12-performance-portability-and-release-hardening
plan: "20"
type: execute
wave: 5
depends_on: ["12-19"]
files_modified:
  - crates/liquidfun-differential/src/performance.rs
  - crates/liquidfun-differential/src/performance/oracle.rs
  - crates/liquidfun-differential/src/performance/report.rs
  - crates/liquidfun-differential/tests/paired_performance.rs
autonomous: true
requirements: ["PERF-02", "PERF-03", "PERF-05"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T16:17:43Z
must_haves:
  truths:
    - "Rust and C++ samples alternate within one hardware session over identical sealed inputs."
    - "Unprofiled totals are authoritative while parent/child profiles remain diagnostic."
    - "Harness failures, physics mismatches, and valid performance samples are disjoint report states."
  artifacts:
    - path: "crates/liquidfun-differential/src/performance/report.rs"
      provides: "Immutable raw paired report builder"
      contains: "raw"
    - path: "crates/liquidfun-differential/tests/paired_performance.rs"
      provides: "Interleaving, identity, failure, and reset proof"
      contains: "interleav"
  key_links:
    - from: "crates/liquidfun-differential/src/performance.rs"
      to: "crates/liquidfun-differential/src/performance/oracle.rs"
      via: "explicit child-module declaration and reviewed re-export"
      pattern: "mod oracle|pub use oracle"
    - from: "crates/liquidfun-differential/src/performance.rs"
      to: "crates/liquidfun-differential/src/performance/report.rs"
      via: "explicit child-module declaration and reviewed re-export"
      pattern: "mod report|pub use report"
---

<objective>
Interleave native and oracle samples and build immutable raw paired reports.

Purpose: Produce trustworthy same-host evidence with exact failure and profile authority boundaries.
Output: Oracle adapter, report builder, and exhaustive paired execution tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@.planning/phases/12-performance-portability-and-release-hardening/12-19-SUMMARY.md
@crates/liquidfun-differential/src/supervisor/catalog.rs
@crates/liquidfun-differential/src/performance/native.rs
@crates/liquidfun-differential/src/runner.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Interleave paired samples and build immutable raw reports</name>
  <files>crates/liquidfun-differential/src/performance.rs, crates/liquidfun-differential/src/performance/oracle.rs, crates/liquidfun-differential/src/performance/report.rs, crates/liquidfun-differential/tests/paired_performance.rs</files>
  <read-first>Read every modified file, the Plan 12-07 wire types, Plan 12-19 endpoint, supervisor failure evidence, native paired executor, and D-01/D-03/D-05.</read-first>
  <action>Per D-01, D-03, and D-05, declare the new `oracle` and `report` children in `performance.rs` and re-export only the reviewed adapter/report surface used by callers; do not leave either child unreachable. Use the existing synchronous supervisor and bounded drains. For each independent run, alternate `Rust,C++,C++,Rust` sample order by ordinal, validate every identity/checkpoint/reset epoch, and retain raw nanoseconds before computing intervals. Build reports containing every D-05 field, unprofiled totals as authority, common parent profiles and optional Rust children as diagnostics, and explicit compatibility status. Classify timeout/crash/schema/provenance/sanitizer as harness failure; classify semantic divergence as physics mismatch; never coerce either into a duration. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>cargo test -p liquidfun-differential --test paired_performance && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>`performance.rs` declares and exposes both children; tests import their reviewed surface through the parent and assert exact alternating order, five independent-run grouping, raw sample retention, stable first failure, diagnostic-only profiles, and disjoint harness/mismatch/performance variants.</acceptance-criteria>
  <done>One hardware session produces identity-complete paired raw evidence with correct failure boundaries.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| Rust supervisor to C++ oracle | Cross-process results can be mislabeled or mixed. |
| raw measurements to report | Results can be generalized beyond their identity. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-20-01 | Spoofing | engine/report identity | mitigate | Validate roles, revisions, tools, flags, target, hardware, scenario, and policy. |
| T-12-20-02 | Repudiation | paired ordering | mitigate | Persist ordinal, engine order, raw values, and reset epochs. |
| T-12-20-03 | Tampering | failure classification | mitigate | Model harness, mismatch, and result as disjoint typed variants. |
</threat-model>

<verification>Run paired performance tests and the ordered Rust gate.</verification>
<success-criteria>Equivalent scenarios run interleaved through both engines and yield trustworthy, scoped performance evidence.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-20-SUMMARY.md`.</output>
