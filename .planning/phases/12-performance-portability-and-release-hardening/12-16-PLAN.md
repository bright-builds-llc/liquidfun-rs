---
phase: 12-performance-portability-and-release-hardening
plan: "16"
type: execute
wave: 10
depends_on: ["12-15"]
files_modified:
  - scripts/phase12-release-evidence.sh
  - .github/workflows/release.yml
  - tools/xtask/src/release/validation.rs
  - tools/xtask/tests/release_cli.rs
autonomous: true
requirements: ["COMP-10", "API-11", "API-12", "TEST-05", "TEST-06", "TEST-07", "TEST-08", "PERF-04", "PERF-05", "PERF-06", "PLAT-01", "PLAT-02", "PLAT-03", "PLAT-04", "PLAT-05", "PLAT-06", "DOCS-01", "DOCS-04", "DOCS-06", "DOCS-07", "DOCS-08", "DOCS-09"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "A release-candidate workflow downloads independently produced same-commit artifacts and runs the typed audit once."
    - "No all-in-one job reruns fuzz, Miri, sanitizers, coverage, broad platforms, or benchmarks."
    - "Inexpensive candidate-bound checks emit typed hashed evidence before a manifest is constructed or audited."
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Commit-bound artifact aggregation and final audit"
      contains: "release audit"
    - path: "scripts/phase12-release-evidence.sh"
      provides: "Check-first evidence construction and aggregation"
      contains: "set -euo pipefail"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "cargo xtask release audit"
      via: "downloaded artifact set"
      pattern: "release audit"
---
<objective>
Construct and aggregate one candidate-bound evidence set without modifying tracked readiness status.

Purpose: Complete the D-11/D-12 aggregation boundary without a fragile all-in-one workflow or self-referential tracked candidate.
Output: Release aggregation workflow/script and validated untracked candidate evidence artifacts.
</objective>
<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@standards/core/verification.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-15-SUMMARY.md
@reference/release/required-evidence.toml
@RELEASE.md
@.github/workflows/platform.yml
@.github/workflows/safety.yml
@.github/workflows/coverage.yml
@.github/workflows/fuzz.yml
@.github/workflows/regressions.yml
@.github/workflows/performance.yml
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Aggregate independent same-commit artifacts and reject every incomplete set</name>
  <files>scripts/phase12-release-evidence.sh, .github/workflows/release.yml, tools/xtask/src/release/validation.rs, tools/xtask/tests/release_cli.rs</files>
  <read-first>Read every modified file or record absence, all evidence workflows/artifact names, release validator, GitHub action pins, D-11/D-12, and exact candidate commit rules.</read-first>
  <behavior>
    - "The workflow accepts an explicit full candidate SHA and exact producer run/artifact identities."
    - "Aggregation downloads and hashes artifacts; it does not execute the expensive producers."
    - "Audit and package publication dry-run pass before any readiness artifact is uploaded."
  </behavior>
  <action>Per D-11, D-12, and D-16, create a full-SHA-pinned manual `workflow_dispatch` release-candidate constructor with concurrency cancellation disabled. Inputs name a frozen full source-candidate SHA and reviewed producer run IDs; checkout is detached at that exact SHA. Before constructing any manifest or invoking release audit, run inexpensive candidate-bound package creation/verification, `cargo publish -p liquidfun --dry-run`, cargo-deny, rustdoc, docs, notices/licenses, corpus closure, compatibility closure, and read-only generation checks; each check must emit a typed hashed evidence record with candidate SHA, tool identity, payload hash, producer job/run ID, and reviewed status. Only after those records validate, download exact platform/fuzz/regression/safety/coverage/performance/oracle artifacts, including the one distinct `phase12-regressions-<candidate>` artifact from Plan 12-25; reject run/SHA/job/name/cardinality mismatch before opening payloads, build the manifest in a confined target directory, and run `cargo xtask release audit`. Do not rerun named regressions, benchmarks, platform matrices, fuzz, Miri, sanitizers, or coverage. Write audit identity last and upload only validated untracked source-candidate manifest/report/package digests; do not edit tracked `reference/release/*.json` or public docs in this plan. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>bash -n scripts/phase12-release-evidence.sh && actionlint .github/workflows/release.yml && cargo test -p xtask --test release_cli && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>Workflow tests reject manifest/audit before inexpensive typed evidence, missing/duplicate/wrong-run/wrong-SHA artifacts, tracked-report writes, and all-in-one producer commands; concurrency is non-cancelling; publication is dry-run only; upload occurs only after audit success.</acceptance-criteria>
  <done>The release lane aggregates rather than duplicates evidence and cannot mix commits or partial runs.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| multiple workflow artifacts to release authority | Cross-run artifacts may be substituted or mixed. |
| release authority to public claims | A partial decision must never become a parity or readiness claim. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-16-01 | Spoofing | producer artifacts | mitigate | Full candidate/run/job/name/cardinality checks and independent hashes. |
| T-12-16-02 | Tampering | candidate manifest/report | mitigate | Confined construction, strict validation, identity-last write, read-only recheck. |
| T-12-16-03 | Repudiation | release decision | mitigate | Immutable machine report enumerates every accepted evidence identity and scope. |
| T-12-16-04 | Elevation of privilege | public parity claim | mitigate | Docs derive readiness only from `ready: true`; incomplete audit leaves claims absent. |
| T-12-16-05 | Denial of service | all-in-one workflow | mitigate | Aggregate existing bounded artifacts; do not rerun expensive suites. |
</threat-model>
<verification>Lint the constructor workflow, run release negatives, prove inexpensive typed evidence precedes manifest/audit, and run the ordered Rust gate.</verification>
<success-criteria>One frozen source candidate yields a validated untracked evidence set without self-reference, tracked status changes, or expensive-suite duplication.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-16-SUMMARY.md`.</output>
