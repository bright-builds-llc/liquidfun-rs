---
phase: 12-performance-portability-and-release-hardening
plan: "24"
type: execute
wave: 3
depends_on: ["12-10"]
files_modified:
  - scripts/phase12-miri.sh
  - scripts/phase12-rust-sanitizers.sh
  - scripts/phase12-coverage.sh
  - .github/workflows/safety.yml
  - .github/workflows/coverage.yml
autonomous: true
requirements: ["API-12", "TEST-06", "TEST-08"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T16:41:48Z
must_haves:
  truths:
    - "Miri exercises allowlisted pure-Rust subsets without initializing or spawning C++."
    - "Rust sanitizer, C++ ASan/UBSan, Rust coverage, C++ coverage, and differential coverage execute and upload as separate evidence kinds."
    - "Every coverage script invokes Plan 12-10's typed xtask validator before writing identity last."
  artifacts:
    - path: "scripts/phase12-coverage.sh"
      provides: "Separate validated Rust, C++, and differential coverage orchestration"
      contains: "safety-evidence validate-coverage"
    - path: ".github/workflows/safety.yml"
      provides: "Pinned Miri and Rust sanitizer evidence lanes"
      contains: "miri"
    - path: ".github/workflows/coverage.yml"
      provides: "Separate coverage artifact producer lanes"
      contains: "coverage"
  key_links:
    - from: "scripts/phase12-coverage.sh"
      to: "tools/xtask/src/safety_evidence.rs"
      via: "closed typed coverage validation command"
      pattern: "cargo xtask safety-evidence validate-coverage"
    - from: ".github/workflows/safety.yml"
      to: "rust-toolchain-nightly.toml"
      via: "exact dated nightly toolchain consumption"
      pattern: "nightly-2026-07-15"
---

<objective>
Execute the pinned safety, sanitizer, and coverage evidence lanes through the typed Plan 12-10 validation boundary.

Purpose: Complete D-12/D-14 without mixing contract design, shell orchestration, and CI production in one plan.
Output: Three rerunnable scripts and two isolated scheduled/manual workflows.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-10-SUMMARY.md
@rust-toolchain-nightly.toml
@.github/workflows/oracle.yml
@tools/xtask/src/safety_evidence.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement rerunnable validated safety and coverage scripts</name>
  <files>scripts/phase12-miri.sh, scripts/phase12-rust-sanitizers.sh, scripts/phase12-coverage.sh</files>
  <read-first>Read every modified file or record absence, the Plan 12-10 validator commands, exact nightly toolchain file, oracle sanitizer workflow, repository script standards, and supported pure-Rust tests.</read-first>
  <action>Per D-12 and D-14, create `set -euo pipefail` scripts with closed modes and logs/summaries beneath `target/phase12-*`. Every Rust unstable-tool command must invoke exact `nightly-2026-07-15`. Miri must run allowlisted math, arena/handle, collision, protocol codec, particle storage/permutation, and group-model tests with no submodule or C++ environment. Rust sanitizers run supported Linux x86_64 subsets under that exact nightly ASan separately from the existing C++ Clang ASan/UBSan job. Coverage uses cargo-llvm-cov 0.8.7 for Rust and a separate Clang 22 build/profile for C++, never merges profiles, and joins only semantic differential leaf coverage. Before any coverage identity is written, invoke the exact Plan 12-10 command `cargo xtask safety-evidence validate-coverage`; shell code must not duplicate typed parsing. Write identity last only after the command succeeds. Plan 12-25 owns the distinct TEST-07 regression producer and `validate-regressions` call. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.</action>
  <verify><automated>bash -n scripts/phase12-miri.sh scripts/phase12-rust-sanitizers.sh scripts/phase12-coverage.sh && scripts/phase12-miri.sh check && scripts/phase12-coverage.sh check && rg -n "cargo xtask safety-evidence validate-coverage" scripts/phase12-coverage.sh && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>Scripts are syntactically valid, bounded, and identity-last; Miri has no CMake/oracle call; coverage parsing occurs only through the exact typed xtask command; separate Rust and C++ coverage records remain non-authoritative for parity.</acceptance-criteria>
  <done>Every expensive local evidence path is rerunnable, pinned, bounded, and connected to shared typed validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run safety and coverage in isolated CI producers</name>
  <files>.github/workflows/safety.yml, .github/workflows/coverage.yml</files>
  <read-first>Read both modified workflows or record absence, Task 1 scripts, existing full-SHA action pins, release-candidate workflow inputs, and D-12/D-14.</read-first>
  <behavior>
    - "Scheduled and manual release-candidate lanes use finite timeouts and exact candidate/toolchain identities."
    - "Miri and Rust sanitizers never initialize C++; existing C++ sanitizer evidence remains a separate producer."
    - "Rust, C++, and differential coverage upload under distinct candidate-scoped artifact names only after typed validation."
  </behavior>
  <action>Per D-12 and D-14, create scheduled/manual release-candidate workflows using full action SHAs, explicit full candidate SHA inputs, finite timeouts, and exact `nightly-2026-07-15`. Invoke only the Task 1 scripts for Miri, Rust sanitizer, and coverage production. Keep Rust safety evidence distinct from the existing C++ Clang ASan/UBSan producer; upload Rust coverage, C++ coverage, and non-authoritative differential coverage separately with candidate commit, toolchain, producer workflow/job/run ID, and payload hash. Upload only after the scripts' Plan 12-10 typed validator calls and identity-last completion. Add workflow-level negative assertions in the existing xtask workflow test surface or static contract checks for floating action/toolchain pins, missing candidate SHA, merged coverage artifacts, C++ startup in Miri, validation after upload, and absent finite timeouts. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>actionlint .github/workflows/safety.yml .github/workflows/coverage.yml && rg -n "nightly-2026-07-15|timeout-minutes|phase12-(miri|rust-sanitizers|coverage)\\.sh" .github/workflows/safety.yml .github/workflows/coverage.yml && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>Both workflows parse, use full action SHAs, exact dated nightly, full candidate identity, and finite timeouts; Miri has no C++ startup; Rust/C++/differential outputs have separate names and upload only after typed validation.</acceptance-criteria>
  <done>Expensive safety and coverage evidence runs independently and is ready for same-commit release aggregation.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| unstable tools and native instrumentation to evidence scripts | Tool output can be incomplete, mixed, or misclassified. |
| scripts to CI artifacts | Partial or unvalidated output must not become release evidence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-24-01 | Spoofing | toolchain/candidate identity | mitigate | Date-pin nightly, require full candidate SHA, and bind resolved tool identities. |
| T-12-24-02 | Tampering | coverage artifact | mitigate | Invoke the shared typed validator, hash payloads, and write identity last. |
| T-12-24-03 | Repudiation | coverage claim | mitigate | Retain separate evidence kinds, missed leaves, and explicit no-parity marker. |
| T-12-24-04 | Denial of service | expensive suites | mitigate | Allowlisted subsets, finite script limits, and workflow timeouts. |
</threat-model>

<verification>Run script check modes, workflow lint/static contracts, exact validator-command grep, and the ordered Rust gate.</verification>
<success-criteria>Safety and coverage producers are isolated from contract design, invoke one typed validation authority, and cannot merge runtime evidence or assert parity.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-24-SUMMARY.md`.</output>
