---
phase: 12-performance-portability-and-release-hardening
plan: "23"
type: execute
wave: 7
depends_on: ["12-08"]
files_modified:
  - .github/workflows/performance.yml
  - tools/xtask/tests/performance_workflow.rs
autonomous: true
requirements: ["PERF-04", "PERF-05", "PERF-06"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T16:41:48Z
must_haves:
  truths:
    - "Scheduled and manual performance runs capture an explicit full candidate SHA and controlled-host identity before measurement."
    - "Only validated complete fourteen-workload performance reports are uploaded with producer workflow, job, run ID, and payload hash."
    - "A generic shared-host trend run cannot be promoted into controlled release-claim evidence."
  artifacts:
    - path: ".github/workflows/performance.yml"
      provides: "Commit-bound controlled-host performance evidence producer"
      contains: "workflow_dispatch"
    - path: "tools/xtask/tests/performance_workflow.rs"
      provides: "Workflow identity, ordering, pinning, and artifact-cardinality contract"
      contains: "controlled"
  key_links:
    - from: ".github/workflows/performance.yml"
      to: "scripts/phase12-performance.sh"
      via: "calibrate, paired, then validate producer sequence"
      pattern: "phase12-performance\\.sh"
    - from: ".github/workflows/performance.yml"
      to: "cargo xtask performance validate"
      via: "validation before identity-last upload"
      pattern: "performance validate"
---

<objective>
Produce commit-bound controlled-host performance artifacts through an isolated workflow.

Purpose: Complete the D-05/D-12 producer boundary after Plan 12-08 has made analysis and local execution independently testable.
Output: Full-SHA-pinned performance workflow and negative workflow contract tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/verification.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-08-SUMMARY.md
@scripts/phase12-performance.sh
@tools/xtask/src/performance.rs
@reference/performance/manifest.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Produce validated controlled-host performance artifacts</name>
  <files>.github/workflows/performance.yml, tools/xtask/tests/performance_workflow.rs</files>
  <read-first>Read both modified files or record absence, the Plan 12-08 runner and commands, performance validator/manifest, existing full-SHA action pins, release-candidate workflow input conventions, D-05, and D-12 before editing.</read-first>
  <behavior>
    - "Manual release-candidate runs require a full candidate SHA and reviewed controlled-host label plus identity."
    - "Scheduled shared-host runs are trend diagnostics and cannot set release-reviewed status."
    - "Upload occurs only after fourteen-workload validation and identity-last construction."
  </behavior>
  <action>Per D-05 and D-12, add a scheduled/manual full-SHA-pinned performance workflow. Require manual release-candidate inputs for full candidate SHA and reviewed controlled-host label/identity; scheduled runs may produce trend diagnostics but cannot set release-reviewed status. Checkout detached at the candidate, invoke `scripts/phase12-performance.sh calibrate`, `paired`, then `validate`, bind scenario/policy/compiler/linker/target/flags/hardware identities, validate the complete fourteen-workload report through `cargo xtask performance validate`, write identity last, and upload raw samples, summary, manifest entry, producer workflow/job, and `${{ github.run_id }}` under one candidate-scoped artifact name. Add negative workflow tests for missing or short candidate SHA, absent controlled-host identity, shared-host claim promotion, validation after upload, missing run ID, floating action tags, wrong command order, and wrong artifact cardinality. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>actionlint .github/workflows/performance.yml && cargo test -p xtask --test performance_workflow && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>The workflow uses full action SHAs, explicit full candidate and controlled-host inputs, invokes the Plan 12-08 runner in calibrate/paired/validate order, validates before upload, captures run ID, and emits one complete candidate-bound artifact set; all negative tests pass.</acceptance-criteria>
  <done>A dedicated producer emits only complete validated performance artifacts for later release aggregation.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| controlled host and workflow inputs to performance artifact | Host or candidate substitution could create false release claims. |
| validated report to artifact upload | Upload ordering could publish partial or unvalidated evidence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-23-01 | Spoofing | host/candidate identity | mitigate | Require full candidate SHA, reviewed controlled-host identity, and detached checkout. |
| T-12-23-02 | Tampering | workflow ordering | mitigate | Contract-test calibrate/paired/validate and identity-last upload order. |
| T-12-23-03 | Repudiation | producer provenance | mitigate | Persist workflow, job, run ID, payload hash, and exact artifact cardinality. |
| T-12-23-04 | Elevation of privilege | shared-host trend result | mitigate | Closed status model forbids trend diagnostics from becoming reviewed claim evidence. |
</threat-model>

<verification>Lint the workflow, run all negative workflow-contract tests, and run the exact ordered Rust commit gate.</verification>
<success-criteria>The performance producer is independently scoped, fully pinned, candidate-bound, controlled-host-aware, and incapable of uploading unvalidated claim evidence.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-23-SUMMARY.md`.</output>
