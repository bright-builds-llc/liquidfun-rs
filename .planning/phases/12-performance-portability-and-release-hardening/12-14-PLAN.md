---
phase: 12-performance-portability-and-release-hardening
plan: "14"
type: execute
wave: 8
depends_on: ["12-23", "12-24", "12-25", "12-12", "12-13"]
files_modified:
  - BENCHMARKING.md
  - COMPATIBILITY.md
  - reference/compatibility.json
  - tools/xtask/src/inventory/report.rs
  - tools/xtask/src/inventory/validation.rs
  - tools/xtask/tests/inventory_cli.rs
autonomous: true
requirements: ["COMP-10", "PERF-06", "PLAT-06", "DOCS-04", "DOCS-06"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "Compatibility report exposes every evidence dimension, known difference, tolerance scope, platform tier, and unexplained-gap count."
    - "Benchmarking methodology and claims resolve to immutable machine reports and never generalize beyond workload/hardware evidence."
    - "Zero-gap means no applicable unexplained compatibility row and no nonterminal upstream corpus item."
  artifacts:
    - path: "BENCHMARKING.md"
      provides: "Paired methodology, calibration, profiling, interpretation, and claim rules"
      contains: "max(3%, calibrated noise floor)"
    - path: "COMPATIBILITY.md"
      provides: "Generated complete traceability and no-gap status"
      contains: "Unexplained gaps"
  key_links:
    - from: "tools/xtask/src/inventory/validation.rs"
      to: "reference/upstream-corpus.json"
      via: "terminal corpus and compatibility join"
      pattern: "corpus"
---
<objective>
Project performance, platform, tolerance, corpus, and compatibility authorities into truthful public reports.

Purpose: Complete COMP-10, PERF-06, PLAT-06, DOCS-04, and DOCS-06 before the final release audit.
Output: BENCHMARKING.md and a generated compatibility report with executable zero-gap semantics.
</objective>
<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@.planning/phases/12-performance-portability-and-release-hardening/12-23-SUMMARY.md
@.planning/phases/12-performance-portability-and-release-hardening/12-24-SUMMARY.md
@.planning/phases/12-performance-portability-and-release-hardening/12-25-SUMMARY.md
@.planning/phases/12-performance-portability-and-release-hardening/12-12-SUMMARY.md
@reference/compatibility.json
@reference/upstream-corpus.json
@protocol/tolerances
@tools/xtask/src/inventory/report.rs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce zero-gap compatibility and truthful generated status</name>
  <files>COMPATIBILITY.md, reference/compatibility.json, tools/xtask/src/inventory/report.rs, tools/xtask/src/inventory/validation.rs, tools/xtask/tests/inventory_cli.rs</files>
  <read-first>Read every modified file, compatibility/corpus schemas, all evidence identity validators, platform support manifest, and COMP-10/DOCS-04.</read-first>
  <behavior>
    - "Every applicable row has terminal implementation/test/differential/documentation disposition or an explicit reviewed difference/unsupported rationale."
    - "Every upstream corpus item has one terminal reviewed outcome and its compatibility impact resolves."
    - "Unexplained gap count is computed, never entered manually."
  </behavior>
  <action>Per D-16, extend inventory validation with a release-readiness projection that derives the authoritative compatibility-row set and count directly from `reference/compatibility.json`, then joins every ledger identity to the corpus, evidence manifests, tolerance profiles, and platform tiers. Do not hardcode 177, 181, or any other row total. Preserve eight independent dimensions; use set equality and per-identity cardinality checks so no row can be dropped, duplicated, or joined twice. Compute counts for unexplained gaps, nonterminal corpus items, stale/mixed evidence, reviewed differences, intentional unsupported items, D1 rows, and D2 platform rows. Reject missing/duplicate joins, empty rationales/references, performance/coverage as parity authority, and platform fixture promotion. Generate COMPATIBILITY.md deterministically with explicit no-gap status only when computed counts are zero; update ledger evidence only from accepted immutable Phase 12 artifacts, never from local success. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then run `just markdown-check`. Do not commit a failing RED state.</action>
  <verify><automated>cargo test -p xtask --test inventory_cli && cargo xtask inventory check-report && cargo xtask inventory corpus check-closure && cargo xtask inventory generate && git diff --exit-code -- COMPATIBILITY.md && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && just markdown-check</automated></verify>
  <acceptance-criteria>Negative tests reject one omitted identity, one duplicate identity, one unexplained row, one nonterminal corpus item, mixed commit evidence, empty rationale, coverage-as-parity, and D2 promotion; generated report count equals the ledger-derived identity cardinality without a fixed constant and is byte-stable.</acceptance-criteria>
  <done>Compatibility and corpus closure have one fail-closed machine definition and deterministic public projection.</done>
</task>
<task type="auto">
  <name>Task 2: Document reproducible paired benchmarking and bounded claims</name>
  <files>BENCHMARKING.md</files>
  <read-first>Read the modified file or record absence, performance policy/matrix/manifest, profile API, scripts, and PERF-06/DOCS-06.</read-first>
  <action>Per D-05, create BENCHMARKING.md with the 14 workloads and size sweeps, sealed resolved-byte identity, scalar release builds, same-host interleaving, excluded setup boundaries, at least five baselines, 95% intervals, `max(3%, calibrated noise floor)`, unprofiled authority, diagnostic parent/child profiles, optimization admission, hardware/tool/flag capture, raw report reproduction commands, result interpretation, compatibility status, and claim rules. Public numbers must link an immutable report and name workload/version/flags/hardware/interval; include no unsupported universal faster claim. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then run `just markdown-check`.</action>
  <verify><automated>rg -n "14|max\\(3%, calibrated noise floor\\)|95%|unprofiled|immutable" BENCHMARKING.md && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && just markdown-check</automated></verify>
  <acceptance-criteria>All exact methodology tokens and reproducible commands are present; mdformat passes; any example claim names a workload and immutable report and is explicitly non-generalized.</acceptance-criteria>
  <done>Performance methodology and claim boundaries are complete and reproducible.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| machine evidence to public compatibility/performance reports | Generated claims must not omit gaps or broaden scoped results. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-14-01 | Spoofing | no-gap status | mitigate | Compute from exhaustive joined authorities; forbid manual marker. |
| T-12-14-02 | Tampering | generated reports | mitigate | Deterministic generation and clean diff checks. |
| T-12-14-03 | Repudiation | performance claims | mitigate | Immutable report links and exact scope fields. |
</threat-model>
<verification>Run inventory/corpus/report tests, regenerate twice, markdown check, and review all computed counts and claims.</verification>
<success-criteria>Public compatibility and benchmark documents are complete projections of machine authority with no hidden gap or generalized claim.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-14-SUMMARY.md`.</output>
