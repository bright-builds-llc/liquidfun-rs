---
phase: 12-performance-portability-and-release-hardening
plan: "25"
type: execute
wave: 3
depends_on: ["12-09", "12-10"]
files_modified:
  - scripts/phase12-regressions.sh
  - .github/workflows/regressions.yml
  - tools/xtask/tests/regression_workflow.rs
autonomous: true
requirements: ["TEST-07"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T17:15:00Z
must_haves:
  truths:
    - "Every registered named corrected differential regression executes against its exact minimized input and oracle/tolerance provenance for one full candidate SHA."
    - "The regression producer invokes Plan 12-10's validate-regressions command before writing identity last."
    - "Regression evidence uploads as one distinct candidate-bound artifact and cannot be merged with fuzz, sanitizer, or coverage evidence."
  artifacts:
    - path: "scripts/phase12-regressions.sh"
      provides: "Bounded registered named-regression evidence producer"
      contains: "safety-evidence validate-regressions"
    - path: ".github/workflows/regressions.yml"
      provides: "Scheduled/manual candidate-bound TEST-07 producer"
      contains: "phase12-regressions.sh"
    - path: "tools/xtask/tests/regression_workflow.rs"
      provides: "Producer order, identity, and artifact-cardinality contract"
      contains: "validate-regressions"
  key_links:
    - from: "scripts/phase12-regressions.sh"
      to: "reference/regressions/manifest.toml"
      via: "closed manifest enumeration of reviewed named test paths"
      pattern: "validate-regressions"
    - from: ".github/workflows/regressions.yml"
      to: "scripts/phase12-regressions.sh"
      via: "single producer invocation before candidate-scoped upload"
      pattern: "phase12-regressions\\.sh"
---

<objective>
Produce executable TEST-07 evidence for every registered corrected differential mismatch.

Purpose: Turn Plan 12-10's regression validation contract and Plan 12-09's minimized handoff into a concrete candidate-bound producer.
Output: One bounded local script, one isolated workflow, and owned workflow-contract tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-09-SUMMARY.md
@.planning/phases/12-performance-portability-and-release-hardening/12-10-SUMMARY.md
@reference/regressions/manifest.toml
@tools/xtask/src/safety_evidence.rs
@.github/workflows/fuzz.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute every registered named regression and validate its evidence</name>
  <files>scripts/phase12-regressions.sh, tools/xtask/tests/regression_workflow.rs</files>
  <read-first>Read both modified files or record absence, the Plan 12-10 regression validator/manifest, Plan 12-09 minimized-regression metadata, existing differential replay entrypoints, and D-13/TEST-07 before editing.</read-first>
  <behavior>
    - "Only reviewed manifest entries with a concrete named test path and exact minimized input hash execute."
    - "Every executed result binds candidate SHA, test path, input hash, oracle identity, tolerance identity, first-divergence signature, fix commit, and pass/fail disposition."
    - "validate-regressions succeeds before identity exists; a failed, missing, duplicate, or unregistered test leaves no complete artifact."
  </behavior>
  <action>Per D-13 and TEST-07, create `scripts/phase12-regressions.sh` with `set -euo pipefail`, closed `check|run` modes, a required canonical full 40-hex candidate SHA for `run`, finite per-test and total budgets, and output confined to the normalized directory `target/phase12-regressions/<candidate>`. Parse no manifest fields in shell: use `cargo xtask safety-evidence validate-regressions --emit-execution-list` to obtain the closed reviewed named-test/minimized-input/provenance set. Execute each registered named regression once with no retries, capture bounded logs and typed per-test results, reject zero tests or any missing/duplicate/unregistered result, and write the result-set completion marker. Only then invoke the exact typed command `cargo xtask safety-evidence validate-regression-results --candidate "$candidate_sha" --results "target/phase12-regressions/$candidate_sha"`; do not pass a caller-selected path. Write candidate/workflow identity last only after that command succeeds. Add Arrange/Act/Assert contract tests that execute the script with a confined fixture/fake command recorder and prove the exact order `complete result set -> validate-regression-results -> identity`; cover empty registration, omitted/duplicated/unregistered result, wrong input/oracle/tolerance/fix identity, validator-before-completion, validator-after-identity, alternate/escaping results path, retry loops, and unbounded commands. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>bash -n scripts/phase12-regressions.sh && scripts/phase12-regressions.sh check && cargo test -p xtask --test regression_workflow && rg -n 'cargo xtask safety-evidence validate-regression-results --candidate.*--results.*target/phase12-regressions' scripts/phase12-regressions.sh && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>The script executes the typed registry-provided named regression set, completes the full result set in exactly `target/phase12-regressions/<full-sha>`, invokes the typed confined results command with that exact candidate/path only after completion, and creates identity only after validation succeeds; focused tests invoke/record the same command and reject every ordering, confinement, completeness, or identity violation; the ordered Rust gate passes.</acceptance-criteria>
  <done>Every registered corrected mismatch has a bounded executable producer path and candidate-bound validated result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish one distinct candidate-bound regression artifact</name>
  <files>.github/workflows/regressions.yml, tools/xtask/tests/regression_workflow.rs</files>
  <read-first>Read both modified files, Task 1's script/contract tests, existing full-SHA action pins, fuzz/safety/coverage producer conventions, and D-12/D-13 before editing.</read-first>
  <behavior>
    - "Manual release-candidate runs require an explicit full candidate SHA; scheduled runs bind the resolved full SHA before execution."
    - "The workflow invokes the regression script once and uploads exactly one candidate-scoped regression artifact after typed validation and identity-last completion."
    - "Regression evidence has a distinct artifact name/kind and cannot share a directory or upload with fuzz, sanitizer, or coverage evidence."
  </behavior>
  <action>Per D-12 and D-13, create a scheduled/manual workflow with full action SHAs, detached checkout at the exact full candidate SHA, finite job timeout, no deterministic retry, and `persist-credentials: false`. Invoke only `scripts/phase12-regressions.sh run <candidate>`, then independently check that result-set completion precedes the script's exact `cargo xtask safety-evidence validate-regression-results --candidate <candidate> --results target/phase12-regressions/<candidate>` invocation, validation precedes identity, and identity precedes upload. The owned workflow-contract tests must execute the confined results command against their valid fixture and use a command recorder/static workflow projection to prove the same completion/validation/identity/upload order. Require the payload to bind candidate, producer workflow/job, `${{ github.run_id }}`, manifest hash, named-test cardinality, and payload hash. Upload exactly one artifact named `phase12-regressions-<candidate>` from the confined regression output; do not include fuzz, sanitizer, coverage, arbitrary repository, or unvalidated paths. Extend the tests to reject short/missing candidate SHA, floating actions, missing timeout/run ID, wrong command cardinality, alternate results paths, upload before validation/identity, multiple or merged uploads, and a non-candidate-scoped artifact name. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>actionlint .github/workflows/regressions.yml && cargo test -p xtask --test regression_workflow && rg -n "timeout-minutes|github\\.run_id|phase12-regressions\\.sh|phase12-regressions-" .github/workflows/regressions.yml && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>The workflow parses, uses full action SHAs and a full candidate identity, invokes the producer exactly once, and uploads exactly one distinct candidate-scoped artifact only after the workflow-contract tests prove result completion, exact confined results-command invocation, validation success, and identity creation in that order; all negative confinement, ordering, identity, and upload-cardinality tests pass.</acceptance-criteria>
  <done>TEST-07 regression evidence is independently produced, immutable, and ready for release aggregation.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| reviewed regression registry to test execution | Missing or substituted named tests could falsely claim corrected mismatches remain fixed. |
| validated local payload to CI artifact | Mixed or partial output must not become release evidence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-25-01 | Spoofing | candidate/regression identity | mitigate | Bind full candidate, exact minimized input, oracle/tolerance, fix, test path, and manifest hashes. |
| T-12-25-02 | Tampering | result completeness | mitigate | Set-equality validation rejects omitted, duplicate, and unregistered results before identity. |
| T-12-25-03 | Repudiation | producer provenance | mitigate | Persist workflow, job, run ID, payload hash, and exact one-artifact cardinality. |
| T-12-25-04 | Denial of service | named regression execution | mitigate | Closed registry, finite per-test/total budgets, no retries, and confined outputs. |
</threat-model>

<verification>Run script check mode, workflow lint, owned negative contract tests, exact validator-command grep, and the ordered Rust gate.</verification>
<success-criteria>Every registered corrected mismatch executes as a named regression and yields one validated candidate-bound artifact with complete provenance.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-25-SUMMARY.md`.</output>
