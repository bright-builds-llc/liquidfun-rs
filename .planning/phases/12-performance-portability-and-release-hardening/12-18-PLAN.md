---
phase: 12-performance-portability-and-release-hardening
plan: "18"
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - crates/liquidfun-testbed/Cargo.toml
  - Cargo.lock
  - deny.toml
  - crates/liquidfun-testbed/CAPABILITY.md
autonomous: true
requirements: ["API-12", "DOCS-07", "DOCS-09"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T16:17:43Z
must_haves:
  truths:
    - "Macroquad and ttf-parser are absent from the workspace dependency graph."
    - "Both temporary advisory waivers are removed without adding a replacement waiver."
    - "The capability report identifies the exact replacement renderer and preserves parity/performance exclusions."
  artifacts:
    - path: "deny.toml"
      provides: "Zero-waiver advisory policy"
      contains: "ignore = []"
    - path: "crates/liquidfun-testbed/CAPABILITY.md"
      provides: "Replacement renderer capability evidence"
      contains: "eframe-egui-0.35.0+tiny-skia-0.12.0"
  key_links:
    - from: "crates/liquidfun-testbed/Cargo.toml"
      to: "Cargo.lock"
      via: "Macroquad removal and exact replacement dependency closure"
      pattern: "eframe"
---

<objective>
Remove the retired renderer dependency and its temporary advisory waivers after the replacement shell compiles.

Purpose: Complete the inherited security obligation at a narrow dependency-policy boundary.
Output: A Macroquad-free workspace, clean cargo-deny policy, and exact replacement capability record.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/verification.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-03-SUMMARY.md
@deny.toml
@crates/liquidfun-testbed/Cargo.toml
@crates/liquidfun-testbed/CAPABILITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Macroquad and both temporary advisory waivers</name>
  <files>crates/liquidfun-testbed/Cargo.toml, Cargo.lock, deny.toml, crates/liquidfun-testbed/CAPABILITY.md</files>
  <read-first>Read every modified file, `.codex/tasks/todo.md` renderer task, Plan 11-29's waiver decision, and the passing Plan 12-03 dependency tree.</read-first>
  <action>Per D-15, delete `macroquad = "=0.4.15"`, update the lockfile through Cargo, set `advisories.ignore = []`, and rewrite CAPABILITY.md to name the exact replacement versions and hashes while preserving explicit renderer/performance/parity exclusions. Confirm neither `macroquad` nor its transitive `ttf-parser` remains in `cargo tree --workspace`; do not add an alternative waiver. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then run `just markdown-check`. Do not commit a failing RED state.</action>
  <verify>
    <automated>! cargo tree --workspace | rg "macroquad|ttf-parser" && cargo deny --locked check && cargo xtask package verify && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && just markdown-check</automated>
  </verify>
  <acceptance-criteria>`Cargo.toml` and `Cargo.lock` contain no Macroquad; `deny.toml` contains `ignore = []`; cargo-deny, package verification, ordered Rust gate, and Markdown check exit 0.</acceptance-criteria>
  <done>The workspace has no renderer advisory waiver and the published package remains renderer-free.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| private dependency graph to release gate | Unmaintained or unsound dependencies must not be hidden by waivers. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-18-01 | Denial of service | dependency unsoundness | mitigate | Remove Macroquad, ttf-parser, and both advisory ignores; require cargo-deny. |
| T-12-18-02 | Spoofing | capability record | mitigate | Bind exact replacement versions and verified capture hashes. |
</threat-model>

<verification>Run dependency grep, cargo-deny, package verification, Markdown check, and the ordered Rust gate.</verification>
<success-criteria>Macroquad, ttf-parser, and their waivers are absent while replacement capability evidence stays exact.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-18-SUMMARY.md`.</output>
