---
phase: 12-performance-portability-and-release-hardening
plan: "10"
type: execute
wave: 2
depends_on: ["12-17"]
files_modified:
  - tools/xtask/src/safety_evidence.rs
  - tools/xtask/src/safety_evidence/contract.rs
  - tools/xtask/src/main.rs
  - tools/xtask/tests/safety_evidence_contract.rs
  - reference/regressions/manifest.toml
  - reference/coverage/contract.json
autonomous: true
requirements: ["API-12", "TEST-06", "TEST-07", "TEST-08"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "Regression and coverage records parse through shared typed validators used by both CLI scripts and the release audit."
    - "Rust sanitizer, C++ ASan/UBSan, Rust coverage, C++ coverage, and differential leaf coverage remain distinct evidence kinds."
    - "Closed xtask commands validate the tracked regression and coverage authorities without shell-owned parsing."
  artifacts:
    - path: "tools/xtask/src/safety_evidence/contract.rs"
      provides: "Reusable typed regression and coverage validation"
      contains: "parity_authority"
    - path: "reference/coverage/contract.json"
      provides: "Separate Rust/C++/differential coverage schema"
      contains: "differential"
  key_links:
    - from: "tools/xtask/src/main.rs"
      to: "tools/xtask/src/safety_evidence.rs"
      via: "closed validate-regressions and validate-coverage commands"
      pattern: "safety-evidence.*validate"
---
<objective>
Define the shared typed safety, minimized-regression, and coverage evidence boundary.

Purpose: Implement D-14/D-15 and TEST-06 through TEST-08 without crossing C++ failures into Rust or using coverage as parity.
Output: Reusable typed validators, closed xtask commands, regression registry, and coverage contract.
</objective>
<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@standards/core/testing.md
@standards/core/verification.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-17-SUMMARY.md
@TESTING.md
@.github/workflows/oracle.yml
@reference/artifacts/manifest.toml
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define safety, regression, and coverage evidence contracts</name>
  <files>tools/xtask/src/safety_evidence.rs, tools/xtask/src/safety_evidence/contract.rs, tools/xtask/src/main.rs, tools/xtask/tests/safety_evidence_contract.rs, reference/regressions/manifest.toml, reference/coverage/contract.json</files>
  <read-first>Read every modified file or record absence, root toolchain, existing artifact manifest, differential failure bundle contract, TESTING.md, and D-13 through D-15.</read-first>
  <action>Per D-13 and D-14, define regression manifest v1 with exact minimized bytes/hash, target, generator/toolchain, candidate/fix commits, oracle/tolerance identities, first-divergence signature, failure class, review status, and named test path. Define coverage contract v1 with distinct `rust`, `cpp`, and `differential` sections, toolchain/commit/artifact identities, per-subsystem exercised/missed files or leaves, and explicit `parity_authority: false`. Implement shared typed validators in `safety_evidence/contract.rs` and publicly expose them within xtask for direct Rust reuse by the release validator in Plan 12-15. Register closed `cargo xtask safety-evidence validate-regressions`, `cargo xtask safety-evidence validate-regression-results --candidate FULL_SHA --results PATH`, and `cargo xtask safety-evidence validate-coverage` commands in `main.rs`. The manifest command reads only the tracked reviewed registry and supports closed `--emit-execution-list`, which validates first and then emits the complete reviewed named-test/minimized-input/provenance set for Plan 12-25 without executing tests or accepting arbitrary paths. The distinct results command accepts only a canonical full 40-hex candidate SHA and a normalized regular directory exactly equal to `target/phase12-regressions/<candidate>` beneath the repository root; reject absolute paths, symlinks, `..`, alternate target directories, candidate/path mismatch, missing result-set completion marker, any preexisting identity record, and incomplete/duplicate/unregistered results before invoking the shared typed result-set validator. Add focused negative tests for missing/duplicate/mixed identity, unknown fields, wrong hashes, invalid failure classes, merged Rust/C++ coverage, coverage-as-parity, incomplete subsystem leaves, missing command registration, incomplete/unstable execution-list projection, every results-path escape/substitution, identity-before-validation, and any duplicated CLI-only validation path. Do not leave contract validation embedded only in shell or inaccessible from the CLI. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>cargo test -p xtask --test safety_evidence_contract && cargo xtask safety-evidence validate-regressions && cargo xtask safety-evidence validate-regressions --emit-execution-list > /dev/null && cargo xtask safety-evidence validate-regression-results --help && cargo xtask safety-evidence validate-coverage && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>All contracts parse only through shared typed validators; the tracked-registry, confined result-set, and coverage commands are registered; the regression execution-list projection is complete and deterministic; focused tests invoke the results command with a valid confined fixture and reject absolute/symlink/parent/alternate-root/candidate-mismatch paths, incomplete sets, and identity-before-validation; the release module can call the same Rust validators directly; the ordered Rust gate passes.</acceptance-criteria>
  <done>Safety, regression, and coverage artifacts have strict non-overlapping identities.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| CI tools to evidence artifacts | Tool output can be incomplete, mixed, or misclassified. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-10-01 | Spoofing | toolchain identity | mitigate | Date-pin nightly and bind exact Rust/LLVM/Clang versions. |
| T-12-10-02 | Tampering | regression/coverage artifact | mitigate | Hash payloads, validate complete identities, write identity last. |
| T-12-10-03 | Repudiation | coverage claim | mitigate | Retain missed subsystems/leaves and explicit no-parity marker. |
| T-12-10-04 | Denial of service | expensive suites | mitigate | Allowlisted subsets, finite budgets, scheduled/manual lanes. |
</threat-model>
<verification>Run the contract/CLI tests, both concrete tracked-record validation commands, schema negatives, and the ordered Rust gate; Plan 12-24 separately verifies the effectful evidence lanes.</verification>
<success-criteria>Safety and coverage evidence has one reusable typed validation authority and executable closed CLI paths before any scripts or workflows produce artifacts.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-10-SUMMARY.md`.</output>
