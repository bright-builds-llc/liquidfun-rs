---
phase: 12-performance-portability-and-release-hardening
plan: "09"
type: execute
wave: 2
depends_on: ["12-17"]
files_modified:
  - fuzz/Cargo.toml
  - fuzz/fuzz_targets/protocol.rs
  - fuzz/fuzz_targets/shapes_collision.rs
  - fuzz/fuzz_targets/world_mutation.rs
  - fuzz/fuzz_targets/particles.rs
  - fuzz/fuzz_targets/groups_ownership.rs
  - fuzz/src/lib.rs
  - fuzz/corpus/README.md
  - .github/workflows/fuzz.yml
autonomous: true
requirements: ["TEST-05", "TEST-07"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "Bounded fuzz targets exercise protocol parsing, collision, world mutation, and particles/groups through public or reviewed private boundaries."
    - "Any accepted failure can be minimized into a named provenance-bearing regression without confusing harness and physics failures."
  artifacts:
    - path: "fuzz/Cargo.toml"
      provides: "Private cargo-fuzz package outside workspace/default members"
      contains: "cargo-fuzz"
    - path: ".github/workflows/fuzz.yml"
      provides: "Scheduled and release-candidate time-bounded fuzz execution"
      contains: "workflow_dispatch"
  key_links:
    - from: "fuzz/fuzz_targets/protocol.rs"
      to: "liquidfun-test-protocol"
      via: "bounded strict decoders"
      pattern: "decode"
---
<objective>
Add bounded fuzz coverage and a reproducible minimized-regression handoff for the supported attack and mutation surfaces.

Purpose: Implement D-13, TEST-05, and TEST-07 without adding fuzzing to ordinary consumer builds.
Output: Five reviewed targets, seed corpus contract, and scheduled/release-candidate workflow.
</objective>
<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@standards/core/testing.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-17-SUMMARY.md
@crates/liquidfun-test-protocol/src/codec.rs
@crates/liquidfun/tests/world.rs
@crates/liquidfun/tests/phase10_group_api.rs
@.github/workflows/oracle.yml
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build five bounded fuzz targets and regression metadata</name>
  <files>fuzz/Cargo.toml, fuzz/src/lib.rs, fuzz/fuzz_targets/protocol.rs, fuzz/fuzz_targets/shapes_collision.rs, fuzz/fuzz_targets/world_mutation.rs, fuzz/fuzz_targets/particles.rs, fuzz/fuzz_targets/groups_ownership.rs, fuzz/corpus/README.md</files>
  <read-first>Read every modified file or record absence, public shape/world/particle APIs, protocol limits, mutation property tests, failure taxonomy, and D-13 before editing.</read-first>
  <behavior>
    - "Protocol input is capped at 1 MiB and uses production strict decoders."
    - "Mutation programs are capped at 256 operations, 128 bodies/fixtures, 4096 particles, and 64 groups."
    - "No fuzz target uses C++, private dense indices, raw pointers, unchecked allocations, or unwrap."
  </behavior>
  <action>Per D-13, create a `cargo-fuzz` package excluded from the workspace with exact `libfuzzer-sys` and `arbitrary` versions compatible with `nightly-2026-07-15`. Register exactly five bins backed by exactly five concrete target files: `protocol.rs`, `shapes_collision.rs`, `world_mutation.rs`, `particles.rs`, and `groups_ownership.rs`. The fifth target exercises buffer ownership/capacity and handle invalidation rather than inventing an unsafe boundary. Convert bytes into bounded typed operations before effects, catch expected typed rejections, and treat panics/non-finite committed state/invariant failures as findings. Add one N and N+1 bounds test per target, including exact 1 MiB/256/128/4096/64 limits. Define minimized regression metadata fields: target, SHA-256 input, exact bytes path, generator/toolchain, candidate commit, oracle/policy identity when applicable, Harness|PhysicsMismatch|Sanitizer|Timeout|Schema classification, and fix reference. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then run `just markdown-check`. Do not commit a failing RED state.</action>
  <verify><automated>cargo +nightly-2026-07-15 fuzz build && cargo test --manifest-path fuzz/Cargo.toml && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && just markdown-check</automated></verify>
  <acceptance-criteria>All five named target binaries build; each has N/N+1 bounds tests; no target contains `unsafe`, `unwrap()`, or C++ startup; ordered Rust and Markdown gates pass.</acceptance-criteria>
  <done>Every required fuzz surface is bounded, reproducible, and connected to typed failure classification.</done>
</task>
<task type="auto">
  <name>Task 2: Run fuzzing in isolated scheduled and release-candidate lanes</name>
  <files>.github/workflows/fuzz.yml</files>
  <read-first>Read the modified workflow, `.github/workflows/ci.yml`, `.github/workflows/oracle.yml`, action pins, fuzz Cargo metadata, and D-12/D-13.</read-first>
  <action>Per D-12 and D-13, create a full-SHA-pinned workflow for schedule and explicit `workflow_dispatch` release-candidate SHA. Install and invoke only `nightly-2026-07-15`; never use a floating nightly alias. Keep PRs to `cargo fuzz build`; run each target for an explicit 600 seconds scheduled and 1800 seconds release-candidate, one job matrix with fail-fast false. Upload only minimized inputs, bounded logs, target/toolchain/commit identity, and classification; never upload arbitrary repository paths. Do not retry deterministic findings. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.</action>
  <verify><automated>actionlint .github/workflows/fuzz.yml && rg -n "nightly-2026-07-15|600|1800|persist-credentials: false|fail-fast: false" .github/workflows/fuzz.yml && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>Workflow parses, uses full action SHAs, has bounded scheduled/release budgets, records candidate SHA, uploads minimized evidence, and is absent from pull-request expensive execution.</acceptance-criteria>
  <done>Fuzz work is isolated, time-bounded, and produces regression-ready artifacts.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| arbitrary bytes to parsers/physics | Adversarial inputs can trigger excessive allocation, panic, or stale identity use. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-09-01 | Denial of service | fuzz inputs | mitigate | Pre-parse byte/operation/entity caps and CI timeouts. |
| T-12-09-02 | Tampering | promoted regression | mitigate | Hash exact minimized bytes and bind target/toolchain/commit/classification. |
| T-12-09-03 | Information disclosure | artifacts | mitigate | Upload only bounded minimized inputs/logs from confined directories. |
</threat-model>
<verification>Build all fuzz targets, execute bounded smoke iterations, lint workflow, and inspect dependency/package isolation.</verification>
<success-criteria>Fuzzing covers all required surfaces and every accepted finding has a complete provenance path to regression.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-09-SUMMARY.md`.</output>
