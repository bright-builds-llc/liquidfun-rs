---
phase: 12-performance-portability-and-release-hardening
plan: "12"
type: execute
wave: 2
depends_on: ["12-11"]
files_modified:
  - scripts/phase12-platform.sh
  - .github/workflows/platform.yml
  - .github/workflows/ci.yml
  - tools/xtask/tests/platform_workflow.rs
autonomous: true
requirements: ["FND-06", "PLAT-01", "PLAT-02", "PLAT-03", "PLAT-04", "PLAT-05", "PLAT-06"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "Canonical Linux creates one .crate; MSRV and all native targets verify those exact bytes."
    - "Supported-platform success produces D2 portability evidence and cannot overwrite canonical fixtures."
    - "Conditional macOS x86_64 absence causes a documented tier downgrade, not a stale claim."
  artifacts:
    - path: ".github/workflows/platform.yml"
      provides: "Artifact-first MSRV/native platform release-candidate matrix"
      contains: "aarch64-unknown-linux-gnu"
    - path: "tools/xtask/tests/platform_workflow.rs"
      provides: "Workflow identity, runner, and D2 authority contract"
      contains: "conditional"
  key_links:
    - from: ".github/workflows/platform.yml"
      to: "tools/xtask/src/package/artifact.rs"
      via: "downloaded archive and identity validation"
      pattern: "verify-artifact"
---
<objective>
Execute the artifact-first MSRV and native supported-platform contract in CI.

Purpose: Produce sustainable PLAT-01 through PLAT-06 evidence without mixing toolchains, sources, or numerical authority.
Output: Rerunnable platform script, release-candidate workflow, focused workflow tests, and a slimmed PR gate.
</objective>
<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@standards/core/verification.md
@.planning/phases/12-performance-portability-and-release-hardening/12-11-SUMMARY.md
@.github/workflows/ci.yml
@.github/workflows/oracle.yml
@reference/platform/support.json
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Fan one reviewed archive across MSRV and native runners</name>
  <files>scripts/phase12-platform.sh, .github/workflows/platform.yml, .github/workflows/ci.yml, tools/xtask/tests/platform_workflow.rs</files>
  <read-first>Read every modified file, package artifact commands, support manifest, current CI actions/pins, D-06 through D-10, and the exact platform requirement texts.</read-first>
  <behavior>
    - "One producer uploads liquidfun crate bytes and identity; no consumer runs cargo package."
    - "MSRV Linux uses 1.92.0; native platform jobs use 1.97.0."
    - "Every platform result includes archive hash, target, compiler, scalar mode, D2 tier, and candidate SHA."
  </behavior>
  <action>Per D-06 and D-10, create a full-SHA-pinned workflow with producer `ubuntu-24.04`, MSRV consumer `ubuntu-24.04`, durable native consumers `ubuntu-24.04`, `ubuntu-24.04-arm`, `macos-15` for ARM64, and `windows-2025`, plus conditional x86_64 execution only on `macos-15-intel`. Bind conditional availability, native runner identity, evidence timestamp, and 90-day freshness to `reference/platform/support.json`; implement a distinct downgrade job/test path for unavailable or expired evidence rather than selecting a retired or deprecated runner label. Per D-07 and D-08, the producer creates/uploads one archive, Rust 1.92 validates it only on canonical Linux, and Rust 1.97 consumers download the same SHA, extract, build/test all publishable features, run rustdoc, package isolation, and platform smoke tests without submodules. Per D-09, the script accepts only a target/tier/archive/identity tuple and writes D2 identity last; tests ban D1/promotion strings. Keep fast PR CI to Linux quality plus three mainstream default-feature smoke jobs; broad artifact evidence runs on schedule/manual release candidate. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>bash -n scripts/phase12-platform.sh && actionlint .github/workflows/platform.yml .github/workflows/ci.yml && cargo test -p xtask --test platform_workflow && rg -n "macos-15-intel|macos-15" .github/workflows/platform.yml && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>Workflow names all five target triples and exact Rust versions; macOS ARM uses `macos-15`, conditional x86_64 uses only `macos-15-intel`, and downgrade is tested separately against the 90-day policy; only producer contains `cargo package`; archive SHA reuse, D2-only output, submodule false, and full-SHA action pins are proven.</acceptance-criteria>
  <done>MSRV and platform evidence is artifact-first, native, isolated, and correctly tiered.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| producer artifact to heterogeneous runners | CI artifacts cross machine/OS trust boundaries. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-12-01 | Spoofing | downloaded archive | mitigate | Verify exact producer SHA and candidate identity before extraction. |
| T-12-12-02 | Tampering | runner result | mitigate | Identity-last signed-by-workflow metadata and exact target/compiler capture. |
| T-12-12-03 | Elevation of privilege | D2 to D1 | mitigate | Closed D2 schema and tests banning promotion fields. |
| T-12-12-04 | Repudiation | conditional support | mitigate | Explicit available/downgraded record with freshness and runner identity. |
</threat-model>
<verification>Lint workflows, run workflow-contract tests, local producer/consumer smoke, package isolation, and target-specific compile tests where available.</verification>
<success-criteria>All durable platforms and conditional macOS x86_64 have truthful, commit-bound artifact evidence and no platform run can alter canonical fixtures.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-12-SUMMARY.md`.</output>
