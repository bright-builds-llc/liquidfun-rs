---
phase: 12-performance-portability-and-release-hardening
plan: "17"
type: execute
wave: 1
depends_on: []
files_modified:
  - rust-toolchain-nightly.toml
  - tools/xtask/tests/nightly_toolchain.rs
autonomous: true
requirements: ["TEST-05", "TEST-06", "TEST-08"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T16:17:43Z
must_haves:
  truths:
    - "Fuzz, Miri, Rust sanitizer, and coverage lanes share one exact dated nightly."
    - "Floating nightly aliases are rejected from Phase 12 scripts and workflows."
    - "The pin declares every required component for the safety and coverage lanes."
  artifacts:
    - path: "rust-toolchain-nightly.toml"
      provides: "Shared exact nightly-2026-07-15 toolchain contract"
      contains: "nightly-2026-07-15"
    - path: "tools/xtask/tests/nightly_toolchain.rs"
      provides: "Floating-alias and component contract tests"
      contains: "nightly-2026-07-15"
  key_links:
    - from: "rust-toolchain-nightly.toml"
      to: ".github/workflows/fuzz.yml"
      via: "exact toolchain file consumption"
      pattern: "nightly-2026-07-15"
---

<objective>
Pin the one shared nightly used by all Phase 12 unstable-tool evidence lanes.

Purpose: Prevent fuzz, Miri, sanitizer, and coverage behavior from changing without a repository diff.
Output: Exact dated toolchain contract and focused repository-policy tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@rust-toolchain.toml
@tools/xtask/tests
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin and enforce the shared Phase 12 nightly</name>
  <files>rust-toolchain-nightly.toml, tools/xtask/tests/nightly_toolchain.rs</files>
  <read-first>Read both modified files or record absence, root stable toolchain policy, existing workflow toolchain setup, and D-12 through D-15 before editing.</read-first>
  <behavior>
    - "The channel is exactly nightly-2026-07-15."
    - "Components include miri, rust-src, and llvm-tools-preview."
    - "Phase 12 plans/scripts/workflows may not use +nightly, channel nightly, or an unexpanded nightly alias."
  </behavior>
  <action>Per D-12 through D-15, create `rust-toolchain-nightly.toml` with exact channel `nightly-2026-07-15`, minimal profile, and components `miri`, `rust-src`, and `llvm-tools-preview`. Add a focused repository test that parses the TOML, rejects any undated `nightly` alias in Phase 12 workflows/scripts/tool declarations, and exposes the exact channel string for Plans 12-09 and 12-10 to consume. Use test-first implementation, but do not commit a failing RED state. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.</action>
  <verify><automated>cargo test -p xtask --test nightly_toolchain && rustup run nightly-2026-07-15 rustc --version && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>The exact dated channel and all three components parse; tests reject floating nightly references; the ordered Rust gate exits 0.</acceptance-criteria>
  <done>All unstable-tool evidence has one checked-in reproducible toolchain predecessor.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| toolchain resolver to safety evidence | A floating toolchain can silently change evidence behavior. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-17-01 | Spoofing | toolchain identity | mitigate | Require exact dated channel and record the resolved rustc identity. |
| T-12-17-02 | Tampering | workflow toolchain selection | mitigate | Repository test rejects floating aliases in every consumer. |
</threat-model>

<verification>Parse the pin, resolve the exact nightly, reject floating aliases, and run the ordered Rust gate.</verification>
<success-criteria>Fuzz, Miri, sanitizer, and coverage work cannot float independently.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-17-SUMMARY.md`.</output>
