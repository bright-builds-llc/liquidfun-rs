---
phase: 12-performance-portability-and-release-hardening
plan: "03"
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - crates/liquidfun-testbed/src/bin/interactive.rs
  - crates/liquidfun-testbed/src/main.rs
  - crates/liquidfun-testbed/src/interactive.rs
  - crates/liquidfun-testbed/src/input.rs
  - crates/liquidfun-testbed/tests/comparison_lifecycle.rs
  - crates/liquidfun-testbed/tests/controller_ui.rs
autonomous: true
requirements: ["API-12", "DOCS-07", "DOCS-09"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "The desktop testbed runs through eframe/egui without Macroquad imports in the migrated shell."
    - "Controller commands, settings, comparison lifecycle, provenance links, and screenshots remain presentation-only."
    - "The entire workspace passes cargo-deny with no ignored advisory."
  artifacts:
    - path: "crates/liquidfun-testbed/src/bin/interactive.rs"
      provides: "Replacement desktop event shell"
      contains: "eframe"
    - path: "crates/liquidfun-testbed/tests/controller_ui.rs"
      provides: "Passive desktop-command and comparison regressions"
      contains: "SessionCommand"
  key_links:
    - from: "crates/liquidfun-testbed/src/bin/interactive.rs"
      to: "SessionCommand"
      via: "typed controller submission only"
      pattern: "SessionCommand"
---

<objective>
Complete the desktop-shell portion of the renderer migration.

Purpose: Close the interactive migration risk without weakening D-15 or moving simulation authority into UI code.
Output: An eframe/egui desktop shell whose simulation effects remain typed and controller-owned.
</objective>
<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-02-SUMMARY.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-30-SUMMARY.md
@deny.toml
@crates/liquidfun-testbed/src/bin/interactive.rs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Port the desktop shell without changing semantic authority</name>
  <files>crates/liquidfun-testbed/src/bin/interactive.rs, crates/liquidfun-testbed/src/main.rs, crates/liquidfun-testbed/src/interactive.rs, crates/liquidfun-testbed/src/input.rs, crates/liquidfun-testbed/tests/comparison_lifecycle.rs, crates/liquidfun-testbed/tests/controller_ui.rs</files>
  <read-first>Read every modified file, `renderer.rs`, all existing interactive tests, Plan 11-30's comparison lifecycle, and `SessionController`/`SessionCommand` definitions before editing.</read-first>
  <behavior>
    - "One eframe update renders current immutable state and emits only typed SessionCommand effects."
    - "Run advances only through explicit logical driver calls; repaint/frame time never advances physics."
    - "Comparison success/reset and generic-error isolation retain the Plan 11-30 behavior."
  </behavior>
  <action>Replace the Macroquad entry attribute, event loop, input polling, drawing, clipboard, quit, and frame presentation with `eframe::run_native`, one `eframe::App` implementation, and egui input/drawing APIs. Preserve stable catalog slug/version/seed selection, immutable staged settings until Apply Settings and Restart, Pause/Step/Run/Restart meanings, provenance URL allowlist, Overlay and Side-by-side modes, 35% exact overlay opacity, screenshot confinement beneath `target`, and `DesktopDiagnostics` transitions. Frame callbacks may request repaint but must never call simulation stepping except through the existing typed command driver. Split helpers out of the 2,400-line shell when cohesive, using `foo.rs` plus `foo/`; do not duplicate controller or comparator logic. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify>
    <automated>cargo test -p liquidfun-testbed --test comparison_lifecycle --test controller_ui --test interactive && cargo clippy -p liquidfun-testbed --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance-criteria>`interactive.rs` contains `impl eframe::App`; the listed files contain no `macroquad`; lifecycle, controller UI, and interactive tests pass; no renderer method directly calls `World::step`.</acceptance-criteria>
  <done>The desktop shell compiles and preserves the passive controller/comparison boundary.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| desktop events to typed commands | Untrusted user timing/input must not bypass controller validation. |
| private dependency graph to release gate | Unmaintained/unsound dependencies must not be hidden by waivers. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-03-01 | Elevation of privilege | frame callback | mitigate | Permit simulation effects only through validated SessionCommand paths. |
| T-12-03-02 | Tampering | comparison state | mitigate | Retain compiled lifecycle regressions and immutable comparison inputs. |
| T-12-03-03 | Information disclosure | links/screenshots | mitigate | Preserve HTTPS allowlist, bounded copy, and target-confined PNGs. |
| T-12-03-04 | Denial of service | dependency unsoundness | mitigate | Remove Macroquad, ttf-parser, and both advisory ignores; require cargo-deny. |
</threat-model>
<verification>Run focused UI tests and the ordered Rust gate; Plan 12-18 performs dependency and waiver removal after this shell compiles.</verification>
<success-criteria>The replacement desktop shell works and all semantic/controller boundaries remain green.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-03-SUMMARY.md`.</output>
