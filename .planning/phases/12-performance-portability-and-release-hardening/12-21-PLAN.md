---
phase: 12-performance-portability-and-release-hardening
plan: "21"
type: execute
wave: 11
depends_on: ["12-16"]
files_modified:
  - .github/workflows/release.yml
  - tools/xtask/src/release.rs
  - tools/xtask/src/release/attestation.rs
  - tools/xtask/src/main.rs
  - tools/xtask/tests/release_attestation.rs
  - tools/xtask/tests/release_cli.rs
autonomous: true
requirements: ["COMP-10", "DOCS-09", "PERF-04", "PERF-05", "PERF-06", "PLAT-01", "PLAT-02", "PLAT-03", "PLAT-04", "PLAT-05", "PLAT-06"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T16:17:43Z
must_haves:
  truths:
    - "A future completed release-candidate run retains every manifest-referenced artifact envelope and exact package archive needed to re-run the audit."
    - "Attestation validation is fail-closed when the real run-bound source, manifest, report, or payload set is absent."
    - "No tracked ready:true record exists before the independently produced release-candidate artifact set exists."
    - "The frozen-source/later-attestation validator never substitutes current HEAD for the source candidate supplied by a future evidence record."
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Complete future release-to-attestation evidence handoff"
      contains: "artifacts/*.json"
    - path: "tools/xtask/src/release/attestation.rs"
      provides: "Closed frozen-source worktree and committed attestation validators"
      contains: "validate-worktree"
    - path: "tools/xtask/tests/release_attestation.rs"
      provides: "Fail-closed absent, malformed, mixed-candidate, source-tree, payload, and diff tests"
      contains: "missing"
  key_links:
    - from: "tools/xtask/src/release.rs"
      to: "tools/xtask/src/release/attestation.rs"
      via: "closed release attestation command dispatch"
      pattern: "attestation"
    - from: ".github/workflows/release.yml"
      to: "tools/xtask/src/release/attestation.rs"
      via: "future bundle retains manifest-referenced payloads for later validation"
      pattern: "package/liquidfun.crate"
---

<objective>
Make the future release-evidence handoff complete and implement fail-closed attestation tooling without claiming that unavailable external evidence already exists.

Purpose: Preserve the frozen-source/later-attestation design while respecting the no-push/no-workflow-before-verification lifecycle.
Output: Complete future release bundle retention and strict attestation validation commands/tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/verification.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-16-SUMMARY.md
@reference/release/schema.json
@RELEASE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retain the complete future audit bundle and implement fail-closed attestation validation</name>
  <files>.github/workflows/release.yml, tools/xtask/src/release.rs, tools/xtask/src/release/attestation.rs, tools/xtask/src/main.rs, tools/xtask/tests/release_attestation.rs, tools/xtask/tests/release_cli.rs</files>
  <read-first>Read every modified file or record absence, Plan 12-16 output paths, release schema/validator/report, source-tree identity rules, D-11/D-16, and confirm whether real same-candidate producer artifacts exist before editing.</read-first>
  <behavior>
    - "The release upload includes candidate manifest, audit report/identity, all 19 referenced evidence envelopes, exact .crate bytes, and package identity."
    - "validate-worktree and validate share strict typed source/manifest/report validation and reject missing real payloads."
    - "The source candidate comes only from the supplied source record; source-tree identity is independently recomputed from Git."
    - "Worktree and committed diff checks allow only attestation-owned implementation/test/record paths and reject every other tracked or untracked path."
    - "No source-candidate.json, candidate-manifest.json, or audit-report.json is tracked by this plan because no real Plan 12-16 run exists."
  </behavior>
  <action>Repair the Plan 12-16 handoff by retaining `artifacts/*.json`, `package/liquidfun.crate`, package identity, manifest, report, and audit identity in the future release artifact. Declare the attestation child from `release.rs` and register the closed paths `cargo xtask release attestation validate-worktree --source PATH --manifest PATH --report PATH` and `cargo xtask release attestation validate --source PATH --manifest PATH --report PATH --attestation-commit COMMIT`. Both commands use one typed validator that requires a strict ready source record, full frozen SHA, independently recomputed Git source-tree hash, manifest/report byte hashes, exact candidate equality, the existing pure release audit over retained payloads, and exact report equality. The worktree command checks staged, unstaged, and untracked paths against the frozen source without resolving a future commit. The committed command checks the concrete frozen-source-to-attestation commit diff. Add negative tests for missing records/payloads, ready:false, current-HEAD candidate substitution through mixed identities, wrong source-tree hash, wrong manifest/report/payload hash, and non-attestation diff paths. Because the branch has not been pushed and no release-candidate run exists, do not create or track ready records and do not manufacture producer identities. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>actionlint .github/workflows/release.yml && cargo test -p xtask --test release_attestation && cargo test -p xtask --test release_cli && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>The future upload contains every byte the release audit reopens; attestation tests reject all absent/substituted/tampered boundaries; release help exposes both closed commands; no tracked ready record exists; and the ordered Rust gate passes.</acceptance-criteria>
  <done>The repository can truthfully attest a future real release bundle, while the current unrun candidate remains explicitly unattested.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| future workflow artifact to tracked attestation | Evidence could be omitted or substituted while being checked in. |
| source candidate to attestation commit | Attestation changes could silently alter audited source. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-21-01 | Spoofing | source candidate | mitigate | Require supplied full SHA and independently recompute its Git source-tree hash. |
| T-12-21-02 | Tampering | evidence handoff | mitigate | Retain every manifest-referenced payload and recompute manifest, report, artifact, and package hashes. |
| T-12-21-03 | Tampering | attestation commit | mitigate | Validate staged, unstaged, untracked, and committed diff paths against a closed allowlist. |
| T-12-21-04 | Repudiation | unavailable readiness | mitigate | Absence remains non-ready; no placeholder producer run or ready record is accepted. |
</threat-model>

<verification>Lint the workflow, run attestation/release negatives, prove ready records remain absent, and run the ordered Rust gate.</verification>
<success-criteria>The future evidence bundle is sufficient for real attestation and current readiness remains fail-closed until external producer runs exist.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-21-SUMMARY.md`.</output>
