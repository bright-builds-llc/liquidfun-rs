---
phase: 12-performance-portability-and-release-hardening
plan: "21"
type: execute
wave: 11
depends_on: ["12-16"]
files_modified:
  - tools/xtask/src/release.rs
  - tools/xtask/src/release/attestation.rs
  - tools/xtask/src/main.rs
  - tools/xtask/tests/release_attestation.rs
  - reference/release/source-candidate.json
  - reference/release/candidate-manifest.json
  - reference/release/audit-report.json
autonomous: true
requirements: ["COMP-10", "DOCS-09", "PERF-04", "PERF-05", "PERF-06", "PLAT-01", "PLAT-02", "PLAT-03", "PLAT-04", "PLAT-05", "PLAT-06"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T16:17:43Z
must_haves:
  truths:
    - "Tracked release records attest a frozen source-candidate SHA, never the later attestation commit's HEAD."
    - "Pre-commit validation checks the proposed attestation worktree and index against the frozen source candidate without pretending a future commit already exists."
    - "Every manifest/report hash and evidence identity is independently recomputed before tracking."
  artifacts:
    - path: "reference/release/source-candidate.json"
      provides: "Frozen source-candidate SHA and source-tree identity"
      contains: "source_candidate_commit"
    - path: "reference/release/audit-report.json"
      provides: "Later-commit attestation of the frozen source candidate"
      contains: "\"ready\": true"
  key_links:
    - from: "tools/xtask/src/main.rs"
      to: "tools/xtask/src/release/attestation.rs"
      via: "closed release attestation validate command"
      pattern: "release.*attestation.*validate"
    - from: "reference/release/audit-report.json"
      to: "reference/release/source-candidate.json"
      via: "exact frozen source_candidate_commit equality"
      pattern: "source_candidate_commit"
---

<objective>
Attest the validated source candidate in a later commit without changing what was audited.

Purpose: Resolve the candidate==HEAD self-reference by separating source construction from tracked attestation.
Output: Frozen source identity, accepted manifest/report, and attestation integrity tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/verification.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-16-SUMMARY.md
@reference/release/schema.json
@RELEASE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze and attest the validated source candidate</name>
  <files>tools/xtask/src/release.rs, tools/xtask/src/release/attestation.rs, tools/xtask/src/main.rs, tools/xtask/tests/release_attestation.rs, reference/release/source-candidate.json, reference/release/candidate-manifest.json, reference/release/audit-report.json</files>
  <read-first>Read every modified file or record absence, the fresh Plan 12-16 workflow artifacts, release schema/validator, source tree at the candidate SHA, and D-11/D-16.</read-first>
  <behavior>
    - "source_candidate_commit is an immutable full SHA distinct from or equal only by coincidence to the later attestation commit, never derived from current HEAD during validation."
    - "Manifest and report candidate fields equal the frozen source SHA and all payload hashes recompute."
    - "The proposed worktree/index diff from the source candidate is allowlisted to attestation-owned files; Plan 12-22 proves the committed HEAD diff afterward."
  </behavior>
  <action>Per D-11 and D-16, acquire one fresh complete Plan 12-16 artifact set and freeze its exact full source-candidate SHA plus source-tree hash in `source-candidate.json`. Independently recompute every archive/payload/report hash, tool/target/producer/run identity, ledger-derived compatibility identity set, corpus closure, notice/license/package list, and review status. Validate with `cargo xtask release audit --candidate <frozen-source-sha>`; never derive the audited candidate from the later attestation checkout. Declare the attestation child from `release.rs`, register both closed paths `cargo xtask release attestation validate-worktree --source PATH --manifest PATH --report PATH` and `cargo xtask release attestation validate --source PATH --manifest PATH --report PATH --attestation-commit COMMIT` in `main.rs`, and have both invoke the same typed validator. The worktree path must check full SHA form, exact candidate equality across all records, source-tree identity, and the actual staged-plus-unstaged diff against the frozen source candidate using an allowlist containing only the three tracked attestation records and attestation implementation/tests; it must not resolve future HEAD. The committed path retains the actual `<frozen-source-sha>..<attestation-commit>` check for dependent Plan 12-22, after this task has been committed. Store manifest/report only after worktree validation and publication dry run pass; if any evidence fails, retain `ready: false` and do not manufacture a success record. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>cargo test -p xtask --test release_attestation && cargo xtask release attestation validate-worktree --source reference/release/source-candidate.json --manifest reference/release/candidate-manifest.json --report reference/release/audit-report.json && cargo xtask release audit --manifest reference/release/candidate-manifest.json --candidate "$(jq -r .source_candidate_commit reference/release/source-candidate.json)" --output json && cargo publish -p liquidfun --dry-run && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>Negative tests reject current-HEAD substitution for the frozen candidate, a modified non-attestation worktree/index path, wrong source-tree hash, mixed candidate fields, and wrong payload hash; the concrete worktree command validates the proposed tracked records without inspecting a nonexistent future commit; the audit is evaluated against the frozen source SHA and the ordered Rust gate passes. Plan 12-22 owns the post-commit HEAD proof.</acceptance-criteria>
  <done>A later commit truthfully attests the exact source candidate that produced the accepted evidence.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| untracked workflow artifacts to tracked attestation | Evidence could be substituted while being checked in. |
| source candidate to attestation commit | Attestation changes could silently alter audited source. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-21-01 | Spoofing | source candidate | mitigate | Freeze full SHA and independent source-tree hash before attestation. |
| T-12-21-02 | Tampering | attestation commit | mitigate | Allowlist attestation-owned diff paths and recompute all hashes. |
| T-12-21-03 | Repudiation | readiness report | mitigate | Bind report, manifest, producer runs, and source identity immutably. |
</threat-model>

<verification>Run attestation negatives, frozen-SHA release audit, publication dry run, and ordered Rust gate.</verification>
<success-criteria>The tracked report is satisfiable, non-self-referential, and bound to unchanged audited source.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-21-SUMMARY.md`.</output>
