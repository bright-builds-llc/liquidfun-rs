---
phase: 12-performance-portability-and-release-hardening
plan: "06"
type: execute
wave: 2
depends_on: ["12-04", "12-05"]
files_modified:
  - crates/liquidfun-benchmarks/Cargo.toml
  - crates/liquidfun-benchmarks/src/lib.rs
  - crates/liquidfun-benchmarks/src/paired.rs
  - crates/liquidfun-benchmarks/benches/catalog.rs
  - crates/liquidfun-benchmarks/tests/catalog_equivalence.rs
  - crates/liquidfun-benchmarks/tests/performance_matrix.rs
  - crates/liquidfun-differential/src/performance.rs
  - crates/liquidfun-differential/src/performance/native.rs
  - crates/liquidfun-differential/src/lib.rs
autonomous: true
requirements: ["PERF-01", "PERF-02", "PERF-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "Criterion retains Rust-only diagnostic benchmarks over the complete matrix."
    - "A paired native executor consumes sealed resolved bytes and measures only declared logical actions."
    - "Setup, resolution, restart, capture, validation, and teardown are outside the authoritative wall-clock interval."
  artifacts:
    - path: "crates/liquidfun-benchmarks/src/paired.rs"
      provides: "Sealed measured-region executor"
      contains: "PairedBenchmarkCase"
    - path: "crates/liquidfun-benchmarks/tests/performance_matrix.rs"
      provides: "Fourteen-workload coverage and boundary tests"
      contains: "large_particle_system"
  key_links:
    - from: "crates/liquidfun-benchmarks/src/paired.rs"
      to: "ResolvedScenario"
      via: "exact canonical bytes/hash"
      pattern: "resolved_sha256"
---
<objective>
Expand the existing benchmark package into the complete Rust execution side of the paired contract.

Purpose: Deliver D-01/D-02 measured-region discipline without comparing Criterion's adaptive loop to C++.
Output: Complete Criterion diagnostics plus a sealed fixed-sample native paired executor.
</objective>
<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@standards/core/architecture.md
@.planning/phases/12-performance-portability-and-release-hardening/12-04-SUMMARY.md
@.planning/phases/12-performance-portability-and-release-hardening/12-05-SUMMARY.md
@crates/liquidfun-benchmarks/src/lib.rs
@crates/liquidfun-differential/src/runner/catalog.rs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Execute the complete matrix with sealed timing boundaries</name>
  <files>crates/liquidfun-benchmarks/Cargo.toml, crates/liquidfun-benchmarks/src/lib.rs, crates/liquidfun-benchmarks/src/paired.rs, crates/liquidfun-benchmarks/benches/catalog.rs, crates/liquidfun-benchmarks/tests/catalog_equivalence.rs, crates/liquidfun-benchmarks/tests/performance_matrix.rs, crates/liquidfun-differential/src/performance.rs, crates/liquidfun-differential/src/performance/native.rs, crates/liquidfun-differential/src/lib.rs</files>
  <read-first>Read every modified file, the Phase 12 performance types/matrix, native catalog executor, DiagnosticProfileSchema, and Phase 11 benchmark summary before editing.</read-first>
  <behavior>
    - "All 14 workload kinds and required size points register in Criterion diagnostics."
    - "Fixed paired samples alternate engine order at the caller contract but native timing measures only logical actions."
    - "Semantic or identity mismatch rejects the sample before it enters a report."
  </behavior>
  <action>Per D-01 through D-03, replace the seven-case constant with typed matrix loading and exact case preparation. Keep Criterion `iter_custom` Rust-only and label it diagnostic. Add `PairedBenchmarkCase::measure_native_sample` using a fresh preconstructed session, `Instant` immediately around the exact measured action horizon, post-timer semantic checkpoint validation, and optional Phase12V1 diagnostics captured separately. Bound warmups to 100, samples to 10,000, actions to 1,000,000, and size points to the sealed matrix. Add tests with injected clocks/counters proving excluded setup/restart/capture/teardown, complete registration, byte/hash validation, and package isolation. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>cargo test -p liquidfun-benchmarks --all-features && cargo bench -p liquidfun-benchmarks --bench catalog --no-run && cargo clippy -p liquidfun-benchmarks -p liquidfun-differential --all-targets --all-features -- -D warnings</automated></verify>
  <acceptance-criteria>Tests enumerate exactly 14 workload kinds and required size points; injected counters prove only declared actions occur under the timer; Criterion compiles; the liquidfun normal dependency tree remains unchanged.</acceptance-criteria>
  <done>The native and Criterion sides cover the full matrix with explicit, tested timing boundaries.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| matrix case to repeated execution | Validated inputs control expensive benchmark loops. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-06-01 | Tampering | measured interval | mitigate | Injected boundary tests count every timed and untimed operation. |
| T-12-06-02 | Denial of service | horizons/samples | mitigate | Closed matrix and explicit numeric caps. |
| T-12-06-03 | Spoofing | semantic success | mitigate | Reject hash/checkpoint drift before accepting duration. |
</threat-model>
<verification>Run benchmark tests, Criterion compile-only smoke, Clippy, dependency isolation, and two-run semantic determinism.</verification>
<success-criteria>Complete Rust benchmark coverage exists without self-blessing results or widening consumer dependencies.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-06-SUMMARY.md`.</output>
