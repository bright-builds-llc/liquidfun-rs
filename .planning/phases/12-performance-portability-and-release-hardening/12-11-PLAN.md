---
phase: 12-performance-portability-and-release-hardening
plan: "11"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/xtask/src/package.rs
  - tools/xtask/src/package/artifact.rs
  - tools/xtask/src/package/metadata.rs
  - tools/xtask/tests/package_cli.rs
  - reference/platform/support.json
  - reference/platform/schema.json
autonomous: true
requirements: ["FND-06", "PLAT-01", "PLAT-02", "PLAT-03", "PLAT-04", "PLAT-05", "PLAT-06"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "One reviewed .crate archive is verified at Rust 1.92 on canonical Linux and reused under Rust 1.97 across native platform lanes."
    - "Package verification remains Cargo-only, submodule-free, feature-complete, and source-isolated."
    - "macOS x86_64 support downgrades fail closed when sustainable native evidence is absent."
  artifacts:
    - path: "tools/xtask/src/package/artifact.rs"
      provides: "Content-addressed reusable .crate verification"
      contains: "PackageArtifactIdentity"
    - path: "reference/platform/support.json"
      provides: "Durable/conditional target and D2 evidence policy"
      contains: "x86_64-apple-darwin"
  key_links:
    - from: "tools/xtask/src/package/artifact.rs"
      to: "crates/liquidfun/Cargo.toml"
      via: "normalized rust-version/features/archive validation"
      pattern: "1.92"
---
<objective>
Make the publishable artifact and platform-support policy content-addressed before CI fan-out.

Purpose: Implement D-06 through D-10 and FND-06 without a wasteful two-toolchain-by-five-target matrix.
Output: Reusable `.crate` identity verifier, strict support manifest, and negative contract tests.
</objective>
<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@standards/core/verification.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@tools/xtask/src/package.rs
@tools/xtask/src/package/metadata.rs
@.github/workflows/ci.yml
@protocol/tolerances/phase4-v1.toml
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Verify one reusable publishable artifact and strict platform policy</name>
  <files>tools/xtask/src/package.rs, tools/xtask/src/package/artifact.rs, tools/xtask/src/package/metadata.rs, tools/xtask/tests/package_cli.rs, reference/platform/support.json, reference/platform/schema.json</files>
  <read-first>Read every modified file or record absence, current package CLI tests, root and crate Cargo manifests, Rust toolchain, Phase 4 tiers, and D-06 through D-10.</read-first>
  <behavior>
    - "Archive identity binds SHA-256, package/version, rust-version 1.92, exact feature set, source file list, license/notices, and candidate commit."
    - "Durable targets are x86_64-unknown-linux-gnu, aarch64-unknown-linux-gnu, aarch64-apple-darwin, and x86_64-pc-windows-msvc."
    - "x86_64-apple-darwin is conditional_supported and requires an unexpired native-runner evidence record."
  </behavior>
  <action>Per D-06, encode the four durable targets and conditional macOS x86_64 tier. Per D-07, add `package create-artifact` and `package verify-artifact --archive --identity --toolchain --target` under the existing package command, create the archive once with Rust 1.97, run its full publishable all-feature build/test at Rust 1.92 only on canonical Linux, and use Rust 1.97 on native platform targets. Per D-08, validate path/type/size/license/notices/normal dependencies/features/rust-version and require downstream builds/tests to consume the exact SHA-256 bytes after confined extraction. Per D-09, make platform results D2-only with no fixture-promotion field. Per D-10, encode a 90-day native evidence expiry and explicit downgrade outcome for missing/expired conditional macOS x86_64 evidence. Preserve scalar mode and compiler/tolerance classification throughout. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>cargo test -p xtask --test package_cli && cargo xtask package verify && cargo +1.92.0 check -p liquidfun --all-targets --all-features</automated></verify>
  <acceptance-criteria>Tests reject archive hash substitution, rust-version other than 1.92, missing feature, private/native source leakage, D1 promotion, and stale conditional evidence; package verify and MSRV check exit 0.</acceptance-criteria>
  <done>The exact consumer artifact and all support-tier rules are machine-verifiable before platform execution.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| built archive to platform runner | Runners must not substitute source or rebuild a different artifact. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-11-01 | Spoofing | package artifact | mitigate | SHA-256 and exact normalized metadata/source inventory. |
| T-12-11-02 | Tampering | support tier | mitigate | Closed durable/conditional enums and expiry validation. |
| T-12-11-03 | Elevation of privilege | platform evidence | mitigate | D2-only type with no fixture-promotion capability. |
| T-12-11-04 | Information disclosure | archive | mitigate | Existing path/type/content allowlists and external isolated build. |
</threat-model>
<verification>Run package CLI negatives, extracted package verification, Rust 1.92 check, source isolation, and support manifest schema checks.</verification>
<success-criteria>One content-addressed crate proves MSRV and feeds every supported platform without weakening Cargo-only isolation or D1 authority.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-11-SUMMARY.md`.</output>
