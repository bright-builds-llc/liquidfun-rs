---
phase: 12-performance-portability-and-release-hardening
plan: "07"
type: execute
wave: 3
depends_on: ["12-06"]
files_modified:
  - crates/liquidfun-test-protocol/src/performance.rs
  - crates/liquidfun-test-protocol/src/performance/wire.rs
  - crates/liquidfun-test-protocol/tests/performance_wire.rs
autonomous: true
requirements: ["PERF-02", "PERF-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "The performance wire contract binds identical resolved bytes, settings, horizon, policy, profile mode, and sample identity."
    - "Malformed, duplicate, oversized, or identity-incomplete benchmark records fail before either engine executes."
  artifacts:
    - path: "crates/liquidfun-test-protocol/tests/performance_wire.rs"
      provides: "Strict benchmark record and limit tests"
      contains: "benchmark_run"
  key_links:
    - from: "crates/liquidfun-test-protocol/src/performance.rs"
      to: "crates/liquidfun-test-protocol/src/performance/wire.rs"
      via: "explicit child-module declaration and public re-export"
      pattern: "pub mod wire|pub use wire"
---
<objective>
Define the strict benchmark wire contract consumed by both engine adapters.

Purpose: Freeze identity, bounds, and failure semantics before either effectful executor is implemented.
Output: Strict benchmark request/result records with focused codec and limit tests.
</objective>
<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@standards/core/architecture.md
@.planning/phases/12-performance-portability-and-release-hardening/12-06-SUMMARY.md
@tools/reference/src/catalog_run.cpp
@crates/liquidfun-differential/src/supervisor/catalog.rs
@crates/liquidfun-differential/src/runner.rs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add strict benchmark request and result records</name>
  <files>crates/liquidfun-test-protocol/src/performance.rs, crates/liquidfun-test-protocol/src/performance/wire.rs, crates/liquidfun-test-protocol/tests/performance_wire.rs</files>
  <read-first>Read every modified file, catalog request codecs, protocol limits, Phase 12 policy types, D-01 through D-03, and existing strict unknown-field tests before editing.</read-first>
  <action>Per D-01 and D-02, declare `pub mod wire;` in `performance.rs` and publicly re-export the reviewed request/result/error types so the crate's existing `performance` parent is the only supported import path; do not leave `wire.rs` as an orphan file. Add `benchmark_run_request` and `benchmark_run_result` records requiring request ID, exact resolved bytes/hash, settings, workload/size, scalar optimization mode, warmup count, measured horizon, sample ordinal, policy hash, profile-enabled flag, reset epoch, unprofiled nanoseconds, optional common-parent diagnostics, and semantic checkpoint identity. Enforce the reviewed limits during typed parsing, reject duplicate/unknown/missing fields and profiled totals as authority, and keep harness failures distinct from physics mismatches and performance results. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>cargo test -p liquidfun-test-protocol --test performance_wire && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>`performance.rs` declares and re-exports `wire`; protocol tests import the wire contract through the parent module, accept one complete pair, reject duplicate/unknown/oversized/mismatched records, preserve raw values, and distinguish all failure variants.</acceptance-criteria>
  <done>Both engine executors have one bounded, identity-complete wire contract.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| Rust supervisor to C++ oracle | Untrusted cross-process records control native execution. |
| raw measurements to report | Results can be mislabeled or generalized beyond their identity. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-07-01 | Spoofing | engine/report identity | mitigate | Validate role, revisions, tools, flags, target, hardware, scenario, and policy hashes. |
| T-12-07-02 | Denial of service | native requests | mitigate | Strict framing/depth/count/horizon deadlines and child kill/reap. |
| T-12-07-03 | Repudiation | paired ordering | mitigate | Persist sample ordinal, engine order, raw values, and reset epochs. |
| T-12-07-04 | Information disclosure | native diagnostics | mitigate | Protocol-only stdout and bounded stderr categories. |
</threat-model>
<verification>Run the focused wire contract tests and ordered Rust gate.</verification>
<success-criteria>The benchmark wire contract is strict, bounded, typed, and ready for independent Rust and C++ adapters.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-07-SUMMARY.md`.</output>
