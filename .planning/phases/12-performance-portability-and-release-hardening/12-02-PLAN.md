---
phase: 12-performance-portability-and-release-hardening
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - crates/liquidfun-testbed/src/capability/render.rs
  - crates/liquidfun-testbed/src/capability/input.rs
  - crates/liquidfun-testbed/src/capability/report.rs
  - crates/liquidfun-testbed/src/ui/viewport/draw.rs
  - crates/liquidfun-testbed/src/ui/protocol_viewport.rs
  - crates/liquidfun-testbed/tests/capability.rs
  - crates/liquidfun-testbed/tests/interactive.rs
autonomous: true
requirements: ["API-12", "DOCS-07"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "Offscreen capability evidence and semantic viewport drawing no longer import Macroquad."
    - "Existing resize, DPI, accessibility, overlays, and screenshot exclusions remain executable."
  artifacts:
    - path: "crates/liquidfun-testbed/src/ui/viewport/draw.rs"
      provides: "Renderer-contract semantic primitive projection"
      contains: "renderer"
    - path: "crates/liquidfun-testbed/tests/capability.rs"
      provides: "Replacement-adapter capability matrix"
      contains: "replacement"
  key_links:
    - from: "crates/liquidfun-testbed/src/ui/protocol_viewport.rs"
      to: "crates/liquidfun-testbed/src/renderer.rs"
      via: "draw and text methods"
      pattern: "renderer"
---

<objective>
Port deterministic capture and viewport projection to the replacement renderer contract.

Purpose: Reduce the Macroquad removal to the desktop event shell while preserving every Phase 11 capability and passive-renderer decision.
Output: Macroquad-free capability and viewport modules with replacement-specific regression evidence.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-01-SUMMARY.md
@crates/liquidfun-testbed/src/renderer.rs
@crates/liquidfun-testbed/CAPABILITY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Port capability capture and semantic viewport drawing</name>
  <files>crates/liquidfun-testbed/src/capability/render.rs, crates/liquidfun-testbed/src/capability/input.rs, crates/liquidfun-testbed/src/capability/report.rs, crates/liquidfun-testbed/src/ui/viewport/draw.rs, crates/liquidfun-testbed/src/ui/protocol_viewport.rs, crates/liquidfun-testbed/tests/capability.rs, crates/liquidfun-testbed/tests/interactive.rs</files>
  <read-first>Read every modified file, `renderer.rs`, `renderer/image.rs`, `capability/passive.rs`, `ui/viewport.rs`, and the Phase 11 capability report before editing.</read-first>
  <behavior>
    - "The three fixed 640x480, 800x600, and 1280x960 captures pass through the replacement CPU image path."
    - "Semantic primitives keep stable owner/layer/kind/child/occurrence identity and comparison-overlay behavior."
    - "Capabilities continue to assert five structural profile names without durations and zero session effects."
  </behavior>
  <action>Replace Macroquad `Image`, `Color`, key, and immediate draw calls in the listed files with the Plan 12-01 contract. Preserve the three exact capture sizes, logical DPI transforms, overlay opacity 35%, stable semantic ordering, accessibility labels, and screenshot non-authority. Rename fixture/report adapter identity to `eframe-egui-0.35.0+tiny-skia-0.12.0` and capability profile to `phase12-v1`; regenerate expected hashes only from the independently tested replacement renderer, never from physics output. Update focused tests to reject any `macroquad` import in these modules and to exercise all existing capability rows. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify>
    <automated>cargo test -p liquidfun-testbed --test capability && cargo test -p liquidfun-testbed --test interactive && ! rg -n "macroquad" crates/liquidfun-testbed/src/capability crates/liquidfun-testbed/src/ui/viewport/draw.rs crates/liquidfun-testbed/src/ui/protocol_viewport.rs</automated>
  </verify>
  <acceptance-criteria>The grep finds no Macroquad import in the ported modules; report identity is exactly `eframe-egui-0.35.0+tiny-skia-0.12.0`; capability and interactive tests pass all existing rows.</acceptance-criteria>
  <done>All deterministic image and viewport behavior uses the replacement contract with Phase 11 semantics intact.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| checkpoint/debug records to pixels | Semantic evidence is projected into non-authoritative presentation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-02-01 | Spoofing | capability identity | mitigate | Bind fixed adapter/profile strings and independently recomputed image hashes. |
| T-12-02-02 | Tampering | semantic ordering | mitigate | Preserve stable semantic keys and assert complete ordered projections. |
| T-12-02-03 | Information disclosure | diagnostics | mitigate | Keep raw pointers/private indices and unbounded errors outside presentation records. |
</threat-model>
<verification>Run focused capability/interactive suites, Macroquad source grep, Clippy, and image hash reproduction.</verification>
<success-criteria>Capture and viewport modules are Macroquad-free and retain every executable capability contract.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-02-SUMMARY.md`.</output>
