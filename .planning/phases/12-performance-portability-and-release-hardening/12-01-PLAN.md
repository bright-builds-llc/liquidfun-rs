---
phase: 12-performance-portability-and-release-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun-testbed/Cargo.toml
  - Cargo.lock
  - crates/liquidfun-testbed/src/renderer.rs
  - crates/liquidfun-testbed/src/renderer/image.rs
  - crates/liquidfun-testbed/src/lib.rs
  - crates/liquidfun-testbed/tests/renderer_contract.rs
autonomous: true
requirements: ["API-12", "DOCS-07"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "The private testbed has a renderer-neutral drawing/image contract that cannot advance simulation or comparison state."
    - "The replacement stack compiles on Rust 1.97 without changing the publishable liquidfun dependency graph."
  artifacts:
    - path: "crates/liquidfun-testbed/src/renderer.rs"
      provides: "Passive window, input, drawing, text, clipboard, and image-capture contracts"
      contains: "pub(crate) trait"
    - path: "crates/liquidfun-testbed/tests/renderer_contract.rs"
      provides: "Effect-isolation and deterministic image tests"
      contains: "renderer"
  key_links:
    - from: "crates/liquidfun-testbed/src/renderer/image.rs"
      to: "tiny-skia"
      via: "CPU image implementation"
      pattern: "tiny_skia"
---

<objective>
Introduce the safe passive renderer boundary used to remove Macroquad without redesigning the catalog, controller, or comparison architecture.

Purpose: Satisfy the inherited Phase 11 security obligation while preserving D-15's zero-unsafe production policy.
Output: An eframe/egui 0.35.0 desktop adapter contract and tiny-skia 0.12.0 deterministic CPU image backend alongside the still-working Macroquad shell.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-29-SUMMARY.md
@crates/liquidfun-testbed/Cargo.toml
@crates/liquidfun-testbed/src/capability/passive.rs
@crates/liquidfun-testbed/src/capability/render.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and prove the passive replacement renderer contract</name>
  <files>crates/liquidfun-testbed/Cargo.toml, Cargo.lock, crates/liquidfun-testbed/src/renderer.rs, crates/liquidfun-testbed/src/renderer/image.rs, crates/liquidfun-testbed/src/lib.rs, crates/liquidfun-testbed/tests/renderer_contract.rs</files>
  <read-first>Read every modified file, `crates/liquidfun-testbed/src/capability/passive.rs`, `crates/liquidfun-testbed/src/capability/render.rs`, `crates/liquidfun-testbed/tests/capability.rs`, `deny.toml`, and Plan 11-29's renderer obligation before editing.</read-first>
  <behavior>
    - "Drawing and capture consume owned semantic presentation values and return pixels/errors only."
    - "No renderer method accepts World, SessionController, ComparisonModel mutation, a clock tick, or a protocol writer."
    - "A 640x480 CPU capture is byte-stable across two runs and all invalid dimensions fail before allocation."
  </behavior>
  <action>Add exact private dependencies `eframe = "=0.35.0"`, `egui = "=0.35.0"`, and `tiny-skia = "=0.12.0"` while retaining Macroquad until Plan 12-18 completes the compiling migration. Create a cohesive `renderer.rs` contract for bounded RGBA colors, physical/logical dimensions, semantic key/input events, clipboard requests, primitive/text drawing, and image capture; create `renderer/image.rs` as the tiny-skia CPU implementation with checked `u32` dimensions, a 4096x4096 maximum, checked byte-count multiplication, and PNG output only beneath caller-validated paths. Keep types crate-private, effect-free with respect to simulation, and free of `unsafe`. Add Arrange/Act/Assert tests for bounds, deterministic bytes, passive-state isolation, and the unchanged `cargo tree -p liquidfun --edges normal` boundary. Do not remove either advisory ignore in this plan because Macroquad remains reachable until the complete migration is compiled. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify>
    <automated>cargo test -p liquidfun-testbed --test renderer_contract && cargo clippy -p liquidfun-testbed --all-targets --all-features -- -D warnings && cargo tree -p liquidfun --edges normal</automated>
  </verify>
  <acceptance-criteria>`Cargo.toml` contains exact `=0.35.0`, `=0.35.0`, and `=0.12.0` pins; `renderer.rs` contains no `World`, `SessionController`, or `unsafe`; the focused tests pass; `cargo tree -p liquidfun --edges normal` contains none of eframe, egui, tiny-skia, or Macroquad.</acceptance-criteria>
  <done>The replacement adapter contract is compiled, tested, bounded, passive, and isolated from the published crate.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| semantic presentation to renderer | Untrusted sizes, text, and draw counts enter private graphics code. |
| renderer to filesystem | Diagnostic PNG output must remain bounded and non-authoritative. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-01-01 | Tampering | renderer authority | mitigate | Contract accepts presentation values only and has no simulation mutation capability. |
| T-12-01-02 | Denial of service | image allocation | mitigate | Enforce 4096x4096 and checked byte-count limits before allocation. |
| T-12-01-03 | Elevation of privilege | private dependency graph | mitigate | Preserve workspace unsafe forbid and prove renderer dependencies never enter liquidfun. |
</threat-model>

<verification>Run the focused contract suite, testbed Clippy, dependency-tree isolation, and `cargo deny --locked check` as a diagnostic baseline.</verification>
<success-criteria>The new adapter is safe, bounded, passive, and ready to replace Macroquad consumers without changing physics behavior.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-01-SUMMARY.md`.</output>
