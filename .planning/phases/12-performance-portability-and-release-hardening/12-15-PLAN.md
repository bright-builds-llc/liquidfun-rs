---
phase: 12-performance-portability-and-release-hardening
plan: "15"
type: execute
wave: 9
depends_on: ["12-23", "12-09", "12-24", "12-11", "12-13", "12-14"]
files_modified:
  - tools/xtask/src/release.rs
  - tools/xtask/src/release/domain.rs
  - tools/xtask/src/release/validation.rs
  - tools/xtask/src/release/report.rs
  - tools/xtask/src/main.rs
  - tools/xtask/tests/release_cli.rs
  - reference/release/schema.json
  - reference/release/required-evidence.toml
  - justfile
autonomous: true
requirements: ["COMP-10", "DOCS-09", "PERF-04", "PERF-06"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "cargo xtask release audit accepts only a complete reviewed evidence set bound to one exact candidate commit."
    - "Mixed commits, missing evidence, stale conditional support, advisory waivers, unsafe weakening, gaps, or package drift reject readiness."
    - "The audit validates existing artifacts instead of rerunning expensive suites."
  artifacts:
    - path: "tools/xtask/src/release/domain.rs"
      provides: "Illegal-state-free release evidence types"
      contains: "ReleaseEvidenceKind"
    - path: "reference/release/required-evidence.toml"
      provides: "Complete closed evidence-kind set"
      contains: "performance"
  key_links:
    - from: "tools/xtask/src/release/validation.rs"
      to: "reference/release/required-evidence.toml"
      via: "exhaustive commit-bound join"
      pattern: "candidate_commit"
---
<objective>
Build the typed fail-closed release-audit core over independently produced evidence artifacts.

Purpose: Implement D-11/D-12 and the release gate's hardest constraint before workflow aggregation.
Output: Closed evidence manifest, pure validation core, human/machine reports, CLI, and exhaustive negative tests.
</objective>
<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@standards/core/architecture.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-23-SUMMARY.md
@.planning/phases/12-performance-portability-and-release-hardening/12-24-SUMMARY.md
@.planning/phases/12-performance-portability-and-release-hardening/12-14-SUMMARY.md
@tools/xtask/src/phase11_evidence.rs
@tools/xtask/src/package.rs
@tools/xtask/src/inventory/validation.rs
@reference/artifacts/manifest.toml
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Parse and validate one complete commit-bound release evidence set</name>
  <files>tools/xtask/src/release.rs, tools/xtask/src/release/domain.rs, tools/xtask/src/release/validation.rs, tools/xtask/src/release/report.rs, tools/xtask/src/main.rs, tools/xtask/tests/release_cli.rs, reference/release/schema.json, reference/release/required-evidence.toml, justfile</files>
  <read-first>Read every modified file or record absence, all Phase 9-11 evidence validators, package/performance/platform/safety/coverage contracts, inventory closure, docs checker, D-11/D-12/D-16, and RELEASE.md.</read-first>
  <behavior>
    - "Required evidence kinds include package, msrv, four durable platforms, conditional macOS disposition, canonical differential, Rust safety, C++ sanitizer, fuzz, regressions, Rust coverage, C++ coverage, performance, docs/rustdoc, notices/licenses, corpus closure, and compatibility closure."
    - "Every accepted item binds exact candidate commit, producer workflow/job, artifact SHA-256, toolchain, target, kind, review status, and payload hash."
    - "Audit never invokes a workflow, benchmark, fuzz target, sanitizer, or external network."
  </behavior>
  <action>Per D-11, D-12, and D-16, create domain newtypes/enums so only complete reviewed evidence records can enter validation. Implement `cargo xtask release audit --manifest PATH --candidate COMMIT --output human|json` with confined regular-file reads, 4 MiB manifest limit, 256 item limit, unique kind/target keys, exact candidate equality, artifact hash revalidation, producer allowlist, and reviewed status. Join required kinds to package archive identity, platform support/freshness, performance policy/report, the shared typed regression and coverage validators from Plan 12-10, zero unsafe/advisory waivers, licenses/notices, rustdoc/docs, corpus closure, and Plan 12-14 zero-gap compatibility closure. Reject unknown/duplicate/missing/mixed/stale/unreviewed items and report stable categories without raw secrets. Add a thin `release-audit` just recipe. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>cargo test -p xtask --test release_cli && just --list | rg "release-audit" && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>Tests independently fail each required evidence kind, mixed commit, wrong artifact hash, stale conditional target, profile/coverage parity misuse, unsafe/advisory weakening, incomplete corpus/gap count, package drift, and unreviewed status; no test or implementation launches expensive evidence producers.</acceptance-criteria>
  <done>The final readiness decision is a deterministic pure validation of one complete immutable evidence set.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| CI artifacts/manifests to release decision | External artifact metadata can be missing, substituted, or mixed. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-15-01 | Spoofing | candidate/evidence identity | mitigate | Exact commit, workflow/job, artifact/payload hash, toolchain, target, and kind checks. |
| T-12-15-02 | Tampering | manifest paths/content | mitigate | Confined regular files, byte limits, strict schemas, hash revalidation. |
| T-12-15-03 | Repudiation | audit result | mitigate | Stable machine report lists every accepted/rejected evidence identity. |
| T-12-15-04 | Denial of service | manifest joins | mitigate | 4 MiB/256 item bounds and unique keys before joins. |
</threat-model>
<verification>Run exhaustive release CLI negatives, Clippy, schema validation, local synthetic complete-set audit, and prove no producer command is invoked.</verification>
<success-criteria>Only one complete reviewed same-commit evidence set can produce ready status.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-15-SUMMARY.md`.</output>
