---
phase: 12-performance-portability-and-release-hardening
plan: "08"
type: execute
wave: 6
depends_on: ["12-10", "12-20"]
files_modified:
  - tools/xtask/src/performance.rs
  - tools/xtask/src/performance/analysis.rs
  - tools/xtask/src/performance/paths.rs
  - tools/xtask/src/main.rs
  - tools/xtask/tests/performance_cli.rs
  - reference/performance/policy.json
  - reference/performance/manifest.toml
  - scripts/phase12-performance.sh
  - justfile
autonomous: true
requirements: ["PERF-04", "PERF-05", "PERF-06"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "Five-run calibration produces a noise floor and optimization threshold before any comparison claim."
    - "The closed cargo xtask performance paired entrypoint invokes Plan 12-20's paired runner and writes its immutable raw report before analysis."
    - "An optimization is admissible only with >=10% profile attribution or a concrete allocation/cache/scaling bottleneck, a cleared 95% interval, no workload regression, and all correctness gates."
    - "Every public claim resolves to an immutable machine report."
  artifacts:
    - path: "tools/xtask/src/performance/analysis.rs"
      provides: "Pure calibration, interval, regression, and optimization admission logic"
      contains: "OptimizationDisposition"
    - path: "scripts/phase12-performance.sh"
      provides: "Rerunnable breadcrumbed controlled-host runner"
      contains: "set -euo pipefail"
  key_links:
    - from: "tools/xtask/src/performance.rs"
      to: "crates/liquidfun-differential/src/performance.rs"
      via: "closed performance paired command calling the reviewed paired-runner/report API"
      pattern: "paired"
    - from: "tools/xtask/src/performance.rs"
      to: "reference/performance/manifest.toml"
      via: "typed report validation"
      pattern: "manifest"
---
<objective>
Turn paired raw measurements into calibrated, immutable performance evidence and a fail-closed optimization gate.

Purpose: Enforce D-04/D-05 and prevent unprofiled claims, regressions, or scalar-baseline erosion.
Output: Pure analysis core, xtask commands, controlled-host runner, report manifest, and thin just recipes.
</objective>
<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-20-SUMMARY.md
@tools/xtask/src/phase11_evidence.rs
@scripts/phase11-evidence.sh
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement calibration and optimization admission as a pure core</name>
  <files>tools/xtask/src/performance.rs, tools/xtask/src/performance/analysis.rs, tools/xtask/src/performance/paths.rs, tools/xtask/src/main.rs, tools/xtask/tests/performance_cli.rs, reference/performance/policy.json, reference/performance/manifest.toml</files>
  <read-first>Read every modified file or record absence, Phase 10/11 evidence validators, the paired report types, policy JSON, and D-04/D-05 before editing.</read-first>
  <behavior>
    - "Threshold is exactly max(3.0%, calibrated_noise_floor)."
    - "Admit requires profile share >=10.0% or a typed Allocation/Cache/Scaling bottleneck."
    - "Every mandatory workload must be present and none may regress beyond its own calibrated threshold."
    - "cargo xtask performance paired loads the sealed matrix/policy, invokes the Plan 12-20 runner, and persists the returned raw report without substituting synthetic durations."
  </behavior>
  <action>Per D-01, D-04, and D-05, add the closed `cargo xtask performance paired|calibrate|validate|optimization-check` command set with no arbitrary paths outside confined target/reference locations. The `paired` branch must load the tracked sealed matrix and policy, select exact `oracle-release`, invoke Plan 12-20's reviewed paired runner/report API for every required case, reject harness failures or physics mismatches, and persist raw samples plus report identity beneath `target/phase12-performance`; it must not reimplement either engine adapter or synthesize durations. Implement bootstrap or Student interval method explicitly identified in policy, retain all raw samples, compute 95% intervals, and require five independent runs. An optimization record must bind before/after commits, scalar mode, profile/bottleneck evidence, every workload result, determinism/differential/safety/API gate hashes, and disposition. Reject SIMD, parallel, fast-math, unsafe, missing workloads, mixed commits, profiled totals as authority, generalized claims, and any regression above threshold. Register only reviewed reports in the manifest. Add focused CLI tests with injected paired-runner/report fakes proving the paired branch invokes the Plan 12-20 API exactly once per sealed case, preserves returned raw values and disjoint failure variants, rejects unknown modes/paths, and writes validation identity last. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>cargo test -p xtask --test performance_cli && cargo xtask performance paired --check && cargo xtask performance validate && cargo clippy -p xtask --all-targets --all-features -- -D warnings</automated></verify>
  <acceptance-criteria>`cargo xtask performance paired --check` resolves the sealed matrix/policy and Plan 12-20 adapter/report surface; focused tests prove real runner invocation rather than duplicated measurement logic; analysis tests cover 9.99% rejection, 10.0% admission, 2.99% floor rejection, noise-floor dominance, interval crossing, one regressed workload, mixed commits, missing correctness gate, and scalar-mode enforcement.</acceptance-criteria>
  <done>Performance evidence and structural optimization decisions are machine-validatable and fail closed.</done>
</task>
<task type="auto">
  <name>Task 2: Add a rerunnable controlled-host evidence runner</name>
  <files>scripts/phase12-performance.sh, justfile</files>
  <read-first>Read every modified file, existing phase evidence scripts, xtask performance usage, and the repo script/logging standards before editing.</read-first>
  <action>Create a Bash script with `set -euo pipefail` accepting only `calibrate|paired|validate`, a reviewed output directory beneath `target/phase12-performance`, and exact preset `oracle-release`. The `paired` branch must invoke the concrete `cargo xtask performance paired` entrypoint from Task 1 and must not call either engine adapter, fabricate a report, or substitute `validate` for measurement. Print breadcrumb stages, preserve raw logs plus `summary.json`, write identity last after xtask validation, and never use shell-evaluated user strings. Add thin `performance-calibrate`, `performance-paired`, and `performance-validate` just recipes that show the exact script/xtask command. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`.</action>
  <verify><automated>bash -n scripts/phase12-performance.sh && cargo test -p xtask --test performance_cli && just --list | rg "performance-(calibrate|paired|validate)"</automated></verify>
  <acceptance-criteria>The script contains `set -euo pipefail`, three closed modes, target confinement, logs/summary, and identity-last ordering; its paired branch contains the exact `cargo xtask performance paired` invocation; just recipes contain no loops, adapters, or policy logic.</acceptance-criteria>
  <done>Contributors can reproduce and validate bounded performance evidence through discoverable commands.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| raw host measurements to public claim | Noisy or substituted measurements could create false claims. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-08-01 | Spoofing | host/report identity | mitigate | Require exact hardware/tool/commit/scenario/policy identities and report hashes. |
| T-12-08-02 | Tampering | optimization admission | mitigate | Pure exhaustive rules and negative boundary tests. |
| T-12-08-03 | Repudiation | public claims | mitigate | Immutable raw samples, intervals, scope, and compatibility status. |
| T-12-08-04 | Denial of service | runner controls | mitigate | Closed modes, bounded samples, confined output, no shell interpolation. |
</threat-model>
<verification>Run xtask tests/validation, script syntax, thin-recipe checks, and a small local non-claiming calibration smoke; Plan 12-23 separately verifies the CI producer boundary.</verification>
<success-criteria>Only calibrated, complete, scalar, correctness-preserving reports can support performance claims, and the local runner is ready for the isolated producer plan.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-08-SUMMARY.md`.</output>
