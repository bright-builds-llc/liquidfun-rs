---
phase: 12-performance-portability-and-release-hardening
plan: "13"
type: execute
wave: 5
depends_on: ["12-10", "12-11", "12-18"]
files_modified:
  - README.md
  - SAFETY.md
  - CONTRIBUTING.md
  - RELEASE.md
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/tests/public_api_documentation.rs
  - tools/xtask/src/docs.rs
  - tools/xtask/tests/docs_contract.rs
autonomous: true
requirements: ["API-11", "API-12", "DOCS-01", "DOCS-07", "DOCS-08"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T15:46:19.909Z
must_haves:
  truths:
    - "Every public API builds with missing-rustdoc and rustdoc warnings denied and its contract covers relevant units, invariants, invalidation, callbacks, failures, and upstream mapping."
    - "README maturity, commands, MSRV, platforms, Cargo-only use, oracle prerequisites, contribution path, and license claims match executable evidence."
    - "SAFETY documents the zero-unsafe workspace policy and all ownership/invalidation/callback/buffer boundaries."
  artifacts:
    - path: "SAFETY.md"
      provides: "Public safety model and zero-unsafe evidence"
      contains: "unsafe_code = \"forbid\""
    - path: "RELEASE.md"
      provides: "SemVer, MSRV, provenance, evidence, packaging, and publication contract"
      contains: "cargo xtask release audit"
  key_links:
    - from: "tools/xtask/src/docs.rs"
      to: "README.md"
      via: "read-only exact documentation contract"
      pattern: "README"
---
<objective>
Complete the public API, safety, contribution, and release documentation contracts.

Purpose: Satisfy D-15/D-16 and prevent release claims from outrunning rustdoc, safety, platform, or package evidence.
Output: Audited rustdoc plus README, SAFETY, CONTRIBUTING, RELEASE, and executable docs checks.
</objective>
<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>
<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-18-SUMMARY.md
@ARCHITECTURE.md
@TESTING.md
@UPSTREAM.md
@crates/liquidfun/src/lib.rs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Make public rustdoc and zero-unsafe evidence exhaustive</name>
  <files>crates/liquidfun/src/lib.rs, crates/liquidfun/tests/public_api_documentation.rs, SAFETY.md</files>
  <read-first>Read every modified file or record absence, public reexports/modules, ARCHITECTURE ownership sections, workspace lint policy, API-11/API-12, and D-15.</read-first>
  <behavior>
    - "RUSTDOCFLAGS=-D warnings documents every public item."
    - "Public concept groups have exact MKS/radian units, handle invalidation, callback mutation, failure, and LiquidFun mapping where applicable."
    - "Repository-owned Rust contains no unsafe block and workspace lint remains forbid."
  </behavior>
  <action>Per D-15, add crate-level rustdoc navigation for math/settings, collision/shapes, world/bodies/fixtures, joints/rope, particles/groups, callbacks/events, queries, observations/profiles, handles/invalidation, errors, and upstream naming. Audit public module/type/function docs and fill only missing contract facts; examples must compile as doctests and avoid `unwrap()`. Add a black-box test that executes representative error/invalidation/callback/buffer paths. Create SAFETY.md covering identity, contacts, callback lock/commands, user data, external-equivalent buffers, owned events, renderer/oracle isolation, and panic policy. Assert `[workspace.lints.rust] unsafe_code = "forbid"` separately, and implement a construct-limited source scanner in the test that strips comments and string literals before detecting unsafe blocks/functions/traits/impls/extern declarations and unsafe attributes; do not use a raw `rg "\\bunsafe\\b"` check that matches the required lint or documentation. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then run `just markdown-check`. Do not commit a failing RED state.</action>
  <verify><automated>RUSTDOCFLAGS="-D warnings" cargo doc -p liquidfun --all-features --no-deps && cargo test -p liquidfun --doc && cargo test -p liquidfun --test public_api_documentation && rg -n 'unsafe_code = "forbid"' Cargo.toml && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && just markdown-check</automated></verify>
  <acceptance-criteria>Rustdoc/doctests/public contract tests pass; `lib.rs` links all listed groups; SAFETY contains every listed boundary; the construct-limited scanner finds zero production unsafe constructs while the separate lint assertion proves forbid; ordered Rust and Markdown gates pass.</acceptance-criteria>
  <done>The public API and safety model are complete, executable, and truthful.</done>
</task>
<task type="auto">
  <name>Task 2: Complete public, contributor, and release documentation checks</name>
  <files>README.md, CONTRIBUTING.md, RELEASE.md, tools/xtask/src/docs.rs, tools/xtask/tests/docs_contract.rs</files>
  <read-first>Read every modified file or record absence, TESTING, UPSTREAM, LICENSE/notices, package/platform contracts, renderer summary, and DOCS-01/DOCS-08.</read-first>
  <action>Per D-16, rewrite README maturity and capability copy from the compatibility authority; include Cargo-only install/use, Rust 1.92 MSRV, Rust 1.97 dev pin, durable and conditional platform table, headless/catalog/testbed commands, optional oracle/submodule prerequisites, contribution link, MIT/upstream notice status, and no generalized parity/performance claim before release audit. CONTRIBUTING must document bootstrap, mdformat 1.0/Python 3.13, ordered Rust gates, fast versus expensive CI, artifact/provenance/generated-file rules, regression promotion, and compatibility sign-off. RELEASE.md must define SemVer, v1.0.x MSRV stability, source-candidate SHA freeze, no-gap/audit sequence, exact package reuse, notices, evidence review, later attestation commit/tag, publication dry run, and rollback/non-publication. Extend docs checks with exact required headings/values and negative stale-claim fixtures. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then run `just markdown-check`. Do not commit a failing RED state.</action>
  <verify><automated>cargo test -p xtask --test docs_contract && cargo xtask docs check && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && just markdown-check</automated></verify>
  <acceptance-criteria>All three docs contain required exact versions/tiers/commands; tests reject a complete-parity or broad-performance claim without release authority; docs check and mdformat check exit 0.</acceptance-criteria>
  <done>Public and maintainer docs describe the actual executable release contract without overclaiming.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| evidence state to public documentation | Stale prose can spoof maturity, safety, or support. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-13-01 | Spoofing | maturity/platform claims | mitigate | Generate/check exact claims against typed authorities and reject unsupported text. |
| T-12-13-02 | Repudiation | safety claim | mitigate | Workspace forbid, source grep, public tests, and SAFETY evidence references. |
| T-12-13-03 | Information disclosure | examples/diagnostics | mitigate | Use bounded public values and no raw/private identities. |
</threat-model>
<verification>Run rustdoc/doctests/public tests, docs contract, markdown check, zero-unsafe grep, package verification, and review rendered docs.</verification>
<success-criteria>API-11/API-12 and public/release documentation are evidence-backed and fail closed on stale claims.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-13-SUMMARY.md`.</output>
