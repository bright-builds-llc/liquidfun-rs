---
phase: 12-performance-portability-and-release-hardening
plan: "22"
type: execute
wave: 12
depends_on: ["12-21"]
files_modified:
  - README.md
  - COMPATIBILITY.md
  - RELEASE.md
  - .codex/tasks/todo.md
autonomous: true
requirements: ["COMP-10", "DOCS-01", "DOCS-04", "DOCS-09"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T16:17:43Z
must_haves:
  truths:
    - "Public status remains explicitly non-ready while no real run-bound source-candidate attestation exists."
    - "Documentation names the missing release-candidate workflow run and attestation records instead of projecting readiness from local checks."
    - "The real release procedure still requires a frozen source candidate and later allowlisted attestation commit before any readiness claim."
    - "A future ready projection is permitted only from records accepted by the Plan 12-21 attestation validator."
  artifacts:
    - path: "README.md"
      provides: "Truthful current non-ready maturity status"
      contains: "not release-ready"
    - path: "RELEASE.md"
      provides: "Future run-bound attestation and publication procedure"
      contains: "release-candidate workflow run"
  key_links:
    - from: "README.md"
      to: ".github/workflows/release.yml"
      via: "names the missing external release-candidate run"
      pattern: "release-candidate"
    - from: "RELEASE.md"
      to: "tools/xtask/src/release/attestation.rs"
      via: "requires validated frozen-source records before public projection"
      pattern: "attestation validate"
---

<objective>
Project the truthful absence of a real run-bound attestation into public release status.

Purpose: Prevent local implementation completion from being mistaken for release readiness before external evidence workflows run.
Output: Explicit non-ready README, compatibility, release, and follow-up status with the future attestation procedure preserved.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/verification.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-21-SUMMARY.md
@README.md
@COMPATIBILITY.md
@RELEASE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish explicit non-ready status and preserve the real attestation path</name>
  <files>README.md, COMPATIBILITY.md, RELEASE.md, .codex/tasks/todo.md</files>
  <read-first>Read every modified file, Plan 12-21's fail-closed tooling summary, compatibility/corpus reports, package/notices policy, the absence of tracked ready records, and the D-16 checklist before editing.</read-first>
  <action>State plainly that the implementation and local verification are not a release-ready attestation. Name the missing inputs: one completed full-SHA `release-candidate` workflow run, its retained complete evidence bundle, and later tracked source/manifest/report records accepted by `cargo xtask release attestation validate`. Keep all parity, platform, safety, coverage, performance, and packaging descriptions no broader than their existing evidence; do not name the current worktree or HEAD as an audited source candidate. Update RELEASE with the exact future sequence: push only after phase lifecycle verification, run reviewed producers and release aggregation, retain/download the complete bundle, validate worktree records, commit the attestation, validate the committed range, then project public readiness. Mark only the renderer-replacement todo complete with its existing command/evidence references; do not mark release readiness complete. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then run `just markdown-check`.</action>
  <verify><automated>cargo xtask docs check && cargo xtask inventory check-report && test ! -e reference/release/source-candidate.json && test ! -e reference/release/candidate-manifest.json && test ! -e reference/release/audit-report.json && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && just markdown-check</automated></verify>
  <acceptance-criteria>All public surfaces explicitly remain non-ready and name the missing run-bound attestation; no ready record is tracked; no claim identifies current HEAD as audited; docs/inventory/Rust/Markdown gates pass.</acceptance-criteria>
  <done>Public release status is truthful about the missing external evidence while preserving the complete path to a future real attestation.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| implementation completion to public claims | Local green checks could be mistaken for run-bound release evidence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-22-01 | Spoofing | readiness claims | mitigate | Explicitly state non-ready status and enumerate missing external records. |
| T-12-22-02 | Repudiation | future projection | mitigate | Require both frozen source and later attestation identities before claims change. |
</threat-model>

<verification>Run docs/inventory checks, assert ready records remain absent, run the ordered Rust gate, and run Markdown verification.</verification>
<success-criteria>Public status remains non-ready until a real externally produced bundle passes frozen-source attestation.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-22-SUMMARY.md`.</output>
