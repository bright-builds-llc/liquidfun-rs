---
phase: 12-performance-portability-and-release-hardening
plan: "22"
type: execute
wave: 12
depends_on: ["12-21"]
files_modified:
  - README.md
  - COMPATIBILITY.md
  - RELEASE.md
  - .codex/tasks/todo.md
autonomous: true
requirements: ["COMP-10", "DOCS-01", "DOCS-04", "DOCS-09"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T16:17:43Z
must_haves:
  truths:
    - "The committed Plan 12-21 HEAD diff is validated against the frozen source candidate before any public status is projected."
    - "Public readiness status is a projection of the accepted frozen-source attestation."
    - "Claims name the source-candidate SHA and exact evidence scope rather than the projection commit."
    - "A non-ready attestation cannot produce parity or release-ready language."
  artifacts:
    - path: "README.md"
      provides: "Evidence-bound public maturity projection"
      contains: "source candidate"
    - path: "RELEASE.md"
      provides: "Attested source candidate and publication status"
      contains: "attestation"
  key_links:
    - from: "reference/release/audit-report.json"
      to: "tools/xtask/src/release/attestation.rs"
      via: "post-commit release attestation validate with HEAD"
      pattern: "attestation validate"
    - from: "README.md"
      to: "reference/release/audit-report.json"
      via: "accepted source-candidate readiness projection"
      pattern: "source_candidate"
---

<objective>
Project the accepted attestation into truthful public release status.

Purpose: Keep public claims downstream of candidate construction and attestation.
Output: Updated README, compatibility/release status, and closed renderer follow-up.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/verification.md
@.planning/phases/12-performance-portability-and-release-hardening/12-CONTEXT.md
@.planning/phases/12-performance-portability-and-release-hardening/12-21-SUMMARY.md
@reference/release/source-candidate.json
@reference/release/audit-report.json
@README.md
@COMPATIBILITY.md
@RELEASE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project attested readiness into public status</name>
  <files>README.md, COMPATIBILITY.md, RELEASE.md, .codex/tasks/todo.md</files>
  <read-first>Read every modified file, accepted source-candidate/manifest/report records, compatibility/corpus reports, package/notices evidence, and D-16 checklist before editing.</read-first>
  <action>Before editing public status, prove the now-committed Plan 12-21 boundary by running `cargo xtask release attestation validate --source reference/release/source-candidate.json --manifest reference/release/candidate-manifest.json --report reference/release/audit-report.json --attestation-commit HEAD`; this is the first plan allowed to use concrete HEAD because `depends_on: ["12-21"]` guarantees the attestation implementation and records are committed. Require the command to inspect the actual frozen-source-to-HEAD name-only diff and fail on every non-attestation path. Then, per D-16, regenerate README, COMPATIBILITY, and RELEASE readiness status only from the accepted Plan 12-21 report. Claims must name the exact frozen source-candidate SHA, evidence scope, and later attestation identity; never call the projection worktree or later projection commit itself the audited candidate. Mark the Macroquad replacement todo complete with command/evidence references. If committed-diff validation fails, `ready` is false, or any required evidence identity is missing, retain non-ready language and report the exact gap. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then run `just markdown-check`.</action>
  <verify><automated>cargo xtask release attestation validate --source reference/release/source-candidate.json --manifest reference/release/candidate-manifest.json --report reference/release/audit-report.json --attestation-commit HEAD && cargo xtask docs check && cargo xtask inventory check-report && cargo xtask release audit --manifest reference/release/candidate-manifest.json --candidate "$(jq -r .source_candidate_commit reference/release/source-candidate.json)" --output json && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && just markdown-check</automated></verify>
  <acceptance-criteria>The concrete committed command validates the actual frozen-source-to-HEAD allowlisted diff before docs generation; docs cite the frozen source candidate and attestation, no claim names the projection worktree/current HEAD as the audited candidate, a false-ready fixture suppresses claims, and all docs/inventory/Rust/Markdown gates pass.</acceptance-criteria>
  <done>Public readiness is a deterministic projection of a complete frozen-source attestation.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| attestation report to public claims | Stale or broadened prose could misrepresent audited readiness. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-22-01 | Spoofing | readiness claims | mitigate | Generate/check claims against accepted source-candidate report fields. |
| T-12-22-02 | Repudiation | projection identity | mitigate | Name both frozen source candidate and later attestation identity. |
</threat-model>

<verification>Run docs/inventory/audit checks, ordered Rust gate, Markdown check, and review the public claim diff.</verification>
<success-criteria>Public status is truthful, scoped, and impossible to produce from an incomplete attestation.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-22-SUMMARY.md`.</output>
