---
phase: 12-performance-portability-and-release-hardening
plan: "19"
type: execute
wave: 4
depends_on: ["12-07"]
files_modified:
  - tools/reference/src/benchmark_run.cpp
  - tools/reference/src/benchmark_run.hpp
  - tools/reference/src/protocol.cpp
  - tools/reference/src/protocol.hpp
  - tools/reference/tests/protocol_tests.cpp
  - tools/reference/CMakeLists.txt
autonomous: true
requirements: ["PERF-02", "PERF-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-07-23T15-16-59
generated_at: 2026-07-23T16:17:43Z
must_haves:
  truths:
    - "The pinned C++ oracle executes benchmark requests through the existing long-lived process."
    - "Warmup, construction, encoding, checkpoint validation, and teardown stay outside returned elapsed nanoseconds."
    - "Rejection and completion both advance request-local reset identity without contaminating later requests."
  artifacts:
    - path: "tools/reference/src/benchmark_run.cpp"
      provides: "Bounded request-local C++ measured action execution"
      contains: "benchmark_run_request"
    - path: "tools/reference/tests/protocol_tests.cpp"
      provides: "Timing-boundary, recovery, and reset-epoch proof"
      contains: "benchmark_run"
  key_links:
    - from: "tools/reference/src/protocol.cpp"
      to: "tools/reference/src/benchmark_run.cpp"
      via: "strict benchmark request dispatch"
      pattern: "benchmark_run"
---

<objective>
Implement the C++ benchmark endpoint over the strict Phase 12 wire contract.

Purpose: Add an equivalent measured oracle side without introducing a second executable or ABI boundary.
Output: Request-local C++ execution, protocol dispatch, and focused native tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@.planning/phases/12-performance-portability-and-release-hardening/12-07-SUMMARY.md
@tools/reference/src/catalog_run.cpp
@tools/reference/src/protocol.cpp
@tools/reference/tests/protocol_tests.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute bounded request-local C++ benchmark actions</name>
  <files>tools/reference/src/benchmark_run.cpp, tools/reference/src/benchmark_run.hpp, tools/reference/src/protocol.cpp, tools/reference/src/protocol.hpp, tools/reference/tests/protocol_tests.cpp, tools/reference/CMakeLists.txt</files>
  <read-first>Read every modified file, Plan 12-07 wire contract, catalog request execution, protocol limits, reset-epoch tests, and D-01 through D-03 before editing.</read-first>
  <action>Per D-01 through D-03, decode the exact Phase 12 benchmark request in the existing JSONL loop. Construct a fresh request-local world, warm up outside timing, time only declared actions with `steady_clock`, validate the final semantic checkpoint after timing, emit unprofiled nanoseconds plus common-parent diagnostics, destroy state, and advance reset epoch. Enforce identical reviewed limits, stdout protocol-only, bounded stderr diagnostics, rejection recovery, and exception containment. Before this task's commit, run exactly in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit a failing RED state.</action>
  <verify><automated>cmake --build target/reference/oracle-debug --target liquidfun-reference-protocol-tests && ctest --test-dir target/reference/oracle-debug --output-on-failure -R '^liquidfun-reference-protocol$' && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>Native tests accept one valid request, reject malformed/oversized/mismatched requests, prove recovery and epoch advancement, and show setup/encode/destruction are outside returned elapsed nanoseconds.</acceptance-criteria>
  <done>The pinned oracle executes the identical sealed performance case through the existing isolated process.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| strict JSONL request to C++ oracle | Untrusted records control bounded native execution. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-19-01 | Denial of service | native requests | mitigate | Strict framing, count, horizon, and deadline limits. |
| T-12-19-02 | Information disclosure | native diagnostics | mitigate | Protocol-only stdout and bounded categorized stderr. |
| T-12-19-03 | Tampering | timing boundary | mitigate | Injected/native tests prove every excluded operation remains outside timing. |
</threat-model>

<verification>Run native protocol tests, debug C++ build, and the ordered Rust gate.</verification>
<success-criteria>The oracle endpoint is bounded, recoverable, and measurement-equivalent to the native Rust contract.</success-criteria>
<output>After completion, create `.planning/phases/12-performance-portability-and-release-hardening/12-19-SUMMARY.md`.</output>
