---
phase: 17-shared-player-and-early-pages-delivery
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - web/src/catalog/scenes.ts
  - web/src/routing/hash.ts
  - web/tests/scenes.test.ts
  - web/tests/hash.test.ts
autonomous: true
requirements: [WEB-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-09-17T11-24-10
generated_at: 2026-09-17T11:35:00Z
must_haves:
  truths:
    - "A hash string is parsed into empty, unknown, or an allowlisted lowercase hyphenated scene id without reading the DOM or window."
    - "Catalog data lists Dam Break as the only ready scene and names Fountain, Float or Sink, Color Mixer, Jelly Drop, and Water Wheel as not ready."
    - "Unknown and empty hashes stay distinct from a known not-ready id so later UI can show the matching fallback copy."
  artifacts:
    - path: web/src/catalog/scenes.ts
      provides: "Six locked scene ids, titles, and ready flags"
      exports: ["SCENE_IDS", "SCENES", "isReadySceneId", "maybeSceneById"]
    - path: web/src/routing/hash.ts
      provides: "Pure hash-route parser for #/scene/{id}"
      exports: ["maybeParseSceneRoute"]
    - path: web/tests/hash.test.ts
      provides: "Allowlist, empty, and unknown hash cases"
  key_links:
    - from: web/src/routing/hash.ts
      to: web/src/catalog/scenes.ts
      via: "Parser allowlists SCENE_IDS only"
      pattern: "SCENE_IDS"
    - from: web/src/catalog/scenes.ts
      to: "dam-break"
      via: "Only Dam Break is ready"
      pattern: "ready: true"
---

<objective>
Define the locked scene catalog and a pure hash parser so later player and Pages work share one allowlist.

Purpose: WEB-02 needs stable `#/scene/{id}` identifiers and a useful unknown/empty fallback. Parsing must stay a data-in/data-out function so GitHub Pages needs no rewrite and hash input cannot become an open redirect.
Output: Tested `SCENE_IDS` / catalog records and `maybeParseSceneRoute`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@PROJECT-SCOPE.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/typescript-javascript.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-CONTEXT.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-RESEARCH.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-UI-SPEC.md

<interfaces>
Create these exact contracts. Later plans import them; do not invent a router library.

```typescript
export const SCENE_IDS = [
  "dam-break",
  "fountain",
  "float-or-sink",
  "color-mixer",
  "jelly-drop",
  "water-wheel",
] as const;

export type SceneId = (typeof SCENE_IDS)[number];

export type SceneRecord = {
  readonly id: SceneId;
  readonly title: string;
  readonly ready: boolean;
};

export const SCENES: readonly SceneRecord[];

export function maybeSceneById(id: string): SceneRecord | undefined;
export function isReadySceneId(id: SceneId): boolean;

export type SceneRoute =
  | { readonly kind: "scene"; readonly id: SceneId }
  | { readonly kind: "empty" }
  | { readonly kind: "unknown"; readonly maybeRaw: string };

export function maybeParseSceneRoute(hash: string): SceneRoute;
```

Titles must be exactly: `Dam Break`, `Fountain`, `Float or Sink`, `Color Mixer`, `Jelly Drop`, `Water Wheel`. Only `dam-break` has `ready: true`.
</interfaces>
</context>

<feature>
  <name>Allowlisted hash scene routes</name>
  <files>web/src/catalog/scenes.ts, web/src/routing/hash.ts, web/tests/scenes.test.ts, web/tests/hash.test.ts</files>
  <behavior>
    Catalog order and readiness are fixed. Parser accepts only `#/scene/{allowlisted-id}` and classifies empty vs unknown separately.
    Cases:
    - `SCENES.map((scene) => scene.id)` -> exact SCENE_IDS order
    - `isReadySceneId("dam-break")` -> true
    - `isReadySceneId("fountain")` -> false
    - `maybeParseSceneRoute("#/scene/dam-break")` -> `{ kind: "scene", id: "dam-break" }`
    - `maybeParseSceneRoute("")`, `"#"`, `"#/"`, `"#/scene"`, `"#/scene/"` -> `{ kind: "empty" }`
    - `maybeParseSceneRoute("#/scene/not-a-scene")` -> `{ kind: "unknown", maybeRaw: "not-a-scene" }`
    - `maybeParseSceneRoute("#/scene/fountain")` -> `{ kind: "scene", id: "fountain" }` (readiness is catalog metadata)
    - `maybeParseSceneRoute("#/evil")` and `"#/scene/dam-break/extra"` -> unknown
  </behavior>
  <implementation>
    Pure modules only. No Solid, window, or history API.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the six-scene catalog data</name>
  <files>web/src/catalog/scenes.ts, web/tests/scenes.test.ts</files>
  <read_first>17-CONTEXT.md D-01, D-02, D-03, D-04; 17-UI-SPEC.md Navigation and URL Chrome; 17-RESEARCH.md Pattern 1; standards/core/testing.md; web/vitest.config.ts</read_first>
  <behavior>
    - Test: `SCENES` has length 6 in the locked order with those exact titles.
    - Test: only `dam-break` is `ready: true`; the other five are `ready: false`.
    - Test: `maybeSceneById("dam-break")` returns the Dam Break record; `maybeSceneById("not-a-scene")` is `undefined`.
    - Test: `isReadySceneId` is true only for `dam-break`.
  </behavior>
  <action>Per D-01, D-02, and D-03, write failing Vitest cases first, then implement `web/src/catalog/scenes.ts` as a pure data module. Export `SCENE_IDS`, `SCENES`, `maybeSceneById`, and `isReadySceneId`. Use the exact ids `dam-break`, `fountain`, `float-or-sink`, `color-mixer`, `jelly-drop`, `water-wheel`. Do not add descriptions, preview URLs, per-scene controls, or inspiration links. Do not install a router or UI library. Keep `maybe_` naming for the lookup.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/scenes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "dam-break|fountain|float-or-sink|color-mixer|jelly-drop|water-wheel" web/src/catalog/scenes.ts` matches all six ids.
    - `rg -n "ready: true" web/src/catalog/scenes.ts` has exactly one match and that record id is `dam-break`.
    - `rg -n "description|preview|inspiration" web/src/catalog/scenes.ts` is empty.
    - `cd web && bun run test:unit -- tests/scenes.test.ts` exits 0.
  </acceptance_criteria>
  <done>Catalog data names six locked scenes and marks only Dam Break ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse hash routes without a router library</name>
  <files>web/src/routing/hash.ts, web/tests/hash.test.ts</files>
  <read_first>web/src/catalog/scenes.ts; 17-CONTEXT.md D-04, D-06; 17-RESEARCH.md Pattern 1 and Anti-Patterns; 17-UI-SPEC.md Navigation and URL Chrome; standards/core/architecture.md</read_first>
  <behavior>
    - Test: `#/scene/dam-break` and `/scene/dam-break` both parse as scene `dam-break`.
    - Test: empty, `#`, `#/`, `#/scene`, and `#/scene/` parse as `{ kind: "empty" }`.
    - Test: `#/scene/not-a-scene` and `#/scene/Dam-Break` parse as unknown with `maybeRaw` set to the raw token.
    - Test: `#/scene/fountain` parses as scene `fountain` (not unknown).
    - Test: `#/foo`, `#/scene/dam-break/extra`, and `https://evil.example/#/scene/dam-break` do not produce a scene route from the full URL; the function parses only the hash argument, so pass the hash portion in tests.
  </behavior>
  <action>Per D-04 and D-06, write failing tests first, then implement `maybeParseSceneRoute(hash)` exactly as researched: strip a leading `#`, split on `/`, drop empty parts, require exactly `["scene", id]`, and allowlist `id` against `SCENE_IDS`. Do not lowercase caller input to coerce `Dam-Break` into `dam-break`. Do not read `window.location`. Do not import `@solidjs/router`. Known not-ready ids remain `{ kind: "scene" }`; readiness stays in catalog data.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/scenes.test.ts tests/hash.test.ts &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "maybeParseSceneRoute" web/src/routing/hash.ts` matches.
    - `rg -n "@solidjs/router|createRouter|history\\.push" web/src` is empty.
    - Focused unit tests cover empty, unknown, dam-break, and fountain cases.
    - `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Hash input becomes an allowlisted scene, empty, or unknown result with no DOM or router dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser location hash → parser | Untrusted path tokens enter the allowlist. |
| Parser → later player shell | Only `SceneRoute` values may decide whether a WASM world is constructed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | S | `maybeParseSceneRoute` | mitigate | Allowlist `SCENE_IDS` only; unknown tokens never become scene ids or URLs. |
| T-17-01-02 | T | Hash / scene strings | mitigate | Keep `maybeRaw` as escaped later text, never `innerHTML` or `href` interpolation from raw input. |
| T-17-01-03 | I | Catalog readiness | mitigate | Ready flag is data, not parser output; only `dam-break` is ready so later code cannot "play" fountain from a parsed scene kind alone. |
| T-17-01-04 | D | WASM session disposal | accept | No session exists in this plan; Plan 17-05 disposes on route change. |
| T-17-01-05 | E | Pages OIDC / permissions | accept | No workflow files; Plan 17-07 pins OIDC and forbids a PAT. |
| T-17-01-06 | T | Artifact integrity | accept | No dist or Pages artifact; Plan 17-03 and 17-07 bind same-checkout build to deploy. |
</threat_model>

<verification>
Run `cd web && bun run test:unit -- tests/scenes.test.ts tests/hash.test.ts && bun run typecheck`. Do not run Playwright, Pages deploy, or `just web-smoke` in this plan.
</verification>

<success_criteria>
- Six locked ids and titles exist; only Dam Break is ready.
- `#/scene/dam-break` parses; empty and unknown hashes are distinct useful states.
- No router package, path-based `/scene/` navigation, or Phase 18 card fields are added.
</success_criteria>

<output>
After completion, create `.planning/phases/17-shared-player-and-early-pages-delivery/17-01-SUMMARY.md`
</output>
