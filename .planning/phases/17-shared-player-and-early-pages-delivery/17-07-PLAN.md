---
phase: 17-shared-player-and-early-pages-delivery
plan: "07"
type: execute
wave: 4
depends_on: [17-03, 17-05]
files_modified:
  - .github/workflows/pages.yml
autonomous: true
requirements: [HOST-01, HOST-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-09-17T11-24-10
generated_at: 2026-09-17T11:35:00Z
must_haves:
  truths:
    - "Every push to main and every pull request starts a same-checkout WASM plus production-site build with no paths: filter."
    - "The deploy job runs only after the build job succeeds, only on main pushes, and uses pages: write plus id-token: write without a PAT."
    - "Main concurrency uses cancel-in-progress: false so an in-flight deploy finishes while newer queued revisions may coalesce."
  artifacts:
    - path: .github/workflows/pages.yml
      provides: "SHA-pinned build-site and deploy-pages jobs"
  key_links:
    - from: .github/workflows/pages.yml
      to: scripts/web-build.ts
      via: "build-site runs bun scripts/web-build.ts build"
      pattern: "bun scripts/web-build.ts build"
    - from: deploy-pages
      to: build-site
      via: "needs: build-site and official upload/deploy actions"
      pattern: "needs: build-site"
---

<objective>
Add the GitHub Pages workflow that builds WASM and the Solid site from one checkout and deploys that artifact with OIDC.

Purpose: HOST-01 and HOST-02. A website delivery runner is not Linux native qualification. Do not wait on Cargo CI or rebuild WASM in the deploy job.
Output: `.github/workflows/pages.yml` with the researched SHA-pinned official actions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-CONTEXT.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-RESEARCH.md
@.github/workflows/ci.yml

<interfaces>
Use these exact action SHAs (comment the friendly major like `ci.yml`):

| Action | SHA |
|--------|-----|
| `actions/checkout` | `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0` |
| `actions/configure-pages` | `45bfe0192ca1faeb007ade9deae92b16b8254a0d` |
| `actions/upload-pages-artifact` | `fc324d3547104276b827a68afc52ff2a11cc49c9` |
| `actions/deploy-pages` | `368f82528645a54fb793d4d04e342629a3f51346` |
| `oven-sh/setup-bun` | `0c5077e51419868618aeaa5fe8019c62421857d6` |

Do not copy floating `oven-sh/setup-bun@v2` or Bun 1.3.9 from `bright-builds-checks.yml`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the two-job Pages workflow</name>
  <files>.github/workflows/pages.yml</files>
  <read_first>.github/workflows/ci.yml; 17-CONTEXT.md D-14, D-15, D-16; 17-RESEARCH.md Pattern 5, Pitfalls 4 and 6; PROJECT-SCOPE.md; scripts/web-build.ts</read_first>
  <action>Per D-14, D-15, and D-16, create `.github/workflows/pages.yml` named `Pages` with `on.push.branches: [main]`, `on.pull_request`, and `on.workflow_dispatch`. Do not add a `paths:` filter. Workflow-level `permissions: contents: read` only.

Concurrency:
```yaml
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}
```
That expression is false on `main`, which is the required `cancel-in-progress: false` for production deploys. PRs may cancel superseded builds.

`build-site` job: `ubuntu-24.04`, `timeout-minutes: 30`, `permissions: contents: read`. Steps:
1. `actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0` with `persist-credentials: false` and `submodules: false`.
2. Install Rust `1.97.0` plus target `wasm32-unknown-unknown` (no clippy/fmt workspace gate).
3. Install `wasm-pack` `0.15.0` with `--locked`.
4. `oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6` with `bun-version: 1.4.2`.
5. `bun scripts/web-build.ts build` with `VITE_APP_VERSION`, `VITE_GIT_SHA=$GITHUB_SHA`, `VITE_BUILD_ID=$GITHUB_RUN_ID`, and `VITE_BUILD_URL` from `github.server_url` / `github.repository` / `github.run_id`.
6. `actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d`.
7. `actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9` with `path: web/dist`.

`deploy-pages` job: `if: github.event_name == 'push' &amp;&amp; github.ref == 'refs/heads/main'`, `needs: build-site`, `ubuntu-24.04`, `timeout-minutes: 10`, permissions `contents: read`, `pages: write`, `id-token: write`, environment `name: github-pages` and `url: ${{ steps.deployment.outputs.page_url }}`. Single step id `deployment` using `actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346`.

Do not add a PAT, `peaceiris/actions-gh-pages`, `secrets.*`, Cargo CI `needs`, oracle/sanitizer/coverage/performance jobs, or a second WASM rebuild in deploy. Do not grant `pages: write` at workflow top level.</action>
  <verify>
    <automated>rg -n "paths:|peaceiris|secrets\\.|GITHUB_TOKEN:|cargo clippy|oracle|cancel-in-progress: true" .github/workflows/pages.yml; rg -n "9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0|45bfe0192ca1faeb007ade9deae92b16b8254a0d|fc324d3547104276b827a68afc52ff2a11cc49c9|368f82528645a54fb793d4d04e342629a3f51346|0c5077e51419868618aeaa5fe8019c62421857d6" .github/workflows/pages.yml; rg -n "pages: write|id-token: write|needs: build-site|bun scripts/web-build.ts build|path: web/dist" .github/workflows/pages.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/pages.yml` exists and contains no `paths:` filter.
    - All five researched action SHAs are present.
    - `pages: write` and `id-token: write` appear only on `deploy-pages`.
    - `deploy-pages` has `needs: build-site` and the main-push `if:` guard.
    - `rg -n "peaceiris|secrets\\.|PAT|gh-pages" .github/workflows/pages.yml` is empty.
    - Workflow file does not run Cargo workspace clippy/test or oracle jobs.
  </acceptance_criteria>
  <done>Pages build and OIDC deploy are expressed as one SHA-pinned workflow with latest-main-safe concurrency.</done>
</task>

<task type="auto">
  <name>Task 2: Statically prove HOST-01/02 shape</name>
  <files>.github/workflows/pages.yml</files>
  <read_first>.github/workflows/pages.yml; 17-CONTEXT.md D-14, D-15, D-16; 17-RESEARCH.md Pattern 5</read_first>
  <action>Re-read the committed workflow and add comments above concurrency and deploy permissions that cite D-14 (no path filters, same checkout), D-15 (OIDC, no PAT, SHA pins), and D-16 (`cancel-in-progress` false on main so overlapping pushes cannot strand a newer revision behind a cancelled deploy). Confirm `github-pages` is the environment name. Do not push to main in this plan; Plan 17-08 records the live URL after an ordinary main push.</action>
  <verify>
    <automated>python3 -c "import pathlib; text=pathlib.Path('.github/workflows/pages.yml').read_text(); assert 'paths:' not in text; assert 'cancel-in-progress: \${{ github.ref != \\'refs/heads/main\\' }}' in text or 'cancel-in-progress:' in text; assert 'id-token: write' in text; assert 'peaceiris' not in text"</automated>
  </verify>
  <acceptance_criteria>
    - Workflow comments mention D-14, D-15, and D-16.
    - `environment: name: github-pages` is present.
    - File is valid YAML (`python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/pages.yml'))"` if PyYAML is available, otherwise the python assert above plus `rg` checks).
    - No force-push, PAT, or live deploy is performed in this plan.
  </acceptance_criteria>
  <done>The workflow documentably matches HOST-01/02 without deploying yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → Pages | OIDC token and `GITHUB_TOKEN` can publish the public site. |
| Build job → deploy job | Only the uploaded `web/dist` artifact may be published. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07-01 | E | Deploy permissions | mitigate | No PAT; job-scoped `pages: write` + `id-token: write`; workflow top-level is `contents: read`. |
| T-17-07-02 | T | Artifact integrity | mitigate | Deploy `needs: build-site` and publishes the uploaded `web/dist`; no WASM rebuild in deploy. |
| T-17-07-03 | T | Older revision wins | mitigate | Concurrency group per workflow+ref; `cancel-in-progress` false on main; queued runs coalesce. |
| T-17-07-04 | S | Hash-route handling | accept | Workflow does not parse hashes; the built player already allowlists ids. |
| T-17-07-05 | D | WASM session disposal | accept | Serverless static host; disposal remains a client behavior from Plan 17-05. |
| T-17-07-06 | T | XSS from scene/error strings | accept | No server-rendered scene strings; client uses fixed copy. |
| T-17-07-07 | I | Qualification creep | mitigate | Pages runner builds `liquidfun-wasm` + `web/` only; no oracle/sanitizer/coverage gates. |
</threat_model>

<verification>
Run the ripgrep/python workflow assertions. Do not dispatch a live deploy in this plan.
</verification>

<success_criteria>
- One SHA-pinned workflow builds WASM and `web/dist` from the same checkout with no path filters.
- Main deploys use OIDC and the `github-pages` environment after the build job succeeds.
- Concurrent main deploys cannot cancel an in-flight publish.
</success_criteria>

<output>
After completion, create `.planning/phases/17-shared-player-and-early-pages-delivery/17-07-SUMMARY.md`
</output>
