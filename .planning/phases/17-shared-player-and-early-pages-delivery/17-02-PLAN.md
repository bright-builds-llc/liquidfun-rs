---
phase: 17-shared-player-and-early-pages-delivery
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - web/src/physics/clock.ts
  - web/tests/clock.test.ts
  - web/src/physics/session.ts
  - web/tests/session.test.ts
autonomous: true
requirements: [WASM-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-09-17T11-24-10
generated_at: 2026-09-17T11:35:00Z
must_haves:
  truths:
    - "A finite elapsed time yields at most 4 accepted 1/60-second steps and never grows an unbounded accumulator."
    - "The TypeScript session owner can forward 1 through 4 Rust advances in one nextFrame call and still capture once."
    - "Zero or out-of-range step counts never invoke generated advance."
  artifacts:
    - path: web/src/physics/clock.ts
      provides: "Pure acceptedStepCount with MAX_STEPS_PER_FRAME = 4"
      exports: ["STEP_SECONDS", "MAX_STEPS_PER_FRAME", "MAX_DELTA_SECONDS", "acceptedStepCount"]
    - path: web/src/physics/session.ts
      provides: "nextFrame(stepCount) forwards 1..=4 then one capture"
    - path: web/tests/clock.test.ts
      provides: "Zero, clamp, and max-4 clock cases"
  key_links:
    - from: web/src/physics/clock.ts
      to: web/src/physics/session.ts
      via: "Shell later calls nextFrame(acceptedStepCount(elapsed))"
      pattern: "MAX_STEPS_PER_FRAME = 4"
    - from: web/src/physics/session.ts
      to: "generatedSession.advance"
      via: "One advance(n) then one captureFrame"
      pattern: "advance\\(stepCount\\)"
---

<objective>
Add a pure step-budget clock and extend the existing session owner to one 1–4 advance plus one capture.

Purpose: WASM-04 requires bounded stepping so a 120 Hz display or hidden-tab resume cannot dump unbounded simulation. Rust already rejects counts outside 1–4; TypeScript must not call `nextFrame()` four times (four captures).
Output: Tested `acceptedStepCount` and `nextFrame(stepCount)` that preserves existing single-step tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/typescript-javascript.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-CONTEXT.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-RESEARCH.md
@web/src/physics/session.ts
@web/tests/session.test.ts
@crates/liquidfun-wasm/src/session.rs

<interfaces>
Existing owner (keep dispose / poison / finally-free behavior):

```typescript
export interface SceneSession {
  nextFrame(stepCount?: number): RenderFrame;
  dispose(): void;
}

export function createSceneSession(
  generatedSession: GeneratedProofSession,
): SceneSession;
```

Existing generated session already accepts `advance(stepCount: number)`. Rust `MAX_ADVANCE_STEPS` is 4 and rejects `0` and `5+` with `Rust/WASM step count must be within 1 through 4`.

Clock contract to create:

```typescript
export const STEP_SECONDS = 1 / 60;
export const MAX_STEPS_PER_FRAME = 4;
export const MAX_DELTA_SECONDS = MAX_STEPS_PER_FRAME * STEP_SECONDS;

export function acceptedStepCount(elapsedSeconds: number): number;
```
</interfaces>
</context>

<feature>
  <name>Bounded 1–4 step budget</name>
  <files>web/src/physics/clock.ts, web/tests/clock.test.ts, web/src/physics/session.ts, web/tests/session.test.ts</files>
  <behavior>
    Clock:
    - `acceptedStepCount(0)`, negative, `NaN`, `Infinity` -> 0
    - `acceptedStepCount(1 / 60)` -> 1
    - `acceptedStepCount(3 / 60)` -> 3
    - `acceptedStepCount(1)` and `acceptedStepCount(10)` -> 4
    Session:
    - `nextFrame()` and `nextFrame(1)` still advance `[1]`, capture once, free the frame
    - `nextFrame(4)` advances `[4]`, capture once
    - `nextFrame(0)` and `nextFrame(5)` throw the existing failed/disposed path without calling `advance`
  </behavior>
  <implementation>
    Guard stepCount in TypeScript before `generatedSession.advance`. Do not add workers, raw pointers, or a second capture.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compute a capped step budget</name>
  <files>web/src/physics/clock.ts, web/tests/clock.test.ts</files>
  <read_first>17-CONTEXT.md D-12, D-13; 17-RESEARCH.md Pattern 3; crates/liquidfun-wasm/src/session.rs; standards/core/architecture.md; standards/core/testing.md</read_first>
  <behavior>
    - Test: non-finite and non-positive elapsed values return 0.
    - Test: one, two, and three sixtieths return 1, 2, and 3.
    - Test: elapsed `MAX_DELTA_SECONDS` and any larger finite value return exactly 4.
    - Test: `MAX_STEPS_PER_FRAME` equals 4 and `MAX_DELTA_SECONDS` equals `4 / 60`.
  </behavior>
  <action>Per D-12, write failing Vitest cases first, then implement `acceptedStepCount` as a pure function: if `elapsedSeconds` is not finite or `<= 0`, return 0; clamp to `MAX_DELTA_SECONDS`; return `Math.min(4, Math.floor(clamped / STEP_SECONDS))`. Do not read `performance.now` or `document.hidden` here. Do not accumulate leftover time in this function; leftover catch-up is cleared by the later shell when the tab is hidden.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/clock.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MAX_STEPS_PER_FRAME = 4" web/src/physics/clock.ts` matches.
    - `rg -n "document\\.hidden|requestAnimationFrame|performance\\.now" web/src/physics/clock.ts` is empty.
    - `cd web && bun run test:unit -- tests/clock.test.ts` exits 0.
  </acceptance_criteria>
  <done>Step budget is a tested pure function capped at 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Forward 1–4 advances in one capture</name>
  <files>web/src/physics/session.ts, web/tests/session.test.ts</files>
  <read_first>web/src/physics/session.ts; web/tests/session.test.ts; 17-CONTEXT.md D-11, D-13; 17-RESEARCH.md Pattern 3; crates/liquidfun-wasm/src/session.rs</read_first>
  <behavior>
    - Existing tests still pass: default `nextFrame()` advances `[1]`, captures once, frees the raw frame, and poisons after advance/capture/parse failure.
    - New test: `nextFrame(4)` records `advanceCalls === [4]` and `captureCalls === 1`.
    - New test: `nextFrame(0)` and `nextFrame(5)` throw `Rust/WASM session failed` then `Rust/WASM session is disposed`, with `advanceCalls` empty and `freeCalls === 1`.
    - New test: `nextFrame(2)` does not call `advance` twice.
  </behavior>
  <action>Per D-11 and D-13, keep the composition owner, `finally` `frame.free()`, and idempotent `dispose()`. Change `nextFrame(stepCount = 1)` to require a safe integer in `1..=4` before calling `generatedSession.advance(stepCount)` once and `captureFrame()` once. On an invalid count, poison and throw the existing `FAILED_MESSAGE` without invoking generated advance. Do not expose raw pointers, WASM memory, or per-particle calls. Do not rename `ProofSession`. Keep copied typed-array parsing unchanged.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/clock.test.ts tests/session.test.ts &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "advance\\(stepCount\\)" web/src/physics/session.ts` matches and there is no loop that calls `nextFrame` or `advance` four times.
    - `rg -n "WebAssembly\\.Memory|\\.buffer|pointer|ptr" web/src/physics/session.ts` is empty.
    - Existing dispose/poison tests and the new 0/2/4/5 cases pass.
    - `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>One session call can step 1–4 Rust ticks and still capture exactly once, without leaking ownership rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Animation timestamp → clock | Untrusted elapsed values must not produce unbounded steps. |
| TypeScript owner → generated WASM | Only checked 1–4 counts may cross into `advance`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | D | `acceptedStepCount` | mitigate | Cap at 4 steps; reject non-finite elapsed time. |
| T-17-02-02 | D | `SceneSession.nextFrame` | mitigate | Guard 1..=4 in TypeScript; poison on violation; one capture. |
| T-17-02-03 | T | WASM memory | mitigate | Keep Phase 16 copied arrays and `finally` free; no workers or zero-copy views (D-13). |
| T-17-02-04 | I | Session disposal | mitigate | Preserve idempotent `dispose()` and use-after-dispose tests. |
| T-17-02-05 | S | Hash-route handling | accept | No hash parser here; Plan 17-01 allowlists ids and Plan 17-05 disposes on route change. |
| T-17-02-06 | E | Pages OIDC / permissions | accept | No workflow; Plan 17-07 uses OIDC and no PAT. |
| T-17-02-07 | T | Artifact integrity | accept | No Pages artifact; Plan 17-03/17-07 bind build to deploy. |
| T-17-02-08 | T | XSS from scene/error strings | mitigate | Keep throwing fixed `FAILED_MESSAGE` / `DISPOSED_MESSAGE`; never put generated exception text into HTML. |
</threat_model>

<verification>
Run `cd web && bun run test:unit -- tests/clock.test.ts tests/session.test.ts && bun run typecheck`. Do not change App.tsx, Pages, or Playwright in this plan.
</verification>

<success_criteria>
- Clock returns 0–4 only.
- `nextFrame(n)` forwards one legal advance and one capture.
- Phase 16 ownership, copied frames, and poison-on-failure remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/17-shared-player-and-early-pages-delivery/17-02-SUMMARY.md`
</output>
