---
phase: 17-shared-player-and-early-pages-delivery
plan: "05"
type: execute
wave: 3
depends_on: [17-01, 17-02, 17-04]
files_modified:
  - web/src/player/generation.ts
  - web/tests/generation.test.ts
  - web/src/App.tsx
autonomous: true
requirements: [WASM-04, WEB-02, WEB-03, WEB-06]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-09-17T11-24-10
generated_at: 2026-09-17T11:35:00Z
must_haves:
  truths:
    - "Opening #/scene/dam-break loads one Rust WASM Dam Break world, shows Loading Dam Break…, then Playing after the first drawn frame."
    - "Play resumes stepping, Pause freezes the world without dispose, Reset and Retry dispose then recreate the documented 192-particle basin."
    - "Leaving Dam Break, changing hash, or Solid cleanup cancels rAF, disconnects listeners/observers, discards stale loads, and dispose()s the prior session."
    - "While document.hidden is true the player keeps the Playing label but does not step; on resume the timestamp accumulator is cleared and at most 4 steps run per callback."
  artifacts:
    - path: web/src/player/generation.ts
      provides: "Stale-load generation token helpers"
      exports: ["nextGeneration", "isStaleGeneration"]
    - path: web/src/App.tsx
      provides: "One-session playground shell with hash, playback, and visibility"
  key_links:
    - from: web/src/App.tsx
      to: web/src/routing/hash.ts
      via: "hashchange updates maybeParseSceneRoute(window.location.hash)"
      pattern: "maybeParseSceneRoute"
    - from: web/src/App.tsx
      to: web/src/physics/session.ts
      via: "createSceneSession + dispose on reset/leave/cleanup"
      pattern: "dispose\\(\\)"
    - from: web/src/App.tsx
      to: web/src/physics/clock.ts
      via: "acceptedStepCount before nextFrame(n)"
      pattern: "acceptedStepCount"
    - from: web/src/App.tsx
      to: web/src/physics/loader.ts
      via: "loadProofSession only for dam-break"
      pattern: "loadProofSession"
---

<objective>
Replace the Phase 16 proof page with the one-session Dam Break player, hash routing, and bounded playback loop.

Purpose: This is the product shell for WASM-04, WEB-02, WEB-03, and WEB-06. Visitors must play, pause, reset, retry, recover from failure, and leave without leaking worlds or catch-up debt.
Output: Thin `App.tsx` that mounts Plan 17-04 chrome and owns exactly one WASM session.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/code-shape.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-CONTEXT.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-UI-SPEC.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-RESEARCH.md
@web/src/App.tsx
@web/src/physics/session.ts
@web/src/physics/clock.ts
@web/src/physics/loader.ts
@web/src/render/canvas.ts
@crates/liquidfun-wasm/src/scene.rs

<interfaces>
Reuse Phase 16 `loadProofSession()` / `ProofSession` as Dam Break. Documented initial state (do not retune): gravity `(0, -10)`, 16×12 = 192 particles, radius `0.2`, spacing `0.32`, origin `(-4.7, 0.4)`, color `(57, 211, 199, 255)`, basin floor `y=0` from `x=-5.5..5.5` with walls to `y=8`, dynamic circle `(2.5, 5.5)` radius `0.75`, particle cap 512, timestep `1/60`.

```typescript
export function nextGeneration(current: number): number;
export function isStaleGeneration(started: number, current: number): boolean;
```

App state machine (replace proof `disposed` with product states):

- `fallback` — empty, unknown, or not-ready route; no WASM world
- `loading` — Dam Break initializing
- `playing` | `paused` — live session exists
- `failure` — session stopped; Retry/Reset recreate

`document.title`:
- Dam Break player → `Dam Break · liquidfun-rs playground`
- not-ready known scene → `{Scene name} · liquidfun-rs playground`
- empty/unknown → `liquidfun-rs playground`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test generation-token stale-load discard</name>
  <files>web/src/player/generation.ts, web/tests/generation.test.ts</files>
  <read_first>17-CONTEXT.md D-11; 17-RESEARCH.md Pattern 2; standards/core/testing.md; web/src/App.tsx</read_first>
  <behavior>
    - Test: `nextGeneration(0)` is `1`; `nextGeneration(7)` is `8`.
    - Test: `isStaleGeneration(3, 3)` is false; `isStaleGeneration(3, 4)` is true.
    - Test: a later generation makes the earlier token stale so an async load must dispose immediately.
  </behavior>
  <action>Per D-11, write failing tests first, then implement `nextGeneration` and `isStaleGeneration` as pure functions. Do not import Solid or the generated WASM package. The later shell increments a generation on every start, reset, retry, and route change, then after `await loadProofSession()` disposes immediately when `isStaleGeneration(started, current)` is true.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/generation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/player/generation.ts` exports `nextGeneration` and `isStaleGeneration`.
    - `cd web && bun run test:unit -- tests/generation.test.ts` exits 0.
    - No `window` or WASM imports exist in `generation.ts`.
  </acceptance_criteria>
  <done>Stale async loads are a tested data decision before the shell awaits WASM.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the one-session Dam Break playground shell</name>
  <files>web/src/App.tsx</files>
  <read_first>web/src/App.tsx; web/src/components/CatalogNav.tsx; web/src/components/PlayerPanel.tsx; web/src/components/FallbackPanel.tsx; web/src/components/SiteFooter.tsx; web/src/physics/clock.ts; web/src/physics/session.ts; web/src/physics/loader.ts; 17-CONTEXT.md D-01, D-04, D-06, D-07, D-08, D-11, D-12, D-13; 17-UI-SPEC.md State and Interaction Contract; 17-RESEARCH.md Patterns 2–3 and Pitfalls 2–3</read_first>
  <action>Per D-01, D-07, D-08, D-11, and D-12, replace the Phase 16 proof page in `web/src/App.tsx`. Split if needed so `App.tsx` stays well under the 628-line file trigger. Semantic order: `main aria-labelledby="site-title"` → header `h1` `liquidfun-rs playground` plus the exact page summary → `CatalogNav` → player or fallback section → `SiteFooter`.

Hash: initialize from `maybeParseSceneRoute(window.location.hash)`, listen to `hashchange`, remove the listener on `onCleanup`. Only `kind === "scene" &amp;&amp; id === "dam-break"` constructs a WASM world. Fountain through Water Wheel and empty/unknown routes render `FallbackPanel` and must not call `loadProofSession`. `Open Dam Break` and catalog Dam Break set `window.location.hash = "#/scene/dam-break"`.

Lifecycle: own exactly one `SceneSession` and one rAF id. On reset, retry, leaving Dam Break, or cleanup: increment generation, `cancelAnimationFrame`, disconnect `ResizeObserver` and `visibilitychange` / `hashchange` listeners, `dispose()` the session, and drop stale in-flight loads. After `await loadProofSession()`, if `isStaleGeneration` then `dispose()` immediately and return.

Playback: after the first validated drawn frame, start `Playing` unless `matchMedia("(prefers-reduced-motion: reduce)").matches`, in which case enter `Paused`. `Play scene` resumes stepping. `Pause scene` freezes without dispose. `Reset scene` and `Retry scene` dispose and recreate the same Dam Break constructor (`loadProofSession` + `createSceneSession`). Keep the Phase 16 resize observer: redraw the last Rust frame only.

Clock: while Playing and `document.hidden === false`, compute `acceptedStepCount` from the rAF timestamp, skip `nextFrame` when the count is 0, otherwise call `nextFrame(n)` once (n is 1–4). On `visibilitychange`, set `maybeLastTimestamp = undefined` and do not step while hidden. Keep the Playing label during hidden-tab suspension. Cap remains 4; do not accumulate leftover time across hidden periods.

Preserve copied typed arrays, no raw pointers, no workers, no canvas pointer handlers. Remove `Dispose session`, `LiquidFun Rust/WASM browser proof`, and `Waiting for first Rust frame`. Keep `data-step-index` and add `data-playback` plus `data-scene` for later Playwright; do not show those numbers in the live region.

Do not implement the other five physics demos, cards, or Phase 19 pointer polish. Do not run `just web-smoke` in this plan; Phase 16 selectors are updated in Plan 17-06.</action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit &amp;&amp; bun run typecheck &amp;&amp; cd .. &amp;&amp; rg -n "Play scene|Pause scene|Reset scene|Retry scene|document.hidden|acceptedStepCount|isStaleGeneration|Dispose session" web/src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/App.tsx` contains `maybeParseSceneRoute`, `acceptedStepCount`, `document.hidden`, `isStaleGeneration`, and `loadProofSession`.
    - `rg -n "Dispose session|LiquidFun Rust/WASM browser proof|Waiting for first Rust frame" web/src/App.tsx` is empty.
    - `rg -n "innerHTML|pointerdown|onPointer" web/src/App.tsx` is empty.
    - `rg -n "fountain|float-or-sink|color-mixer|jelly-drop|water-wheel" web/src/App.tsx` does not construct those worlds (no extra `loadProofSession` branches).
    - `cd web && bun run test:unit && bun run typecheck` exits 0.
    - `just web-build` exits 0 after the player compiles.
  </acceptance_criteria>
  <done>The shared player opens Dam Break from `#/scene/dam-break`, controls it with the locked CTAs, and releases the prior world on reset, route change, and cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Location hash → session constructor | Only allowlisted Dam Break may create a WASM world. |
| Exception objects → UI | Generated/browser errors must not become HTML. |
| Animation timestamps → Rust advance | Hidden-tab deltas must not dump unbounded steps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05-01 | D | Session / rAF ownership | mitigate | One session, generation token, `onCleanup` dispose, stale-load discard. |
| T-17-05-02 | D | Hidden-tab clock | mitigate | Skip steps while `document.hidden`; clear `maybeLastTimestamp`; max 4 steps. |
| T-17-05-03 | S | Hash-route handling | mitigate | `maybeParseSceneRoute` allowlist; WASM only for `dam-break`; fallback otherwise. |
| T-17-05-04 | T | XSS from scene/error strings | mitigate | Fixed UI-SPEC copy in text nodes; development `Details:` sliced text, never `innerHTML`. |
| T-17-05-05 | T | WASM memory | mitigate | Keep copied frames and `dispose()`; no workers or zero-copy views. |
| T-17-05-06 | E | Pages OIDC / permissions | accept | No workflow; Plan 17-07. |
| T-17-05-07 | T | Artifact integrity | accept | Player still builds through Plan 17-03 same-checkout `just web-build`. |
</threat_model>

<verification>
Run `cd web && bun run test:unit && bun run typecheck` and `just web-build`. Confirm `web/dist/index.html` still contains `/liquidfun-rs/assets/`. Do not run Phase 16 `just web-smoke` until Plan 17-06 updates selectors.
</verification>

<success_criteria>
- Dam Break is the only constructed scene and resets to the documented basin.
- Play/pause/reset/retry match D-08.
- Empty/unknown/not-ready hashes show fallback with `Open Dam Break`.
- Hidden tabs and cleanup cannot leak a second world or catch-up storm.
</success_criteria>

<output>
After completion, create `.planning/phases/17-shared-player-and-early-pages-delivery/17-05-SUMMARY.md`
</output>
