---
phase: 17-shared-player-and-early-pages-delivery
plan: "04"
type: execute
wave: 2
depends_on: [17-01, 17-03]
files_modified:
  - standards-overrides.md
  - web/src/app.css
  - web/index.html
  - web/src/components/CatalogNav.tsx
  - web/src/components/FallbackPanel.tsx
  - web/src/components/SiteFooter.tsx
  - web/src/components/PlayerPanel.tsx
autonomous: true
requirements: [WEB-02, WEB-06]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-09-17T11-24-10
generated_at: 2026-09-17T11:35:00Z
must_haves:
  truths:
    - "Catalog navigation lists the six locked names with Ready on Dam Break and Not ready yet on the other five."
    - "Fallback copy for empty, unknown, and not-ready hashes includes an Open Dam Break control that targets #/scene/dam-break."
    - "Footer chrome always shows View source on GitHub, Free and open source, By Peter Ryszkiewicz, and Version/Commit/Build with Unavailable when a field is missing."
  artifacts:
    - path: standards-overrides.md
      provides: "D-09 thin-slice exception skipping MysticUI/Tailwind"
    - path: web/src/components/CatalogNav.tsx
      provides: "Honest six-name list with hash-only Dam Break link"
    - path: web/src/components/FallbackPanel.tsx
      provides: "Empty, unknown, and not-ready copy plus Open Dam Break"
    - path: web/src/components/PlayerPanel.tsx
      provides: "Play scene / Pause scene / Reset scene / Retry scene chrome"
    - path: web/src/components/SiteFooter.tsx
      provides: "Source, FOSS, maintainer, and provenance chrome"
    - path: web/src/app.css
      provides: "Evolved Phase 16 dark tokens without Tailwind"
  key_links:
    - from: web/src/components/CatalogNav.tsx
      to: "#/scene/dam-break"
      via: "Hash-only href, never /#/scene/"
      pattern: 'href="#/scene/dam-break"'
    - from: web/src/components/SiteFooter.tsx
      to: web/src/build-info.ts
      via: "readBuildInfo() labels and safe URLs"
      pattern: "readBuildInfo"
    - from: web/src/components/FallbackPanel.tsx
      to: "Open Dam Break"
      via: "Exact UI-SPEC CTA"
      pattern: "Open Dam Break"
---

<objective>
Build the presentational playground chrome from the UI-SPEC without wiring the WASM session yet.

Purpose: WEB-02 and WEB-06 need honest catalog/fallback copy, locked control labels, and site chrome before App.tsx becomes the imperative shell. D-09 must be recorded so Pages is not blocked by a design-system adoption.
Output: Semantic components, evolved `app.css`, default document title, and a standards override.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards-overrides.md
@standards/core/frontend-ui.md
@standards/languages/typescript-javascript.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-CONTEXT.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-UI-SPEC.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-01-SUMMARY.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-03-SUMMARY.md
@web/src/app.css
@web/src/catalog/scenes.ts
@web/src/routing/hash.ts
@web/src/build-info.ts

<interfaces>
Use Plan 17-01 catalog/hash types and Plan 17-03 `readBuildInfo()`. Do not edit `App.tsx` in this plan.

Exact locked strings from 17-UI-SPEC.md: `Play scene`, `Pause scene`, `Reset scene`, `Retry scene`, `Open Dam Break`, `Ready`, `Not ready yet`, `Scenes`, `Loading Dam Break…`, `Playing`, `Paused`, `Dam Break failed`, `Scene not found`, `Choose a scene`, `View source on GitHub`, `Free and open source`, `By Peter Ryszkiewicz`, `Unavailable`, `Runtime`, `Rust engine · WebAssembly`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the D-09 CSS exception and evolve tokens</name>
  <files>standards-overrides.md, web/src/app.css, web/index.html</files>
  <read_first>standards-overrides.md; web/src/app.css; web/index.html; 17-CONTEXT.md D-09, D-10; 17-UI-SPEC.md Design System, Color, Typography, Spacing, Narrow-Viewport Minimum; standards/core/frontend-ui.md</read_first>
  <action>Per D-09, replace the placeholder row in `standards-overrides.md` with a real exception: standard `standards/languages/typescript-javascript.md` SolidJS/MysticUI+Tailwind default; local decision is semantic HTML plus one scoped CSS file for Phase 17; rationale is D-09 thin-slice so Pages delivery is not blocked by a new design-system adoption; owner is the repository owner; review date `2026-12-17`. Keep the existing autonomous-iteration, independent-review, and hobby-scope notes.

Evolve `web/src/app.css` from the Phase 16 dark tokens. Keep `#0B0F14`, `#151D28`, `#39D3C7`, `#F87171`, `#F4F7FA`, `#A9B4C0`, `#2A3441`, `#071018`, and the `--space-*` scale. Style catalog chips, player/fallback/footer panels (1px border, 12px radius, 24px padding), 44px min-block-size on Play/Pause/Reset/Retry/Open Dam Break, accent fill only on Play and Retry, secondary fill on Pause/Reset/Open Dam Break, and a 2px `#39D3C7` `:focus-visible` outline with 4px offset. Do not add Tailwind, PostCSS, `components.json`, gradients, or glow. Reset must not use the Phase 16 Dispose red treatment.

Set `web/index.html` default `<title>` to `liquidfun-rs playground` and keep `#root` plus `/src/main.tsx`.</action>
  <verify>
    <automated>rg -n "MysticUI|Tailwind|D-09" standards-overrides.md &amp;&amp; rg -n "Play scene|#39D3C7|#F87171|44px" web/src/app.css &amp;&amp; rg -n "liquidfun-rs playground" web/index.html &amp;&amp; test ! -f web/components.json &amp;&amp; test ! -f web/tailwind.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `standards-overrides.md` contains `D-09` and does not still contain `REPLACE_WITH_STANDARD`.
    - `web/src/app.css` contains `#39D3C7`, `#0B0F14`, and a 44px minimum block size for product controls.
    - `rg -n "Dispose session|LiquidFun Rust/WASM browser proof" web/src/app.css web/index.html` is empty.
    - No Tailwind/MysticUI/shadcn config files exist under `web/`.
  </acceptance_criteria>
  <done>Phase 17 chrome uses evolved semantic CSS and the D-09 exception is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Add catalog, fallback, footer, and player panels</name>
  <files>web/src/components/CatalogNav.tsx, web/src/components/FallbackPanel.tsx, web/src/components/SiteFooter.tsx, web/src/components/PlayerPanel.tsx</files>
  <read_first>17-UI-SPEC.md Copywriting Contract, Page Structure, State and Interaction Contract, Site Identity; web/src/catalog/scenes.ts; web/src/routing/hash.ts; web/src/build-info.ts; web/src/App.tsx</read_first>
  <action>Per D-02, D-03, D-06, D-07, and D-10, create presentational Solid components only. Do not mount a WASM session, rAF loop, or hash listener here.

`CatalogNav`: `nav aria-labelledby="scene-nav-title"` with heading `Scenes`. Render `SCENES` in order. Dam Break is an `<a href="#/scene/dam-break">` and gets `aria-current="page"` plus the 4px accent marker only when the current route is scene `dam-break`. The five later names may use `href="#/scene/{id}"` but must show the `Not ready yet` chip, muted styles, and no accent marker. Never use `href="/#/scene/dam-break"` or `href="/scene/dam-break"`.

`FallbackPanel`: render the exact empty (`Choose a scene` / empty-hash body), unknown (`Scene not found` / unknown body), and not-ready (`{Scene name} is not ready yet` / not-ready body) copy. Include `<button type="button">Open Dam Break</button>` that the parent will use to set `window.location.hash = "#/scene/dam-break"`. Do not construct a canvas or show `Loading Dam Break…`.

`PlayerPanel`: heading `Dam Break`, polite live status using the exact strings `Loading Dam Break…`, `Playing`, `Paused`, and `Dam Break failed`, identity `Runtime` / `Rust engine · WebAssembly`, canvas slot via a callback ref, caption `Live frame from this repository's Rust engine, drawn with Canvas 2D.`, canvas accessible name `Live Dam Break scene: Rust particles and a rigid body in a basin.`, fallback text `Canvas is required to display the Dam Break scene.`, and buttons `Play scene`, `Pause scene`, `Reset scene`, plus `Retry scene` only when failed. Use `role="alert"` only for the fixed failure sentence `Dam Break could not start or continue. Use Retry scene to recreate it, or Reset scene to return to the documented initial state.` Optional development `Details:` text uses a text node, never `innerHTML`. Do not show `Dispose session`.

`SiteFooter`: `View source on GitHub` → `https://github.com/bright-builds-llc/liquidfun-rs`; `Free and open source`; `By Peter Ryszkiewicz` → `https://openlinks.us/`; labels `Version`, `Commit`, `Build` from `readBuildInfo()`. External links use `target="_blank"` and `rel="noopener noreferrer"`. Show `Unavailable` for missing fields. Do not add per-scene credits.

Do not implement Fountain through Water Wheel physics, cards, previews, or pointer handlers.</action>
  <verify>
    <automated>rg -n "Play scene|Pause scene|Reset scene|Retry scene|Open Dam Break" web/src/components/*.tsx &amp;&amp; rg -n 'href="#/scene/dam-break"' web/src/components/CatalogNav.tsx web/src/components/FallbackPanel.tsx &amp;&amp; rg -n "innerHTML|Dispose session|MysticUI|tailwind" web/src/components &amp;&amp; cd web &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All five locked CTA strings exist in `web/src/components`.
    - Catalog and fallback hrefs use `#/scene/dam-break` and never `/#/scene/` or `/scene/dam-break`.
    - `rg -n "innerHTML" web/src/components` is empty.
    - `rg -n "Dispose session|LiquidFun Rust/WASM browser proof" web/src/components` is empty.
    - `cd web && bun run typecheck` exits 0.
    - `web/src/App.tsx` is unchanged in this plan.
  </acceptance_criteria>
  <done>Presentational playground chrome matches the UI-SPEC and is ready for the session shell to mount.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route / error strings → DOM | Scene names and failure text are rendered to visitors. |
| Footer links → new tab | Only constant or SHA-derived GitHub/OpenLinks URLs may be used. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | T | Fallback and error copy | mitigate | Fixed UI-SPEC strings through text nodes; never `innerHTML`; optional `Details:` is escaped text. |
| T-17-04-02 | S | Catalog hrefs | mitigate | Hash-only `#/scene/{id}` from allowlisted ids; no origin-absolute `/#/scene/`. |
| T-17-04-03 | S | Footer links | mitigate | Hard-coded GitHub and OpenLinks URLs plus `readBuildInfo()` allowlisted URLs; `rel="noopener noreferrer"`. |
| T-17-04-04 | D | WASM session disposal | accept | Components are presentational; Plan 17-05 constructs and disposes the one session. |
| T-17-04-05 | E | Pages OIDC / permissions | accept | No workflow; Plan 17-07. |
| T-17-04-06 | T | Artifact integrity | accept | No deploy; Plan 17-03 already gates `/liquidfun-rs/` assets. |
</threat_model>

<verification>
Run the ripgrep acceptance checks and `cd web && bun run typecheck`. Do not wire `App.tsx` or deploy Pages.
</verification>

<success_criteria>
- Honest six-name catalog, locked CTAs, and footer chrome exist as semantic components.
- D-09 is recorded; no design-system packages are added.
- `App.tsx` still owns the Phase 16 proof until Plan 17-05.
</success_criteria>

<output>
After completion, create `.planning/phases/17-shared-player-and-early-pages-delivery/17-04-SUMMARY.md`
</output>
