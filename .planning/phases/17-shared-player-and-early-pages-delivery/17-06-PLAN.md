---
phase: 17-shared-player-and-early-pages-delivery
plan: "06"
type: execute
wave: 4
depends_on: [17-03, 17-05]
files_modified:
  - web/playwright.config.ts
  - web/e2e/player.spec.ts
  - web/e2e/rust-wasm-proof.spec.ts
  - web/package.json
  - justfile
  - scripts/web-build.ts
autonomous: true
requirements: [WEB-02, WEB-03, WEB-06, WASM-04, HOST-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-09-17T11-24-10
generated_at: 2026-09-17T11:35:00Z
must_haves:
  truths:
    - "Chromium opens /liquidfun-rs/#/scene/dam-break, observes Loading Dam Break… then Playing, and exercises Pause scene and Reset scene."
    - "Unknown hash /liquidfun-rs/#/scene/not-a-scene shows Scene not found and Open Dam Break returns to the working player."
    - "Reset and hash-leave dispose the prior session so only one data-step-index series advances."
    - "Phase 16 forensic smoke remains opt-in and no longer runs as the default browser suite."
  artifacts:
    - path: web/e2e/player.spec.ts
      provides: "Production-base Dam Break, fallback, reset, and hidden-tab proofs"
    - path: web/playwright.config.ts
      provides: "baseURL http://127.0.0.1:4173 and preview under /liquidfun-rs/"
    - path: justfile
      provides: "web-player-smoke recipe"
  key_links:
    - from: web/e2e/player.spec.ts
      to: "/liquidfun-rs/#/scene/dam-break"
      via: "Playwright goto under the project base"
      pattern: "/liquidfun-rs/#/scene/dam-break"
    - from: web/playwright.config.ts
      to: "vite preview"
      via: "webServer url http://127.0.0.1:4173/liquidfun-rs/"
      pattern: "127\\.0\\.0\\.1:4173/liquidfun-rs/"
---

<objective>
Prove the production-base player locally in Chromium: Dam Break controls, unknown-hash fallback, disposal, and hidden-tab no-catch-up.

Purpose: D-17 requires these proofs without the Phase 19 six-scene matrix. Phase 16's `Dispose session` spec must not be the default or it will fail against the new chrome.
Output: `web/e2e/player.spec.ts`, a player-first Playwright config, and `just web-player-smoke`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-CONTEXT.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-UI-SPEC.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-RESEARCH.md
@web/playwright.config.ts
@web/e2e/rust-wasm-proof.spec.ts
@scripts/web-build.ts
@justfile

<interfaces>
Playwright must use `baseURL: "http://127.0.0.1:4173"` and `page.goto("/liquidfun-rs/#/scene/dam-break")`. Do not set `baseURL` to a path ending in `/liquidfun-rs/` and then `goto("/")`.

Locked selectors:
- `getByRole("status")` / polite live region texts `Loading Dam Break…`, `Playing`, `Paused`
- `getByRole("button", { name: "Play scene" | "Pause scene" | "Reset scene" | "Retry scene" | "Open Dam Break" })`
- `getByRole("heading", { name: "Scene not found" })`
- `getByRole("heading", { name: "Dam Break" })`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the production-base player spec</name>
  <files>web/playwright.config.ts, web/e2e/player.spec.ts, web/e2e/rust-wasm-proof.spec.ts</files>
  <read_first>web/playwright.config.ts; web/e2e/rust-wasm-proof.spec.ts; 17-CONTEXT.md D-17; 17-RESEARCH.md Pattern 4, Pitfall 5, Production-base Playwright navigation; 17-UI-SPEC.md Copywriting Contract</read_first>
  <action>Per D-17, stop requiring `PHASE16_CLOSURE_ATTEMPT_DIR` for the default Playwright config. Keep that env only for the Phase 16 forensic spec: wrap `web/e2e/rust-wasm-proof.spec.ts` so it `test.skip`s unless `PHASE16_CLOSURE_ATTEMPT_DIR` is set. Point `webServer` at `bun run preview -- --strictPort` and `url: "http://127.0.0.1:4173/liquidfun-rs/"`. Keep `baseURL: "http://127.0.0.1:4173"`, zero retries, and package-pinned Chromium.

Create `web/e2e/player.spec.ts` that:
1. `goto("/liquidfun-rs/#/scene/dam-break")` and assert a request URL containing `/liquidfun-rs/` and `.wasm`.
2. Observe `Loading Dam Break…` then `Playing` (default motion; do not emulate `prefers-reduced-motion: reduce`).
3. Click `Pause scene` and assert `Paused` while `data-step-index` stays constant for at least 500ms.
4. Click `Reset scene`, wait for `Playing`, and assert `data-step-index` restarts near 0 rather than continuing the previous series.
5. `goto("/liquidfun-rs/#/scene/not-a-scene")`, assert `Scene not found`, click `Open Dam Break`, and land on `#/scene/dam-break` with `Playing`.
6. From Dam Break, `goto("/liquidfun-rs/#/scene/fountain")`, assert `{Fountain} is not ready yet` / not-ready heading, and assert no second WASM session is started (no additional `.wasm` init beyond the first module load, and fallback has no live canvas session).
7. Hidden-tab: from Playing, set `document.hidden` via `page.evaluate` plus a `visibilitychange` dispatch, wait 2s, restore visible, and assert `data-step-index` increases by at most 4 on the next observed frame rather than tens of steps.

Do not require the Phase 16 PNG closure matrix or six-scene WEBTEST-01 coverage. Do not add pointer/canvas interaction tests.</action>
  <verify>
    <automated>rg -n "/liquidfun-rs/#/scene/dam-break|/liquidfun-rs/#/scene/not-a-scene|Open Dam Break|Pause scene|Reset scene" web/e2e/player.spec.ts &amp;&amp; rg -n "PHASE16_CLOSURE_ATTEMPT_DIR" web/e2e/rust-wasm-proof.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `web/e2e/player.spec.ts` contains `/liquidfun-rs/#/scene/dam-break` and `/liquidfun-rs/#/scene/not-a-scene`.
    - `web/playwright.config.ts` uses `baseURL` `http://127.0.0.1:4173` and webServer URL `http://127.0.0.1:4173/liquidfun-rs/`.
    - `web/e2e/rust-wasm-proof.spec.ts` skips unless `PHASE16_CLOSURE_ATTEMPT_DIR` is set.
    - Default config no longer throws when that env is missing.
  </acceptance_criteria>
  <done>The player spec encodes D-17 navigation and control proofs against the production base.</done>
</task>

<task type="auto">
  <name>Task 2: Add a player-smoke recipe and run it</name>
  <files>web/package.json, justfile, scripts/web-build.ts</files>
  <read_first>scripts/web-build.ts; justfile; web/package.json; 17-CONTEXT.md D-17; 17-RESEARCH.md Open Questions</read_first>
  <action>Per D-17, add `web/package.json` script `test:player = "playwright test e2e/player.spec.ts"`. Add just recipe `web-player-smoke` that runs `bun scripts/web-build.ts build` then `cd web && bun run browser:install && bun run test:player` (or a new `player-smoke` mode in `scripts/web-build.ts` that reuses the existing build and does not allocate `target/phase16/closure-attempt-N`). Keep `just web-smoke` as the opt-in Phase 16 forensic path that still sets `PHASE16_CLOSURE_ATTEMPT_DIR`. Do not add Cargo, oracle, sanitizer, or coverage steps. Run `just web-player-smoke` once and keep the log under ignored `target/web-build/`.</action>
  <verify>
    <automated>just web-player-smoke</automated>
  </verify>
  <acceptance_criteria>
    - `justfile` contains `web-player-smoke`.
    - `just web-player-smoke` exits 0.
    - The run requested a `.wasm` URL under `/liquidfun-rs/`.
    - `just web-smoke` is unchanged in name and still uses `PHASE16_CLOSURE_ATTEMPT_DIR` when invoked.
    - No Pages workflow is added in this plan.
  </acceptance_criteria>
  <done>Local Chromium proves production-base Dam Break, fallback, reset, and hidden-tab bounding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preview server → browser | Tests load real JS/WASM under `/liquidfun-rs/`. |
| Hash fixture → UI | Unknown ids must stay on the fallback path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06-01 | T | Production-base assets | mitigate | Assert WASM URLs contain `/liquidfun-rs/` so a root-base preview cannot pass. |
| T-17-06-02 | S | Hash-route handling | mitigate | Unknown id shows `Scene not found`; Open Dam Break uses `#/scene/dam-break`. |
| T-17-06-03 | D | WASM session disposal | mitigate | Reset and hash-leave assertions require a restarted or absent step series. |
| T-17-06-04 | T | XSS from scene/error strings | mitigate | Spec asserts fixed headings/buttons, not raw exception HTML. |
| T-17-06-05 | T | Artifact integrity | mitigate | Player smoke builds via `scripts/web-build.ts` from the current checkout. |
| T-17-06-06 | E | Pages OIDC / permissions | accept | No deploy; Plan 17-07. |
</threat_model>

<verification>
Run `just web-player-smoke`. Do not push to main or claim a live Pages URL in this plan.
</verification>

<success_criteria>
- Chromium covers D-17 local proofs on `/liquidfun-rs/`.
- Phase 16 forensic smoke is opt-in.
- No six-scene WEBTEST-01 scope is added.
</success_criteria>

<output>
After completion, create `.planning/phases/17-shared-player-and-early-pages-delivery/17-06-SUMMARY.md`
</output>
