---
phase: 17-shared-player-and-early-pages-delivery
plan: "08"
type: execute
wave: 5
depends_on: [17-06, 17-07]
files_modified:
  - README.md
  - TESTING.md
  - .planning/phases/17-shared-player-and-early-pages-delivery/17-HOST-EVIDENCE.md
  - .planning/phases/17-shared-player-and-early-pages-delivery/17-REVIEW.md
autonomous: true
requirements: [HOST-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-09-17T11-24-10
generated_at: 2026-09-17T11:35:00Z
must_haves:
  truths:
    - "The live GitHub Pages URL and exact source SHA are recorded after a real main deployment."
    - "The hosted site loads JS/WASM under /liquidfun-rs/ and opens #/scene/dam-break."
    - "A separate identified AI reviewer acknowledges the player-plus-delivery diff and evidence at an exact digest; the implementing agent does not approve its own work."
  artifacts:
    - path: .planning/phases/17-shared-player-and-early-pages-delivery/17-HOST-EVIDENCE.md
      provides: "Recorded page_url, SHA, workflow run, and asset checks"
    - path: .planning/phases/17-shared-player-and-early-pages-delivery/17-REVIEW.md
      provides: "Independent exact-digest acknowledgment"
    - path: README.md
      provides: "Playground URL and contributor Pages/build commands"
    - path: TESTING.md
      provides: "web-player-smoke versus opt-in Phase 16 forensic smoke"
  key_links:
    - from: .planning/phases/17-shared-player-and-early-pages-delivery/17-HOST-EVIDENCE.md
      to: "https://bright-builds-llc.github.io/liquidfun-rs/"
      via: "Recorded live URL or the proven replacement from Pages settings"
      pattern: "github.io/liquidfun-rs"
    - from: README.md
      to: "#/scene/dam-break"
      via: "Documented first-scene hash"
      pattern: "#/scene/dam-break"
---

<objective>
Ship the player to GitHub Pages, record the live URL and SHA, update contributor docs, and obtain independent review.

Purpose: HOST-03 and D-17/D-18 close only after a real main deployment and a non-self review. Standing 2026-09-13 authorization covers an ordinary non-force push to `main` on `bright-builds-llc/liquidfun-rs`. Package publication remains unauthorized.
Output: Hosted Dam Break, `17-HOST-EVIDENCE.md`, docs, and `17-REVIEW.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@README.md
@TESTING.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-CONTEXT.md
@.planning/phases/16-rust-wasm-browser-bridge/16-04-SUMMARY.md
@.github/workflows/pages.yml

<interfaces>
Expected hosted origin until Pages settings prove otherwise: `https://bright-builds-llc.github.io/liquidfun-rs/`.
Canonical first scene: `https://bright-builds-llc.github.io/liquidfun-rs/#/scene/dam-break`.

Evidence file must contain literal fields:
- `page_url:`
- `source_sha:`
- `workflow_run_url:`
- `wasm_asset_url:` (a `/liquidfun-rs/assets/*.wasm` URL that returned a non-HTML body)
- `checked_at:` ISO timestamp
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the playground and player smoke</name>
  <files>README.md, TESTING.md</files>
  <read_first>README.md; TESTING.md; 17-CONTEXT.md D-05, D-10, D-16; PROJECT-SCOPE.md</read_first>
  <action>Per D-05 and D-10, replace the Phase 16-only "Web browser proof" README section with playground language: visitors open `https://bright-builds-llc.github.io/liquidfun-rs/#/scene/dam-break`; contributors rebuild with `just web-build` and prove locally with `just web-player-smoke`. State that production assets live under `/liquidfun-rs/`. Keep the pinned tool list (Rust 1.97.0, wasm-pack 0.15.0, Bun 1.4.2). Keep `just web-smoke` documented as the opt-in Phase 16 forensic closure, not the product default. State clearly that Pages delivery is not Linux native qualification and that no package is published.

In `TESTING.md`, add a short Web Playground subsection that lists `just web-build`, `just web-player-smoke`, hash fixtures `#/scene/dam-break` and `#/scene/not-a-scene`, and the Pages workflow name `Pages`. Do not claim WEBTEST-01 six-scene coverage or strict native certification.</action>
  <verify>
    <automated>rg -n "#/scene/dam-break|web-player-smoke|/liquidfun-rs/" README.md TESTING.md &amp;&amp; test -z "$(rg -n "Dispose session|complete parity|published crate" README.md || true)"</automated>
  </verify>
  <acceptance_criteria>
    - README and TESTING mention `#/scene/dam-break`, `just web-player-smoke`, and `/liquidfun-rs/`.
    - README does not claim package publication or complete parity.
    - README still says Pages/web delivery is not Linux native qualification.
  </acceptance_criteria>
  <done>Contributor and visitor docs describe the hosted Dam Break playground honestly.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy main and record HOST-03 evidence</name>
  <files>.planning/phases/17-shared-player-and-early-pages-delivery/17-HOST-EVIDENCE.md</files>
  <read_first>AGENTS.md Standing authorization; 17-CONTEXT.md D-16, D-17; .github/workflows/pages.yml; PROJECT-SCOPE.md</read_first>
  <action>Per D-16 and D-17, using the 2026-09-13 standing authorization, perform an ordinary non-force push of the completed Phase 17 work to `main` on `bright-builds-llc/liquidfun-rs`. Recheck repository, ref, and workflow identity before the push: remote must be that GitHub repository, branch `main`, workflow file `.github/workflows/pages.yml`, workflow name `Pages`. Do not `--force`. Do not create a release tag or publish crates/npm.

After the push, wait for the `Pages` run on that SHA. Confirm `build-site` succeeded and `deploy-pages` published. Record in `17-HOST-EVIDENCE.md`:
- `page_url` from `steps.deployment.outputs.page_url` or `gh api repos/bright-builds-llc/liquidfun-rs/pages --jq .html_url`
- `source_sha` equal to the deployed commit
- `workflow_run_url`
- a `wasm_asset_url` under `/liquidfun-rs/assets/` whose `Content-Type` is not `text/html`
- that `GET {page_url}#/scene/dam-break` returns HTML containing `liquidfun-rs playground` and that a hard reload of that hash URL still serves the app shell

If the live origin differs from `https://bright-builds-llc.github.io/liquidfun-rs/`, record the actual URL and why. Do not treat a failed or stale run as passing. If the deploy fails, diagnose and fix in a new attempt rather than overwriting failed logs.</action>
  <verify>
    <automated>rg -n "page_url:|source_sha:|workflow_run_url:|wasm_asset_url:|/liquidfun-rs/" .planning/phases/17-shared-player-and-early-pages-delivery/17-HOST-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `17-HOST-EVIDENCE.md` contains `page_url`, `source_sha`, `workflow_run_url`, and `wasm_asset_url`.
    - `source_sha` is a 40-character hex SHA that exists on `origin/main`.
    - `wasm_asset_url` contains `/liquidfun-rs/assets/` and `.wasm`.
    - Evidence names the actual checked URL, not a placeholder.
    - No release tag or package publish occurred.
  </acceptance_criteria>
  <done>The live Pages site and its source revision are recorded with functioning project-base WASM.</done>
</task>

<task type="auto">
  <name>Task 3: Obtain independent exact-digest review</name>
  <files>.planning/phases/17-shared-player-and-early-pages-delivery/17-REVIEW.md</files>
  <read_first>AGENTS.md Independent review; 17-CONTEXT.md D-18; .planning/phases/16-rust-wasm-browser-bridge/16-REVIEW.md; 17-HOST-EVIDENCE.md</read_first>
  <action>Per D-18, the implementing agent must not approve its own work. Build a review manifest over the Phase 17 player, workflow, evidence, and docs diff. Include `17-HOST-EVIDENCE.md`, `.github/workflows/pages.yml`, `web/src/App.tsx`, routing/clock/session/components, and the player spec. Compute one lowercase SHA-256 digest of that fixed manifest. Launch a separate identified AI reviewer (not this implementing agent) to inspect the complete relevant diff and evidence and to record an acknowledgment bound to that exact digest, the reviewer's actual identity, and the actual review time. Write `17-REVIEW.md` with the digest, reviewer identity, timestamp, and pass/fail. Identify the review as AI review honestly. Passing `just web-player-smoke` alone is not an acknowledgment.</action>
  <verify>
    <automated>rg -n "digest|reviewer|acknowled" .planning/phases/17-shared-player-and-early-pages-delivery/17-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - `17-REVIEW.md` contains a 64-character hex digest, a reviewer identity different from the implementing agent label, and an ISO timestamp.
    - The file states that the review is independent AI review, not human approval.
    - The acknowledgment quotes the same digest as the manifest.
    - No self-approval sentence from the implementing agent is used as the acknowledgment.
  </acceptance_criteria>
  <done>Independent review is bound to the exact player-and-delivery digest and live evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local checkout → origin/main | Ordinary push publishes source and triggers deploy. |
| Live Pages → visitors | JS/WASM and hash routes become public. |
| Review digest → acknowledgment | Only a separate reviewer may bind approval to the digest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08-01 | T | Artifact integrity | mitigate | Record the deployed SHA and workflow URL; verify the live WASM URL is not HTML. |
| T-17-08-02 | E | Pages OIDC / permissions | mitigate | Push uses ordinary `GITHUB_TOKEN`/SSH git credentials already available; workflow still has no PAT. |
| T-17-08-03 | S | Hash-route handling | mitigate | Evidence includes `#/scene/dam-break` reload; unknown hashes stay a client fallback. |
| T-17-08-04 | T | XSS from scene/error strings | mitigate | Reviewer inspects text-node error/fallback copy; no `innerHTML`. |
| T-17-08-05 | D | WASM session disposal | mitigate | Reviewer inspects reset/leave dispose and hidden-tab tests from Plan 17-06 evidence. |
| T-17-08-06 | R | Self-review | mitigate | Separate AI reviewer + exact digest (D-18). |
</threat_model>

<verification>
Confirm `17-HOST-EVIDENCE.md` fields, that `origin/main` contains the recorded SHA, and that `17-REVIEW.md` acknowledges that digest. Do not publish a crate or create a release tag.
</verification>

<success_criteria>
- Live Pages serves Dam Break under `/liquidfun-rs/` at a recorded URL and SHA.
- Docs match the hosted playground.
- Independent AI review is recorded against the exact digest.
</success_criteria>

<output>
After completion, create `.planning/phases/17-shared-player-and-early-pages-delivery/17-08-SUMMARY.md`
</output>
