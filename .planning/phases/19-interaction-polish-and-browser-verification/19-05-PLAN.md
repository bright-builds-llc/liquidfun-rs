---
phase: 19-interaction-polish-and-browser-verification
plan: "05"
type: execute
wave: 3
depends_on: [19-04]
files_modified:
  - web/src/catalog/scenes.ts
  - web/tests/scenes.test.ts
  - web/src/components/PlayerPanel.tsx
  - web/src/App.tsx
  - web/src/app.css
autonomous: true
requirements: [WEB-07]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 19-2026-09-19T01-44-48
generated_at: 2026-09-19T02:10:00Z
must_haves:
  truths:
    - "Each ready scene figcaption is the locked two-sentence interactionHint; fallback views show no leftover Dam Break hint."
    - "select:focus-visible uses the same 2px #39D3C7 outline and 4px offset as buttons and links."
    - "Labeled Play, Pause, Reset, Apply setting, Open, and scene actions remain keyboard-operable with no Space-to-pause handler."
    - "At max-width 480px, controls stack full width at 44px minimum and the page keeps min-width 0."
  artifacts:
    - path: web/src/catalog/scenes.ts
      provides: "interactionHint on SceneRecord"
      exports: ["interactionHint"]
    - path: web/src/components/PlayerPanel.tsx
      provides: "figcaption + canvas aria-describedby"
    - path: web/src/app.css
      provides: "select:focus-visible and 480 wrap polish"
  key_links:
    - from: web/src/components/PlayerPanel.tsx
      to: web/src/catalog/scenes.ts
      via: "props.interactionHint rendered as figcaption text"
      pattern: "interactionHint"
    - from: web/src/app.css
      to: "select:focus-visible"
      via: "shared 2px / 4px accent outline"
      pattern: "select:focus-visible"
---

<objective>
Replace the generic Canvas 2D figcaption with the locked per-scene instruction strings, close the select focus-ring gap, and polish the 480px stack without a design-system migration.

Purpose: D-08, D-09, D-10, D-11 and WEB-07. Keep every Phase 18 labeled control. Do not add hamburger, Space-to-pause, or MysticUI/Tailwind.
Output: `interactionHint` catalog field, figcaption binding, `select:focus-visible`, narrow-width CSS. No new scenes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@standards/core/frontend-ui.md
@standards-overrides.md
@.planning/phases/19-interaction-polish-and-browser-verification/19-CONTEXT.md
@.planning/phases/19-interaction-polish-and-browser-verification/19-UI-SPEC.md
@web/src/catalog/scenes.ts
@web/tests/scenes.test.ts
@web/src/components/PlayerPanel.tsx
@web/src/App.tsx
@web/src/app.css

<interfaces>
Exact `SceneRecord` addition:

```ts
export type SceneRecord = {
  // existing fields unchanged
  readonly interactionHint: string;
};
```

Exact strings from 19-UI-SPEC (copy verbatim):

| id | interactionHint |
|----|-----------------|
| dam-break | `Drag the obstacle to a new place in the basin. Labeled controls also work from the keyboard.` |
| fountain | `Drag on the canvas to aim the stream. Labeled controls also work from the keyboard.` |
| float-or-sink | `Click or tap the canvas to drop the selected body at that horizontal position. Labeled controls also work from the keyboard.` |
| color-mixer | `Drag on the canvas to stir the colored groups. Labeled controls also work from the keyboard.` |
| jelly-drop | `Click or tap the canvas to poke the jelly at that location. Labeled controls also work from the keyboard.` |
| water-wheel | `Drag on the canvas to aim the jet. Labeled controls also work from the keyboard.` |

`PlayerPanelProps` gains `readonly interactionHint: string`. Replace `CANVAS_CAPTION`. Render:

```tsx
<canvas
  aria-describedby="scene-interaction-hint"
  /* existing role="img" and aria-label stay */
>
...
<figcaption id="scene-interaction-hint">{props.interactionHint}</figcaption>
```

Do not set `tabindex` on the canvas. Do not add a third sentence or `Press Space to pause`.

Fallback views: `FallbackPanel` must not render a figcaption or Dam Break hint. App passes `scene()?.interactionHint` only inside the ready-scene `PlayerPanel` branch.

Focus CSS — extend the existing rule to include `select`:

```css
.product-control:focus-visible,
button:focus-visible,
select:focus-visible,
a:focus-visible {
  outline: 2px solid #39D3C7;
  outline-offset: 4px;
}
```

480px polish (D-10), keep tokens, add if missing:

```css
@media (max-width: 480px) {
  html,
  body,
  #root,
  main {
    min-width: 0;
  }

  .scene-control,
  .scene-control-label {
    width: 100%;
  }

  figcaption {
    overflow-wrap: break-word;
  }
}
```

Keep existing full-width `button`/`select` rules and `min-block-size: 44px`. Do not add a hamburger, orientation media, or breakpoint-specific scene list. Do not introduce Tailwind, MysticUI, `components.json`, or an icon package.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add locked interactionHint strings to the catalog</name>
  <files>web/src/catalog/scenes.ts, web/tests/scenes.test.ts</files>
  <read_first>
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-UI-SPEC.md` Per-scene interaction instructions
    1. `web/src/catalog/scenes.ts`
    1. `web/tests/scenes.test.ts`
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-CONTEXT.md` D-01, D-11
  </read_first>
  <behavior>
    1. Test: each of the six `SCENES` entries has the exact `interactionHint` string from the UI-SPEC table.
    1. Test: every hint includes `Labeled controls also work from the keyboard.` and none include `Press Space to pause` or `Live frame from this repository's Rust engine`.
    1. Test: control labels still include `Drop obstacle`, `Aim angle`, `Drop body`, `Stir speed`, `Poke jelly`, and `Jet strength`.
  </behavior>
  <action>
    Per D-01 and D-11, add `interactionHint: string` to `SceneRecord` and the six exact strings. Do not change descriptions, control ids, or credits. Update `web/tests/scenes.test.ts` with a `UI_SPEC_HINTS` record parallel to `UI_SPEC_DESCRIPTIONS`. Instruction text is a TypeScript string constant, never `innerHTML`.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/scenes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "interactionHint" web/src/catalog/scenes.ts` matches seven or more times (type + six scenes).
    1. `rg -n "Drag the obstacle to a new place in the basin" web/src/catalog/scenes.ts` matches.
    1. `rg -n "Press Space to pause|Live frame from this repository" web/src/catalog/scenes.ts` is empty.
    1. `rg -n "Drop obstacle|Poke jelly|Apply setting" web/src/catalog/scenes.ts` still matches the labeled path (Apply setting lives in controls chrome; Drop/Poke stay).
    1. `cd web && bun run test:unit -- tests/scenes.test.ts` exits 0.
  </acceptance_criteria>
  <done>Catalog data carries the locked two-sentence hint for every ready scene.</done>
</task>

<task type="auto">
  <name>Task 2: Bind figcaption, select focus, and 480px stacking</name>
  <files>web/src/components/PlayerPanel.tsx, web/src/App.tsx, web/src/app.css</files>
  <read_first>
    1. `web/src/components/PlayerPanel.tsx`
    1. `web/src/App.tsx`
    1. `web/src/app.css` lines 388–393 and 501–540
    1. `standards-overrides.md`
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-CONTEXT.md` D-08, D-09, D-10
  </read_first>
  <action>
    Per D-08, D-09, D-10, and D-11, pass `interactionHint={currentScene().interactionHint}` into `PlayerPanel`. Set `id="scene-interaction-hint"` and `aria-describedby="scene-interaction-hint"`. Delete `CANVAS_CAPTION`. Do not add `tabindex` or `onKeyDown` on the canvas or `window`. Add `select:focus-visible` to the existing 2px/4px `#39D3C7` rule. Add the 480px `min-width: 0` / full-width `.scene-control` polish. Do not steal focus on hash change. Do not add MysticUI, Tailwind, shadcn, or a hamburger. Keep `App.tsx` `<= 628` lines.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck &amp;&amp; rg -n "select:focus-visible|scene-interaction-hint|CANVAS_CAPTION|addEventListener\\(\"keydown\"|Space to pause" src/components/PlayerPanel.tsx src/App.tsx src/app.css</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "CANVAS_CAPTION|Live frame from this repository" web/src/components/PlayerPanel.tsx` is empty.
    1. `rg -n "scene-interaction-hint" web/src/components/PlayerPanel.tsx` matches both `id` and `aria-describedby`.
    1. `rg -n "tabindex|onKeyDown|keydown" web/src/components/PlayerPanel.tsx web/src/App.tsx` is empty.
    1. `rg -n "select:focus-visible" web/src/app.css` matches.
    1. `rg -n "outline: 2px solid #39D3C7" web/src/app.css` matches.
    1. `rg -n "min-width: 0" web/src/app.css` matches inside the 480px block or a global page rule.
    1. `rg -n "mysticui|tailwind|components.json|hamburger" web/src web/package.json` is empty (case insensitive).
    1. `cd web && bun run typecheck` exits 0.
    1. `wc -l web/src/App.tsx` is `<= 628`.
  </acceptance_criteria>
  <done>Keyboard users get focus rings on selects and a figcaption that names the pointer gesture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog strings → figcaption text | Instruction copy is rendered in the DOM. |
| Keyboard focus → controls | Focus styles must remain visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05-01 | T | XSS via hints | mitigate | Static catalog strings through JSX text; no `innerHTML`. |
| T-19-05-02 | T | Open redirect | mitigate | Do not build hrefs from hint or hash input; credits stay host-locked. |
| T-19-05-03 | D | keyboard trap | mitigate | No canvas tabindex; no custom key handlers; native controls only. |
| T-19-05-04 | I | pointer kinds in copy | accept | Hints do not expose `data-last-pointer-kind`. |
| T-19-05-05 | E | design-system swap | mitigate | D-09 forbids MysticUI/Tailwind until 2026-12-17. |
| T-19-05-06 | E | Pages / PAT | accept | No workflow changes. |
</threat_model>

<verification>
Run `cd web && bun run test:unit -- tests/scenes.test.ts && bun run typecheck`. Visual 375px proof waits for Plan 19-06. Do not deploy Pages.
</verification>

<success_criteria>
- Figcaption text matches the UI-SPEC table on ready scenes only.
- Selects share the accent focus ring.
- Narrow CSS stacks controls without a hamburger or Space shortcut.
</success_criteria>

<output>
After completion, create `.planning/phases/19-interaction-polish-and-browser-verification/19-05-SUMMARY.md`
</output>
