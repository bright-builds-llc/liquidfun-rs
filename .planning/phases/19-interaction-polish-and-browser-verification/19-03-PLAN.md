---
phase: 19-interaction-polish-and-browser-verification
plan: "03"
type: tdd
wave: 2
depends_on: [19-02]
files_modified:
  - crates/liquidfun-wasm/src/scene/color_mixer.rs
  - crates/liquidfun-wasm/src/scene/jelly_drop.rs
  - crates/liquidfun-wasm/src/scene/water_wheel.rs
autonomous: true
requirements: [WEB-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 19-2026-09-19T01-44-48
generated_at: 2026-09-19T02:10:00Z
must_haves:
  truths:
    - "Color Mixer drag stores maybe_pointer and applies per-particle engine forces near that point; cancel clears leftover force."
    - "Jelly Drop pointer-down applies per-particle impulses near the click; labeled poke-jelly still uses the first-third contiguous slice."
    - "Water Wheel drag steers jet direction from JET_POSITION (-3.9, 3.15) toward the pointer; cancel reverts to Vec2::new(speed, -0.4)."
    - "Localized stir and poke never call apply_particle_force_range / apply_particle_linear_impulse_range with a scattered nearby-id list."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/color_mixer.rs
      provides: "Pointer stir via Rust-internal per-particle forces"
    - path: crates/liquidfun-wasm/src/scene/jelly_drop.rs
      provides: "Pointer poke at world location"
    - path: crates/liquidfun-wasm/src/scene/water_wheel.rs
      provides: "Pointer jet aim override"
  key_links:
    - from: crates/liquidfun-wasm/src/scene/color_mixer.rs
      to: crates/liquidfun/src/world/particle_object/system.rs
      via: "apply_particle_force per nearby id"
      pattern: "apply_particle_force\\("
    - from: crates/liquidfun-wasm/src/scene/jelly_drop.rs
      to: crates/liquidfun/src/world/particle_object/system.rs
      via: "apply_particle_linear_impulse per nearby id"
      pattern: "apply_particle_linear_impulse\\("
    - from: crates/liquidfun-wasm/src/scene/water_wheel.rs
      to: crates/liquidfun-wasm/src/scene/water_wheel.rs
      via: "maybe_aim_velocity override in emit_jet"
      pattern: "maybe_aim_velocity"
---

<objective>
Replace the three compile stubs with Color Mixer stir, Jelly Drop poke-at-location, and Water Wheel jet steering, using Rust-internal per-particle engine calls.

Purpose: D-03 remaining gestures. `apply_particle_force_range` requires contiguous source ids, so nearby stir/poke must loop `apply_particle_force` / `apply_particle_linear_impulse` inside Rust (D-02 allows this; it is not a JS/Rust per-particle crossing).
Output: Three scene modules with cancel-clears-transient tests. No public `liquidfun` API expansion. No JavaScript position animation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@standards/languages/rust.md
@standards/core/testing.md
@.planning/phases/19-interaction-polish-and-browser-verification/19-CONTEXT.md
@.planning/phases/19-interaction-polish-and-browser-verification/19-RESEARCH.md
@.planning/phases/19-interaction-polish-and-browser-verification/19-UI-SPEC.md
@crates/liquidfun-wasm/src/scene/color_mixer.rs
@crates/liquidfun-wasm/src/scene/jelly_drop.rs
@crates/liquidfun-wasm/src/scene/water_wheel.rs
@crates/liquidfun/src/world/particle_object/system.rs

<interfaces>
Plan 19-02 already added `SceneHooks::apply_pointer` and `PointerKind`. Implement only these three files.

Color Mixer (`color_mixer.rs`):
```rust
const POINTER_STIR_RADIUS: f32 = 1.25;
const POINTER_STIR_FORCE: f32 = 12.0;
// ColorMixerHooks gains: maybe_pointer: Option<Vec2>
```
1. `Down`/`Move` store `maybe_pointer = Some(Vec2::new(world_x, world_y))`.
1. `Up`/`Cancel` set `maybe_pointer = None`.
1. In `on_advance`, keep labeled `stir_speed` global `apply_particle_force_range` on the full id list (`SLOW_STIR_FORCE (8,0)` / `FAST_STIR_FORCE (18,0)`).
1. After that, if `maybe_pointer` is `Some(origin)`, copy positions and ids, then for each particle with `(position - origin).length() <= POINTER_STIR_RADIUS` compute tangent `Vec2::new(-(p.y - origin.y), p.x - origin.x)`, normalize (skip length `< 1e-4`), scale by `POINTER_STIR_FORCE`, and call `world.apply_particle_force(id, force)` once per id. Never pass the nearby subset to `apply_particle_force_range`.

Jelly Drop (`jelly_drop.rs`):
```rust
const POINTER_POKE_RADIUS: f32 = 1.0;
// reuse POKE_IMPULSE = Vec2::new(0.0, -8.0)
```
1. `Down` only: for each group member whose position is within `POINTER_POKE_RADIUS`, call `world.apply_particle_linear_impulse(id, POKE_IMPULSE)`.
1. If none are nearby, return `Ok(())`.
1. `Move`/`Up`/`Cancel` are no-ops.
1. Labeled `poke-jelly` must still call `apply_particle_linear_impulse_range` on the first-third contiguous `members[..poke_len]` slice.

Water Wheel (`water_wheel.rs`):
```rust
// WaterWheelHooks gains: maybe_aim_velocity: Option<Vec2>
// JET_POSITION = Vec2::new(-3.9, 3.15)
```
1. `Down`/`Move`: `delta = Vec2::new(world_x + 3.9, world_y - 3.15)`; if `delta.length()` is finite and `> 1e-4`, store `maybe_aim_velocity = Some(delta * (self.jet_speed / length))`.
1. `Up` keeps the last override.
1. `Cancel` sets `maybe_aim_velocity = None`.
1. Change `emit_jet` to take `velocity: Vec2` and use `maybe_aim_velocity.unwrap_or(Vec2::new(speed, -0.4))`. Labeled `jet-strength` still sets speed. Wheel motor stays off.
</interfaces>
</context>

<feature>
  <name>Localized stir, poke, and steered jet</name>
  <files>crates/liquidfun-wasm/src/scene/color_mixer.rs, crates/liquidfun-wasm/src/scene/jelly_drop.rs, crates/liquidfun-wasm/src/scene/water_wheel.rs</files>
  <behavior>
    Native mappings for the remaining three scenes. Cases:
    - Color Mixer cancel after down+move leaves `maybe_pointer` None so a later advance does not keep applying tangent forces
    - Jelly pointer down near the blob changes some particle positions without changing count; labeled poke still uses the first-third range
    - Water Wheel pointer toward `(0.0, 3.15)` makes jet velocity x positive; cancel restores `(speed, -0.4)`
    - Nearby-id lists are never passed to `*_range` APIs
  </behavior>
  <implementation>
    Loop per-particle engine methods inside Rust. Do not expand `liquidfun`. Do not move particles from JavaScript.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stir Color Mixer at a stored world point</name>
  <files>crates/liquidfun-wasm/src/scene/color_mixer.rs</files>
  <read_first>
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-CONTEXT.md` D-03, D-05
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-RESEARCH.md` Pattern 4 and Pitfall 4
    1. `crates/liquidfun-wasm/src/scene/color_mixer.rs`
    1. `crates/liquidfun/src/world/particle_object/system.rs`
  </read_first>
  <behavior>
    1. Test: `apply_pointer(Down, 0.0, 2.2)` then `advance(1)` changes at least one particle position versus a no-pointer control world; colors still come from engine lanes.
    1. Test: `apply_pointer(Cancel, 0.0, 0.0)` after a drag, then `advance(4)` while `stir-speed=off`, does not keep applying a leftover local force (position delta after cancel matches a fresh off-stir session within a loose 0.05 bound, or `maybe_pointer` test via a `#[cfg(test)]` getter).
    1. Test: `apply_control("stir-speed", "slow")` still applies `(8, 0)` globally.
    1. Test: color_mixer.rs pointer path contains `apply_particle_force(` and does not call `apply_particle_force_range` with a nearby subset (the labeled global stir may still use range on the full id vec).
  </behavior>
  <action>
    Per D-03 and D-05, replace the 19-02 stub. Add `maybe_pointer: Option<Vec2>` on `ColorMixerHooks`. Implement the radius/tangent loop with `apply_particle_force` per id as specified. Keep labeled stir. Do not use `innerHTML`. Do not animate positions in a test harness outside the engine. If a single-id force errors, map to `SessionError::SceneConstruction` and stop the loop rather than panic.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm --lib color_mixer -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "maybe_pointer" crates/liquidfun-wasm/src/scene/color_mixer.rs` matches.
    1. `rg -n "apply_particle_force\\(" crates/liquidfun-wasm/src/scene/color_mixer.rs` matches.
    1. `rg -n "POINTER_STIR_RADIUS" crates/liquidfun-wasm/src/scene/color_mixer.rs` matches `1.25`.
    1. `rg -n "fn apply_pointer" crates/liquidfun-wasm/src/scene/color_mixer.rs` matches and is not a blanket `Ok(())` stub for `Down`.
    1. `cargo test -p liquidfun-wasm --lib color_mixer` exits 0.
  </acceptance_criteria>
  <done>Color Mixer pointer stir is engine forces in Rust, and cancel clears the stored point.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Poke Jelly at the click and steer the Water Wheel jet</name>
  <files>crates/liquidfun-wasm/src/scene/jelly_drop.rs, crates/liquidfun-wasm/src/scene/water_wheel.rs</files>
  <read_first>
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-UI-SPEC.md` Gesture map
    1. `crates/liquidfun-wasm/src/scene/jelly_drop.rs`
    1. `crates/liquidfun-wasm/src/scene/water_wheel.rs`
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-RESEARCH.md` Pattern 4
  </read_first>
  <behavior>
    1. Test: Jelly `apply_pointer(Down, 0.0, 2.0)` changes some particle positions and keeps the same particle count; `Cancel` after down does not apply a second impulse.
    1. Test: labeled `poke-jelly` still uses `apply_particle_linear_impulse_range` on a first-third slice.
    1. Test: Water Wheel `apply_pointer(Down, 0.0, 3.15)` then `advance(1)` emits with a positive x velocity component; `Cancel` then `advance(1)` uses `Vec2::new(speed, -0.4)` again.
    1. Test: Water Wheel `apply_control("jet-strength", "strong")` still sets speed `12.0`; no `enable_motor` / motor speed write.
  </behavior>
  <action>
    Per D-03 discretion (Water Wheel aims direction, not strength): implement the Jelly radius poke with per-id `apply_particle_linear_impulse` and the Water Wheel `maybe_aim_velocity` override. Keep `JET_POSITION (-3.9, 3.15)` and `EMIT_PER_STEP = 2`. Do not enable the revolute motor. Do not expand the public `liquidfun` API. Do not edit `scene.rs` or `session.rs`.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm --lib -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "apply_particle_linear_impulse\\(" crates/liquidfun-wasm/src/scene/jelly_drop.rs` matches.
    1. `rg -n "apply_particle_linear_impulse_range" crates/liquidfun-wasm/src/scene/jelly_drop.rs` still matches the labeled poke.
    1. `rg -n "maybe_aim_velocity" crates/liquidfun-wasm/src/scene/water_wheel.rs` matches.
    1. `rg -n "enable_motor|motor_speed" crates/liquidfun-wasm/src/scene/water_wheel.rs` is empty.
    1. `rg -n "Vec2::new\\(speed, -0.4\\)" crates/liquidfun-wasm/src/scene/water_wheel.rs` matches the cancel default.
    1. `cargo test -p liquidfun-wasm --lib` exits 0.
  </acceptance_criteria>
  <done>Jelly pokes at the click and the wheel jet steers from the fixed nozzle without scripted rotation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| World x/y → nearby particle selection | Pointer samples choose which particles receive force. |
| Force/impulse vectors → engine | Non-finite or range API misuse can poison the session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | D | Color Mixer leftover stir | mitigate | `Cancel`/`Up` clear `maybe_pointer` before the next `on_advance`. |
| T-19-03-02 | T | Scattered ids + range API | mitigate | Localized stir/poke use per-particle methods only; range stays on contiguous labeled poke / full-system stir. |
| T-19-03-03 | D | Water Wheel stuck aim | mitigate | `Cancel` clears `maybe_aim_velocity` back to `(speed, -0.4)`. |
| T-19-03-04 | I | Particle ids | mitigate | Ids stay inside Rust hooks; no wasm export. |
| T-19-03-05 | T | Public engine expansion | mitigate | Do not add new `liquidfun` APIs. |
| T-19-03-06 | E | Pages / PAT | accept | No workflow changes. |
</threat_model>

<verification>
Run `cargo test -p liquidfun-wasm --lib`. Do not run Playwright or deploy Pages.
</verification>

<success_criteria>
- All six scene modules handle the four pointer kinds with native physics.
- Cancel clears Color Mixer force and Water Wheel aim override.
- Range APIs are never fed a scattered nearby-id list.
</success_criteria>

<output>
After completion, create `.planning/phases/19-interaction-polish-and-browser-verification/19-03-SUMMARY.md`
</output>
