# Phase 19 deferred items

Discovered during 19-07 and left out of scope (pre-existing, not caused by
this plan's files).

- Bright Builds `file-lengths` on push `d3d8688` failed for
  `crates/liquidfun-wasm/src/scene/color_mixer.rs` (656),
  `crates/liquidfun-wasm/src/scene/dam_break.rs` (652), and
  `crates/liquidfun-wasm/src/scene/water_wheel.rs` (811). Pages
  `deploy-pages` still succeeded. Split or exception belongs to a later
  cleanup, not this hosted-evidence plan.
