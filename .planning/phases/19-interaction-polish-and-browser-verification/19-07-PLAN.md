---
phase: 19-interaction-polish-and-browser-verification
plan: "07"
type: execute
wave: 5
depends_on: [19-06]
files_modified:
  - .planning/phases/19-interaction-polish-and-browser-verification/19-HOST-EVIDENCE.md
  - .planning/phases/19-interaction-polish-and-browser-verification/19-REVIEW.md
  - README.md
  - TESTING.md
autonomous: true
requirements: [WEBTEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 19-2026-09-19T01-44-48
generated_at: 2026-09-19T02:10:00Z
must_haves:
  truths:
    - "A fresh main deploy records page_url, source_sha, workflow_run_url, wasm_asset_url, and checked_at that are not the Phase 17 SHA 50a15562b356ed941266eedddc636df3f76e7e7e."
    - "Live JS/WASM loads under /liquidfun-rs/ with Content-Type application/wasm, and all six #/scene/{id} hashes return the app shell."
    - "A separate identified AI reviewer acknowledges the Phase 19 diff and evidence at an exact SHA-256 digest; the implementing agent does not approve its own work."
  artifacts:
    - path: .planning/phases/19-interaction-polish-and-browser-verification/19-HOST-EVIDENCE.md
      provides: "Live Pages URL, SHA, workflow, WASM MIME, six hash checks"
    - path: .planning/phases/19-interaction-polish-and-browser-verification/19-REVIEW.md
      provides: "Independent exact-digest acknowledgment"
  key_links:
    - from: .planning/phases/19-interaction-polish-and-browser-verification/19-HOST-EVIDENCE.md
      to: "https://bright-builds-llc.github.io/liquidfun-rs/"
      via: "Recorded live origin plus six hash URLs"
      pattern: "#/scene/"
    - from: .planning/phases/19-interaction-polish-and-browser-verification/19-REVIEW.md
      to: "digest"
      via: "Separate reviewer identity and SHA-256"
      pattern: "digest"
---

<objective>
Push the phase to main, record live Pages evidence for all six scene hashes, and obtain independent AI review.

Purpose: D-13 and D-14 close WEBTEST-01 hosted half. Standing 2026-09-13 authorization covers an ordinary non-force push to `main` on `bright-builds-llc/liquidfun-rs`. Package publication remains unauthorized. The implementing agent must not acknowledge its own digest.
Output: `19-HOST-EVIDENCE.md`, `19-REVIEW.md`, docs that cite the new URL/SHA.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@README.md
@TESTING.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-HOST-EVIDENCE.md
@.planning/phases/17-shared-player-and-early-pages-delivery/17-REVIEW.md
@.planning/phases/18-six-native-physics-demos/18-REVIEW.md
@.github/workflows/pages.yml

<interfaces>
Expected hosted origin until Pages settings prove otherwise: `https://bright-builds-llc.github.io/liquidfun-rs/`.

`19-HOST-EVIDENCE.md` must contain these literal field labels:
1. `page_url:`
1. `source_sha:` (full 40-hex SHA of the deployed main commit)
1. `workflow_run_url:`
1. `wasm_asset_url:` (a `/liquidfun-rs/assets/*.wasm` URL)
1. `checked_at:` ISO timestamp

Required live checks (record the command and result):
1. `GET {page_url}` HTTP 200 HTML containing `liquidfun-rs playground` and `/liquidfun-rs/assets/`.
1. WASM GET returns HTTP 200, `Content-Type: application/wasm` (not `text/html`), body starts with `\0asm`.
1. Refresh each of:
   - `{page_url}#/scene/dam-break`
   - `{page_url}#/scene/fountain`
   - `{page_url}#/scene/float-or-sink`
   - `{page_url}#/scene/color-mixer`
   - `{page_url}#/scene/jelly-drop`
   - `{page_url}#/scene/water-wheel`
   App-shell HTML must contain `liquidfun-rs playground`. Prefer a headed or headless Chromium open of each hash that waits for `Playing` and the scene title; if only curl is used for hashes, still open at least Dam Break and one other scene in Chromium to prove JS/WASM execute.
1. `source_sha` must not equal `50a15562b356ed941266eedddc636df3f76e7e7e`.
1. Do not add Playwright to `.github/workflows/pages.yml`.
1. Recheck remote `bright-builds-llc/liquidfun-rs`, ref `main`, workflow `.github/workflows/pages.yml` before push.

Independent review:
1. Build a manifest over pointer/camera/input files, six `scene/*.rs` pointer mappings, `player.spec.ts`, smoke log, and `19-HOST-EVIDENCE.md`.
1. Compute one lowercase SHA-256 digest.
1. Launch a separate identified AI reviewer (not this implementing agent) to inspect the complete relevant diff and evidence.
1. Write `19-REVIEW.md` bound to that digest, actual reviewer identity, and actual ISO review time. State AI review honestly. Passing `just web-player-smoke` is not the acknowledgment.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy main and write 19-HOST-EVIDENCE.md</name>
  <files>.planning/phases/19-interaction-polish-and-browser-verification/19-HOST-EVIDENCE.md, README.md, TESTING.md</files>
  <read_first>
    1. `AGENTS.md` Standing authorization for autonomous iteration
    1. `.planning/phases/17-shared-player-and-early-pages-delivery/17-HOST-EVIDENCE.md`
    1. `.github/workflows/pages.yml`
    1. `README.md`
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-CONTEXT.md` D-13
  </read_first>
  <action>
    Per D-13 and standing 2026-09-13 authorization, perform an ordinary non-force push to `main` on `bright-builds-llc/liquidfun-rs` after rechecking repository, ref, and workflow identity. Wait for workflow `Pages` on that SHA to finish `deploy-pages` successfully. Write `19-HOST-EVIDENCE.md` with the required fields and six hash-route checks. Update README/TESTING to cite this URL and SHA, not Phase 17 SHA `50a1556`. Do not create a release tag. Do not publish npm or crates. Do not force-push. Do not add Playwright to the Pages workflow. Authority line: standing 2026-09-13 authorization in `AGENTS.md`. If README/TESTING change, run `just markdown-check`.
  </action>
  <verify>
    <automated>rg -n "page_url:|source_sha:|workflow_run_url:|wasm_asset_url:|#/scene/dam-break|#/scene/water-wheel|50a15562" .planning/phases/19-interaction-polish-and-browser-verification/19-HOST-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    1. `19-HOST-EVIDENCE.md` contains `page_url:`, `source_sha:`, `workflow_run_url:`, `wasm_asset_url:`, and `checked_at:`.
    1. `source_sha` is 40 lowercase hex and is not `50a15562b356ed941266eedddc636df3f76e7e7e`.
    1. The file lists all six `#/scene/` ids: `dam-break`, `fountain`, `float-or-sink`, `color-mixer`, `jelly-drop`, `water-wheel`.
    1. The file records WASM `Content-Type: application/wasm`.
    1. `rg -n "playwright" .github/workflows/pages.yml` is empty.
    1. README or TESTING mentions the recorded Pages origin.
  </acceptance_criteria>
  <done>Live Pages evidence is bound to this phase's SHA and the complete six-scene gallery.</done>
</task>

<task type="auto">
  <name>Task 2: Obtain independent exact-digest AI review</name>
  <files>.planning/phases/19-interaction-polish-and-browser-verification/19-REVIEW.md</files>
  <read_first>
    1. `AGENTS.md` Independent review
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-CONTEXT.md` D-14
    1. `.planning/phases/18-six-native-physics-demos/18-REVIEW.md`
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-HOST-EVIDENCE.md`
  </read_first>
  <action>
    Per D-14, the implementing agent must not approve its own work. Build the review manifest and lowercase SHA-256 digest over the Phase 19 implementation, `player.spec.ts`, smoke evidence, and `19-HOST-EVIDENCE.md`. Launch a separate identified AI reviewer to inspect the complete relevant diff and evidence and record an acknowledgment bound to that exact digest, the reviewer's actual identity, and the actual review time. Write `19-REVIEW.md` identifying AI review honestly. Do not treat passing automated checks as the acknowledgment. Do not publish a crate.
  </action>
  <verify>
    <automated>rg -n "digest|reviewer|acknowled" .planning/phases/19-interaction-polish-and-browser-verification/19-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    1. `19-REVIEW.md` contains a 64-character hex digest, a reviewer identity, and an ISO timestamp.
    1. The reviewer identity is not the implementing agent label.
    1. The file states independent AI review, not human approval.
    1. The acknowledgment quotes the same digest as the manifest.
    1. No self-approval sentence from the implementing agent is used as the acknowledgment.
  </acceptance_criteria>
  <done>Independent review is bound to the exact Phase 19 digest and hosted evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → Pages origin | Deployed assets are public. |
| Review digest → acknowledgment | Only a separate reviewer may bind approval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07-01 | T | stale HOST proof | mitigate | New file bound to this SHA; forbid reuse of `50a1556`. |
| T-19-07-02 | T | WASM MIME confusion | mitigate | Require `application/wasm` and `\0asm` magic. |
| T-19-07-03 | S | hash-route handling | mitigate | Record all six allowlisted hashes; unknown remains a fallback (already shipped). |
| T-19-07-04 | R | self-review | mitigate | Separate AI reviewer + exact digest (D-14). |
| T-19-07-05 | E | PAT / force push | mitigate | Ordinary non-force push; existing `pages: write` job permissions; no PAT. |
| T-19-07-06 | E | package release | accept | Publication remains unauthorized; do not tag. |
</threat_model>

<verification>
Confirm `19-HOST-EVIDENCE.md` fields and `19-REVIEW.md` digest identity. Do not claim Firefox/Safari coverage. Do not format `.planning/**` with mdformat.
</verification>

<success_criteria>
- Live Pages URL and SHA are recorded for the complete gallery.
- WASM loads under `/liquidfun-rs/` with the correct content type.
- Independent AI review acknowledges the exact digest.
</success_criteria>

<output>
After completion, create `.planning/phases/19-interaction-polish-and-browser-verification/19-07-SUMMARY.md`
</output>
