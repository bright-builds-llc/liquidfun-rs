---
phase: 19-interaction-polish-and-browser-verification
plan: "06"
type: execute
wave: 4
depends_on: [19-03, 19-05]
files_modified:
  - web/e2e/player.spec.ts
  - README.md
  - TESTING.md
autonomous: true
requirements: [WEBTEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 19-2026-09-19T01-44-48
generated_at: 2026-09-19T02:10:00Z
must_haves:
  truths:
    - "just web-player-smoke on /liquidfun-rs/ covers all six scenes with visible stepping, one pointer gesture, one labeled control, and existing reset/switch cases."
    - "Dam Break pointercancel sets data-last-pointer-kind to cancel, and a resize-then-drag still increments data-pointer-accepted."
    - "A 375×812 pass Tabs to a select with a visible focus ring, wraps without requiring a second Playwright project, and can scroll the catalog."
    - "Hidden-tab recovery still caps the next step delta at 4 after at least one pointer gesture."
  artifacts:
    - path: web/e2e/player.spec.ts
      provides: "Pointer, cancel, resize, 375px, and six-scene WEBTEST-01 coverage"
    - path: README.md
      provides: "Honest mention of pointer + Chromium player smoke"
    - path: TESTING.md
      provides: "just web-player-smoke is the local WEBTEST-01 gate"
  key_links:
    - from: web/e2e/player.spec.ts
      to: "/liquidfun-rs/#/scene/{id}"
      via: "Existing SCENE_HASH_PATHS plus pointer helpers"
      pattern: "data-last-pointer-kind"
    - from: web/e2e/player.spec.ts
      to: "375×812 viewport"
      via: "page.setViewportSize before goto"
      pattern: "width: 375"
---

<objective>
Extend the existing Chromium Playwright suite so `just web-player-smoke` proves pointer, cancel, resize, keyboard focus, and page scroll on the production-base build.

Purpose: D-12 / WEBTEST-01 local half. Chromium only. No Playwright in Pages CI. Visual screenshot hashes are optional diagnostics, not the oracle.
Output: Updated `player.spec.ts`, passing `just web-player-smoke`, honest README/TESTING lines.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@.planning/phases/19-interaction-polish-and-browser-verification/19-CONTEXT.md
@.planning/phases/19-interaction-polish-and-browser-verification/19-UI-SPEC.md
@web/e2e/player.spec.ts
@web/playwright.config.ts
@README.md
@TESTING.md

<interfaces>
Keep `playwright.config.ts` at one `projects` entry: `chromium` / Desktop Chrome. Do not add Firefox, WebKit, or a second project. Keep `baseURL http://127.0.0.1:4173` and `/liquidfun-rs/`.

Keep every existing test (Dam Break pause/play/reset, six-scene open/credits/reset, unknown hash, Dam Break→Fountain dispose, Gravity Apply setting, hidden-tab max-4).

Add helpers and tests:

```ts
async function dragCanvas(page: Page, startRatio = { x: 0.5, y: 0.45 }, endRatio = { x: 0.7, y: 0.35 }): Promise<void> {
  const box = await page.locator("canvas").boundingBox();
  if (box === null) throw new Error("canvas box missing");
  await page.mouse.move(box.x + box.width * startRatio.x, box.y + box.height * startRatio.y);
  await page.mouse.down();
  await page.mouse.move(box.x + box.width * endRatio.x, box.y + box.height * endRatio.y);
  await page.mouse.up();
}

const POINTER_CONTROL: Readonly<Record<SceneId, { gesture: "drag" | "click"; control: string }>> = {
  "dam-break": { gesture: "drag", control: "Drop obstacle" },
  fountain: { gesture: "drag", control: "Aim angle" },
  "float-or-sink": { gesture: "click", control: "Drop body" },
  "color-mixer": { gesture: "drag", control: "Stir speed" },
  "jelly-drop": { gesture: "click", control: "Poke jelly" },
  "water-wheel": { gesture: "drag", control: "Jet strength" },
};
```

Required new cases (use `test.setTimeout(120_000)` on any six-scene loop):
1. For each `SCENE_IDS` entry: open hash, wait `Playing`, assert `data-step-index > 0`, perform the mapped gesture, assert `data-pointer-accepted` is a positive integer, assert `data-last-pointer-kind` is `up` or `down`, activate the mapped labeled control (select change for Aim angle / Stir speed / Jet strength; button click otherwise), keep `Playing`.
1. Dam Break: mouse down, `canvas.dispatchEvent("pointercancel", { pointerId: 1 })`, expect `main` `data-last-pointer-kind` `cancel`. Do not require `event.isTrusted`.
1. Dam Break: `page.setViewportSize({ width: 1280, height: 720 })`, `goto`, play, then `setViewportSize({ width: 375, height: 812 })`, wait 200ms, `dragCanvas`, expect `data-pointer-accepted` to increase. Do not reconstruct a world in the assertion (step index may keep growing).
1. After one Dam Break drag, rerun the existing hidden-tab max-4 proof (reuse helpers).
1. Narrow pass: `setViewportSize({ width: 375, height: 812 })` **before** `goto("/liquidfun-rs/#/scene/dam-break")`. Assert figcaption text `Drag the obstacle to a new place in the basin. Labeled controls also work from the keyboard.` Tab until `page.locator("select").first()` is focused (`toBeFocused`). Assert `document.scrollingElement.scrollHeight > window.innerHeight` after `locator(".catalog-nav").evaluate((node) => node.scrollIntoView())`. Assert no Playwright project named `firefox` or `webkit`.

Do not add screenshot-hash goldens as the pass condition. Do not edit `.github/workflows/pages.yml`. Rebuild WASM before smoke: `just web-wasm` then `just web-player-smoke` (player-smoke already rebuilds; still run `just web-wasm` first so scene pointer_action is in the artifact).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend player.spec.ts with pointer, cancel, resize, and 375px</name>
  <files>web/e2e/player.spec.ts</files>
  <read_first>
    1. `web/e2e/player.spec.ts`
    1. `web/playwright.config.ts`
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-CONTEXT.md` D-12
    1. `.planning/phases/19-interaction-polish-and-browser-verification/19-UI-SPEC.md` Focused Browser Evidence
  </read_first>
  <action>
    Per D-12, add the helpers and tests in `<interfaces>`. Extract helpers if the file would exceed 400 lines. Keep Chromium-only config. Assert `data-step-index` and `data-last-pointer-kind` / `data-pointer-accepted`, not PNG hashes. Do not add `isTrusted` checks. Do not add Firefox/WebKit projects.
  </action>
  <verify>
    <automated>rg -n "data-last-pointer-kind|width: 375|pointercancel|Drop obstacle" web/e2e/player.spec.ts &amp;&amp; rg -n "firefox|webkit" web/playwright.config.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "data-last-pointer-kind" web/e2e/player.spec.ts` matches.
    1. `rg -n "width: 375" web/e2e/player.spec.ts` matches.
    1. `rg -n "pointercancel" web/e2e/player.spec.ts` matches.
    1. `rg -n "SIX_SCENE_TIMEOUT_MS|setTimeout\\(120_000\\)" web/e2e/player.spec.ts` matches.
    1. `rg -n "firefox|webkit" web/playwright.config.ts` is empty.
    1. Existing tests for `Apply setting`, `Scene not found`, and hidden-tab remain.
  </acceptance_criteria>
  <done>The spec file encodes WEBTEST-01 local cases without a new browser matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Run just web-player-smoke and document the local gate</name>
  <files>README.md, TESTING.md</files>
  <read_first>
    1. `README.md`
    1. `TESTING.md`
    1. `justfile`
    1. `.github/workflows/pages.yml`
  </read_first>
  <action>
    Per D-12, run `just web-wasm` then `just web-player-smoke` once. Keep logs under ignored `target/web-build/` if the recipe already writes there. Update README/TESTING to say `just web-player-smoke` exercises six scenes, playback/reset, representative pointer/control input, pointercancel, resize-then-drag, hidden-tab, and a 375px keyboard/scroll pass against `/liquidfun-rs/`. Do not claim Firefox/Safari coverage, complete LiquidFun parity, pigment chemistry, live card previews, or that Phase 17 `17-HOST-EVIDENCE.md` SHA `50a1556` is the Phase 19 Pages proof. Do not add Playwright to `.github/workflows/pages.yml`. If README or TESTING change, run `just markdown-check`.
  </action>
  <verify>
    <automated>just web-player-smoke</automated>
  </verify>
  <acceptance_criteria>
    1. `just web-player-smoke` exits 0.
    1. `rg -n "web-player-smoke" README.md TESTING.md` matches.
    1. `rg -n "50a1556" README.md TESTING.md` is empty.
    1. `rg -n "playwright|web-player-smoke" .github/workflows/pages.yml` is empty.
    1. `git diff --name-only .github/workflows/pages.yml` is empty (workflow file unchanged).
  </acceptance_criteria>
  <done>Local Chromium proves the production-subpath player including pointer and 375px paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preview server → Chromium | Tests load real JS/WASM under `/liquidfun-rs/`. |
| Synthetic pointercancel | Untrusted dispatch must still cancel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06-01 | T | production-base assets | mitigate | Spec stays on `/liquidfun-rs/` WASM URLs. |
| T-19-06-02 | D | stuck gesture | mitigate | Assert `data-last-pointer-kind` `cancel` after `pointercancel`. |
| T-19-06-03 | T | screenshot-oracle spoof | mitigate | Primary oracle is DOM attributes and step index, not PNG hashes. |
| T-19-06-04 | E | CI browser matrix | mitigate | Do not add Playwright to Pages workflow; Chromium only. |
| T-19-06-05 | T | XSS in instructions | mitigate | Assert the fixed Dam Break figcaption string. |
| T-19-06-06 | E | Pages / PAT | accept | No deploy in this plan. |
</threat_model>

<verification>
`just web-player-smoke` is the plan gate. Do not push to main here. Do not treat `17-HOST-EVIDENCE.md` as Phase 19 proof.
</verification>

<success_criteria>
- All six scenes receive a pointer gesture and a labeled control in Chromium.
- Cancel, resize-then-drag, hidden-tab, and 375px keyboard/scroll pass.
- Pages workflow still has no Playwright job.
</success_criteria>

<output>
After completion, create `.planning/phases/19-interaction-polish-and-browser-verification/19-06-SUMMARY.md`
</output>
