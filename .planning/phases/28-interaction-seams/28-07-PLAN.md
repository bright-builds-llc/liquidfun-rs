---
phase: 28-interaction-seams
plan: "07"
type: tdd
wave: 7
depends_on: ["06"]
files_modified:
  - web/tests/scenes.test.ts
  - web/tests/navigation.test.ts
  - web/scripts/demo-media/model.ts
  - web/tests/demo-media-model.test.ts
autonomous: true
requirements: [ACT-01, ACT-02, ACT-03, ACT-04, ACT-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-09-22T12-56-07
generated_at: 2026-09-22T13:15:00Z
must_haves:
  truths:
    - "Vitest locks sixteen SCENE_IDS in order with UI-SPEC descriptions, hints, controls, and pinned credit URLs for the five interaction scenes."
    - "SCENE_CAPTURE_PLANS length and order match SCENE_IDS including the five new ids."
  artifacts:
    - path: web/tests/scenes.test.ts
      provides: "Sixteen-scene catalog contract tests"
    - path: web/scripts/demo-media/model.ts
      provides: "Capture plans for five interaction scenes"
  key_links:
    - from: web/tests/scenes.test.ts
      to: web/src/catalog/scenes.ts
      via: "UI_SPEC_DESCRIPTIONS / control / credit asserts"
      pattern: "soup|soup-stirrer|impulse|wave-machine|theo-jansen"
    - from: web/scripts/demo-media/model.ts
      to: web/src/catalog/scenes.ts
      via: "assertSceneCapturePlanCoverage"
      pattern: "SCENE_CAPTURE_PLANS"
---

<objective>
Lock the sixteen-scene catalog contract in Vitest and keep demo-media capture plans synchronized with SCENE_IDS.

Purpose: Prevent eleven-scene hardcoding rot before Chromium smoke (research Pitfall 8; Phase 27 lesson on incomplete Record tables).
Output: Updated unit tests and capture plan stubs for the five interaction scenes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@.planning/phases/28-interaction-seams/28-UI-SPEC.md
@.planning/phases/28-interaction-seams/28-RESEARCH.md
@.planning/phases/28-interaction-seams/28-06-SUMMARY.md
@web/src/catalog/scenes.ts
@web/tests/scenes.test.ts
@web/scripts/demo-media/model.ts

<interfaces>
Tests must assert SCENE_IDS === sixteen ids ending with:
`soup`, `soup-stirrer`, `impulse`, `wave-machine`, `theo-jansen`

Watch-first hint for Soup and Wave Machine:
`This scene is watch-first. Use Play scene, Pause scene, and Reset scene.`

Stirrer hint exact UI-SPEC string; Impulse and Theo hints exact UI-SPEC strings.

Controls:
- soup / wave-machine: length 0
- soup-stirrer: action id `toggle-paddle-rail` label `Toggle paddle rail`
- impulse: preset id `push-mode` label `Push` values `force`/`impulse`
- theo-jansen: preset id `motor-direction` label `Motor direction` values `forward`/`reverse`

Pin commit in inspiration hrefs: `7f20402173fd143a3988c921bc384459c6a858f2`

Capture plans: append entries with matching id/title/route; watch-first (soup, wave-machine) use center click SceneAction stub (pointer no-op); interactive scenes use their real gesture (stirrer click, impulse click, theo may use control-only — keep a SceneAction stub if capture script requires one). Keep `interactionStep: 180` unless existing pattern differs.
</interfaces>
</context>

<feature>
  <name>Sixteen-scene catalog unit contract</name>
  <files>web/tests/scenes.test.ts, web/scripts/demo-media/model.ts</files>
  <behavior>
    - lists sixteen locked scenes in approved order
    - descriptions/hints/controls match UI-SPEC for five new scenes
    - inspiration hrefs include ten pinned JS/C++ files at the pin
    - implementation paths end with soup.rs / soup_stirrer.rs / impulse.rs / wave_machine.rs / theo_jansen.rs and never google/liquidfun
    - SCENE_CAPTURE_PLANS covers all SCENE_IDS in order
  </behavior>
  <implementation>
    Update Vitest tables and capture plans only. No Playwright in this plan.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand scenes.test.ts to sixteen scenes and controls</name>
  <files>web/tests/scenes.test.ts, web/tests/navigation.test.ts</files>
  <read_first>
    1. `.planning/phases/28-interaction-seams/28-UI-SPEC.md` Catalog demo descriptions + Scene controls + Credits
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-01, D-04–D-08, D-21–D-23
    1. `web/tests/scenes.test.ts` eleven-scene tables
    1. `web/src/catalog/scenes.ts` Plan 06 records
    1. `web/tests/navigation.test.ts` any eleven hardcoding
  </read_first>
  <behavior>
    - SCENE_IDS length 16 and approved order
    - five new descriptions/hints exact
    - controls contracts for stirrer/impulse/theo
    - credits pin + ten labels
    - no sealed-parity strings
  </behavior>
  <action>
    Per D-01/D-04–D-08/D-22: rewrite eleven→sixteen asserts in `scenes.test.ts` (and navigation tests if they hardcode eleven). Assert UI_SPEC descriptions, WATCH_FIRST for soup/wave-machine, exact interactive hints, control ids/labels/values, inspiration href pin, host-locked implementation paths. Do not run Playwright here.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run test -- tests/scenes.test.ts tests/navigation.test.ts 2&gt;&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'sixteen|16' web/tests/scenes.test.ts` matches updated count language (or explicit length 16 assert).
    1. `rg -n 'soup-stirrer|toggle-paddle-rail|push-mode|motor-direction' web/tests/scenes.test.ts` matches.
    1. `rg -n '7f20402173fd143a3988c921bc384459c6a858f2' web/tests/scenes.test.ts` matches.
    1. `rg -n 'All eleven|lists eleven' web/tests/scenes.test.ts` is empty.
    1. `cd web && bun run test -- tests/scenes.test.ts tests/navigation.test.ts` exits 0.
  </acceptance_criteria>
  <done>Vitest locks the sixteen-scene catalog contract including interactive controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sync SCENE_CAPTURE_PLANS for five interaction scenes</name>
  <files>web/scripts/demo-media/model.ts, web/tests/demo-media-model.test.ts</files>
  <read_first>
    1. `web/scripts/demo-media/model.ts` existing watch-first stubs
    1. `web/tests/demo-media-model.test.ts`
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-24
    1. `.planning/phases/27-material-flag-groups/27-05-PLAN.md` capture stub pattern
  </read_first>
  <behavior>
    - SCENE_CAPTURE_PLANS covers all sixteen SCENE_IDS in order
    - assertSceneCapturePlanCoverage still passes
  </behavior>
  <action>
    Append capture plans for `soup`, `soup-stirrer`, `impulse`, `wave-machine`, `theo-jansen` with matching titles/routes. Watch-first soup/wave-machine use center-click SceneAction stub. Interactive scenes include a SceneAction the capture script can emit (click for stirrer/impulse; control or click stub for theo). Update demo-media-model tests for sixteen coverage. Do not change MAX_ADVANCE_STEPS or particle counts.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run test -- tests/demo-media-model.test.ts 2&gt;&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'soup|soup-stirrer|impulse|wave-machine|theo-jansen' web/scripts/demo-media/model.ts` matches all five plan ids.
    1. `rg -n 'SCENE_CAPTURE_PLANS' web/tests/demo-media-model.test.ts` still asserts coverage.
    1. `cd web && bun run test -- tests/demo-media-model.test.ts` exits 0.
    1. `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Capture plans and Vitest coverage match the sixteen-scene catalog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit tests → catalog honesty | Tests are the gate that catalog copy matches allowlisted WASM ids. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07-01 | I | Catalog/test drift | mitigate | Assert exact SCENE_IDS order, control ids, and pinned credit URLs. |
| T-28-07-02 | T | Capture plan orphans | mitigate | assertSceneCapturePlanCoverage requires 1:1 with SCENE_IDS. |
| T-28-07-03 | S | google/liquidfun as implementation | mitigate | Assert implementation paths never contain google/liquidfun. |
</threat_model>

<verification>
`cd web && bun run test -- tests/scenes.test.ts tests/navigation.test.ts tests/demo-media-model.test.ts` and `bun run typecheck` exit 0.
</verification>

<success_criteria>
1. Sixteen-scene Vitest contract covers ACT control metadata and credits.
1. Capture plans stay synchronized with SCENE_IDS.
</success_criteria>

<output>
After completion, create `.planning/phases/28-interaction-seams/28-07-SUMMARY.md`
</output>
