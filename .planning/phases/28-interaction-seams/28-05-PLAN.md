---
phase: 28-interaction-seams
plan: "05"
type: tdd
wave: 5
depends_on: ["04"]
files_modified:
  - crates/liquidfun-wasm/src/scene.rs
  - crates/liquidfun-wasm/src/scene/theo_jansen.rs
  - crates/liquidfun-wasm/src/session/tests.rs
autonomous: true
requirements: [ACT-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-09-22T12-56-07
generated_at: 2026-09-22T13:15:00Z
must_haves:
  truths:
    - "Visitor can construct Theo Jansen with ground, walls, soft-distance legs, collision filter groupIndex -1, motorized revolute, and particle load."
    - "Live motor-direction forward/reverse flips set_revolute_motor_speed sign without recreating the world; Reset restores forward."
    - "Legs are not welded into rigid polygons; Water Wheel motor-off is not copied."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/theo_jansen.rs
      provides: "Theo Jansen BuiltScene with CreateLeg soft distance set and live motor reverse"
      exports: ["build"]
    - path: crates/liquidfun-wasm/src/scene.rs
      provides: "SceneId::TheoJansen allowlist + build_scene arm"
  key_links:
    - from: crates/liquidfun-wasm/src/scene/theo_jansen.rs
      to: DistanceJointDef::with_frequency / with_damping_ratio
      via: "CreateLeg soft suspension freq 10 damp 0.5"
      pattern: "with_frequency\\(10|with_damping_ratio\\(0\\.5"
    - from: crates/liquidfun-wasm/src/scene/theo_jansen.rs
      to: set_revolute_motor_speed
      via: "motor-direction live preset"
      pattern: "motor-direction|set_revolute_motor_speed"
---

<objective>
Port Theo Jansen walker with soft distance legs, self-collision filtering, particle load, and live motor direction reverse.

Purpose: Deliver ACT-05 walking under load with reverse control (D-07, D-13, D-14, D-17, D-20). Discretion lock: direction-only (no speed magnitude). No catalog yet.
Output: `theo_jansen.rs`, `SceneId::TheoJansen`, soft-joint + reverse unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/28-interaction-seams/28-CONTEXT.md
@.planning/phases/28-interaction-seams/28-RESEARCH.md
@third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testTheoJansen.js
@crates/liquidfun-wasm/src/scene/water_wheel.rs

<interfaces>
Pinned Theo Jansen essentials (`testTheoJansen.js`):

```text
offset = (0, 8); motorSpeed = 2.0; pivot = (0, 0.8)
Ground edges: (-50,0)–(50,0), (-50,0)–(−50,10), (50,0)–(50,10)
40 dynamic balls r=0.25 along x=-40+2i at y=0.5
Chassis box 2.5×1 and wheel circle r=1.6 at pivot+offset, FilterData groupIndex=-1
Motorized revolute wheel↔chassis at pivot+offset: speed +2.0, maxTorque 400, enableMotor true
CreateLeg×6 (three wheel angles ±120°) using DistanceJointDef frequencyHz=10, dampingRatio=0.5
  — do NOT weld legs into rigid polygons
Particles: radius 0.2, damping 0.2; slab box 7×0.5 at (0,15)
Live control: "motor-direction" values "forward" (default, +2.0) | "reverse" (-2.0) → ControlEffect::Live
apply_pointer: no required gesture (no-op success OK)
```

Do not add limit-toggle or speed-magnitude controls. Do not copy Water Wheel motor-off. Do not raise MAX_ADVANCE_STEPS. Do not edit web/**.
</interfaces>
</context>

<feature>
  <name>Theo Jansen WASM scene</name>
  <files>crates/liquidfun-wasm/src/scene/theo_jansen.rs, crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <behavior>
    - `parse_scene_id("theo-jansen")` → `Ok(SceneId::TheoJansen)`
    - Create Ok; particles &gt; 0; at least one distance joint exists (or soft-joint count &gt; 0)
    - Chassis/wheel fixtures use groupIndex -1
    - apply_control motor-direction reverse then forward → Live; motor speed sign flips via set_revolute_motor_speed
    - Fresh create defaults to forward (+2.0)
    - Unknown control → UnknownControl
    - MAX_ADVANCE_STEPS stays 4
  </behavior>
  <implementation>
    Port CreateLeg distance set closely enough to walk. Prefer file split under `theo_jansen/` if file-length trigger approached (Claude Discretion). Direction-only reverse.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing Theo Jansen allowlist and reverse tests</name>
  <files>crates/liquidfun-wasm/src/session/tests.rs, crates/liquidfun-wasm/src/scene/theo_jansen.rs</files>
  <read_first>
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-07, D-13, D-17
    1. `.planning/phases/28-interaction-seams/28-RESEARCH.md` Pattern 5, Pitfall 6, Code Examples soft distance
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testTheoJansen.js` CreateLeg
    1. `standards/core/testing.md`
  </read_first>
  <behavior>
    1. Test: `parse_scene_id("theo-jansen")` → `Ok(SceneId::TheoJansen)`.
    1. Test: create Ok; particle_count &gt; 0.
    1. Test: scene exposes soft distance joints (count ≥ 1) and motorized revolute.
    1. Test: `motor-direction`/`reverse` → Live and motor speed &lt; 0; `forward` → speed &gt; 0.
    1. Test: unknown control → UnknownControl.
  </behavior>
  <action>
    Per D-07/D-13, write failing tests first. Extend allowlist with `("theo-jansen", SceneId::TheoJansen)`. Add theo_jansen tests for create, soft joints present, and live reverse. Do not edit web/**.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture 2&gt;&amp;1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n '"theo-jansen"' crates/liquidfun-wasm/src/session/tests.rs` matches.
    1. `rg -n 'motor-direction' crates/liquidfun-wasm/src/scene/theo_jansen.rs` matches test references.
    1. `rg -n 'SceneId::TheoJansen' crates/liquidfun-wasm/src/scene/theo_jansen.rs crates/liquidfun-wasm/src/session/tests.rs` matches.
    1. Tests exist before production build is complete (RED/compile-fail until Task 2).
  </acceptance_criteria>
  <done>Failing Theo Jansen allowlist/reverse coverage is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Theo Jansen scene and factory wiring</name>
  <files>crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/scene/theo_jansen.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <read_first>
    1. `crates/liquidfun/src/joint/definition/distance_pulley_mouse.rs` `DistanceJointDef::with_frequency` / `with_damping_ratio`
    1. `crates/liquidfun/src/world/joint/revolute.rs` `set_revolute_motor_speed`
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testTheoJansen.js`
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-07, D-13, D-17, D-20
  </read_first>
  <behavior>
    Same as Task 1; all tests pass.
  </behavior>
  <action>
    Per D-07/D-13/D-14/D-17/D-20: add `mod theo_jansen;` and `SceneId::TheoJansen`. Map `"theo-jansen"`. Port ground/walls, 40 balls, chassis+wheel with `FilterData::new(_, _, -1)`, motorized revolute at speed `+2.0` max torque `400`, six CreateLeg calls with soft `DistanceJointDef` frequency `10` damping ratio `0.5`, particle system radius `0.2` damping `0.2`, slab `7×0.5` at `(0,15)`. `apply_control("motor-direction", "forward"|"reverse")` → `set_revolute_motor_speed` to `±2.0` and `ControlEffect::Live`. No speed-magnitude control. No limit toggle. Do not weld legs. Do not copy Water Wheel motor-off. Do not raise MAX_ADVANCE_STEPS. Do not touch web/**. Split module files if length requires.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm theo_jansen -- --nocapture &amp;&amp; cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture &amp;&amp; rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs &amp;&amp; rg -n 'with_frequency\(10|with_damping_ratio\(0\.5' crates/liquidfun-wasm/src/scene/theo_jansen.rs crates/liquidfun-wasm/src/scene/theo_jansen/</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'SceneId::TheoJansen' crates/liquidfun-wasm/src/scene.rs` matches enum, parse, build.
    1. `rg -n 'motor-direction' crates/liquidfun-wasm/src/scene/theo_jansen.rs crates/liquidfun-wasm/src/scene/theo_jansen/` matches.
    1. `rg -n 'with_frequency\(10|with_damping_ratio\(0\.5' crates/liquidfun-wasm/src/scene/theo_jansen.rs crates/liquidfun-wasm/src/scene/theo_jansen/` matches.
    1. `rg -n 'group_index|-1|FilterData::new' crates/liquidfun-wasm/src/scene/theo_jansen.rs crates/liquidfun-wasm/src/scene/theo_jansen/` matches filter -1.
    1. `rg -n 'set_revolute_motor_speed' crates/liquidfun-wasm/src/scene/theo_jansen.rs crates/liquidfun-wasm/src/scene/theo_jansen/` matches.
    1. `rg -n 'enable_motor:\s*false' crates/liquidfun-wasm/src/scene/theo_jansen.rs crates/liquidfun-wasm/src/scene/theo_jansen/` is empty.
    1. `rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs` matches.
    1. `cargo test -p liquidfun-wasm theo_jansen` exits 0.
    1. `cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens` exits 0 (all five new ids present).
  </acceptance_criteria>
  <done>Theo Jansen constructs with soft legs, walks under particle load, and reverses via live motor direction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Control name/value → motor speed | Untrusted preset flips motor sign. |
| Scene construction → many joints | Complex joint graph must stay typed/safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05-01 | T | motor-direction | mitigate | Allowlist exact `"forward"` / `"reverse"` only. |
| T-28-05-02 | D | Walker instability | mitigate | Soft distance joints + groupIndex -1; fixed motor magnitude ±2.0; existing particle caps. |
| T-28-05-03 | E | No freeglut-only chrome | mitigate | Labeled control only; no limit-toggle key surface. |
| T-28-05-04 | I | Soft-leg honesty | mitigate | Unit tests require DistanceJoint freq/damp; forbid motor-off Water Wheel copy. |
</threat_model>

<verification>
`cargo test -p liquidfun-wasm theo_jansen` and full allowlist parse exit 0; MAX_ADVANCE_STEPS stays 4.
</verification>

<success_criteria>
1. ACT-05 Theo Jansen builds with soft legs and particle load; reverse flips motor sign live.
1. All five Phase 28 scene ids parse through the factory.
</success_criteria>

<output>
After completion, create `.planning/phases/28-interaction-seams/28-05-SUMMARY.md`
</output>
