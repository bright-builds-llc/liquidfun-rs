---
phase: 28-interaction-seams
plan: "03"
type: tdd
wave: 3
depends_on: ["02"]
files_modified:
  - crates/liquidfun-wasm/src/scene.rs
  - crates/liquidfun-wasm/src/scene/impulse.rs
  - crates/liquidfun-wasm/src/session/tests.rs
autonomous: true
requirements: [ACT-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-09-22T12-56-07
generated_at: 2026-09-22T13:15:00Z
must_haves:
  truths:
    - "Visitor can construct Impulse with one particle group in a chain-loop box and shove the whole blob via group force/impulse."
    - "Focused test shows group momentum changes after an inside-box shove; outside-box pointer is a no-op."
    - "Live push-mode force (default) vs impulse does not recreate the world; Reset restores force."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/impulse.rs
      provides: "Impulse BuiltScene with box hit-test and member_ids range shove"
      exports: ["build"]
    - path: crates/liquidfun-wasm/src/scene.rs
      provides: "SceneId::Impulse allowlist + build_scene arm"
  key_links:
    - from: crates/liquidfun-wasm/src/scene/impulse.rs
      to: apply_particle_force_range / apply_particle_linear_impulse_range
      via: "direction * magnitude * member_count on full member_ids"
      pattern: "apply_particle_force_range|apply_particle_linear_impulse_range"
    - from: crates/liquidfun-wasm/src/scene/impulse.rs
      to: ControlEffect::Live
      via: "push-mode force|impulse"
      pattern: "push-mode"
---

<objective>
Port Impulse as a box + single particle group with whole-blob shove using native group force/linear impulse and a live push-mode preset.

Purpose: Deliver ACT-03 click-to-shove without single-particle poke fakes (D-06, D-11, D-14, D-16, D-20). No catalog yet.
Output: `impulse.rs`, `SceneId::Impulse`, momentum and hit-test unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/28-interaction-seams/28-CONTEXT.md
@.planning/phases/28-interaction-seams/28-RESEARCH.md
@crates/liquidfun-wasm/src/scene/jelly_drop.rs
@third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testImpulse.js

<interfaces>
Pinned Impulse layout (`testImpulse.js`):

```text
Box: left=-2, right=2, bottom=0, top=4 as ChainShape loop on static ground
ParticleSystemDef.radius = 0.025, damping = 0.2
Group: box half-extents 0.8×1.0 centered (0, 1.01)
Default push mode: force
On Pointer Up inside box:
  boxCenter = (0, 2)  // 0.5*(left+right), 0.5*(bottom+top)
  direction = normalize(pointer - boxCenter)
  members = particle_group_view(group).member_ids().to_vec()
  n = members.len() as f32
  if force:   apply_particle_force_range(system, &members, direction * 1.0 * n)
  if impulse: apply_particle_linear_impulse_range(system, &members, direction * 0.005 * n)
Outside box: no-op success
Control: name "push-mode", values "force" | "impulse" → ControlEffect::Live (recreates false)
```

Do not add particle-type presets (PRESET-01 deferred). Do not shove one particle. Do not raise MAX_ADVANCE_STEPS. Do not silently change radius from 0.025. Do not edit web/**.
</interfaces>
</context>

<feature>
  <name>Impulse WASM scene</name>
  <files>crates/liquidfun-wasm/src/scene/impulse.rs, crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <behavior>
    - `parse_scene_id("impulse")` → `Ok(SceneId::Impulse)`
    - Create Ok; particles &gt; 0
    - Inside-box pointer up changes group linear velocity / momentum vs pre-shove baseline
    - Outside-box pointer up leaves momentum unchanged
    - `push-mode=impulse` Live then shove uses impulse path; default/reset path is force
    - Unknown control → UnknownControl
    - MAX_ADVANCE_STEPS stays 4
  </behavior>
  <implementation>
    Copy full `member_ids` and scale by `n` like testImpulse.js / RESEARCH Pattern 4. Do not copy Jelly Drop nearby-only poke.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing Impulse allowlist and shove tests</name>
  <files>crates/liquidfun-wasm/src/session/tests.rs, crates/liquidfun-wasm/src/scene/impulse.rs</files>
  <read_first>
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-06, D-11, D-16
    1. `.planning/phases/28-interaction-seams/28-RESEARCH.md` Pattern 4, Pitfall 2, Pitfall 3
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testImpulse.js`
    1. `crates/liquidfun-wasm/src/scene/jelly_drop.rs` member_ids range usage (contrast: full group vs poke subset)
    1. `standards/core/testing.md`
  </read_first>
  <behavior>
    1. Test: `parse_scene_id("impulse")` → `Ok(SceneId::Impulse)`.
    1. Test: create Ok; particle_count &gt; 0.
    1. Test: after create, sample group linear velocity; pointer up at (1,2) changes magnitude; pointer up at (10,10) does not.
    1. Test: apply_control `push-mode`/`impulse` → Live; `push-mode`/`force` → Live.
    1. Test: unknown control → UnknownControl.
  </behavior>
  <action>
    Per D-16, write failing tests first. Extend allowlist with `("impulse", SceneId::Impulse)`. Add impulse module tests for create, inside vs outside shove momentum, and live push-mode. Do not edit web/**. Do not add wave-machine/theo-jansen ids yet.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture 2&gt;&amp;1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n '"impulse"' crates/liquidfun-wasm/src/session/tests.rs` matches.
    1. `rg -n 'push-mode' crates/liquidfun-wasm/src/scene/impulse.rs` matches test references.
    1. `rg -n 'SceneId::Impulse' crates/liquidfun-wasm/src/scene/impulse.rs crates/liquidfun-wasm/src/session/tests.rs` matches.
    1. Tests exist before production build is complete (RED/compile-fail until Task 2).
  </acceptance_criteria>
  <done>Failing Impulse allowlist/shove coverage is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Impulse scene and factory wiring</name>
  <files>crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/scene/impulse.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <read_first>
    1. `crates/liquidfun-wasm/src/scene/jelly_drop.rs` apply_pointer / member_ids
    1. `crates/liquidfun/src/world/particle_object/system.rs` `apply_particle_force_range` / `apply_particle_linear_impulse_range`
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testImpulse.js`
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-06, D-16, D-20
  </read_first>
  <behavior>
    Same as Task 1; all tests pass.
  </behavior>
  <action>
    Per D-06/D-11/D-14/D-16/D-20: add `mod impulse;` and `SceneId::Impulse`. Map `"impulse"`. Build chain-loop box `[-2,2]×[0,4]`, particle system radius `0.025` damping `0.2`, group box `0.8×1.0` at `(0,1.01)`. Default `push_mode = Force`. On Pointer Up inside box, normalize direction from box center `(0,2)` toward pointer, copy full `member_ids`, apply `direction * 1.0 * n` via `apply_particle_force_range` or `direction * 0.005 * n` via `apply_particle_linear_impulse_range`. Outside box: no-op. `apply_control("push-mode", "force"|"impulse")` → `ControlEffect::Live`. Reject unknown controls. Do not use singular `apply_particle_linear_impulse` for the shove. Do not change radius. Do not raise MAX_ADVANCE_STEPS. Do not touch web/**.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm impulse -- --nocapture &amp;&amp; cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture &amp;&amp; rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs &amp;&amp; rg -n '0\.025' crates/liquidfun-wasm/src/scene/impulse.rs</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'SceneId::Impulse' crates/liquidfun-wasm/src/scene.rs` matches enum, parse, build.
    1. `rg -n 'apply_particle_force_range|apply_particle_linear_impulse_range' crates/liquidfun-wasm/src/scene/impulse.rs` matches production shove.
    1. `rg -n 'member_ids' crates/liquidfun-wasm/src/scene/impulse.rs` matches.
    1. `rg -n 'push-mode' crates/liquidfun-wasm/src/scene/impulse.rs` matches.
    1. `rg -n '0\.025' crates/liquidfun-wasm/src/scene/impulse.rs` matches radius.
    1. `rg -n '0\.005' crates/liquidfun-wasm/src/scene/impulse.rs` matches impulse magnitude constant.
    1. `rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs` matches.
    1. `cargo test -p liquidfun-wasm impulse` exits 0.
  </acceptance_criteria>
  <done>Impulse constructs and shoves the full particle group via native range force/impulse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer coords → particle forces | Untrusted pointer can inject large forces if unbounded. |
| Control name/value → live preset | Untrusted strings select shove mode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | T | push-mode control | mitigate | Allowlist exact `"force"` / `"impulse"` only; else UnknownControl. |
| T-28-03-02 | D | Force amplification | mitigate | Magnitudes fixed (1.0 / 0.005 × n); box hit-test required; existing particle caps. |
| T-28-03-03 | T | Outside-box clicks | mitigate | Explicit no-op outside `[-2,2]×[0,4]`. |
| T-28-03-04 | I | Group shove honesty | mitigate | Unit test asserts momentum change uses full member_ids range APIs. |
</threat_model>

<verification>
`cargo test -p liquidfun-wasm impulse` exits 0; radius stays 0.025; MAX_ADVANCE_STEPS stays 4.
</verification>

<success_criteria>
1. ACT-03 Impulse shoves the whole blob with force default and live impulse mode.
1. Focused momentum test proves native group shove; outside clicks do nothing.
</success_criteria>

<output>
After completion, create `.planning/phases/28-interaction-seams/28-03-SUMMARY.md`
</output>
