---
phase: 28-interaction-seams
plan: "06"
type: execute
wave: 6
depends_on: ["01", "02", "03", "04", "05"]
files_modified:
  - web/src/catalog/scenes.ts
  - web/src/catalog/previews.tsx
  - web/src/player/runtime.ts
  - web/src/components/PlaygroundShell.tsx
  - THIRD_PARTY_NOTICES.md
autonomous: true
requirements: [ACT-01, ACT-02, ACT-03, ACT-04, ACT-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-09-22T12-56-07
generated_at: 2026-09-22T13:15:00Z
must_haves:
  truths:
    - "SCENE_IDS append soup, soup-stirrer, impulse, wave-machine, theo-jansen after the eleven existing ids; all five ready:true."
    - "Controls/hints match UI-SPEC: Soup/Wave watch-first empty; Stirrer Toggle paddle rail; Impulse push-mode; Theo motor-direction."
    - "PAGE_SUMMARY says sixteen demos; Static preview SVGs exist; pinned inspiration at 7f204021…; no .catalog-card; no sealed parity claims."
  artifacts:
    - path: web/src/catalog/scenes.ts
      provides: "Sixteen-scene catalog records, controls, credits"
    - path: web/src/catalog/previews.tsx
      provides: "Five new Static preview SVG cases"
    - path: web/src/player/runtime.ts
      provides: "PAGE_SUMMARY sixteen-demo copy"
  key_links:
    - from: web/src/catalog/scenes.ts
      to: liquidfun-wasm SceneId tokens
      via: "id tokens match parse_scene_id"
      pattern: "\"soup\"|\"soup-stirrer\"|\"impulse\"|\"wave-machine\"|\"theo-jansen\""
    - from: web/src/catalog/previews.tsx
      to: DemoNavigation
      via: "ScenePreview switch exhaustiveness"
      pattern: "case \"soup\"|case \"soup-stirrer\"|case \"impulse\"|case \"wave-machine\"|case \"theo-jansen\""
---

<objective>
Append the five interaction scenes to the shared catalog/player chrome with exact UI-SPEC copy, controls, static previews, and pinned credits.

Purpose: Advertise ready WASM scenes (D-01–D-08, D-21–D-24) only after Plans 01–05 can build them; keep PlaygroundShell; ban `.catalog-card`.
Output: Updated `scenes.ts`, `previews.tsx`, `PAGE_SUMMARY`, shell sixteen-demo focus wrap if needed, THIRD_PARTY_NOTICES when adapted.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@standards/core/frontend-ui.md
@standards/languages/typescript-javascript.md
@standards-overrides.md
@.planning/phases/28-interaction-seams/28-CONTEXT.md
@.planning/phases/28-interaction-seams/28-UI-SPEC.md
@.planning/phases/28-interaction-seams/28-01-SUMMARY.md
@.planning/phases/28-interaction-seams/28-02-SUMMARY.md
@.planning/phases/28-interaction-seams/28-03-SUMMARY.md
@.planning/phases/28-interaction-seams/28-04-SUMMARY.md
@.planning/phases/28-interaction-seams/28-05-SUMMARY.md
@web/src/catalog/scenes.ts
@web/src/catalog/previews.tsx
@web/src/player/runtime.ts
@web/src/components/scene-credits.ts

<interfaces>
Exact SCENE_IDS append order (D-01):

```ts
// …existing eleven…
"soup",
"soup-stirrer",
"impulse",
"wave-machine",
"theo-jansen",
```

| Scene | title | description | controls | hint |
|-------|-------|-------------|----------|------|
| soup | Soup | Watch a basin of liquid hold floating solid bits. | `[]` | WATCH_FIRST_HINT |
| soup-stirrer | Soup Stirrer | Watch a paddle stir soup, and free or restore its rail. | `action("toggle-paddle-rail", "Toggle paddle rail")` | Click or tap the canvas, or use Toggle paddle rail, to free the paddle from its rail or put it back. Labeled controls also work from the keyboard. |
| impulse | Impulse | Click or tap inside the box to shove the whole particle blob. | `runtimePreset("push-mode", "Push", [Force, Impulse])` ids force (default) / impulse; recreates:false | Click or tap inside the box to shove the particle blob. Use Push to choose force or impulse. Clicks outside the box do nothing. Labeled controls also work from the keyboard. |
| wave-machine | Wave Machine | Watch a motorized tank rock and slosh the water inside. | `[]` | WATCH_FIRST_HINT |
| theo-jansen | Theo Jansen | Watch a walker move under a particle load and reverse its motor. | `runtimePreset("motor-direction", "Motor direction", [Forward, Reverse])` ids forward (default) / reverse; recreates:false | Use Motor direction to walk forward or reverse under the particle load. Labeled controls also work from the keyboard. |

Inspiration labels + paths at pin `7f20402173fd143a3988c921bc384459c6a858f2`:
- Soup: `Pinned Soup.js` → `testSoup.js`; `Pinned Soup.h` → `Soup.h`
- Soup Stirrer: `Pinned SoupStirrer.js` → `testSoupStirrer.js`; `Pinned SoupStirrer.h` → `SoupStirrer.h`
- Impulse: `Pinned Impulse.js` → `testImpulse.js`; `Pinned Impulse.h` → `Impulse.h`
- Wave Machine: `Pinned WaveMachine.js` → `testWaveMachine.js`; `Pinned WaveMachine.h` → `WaveMachine.h`
- Theo Jansen: `Pinned TheoJansen.js` → `testTheoJansen.js`; `Pinned TheoJansen.h` → `TheoJansen.h`

Implementation paths: `soup.rs`, `soup_stirrer.rs`, `impulse.rs`, `wave_machine.rs`, `theo_jansen.rs` via host-locked `sceneSource(...)`.

PAGE_SUMMARY exact:
`Play experimental Rust physics scenes in the browser. All sixteen demos run this repository's engine through WebAssembly.`

Preview art (28-UI-SPEC): Soup basin+solids+noodles; Stirrer + paddle/rail; Impulse box+blob; Wave tilted tank+water; Theo ground/legs+slab. Tokens `#4DA3FF` `#39D3C7` `#F87171` `#3DDC97` `#CBD5E1` `#F4F7FA` `#071018`. Caption stays `Static preview`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append scene records, controls, credits, PAGE_SUMMARY</name>
  <files>web/src/catalog/scenes.ts, web/src/player/runtime.ts, web/src/components/PlaygroundShell.tsx, THIRD_PARTY_NOTICES.md</files>
  <read_first>
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-01, D-03, D-04, D-05, D-06, D-07, D-08, D-21, D-22, D-23
    1. `.planning/phases/28-interaction-seams/28-UI-SPEC.md` Copywriting Contract + Scene controls + Credits copy
    1. `web/src/catalog/scenes.ts` full file (eleven-scene baseline + action/runtimePreset helpers)
    1. `web/src/player/runtime.ts` `PAGE_SUMMARY`
    1. `web/src/components/scene-credits.ts` host-lock helpers
    1. `web/src/components/PlaygroundShell.tsx` any eleven/focus-wrap hardcoding
    1. `THIRD_PARTY_NOTICES.md`
  </read_first>
  <action>
    Per D-01/D-04–D-08/D-21–D-23: append the five SCENE_IDS after `rigid-particles` (never reorder the first eleven). Append SCENES records with `ready: true`, exact UI-SPEC titles/descriptions/hints/controls from the interfaces table, host-locked implementation paths, and pinned inspiration hrefs including commit `7f20402173fd143a3988c921bc384459c6a858f2` (optional SHOWCASE allowed). Impulse `runtimePreset("push-mode", "Push", …)` with option ids `force`/`impulse` and visible labels `Force`/`Impulse`, `recreates: false`. Theo `runtimePreset("motor-direction", "Motor direction", …)` with `forward`/`reverse`, labels `Forward`/`Reverse`, `recreates: false`. Stirrer `action("toggle-paddle-rail", "Toggle paddle rail")`. Soup and Wave Machine `controls: []` + `WATCH_FIRST_HINT`. Replace PAGE_SUMMARY with the exact sixteen-demo sentence. Update PlaygroundShell only if it hardcodes eleven-demo or Rigid-Particles-last focus wrap — change last demo identity to Theo Jansen / sixteen as needed; do not redesign layout. If upstream geometry/text was adapted into notices, append NOTICE entries; otherwise leave notices unchanged. Never point implementation href at google/liquidfun. No sealed C++ parity / live thumbnail / bit-exact copy. No `.catalog-card`. No PRESET-01 particle-type controls. Do not edit WASM modules.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck 2&gt;&amp;1 | head -80</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n '"soup"|"soup-stirrer"|"impulse"|"wave-machine"|"theo-jansen"' web/src/catalog/scenes.ts` matches SCENE_IDS and records.
    1. `rg -n 'All sixteen demos' web/src/player/runtime.ts` matches.
    1. `rg -n 'All eleven demos' web/src/player/runtime.ts` is empty.
    1. `rg -n 'toggle-paddle-rail|Toggle paddle rail' web/src/catalog/scenes.ts` matches.
    1. `rg -n 'push-mode|"Push"' web/src/catalog/scenes.ts` matches.
    1. `rg -n 'motor-direction|"Motor direction"' web/src/catalog/scenes.ts` matches.
    1. `rg -n '7f20402173fd143a3988c921bc384459c6a858f2' web/src/catalog/scenes.ts` matches ≥10 inspiration hrefs for the five scenes.
    1. `rg -n 'Pinned Soup\.js|Pinned Soup\.h|Pinned SoupStirrer\.js|Pinned SoupStirrer\.h|Pinned Impulse\.js|Pinned Impulse\.h|Pinned WaveMachine\.js|Pinned WaveMachine\.h|Pinned TheoJansen\.js|Pinned TheoJansen\.h' web/src/catalog/scenes.ts` matches all ten labels.
    1. `rg -n 'sealed C\+\+|bit-exact|live thumbnail' web/src/catalog/scenes.ts web/src/player/runtime.ts` is empty.
    1. Typecheck may fail until Task 2 updates previews — finish Task 2 before claiming plan done.
  </acceptance_criteria>
  <done>Catalog metadata and PAGE_SUMMARY advertise sixteen ready scenes with locked controls and pinned credits.</done>
</task>

<task type="auto">
  <name>Task 2: Static SVG previews for five interaction scenes</name>
  <files>web/src/catalog/previews.tsx</files>
  <read_first>
    1. `.planning/phases/28-interaction-seams/28-UI-SPEC.md` Catalog Shell + Preview art direction
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-02, D-03
    1. `web/src/catalog/previews.tsx` existing cases as pattern
    1. `web/src/components/DemoNavigation.tsx` caption `Static preview`
  </read_first>
  <action>
    Per D-02/D-03/UI-SPEC preview art direction: add five still inline SVG switch cases for `"soup"`, `"soup-stirrer"`, `"impulse"`, `"wave-machine"`, `"theo-jansen"` matching the art table (basin/solids, paddle+rail, box+blob, tilted tank, walker+slab). Use only declared tokens. Keep SVGs still (no CSS/SMIL animation). No `&lt;img&gt;`, canvas, or WASM. Confirm `rg 'catalog-card' web/src` stays empty. Leave SceneControls empty-hide gate intact (`controls.length === 0`).
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'case "soup"|case "soup-stirrer"|case "impulse"|case "wave-machine"|case "theo-jansen"' web/src/catalog/previews.tsx` matches all five.
    1. `rg -n '#4DA3FF|#CBD5E1|#071018' web/src/catalog/previews.tsx` matches new preview tokens.
    1. `rg -n 'catalog-card' web/src` is empty.
    1. `rg -n 'controls\.length' web/src/components/SceneControls.tsx web/src/components/PlayerSceneChrome.tsx` still matches a hide gate.
    1. `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Sixteen-scene TypeScript catalog compiles with five new static previews.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog metadata → visitor | Copy and links shape trust about what runs. |
| Inspiration hrefs → external GitHub | Allowed citation; implementation must stay host-locked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06-01 | S | Implementation credit links | mitigate | Keep implementationPath under `crates/liquidfun-wasm/src/scene/{soup,soup_stirrer,impulse,wave_machine,theo_jansen}.rs`; host-lock unchanged. |
| T-28-06-02 | I | Catalog honesty | mitigate | No sealed-parity / live-thumbnail copy; Static preview caption unchanged; ready only after WASM builds. |
| T-28-06-03 | T | Scene id allowlist | mitigate | Only append ids Plans 01–05 already parse. |
| T-28-06-04 | S | `.catalog-card` revival | mitigate | Do not introduce card-grid classes; smoke later asserts count 0. |
</threat_model>

<verification>
`cd web && bun run typecheck` passes; scenes.ts and previews cover sixteen ids with locked controls and credits.
</verification>

<success_criteria>
1. Catalog lists sixteen ready scenes with ACT control contracts and pinned credits.
1. No card grid; PAGE_SUMMARY says sixteen; honest experimental copy only.
</success_criteria>

<output>
After completion, create `.planning/phases/28-interaction-seams/28-06-SUMMARY.md`
</output>
