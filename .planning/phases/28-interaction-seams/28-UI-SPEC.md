---
phase: 28
slug: interaction-seams
status: approved
shadcn_initialized: false
preset: none
created: 2026-09-22
---

# Phase 28 — UI Design Contract

> Visual and interaction contract for appending Soup, Soup Stirrer, Impulse, Wave Machine, and Theo Jansen to the existing SolidJS playground catalog/player. Generated by gsd-ui-researcher for `/gsd-ui-phase`. Approved by gsd-ui-checker on 2026-09-22 (6/6 dimensions).

This phase is an extension of the existing playground, not a new product surface. Reuse the shipped dark playground tokens in live `web/src/app.css` and `web/src/styles/player.css`, the 44px hit targets, `#4DA3FF` accent / focus ring, `PlaygroundShell` (sticky header, desktop sidebar, Corvu/Kobalte drawer), play/pause/reset chrome, credits, and footer. Grow the catalog from eleven to sixteen ready scenes. Do not invent a new visual system, revive `.catalog-card`, restyle `SceneControls`, or copy lfjs freeglut chrome.

Source: `28-CONTEXT.md` D-01–D-26, `28-RESEARCH.md`, `27-UI-SPEC.md` structure, live `web/src/app.css` / `player.css` (accent is `#4DA3FF`, not the outdated Phase 27 teal contract).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none as a design system — existing shadcn-solid shell pieces only (`SidebarProvider` / Corvu drawer via `web/src/components/ui/*`, `@kobalte/core` where already imported) |
| Icon library | none |
| Font | system UI stack: `ui-sans-serif, system-ui, sans-serif` |

Keep semantic HTML plus the existing page-scoped stylesheets (`web/src/app.css`, `web/src/styles/player.css`, `web/src/styles/responsive.css`). `standards-overrides.md` (shadcn-solid / Tailwind CSS v4 / Kobalte / Corvu drawer; `skipLibCheck`) remains in force for shell chrome only.

No `components.json` is present. Confirmed 2026-09-22: no shadcn preset string; do not run `npx shadcn init` this phase. Catalog appends and interaction scenes do not justify a design-system migration or registry install.

**shadcn gate (auto):** skipped ask — SolidJS stack already uses the recorded shadcn-solid shell override without a `components.json` preset; Tool recorded as `none`.

Source: `28-CONTEXT.md` D-03, `28-RESEARCH.md` Standard Stack, `standards-overrides.md`, live `PlaygroundShell.tsx`.

---

## Spacing Scale

Declared values (must be multiples of 4). Reuse the live CSS custom properties in `web/src/app.css` — identical to Phases 26–27:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Demo-nav list and link gaps, status-dot gaps, definition-term spacing, preview-to-caption gap |
| sm | 8px | Demo-nav-link padding block, preview frame radius, toolbar/control-group internal gaps, footer row gaps |
| md | 16px | Sidebar/drawer padding, header padding, default control gaps, narrow-page padding |
| lg | 24px | App-shell / player-panel / footer padding and section gaps |
| xl | 32px | Player-main vertical gap on desktop |
| 2xl | 48px | Unused this phase; keep token for existing page-level history |
| 3xl | 64px | Unused this phase; keep token for existing page-level history |

Exceptions (layout constants, not new spacing-scale tokens):

- Play, Pause, Reset, Retry, Apply setting, scene actions, fallback Open Dam Break, Demos trigger, Close, GitHub source, and every native `<select>` keep a **44px** minimum block size.
- Desktop demo sidebar remains the shipped sidebar column (collapsible offcanvas; do not restore a card grid).
- Compact preview frame is `width: 100%`, `aspect-ratio: 16 / 9`, **`max-block-size: 72px`**.
- Current-demo marker remains the shipped **3px** inset start bar on `.demo-nav-link[aria-current="page"]`.
- One-pixel borders and a two-pixel focus outline are visual strokes, not layout spacing.
- Do not introduce 44px, 72px, or 3px as spacing-scale tokens.
- No new spacing tokens for Phase 28.

Source: `web/src/app.css` `--space-*`, `27-UI-SPEC.md` Spacing Scale, `28-CONTEXT.md` D-03.

---

## Typography

Use exactly four public sizes and two weights — identical to the live player:

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Label / caption / footer / credit / reset-hint / interaction instruction / Static preview | 14px | 600 labels and credit terms; 400 captions, footer body, reset-hint, Static preview, interaction instruction | 1.4 |
| Body / demo-nav title / control value / buttons | 16px | 400 descriptions and body; 600 demo-nav titles, control labels, and buttons | 1.5 |
| Section heading / scene title / drawer title | 20px | 600 | 1.3 |
| Page heading (`h1`) | 28px | 600 | 1.2 |

Do not introduce a display face, web font, all-caps paragraph text, or weights other than 400 and 600.

Pre-existing shell exception, not a new Phase 28 size: `.demo-nav-description` remains **13px / 400 / 1.4** as shipped. Keep that frozen shell rule; do not restyle it to 14px, and do not use 13px on new Phase 28 chrome strings (the `Static preview` caption stays 14px). One-behavior demo descriptions for the five new scenes use the existing `.demo-nav-description` 13px rule.

The site heading is the only `h1` (`liquidfun-rs`). The player, fallback, or drawer title is the only heading at `h2`/Dialog.Title rank on a given view. Demo titles stay styled spans (`.demo-nav-title`), not headings. Sidebar `aria-label` is `Demos`; drawer label is `Demos`.

Source: live `web/src/app.css`, `27-UI-SPEC.md` Typography.

---

## Color

Reuse the shipped dark playground tokens from live CSS (Phase 28 corrects the Phase 27 teal contract to match the player as shipped):

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0B0F14` | Page background and surrounding negative space |
| Secondary (30%) | `#151D28` | Sticky header fill base, sidebar/drawer surfaces, player panel, fallback panel, footer, control fills |
| Accent (10%) | `#4DA3FF` | Playing indicator, current-demo nav border and inset bar, Play and Retry fills, keyboard focus ring, primary water/particle highlights, static preview water fills |
| Destructive | `#F87171` | Failure status and error copy only; never as a chrome button for Reset |
| Primary text | `#F4F7FA` | Headings, status, demo titles, button text on secondary fills |
| Inverse text | `#0B0F14` | Text on accent-filled Play and Retry buttons |
| Muted text | `#A9B4C0` | Summary, captions, Static preview, interaction instructions, provenance labels, paused and loading status, demo descriptions |
| Border | `#2A3441` | Panels, preview frames, canvas boundary, secondary buttons, selects, hover nav border |
| Hover surface | `#1C2734` | Demo-nav hover, secondary button hover, select hover |
| Current-demo fill | `#132844` | Background of the current `.demo-nav-link` only |
| Canvas / preview ground | `#071018` | Viewport clear color and compact preview frame background |
| Rigid geometry | `#CBD5E1` | Basin, walls, solids, walker outlines in live frames and previews |

Accent reserved for:

1. the playing status dot;
1. the current `.demo-nav-link` 1px `#4DA3FF` border plus 3px inset start bar;
1. Play scene and Retry scene button fills and their hover state (`#2F8CFF`);
1. `:focus-visible` outlines on buttons, selects, and in-page links (2px solid `#4DA3FF`, 4px offset);
1. particle / water colors supplied by the Rust frame when the scene uses water identity;
1. static SVG water fills in compact catalog previews (`WATER` / `#4DA3FF`).

Accent is not used for headings, panel borders, body copy, Pause, Reset, Apply setting, scene action buttons, Demos trigger, Close, GitHub source, footer text, credit links, interaction instructions, canvas cursor chrome, or decorative gradients.

Secondary preview token (illustrative SVG only, already in `previews.tsx`): `#39D3C7` (`ACCENT_WATER`) may appear in soup / wave liquid marks when a second water tone improves recognition. It is not a chrome accent and must not replace `#4DA3FF` for Play, focus, or current-nav.

### Control fills

| Control | Background | Border | Text | Hover |
|---------|------------|--------|------|-------|
| Play scene / Retry scene | `#4DA3FF` | `#4DA3FF` | `#0B0F14` | `#2F8CFF` |
| Pause scene / Reset scene / Apply setting / scene actions / fallback Open Dam Break / Demos / Close | `#151D28` | `#2A3441` | `#F4F7FA` | `#1C2734` |
| Native `<select>` | `#151D28` | `#2A3441` | `#F4F7FA` | `#1C2734` |
| Disabled | `#151D28` | `#2A3441` | `#A9B4C0` | none |

Reset is not destructive. It recreates a transient local simulation and must not use red chrome. No gradients, glow effects, or new glass surfaces. The existing header `backdrop-filter: blur(12px)` stays.

### Preview particle color tokens (illustrative SVG only)

| Token | Hex | Usage in Phase 28 previews |
|-------|-----|----------------------------|
| Water / primary mass | `#4DA3FF` | Soup broth, Impulse blob, Wave Machine water, Theo particle load |
| Accent water (optional) | `#39D3C7` | Alternate liquid mark when a second tone helps recognition |
| Mix / solid accent | `#F87171` | Optional floating solid highlight |
| Soft / secondary solid | `#3DDC97` | Optional noodle or paddle cue |
| Rigid / basin stroke | `#CBD5E1` | Walls, box, paddle, walker chassis/legs |
| Highlight fill | `#F4F7FA` | Optional small highlight only |
| Canvas ground | `#071018` | Preview frame background |

Source: live `web/src/app.css`, `web/src/styles/player.css`, `web/src/catalog/previews.tsx`, `standards/core/frontend-ui.md` dark default, `28-CONTEXT.md` D-23.

---

## Copywriting Contract

Product chrome is a sixteen-demo playground inside a sidebar/drawer, not a card gallery. Copy must identify an experimental native Rust port with recognizable behavior. Do not claim sealed C++ parity, bit-exact layout, or that catalog previews are live simulations.

| Element | Copy |
|---------|------|
| Document title (ready scene) | `{Scene name} · liquidfun-rs playground` |
| Document title (empty or unknown) | `liquidfun-rs playground` |
| Page heading | `liquidfun-rs` |
| Page summary | `Play experimental Rust physics scenes in the browser. All sixteen demos run this repository's engine through WebAssembly.` |
| Header source link | `GitHub source` |
| Catalog / drawer label | `Demos` |
| Drawer description | `Choose a LiquidFun simulation.` |
| Drawer close | `Close` |
| Catalog item CTA | none nested — the whole list item is the `#/scene/{id}` hash link |
| Catalog preview caption | `Static preview` |
| Player heading | `{Scene name}` |
| Runtime identity label | `Runtime` |
| Runtime identity value | `Rust engine · WebAssembly` |
| Loading status | `Loading {Scene name}…` |
| Playing status | `Playing` |
| Paused status | `Paused` |
| Failure status | `{Scene name} failed` |
| Canvas fallback text | `Canvas is required to display the {Scene name} scene.` |
| Primary playback CTA | `Play scene` |
| Pause CTA | `Pause scene` |
| Reset CTA | `Reset scene` |
| Retry CTA | `Retry scene` |
| Fallback CTA | `Open Dam Break` |
| Construction apply CTA | `Apply setting` |
| Empty / loading heading | `Loading {Scene name}` |
| Empty / loading body | `Starting the Rust WebAssembly session. The scene appears when the first frame is ready.` |
| Unknown-scene heading | `Scene not found` |
| Unknown-scene body | `This playground link does not match a known scene. Open Dam Break, or choose a demo from the navigation list.` |
| Empty-hash heading (defensive; empty hashes normalize to Dam Break) | `Choose a scene` |
| Empty-hash body (defensive) | `Choose a demo from the navigation list to open it in the shared player. Each scene is a live Rust simulation.` |
| Error state | `{Scene name} could not start or continue. Use Retry scene to recreate it, or Reset scene to return to the documented initial state.` |
| Construction reset hint | `Changing this setting recreates the scene from its documented initial state.` |
| Credits heading | `Scene source` |
| Implementation label | `Implementation` |
| Implementation link | `View scene source` |
| Inspiration label | `Inspiration` |
| Notices label | `Notices` |
| Notices link | `Third-party notices` |
| Footer source | `View source on GitHub` |
| Footer license | `Free and open source` |
| Footer maintainer | `By Peter Ryszkiewicz` |
| Provenance labels | `Version`, `Commit`, `Build` |
| Missing provenance | `Unavailable` |
| Destructive confirmation | none — Reset, Retry, Apply setting, scene actions, and live presets recreate or mutate only the current local session and do not remove user data |

Phase-28 primary catalog action is activating a demo hash link. Phase-28 primary player actions:

- Soup / Wave Machine: `Play scene` (watch-first).
- Soup Stirrer: `Toggle paddle rail` (non-recreating action; canvas click/tap sends the same command).
- Impulse: canvas shove inside the box; `Push` live preset (`Force` default / `Impulse`).
- Theo Jansen: `Motor direction` live preset (`Forward` default / `Reverse`).

Update `PAGE_SUMMARY` in `web/src/player/runtime.ts` from “eleven” to “sixteen” so header/footer copy matches the catalog count after all five new scenes ship `ready: true`. Update `web/e2e/shell.spec.ts` strings that still assert “All eleven demos”.

### Catalog demo descriptions

One sentence each. Keep the eleven existing Phase 18/26/27 strings exactly. Add the five new descriptions:

| Scene | Description |
|-------|-------------|
| Dam Break | `Release a block of water into a basin and drop one obstacle.` |
| Fountain | `Aim a bounded stream into a bowl until particle count plateaus.` |
| Float or Sink | `Drop cork, wood, or stone into a pool and watch native body response.` |
| Color Mixer | `Stir two colored groups and watch contact-driven particle-color mixing.` |
| Jelly Drop | `Drop an elastic particle shape onto obstacles, then poke it.` |
| Water Wheel | `Vary a jet that turns a pinned paddle wheel through native coupling.` |
| Particles | `Watch water fall in an open basin while a ball drops into it.` |
| Liquid Timer | `Watch tensile, viscous liquid drain through shelves into bottom columns.` |
| Surface Tension | `Watch three colored tensile groups bead and bleed color when a ball hits them.` |
| Elastic Particles | `Watch three soft particle clumps deform when a ball falls on them.` |
| Rigid Particles | `Watch three colored rigid clumps stay solid when a ball hits them.` |
| Soup | `Watch a basin of liquid hold floating solid bits.` |
| Soup Stirrer | `Watch a paddle stir soup, and free or restore its rail.` |
| Impulse | `Click or tap inside the box to shove the whole particle blob.` |
| Wave Machine | `Watch a motorized tank rock and slosh the water inside.` |
| Theo Jansen | `Watch a walker move under a particle load and reverse its motor.` |

### Interaction hints (figcaption)

| Scene | Interaction hint |
|-------|------------------|
| Dam Break … Water Wheel | Unchanged from Phase 19/20 shipped strings in `scenes.ts` |
| Particles / Liquid Timer / Surface Tension / Elastic Particles / Rigid Particles | `This scene is watch-first. Use Play scene, Pause scene, and Reset scene.` |
| Soup | `This scene is watch-first. Use Play scene, Pause scene, and Reset scene.` |
| Wave Machine | `This scene is watch-first. Use Play scene, Pause scene, and Reset scene.` |
| Soup Stirrer | `Click or tap the canvas, or use Toggle paddle rail, to free the paddle from its rail or put it back. Labeled controls also work from the keyboard.` |
| Impulse | `Click or tap inside the box to shove the particle blob. Use Push to choose force or impulse. Clicks outside the box do nothing. Labeled controls also work from the keyboard.` |
| Theo Jansen | `Use Motor direction to walk forward or reverse under the particle load. Labeled controls also work from the keyboard.` |

Reuse `WATCH_FIRST_HINT` for Soup and Wave Machine. Do not mention freeglut key legends as primary UI, particle-type presets, material pickers, pointer drag, or Theo limit-toggle chrome.

### Scene controls

| Scene | Controls |
|-------|----------|
| Dam Break … Water Wheel | Unchanged from Phase 20 control table |
| Particles / Liquid Timer / Surface Tension / Elastic Particles / Rigid Particles | `controls: []` — unchanged |
| Soup | `controls: []` — play/pause/reset only |
| Wave Machine | `controls: []` — play/pause/reset only |
| Soup Stirrer | one non-recreating action: `action("toggle-paddle-rail", "Toggle paddle rail")` |
| Impulse | one non-recreating runtime preset: `runtimePreset("push-mode", "Push", [Force, Impulse])` with ids `force` (default) and `impulse`; visible option labels `Force` and `Impulse` |
| Theo Jansen | one non-recreating runtime preset: `runtimePreset("motor-direction", "Motor direction", [Forward, Reverse])` with ids `forward` (default) and `reverse`; visible option labels `Forward` and `Reverse` |

Hide the empty `SceneControls` region when `controls.length === 0`. Construction reset hint does not appear for watch-first scenes or for these live (non-recreating) presets/actions. Do not add a speed-magnitude control beside Theo direction this phase (discretion locked to direction-only).

Reset restores:

- Soup Stirrer: paddle on the rail (session dispose + rebuild);
- Impulse: `push-mode` → `force`;
- Theo Jansen: `motor-direction` → `forward`.

### Credits copy (PLAY-03 / D-21–D-22)

Keep Phase 18 credit chrome. Implementation links stay host-locked to this repository. Pin commit `7f20402173fd143a3988c921bc384459c6a858f2`.

| Scene | Implementation path | Inspiration entries (exact labels) |
|-------|---------------------|------------------------------------|
| Soup | `crates/liquidfun-wasm/src/scene/soup.rs` | `Pinned Soup.js` → blob URL for `testSoup.js` at the pin; `Pinned Soup.h` → blob URL for `Soup.h` at the same commit; optional `LiquidFun showcase` |
| Soup Stirrer | `crates/liquidfun-wasm/src/scene/soup_stirrer.rs` | `Pinned SoupStirrer.js` → blob URL for `testSoupStirrer.js`; `Pinned SoupStirrer.h` → blob URL for `SoupStirrer.h`; optional `LiquidFun showcase` |
| Impulse | `crates/liquidfun-wasm/src/scene/impulse.rs` | `Pinned Impulse.js` → blob URL for `testImpulse.js`; `Pinned Impulse.h` → blob URL for `Impulse.h`; optional `LiquidFun showcase` |
| Wave Machine | `crates/liquidfun-wasm/src/scene/wave_machine.rs` | `Pinned WaveMachine.js` → blob URL for `testWaveMachine.js`; `Pinned WaveMachine.h` → blob URL for `WaveMachine.h`; optional `LiquidFun showcase` |
| Theo Jansen | `crates/liquidfun-wasm/src/scene/theo_jansen.rs` | `Pinned TheoJansen.js` → blob URL for `testTheoJansen.js`; `Pinned TheoJansen.h` → blob URL for `TheoJansen.h`; optional `LiquidFun showcase` |

Implementation link visible text remains `View scene source`. Never point implementation href at `google/liquidfun`. Preserve notices via `THIRD_PARTY_NOTICES.md` when upstream material is adapted; Notices link remains `Third-party notices`.

### Honesty lines that must not appear

Do not use: `Ready`, `Not ready yet`, `live thumbnail`, `Open` as a nested catalog button, `complete LiquidFun parity`, `sealed C++ parity`, `bit-exact`, `Dispose session`, `demo card`, `catalog card`, freeglut-only key legends as primary controls, particle-type picker chrome, material pickers, or any claim that a catalog preview is a running simulation.

Use fixed application strings through text nodes. Do not insert raw exception text with `innerHTML`. Instruction copy and SVG markup are authored in TSX, never `innerHTML`.

Source: `28-CONTEXT.md` D-04–D-08 / D-21–D-23, ACT-01–ACT-05 recognition copy, `27-UI-SPEC.md` Copywriting.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no `components.json`; confirmed 2026-09-22; shell already uses local shadcn-solid port files without registry install this phase |
| third-party registries | none | not applicable — no registry input permitted by the phase contract; vetting gate not run because no third-party block was declared |

Do not add registry blocks, new UI packages, or `npx shadcn add` calls for Phase 28 scene work. Existing `@kobalte/core` / Corvu drawer usage stays as already shipped shell infrastructure.

---

## Page Structure and Layout

Use the shipped `PlaygroundShell`. Do not restore CatalogNav, a separate catalog page, or a three-column card grid. Layout contract matches Phases 26–27; only catalog length and last-link identity change.

Primary focal point on a ready scene is the live canvas viewport. Chrome (header, demo list, playback, scene controls) is secondary. When the drawer or sidebar is the active surface, catalog-list focus stays supporting.

Semantic order on every view:

1. Sticky `SiteHeader` with `h1#site-title`, page summary, `GitHub source`, and mobile `Demos` trigger.
1. `.app-body` with:
   - desktop `<aside class="demo-sidebar">` (or Sidebar equivalent with that class) containing `DemoNavigation` (`aria-label="Demos"`);
   - `<main class="playground-main">` containing the shared player or fallback only.
1. `SiteFooter` with source, FOSS, maintainer, and provenance chrome.
1. On small viewports, the sidebar becomes the Corvu/Kobalte drawer named `Demos` via the existing `SidebarProvider` / `SidebarTrigger` path.

Desktop contract:

- Sticky header; sticky/offcanvas sidebar with internal scroll.
- Main column is `minmax(0, 1fr)`.
- Catalog scroll happens in the sidebar, never by making the canvas a touch trap.

Mobile contract:

- One-column body. `Demos` trigger visible, 44px min block size (`SidebarTrigger` / `.mobile-demos-trigger`).
- Drawer overlay and focus trap / Escape / overlay / selection close remain as shipped.
- Compact previews appear in the drawer list, not a second layout.
- Drawer focus wrap: Dismiss + **16** scene links. Reverse-tab last link is **Theo Jansen** (was Rigid Particles at eleven scenes).

Do not add a marketing hero, search, filters, scene editor, live WASM thumbnails, or a second page heading.

Source: `28-CONTEXT.md` D-01 / D-03, live `PlaygroundShell.tsx`, `27-UI-SPEC.md` Page Structure.

---

## Catalog Shell (ACT catalog chrome + D-01–D-03)

Catalog order is fixed. Append only; never reorder or replace the first eleven:

1. Dam Break (`dam-break`)
1. Fountain (`fountain`)
1. Float or Sink (`float-or-sink`)
1. Color Mixer (`color-mixer`)
1. Jelly Drop (`jelly-drop`)
1. Water Wheel (`water-wheel`)
1. Particles (`particles`)
1. Liquid Timer (`liquid-timer`)
1. Surface Tension (`surface-tension`)
1. Elastic Particles (`elastic-particles`)
1. Rigid Particles (`rigid-particles`)
1. Soup (`soup`)
1. Soup Stirrer (`soup-stirrer`)
1. Impulse (`impulse`)
1. Wave Machine (`wave-machine`)
1. Theo Jansen (`theo-jansen`)

Each new entry ships `ready: true` only when its native WASM scene runs. Hash routes stay `#/scene/{id}`.

Each `.demo-nav-link` is one semantic `<a href="#/scene/{id}">` containing, in order:

1. compact preview frame (`.demo-nav-preview`) with inline SVG `aria-hidden="true"`;
1. visible caption text `Static preview` (`.demo-nav-preview-caption`);
1. scene title (`.demo-nav-title`);
1. one-sentence description (`.demo-nav-description`).

| Part | Contract |
|------|----------|
| Preview frame | `display: block; width: 100%; max-block-size: 72px; aspect-ratio: 16 / 9; background: #071018; border: 1px solid #2A3441; border-radius: 8px; overflow: hidden` |
| SVG | Inline, `width/height: 100%`, still (no CSS animation, no SMIL). Token fills/strokes only: `#4DA3FF`, `#39D3C7`, `#F87171`, `#3DDC97`, `#CBD5E1`, `#F4F7FA`, `#071018`. Not an `<img>`, not a `<canvas>`, not a WebP. |
| Caption | Exact text `Static preview`. 14px / 400 / 1.4 / `#A9B4C0`. |
| Title | Approved name, 16px / 600 / `#F4F7FA` |
| Description | Exact one-behavior sentence, existing 13px / 400 / `#A9B4C0` |
| Current scene | `aria-current="page"` on that link; shipped `#132844` fill, `#4DA3FF` border, 3px inset start bar |
| Hit target | Entire link remains keyboard-activable; min block size stays ≥44px (taller with preview is fine) |
| Dual mount | Sidebar and drawer both render `DemoNavigation`. Playwright must scope desktop asserts to `.demo-sidebar` and mobile to the Demos dialog. Global `.catalog-card` count stays **0**. |

### Preview art direction (new scenes)

| Scene | Static illustration |
|-------|---------------------|
| Soup | Basin outline (`#CBD5E1`); blue water mass (`#4DA3FF`); one circle and two small squares as floating solids; one or two short edge-noodle strokes |
| Soup Stirrer | Same soup basin + water; add a larger circle paddle (`#CBD5E1` stroke) and a short horizontal rail cue |
| Impulse | Closed box outline (`#CBD5E1`); one centered particle blob (`#4DA3FF`); optional small directional cue toward a corner (still SVG, no animation) |
| Wave Machine | Hollow four-wall tank outline tilted slightly; water fill inside (`#4DA3FF` or `#39D3C7`) |
| Theo Jansen | Ground line + end walls; simple chassis + wheel + two or three leg strokes (`#CBD5E1`); particle slab mark on top (`#4DA3FF`) |

Keep the eleven existing preview illustrations from Phases 20/26/27 unchanged.

Forbidden class names in `web/src`: `.catalog-card`, `.catalog-grid`, `.catalog-heading` as a card-gallery heading. `web/e2e/shell.spec.ts` keeps `toHaveCount(0)` for `.catalog-card`.

Do not start a WASM world per row, capture live Canvas thumbnails, animate fake physics, or use README gallery WebPs.

Source: `28-CONTEXT.md` D-01 / D-02 / D-03 / D-09–D-13, Claude’s Discretion for SVG artwork.

---

## Player Interaction Contract (ACT-01–ACT-05)

### Watch-first: Soup and Wave Machine

| Concern | Contract |
|---------|----------|
| Playback | Same `Play scene` / `Pause scene` / `Reset scene` / `Retry scene` labels and enablement rules as Phase 20/26/27 |
| Reset | Dispose session and recreate documented initial layout; no confirmation dialog; no red chrome |
| Scene controls | None; hide empty control region |
| Pointer | No scene mutation. `apply_pointer` is a no-op. Canvas may keep `cursor: crosshair` as shipped shell behavior |
| Interaction figcaption | Watch-first copy |
| Wave Machine motion | Tank rocks from simulated time in session `on_advance` (`0.05 * cos(t) * π`); pause freezes `t`; reset returns `t` to zero. No pointer control |
| Catch-up | `MAX_ADVANCE_STEPS` stays 4 |

### Interactive: Soup Stirrer

| Concern | Contract |
|---------|----------|
| Labeled action | `Toggle paddle rail` — non-recreating; frees the paddle from its rail or puts it back |
| Pointer | Click/tap on the canvas sends the **same** toggle command as the labeled action (shared CSS-bound unproject path) |
| Visual expectation | Paddle stirs while rail attached; freeing the rail lets the paddle leave; restoring constrains it again |
| Reset | Session dispose; paddle starts on the rail |
| Smoke | One rail toggle via action or canvas |

### Interactive: Impulse

| Concern | Contract |
|---------|----------|
| Live preset | `Push` — values `Force` (default id `force`) and `Impulse` (id `impulse`); `recreates: false` |
| Pointer | Click/tap **inside** the box shoves the whole particle group from its center toward the pointer using the selected mode. Click/tap **outside** the box does nothing |
| Visual expectation | Blob moves as one unit; no single-particle poke, no camera shake |
| Reset | Restores `force` |
| Smoke | One click inside the box |

### Interactive: Theo Jansen

| Concern | Contract |
|---------|----------|
| Live preset | `Motor direction` — values `Forward` (default id `forward`) and `Reverse` (id `reverse`); `recreates: false` |
| Pointer | No required pointer gesture; direction change is the labeled control |
| Visual expectation | Walker moves under particle load; reverse flips walk direction via live revolute motor speed sign |
| Reset | Restores `forward` |
| Smoke | One direction change |
| Discretion lock | No speed-magnitude control this phase |

### Shared camera and budget

Shared fixed `WORLD_BOUNDS` `(-6,-1)–(6,8)`; no per-scene camera this phase unless an existing scene already opts out (do not invent new cameras for these five). Do not raise `MAX_ADVANCE_STEPS` or cut particle counts for smoothness. Do not approximate carving, group shove, or motor speed in SolidJS.

Existing eleven scenes retain their Phase 19/20/26/27 interaction hints, controls, and pointer maps unchanged.

Source: `28-CONTEXT.md` D-04–D-20 / D-24–D-25, `28-RESEARCH.md` Patterns 4–7.

---

## Navigation and URL Chrome

| Route | View |
|-------|------|
| `#/scene/dam-break` … `#/scene/rigid-particles` | Unchanged eleven ready scenes |
| `#/scene/soup` | Shared player for Soup |
| `#/scene/soup-stirrer` | Shared player for Soup Stirrer |
| `#/scene/impulse` | Shared player for Impulse |
| `#/scene/wave-machine` | Shared player for Wave Machine |
| `#/scene/theo-jansen` | Shared player for Theo Jansen |
| empty hash, `#/`, `#/scene`, `#/scene/` | History-replace to `#/scene/dam-break` (existing shell) |
| unknown identifier | Unknown-scene fallback inside the player section |

Canonical identifiers are lowercase hyphenated tokens. Do not coerce case. Reloading a valid scene URL restores that scene. Fallback views keep demo navigation visible and provide `Open Dam Break`.

Production Vite `base` is `/liquidfun-rs/`. Expected hosted origin is `https://bright-builds-llc.github.io/liquidfun-rs/`.

Source: `28-CONTEXT.md` D-01, live shell routing.

---

## State and Interaction Contract

| State | Visible treatment | Behavior |
|-------|-------------------|----------|
| Loading | `Loading {Scene name}…`; muted status; identity visible; loading overlay; Play/Pause/Reset, scene controls, and canvas gestures disabled; Retry hidden | Await generated initializer, construct the selected scene, validate a frame, and draw it before leaving this state |
| Playing | `Playing`; accent status dot; live viewport; Pause and Reset enabled; Play disabled; Retry hidden; scene controls enabled only when present | Advance the current session with Phase 17 ownership caps |
| Paused | `Paused`; muted status; last frame retained; Play and Reset enabled; Pause disabled; Retry hidden | Freeze without disposing; Wave Machine sim-time `t` stays frozen |
| Failure | `{Scene name} failed`; destructive status and error copy; Retry and Reset enabled; Play/Pause, scene controls, and canvas gestures disabled | Retry/Reset recreate from documented initials |
| Fallback | Fallback heading and body; no live session; `Open Dam Break` enabled; Play/Pause/Reset/Retry, scene controls, and canvas hidden | Do not construct a WASM world |

Focus: 2px solid `#4DA3FF` `:focus-visible` with 4px offset on **buttons, selects, and in-page links**, including demo-nav links. Do not add a second focus system.

Catalog interaction: each shipped demo link is keyboard-operable. Visiting a shipped scene hash starts that scene. Interactive scenes mark the canvas interactive (`.canvas-interactive` / crosshair) when pointer gestures are live; watch-first scenes must not mutate on pointer.

Source: live `player.css` / `app.css`, `28-CONTEXT.md` D-04–D-07 / D-24.

---

## Narrow-Viewport Acceptance

Keep Phase 20/26/27 WEB-07 rules with sixteen-scene counts:

- At `max-width: 768px`, compact previews must be visible inside the `Demos` dialog after opening the trigger. At least one preview SVG and one `Static preview` caption must be visible in that dialog.
- Desktop sidebar must show **sixteen** `Static preview` captions and **sixteen** `svg[aria-hidden="true"]` inside `.demo-sidebar`.
- Preview frames must not cause horizontal page overflow at 375×812.
- 44px minimum hit targets remain on Demos, Close, demo links, playback, scene actions, and selects.
- Catalog/drawer scroll stays outside the canvas; `touch-action: none` stays on canvas only.
- Do not restore a one-column card gallery at 480px.
- Shell reverse-tab last focusable demo link is **Theo Jansen**.

Source: `28-CONTEXT.md` D-01 / D-03 / D-25, `27-UI-SPEC.md` Narrow-Viewport Acceptance.

---

## Motion and Accessibility Minimum

- Add no cosmetic animation, pulsing, shimmer, or transition beyond shipped shell drawer open/close. Catalog SVGs stay still. The physics scene is the only continuous motion.
- `prefers-reduced-motion: reduce` still starts paused after the first drawn frame.
- Maintain WCAG AA contrast for declared text/surface pairs, including 14px `#A9B4C0` `Static preview` on `#151D28` sidebar/drawer and `#F4F7FA` titles on `#132844` current links.
- `aria-hidden="true"` on decorative SVG only. Caption remains in the accessibility tree so the link name can include `Static preview` plus title plus description.
- No keyboard trap. Dual `DemoNavigation` must not create duplicate `id` attributes.
- Interactive scenes announce interaction via visible figcaption text, not color alone. Keyboard may mirror labeled controls (D-08).

Source: `28-CONTEXT.md` D-02 / D-08 / D-23.

---

## Focused Browser Evidence (visual oracles)

These are verification contracts, not extra chrome:

- Chromium `just web-player-smoke` covers sixteen desktop-sidebar static previews (visible SVG + `Static preview` text scoped to `.demo-sidebar`).
- Soup and Wave Machine open, play, pause, and reset without requiring pointer gestures or labeled scene controls.
- Soup Stirrer, Impulse, and Theo Jansen open, play, pause, and reset; smoke covers one Soup Stirrer rail toggle, one Impulse click inside the box, and one Theo Jansen direction change.
- Current eleven scenes still open and run.
- Keep existing `.catalog-card` count **0**.
- Update shell copy from eleven → sixteen demos; bump `ALL_SCENE_TIMEOUT_MS` only if sixteen-scene loops time out (do not change physics caps).
- Do not add screenshot-hash oracles, Firefox/Safari/WebKit projects, Linux qualification, or a live Pages redeploy as this phase’s gate.
- If a new Playwright file is added, it must be listed in `web/package.json` `test:player`.

Source: `28-CONTEXT.md` D-25, `28-RESEARCH.md` Validation Architecture.

---

## Explicit Phase Boundaries

Do not add:

- `.catalog-card`, three-column card grid, CatalogNav, nested catalog `Open`, or a separate catalog page;
- README WebP `<img>` previews or per-row WASM/Canvas thumbnails;
- a new visual system, MysticUI adoption, or `components.json` / shadcn registry init;
- freeglut key legends as the only controls; particle-type presets (PRESET-01); material pickers; pointer drag;
- Theo Jansen limit toggle or speed-magnitude control;
- Sparky, Drawing Particles, or the full twelve-scene PLAY-01 catalog claim (Phase 29);
- sealed C++ parity claims or differential evidence UI (PARITY-01);
- raising `MAX_ADVANCE_STEPS` or cutting particle counts for FPS;
- confirmation dialogs for Reset;
- Firefox/Safari/WebKit matrices, Linux qualification, or a live Pages redeploy as the phase gate;
- crate publication or a git release tag;
- teaching cross-links from Soup to Soup Stirrer;
- SolidJS approximations of destroy-in-shape, group shove, or motor speed.

These are locked later-phase or out-of-scope boundaries from `28-CONTEXT.md`, not omissions to fill during Phase 28 implementation.

---

## Component Inventory (this phase)

| Piece | Action |
|-------|--------|
| `web/src/catalog/scenes.ts` | Append `soup`, `soup-stirrer`, `impulse`, `wave-machine`, `theo-jansen`; controls and hints per contract; pinned credits |
| `web/src/catalog/previews.tsx` | Add five token-only SVG cases captioned via existing `Static preview` chrome |
| `web/src/player/runtime.ts` | Update `PAGE_SUMMARY` eleven → sixteen |
| `web/src/components/DemoNavigation.tsx` | Unchanged pattern (maps `SCENES`) |
| `web/src/components/PlaygroundShell.tsx` | No layout redesign; update any hard-coded eleven-demo / focus-wrap assumptions to sixteen / Theo Jansen last |
| `web/src/components/SceneControls.tsx` | Hide when controls empty; render action + live presets without visual redesign |
| `web/src/components/scene-credits.ts` | Reuse host-locked helpers |
| `web/src/app.css` / `player.css` | No new design tokens required; avoid `.catalog-card` |
| `crates/liquidfun-wasm/src/scene/` | New soup family + five scene modules (UI contract: recognizable canvas draw only) |
| `web/e2e/player.spec.ts` / helpers / shell | Sixteen-scene paths and preview counts; watch-first + interactive smoke; last drawer link Theo Jansen |

---

## Source Decisions Applied

- `28-CONTEXT.md`: D-01–D-03 catalog shell; D-04–D-08 controls; D-09–D-13 layouts; D-14–D-19 native seams (UI must not fake them); D-20–D-26 counts/credits/proof; Claude’s Discretion locked for SVG, direction-only Theo, and exact control wording above.
- `28-RESEARCH.md`: Standard stack, architecture patterns, catalog/test seams, sixteen-scene focus wrap, interactive vs watch-first contracts.
- `27-UI-SPEC.md` / live CSS: spacing, typography, shell layout, preview frame CSS — reused; **accent corrected to live `#4DA3FF`**.
- `.planning/REQUIREMENTS.md`: ACT-01–ACT-05.
- `.planning/ROADMAP.md` Phase 28 success criteria.
- `standards/core/frontend-ui.md`: dark default, GitHub source affordance, truthful FOSS copy.
- `standards-overrides.md`: shadcn-solid shell (Tailwind v4, Kobalte, Corvu drawer); `skipLibCheck`.
- Locked parent prompt + `--auto`: no user questions; unanswered packing reused live player tokens and research recommendations.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
