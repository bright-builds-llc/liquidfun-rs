---
phase: 28-interaction-seams
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun/src/world/particle_object/particle.rs
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun-wasm/src/scene.rs
  - crates/liquidfun-wasm/src/scene/soup_family.rs
  - crates/liquidfun-wasm/src/scene/soup.rs
  - crates/liquidfun-wasm/src/session/tests.rs
autonomous: true
requirements: [ACT-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-09-22T12-56-07
generated_at: 2026-09-22T13:15:00Z
must_haves:
  truths:
    - "Visitor can construct Soup via SceneId::Soup: basin liquid with floating circle, two squares, and edge noodles after native carve."
    - "Focused liquidfun test proves destroy-in-shape removes particles under a placed fixture before presentation."
    - "MAX_ADVANCE_STEPS remains 4; destruction-by-age is not enabled on Soup."
  artifacts:
    - path: crates/liquidfun/src/world/particle_object/particle.rs
      provides: "Public World::destroy_particles_in_shape (or equivalent thin helper) using AABB query + test_point + mark_particle_for_destruction"
      exports: ["destroy_particles_in_shape"]
    - path: crates/liquidfun-wasm/src/scene/soup_family.rs
      provides: "Private shared Soup basin/group/solids/carve builder returning ground BodyId"
    - path: crates/liquidfun-wasm/src/scene/soup.rs
      provides: "Watch-first Soup BuiltScene"
      exports: ["build"]
    - path: crates/liquidfun-wasm/src/scene.rs
      provides: "SceneId::Soup allowlist + build_scene arm"
  key_links:
    - from: crates/liquidfun-wasm/src/scene.rs
      to: crates/liquidfun-wasm/src/scene/soup.rs
      via: "build_scene(SceneId::Soup) → soup::build"
      pattern: "SceneId::Soup => soup::build"
    - from: crates/liquidfun-wasm/src/scene/soup_family.rs
      to: World::destroy_particles_in_shape
      via: "carve under circle and two boxes before first frame"
      pattern: "destroy_particles_in_shape"
---

<objective>
Add a thin native destroy-in-shape helper and port pinned Soup as a watch-first WASM scene with a private shared soup builder.

Purpose: Deliver ACT-01 recognizable broth-with-bobbing-solids using native carve (D-09, D-14, D-15, D-19, D-20). Soup Stirrer will compose the same builder in Plan 02.
Output: `destroy_particles_in_shape`, `soup_family.rs`, `soup.rs`, `SceneId::Soup` factory wiring, focused carve + construction tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/28-interaction-seams/28-CONTEXT.md
@.planning/phases/28-interaction-seams/28-RESEARCH.md
@.planning/phases/28-interaction-seams/28-UI-SPEC.md
@crates/liquidfun-wasm/src/scene.rs
@crates/liquidfun-wasm/src/scene/particles.rs
@crates/liquidfun/src/world/particle_object/particle.rs
@crates/liquidfun/src/world/query.rs
@third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testSoup.js

<interfaces>
Pinned Soup layout (from `testSoup.js` at `7f20402173fd143a3988c921bc384459c6a858f2`):

```text
Ground static body fixtures:
  Floor: (-4,-1), (4,-1), (4,0), (-4,0)
  Left:  (-4,-0.1), (-2,-0.1), (-2,2), (-4,3)
  Right: (2,-0.1), (4,-0.1), (4,3), (2,2)
ParticleSystemDef.radius = 0.035 (no damping override in JS Soup)
Gravity: (0, -10)
Water group: box half-extents 2×1 centered (0,1), angle 0
Dynamic solids (density 0.1) then carve each shape at body transform:
  Circle center (0,0.5) r=0.1
  Box half 0.1×0.1 at (-1,0.5) angle 0
  Box half 0.1×0.1 at (1,0.5) angle 0.5 rad
Edge noodles (fixture density 1) with SetMassData mass 0.1 at midpoint:
  (0,2)–(0.1,2.1), (0.3,2)–(0.4,2.1), (-0.3,2.1)–(-0.2,2)
```

Do not enable `with_destruction_by_age(true)`. Do not cut particle counts. Do not raise `MAX_ADVANCE_STEPS` above 4. Do not add catalog/TS edits. Do not add Soup Stirrer / Impulse / Wave / Theo ids yet.
</interfaces>
</context>

<feature>
  <name>Soup WASM scene + destroy-in-shape</name>
  <files>crates/liquidfun/src/world/particle_object/particle.rs, crates/liquidfun-wasm/src/scene/soup_family.rs, crates/liquidfun-wasm/src/scene/soup.rs, crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <behavior>
    - `World::destroy_particles_in_shape` removes particles whose positions pass `shape.test_point` inside the fixture AABB (unit test proves pocket empty under a placed circle)
    - `parse_scene_id("soup")` → `Ok(SceneId::Soup)`
    - `SessionCore::create(SceneId::Soup)` succeeds; particle_count &gt; 0; rigid circles/segments exported for solids
    - Advance several steps does not panic; particle count stays &gt; 0
    - Unknown control/action → `SessionError::UnknownControl`; `apply_pointer` is no-op success
    - Soup particle system never sets destruction-by-age
    - `MAX_ADVANCE_STEPS` source equals `4`
  </behavior>
  <implementation>
    Public thin helper preferred (Claude Discretion / research): AABB via `shape.compute_aabb`, `query_aabb_with_particles`, keep `WorldQueryOccurrence::Particle` hits that pass `test_point`, then `mark_particle_for_destruction`. Shared `soup_family` builds ground + water + solids + carve and returns `ground: BodyId` for Stirrer prismatic later. Soup scene wraps family with watch-first hooks.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing destroy-in-shape and Soup allowlist tests</name>
  <files>crates/liquidfun/src/world/particle_object/particle.rs, crates/liquidfun-wasm/src/session/tests.rs, crates/liquidfun-wasm/src/scene/soup.rs</files>
  <read_first>
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-09, D-14, D-15, D-19, D-20
    1. `.planning/phases/28-interaction-seams/28-RESEARCH.md` Pattern 3, Pitfall 1, Capability Gap Matrix
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testSoup.js`
    1. `crates/liquidfun/src/world/particle_object/particle.rs` `mark_particle_for_destruction`
    1. `crates/liquidfun/src/world/query.rs` `query_aabb_with_particles` / `WorldQueryOccurrence`
    1. `crates/liquidfun-wasm/src/session/tests.rs` `parse_scene_id_maps_allowlisted_tokens`
    1. `standards/core/testing.md`
  </read_first>
  <behavior>
    1. Test: after filling an AABB with particles and placing a circle fixture, `destroy_particles_in_shape` leaves zero particles whose positions pass `test_point` for that shape/transform.
    1. Test: `parse_scene_id("soup")` equals `Ok(SceneId::Soup)`.
    1. Test: `SessionCore::create(SceneId::Soup)` Ok with `particle_count() > 0`.
    1. Test: Soup session exports at least one rigid circle (floating solid).
    1. Test: advance several times does not panic; particle count stays &gt; 0.
    1. Test: unknown control yields `SessionError::UnknownControl`.
  </behavior>
  <action>
    Per D-15 and testing standards, write Arrange/Act/Assert tests first. Add a focused `#[test]` in the liquidfun particle module (or adjacent tests module) that constructs a small world, creates particles overlapping a circle, calls `destroy_particles_in_shape`, and asserts the pocket is empty. Extend `parse_scene_id_maps_allowlisted_tokens` with `("soup", SceneId::Soup)` — expect compile-fail until Task 2 adds the enum. Add `#[cfg(test)]` Soup construction/advance tests in `soup.rs` mirroring Surface Tension watch-first coverage. Do not edit `web/**`. Do not add `soup-stirrer` / `impulse` / `wave-machine` / `theo-jansen`. Do not change `MAX_ADVANCE_STEPS`.
  </action>
  <verify>
    <automated>cargo test -p liquidfun destroy_particles_in_shape -- --nocapture 2&gt;&amp;1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'destroy_particles_in_shape' crates/liquidfun/src` matches at least one test reference.
    1. `rg -n '"soup"' crates/liquidfun-wasm/src/session/tests.rs` matches the allowlist table.
    1. `rg -n 'SceneId::Soup' crates/liquidfun-wasm/src/scene/soup.rs crates/liquidfun-wasm/src/session/tests.rs` matches at least one test reference.
    1. Tests exist before production `soup::build` is complete (RED or compile-fail expected until Task 2).
  </acceptance_criteria>
  <done>Failing destroy-in-shape and Soup allowlist/construction coverage is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement destroy helper, soup_family, Soup scene, factory wiring</name>
  <files>crates/liquidfun/src/world/particle_object/particle.rs, crates/liquidfun/src/lib.rs, crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/scene/soup_family.rs, crates/liquidfun-wasm/src/scene/soup.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <read_first>
    1. `crates/liquidfun-wasm/src/scene/particles.rs` watch-first hooks + collect_segments/circles
    1. `crates/liquidfun-wasm/src/scene.rs` `SceneId`, `parse_scene_id`, `build_scene`, `attach_basin_fixture`
    1. `crates/liquidfun/src/world/object/body_object.rs` `set_body_mass_data` usage pattern
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testSoup.js`
    1. `standards/languages/rust.md`
    1. `standards/core/architecture.md`
  </read_first>
  <behavior>
    Same as Task 1; all listed tests must pass.
  </behavior>
  <action>
    Per D-09/D-14/D-15/D-19/D-20: implement `World::destroy_particles_in_shape(system, shape, transform)` that queries the shape AABB, filters particles with `shape.test_point(transform, position)`, marks them for destruction, and returns the destroyed count. Re-export if needed from `liquidfun` public API. Create private `mod soup_family;` that builds the pinned Soup ground (return `BodyId`), water box group at radius `0.035`, places circle + two boxes + three edge noodles with mass `0.1` at midpoints via `set_body_mass_data`, and carves under the three solid fixtures with `destroy_particles_in_shape` before returning — never call `with_destruction_by_age(true)`. Add `mod soup;` and `SceneId::Soup`. Map `"soup"` in `parse_scene_id`. Call `soup::build` from `build_scene`. Soup wraps `soup_family`, rejects unknown controls/actions, `apply_pointer` no-op success (D-04). Export basin segments and solid circles/edges. Do not cut counts. Do not raise `MAX_ADVANCE_STEPS`. Do not touch `web/**`. Do not add other Phase 28 scene ids.
  </action>
  <verify>
    <automated>cargo test -p liquidfun destroy_particles_in_shape -- --nocapture &amp;&amp; cargo test -p liquidfun-wasm soup -- --nocapture &amp;&amp; cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture &amp;&amp; rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs &amp;&amp; rg -n 'with_destruction_by_age\(true\)' crates/liquidfun-wasm/src/scene/soup.rs crates/liquidfun-wasm/src/scene/soup_family.rs</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'fn destroy_particles_in_shape' crates/liquidfun/src` matches the public helper.
    1. `rg -n 'SceneId::Soup' crates/liquidfun-wasm/src/scene.rs` matches enum, parse, and build arms.
    1. `rg -n '0\.035' crates/liquidfun-wasm/src/scene/soup_family.rs` matches system radius.
    1. `rg -n 'destroy_particles_in_shape' crates/liquidfun-wasm/src/scene/soup_family.rs` matches carve call sites (≥3 solids).
    1. `rg -n 'with_destruction_by_age\(true\)' crates/liquidfun-wasm/src/scene/soup.rs crates/liquidfun-wasm/src/scene/soup_family.rs` is empty.
    1. `rg -n 'soup-stirrer|SoupStirrer|impulse|wave-machine|theo-jansen' crates/liquidfun-wasm/src/scene.rs` is empty for new Phase 28 ids beyond Soup.
    1. `rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs` matches.
    1. `cargo test -p liquidfun destroy_particles_in_shape` exits 0.
    1. `cargo test -p liquidfun-wasm soup` exits 0.
    1. `cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens` exits 0.
  </acceptance_criteria>
  <done>Soup is constructible through the WASM factory with native carve and a shared soup_family builder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hash/scene id string → WASM factory | Untrusted route token crosses into allowlisted `SceneId` parse. |
| Scene/module → liquidfun public API | Carve must stay safe Rust; no raw pointers into JS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | T | `parse_scene_id` | mitigate | Exact lowercase `"soup"` only; unknown tokens still `UnknownScene`; no case folding. |
| T-28-01-02 | D | Particle carve amplification | mitigate | Carve only during build for known fixture shapes; no visitor-supplied shapes; destruction-by-age stays off. |
| T-28-01-03 | D | Catch-up under load | mitigate | Do not raise `MAX_ADVANCE_STEPS` above 4; verify constant remains 4. |
| T-28-01-04 | E | Watch-first controls | mitigate | Reject unknown controls; `apply_pointer` no-op — no hidden mutation surface. |
</threat_model>

<verification>
`cargo test -p liquidfun destroy_particles_in_shape` and `cargo test -p liquidfun-wasm soup` exit 0; `MAX_ADVANCE_STEPS` stays 4; no destruction-by-age on Soup.
</verification>

<success_criteria>
1. ACT-01 native Soup constructs with carved floating solids via shared `soup_family`.
1. Focused destroy-in-shape test proves particle-free pocket under a fixture.
1. No catalog advertising yet; no other Phase 28 scene ids.
</success_criteria>

<output>
After completion, create `.planning/phases/28-interaction-seams/28-01-SUMMARY.md`
</output>
