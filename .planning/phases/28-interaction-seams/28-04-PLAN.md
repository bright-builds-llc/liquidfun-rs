---
phase: 28-interaction-seams
plan: "04"
type: tdd
wave: 4
depends_on: ["03"]
files_modified:
  - crates/liquidfun-wasm/src/scene.rs
  - crates/liquidfun-wasm/src/scene/wave_machine.rs
  - crates/liquidfun-wasm/src/session/tests.rs
autonomous: true
requirements: [ACT-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-09-22T12-56-07
generated_at: 2026-09-22T13:15:00Z
must_haves:
  truths:
    - "Visitor can construct Wave Machine with a motorized revolute four-wall tank of water that rocks from simulated time."
    - "on_advance sets motor speed to 0.05 * cos(t) * π with t += dt per session step; pause freezes t; reset zeroes t."
    - "Water Wheel motor-off revolute is not copied; MAX_ADVANCE_STEPS stays 4."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/wave_machine.rs
      provides: "Wave Machine BuiltScene with enable_motor revolute + on_advance speed"
      exports: ["build"]
    - path: crates/liquidfun-wasm/src/scene.rs
      provides: "SceneId::WaveMachine allowlist + build_scene arm"
  key_links:
    - from: crates/liquidfun-wasm/src/scene/wave_machine.rs
      to: World::set_revolute_motor_speed
      via: "on_advance sim-time update"
      pattern: "set_revolute_motor_speed"
    - from: crates/liquidfun-wasm/src/scene/wave_machine.rs
      to: RevoluteJointDef::with_motor
      via: "enable_motor true at create"
      pattern: "with_motor|enable_motor"
---

<objective>
Port Wave Machine as a watch-first motorized tank whose revolute speed follows simulated time inside session on_advance.

Purpose: Deliver ACT-04 rocking slosh with honest pause/4-step behavior (D-04, D-12, D-14, D-17, D-20). No catalog yet.
Output: `wave_machine.rs`, `SceneId::WaveMachine`, motor-speed-vs-t unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/28-interaction-seams/28-CONTEXT.md
@.planning/phases/28-interaction-seams/28-RESEARCH.md
@crates/liquidfun-wasm/src/scene/water_wheel.rs
@third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testWaveMachine.js

<interfaces>
Pinned Wave Machine (`testWaveMachine.js`):

```text
Static ground body
Dynamic tank body at (0,1), allowSleep=false, four thin walls density 5:
  (2,0) 0.05×1, (-2,0) 0.05×1, (0,1) 2×0.05, (0,-1) 2×0.05  (half-extents)
Revolute ground↔tank at (0,1): enable_motor=true, maxMotorTorque=1e7,
  initial motorSpeed = 0.05 * π
ParticleSystemDef.radius=0.025, damping=0.2
Fill box 0.9×0.9 at (0,1)
on_advance (each session step): t += 1/60; set_revolute_motor_speed(joint, 0.05 * cos(t) * PI)
Watch-first: apply_pointer no-op; reject unknown controls
```

ANTI-PATTERN: Do not copy Water Wheel `enable_motor: false` / empty motor writes. Do not set motor speed from JS rAF. Do not raise MAX_ADVANCE_STEPS. Do not change radius from 0.025. Do not edit web/**.
</interfaces>
</context>

<feature>
  <name>Wave Machine WASM scene</name>
  <files>crates/liquidfun-wasm/src/scene/wave_machine.rs, crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <behavior>
    - `parse_scene_id("wave-machine")` → `Ok(SceneId::WaveMachine)`
    - Create Ok; particles &gt; 0; motorized revolute present
    - After N advances, queried motor speed equals `0.05 * cos(N/60) * π` within float tolerance
    - Creating a fresh session resets t to 0 (first advance uses cos(1/60) path after increment — assert t accounting matches JS Step order: increment then set)
    - apply_pointer no-op; unknown control → UnknownControl
    - MAX_ADVANCE_STEPS stays 4
  </behavior>
  <implementation>
    Use `RevoluteJointDef` with motor enabled. Mutate only in `on_advance` via `set_revolute_motor_speed`. Match JS Step order: `t += 1/60` then set speed.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing Wave Machine allowlist and motor-time tests</name>
  <files>crates/liquidfun-wasm/src/session/tests.rs, crates/liquidfun-wasm/src/scene/wave_machine.rs</files>
  <read_first>
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-04, D-12, D-17, D-20
    1. `.planning/phases/28-interaction-seams/28-RESEARCH.md` Pattern 5, Pitfall 4
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testWaveMachine.js`
    1. `crates/liquidfun-wasm/src/scene/water_wheel.rs` — confirm motor-off anti-pattern; do not copy
    1. `standards/core/testing.md`
  </read_first>
  <behavior>
    1. Test: `parse_scene_id("wave-machine")` → `Ok(SceneId::WaveMachine)`.
    1. Test: create Ok; particle_count &gt; 0.
    1. Test: after k advances, motor speed ≈ `0.05 * (k as f32 / 60.0).cos() * PI`.
    1. Test: fresh create then one advance ≠ stalled zero forever (speed changes from initial).
    1. Test: unknown control → UnknownControl; apply_pointer Ok no-op.
  </behavior>
  <action>
    Per D-12/D-17, write failing tests first. Extend allowlist with `("wave-machine", SceneId::WaveMachine)`. Add wave_machine tests for create and sim-time motor speed. Explicitly document Water Wheel is NOT the template. Do not edit web/**. Do not add theo-jansen yet.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture 2&gt;&amp;1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n '"wave-machine"' crates/liquidfun-wasm/src/session/tests.rs` matches.
    1. `rg -n 'set_revolute_motor_speed|0\.05' crates/liquidfun-wasm/src/scene/wave_machine.rs` matches test references.
    1. `rg -n 'SceneId::WaveMachine' crates/liquidfun-wasm/src/scene/wave_machine.rs crates/liquidfun-wasm/src/session/tests.rs` matches.
    1. Tests exist before production build is complete (RED/compile-fail until Task 2).
  </acceptance_criteria>
  <done>Failing Wave Machine allowlist/motor-time coverage is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Wave Machine scene and factory wiring</name>
  <files>crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/scene/wave_machine.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <read_first>
    1. `crates/liquidfun/src/world/joint/revolute.rs` `set_revolute_motor_speed` / `set_revolute_motor_enabled`
    1. `crates/liquidfun/src/joint/definition/revolute_prismatic.rs` motor builders
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testWaveMachine.js`
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-12, D-17, D-20
  </read_first>
  <behavior>
    Same as Task 1; all tests pass.
  </behavior>
  <action>
    Per D-04/D-12/D-14/D-17/D-20: add `mod wave_machine;` and `SceneId::WaveMachine`. Map `"wave-machine"`. Build four-wall dynamic tank + revolute with `enable_motor: true`, max torque `1e7`, initial speed `0.05 * PI`. Particles radius `0.025` damping `0.2`, fill `0.9×0.9` at `(0,1)`. Store `time: f32 = 0` and joint id. In `on_advance`: `self.time += 1.0/60.0`; `world.set_revolute_motor_speed(joint, 0.05 * self.time.cos() * std::f32::consts::PI)?`. Watch-first hooks. Do NOT copy Water Wheel motor-off. Do not mutate motor from anywhere except on_advance. Do not raise MAX_ADVANCE_STEPS. Do not change radius. Do not touch web/**.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm wave_machine -- --nocapture &amp;&amp; cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture &amp;&amp; rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs &amp;&amp; rg -n '0\.025' crates/liquidfun-wasm/src/scene/wave_machine.rs &amp;&amp; rg -n 'set_revolute_motor_speed' crates/liquidfun-wasm/src/scene/wave_machine.rs</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'SceneId::WaveMachine' crates/liquidfun-wasm/src/scene.rs` matches enum, parse, build.
    1. `rg -n 'set_revolute_motor_speed' crates/liquidfun-wasm/src/scene/wave_machine.rs` matches on_advance path.
    1. `rg -n '0\.05' crates/liquidfun-wasm/src/scene/wave_machine.rs` matches motor formula constant.
    1. `rg -n '0\.025' crates/liquidfun-wasm/src/scene/wave_machine.rs` matches radius.
    1. `rg -n 'enable_motor:\s*false' crates/liquidfun-wasm/src/scene/wave_machine.rs` is empty.
    1. `rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs` matches.
    1. `cargo test -p liquidfun-wasm wave_machine` exits 0.
  </acceptance_criteria>
  <done>Wave Machine constructs and rocks via live revolute motor updates from simulated time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scene id → motorized session | Allowlisted construction only. |
| Session step → motor speed | Motor writes must stay inside session on_advance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | T | parse_scene_id | mitigate | Exact `"wave-machine"` token only. |
| T-28-04-02 | D | Catch-up honesty | mitigate | Motor updates only in on_advance; MAX_ADVANCE_STEPS stays 4; pause freezes steps hence t. |
| T-28-04-03 | E | Watch-first surface | mitigate | Reject unknown controls; apply_pointer no-op. |
| T-28-04-04 | I | Motor pattern honesty | mitigate | Unit test asserts cos(t) speed formula; forbid Water Wheel motor-off copy. |
</threat_model>

<verification>
`cargo test -p liquidfun-wasm wave_machine` exits 0; radius 0.025; MAX_ADVANCE_STEPS 4; motor-on pattern present.
</verification>

<success_criteria>
1. ACT-04 Wave Machine rocks from sim-time motor speed in on_advance.
1. Pause/reset honesty preserved via session steps; no JS motor writes.
</success_criteria>

<output>
After completion, create `.planning/phases/28-interaction-seams/28-04-SUMMARY.md`
</output>
