---
phase: 28-interaction-seams
plan: "02"
type: tdd
wave: 2
depends_on: ["01"]
files_modified:
  - crates/liquidfun-wasm/src/scene.rs
  - crates/liquidfun-wasm/src/scene/soup_stirrer.rs
  - crates/liquidfun-wasm/src/session/tests.rs
autonomous: true
requirements: [ACT-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-09-22T12-56-07
generated_at: 2026-09-22T13:15:00Z
must_haves:
  truths:
    - "Visitor can construct Soup Stirrer that reuses soup_family, adds paddle carve + prismatic rail, and stirs while attached."
    - "Toggle paddle rail twice creates then destroys then recreates the prismatic joint without leaks; Reset remount restores rail attached."
    - "Canvas pointer up inside soup AABB and action toggle-paddle-rail invoke the same toggle path."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/soup_stirrer.rs
      provides: "Soup Stirrer BuiltScene with prismatic Option<JointId>, on_advance stir guards, shared toggle"
      exports: ["build"]
    - path: crates/liquidfun-wasm/src/scene.rs
      provides: "SceneId::SoupStirrer allowlist + build_scene arm"
  key_links:
    - from: crates/liquidfun-wasm/src/scene/soup_stirrer.rs
      to: crates/liquidfun-wasm/src/scene/soup_family.rs
      via: "compose soup_family then paddle/carve/prismatic"
      pattern: "soup_family::"
    - from: crates/liquidfun-wasm/src/scene/soup_stirrer.rs
      to: PrismaticJointDef create_joint/destroy_joint
      via: "Option<JointId> toggle"
      pattern: "destroy_joint|create_joint|PrismaticJointDef"
---

<objective>
Port Soup Stirrer by composing the shared soup builder with a paddle, carve, prismatic rail toggle, and guarded stir force.

Purpose: Deliver ACT-02 free/restore rail behavior with native joints and on_advance stirring (D-05, D-10, D-14, D-18, D-19). No catalog yet.
Output: `soup_stirrer.rs`, `SceneId::SoupStirrer`, toggle×2 + Reset + InSoup pointer tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/28-interaction-seams/28-CONTEXT.md
@.planning/phases/28-interaction-seams/28-RESEARCH.md
@.planning/phases/28-interaction-seams/28-01-SUMMARY.md
@crates/liquidfun-wasm/src/scene/soup_family.rs
@crates/liquidfun-wasm/src/scene/jelly_drop.rs
@third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testSoupStirrer.js

<interfaces>
Pinned Stirrer extras (from `testSoupStirrer.js`):

```text
Reuse soup_family ground BodyId as prismatic bodyA (not a separate g_groundBody).
SetDamping(1.0) on the particle system after soup build.
Paddle: dynamic circle r=0.4 at (0,0.7), density 1.0; carve under paddle shape (identity transform in JS — use paddle body transform).
PrismaticJointDef: bodyA=ground, bodyB=paddle, collide_connected=true, local_axis_a=(1,0), local_anchor_a=paddle position.
Toggle: if Some(joint) → destroy_joint and clear; else create_joint.
Action id: "toggle-paddle-rail" (catalog label later: Toggle paddle rail).
Pointer Up inside InSoup box y∈(-1,2) ∧ x∈(-3,3) calls same toggle; outside no-op.
on_advance while joint Some AND paddle InSoup AND speed < 2.0:
  oscillationOffset += 1/60; wrap period 1/0.2 = 5s
  force angle = offset * 0.2 * τ (use TAU); magnitude 10 via apply_body_force_to_center
When joint None: do not apply continuous stir force.
```

Do not fork a second soup layout. Do not enable destruction-by-age. Do not raise MAX_ADVANCE_STEPS. Do not edit web/**.
</interfaces>
</context>

<feature>
  <name>Soup Stirrer WASM scene</name>
  <files>crates/liquidfun-wasm/src/scene/soup_stirrer.rs, crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <behavior>
    - `parse_scene_id("soup-stirrer")` → `Ok(SceneId::SoupStirrer)`
    - Create succeeds; particles &gt; 0; paddle circle exported
    - Action `toggle-paddle-rail` twice: joint present → absent → present (inspect via subsequent stir behavior or joint query)
    - Pointer up inside soup AABB toggles; outside does not
    - Reset/recreate starts with rail attached (joint Some)
    - Unknown control → UnknownControl
    - MAX_ADVANCE_STEPS stays 4
  </behavior>
  <implementation>
    Compose soup_family only. Store Option&lt;JointId&gt;. Shared private toggle used by apply_control and apply_pointer. Stir only when attached with InSoup + max-speed guards (D-18).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing Soup Stirrer allowlist and toggle tests</name>
  <files>crates/liquidfun-wasm/src/session/tests.rs, crates/liquidfun-wasm/src/scene/soup_stirrer.rs</files>
  <read_first>
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-05, D-10, D-18, D-19
    1. `.planning/phases/28-interaction-seams/28-RESEARCH.md` Pattern 6, Pitfall 5
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testSoupStirrer.js`
    1. `.planning/phases/28-interaction-seams/28-01-SUMMARY.md`
    1. `crates/liquidfun-wasm/src/session/tests.rs` allowlist table
    1. `standards/core/testing.md`
  </read_first>
  <behavior>
    1. Test: `parse_scene_id("soup-stirrer")` → `Ok(SceneId::SoupStirrer)`.
    1. Test: create Ok; particle_count &gt; 0.
    1. Test: apply_control `"toggle-paddle-rail"` returns `ControlEffect::Live` twice without error.
    1. Test: pointer up at (0,0.5) toggles; pointer up at (10,10) is success no-op (rail state unchanged relative to prior).
    1. Test: after recreate via SessionCore::create again, first on_advance path assumes rail attached (document via test that initial toggle detaches rather than attaches-first — i.e. first toggle destroys).
  </behavior>
  <action>
    Per D-05/D-18, write failing tests first. Extend allowlist with `("soup-stirrer", SceneId::SoupStirrer)`. Add soup_stirrer module tests covering create, toggle×2 Live effects, InSoup vs out-of-soup pointer, and remount starts attached. Do not edit web/**. Do not add impulse/wave/theo ids.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture 2&gt;&amp;1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n '"soup-stirrer"' crates/liquidfun-wasm/src/session/tests.rs` matches.
    1. `rg -n 'toggle-paddle-rail' crates/liquidfun-wasm/src/scene/soup_stirrer.rs` matches test references.
    1. `rg -n 'SceneId::SoupStirrer' crates/liquidfun-wasm/src/scene/soup_stirrer.rs crates/liquidfun-wasm/src/session/tests.rs` matches.
    1. Tests exist before production build is complete (RED/compile-fail until Task 2).
  </acceptance_criteria>
  <done>Failing Soup Stirrer allowlist/toggle coverage is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Soup Stirrer scene and factory wiring</name>
  <files>crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/scene/soup_stirrer.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <read_first>
    1. `crates/liquidfun-wasm/src/scene/soup_family.rs` return type / ground BodyId
    1. `crates/liquidfun-wasm/src/scene/jelly_drop.rs` apply_control / apply_pointer patterns
    1. `crates/liquidfun/src/joint/definition/revolute_prismatic.rs` `PrismaticJointDef`
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testSoupStirrer.js`
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-05, D-18, D-19
  </read_first>
  <behavior>
    Same as Task 1; all tests pass.
  </behavior>
  <action>
    Per D-05/D-10/D-14/D-18/D-19: add `mod soup_stirrer;` and `SceneId::SoupStirrer`. Map `"soup-stirrer"`. Implement build: call soup_family, set particle damping `1.0`, create paddle circle at `(0,0.7)` r=`0.4`, carve with `destroy_particles_in_shape`, create prismatic joint on ground→paddle with `local_axis_a=(1,0)`, `collide_connected=true`, store `Option&lt;JointId&gt;::Some`. `apply_control` for `"toggle-paddle-rail"` only → Live. `apply_pointer` on Up inside InSoup AABB calls same toggle; other kinds/positions no-op success. `on_advance`: if joint attached and paddle InSoup and linear speed &lt; `2.0`, apply rotating force magnitude `10` via `apply_body_force_to_center` using τ-based angle from oscillation offset; if joint None, skip force. Reject unknown controls. Do not enable destruction-by-age. Do not raise MAX_ADVANCE_STEPS. Do not touch web/**.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm soup_stirrer -- --nocapture &amp;&amp; cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture &amp;&amp; rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs &amp;&amp; ! rg -n 'with_destruction_by_age\(true\)' crates/liquidfun-wasm/src/scene/soup_stirrer.rs</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'SceneId::SoupStirrer' crates/liquidfun-wasm/src/scene.rs` matches enum, parse, build.
    1. `rg -n 'soup_family::' crates/liquidfun-wasm/src/scene/soup_stirrer.rs` matches composition.
    1. `rg -n 'PrismaticJointDef|destroy_joint' crates/liquidfun-wasm/src/scene/soup_stirrer.rs` matches.
    1. `rg -n 'toggle-paddle-rail' crates/liquidfun-wasm/src/scene/soup_stirrer.rs` matches production control id.
    1. `rg -n 'with_damping\(1\.0\)|set_damping\(1\.0\)|damping.*1\.0' crates/liquidfun-wasm/src/scene/soup_stirrer.rs` matches.
    1. `rg -n 'with_destruction_by_age\(true\)' crates/liquidfun-wasm/src/scene/soup_stirrer.rs` is empty.
    1. `rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs` matches.
    1. `cargo test -p liquidfun-wasm soup_stirrer` exits 0.
  </acceptance_criteria>
  <done>Soup Stirrer constructs, toggles prismatic rail safely, and stirs only while attached.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Control/pointer input → joint lifecycle | Untrusted action names and pointer coords mutate joints. |
| on_advance → body force | Continuous force must be bounded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | T | `toggle-paddle-rail` / pointer | mitigate | Exact action id allowlist; InSoup AABB gate; finite coords via existing session checks. |
| T-28-02-02 | D | Unbounded stir force | mitigate | Force only when `Option&lt;JointId&gt;::Some` AND InSoup AND speed &lt; 2.0; destroy_joint clears force path. |
| T-28-02-03 | T | Joint leak on toggle | mitigate | Toggle×2 tests; destroy before recreate; Reset remount starts attached. |
| T-28-02-04 | E | Unknown controls | mitigate | Reject unknown names with `SessionError::UnknownControl`. |
</threat_model>

<verification>
`cargo test -p liquidfun-wasm soup_stirrer` exits 0; MAX_ADVANCE_STEPS stays 4; no destruction-by-age.
</verification>

<success_criteria>
1. ACT-02 Soup Stirrer reuses soup_family and supports free/restore rail via action and InSoup pointer.
1. Toggle×2 and remount-covered; stir guards prevent runaway when detached.
</success_criteria>

<output>
After completion, create `.planning/phases/28-interaction-seams/28-02-SUMMARY.md`
</output>
