---
phase: 28-interaction-seams
plan: "08"
type: execute
wave: 8
depends_on: ["06", "07"]
files_modified:
  - web/e2e/player-helpers.ts
  - web/e2e/player.spec.ts
  - web/e2e/shell.spec.ts
  - web/e2e/reset-honesty.spec.ts
autonomous: true
requirements: [ACT-01, ACT-02, ACT-03, ACT-04, ACT-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-09-22T12-56-07
generated_at: 2026-09-22T13:15:00Z
must_haves:
  truths:
    - "Chromium smoke opens Soup, Soup Stirrer, Impulse, Wave Machine, and Theo Jansen; plays, pauses, and resets each."
    - "Interactive smoke covers one Soup Stirrer rail toggle, one Impulse click inside the box, and one Theo Jansen direction change; watch-first Soup/Wave skip gestures."
    - "Current eleven scenes still open and run; desktop sidebar shows sixteen Static preview captions; .catalog-card count stays 0; drawer reverse-tab last link is Theo Jansen."
  artifacts:
    - path: web/e2e/player.spec.ts
      provides: "Watch-first + interactive coverage for sixteen scenes including three new gestures"
    - path: web/e2e/player-helpers.ts
      provides: "Sixteen SCENE_HASH_PATHS + timeout if needed"
    - path: web/e2e/shell.spec.ts
      provides: "Sixteen static preview asserts + Theo Jansen last focus + sixteen-demo copy"
  key_links:
    - from: web/e2e/player.spec.ts
      to: web/src/catalog/scenes.ts
      via: "controls.length watch-first split + POINTER_CONTROL for interactive"
      pattern: "controls\\.length|POINTER_CONTROL"
    - from: just web-player-smoke
      to: web/e2e/*
      via: "bun scripts/web-build.ts player-smoke"
      pattern: "web-player-smoke"
---

<objective>
Extend Chromium `just web-player-smoke` so sixteen scenes prove open/play/pause/reset, interactive gestures cover Stirrer/Impulse/Theo, and the prior eleven scenes still run.

Purpose: Close ACT-01–ACT-05 browser proof under D-03/D-25 without Firefox/Safari, Linux qualification, or Pages redeploy. Independent AI review remains eligible (D-26); implementing agent must not self-approve.
Output: Updated Playwright helpers/specs and a green player-smoke gate.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/verification.md
@.planning/phases/28-interaction-seams/28-CONTEXT.md
@.planning/phases/28-interaction-seams/28-RESEARCH.md
@.planning/phases/28-interaction-seams/28-UI-SPEC.md
@.planning/phases/28-interaction-seams/28-06-SUMMARY.md
@.planning/phases/28-interaction-seams/28-07-SUMMARY.md
@web/e2e/player.spec.ts
@web/e2e/player-helpers.ts
@web/e2e/shell.spec.ts

<interfaces>
Required helper updates:

```ts
export const SCENE_HASH_PATHS: Readonly<Record<SceneId, string>> = {
  // existing eleven…
  soup: "/liquidfun-rs/#/scene/soup",
  "soup-stirrer": "/liquidfun-rs/#/scene/soup-stirrer",
  impulse: "/liquidfun-rs/#/scene/impulse",
  "wave-machine": "/liquidfun-rs/#/scene/wave-machine",
  "theo-jansen": "/liquidfun-rs/#/scene/theo-jansen",
};

// Raise ALL_SCENE_TIMEOUT_MS only if sixteen open/reset loops time out (start from 220_000; bump measured — do not change MAX_ADVANCE_STEPS).
```

Watch-first path: `scene.controls.length === 0` → Soup + Wave Machine auto-covered without POINTER_CONTROL.

POINTER_CONTROL additions (Partial Record):
```ts
"soup-stirrer": { gesture: "click", control: "Toggle paddle rail" },
impulse: { gesture: "click", control: "Push" }, // click inside box via performSceneGesture; Push is the labeled preset to exercise
"theo-jansen": { gesture: "click", control: "Motor direction" }, // direction change via activateLabeledControl / select
```

Shell:
- Replace “All eleven demos” asserts with sixteen-demo copy matching PAGE_SUMMARY.
- Desktop sidebar: **sixteen** `Static preview` texts and **sixteen** `svg[aria-hidden='true']` inside `.demo-sidebar`.
- Keep `.catalog-card` `toHaveCount(0)`.
- Drawer reverse-tab last link: **Theo Jansen** (was Rigid Particles).
- Focus wrap: Dismiss + 16 scene links; adjust modulus/comments accordingly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend helpers and player e2e for sixteen scenes + gestures</name>
  <files>web/e2e/player-helpers.ts, web/e2e/player.spec.ts, web/e2e/reset-honesty.spec.ts</files>
  <read_first>
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-04, D-05, D-06, D-07, D-25
    1. `.planning/phases/28-interaction-seams/28-RESEARCH.md` Pitfall 8, Pattern 7
    1. `web/e2e/player.spec.ts` watch-first vs interactive split + POINTER_CONTROL
    1. `web/e2e/player-helpers.ts` `SCENE_HASH_PATHS`, `ALL_SCENE_TIMEOUT_MS`
    1. `web/e2e/reset-honesty.spec.ts` timeout imports
  </read_first>
  <action>
    Per D-25/D-04–D-07: add five hash paths to `SCENE_HASH_PATHS`. Keep `controls.length === 0` watch-first branch (covers Soup + Wave Machine + prior watch-first). Extend POINTER_CONTROL with `soup-stirrer` → click + `Toggle paddle rail`, `impulse` → click (inside box) + `Push`, `theo-jansen` → control activation for `Motor direction` (change to Reverse). Ensure interactive filter still covers prior six + three new interactive scenes. All-scene open/reset loop must iterate sixteen SCENES. Bump `ALL_SCENE_TIMEOUT_MS` only if needed (do not change physics caps). Force CI=true path remains via smoke recipe. Do not add Firefox/WebKit. Do not deploy Pages. Do not cut particle counts or raise `MAX_ADVANCE_STEPS`.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'soup|soup-stirrer|impulse|wave-machine|theo-jansen' web/e2e/player-helpers.ts` matches all five hash paths.
    1. `rg -n 'soup-stirrer|Toggle paddle rail' web/e2e/player.spec.ts` matches POINTER_CONTROL entry.
    1. `rg -n 'impulse|Push' web/e2e/player.spec.ts` matches POINTER_CONTROL entry.
    1. `rg -n 'theo-jansen|Motor direction' web/e2e/player.spec.ts` matches POINTER_CONTROL entry.
    1. `rg -n 'controls\.length === 0|controls\.length > 0' web/e2e/player.spec.ts` matches the watch-first split.
    1. `rg -n 'ALL_SCENE_TIMEOUT_MS\s*=\s*[0-9_]+' web/e2e/player-helpers.ts` shows a timeout ≥ 220_000.
    1. `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Player e2e helpers cover sixteen scenes with interactive gesture maps for Stirrer/Impulse/Theo.</done>
</task>

<task type="auto">
  <name>Task 2: Update shell e2e and run Chromium web-player-smoke</name>
  <files>web/e2e/shell.spec.ts</files>
  <read_first>
    1. `.planning/phases/28-interaction-seams/28-CONTEXT.md` D-03, D-25, D-26
    1. `.planning/phases/28-interaction-seams/28-UI-SPEC.md` Narrow-Viewport Acceptance + Focused Browser Evidence
    1. `web/e2e/shell.spec.ts` eleven-demo / Rigid Particles last-link asserts
    1. Justfile / `just web-player-smoke` recipe
  </read_first>
  <action>
    Per D-03/D-25: update shell.spec copy from eleven→sixteen demos; assert sixteen `Static preview` captions and sixteen `svg[aria-hidden='true']` in `.demo-sidebar`; keep `.catalog-card` `toHaveCount(0)`; change drawer reverse-tab last link from Rigid Particles to Theo Jansen; adjust focus-wrap arithmetic for Dismiss+16. Then run `CI=true just web-player-smoke` (or repo-equivalent) and fix failures without raising MAX_ADVANCE_STEPS or cutting particle counts. Do not add Firefox/Safari/Linux/Pages gates. Record that independent AI review remains eligible (D-26) — implementing agent must not self-approve in SUMMARY.
  </action>
  <verify>
    <automated>CI=true just web-player-smoke</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'All sixteen demos|sixteen static previews|sixteen' web/e2e/shell.spec.ts` matches updated asserts.
    1. `rg -n 'All eleven demos|eleven static previews' web/e2e/shell.spec.ts` is empty.
    1. `rg -n 'Theo Jansen' web/e2e/shell.spec.ts` matches last-link assert.
    1. `rg -n 'catalog-card' web/e2e/shell.spec.ts` still has `toHaveCount(0)`.
    1. `rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs` matches.
    1. `CI=true just web-player-smoke` exits 0.
  </acceptance_criteria>
  <done>Chromium smoke proves sixteen-scene open/play/pause/reset plus Stirrer/Impulse/Theo gestures; .catalog-card stays 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser hash routes → WASM session | E2E exercises allowlisted scene construction only. |
| Pointer/control UI → session | Gestures must hit labeled/allowlisted surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08-01 | T | Hash routes | mitigate | Only SCENE_IDS paths; unknown-scene fallback remains covered by existing tests. |
| T-28-08-02 | D | Sixteen-scene timeout DoS | mitigate | Timeout bump only for smoke loop; never raise MAX_ADVANCE_STEPS or particle caps. |
| T-28-08-03 | S | Catalog card revival | mitigate | Assert `.catalog-card` count 0. |
| T-28-08-04 | I | Gesture coverage honesty | mitigate | Explicit POINTER_CONTROL entries for stirrer/impulse/theo; watch-first scenes must not require gestures. |
</threat_model>

<verification>
`CI=true just web-player-smoke` exits 0; shell asserts sixteen previews and Theo Jansen last; MAX_ADVANCE_STEPS stays 4.
</verification>

<success_criteria>
1. ACT-01–ACT-05 proven in Chromium: five new scenes open/play/pause/reset; interactive gestures covered; eleven regression green.
1. No `.catalog-card`; no cross-browser/Pages gate; D-26 independent review eligibility noted (no self-approval).
</success_criteria>

<output>
After completion, create `.planning/phases/28-interaction-seams/28-08-SUMMARY.md`
</output>
