---
phase: 26-catalog-shell-and-basin-scenes
plan: "02"
type: tdd
wave: 2
depends_on: ["01"]
files_modified:
  - crates/liquidfun-wasm/src/scene.rs
  - crates/liquidfun-wasm/src/scene/liquid_timer.rs
  - crates/liquidfun-wasm/src/session.rs
autonomous: true
requirements: [BASIN-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 26-2026-09-22T00-02-36
generated_at: 2026-09-22T00:20:00Z
must_haves:
  truths:
    - "SessionCore::create(SceneId::LiquidTimer) builds a closed bowl, TENSILE|VISCOUS slab, gap/shelves/columns, and advances without panic."
    - "Created Liquid Timer particles carry ParticleFlags::TENSILE | ParticleFlags::VISCOUS."
    - "collect_segments exports every shelf and column edge so zig-zag geometry is drawable."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/liquid_timer.rs
      provides: "Pinned Liquid Timer chain + edges + tensile/viscous slab"
      exports: ["build"]
    - path: crates/liquidfun-wasm/src/scene.rs
      provides: "SceneId::LiquidTimer allowlist entry and build_scene arm"
  key_links:
    - from: crates/liquidfun-wasm/src/scene.rs
      to: crates/liquidfun-wasm/src/scene/liquid_timer.rs
      via: "build_scene(SceneId::LiquidTimer) → liquid_timer::build"
      pattern: "SceneId::LiquidTimer => liquid_timer::build"
    - from: crates/liquidfun-wasm/src/scene/liquid_timer.rs
      to: liquidfun ParticleFlags
      via: "ParticleFlags::TENSILE | ParticleFlags::VISCOUS on group recipe"
      pattern: "TENSILE.*VISCOUS|VISCOUS.*TENSILE"
---

<objective>
Port the pinned LiquidFun Liquid Timer test as a watch-first WASM scene with tensile/viscous drain geometry.

Purpose: Deliver BASIN-02 recognizable hourglass drain using existing `ParticleFlags` and `ChainShape`/`EdgeShape` APIs (D-11, D-12). Escalate to strength tweaks only if defaults fail recognition; no new engine subsystems.
Output: `liquid_timer.rs`, factory wiring, and flag/segment construction tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-CONTEXT.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-RESEARCH.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-01-SUMMARY.md
@crates/liquidfun-wasm/src/scene.rs
@crates/liquidfun-wasm/src/scene/particles.rs
@crates/liquidfun-wasm/src/scene/jelly_drop.rs
@third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testLiquidTimer.js

<interfaces>
Pinned layout (from `testLiquidTimer.js`):

```text
ChainShape closed loop: (-2,0), (2,0), (2,4), (-2,4)
ParticleSystemDef.radius = 0.025
Particle slab: PolygonShape::oriented_box(2.0, 0.4, Vec2::new(0.0, 3.6), 0.0)
Particle flags: ParticleFlags::TENSILE | ParticleFlags::VISCOUS
Edge shelves (exact endpoints):
  (-2, 3.2)-(−1.2, 3.2)
  (−1.1, 3.2)-(2, 3.2)
  (−1.2, 3.2)-(−1.2, 2.8)
  (−1.1, 3.2)-(−1.1, 2.8)
  (−1.6, 2.4)-(0.8, 2.0)
  (1.6, 1.6)-(−0.8, 1.2)
  (−1.2, 0.8)-(−1.2, 0)
  (−0.4, 0.8)-(−0.4, 0)
  (0.4, 0.8)-(0.4, 0)
  (1.2, 0.8)-(1.2, 0)
```

Public API already available:

```rust
use liquidfun::collision::{ChainShape, EdgeShape, PolygonShape, Shape};
use liquidfun::{ParticleFlags, ParticleGroupDestination, ParticleGroupRecipe, /* ... */};
```

Do not add particle-type presets (PRESET-01 deferred). Keep `MAX_ADVANCE_STEPS = 4`.
</interfaces>
</context>

<feature>
  <name>Liquid Timer WASM scene</name>
  <files>crates/liquidfun-wasm/src/scene/liquid_timer.rs, crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/session.rs</files>
  <behavior>
    - `parse_scene_id("liquid-timer")` → `Ok(SceneId::LiquidTimer)`
    - Construction succeeds; particles include `TENSILE | VISCOUS`
    - `collect_segments` length ≥ 10 (all shelf/column edges; bowl edges optional extras)
    - Advance several steps without panic; particle count stays &gt; 0
    - Watch-first: unknown controls rejected; pointer no-op
  </behavior>
  <implementation>
    Scene-only. Use default `ParticleSystemDef` viscous/surface-tension strengths first. If drain is unrecognizable after geometry+flags, adjust strengths via existing `with_viscous_strength` / `with_surface_tension_*` only (Claude discretion) — do not invent new APIs.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing Liquid Timer allowlist and flag tests</name>
  <files>crates/liquidfun-wasm/src/session.rs, crates/liquidfun-wasm/src/scene/liquid_timer.rs</files>
  <read_first>
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-CONTEXT.md` D-11, D-12
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-RESEARCH.md` Pattern 3, Pitfall 2, Pitfall 4
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testLiquidTimer.js`
    1. `crates/liquidfun-wasm/src/scene/jelly_drop.rs` flag assert test pattern
    1. `crates/liquidfun/src/particle/definition.rs` `TENSILE` / `VISCOUS` bits
  </read_first>
  <behavior>
    1. Test: `parse_scene_id("liquid-timer")` equals `Ok(SceneId::LiquidTimer)`.
    1. Test: `SessionCore::create(SceneId::LiquidTimer)` succeeds with particle count &gt; 0.
    1. Test: at least one particle's flags contain both `TENSILE` and `VISCOUS` (or aggregate buffer check equivalent used by jelly_drop).
    1. Test: hooks/session rigid segments count is at least 10.
    1. Test: advance without panic; particle count remains &gt; 0.
  </behavior>
  <action>
    Write failing tests first. Extend `parse_scene_id_maps_allowlisted_tokens` with `("liquid-timer", SceneId::LiquidTimer)`. Put construction/flag/segment tests in `liquid_timer.rs` `#[cfg(test)]` (or `liquid_timer/tests.rs` if approaching the file-length trigger). Do not edit `web/**`. Do not change Particles geometry. Do not raise `MAX_ADVANCE_STEPS`.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture 2&gt;&amp;1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n '\"liquid-timer\"' crates/liquidfun-wasm/src/session.rs` matches.
    1. `rg -n 'TENSILE|VISCOUS' crates/liquidfun-wasm/src/scene/liquid_timer.rs` matches test expectations.
    1. RED/compile-fail is expected until Task 2 implements the scene.
  </acceptance_criteria>
  <done>Failing Liquid Timer flag and allowlist coverage exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Liquid Timer scene and factory wiring</name>
  <files>crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/scene/liquid_timer.rs, crates/liquidfun-wasm/src/session.rs</files>
  <read_first>
    1. `crates/liquidfun/src/collision/shape/chain.rs` `ChainShape::closed`
    1. `crates/liquidfun/src/collision/shape` `EdgeShape::new`
    1. `crates/liquidfun-wasm/src/scene/particles.rs` watch-first hooks pattern from Plan 01
    1. `crates/liquidfun-wasm/src/scene/color_mixer.rs` `ParticleGroupRecipe::with_particle_flags`
    1. `standards/languages/rust.md`
  </read_first>
  <behavior>
    Same as Task 1; all tests pass.
  </behavior>
  <action>
    Per D-11/D-12/D-13: add `mod liquid_timer;` and `SceneId::LiquidTimer`. Map `"liquid-timer"` in `parse_scene_id`. Implement `liquid_timer::build` with closed `ChainShape` bowl, radius `0.025`, filled box group with `ParticleFlags::TENSILE | ParticleFlags::VISCOUS`, and every listed `EdgeShape` shelf/column fixture. Store every shelf/column endpoint pair in a static `RigidSegment` list returned by `collect_segments` (Pitfall 4). Watch-first hooks: reject unknown controls/actions; pointer no-op. Do not add presets. Do not cut particle counts. Do not raise `MAX_ADVANCE_STEPS`. Prefer default strengths; only if drain is unrecognizable after a local visual/advance check, tweak existing `ParticleSystemDef` strength setters — never add new engine APIs. Do not edit `web/**` in this plan.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm liquid_timer -- --nocapture &amp;&amp; cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture &amp;&amp; rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'SceneId::LiquidTimer' crates/liquidfun-wasm/src/scene.rs` matches enum, parse, and build.
    1. `rg -n 'ParticleFlags::TENSILE\s*\|\s*ParticleFlags::VISCOUS' crates/liquidfun-wasm/src/scene/liquid_timer.rs` matches.
    1. `rg -n '0\.025' crates/liquidfun-wasm/src/scene/liquid_timer.rs` matches.
    1. `rg -n 'ChainShape::closed|EdgeShape::new' crates/liquidfun-wasm/src/scene/liquid_timer.rs` matches both.
    1. `rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs` matches.
    1. `cargo test -p liquidfun-wasm liquid_timer` exits 0.
  </acceptance_criteria>
  <done>Liquid Timer constructs with tensile/viscous flags and drawable shelf segments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scene id → factory | `"liquid-timer"` allowlisted only through `parse_scene_id`. |
| Control/pointer → physics | Watch-first; no visitor-driven material mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | T | `parse_scene_id` | mitigate | Exact token `"liquid-timer"`; case-sensitive reject otherwise. |
| T-26-02-02 | T | `liquid_timer` hooks | mitigate | Unknown control/action → `UnknownControl`; pointer no-op. |
| T-26-02-03 | I | Honesty of material flags | mitigate | Unit test asserts `TENSILE \| VISCOUS` bits present. |
| T-26-02-04 | D | Advance catch-up | accept | Cap remains 4; no new network surface. |
</threat_model>

<verification>
`cargo test -p liquidfun-wasm liquid_timer` passes; allowlist includes both `particles` and `liquid-timer`; catch-up cap unchanged.
</verification>

<success_criteria>
1. Liquid Timer is constructible with recognizable drain geometry and tensile/viscous flags (BASIN-02).
1. Still no catalog advertisement (Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/26-catalog-shell-and-basin-scenes/26-02-SUMMARY.md`
</output>
