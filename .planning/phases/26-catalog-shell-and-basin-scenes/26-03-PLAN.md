---
phase: 26-catalog-shell-and-basin-scenes
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - web/src/catalog/scenes.ts
  - web/src/catalog/previews.tsx
  - web/src/player/runtime.ts
  - web/src/components/SceneControls.tsx
  - web/src/components/PlayerSceneChrome.tsx
autonomous: true
requirements: [PLAY-02, PLAY-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 26-2026-09-22T00-02-36
generated_at: 2026-09-22T00:20:00Z
must_haves:
  truths:
    - "SCENE_IDS order is the six existing demos then particles then liquid-timer; both new records are ready: true with empty controls."
    - "Particles and Liquid Timer credit pinned JS + C++ blob URLs at commit 7f20402173fd143a3988c921bc384459c6a858f2 via host-locked implementation paths."
    - "PAGE_SUMMARY says eight demos; empty SceneControls region is not rendered; Static preview SVGs exist for both new ids."
  artifacts:
    - path: web/src/catalog/scenes.ts
      provides: "Eight-scene catalog records, credits, watch-first hints"
    - path: web/src/catalog/previews.tsx
      provides: "Particles and Liquid Timer static SVG previews"
    - path: web/src/player/runtime.ts
      provides: "PAGE_SUMMARY eight-demo copy"
    - path: web/src/components/SceneControls.tsx
      provides: "Hide when controls.length === 0"
  key_links:
    - from: web/src/catalog/scenes.ts
      to: crates/liquidfun-wasm SceneId tokens
      via: "id particles / liquid-timer match parse_scene_id"
      pattern: "\"particles\"|\"liquid-timer\""
    - from: web/src/catalog/previews.tsx
      to: DemoNavigation caption
      via: "ScenePreview switch exhaustiveness"
      pattern: "case \"particles\"|case \"liquid-timer\""
---

<objective>
Append Particles and Liquid Timer to the shared catalog/player chrome with honest credits, static previews, and watch-first empty controls.

Purpose: Satisfy PLAY-02 chrome (play/pause/reset only) and PLAY-03 pinned-test credits without restoring card grids or advertising unfinished WASM (D-01–D-09, D-03).
Output: Updated `scenes.ts`, `previews.tsx`, `PAGE_SUMMARY`, and empty-controls hide wiring.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@standards/core/frontend-ui.md
@standards/languages/typescript-javascript.md
@standards-overrides.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-CONTEXT.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-UI-SPEC.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-01-SUMMARY.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-02-SUMMARY.md
@web/src/catalog/scenes.ts
@web/src/catalog/previews.tsx
@web/src/player/runtime.ts
@web/src/components/SceneControls.tsx
@web/src/components/scene-credits.ts

<interfaces>
Exact catalog append (D-01 / UI-SPEC):

```ts
export const SCENE_IDS = [
  "dam-break",
  "fountain",
  "float-or-sink",
  "color-mixer",
  "jelly-drop",
  "water-wheel",
  "particles",
  "liquid-timer",
] as const;
```

New records (exact strings from 26-UI-SPEC):

| Field | Particles | Liquid Timer |
|-------|-----------|--------------|
| title | `Particles` | `Liquid Timer` |
| ready | `true` | `true` |
| description | `Watch water fall in an open basin while a ball drops into it.` | `Watch tensile, viscous liquid drain through shelves into bottom columns.` |
| interactionHint | `This scene is watch-first. Use Play scene, Pause scene, and Reset scene.` | same |
| controls | `[]` | `[]` |
| implementationPath | `crates/liquidfun-wasm/src/scene/particles.rs` | `crates/liquidfun-wasm/src/scene/liquid_timer.rs` |

Inspiration entries (two each + optional showcase), commit `7f20402173fd143a3988c921bc384459c6a858f2`:

- Particles: label `Pinned Particles.js` → `.../lfjs/testbed/tests/testParticles.js`; label `Pinned Particles.h` → `.../Testbed/Tests/Particles.h`
- Liquid Timer: label `Pinned LiquidTimer.js` → `.../lfjs/testbed/tests/testLiquidTimer.js`; label `Pinned LiquidTimer.h` → `.../Testbed/Tests/LiquidTimer.h`

`PAGE_SUMMARY` exact replacement:

```ts
"Play experimental Rust physics scenes in the browser. All eight demos run this repository's engine through WebAssembly."
```

Preview tokens: water `#39D3C7` or existing `#4DA3FF` only if matching existing WATER constant; UI-SPEC accent water is `#39D3C7` for new preview water shapes — prefer `#39D3C7` for new scenes' water fills while keeping existing six preview colors unchanged. Rigid `#CBD5E1`, canvas `#071018`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append scene records and pinned credits</name>
  <files>web/src/catalog/scenes.ts, web/src/player/runtime.ts</files>
  <read_first>
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-CONTEXT.md` D-01, D-04, D-05, D-07, D-08, D-09
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-UI-SPEC.md` Copywriting Contract + Credits copy
    1. `web/src/catalog/scenes.ts` full file
    1. `web/src/player/runtime.ts` `PAGE_SUMMARY`
    1. `web/src/components/scene-credits.ts` host-lock helpers (do not weaken)
  </read_first>
  <action>
    Per D-01/D-04/D-05/D-07/D-08/D-09: append `"particles"` and `"liquid-timer"` to `SCENE_IDS` after the six existing ids (never reorder the first six). Append matching `SCENES` records with `ready: true`, exact UI-SPEC descriptions and watch-first interaction hints, `controls: []`, implementation paths via `sceneSource("particles.rs")` / `sceneSource("liquid_timer.rs")`, and inspiration arrays with the two pinned blob URLs each (plus optional existing `SHOWCASE` entry for consistency with Fountain). Implementation `href`s must stay host-locked through existing credit chrome — never set implementationPath to a google/liquidfun URL. Replace `PAGE_SUMMARY` with the exact eight-demo sentence. Do not add water-amount/gravity/particle-type controls. Do not restore `.catalog-card`. Do not edit WASM scene modules in this task.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck 2&gt;&amp;1 | head -60</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n '\"particles\"|\"liquid-timer\"' web/src/catalog/scenes.ts` matches SCENE_IDS and records.
    1. `rg -n 'All eight demos' web/src/player/runtime.ts` matches.
    1. `rg -n '7f20402173fd143a3988c921bc384459c6a858f2' web/src/catalog/scenes.ts` matches ≥4 inspiration hrefs (Particles.js/h + LiquidTimer.js/h).
    1. `rg -n 'Pinned Particles\.js|Pinned Particles\.h|Pinned LiquidTimer\.js|Pinned LiquidTimer\.h' web/src/catalog/scenes.ts` matches all four labels.
    1. `rg -n 'controls:\s*\[\]' web/src/catalog/scenes.ts` matches both new scenes.
    1. `rg -n 'sealed C\+\+|bit-exact|live thumbnail' web/src/catalog/scenes.ts web/src/player/runtime.ts` is empty.
    1. Typecheck may still fail until Task 2 updates `previews.tsx` exhaustive switch — that is expected if Task 1 runs alone; finish Task 2 before claiming plan done.
  </acceptance_criteria>
  <done>Catalog metadata and PAGE_SUMMARY advertise eight ready scenes with pinned credits.</done>
</task>

<task type="auto">
  <name>Task 2: Static previews and hide empty SceneControls</name>
  <files>web/src/catalog/previews.tsx, web/src/components/SceneControls.tsx, web/src/components/PlayerSceneChrome.tsx</files>
  <read_first>
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-UI-SPEC.md` Catalog Shell + Watch-First Player Contract
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-CONTEXT.md` D-02, D-03
    1. `web/src/catalog/previews.tsx`
    1. `web/src/components/SceneControls.tsx`
    1. `web/src/components/PlayerSceneChrome.tsx`
    1. `web/src/components/DemoNavigation.tsx` caption `Static preview` (do not change caption text)
  </read_first>
  <action>
    Per D-02/D-03/UI-SPEC: add `ParticlesPreview` (open three-segment basin `#CBD5E1`, teal water circle, rigid ball above) and `LiquidTimerPreview` (bowl outline, zig-zag shelves, teal slab + four bottom column marks). Wire both into `ScenePreview` switch cases `"particles"` and `"liquid-timer"`. Keep SVGs still (no CSS/SMIL animation). Do not use `&lt;img&gt;`, canvas, or WASM. Hide empty controls: in `SceneControls` (preferred) wrap the root so when `props.controls.length === 0` the component returns null / empty fragment and does not render `.scene-controls`; alternatively gate in `PlayerSceneChrome` with `&lt;Show when={props.scene.controls.length &gt; 0}&gt;`. Do not redesign control styling. Do not introduce `.catalog-card`. Leave DemoNavigation pattern mapping `SCENES` unchanged.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'case \"particles\"|case \"liquid-timer\"' web/src/catalog/previews.tsx` matches both.
    1. `rg -n 'controls\.length' web/src/components/SceneControls.tsx web/src/components/PlayerSceneChrome.tsx` matches a hide gate.
    1. `rg -n 'catalog-card' web/src` is empty (or only comments asserting absence — prefer empty).
    1. `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Eight-scene TypeScript catalog compiles with static previews and no empty controls chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog metadata → visitor | Copy and links shape trust about what runs. |
| Inspiration hrefs → external GitHub | Allowed citation; implementation must stay host-locked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | S | Implementation credit links | mitigate | Keep `implementationPath` under `crates/liquidfun-wasm/...`; `implementationHref` host-lock unchanged. |
| T-26-03-02 | I | Catalog honesty | mitigate | No sealed-parity / live-thumbnail copy; `Static preview` caption unchanged in DemoNavigation. |
| T-26-03-03 | T | Scene id allowlist | mitigate | Only append ids that WASM Plans 01–02 already parse. |
| T-26-03-04 | E | Empty controls UX | mitigate | Hide empty `.scene-controls` so visitors cannot invent fake control surfaces. |
</threat_model>

<verification>
`cd web && bun run typecheck` passes; scenes.ts and previews cover eight ids with credits and watch-first empty controls.
</verification>

<success_criteria>
1. Catalog lists eight ready scenes with PLAY-03 credits and PLAY-02 watch-first chrome.
1. No card grid; PAGE_SUMMARY says eight.
</success_criteria>

<output>
After completion, create `.planning/phases/26-catalog-shell-and-basin-scenes/26-03-SUMMARY.md`
</output>
