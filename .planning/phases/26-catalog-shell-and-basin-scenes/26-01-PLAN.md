---
phase: 26-catalog-shell-and-basin-scenes
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun-wasm/src/scene.rs
  - crates/liquidfun-wasm/src/scene/particles.rs
  - crates/liquidfun-wasm/src/session.rs
autonomous: true
requirements: [BASIN-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 26-2026-09-22T00-02-36
generated_at: 2026-09-22T00:20:00Z
must_haves:
  truths:
    - "SessionCore::create(SceneId::Particles) builds an open basin, a water particle group, and one dynamic ball without panic."
    - "parse_scene_id(\"particles\") returns SceneId::Particles; unknown tokens still fail."
    - "MAX_ADVANCE_STEPS remains 4; Particles apply_pointer / apply_control / apply_action reject or no-op without mutating layout."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/particles.rs
      provides: "Pinned Particles basin + water circle + dynamic ball scene"
      exports: ["build"]
    - path: crates/liquidfun-wasm/src/scene.rs
      provides: "SceneId::Particles allowlist entry and build_scene arm"
    - path: crates/liquidfun-wasm/src/session.rs
      provides: "parse_scene_id allowlist includes particles"
  key_links:
    - from: crates/liquidfun-wasm/src/scene.rs
      to: crates/liquidfun-wasm/src/scene/particles.rs
      via: "build_scene(SceneId::Particles) → particles::build"
      pattern: "SceneId::Particles => particles::build"
    - from: crates/liquidfun-wasm/src/scene/particles.rs
      to: liquidfun public API
      via: "attach_basin_fixture / CircleShape / ParticleGroupRecipe"
      pattern: "create_particle_group|attach_basin_fixture"
---

<objective>
Port the pinned LiquidFun Particles test as a watch-first WASM scene: open basin, falling water circle, and one dynamic ball.

Purpose: Deliver BASIN-01 recognizable motion on the public `liquidfun` API without engine feature work, catalog advertising, or catch-up/cap changes (D-10, D-12, D-13).
Output: `particles.rs`, `SceneId::Particles` factory wiring, and headless construction/advance tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-CONTEXT.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-RESEARCH.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-UI-SPEC.md
@crates/liquidfun-wasm/src/scene.rs
@crates/liquidfun-wasm/src/scene/dam_break.rs
@crates/liquidfun-wasm/src/scene/color_mixer.rs
@crates/liquidfun-wasm/src/session.rs
@third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testParticles.js

<interfaces>
Pinned layout constants (from `testParticles.js`; do not invent alternate geometry):

```text
Floor polygon: (-4,-1), (4,-1), (4,0), (-4,0)
Left wall: (-4,-0.1), (-2,-0.1), (-2,2), (-4,3)
Right wall: (2,-0.1), (4,-0.1), (4,3), (2,2)
ParticleSystemDef.radius = 0.035
Water group: filled CircleShape center (0,3) radius 2; color RGBA (255,0,0,255)
Dynamic ball: BodyType::Dynamic, CircleShape center (0,8) radius 0.5, density 0.5
Gravity: Vec2::new(0.0, -10.0) unless dam_break default world already sets equivalent
```

Existing factory surface in `crates/liquidfun-wasm/src/scene.rs` (extend; keep the six existing variants):

```rust
pub(crate) enum SceneId { DamBreak, Fountain, FloatOrSink, ColorMixer, JellyDrop, WaterWheel /* + Particles */ }
pub(crate) fn parse_scene_id(raw: &str) -> Result<SceneId, SessionError>;
pub(crate) fn build_scene(id: SceneId, presets: &[(String, String)]) -> Result<BuiltScene, SessionError>;
pub(crate) trait SceneHooks { /* on_advance, apply_control, apply_action, apply_pointer, collect_segments, collect_circles */ }
```

Reuse `attach_basin_fixture` for the three basin polygons. Follow Dam Break / Color Mixer for hooks: static `basin_segments`, `collect_circles` returns the dynamic ball pose+radius. Do not add `liquid-timer` in this plan.
</interfaces>
</context>

<feature>
  <name>Particles WASM scene</name>
  <files>crates/liquidfun-wasm/src/scene/particles.rs, crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/session.rs</files>
  <behavior>
    - `parse_scene_id("particles")` → `Ok(SceneId::Particles)`
    - `SessionCore::create(SceneId::Particles)` succeeds; particle count &gt; 0; at least one rigid circle exported
    - Advance 8 steps does not panic; particle count does not drop to zero
    - Unknown control/action returns `SessionError::UnknownControl`; `apply_pointer` is a no-op success or ignore without mutating particle count
    - `MAX_ADVANCE_STEPS` source still equals `4`
  </behavior>
  <implementation>
    Scene-only port per D-10/D-12. No new `liquidfun` APIs. No catalog/TS edits. Do not raise catch-up cap or shrink radius below 0.035.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing Particles allowlist and construction tests</name>
  <files>crates/liquidfun-wasm/src/session.rs, crates/liquidfun-wasm/src/scene/particles.rs</files>
  <read_first>
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-CONTEXT.md` D-10, D-12, D-13
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-RESEARCH.md` Pattern 2, Pitfall 3
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testParticles.js`
    1. `crates/liquidfun-wasm/src/session.rs` `parse_scene_id_maps_allowlisted_tokens`
    1. `crates/liquidfun-wasm/src/scene/dam_break/tests.rs` construction/advance patterns
    1. `standards/core/testing.md`
  </read_first>
  <behavior>
    1. Test: `parse_scene_id("particles")` equals `Ok(SceneId::Particles)`.
    1. Test: `SessionCore::create(SceneId::Particles)` returns Ok and `particle_count() > 0`.
    1. Test: created session `collect`/frame path exposes at least one rigid circle (ball).
    1. Test: `advance(4)` eight times (or equivalent) does not panic and particle count stays &gt; 0.
    1. Test: unknown control name yields `SessionError::UnknownControl`.
  </behavior>
  <action>
    Per D-10 and testing standards, write Arrange/Act/Assert unit tests first. Extend `parse_scene_id_maps_allowlisted_tokens` with `("particles", SceneId::Particles)` — this will not compile until Task 2 adds the enum variant. Add `#[cfg(test)]` module tests in `particles.rs` (or `particles/tests.rs` if the entry file would exceed the ~628-line file-length trigger) that construct via `SessionCore::create(SceneId::Particles)`. Do not edit `web/**`. Do not add `liquid-timer`. Do not change `MAX_ADVANCE_STEPS`.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture 2&gt;&amp;1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n '\"particles\"' crates/liquidfun-wasm/src/session.rs` matches the allowlist table.
    1. `rg -n 'SceneId::Particles' crates/liquidfun-wasm/src/scene/particles.rs crates/liquidfun-wasm/src/session.rs` matches at least one test reference.
    1. Tests exist before production `particles::build` is complete (RED or compile-fail expected until Task 2).
  </acceptance_criteria>
  <done>Failing Particles allowlist/construction coverage is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Particles scene and factory wiring</name>
  <files>crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/scene/particles.rs, crates/liquidfun-wasm/src/session.rs</files>
  <read_first>
    1. `crates/liquidfun-wasm/src/scene/dam_break.rs` basin + dynamic circle pattern
    1. `crates/liquidfun-wasm/src/scene/color_mixer.rs` `ParticleGroupRecipe` + circle source
    1. `crates/liquidfun-wasm/src/scene.rs` `SceneId`, `parse_scene_id`, `build_scene`, `attach_basin_fixture`
    1. `standards/languages/rust.md` foo.rs + foo/ module shape
    1. `standards/core/architecture.md` scene construction in WASM shell
  </read_first>
  <behavior>
    Same as Task 1; all listed tests must pass.
  </behavior>
  <action>
    Per D-10/D-12/D-13: add `mod particles;` and `SceneId::Particles`. Map `"particles"` in `parse_scene_id`. Call `particles::build` from `build_scene`. Implement `particles::build` using the pinned floor + slanted walls via `attach_basin_fixture`, `ParticleSystemDef` radius `0.035`, filled water circle group at `(0, 3)` radius `2` with color `(255, 0, 0, 255)`, and one dynamic ball at `(0, 8)` radius `0.5` density `0.5`. Reject non-empty unknown presets/controls/actions with `SessionError::UnknownControl`. Make `apply_pointer` a no-op success (D-05). Export three basin segments plus the ball in `collect_segments` / `collect_circles`. Do not enable emitter lifetimes or `destroy_by_age` recipes that cull the group. Do not cut particle counts or raise `MAX_ADVANCE_STEPS` above 4. Do not add engine APIs. Do not touch `web/**` or Liquid Timer.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm particles -- --nocapture &amp;&amp; cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture &amp;&amp; rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'SceneId::Particles' crates/liquidfun-wasm/src/scene.rs` matches enum, parse, and build arms.
    1. `rg -n '0\.035' crates/liquidfun-wasm/src/scene/particles.rs` matches the system radius.
    1. `rg -n 'Vec2::new\(0\.0,\s*8\.0\)|Vec2::new\(0,\s*8\)' crates/liquidfun-wasm/src/scene/particles.rs` matches the ball position.
    1. `rg -n 'liquid-timer|LiquidTimer' crates/liquidfun-wasm/src/scene.rs crates/liquidfun-wasm/src/scene/particles.rs` is empty.
    1. `rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs` matches.
    1. `cargo test -p liquidfun-wasm particles` exits 0.
    1. `cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens` exits 0.
  </acceptance_criteria>
  <done>Particles is constructible and advance-safe through the WASM session factory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scene id string → WASM factory | Only allowlisted tokens construct worlds. |
| Pointer/control tokens → scene hooks | Watch-first scene must not accept arbitrary control names as physics mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-01 | T | `parse_scene_id` | mitigate | Allowlist `"particles"` only; unknown ids remain `UnknownScene`. |
| T-26-01-02 | T | `particles` hooks | mitigate | Reject unknown controls/actions; pointer is no-op. |
| T-26-01-03 | D | `SessionCore::advance` | accept | No new network; catch-up stays capped at 4 (D-13). |
| T-26-01-04 | I | Frame copy path | accept | Existing copied typed-array frames; no new pointer export. |
</threat_model>

<verification>
`cargo test -p liquidfun-wasm particles` and allowlist parse tests pass; `MAX_ADVANCE_STEPS` remains 4.
</verification>

<success_criteria>
1. Visitor-facing WASM path can construct Particles with open basin + water + ball (BASIN-01 foundation).
1. No catalog advertisement yet; no Liquid Timer; no engine feature PR.
</success_criteria>

<output>
After completion, create `.planning/phases/26-catalog-shell-and-basin-scenes/26-01-SUMMARY.md`
</output>
