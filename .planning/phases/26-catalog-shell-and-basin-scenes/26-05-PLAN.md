---
phase: 26-catalog-shell-and-basin-scenes
plan: "05"
type: execute
wave: 5
depends_on: ["03", "04"]
files_modified:
  - web/e2e/player-helpers.ts
  - web/e2e/player.spec.ts
  - web/e2e/shell.spec.ts
  - web/e2e/reset-honesty.spec.ts
autonomous: true
requirements: [PLAY-02, BASIN-01, BASIN-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 26-2026-09-22T00-02-36
generated_at: 2026-09-22T00:20:00Z
must_haves:
  truths:
    - "Chromium smoke opens Particles and Liquid Timer, plays, pauses, and resets each without requiring pointer gestures or labeled scene controls."
    - "Desktop sidebar shows eight Static preview captions and eight aria-hidden SVGs; .catalog-card count stays 0."
    - "Original six interactive scenes still open, accept one pointer gesture, and activate a labeled control."
  artifacts:
    - path: web/e2e/player.spec.ts
      provides: "Split interactive vs watch-first player coverage"
    - path: web/e2e/player-helpers.ts
      provides: "Eight SCENE_HASH_PATHS + raised all-scene timeout"
    - path: web/e2e/shell.spec.ts
      provides: "Eight static preview desktop asserts"
  key_links:
    - from: web/e2e/player.spec.ts
      to: web/src/catalog/scenes.ts
      via: "controls.length === 0 watch-first branch"
      pattern: "controls\\.length|WATCH_FIRST|POINTER_CONTROL"
    - from: just web-player-smoke
      to: web/e2e/*
      via: "bun scripts/web-build.ts player-smoke"
      pattern: "web-player-smoke"
---

<objective>
Extend Chromium `just web-player-smoke` so eight scenes prove open/play/pause/reset while watch-first basins skip pointer/control requirements.

Purpose: Close PLAY-02 and provide browser evidence for BASIN-01/BASIN-02 under D-14 without Firefox/Safari, Linux qualification, or Pages redeploy.
Output: Updated Playwright helpers/specs and a green player-smoke gate.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/verification.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-CONTEXT.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-RESEARCH.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-UI-SPEC.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-03-SUMMARY.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-04-SUMMARY.md
@web/e2e/player.spec.ts
@web/e2e/player-helpers.ts
@web/e2e/shell.spec.ts

<interfaces>
Current breakage if left unchanged:

```ts
const POINTER_CONTROL: Readonly<Record<SceneId, { gesture; control }>> = { /* six keys only */ };
// iterating SCENE_IDS will fail typecheck / runtime for particles & liquid-timer
```

Required helper updates:

```ts
export const SCENE_HASH_PATHS: Readonly<Record<SceneId, string>> = {
  // existing six…
  particles: "/liquidfun-rs/#/scene/particles",
  "liquid-timer": "/liquidfun-rs/#/scene/liquid-timer",
};

// Rename SIX_SCENE_TIMEOUT_MS → ALL_SCENE_TIMEOUT_MS (or keep export alias) and raise budget if needed (e.g. 160_000) for eight open/reset loops.
```

Watch-first split (research recommendation): prefer `scene.controls.length === 0` so Phase 27 material scenes reuse the rule. For those scenes assert:
1. `expectReadySceneChrome(page, scene.title)`
2. step index advances while Playing
3. Pause → status `Paused` and step frozen
4. Play → `Playing` and step advances
5. `resetNearZero(page)`
6. Credits visible (`Scene source` / `View scene source`)
7. Do NOT call `performSceneGesture` or `activateLabeledControl`
8. Assert no labeled scene control from the old POINTER_CONTROL set is required

Interactive six: keep existing gesture+control loop via a `Partial<Record<…>>` or filter `SCENE_IDS.filter(id => SCENES.find…controls.length > 0)`.

Shell: change test name/asserts from six → eight `Static preview` and eight `svg[aria-hidden='true']` inside `.demo-sidebar`. Keep `.catalog-card` `toHaveCount(0)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend helpers and split watch-first player e2e</name>
  <files>web/e2e/player-helpers.ts, web/e2e/player.spec.ts, web/e2e/reset-honesty.spec.ts</files>
  <read_first>
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-CONTEXT.md` D-04, D-05, D-06, D-14
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-RESEARCH.md` Pitfall 1, Test/smoke seams, Open Question 2
    1. `web/e2e/player.spec.ts` pointer loop test
    1. `web/e2e/player-helpers.ts` `SCENE_HASH_PATHS`, `SIX_SCENE_TIMEOUT_MS`
    1. `web/e2e/reset-honesty.spec.ts` timeout imports
  </read_first>
  <action>
    Per D-14/D-04/D-05/D-06: add `particles` and `liquid-timer` to `SCENE_HASH_PATHS`. Rename `SIX_SCENE_TIMEOUT_MS` to `ALL_SCENE_TIMEOUT_MS` (keep a deprecated alias export if other files import the old name, and update call sites). Raise timeout enough for eight open/reset loops (start at `160_000` unless local smoke proves `120_000` still passes). Change `POINTER_CONTROL` so it is not a full `Record&lt;SceneId, …&gt;` — use a map covering only interactive scenes, or filter by `controls.length &gt; 0`. Add/adjust a Playwright test that for every scene with `controls.length === 0` proves play/pause/reset + credits without gestures. Keep the interactive six-scene gesture+control test. Ensure “opens each native scene…” still iterates `SCENES` (now eight) under the raised timeout. Do not add Firefox/WebKit projects. Do not deploy Pages.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'particles:|/scene/particles|liquid-timer' web/e2e/player-helpers.ts` matches both hash paths.
    1. `rg -n 'controls\.length === 0|controls\.length &gt; 0' web/e2e/player.spec.ts` matches the watch-first split.
    1. `rg -n 'ALL_SCENE_TIMEOUT_MS|SIX_SCENE_TIMEOUT_MS' web/e2e/player-helpers.ts web/e2e/player.spec.ts web/e2e/reset-honesty.spec.ts` shows a coherent timeout name used by all-scene loops.
    1. `rg -n 'Record&lt;SceneId' web/e2e/player.spec.ts` does not force Particles/Liquid Timer into POINTER_CONTROL.
    1. `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Player e2e compiles with eight hash paths and a watch-first play/pause/reset path.</done>
</task>

<task type="auto">
  <name>Task 2: Eight-preview shell asserts and run web-player-smoke</name>
  <files>web/e2e/shell.spec.ts</files>
  <read_first>
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-UI-SPEC.md` Narrow-Viewport Acceptance + Focused Browser Evidence
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-CONTEXT.md` D-03, D-14
    1. `web/e2e/shell.spec.ts` “shows six static previews…”
    1. `justfile` `web-player-smoke` recipe
  </read_first>
  <action>
    Per D-03/D-14/UI-SPEC: rename/update the desktop sidebar test to expect **eight** exact `Static preview` texts and **eight** `svg[aria-hidden='true']` inside `.demo-sidebar`. Keep `.catalog-card` count `0`. Keep mobile dialog assert that at least one preview + caption is visible. Then run the phase gate `just web-player-smoke` (or `bun scripts/web-build.ts player-smoke` if just unavailable) and fix only Phase-26-related failures (timeouts, missing paths, watch-first assumptions). Do not cut particle counts or raise `MAX_ADVANCE_STEPS` to make smoke smoother. Do not add screenshot-hash oracles.
  </action>
  <verify>
    <automated>just web-player-smoke</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'toHaveCount\(8\)' web/e2e/shell.spec.ts` matches Static preview and svg counts (or equivalent eight asserts).
    1. `rg -n 'toHaveCount\(6\)' web/e2e/shell.spec.ts` is empty for preview counts.
    1. `rg -n 'catalog-card' web/e2e/shell.spec.ts` still asserts `toHaveCount(0)`.
    1. `just web-player-smoke` exits 0.
    1. `rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs` still matches.
  </acceptance_criteria>
  <done>Chromium smoke proves eight-scene catalog chrome and watch-first basin play/pause/reset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hash route → WASM session | Allowlisted SceneId only. |
| Playwright → local static server | No production deploy; no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05-01 | T | Hash scene id | mitigate | Unknown hashes still hit fallback; smoke keeps unknown-scene regression. |
| T-26-05-02 | D | Catch-up under hidden tab | mitigate | Existing max-four proof remains; do not raise cap. |
| T-26-05-03 | I | Preview honesty | mitigate | Shell asserts `Static preview` captions; no live thumbnail paths. |
| T-26-05-04 | E | Watch-first e2e | mitigate | Scenes with empty controls never require pointer mutation to pass. |
</threat_model>

<verification>
`just web-player-smoke` exits 0. Independent AI review (D-15) is eligibility only — the implementing agent must not self-approve the phase.
</verification>

<success_criteria>
1. PLAY-02 proven in Chromium for Particles and Liquid Timer.
1. BASIN-01/BASIN-02 openable and runnable with play/pause/reset.
1. Six original interactive scenes still pass gesture+control coverage.
1. No `.catalog-card`; eight static previews in the desktop sidebar.
</success_criteria>

<output>
After completion, create `.planning/phases/26-catalog-shell-and-basin-scenes/26-05-SUMMARY.md`
</output>
