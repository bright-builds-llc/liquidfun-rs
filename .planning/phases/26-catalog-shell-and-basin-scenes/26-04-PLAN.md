---
phase: 26-catalog-shell-and-basin-scenes
plan: "04"
type: tdd
wave: 4
depends_on: ["03"]
files_modified:
  - web/tests/scenes.test.ts
  - web/tests/navigation.test.ts
  - web/scripts/demo-media/model.ts
  - web/tests/demo-media-model.test.ts
autonomous: true
requirements: [PLAY-02, PLAY-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 26-2026-09-22T00-02-36
generated_at: 2026-09-22T00:20:00Z
must_haves:
  truths:
    - "Vitest locks eight SCENE_IDS in order with UI-SPEC descriptions, watch-first hints, empty controls, and pinned credit URLs."
    - "SCENE_CAPTURE_PLANS length and order match SCENE_IDS including particles and liquid-timer."
  artifacts:
    - path: web/tests/scenes.test.ts
      provides: "Eight-scene catalog contract tests"
    - path: web/scripts/demo-media/model.ts
      provides: "Capture plans for particles and liquid-timer"
  key_links:
    - from: web/tests/scenes.test.ts
      to: web/src/catalog/scenes.ts
      via: "UI_SPEC_DESCRIPTIONS / credit href asserts"
      pattern: "particles|liquid-timer"
    - from: web/scripts/demo-media/model.ts
      to: web/src/catalog/scenes.ts
      via: "assertSceneCapturePlanCoverage"
      pattern: "SCENE_CAPTURE_PLANS"
---

<objective>
Lock the eight-scene catalog contract in Vitest and keep demo-media capture plans synchronized with SCENE_IDS.

Purpose: Prevent the six-scene hardcoding pitfall and keep PLAY-02/PLAY-03 metadata regressions cheap to catch before Chromium smoke (research Pitfall 5).
Output: Updated unit tests and watch-first capture plan stubs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-UI-SPEC.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-RESEARCH.md
@.planning/phases/26-catalog-shell-and-basin-scenes/26-03-SUMMARY.md
@web/src/catalog/scenes.ts
@web/tests/scenes.test.ts
@web/scripts/demo-media/model.ts

<interfaces>
From Plan 03 / UI-SPEC — tests must assert:

```ts
SCENE_IDS === [
  "dam-break","fountain","float-or-sink","color-mixer",
  "jelly-drop","water-wheel","particles","liquid-timer",
]
```

Watch-first hint exact string for both new scenes:
`This scene is watch-first. Use Play scene, Pause scene, and Reset scene.`

Pinned commit in inspiration hrefs:
`7f20402173fd143a3988c921bc384459c6a858f2`

Capture plans for watch-first scenes: append entries with `id`/`title`/`route` matching catalog; use `action: { kind: "click", point: { x: 0.5, y: 0.5 } }` (pointer is no-op in WASM — capture script still needs a SceneAction). Keep `interactionStep: 180`.
</interfaces>
</context>

<feature>
  <name>Eight-scene catalog unit contract</name>
  <files>web/tests/scenes.test.ts, web/scripts/demo-media/model.ts</files>
  <behavior>
    - lists eight locked scenes in approved order
    - descriptions/hints match UI-SPEC for particles and liquid-timer
    - controls length 0 for both
    - inspiration hrefs include pinned Particles.js/h and LiquidTimer.js/h
    - implementation paths end with particles.rs / liquid_timer.rs and never google/liquidfun
    - SCENE_CAPTURE_PLANS covers all SCENE_IDS in order
  </behavior>
  <implementation>
    Update Vitest tables and capture plans only. No Playwright in this plan.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand scenes.test.ts to eight scenes and credits</name>
  <files>web/tests/scenes.test.ts, web/tests/navigation.test.ts</files>
  <read_first>
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-UI-SPEC.md` catalog descriptions, hints, credits
    1. `.planning/phases/26-catalog-shell-and-basin-scenes/26-RESEARCH.md` Pitfall 5
    1. `web/tests/scenes.test.ts`
    1. `web/src/catalog/scenes.ts`
    1. `web/tests/navigation.test.ts`
  </read_first>
  <behavior>
    1. Test title/order length expects 8, not 6.
    1. `UI_SPEC_DESCRIPTIONS` / `UI_SPEC_HINTS` Records include `particles` and `liquid-timer` with exact UI-SPEC strings.
    1. Assert both new scenes have `controls.length === 0`.
    1. Assert inspiration labels/hrefs for the four pinned files at the pinned commit.
    1. Assert implementation paths are `crates/liquidfun-wasm/src/scene/particles.rs` and `.../liquid_timer.rs`.
    1. Assert no implementation href/path contains `google/liquidfun` as the running implementation path.
  </behavior>
  <action>
    Per PLAY-03 and D-07/D-08: rewrite the “lists six locked scenes” test to “lists eight locked scenes…” with the full expected id array. Extend `UI_SPEC_DESCRIPTIONS` and `UI_SPEC_HINTS` Records (TypeScript exhaustiveness requires both new keys). Add focused asserts for empty controls and pinned credit URLs. Keep existing six-scene string expectations unchanged. Update any hard-coded length `6` in `navigation.test.ts` only if present. Prefer Arrange/Act/Assert, one concern per test where practical.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/scenes.test.ts tests/navigation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'lists eight locked scenes|toHaveLength\(8\)' web/tests/scenes.test.ts` matches.
    1. `rg -n 'particles:|\"particles\"' web/tests/scenes.test.ts` matches description/hint tables.
    1. `rg -n 'liquid-timer' web/tests/scenes.test.ts` matches.
    1. `rg -n 'lists six locked scenes|toHaveLength\(6\)' web/tests/scenes.test.ts` is empty.
    1. `cd web && bun run test:unit -- tests/scenes.test.ts tests/navigation.test.ts` exits 0.
  </acceptance_criteria>
  <done>Unit tests lock eight-scene catalog metadata and credits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sync SCENE_CAPTURE_PLANS with eight SCENE_IDS</name>
  <files>web/scripts/demo-media/model.ts, web/tests/demo-media-model.test.ts</files>
  <read_first>
    1. `web/scripts/demo-media/model.ts` `SCENE_CAPTURE_PLANS` + `assertSceneCapturePlanCoverage`
    1. `web/tests/demo-media-model.test.ts`
    1. `web/src/catalog/scenes.ts` `SCENE_IDS`
  </read_first>
  <behavior>
    1. `SCENE_CAPTURE_PLANS` appends Particles and Liquid Timer after Water Wheel in catalog order.
    1. `assertSceneCapturePlanCoverage` does not throw on import.
    1. Existing demo-media-model unit test still passes with eight ids.
  </behavior>
  <action>
    Append two `SceneCapturePlan` objects after `water-wheel`:
    - `{ id: "particles", title: "Particles", route: "/liquidfun-rs/#/scene/particles", interactionStep: 180, action: { kind: "click", point: { x: 0.5, y: 0.5 } } }`
    - `{ id: "liquid-timer", title: "Liquid Timer", route: "/liquidfun-rs/#/scene/liquid-timer", interactionStep: 180, action: { kind: "click", point: { x: 0.5, y: 0.5 } } }`
    Do not change capture profile timing constants. Do not require the click to mutate physics (watch-first no-op is fine). Update `demo-media-model.test.ts` expectations if they hard-code six.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/demo-media-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'id: \"particles\"|id: \"liquid-timer\"' web/scripts/demo-media/model.ts` matches both.
    1. `rg -n 'SCENE_IDS\.length' web/scripts/demo-media/model.ts` still used by coverage assert.
    1. `cd web && bun run test:unit -- tests/demo-media-model.test.ts` exits 0.
  </acceptance_criteria>
  <done>Capture-plan allowlist matches the eight-scene catalog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit tests → catalog honesty | Tests are the regression gate for credit and copy claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | I | Credit URL asserts | mitigate | Vitest requires pinned commit blob paths for JS+C++ inspiration. |
| T-26-04-02 | S | Implementation path asserts | mitigate | Reject google/liquidfun as implementationPath. |
| T-26-04-03 | T | Capture plan sync | mitigate | `assertSceneCapturePlanCoverage` fails import if lengths diverge. |
</threat_model>

<verification>
`cd web && bun run test:unit -- tests/scenes.test.ts tests/navigation.test.ts tests/demo-media-model.test.ts` exits 0.
</verification>

<success_criteria>
1. Catalog unit contract is eight scenes with PLAY-03 credits.
1. Capture plans cannot drift from SCENE_IDS.
</success_criteria>

<output>
After completion, create `.planning/phases/26-catalog-shell-and-basin-scenes/26-04-SUMMARY.md`
</output>
