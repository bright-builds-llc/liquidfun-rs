---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun/src/particle.rs
  - crates/liquidfun/src/particle/group.rs
autonomous: true
requirements: [PART-09, PART-10, TEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Consumers can construct exactly one owned filled-shape, stroke-shape, or explicit-position recipe and choose New or AppendTo(ParticleGroupId) separately."
    - "Public group flags expose SOLID, RIGID, and CAN_BE_EMPTY only; internal lifecycle/depth bits are not constructible or inspectable."
  artifacts:
    - path: crates/liquidfun/src/particle/group.rs
      provides: "Invariant-bearing group recipes, destination, public flags, view contract, and typed errors"
      contains: "pub enum ParticleGroupSource"
    - path: crates/liquidfun/src/particle.rs
      provides: "Curated public particle-group exports"
      contains: "mod group"
  key_links:
    - from: crates/liquidfun/src/particle.rs
      to: crates/liquidfun/src/particle/group.rs
      via: "private module plus curated pub use"
      pattern: "mod group|pub use group"
---

<objective>
Define the complete safe public particle-group contract before storage or world mutation code consumes it.

Purpose: Make D-01 through D-04 and D-24 illegal-state constraints explicit in types instead of reproducing the nullable C++ definition.
Output: Documented owned recipes, separate destination, exact public group flags, group view shape, and typed validation errors.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-CONTEXT.md
@crates/liquidfun/src/particle.rs
@crates/liquidfun/src/particle/definition.rs
@crates/liquidfun/src/particle/view.rs
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleGroup.h
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp

<interfaces>
Existing identity: `ParticleGroupId` is an opaque world-scoped handle.
Required source variants: `FilledShapes`, `StrokeShape`, `Positions`.
Required destination variants: `New`, `AppendTo(ParticleGroupId)`.
Pinned public group bits: SOLID=0x0001, RIGID=0x0002, CAN_BE_EMPTY=0x0004.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode invariant-bearing recipes and group flags</name>
  <files>crates/liquidfun/src/particle/group.rs, crates/liquidfun/src/particle.rs</files>
  <read-first>crates/liquidfun/src/particle.rs — particle module exports; crates/liquidfun/src/particle/definition.rs — `ParticleDef`, `ParticleFlags`, and checked definition builders; crates/liquidfun/src/particle/view.rs — borrow-scoped particle view conventions; crates/liquidfun/src/identity.rs — stable semantic identity types; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleGroup.h — lines 35-169, `b2ParticleGroupFlag` and `b2ParticleGroupDef`; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — lines 930-1110, `CreateParticleGroup`; third_party/liquidfun/liquidfun/Box2D/Box2D/Documentation/Programmers-Guide/Chapter11_Particles.md — "Particle Groups" and group-creation sections.</read-first>
  <behavior>
    - "Exactly one of filled union, stroke, or explicit positions is representable."
    - "Destination is independent and AppendTo carries one checked ParticleGroupId."
    - "Stride is finite and positive; strength is finite and non-negative; transform/velocities/lifetime are validated with existing math/lifetime types."
    - "Unknown public group bits are retained only if the existing particle-flag policy requires it; WILL_BE_DESTROYED and NEEDS_UPDATE_DEPTH never enter ParticleGroupFlags."
  </behavior>
  <action>Create `particle/group.rs` using `foo.rs` plus child-directory conventions. Define owned `ParticleGroupSource` variants for a non-empty filled-shape union, one stroke edge/chain source, and non-empty explicit positions; define `ParticleGroupDestination::{New, AppendTo(ParticleGroupId)}`; define `ParticleGroupRecipe<UserAssociation=()>` carrying particle flags, `ParticleGroupFlags`, transform, linear velocity, angular velocity, color, strength, optional stride, lifetime, and owned user association per D-01/D-03. Use fallible constructors/builders and `ParticleGroupRecipeError` variants for empty sources, unsupported shapes, non-finite values, non-positive stride, and invalid strength. Define `ParticleGroupFlags` with exact bits `SOLID=0x0001`, `RIGID=0x0002`, `CAN_BE_EMPTY=0x0004`; keep the upstream internal bits private and absent from the public type per D-04. Add focused Arrange/Act/Assert tests for each valid source and each invalid combination. Export only the curated public types from `particle.rs`. Do not add a C++-shaped struct with nullable shape pointers, borrowed buffers, public dense indices, or raw handles. Before the task commit run, in order, `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::group && cargo doc -p liquidfun --all-features --no-deps</automated></verify>
  <acceptance-criteria>`group.rs` contains `ParticleGroupSource`, `ParticleGroupDestination`, `ParticleGroupRecipe`, `ParticleGroupFlags`, and `ParticleGroupRecipeError`; the public flags contain exactly 0x0001/0x0002/0x0004; `rg 'will.*destroy|needs.*depth' crates/liquidfun/src/particle/group.rs` finds no public flag; focused tests and rustdoc exit 0.</acceptance-criteria>
  <done>The public recipe surface expresses every D-01/D-03 input and no contradictory or internal state.</done>
</task>

<task type="auto">
  <name>Task 2: Define the borrow-scoped group view contract</name>
  <files>crates/liquidfun/src/particle/group.rs</files>
  <read-first>crates/liquidfun/src/particle/group.rs — Task 1 `ParticleGroupRecipe`, `ParticleGroupSource`, destination, and flag contracts; crates/liquidfun/src/particle/view.rs — borrow-scoped view and semantic-ID conventions; crates/liquidfun/src/world/particle_object.rs — world-facing particle-system access patterns; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleGroup.h — lines 171-360, `b2ParticleGroup` public/private contract; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleGroup.cpp — `b2ParticleGroup::UpdateStatistics` and group accessors.</read-first>
  <behavior>
    - "The view exposes stable group ID, public flags, transform, center, linear/angular velocity, mass, inertia, member count, stable member IDs, and optional aligned depth."
    - "The view borrows storage and exposes neither mutable slices nor dense row coordinates."
    - "A retained empty group reports zero members and exact zero aggregate state."
  </behavior>
  <action>Define `ParticleGroupView<'a>` and its documented read-only accessors for every D-04 field. Member IDs must be a borrow-scoped stable-ID slice/iterator in source order; depth must be absent when no depth lane applies and otherwise aligned with members without exposing row indices. Keep construction crate-private so Plan 10-09 can build it only after same-world/same-system validation. Add compile-time/API unit tests proving returned member/depth borrows cannot outlive the world borrow and that no mutable or dense-index accessor exists. Before the task commit run the exact ordered Rust gate from Task 1.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::group && cargo test -p liquidfun --doc</automated></verify>
  <acceptance-criteria>`ParticleGroupView<'a>` has accessors for all twelve D-04 observation categories; `rg 'pub fn .*index|pub fn .*row|pub fn .*mut' crates/liquidfun/src/particle/group.rs` returns no dense/mutable view accessor; focused tests and doctests exit 0.</acceptance-criteria>
  <done>The group inspection contract is complete, stable-ID based, and borrow-scoped before its storage constructor is implemented.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Consumer recipe to domain value | Untrusted shapes, floats, counts, handles, and source combinations enter the library. |
| Internal storage to public view | Private dense indices, cache bits, and mutable lanes must not escape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-01-01 | Tampering | `ParticleGroupRecipe` | mitigate | Sum types make contradictory sources/destinations unrepresentable; fallible constructors reject non-finite and invalid scalar values. |
| T-10-01-02 | Elevation of privilege | `ParticleGroupFlags` | mitigate | Export only SOLID/RIGID/CAN_BE_EMPTY; internal destruction/depth bits remain private. |
| T-10-01-03 | Information disclosure | `ParticleGroupView` | mitigate | Expose stable semantic IDs and owned/copy values only; no row, pointer, allocation, or cache representation. |
| T-10-01-04 | Denial of service | recipe collections | mitigate | Constructors reject empty/oversized collections against existing i32-derived bounds before later sampling. |
| T-10-01-05 | Tampering | unsafe/FFI boundary | accept | This plan adds no unsafe or FFI; tests assert the published Rust surface remains safe and Cargo-only. |
</threat-model>

<verification>
Run focused group tests, rustdoc, and the exact ordered repository Rust gate. Review the public export diff for raw pointers, internal flags, dense indices, borrowed input buffers, or `unsafe`.
</verification>

<success-criteria>
- All D-01 through D-04 inputs and observations have documented invariant-bearing types.
- Public group bits are exact and internal bits are unrepresentable.
- Every invalid constructor path is typed and effect-free.
- The published crate remains native Rust and Cargo-only.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-01-SUMMARY.md`.</output>
