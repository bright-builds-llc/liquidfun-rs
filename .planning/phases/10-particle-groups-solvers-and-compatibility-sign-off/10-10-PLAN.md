---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "10"
type: execute
wave: 6
depends_on: ["10-09"]
files_modified:
  - crates/liquidfun/src/world/particle_object.rs
  - crates/liquidfun/tests/particle_group_lifecycle.rs
autonomous: true
requirements: [PART-10, TEST-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Destroying group particles marks them through the established zombie lifecycle and defers compaction."
    - "Non-retained groups invalidate after the last member disappears; CAN_BE_EMPTY groups remain valid with exact zero aggregate state."
  artifacts:
    - path: crates/liquidfun/src/world/particle_object.rs
      provides: "Group destruction and empty-retention public workflows"
      contains: "destroy_particle_group_particles"
    - path: crates/liquidfun/tests/particle_group_lifecycle.rs
      provides: "Deferred destruction, callback, invalidation, and retained-empty tests"
  key_links:
    - from: crates/liquidfun/src/world/particle_object.rs
      to: crates/liquidfun/src/world/particle_lifecycle.rs
      via: "existing zombie mark and deferred compaction"
      pattern: "mark.*destruction|compact"
---

<objective>
Complete group destruction and can-be-empty lifecycle semantics.

Purpose: Reuse Phase 9's single zombie authority while making group identity invalidation source-equivalent.
Output: Public group-particle destruction, deferred removal, retained-empty state, and lifecycle tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/world/particle_object.rs
@crates/liquidfun/src/world/particle_lifecycle.rs
@crates/liquidfun/src/particle/storage.rs
@third_party/liquidfun/liquidfun/Box2D/Unittests/Function/FunctionTests.cpp
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route group destruction through zombie compaction and empty retention</name>
  <files>crates/liquidfun/src/world/particle_object.rs, crates/liquidfun/tests/particle_group_lifecycle.rs</files>
  <read-first>crates/liquidfun/src/world/particle_object.rs — particle and group destruction entry points; crates/liquidfun/src/world/particle_lifecycle.rs — O01 lifetime and O02 zombie-compaction behavior; crates/liquidfun/src/particle/storage/group.rs — storage-owned group records and membership ranges; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `DestroyParticles` and `SolveZombie` group teardown; third_party/liquidfun/liquidfun/Box2D/Unittests/Function/FunctionTests.cpp — `CreateEmptyParticleGroupWithNoShape` and `DestroyParticleGroup`.</read-first>
  <behavior>
    - "Members become PendingDelete/ZOMBIE in ascending dense order and listener occurrences remain source-ordered."
    - "Group remains inspectable until compaction removes its final member."
    - "After compaction, ordinary group is stale; CAN_BE_EMPTY group remains live with zero count/mass/inertia/center/velocities and an empty depth slice."
  </behavior>
  <action>Per D-08 and D-24, add `destroy_particle_group_particles(group, call_listener)` using the existing authoritative per-particle marking transition; validate the group before effects and preflight the complete lifecycle journal. Let O02 compaction decide final membership, then invalidate the group shell and association only after a non-retained range becomes empty. Retain CAN_BE_EMPTY records indefinitely until explicit group-shell destruction, with exact zero aggregate cache and no fabricated callback. Add tests for pre-step pending state, ascending callbacks, post-step invalidation, retained zero state, joining into an empty retained target, locked/poisoned/wrong-world failures, and exactly-once events. Run the exact ordered Rust gate before commit.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features --test particle_group_lifecycle</automated></verify>
  <acceptance-criteria>Tests prove deferred rather than immediate member removal; ordinary group becomes stale only after compaction; CAN_BE_EMPTY remains valid/zero; callbacks are exact and nonduplicated; failure snapshots are unchanged.</acceptance-criteria>
  <done>Group destruction has one lifecycle authority and exact retained-empty behavior.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Group destruction call to particle lifecycle | One call fans out to many identity and callback transitions. |
| Zombie compaction to group shell invalidation | Incorrect timing can leave stale live handles or prematurely invalidate retained groups. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-10-01 | Spoofing | group handle | mitigate | Validate scoped identity and owner before enumerating members. |
| T-10-10-02 | Tampering | mass destruction | mitigate | Preflight journal and mark through the existing single particle transition in source order. |
| T-10-10-03 | Denial of service | group member fanout | mitigate | Group range/count is bounded; journal capacity is checked before effects. |
| T-10-10-04 | Repudiation | callbacks/invalidation | mitigate | Owned exact occurrences and before/after handle tests prove timing/multiplicity. |
| T-10-10-05 | Tampering | panic/rollback | mitigate | Candidate world/storage/journal commits together; poison blocks later coherent operations. |
| T-10-10-06 | Tampering | unsafe/FFI boundary | accept | No unsafe/FFI; upstream is test oracle only. |
</threat-model>

<verification>
Run group lifecycle plus Phase 9 zombie/listener tests and ordered Rust gate.
</verification>

<success-criteria>
- Group member destruction is deferred and source-ordered.
- Empty retention exactly follows CAN_BE_EMPTY.
- Handles, associations, caches, and events transition atomically.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-10-SUMMARY.md`.</output>
