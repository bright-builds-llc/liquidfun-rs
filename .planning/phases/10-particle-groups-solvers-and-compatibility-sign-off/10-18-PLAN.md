---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "18"
type: execute
wave: 6
depends_on: ["10-02", "10-03", "10-04", "10-05", "10-06", "10-12", "10-15"]
files_modified:
  - crates/liquidfun/src/particle/solver/material.rs
autonomous: true
requirements: [PART-12, PART-13, TEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Viscous, repulsive, powder, tensile, solid, and color-mixing behaviors have independent observable effects."
    - "Every material solver uses exact coefficients, flag combinations, topology inputs, and deterministic contact order."
  artifacts:
    - path: crates/liquidfun/src/particle/solver/material.rs
      provides: "S07-S12 material kernels"
    - path: crates/liquidfun/src/particle/solver/material.rs
      provides: "Crate-private material kernels with co-located control/activation/interaction unit witnesses"
  key_links:
    - from: crates/liquidfun/src/particle/solver/material.rs
      to: crates/liquidfun/src/particle/definition.rs
      via: "validated exact material coefficients"
      pattern: "viscous_strength|repulsive_strength|powder_strength|surface_tension|color_mixing"
---

<objective>
Implement all contact-driven particle material solvers.

Purpose: Deliver the visible LiquidFun material behaviors that distinguish flags without conflating their formulas or gates.
Output: Exact S07-S12 kernels and independent material evidence.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/solver/manifest.rs
@crates/liquidfun/src/particle/definition.rs
@crates/liquidfun/src/particle/storage.rs
@crates/liquidfun/src/particle/topology.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement exact material interactions</name>
  <files>crates/liquidfun/src/particle/solver/material.rs</files>
  <read-first>.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-RESEARCH.md — exact S07-S12 formulas, constants, and source anchors; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `SolveViscous`, `SolveRepulsive`, `SolvePowder`, `SolveTensile`, `SolveSolid`, and `SolveColorMixing`; crates/liquidfun/src/particle/storage/lanes.rs — Phase 9 particle/body contact records and complete triad/depth lanes; crates/liquidfun/src/particle/group.rs — public/internal particle and group flag mapping; crates/liquidfun/src/particle/definition.rs — Plan 10-02 checked coefficient contracts.</read-first>
  <behavior>
    - "S07 viscous, S08 repulsive, S09 powder, S10 tensile, S11 solid, and S12 color_mixing each have a control scenario and an activation scenario."
    - "Mixed-flag scenarios prove required/suppressing flags and exact pass interaction order."
  </behavior>
  <action>Per D-19 through D-21, D-23, and D-28, implement one crate-private safe-Rust kernel per S07-S12 using upstream operation grouping and `f32` ordering. Use contact weights/normals and group/depth/topology records from ParticleStorage, exact defaults (viscous .25, repulsive 1.0, powder .5, tensile pressure/normal .2, color mixing .5), and the precise public/internal flag gates. Preserve incumbent/contact/dense ordering, saturating color-channel behavior where upstream does, and equal/opposite velocity changes where required. Do not fuse formulas merely because they traverse the same contacts. Place no-flag control, single-flag activation, coefficient-zero suppression, mixed-flag interaction, and deterministic repeat witnesses for every pass in a co-located `#[cfg(test)]` module; do not widen kernel visibility for an integration test.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::solver::material</automated></verify>
  <acceptance-criteria>S07-S12 each have control/activation/interaction evidence; exact locked defaults are exercised; gates and suppression match upstream; deterministic repeats match bit-for-bit where the phase policy requires.</acceptance-criteria>
  <done>All six material flags cause their exact bounded, deterministic LiquidFun interactions.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Flags and coefficients to material kernels | Public configuration controls high-frequency numerical mutation. |
| Contact/topology records to velocity/color lanes | Derived indices select mutation targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-18-01 | Tampering | flags/coefficients | mitigate | Closed flag masks and finite/range validation at definition construction. |
| T-10-18-02 | Tampering | contact indices | mitigate | Generate records from validated storage rows and bounds-check before kernel use. |
| T-10-18-03 | Denial of service | material loops | mitigate | Single bounded traversals over existing contacts/triads; no nested global search. |
| T-10-18-04 | Repudiation | formula activation | mitigate | Separate named control/activation/interaction witnesses for S07-S12. |
| T-10-18-05 | Tampering | panic/rollback | mitigate | Infallible validated kernels; any precondition failure occurs before lane writes. |
| T-10-18-06 | Information disclosure | semantic views | accept | No secrets or raw storage addresses leave the crate. |
| T-10-18-07 | Tampering | unsafe/FFI boundary | accept | Native safe Rust only. |
</threat-model>

<verification>
Run focused material tests and compare their pass labels/default values against the manifest and research table.
</verification>

<success-criteria>
- Every material pass is separately testable and independently gated.
- Exact coefficients replace literals.
- No pass changes state when its activation condition is absent.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-18-SUMMARY.md`.</output>
