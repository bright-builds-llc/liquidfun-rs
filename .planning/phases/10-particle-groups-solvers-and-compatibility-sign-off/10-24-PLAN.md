---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "24"
type: execute
wave: 9
depends_on: ["10-23"]
files_modified:
  - crates/liquidfun-test-protocol/src/scenario/rigid_world.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/phase10.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase10.rs
  - crates/liquidfun-test-protocol/src/schema/rigid_world.rs
  - crates/liquidfun-test-protocol/src/schema/rigid_world/phase10.rs
  - crates/liquidfun-differential/tests/phase10_protocol.rs
autonomous: true
requirements: [PART-09, PART-10, PART-11, PART-12, PART-13, PART-18, TEST-01, TEST-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "One versioned rigid-world schema expresses every Phase 10 setup/action and semantic result needed by both engines."
    - "Unknown fields/tags, duplicate semantic IDs, invalid ownership, non-finite values, and resource excess fail closed."
    - "The schema extends rather than forks the Phase 9 rigid-world harness."
  artifacts:
    - path: crates/liquidfun-test-protocol/src/scenario/rigid_world/phase10.rs
      provides: "Phase 10 request operations and bounds"
    - path: crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase10.rs
      provides: "Groups, topology, typed semantic witness roles, and solver outcomes"
    - path: crates/liquidfun-test-protocol/src/schema/rigid_world/phase10.rs
      provides: "Strict wire validation and canonicalization"
  key_links:
    - from: crates/liquidfun-test-protocol/src/schema/rigid_world/phase10.rs
      to: crates/liquidfun-test-protocol/src/scenario/rigid_world/phase10.rs
      via: "strict decode into shared semantic model"
      pattern: "Phase10"
---

<objective>
Extend the rigid-world differential protocol for all Phase 10 semantics.

Purpose: Give Rust and C++ one strict, bounded, semantic request/result contract before adapters are implemented.
Output: Versioned Phase 10 scenario/result types, strict schema, and malformed/canonical round-trip tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun-test-protocol/src/scenario/rigid_world.rs
@crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs
@crates/liquidfun-test-protocol/src/schema/rigid_world.rs
@crates/liquidfun-differential/tests/particle_protocol.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shared Phase 10 semantic request and result contracts</name>
  <files>crates/liquidfun-test-protocol/src/scenario/rigid_world.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/phase10.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase10.rs, crates/liquidfun-differential/tests/phase10_protocol.rs</files>
  <read-first>crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs — Phase 9 request/result envelopes, operations, semantic IDs, and bounds; crates/liquidfun/src/particle/group.rs — public group recipes and flags; crates/liquidfun/src/world/particle_object.rs — public group mutation API; crates/liquidfun/src/particle/view.rs — full pair/triad semantic views; crates/liquidfun/tests/particle_solver_flags.rs — solver semantic pass/flag witness inventory from Plan 10-23; crates/liquidfun-test-protocol/src/schema.rs — protocol versioning and strict envelope conventions; crates/liquidfun-test-protocol/src/float_bits.rs — exact-f32-bit wire conventions.</read-first>
  <behavior>
    - "Requests cover fill/stroke/explicit groups, append/join/split/flags/destroy/step, exact definitions, and deterministic seed/provenance."
    - "Results cover stable group/member IDs/order, group flags/transforms, particles, pairs/full triads, contacts/events, bodies/joints, typed control/activation/interaction witness roles, and semantic outcomes."
  </behavior>
  <action>Add nested Phase 10 extensions to the existing rigid-world semantic types instead of a parallel harness. Use explicit tagged operations, stable semantic IDs, exact `f32` bit fields, closed public flags, typed `WitnessRole::{Control, Activation, Interaction}`, semantic behavior-leaf IDs, and named version constants. Include all state needed for exact identity/order/topology and semantic outcome witnesses, while excluding addresses, raw memory, internal dense rows, private `PassId`, pass admission traces, and pass inventory. D-27 exact pass order/multiplicity remains solely in native co-located tests; the shared protocol carries only behavior leaf plus witness role plus semantic observations. Define explicit maxima for operations, groups, particles, topology, contacts/events, witnesses, steps, and text; add constructors/validators that reject non-finite semantic values where bits are not intentionally arbitrary.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun-differential --all-features --test phase10_protocol semantic</automated></verify>
  <acceptance-criteria>Every Phase 10 public workflow and evidence field is representable; full triads include pa,pb,pc,ka,kb,kc,s; exact IDs/order are explicit; all collections have documented bounds.</acceptance-criteria>
  <done>Both engines can share one complete semantic Phase 10 model.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce strict canonical wire behavior</name>
  <files>crates/liquidfun-test-protocol/src/schema/rigid_world.rs, crates/liquidfun-test-protocol/src/schema/rigid_world/phase10.rs, crates/liquidfun-differential/tests/phase10_protocol.rs</files>
  <read-first>crates/liquidfun-test-protocol/src/schema/rigid_world/phase9.rs — Phase 9 strict JSON field schema and semantic canonicalization; crates/liquidfun-test-protocol/src/codec.rs — strict JSON decode/encode and duplicate/unknown-field rejection; crates/liquidfun-test-protocol/src/failure.rs — protocol error taxonomy; crates/liquidfun-test-protocol/src/limits.rs — request/resource size bounds; crates/liquidfun-test-protocol/src/scenario/rigid_world/phase10.rs — Task 1 request operations and bounds; crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase10.rs — Task 1 semantic result records; crates/liquidfun-test-protocol/src/schema/rigid_world/phase10.rs — Task 1 strict Phase 10 wire validation/canonicalization.</read-first>
  <behavior>
    - "Canonical Phase 10 JSON round-trips byte-identically after normalization."
    - "Unknown tags/fields, duplicate IDs, dangling topology, wrong ownership, invalid ordering, out-of-bound collections, and schema/version mismatch are hard protocol errors."
  </behavior>
  <action>Extend the existing strict schema decoder/encoder with closed Phase 10 variants and canonical ordering rules. Validate global and per-system ID uniqueness, group-member ownership/order, topology referential integrity, exact flag masks, typed semantic behavior-leaf/witness-role bindings, bounds before allocation, and schema/protocol version agreement. Explicitly reject any private pass-ID, pass-trace, or pass-inventory member. Preserve stderr-only diagnostics and stdout record purity. Add positive canonical fixtures plus one focused negative test per malformed class, including boundary-at-limit/one-over-limit and duplicate/unknown records.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun-differential --all-features --test phase10_protocol wire</automated></verify>
  <acceptance-criteria>Canonical round trips are exact; every malformed class fails with a typed contextual error; decoding preflights sizes; no permissive unknown-field path remains.</acceptance-criteria>
  <done>The Phase 10 protocol is strict, bounded, canonical, and fail-closed.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| JSON Lines bytes to typed scenario/result | Untrusted oracle/corpus data enters Rust allocation and semantic validation. |
| Semantic IDs/ownership to comparator/adapters | Cross-engine identity claims select relationships and comparisons. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-24-01 | Spoofing | semantic IDs/owners | mitigate | Global/per-system uniqueness, referential integrity, and ownership validation. |
| T-10-24-02 | Tampering | unknown/duplicate fields | mitigate | Deny unknown fields/tags and reject duplicates/version mismatches. |
| T-10-24-03 | Denial of service | parser allocation | mitigate | Preflight byte/count/depth bounds before allocation. |
| T-10-24-04 | Repudiation | provenance/version | mitigate | Required protocol/schema/generator/upstream/toolchain fields. |
| T-10-24-05 | Information disclosure | diagnostics | mitigate | Contextual semantic errors only; never echo whole untrusted records or paths with secrets. |
| T-10-24-06 | Tampering | panic/rollback | mitigate | Total decoding returns typed errors; fuzz-style malformed cases prove no panic. |
| T-10-24-07 | Tampering | unsafe/FFI boundary | accept | Rust schema is safe; foreign process communicates only through bounded bytes. |
</threat-model>

<verification>
Run both protocol filters and confirm Phase 9 request/result variants still round-trip unchanged.
</verification>

<success-criteria>
- One strict schema covers all Phase 9 and Phase 10 evidence.
- Malformed/oversized/ambiguous input cannot reach adapters.
- Semantic identity and topology are explicit and canonical.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-24-SUMMARY.md`.</output>
