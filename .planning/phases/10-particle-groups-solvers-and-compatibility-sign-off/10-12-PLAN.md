---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "12"
type: execute
wave: 3
depends_on: ["10-03", "10-05", "10-11"]
files_modified:
  - tools/reference/CMakeLists.txt
  - tools/reference/src/phase10_group_topology_witness.cpp
  - reference/artifacts/phase10/group-topology-witnesses.json
  - reference/artifacts/phase10/group-topology-witnesses.provenance.json
  - crates/liquidfun/src/particle/topology.rs
  - crates/liquidfun/src/particle/topology/constraints.rs
autonomous: true
requirements: [PART-11, TEST-01, TEST-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Pinned-oracle evidence classifies split-created metadata and every listed finite-degenerate topology/solver case before Rust semantics are chosen."
    - "Pair and triad candidates use exact connection gates, source orientation, rest state, minimum strength, stable sort, and first-duplicate retention."
    - "Topology boundaries translate checked stable IDs while solver records retain private dense endpoints."
  artifacts:
    - path: reference/artifacts/phase10/group-topology-witnesses.json
      provides: "Exact pinned split-metadata and finite-degenerate outcomes consumed by topology, split, and constraint plans"
    - path: crates/liquidfun/src/particle/topology/constraints.rs
      provides: "Exact pair/triad generation and append policy"
      contains: "generate_pairs_and_triads"
    - path: crates/liquidfun/src/particle/topology/constraints.rs
      provides: "Private constraint implementation with co-located structural/property unit evidence"
  key_links:
    - from: reference/artifacts/phase10/group-topology-witnesses.json
      to: crates/liquidfun/src/particle/topology/constraints.rs
      via: "case-by-case typed-error versus source-defined behavior decisions"
      pattern: "zero_length_pair|degenerate_triad|barrier_pair|voronoi_no_necessary_generator"
    - from: crates/liquidfun/src/particle/topology/constraints.rs
      to: crates/liquidfun/src/particle/topology/voronoi.rs
      via: "dense-order generators and oriented nodes"
      pattern: "Voronoi"
---

<objective>
Generate complete source-equivalent pair and triad records from contacts and Voronoi nodes after mandatory pinned-oracle decision probes.

Purpose: Close D-12/D-13 and provide preserved rest data to join/reactive/elastic/spring operations.
Output: Provenance-bound split/degenerate oracle outcomes, pure connection filters, exact record calculations, operation-scoped stable deduplication, and structural tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/topology.rs
@crates/liquidfun/src/particle/topology/voronoi.rs
@crates/liquidfun/src/particle/storage/lanes.rs
@tools/reference/CMakeLists.txt
@tools/reference/src/phase9_lifecycle_contact_witness.cpp
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp
</context>

<feature>
  <name>Exact pair/triad topology records</name>
  <files>crates/liquidfun/src/particle/topology.rs, crates/liquidfun/src/particle/topology/constraints.rs</files>
  <behavior>Pairs require spring or barrier and connectivity through wall/spring/elastic or rigid group; emit in contact order, store current distance and minimum endpoint-group strength. Triads require elastic, current dense generator order, distance/group gates, source orientation, pa/pb/pc and ka/kb/kc/s. Append operations use stable lexicographic sort and retain first exact duplicate; preserve/remap operations do not sort.</behavior>
  <implementation>Create typed pure filters/plans over checked private indices and group metadata. Reproduce source expression order for all coefficients and validate finite results. Translate only at semantic views/protocol boundaries.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Probe split metadata and every listed finite-degenerate case</name>
  <files>tools/reference/CMakeLists.txt, tools/reference/src/phase10_group_topology_witness.cpp, reference/artifacts/phase10/group-topology-witnesses.json, reference/artifacts/phase10/group-topology-witnesses.provenance.json</files>
  <read-first>tools/reference/CMakeLists.txt — private witness target and pinned-upstream build pattern; tools/reference/src/phase9_lifecycle_contact_witness.cpp — Phase 9 out-of-process witness/provenance structure; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `CreateParticleGroupsFromParticleList`, `UpdatePairsAndTriads`, `SolveSpring`, `SolveElastic`, `SolveBarrier`, `SolveRigidDamping`, and `SolveRigid`; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2VoronoiDiagram.h — `b2VoronoiDiagram` contract; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2VoronoiDiagram.cpp — `b2VoronoiDiagram::Generate` and `GetNodes`; reference/artifacts/phase9/lifecycle-contact-witnesses.provenance.json — exact Phase 9 oracle provenance fields.</read-first>
  <behavior>
    - "The split witness records later-component public/internal flags, strength, transform, user-data behavior, range/member order, and zero-valued statistics without assuming results in advance."
    - "The finite-degenerate matrix includes zero-length pair, degenerate triad, barrier pair, no-necessary-generator Voronoi input, empty rigid group, and one-particle rigid group."
    - "Each case records exact input bits, exact structural/numeric outcome, finite/non-finite classification, and the resulting Rust decision: preserve source-defined behavior or reject through a named typed invariant."
  </behavior>
  <action>Add the mandatory private out-of-process witness executable `phase10-group-topology-witness` to the existing reference CMake graph. Exercise all seven named cases against pinned upstream revision `7f20402173fd143a3988c921bc384459c6a858f2`; do not infer any expected value from Rust and do not omit a case because source inspection appears sufficient. Write the exact semantic result to `group-topology-witnesses.json` and provenance containing upstream revision, source SHA-256, compiler identity, preset, exact argv, and result SHA-256. Run the witness twice and require byte-identical semantic JSON. Treat probe completion and case-by-case classification as a blocking decision gate: Task 2, Plan 10-14, and Plan 10-20 must consume the recorded outcomes verbatim, using named typed errors only where the artifact classifies rejection and otherwise preserving source-defined finite behavior. Do not add a global epsilon.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo xtask upstream configure --preset oracle-debug && cargo xtask upstream build --preset oracle-debug --target phase10-group-topology-witness && rm -rf target/phase10-group-topology-witness-verify && mkdir -p target/phase10-group-topology-witness-verify/run-1 target/phase10-group-topology-witness-verify/run-2 reference/artifacts/phase10 && WITNESS="$(pwd)/target/reference/oracle-debug/phase10-group-topology-witness" && (cd target/phase10-group-topology-witness-verify/run-1 && "$WITNESS" --output group-topology-witnesses.json --provenance group-topology-witnesses.provenance.json) && (cd target/phase10-group-topology-witness-verify/run-2 && "$WITNESS" --output group-topology-witnesses.json --provenance group-topology-witnesses.provenance.json) && cmp target/phase10-group-topology-witness-verify/run-1/group-topology-witnesses.json target/phase10-group-topology-witness-verify/run-2/group-topology-witnesses.json && cp target/phase10-group-topology-witness-verify/run-1/group-topology-witnesses.json reference/artifacts/phase10/group-topology-witnesses.json && cp target/phase10-group-topology-witness-verify/run-1/group-topology-witnesses.provenance.json reference/artifacts/phase10/group-topology-witnesses.provenance.json && jq -e '(.cases | map(.id) | sort) == (["barrier_pair","degenerate_triad","rigid_group_empty","rigid_group_one_particle","split_created_metadata","voronoi_no_necessary_generator","zero_length_pair"] | sort) and all(.cases[]; (.decision == "preserve_source_behavior" or .decision == "typed_error") and (.input_bits != null) and (.outcome != null))' reference/artifacts/phase10/group-topology-witnesses.json</automated></verify>
  <acceptance-criteria>The checked-in artifact contains exactly the seven required cases with exact inputs/outcomes and an explicit per-case decision; provenance binds it to the pinned upstream/source/compiler/preset; a repeated run is semantically byte-identical; no Rust implementation assumption precedes the artifact.</acceptance-criteria>
  <done>Split metadata and every listed finite-degenerate branch have mandatory pinned evidence and a concrete downstream decision.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pair and triad generation</name>
  <files>crates/liquidfun/src/particle/topology.rs, crates/liquidfun/src/particle/topology/constraints.rs</files>
  <read-first>reference/artifacts/phase10/group-topology-witnesses.json — Task 1 semantic cases and typed-error/source-behavior decisions; reference/artifacts/phase10/group-topology-witnesses.provenance.json — Task 1 pinned revision/compiler/source/result identity; crates/liquidfun/src/particle/topology/voronoi.rs — bounded Voronoi generator created by Plan 10-11; crates/liquidfun/src/particle/storage/lanes.rs — complete pair/triad records; crates/liquidfun/src/particle/view.rs — semantic pair/triad views; crates/liquidfun/src/particle/storage/group.rs — group ranges, flags, and strength; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `ParticleCanBeConnected`, `UpdatePairsAndTriads`, and pair/triad comparators/callbacks; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2Particle.h — `b2ParticlePair` and `b2ParticleTriad` record definitions.</read-first>
  <action>After the mandatory oracle-probe task has classified every listed finite-degenerate case, write the structural tests in a co-located `#[cfg(test)]` module before implementation without committing an intentionally failing state. Cover each flag/group gate, contact order, orientation, strength, rest distance, all seven triad coefficients, stable first duplicate, and no-sort preserve mode. Add 128 properties for endpoint bounds, stable deterministic output, and exact duplicate uniqueness. Apply the exact probe-backed typed-error versus source-defined behavior decision for each degenerate case; do not add a global epsilon or widen private topology APIs for integration tests. Create one all-green atomic task commit only after focused tests and the ordered Rust gate pass.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>PROPTEST_CASES=128 cargo test -p liquidfun --all-features particle::topology::constraints</automated></verify>
  <acceptance-criteria>Every eligibility branch has positive/negative tests; exact pair/triad records include all rest fields; stable duplicate tests retain the first inserted strength/rest record; preserve mode leaves historical order unchanged; 128 properties pass.</acceptance-criteria>
  <done>Topology generation is structurally exact and safe for create/join/reactive operations.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Contacts/group flags to topology records | Invalid endpoints or gates can create cross-owner constraints. |
| Numeric geometry to rest coefficients | Degenerate finite inputs can produce non-finite solver state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-12-01 | Spoofing | topology endpoints | mitigate | Validate same storage/system and dense bounds before record creation. |
| T-10-12-02 | Tampering | gates/order/dedup | mitigate | Closed filters and explicit append/preserve policies with exact tests. |
| T-10-12-03 | Denial of service | candidate volume | mitigate | Bound contacts/generators/Voronoi nodes and checked append products. |
| T-10-12-04 | Tampering | numeric coefficients | mitigate | Named source-branch validation; reject non-finite outcomes without global epsilon. |
| T-10-12-05 | Tampering | panic/rollback | mitigate | Pure candidate generation has no live storage effect. |
| T-10-12-06 | Tampering | unsafe/FFI boundary | accept | Safe Rust records only; no layout or FFI exposure. |
</threat-model>

<verification>
Run fixed/property topology tests and ordered Rust gate. Inspect append vs preserve call sites and all stable sorts.
</verification>

<success-criteria>
- Exact gates/order/orientation/rest state are test-covered.
- Duplicate retention is operation-scoped.
- Degenerate inputs are explicitly classified.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-12-SUMMARY.md`.</output>
