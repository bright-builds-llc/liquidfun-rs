---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "27"
type: execute
wave: 11
depends_on: ["10-24", "10-25"]
files_modified:
  - crates/liquidfun-differential/src/rigid_world/phase10.rs
  - crates/liquidfun-differential/src/rigid_world/phase10/comparator.rs
  - crates/liquidfun-differential/tests/phase10_comparator.rs
autonomous: true
requirements: [PART-09, PART-10, PART-11, PART-12, PART-13, PART-18, TEST-01, TEST-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Comparator policy is explicit per semantic field and distinguishes exact identity/order from numeric tolerance."
    - "D0 deterministic replay requires byte identity; D1 parity reports the first contextual semantic mismatch."
    - "Unknown, missing, duplicate, or unbound result fields fail closed."
  artifacts:
    - path: crates/liquidfun-differential/src/rigid_world/phase10/comparator.rs
      provides: "Closed Phase 10 semantic comparison policy"
    - path: crates/liquidfun-differential/tests/phase10_comparator.rs
      provides: "Exact/tolerant/malformed/diagnostic comparator witnesses"
  key_links:
    - from: crates/liquidfun-differential/src/rigid_world/phase10/comparator.rs
      to: crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase10.rs
      via: "exhaustive result-field comparison"
      pattern: "compare_phase10"
---

<objective>
Define the closed Phase 10 semantic comparator and tolerance policy.

Purpose: Prevent ad hoc tolerances or omitted fields from turning mismatches into false parity.
Output: Exhaustive comparator, D0/D1 modes, and mutation-based comparator tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun-differential/src/rigid_world/phase9/comparator.rs
@crates/liquidfun-differential/src/rigid_world/phase10.rs
@crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase10.rs
@crates/liquidfun-differential/tests/phase9_corpus.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compare every Phase 10 semantic field under closed policy</name>
  <files>crates/liquidfun-differential/src/rigid_world/phase10.rs, crates/liquidfun-differential/src/rigid_world/phase10/comparator.rs, crates/liquidfun-differential/tests/phase10_comparator.rs</files>
  <read-first>crates/liquidfun-differential/src/rigid_world/phase9/comparator.rs — Phase 9 comparator, typed mismatch paths, and tolerance registry; crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase10.rs — complete Phase 10 semantic result schema; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-RESEARCH.md — "Numeric Policy Map" classes, horizons, and exact identity/order requirements; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-CONTEXT.md — D-25 through D-31 evidence authority definitions.</read-first>
  <behavior>
    - "Exact fields include IDs, generations, membership/order, flags, topology connectivity/order, events, typed witness-role metadata, semantic outcomes, and schema/provenance."
    - "Only explicitly registered numerical fields use documented absolute/relative/ULP policies; D0 compares canonical bytes."
  </behavior>
  <action>Per D-23, D-25, D-26, D-27, D-29, D-31, and D-32, create an exhaustive field-policy registry and comparator for all Phase 10 result variants. Require exact equality for semantic identity, ownership, order, flags, operations/errors, pair/triad connectivity/rest-bit fields where locked, event sequences, typed control/activation/interaction witness-role metadata, semantic branch outcomes, and provenance compatibility. Apply named research-backed numeric tolerances only to registered dynamic physics fields, with finite checks and contextual path/index/actual/expected/tolerance diagnostics. D0 must compare canonical encoded semantic bytes; D1 uses the semantic registry. Reject unknown/missing/duplicate policy bindings and schema variants, and reject any attempted pass-ID/pass-trace/pass-inventory field. Add mutation tests that independently perturb every field family, threshold boundary/one-over cases, NaN/infinity, reorder/duplicate/drop records, and first-mismatch diagnostics.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun-differential --all-features --test phase10_comparator</automated></verify>
  <acceptance-criteria>Every result field has exactly one policy binding; D0 is byte identity; D1 detects every mutation family; tolerances are named and localized; diagnostics identify scenario/operation/entity/field/index.</acceptance-criteria>
  <done>Parity comparison is exhaustive, explainable, and fail-closed.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Two engine results to parity verdict | Untrusted/malformed or incomplete records can influence compatibility claims. |
| Tolerance registry to numeric acceptance | Overbroad or missing policies can conceal defects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-27-01 | Spoofing | entity alignment | mitigate | Align by validated semantic IDs and require exact ownership/order. |
| T-10-27-02 | Tampering | tolerance policy | mitigate | Closed one-binding-per-field registry and threshold mutation tests. |
| T-10-27-03 | Repudiation | mismatch verdict | mitigate | First contextual mismatch includes scenario/operation/entity/field and values/policy. |
| T-10-27-04 | Denial of service | result comparison | mitigate | Protocol-bounded collections and linear canonical comparison. |
| T-10-27-05 | Information disclosure | diagnostics | mitigate | Report bounded semantic values, never full environment or raw foreign output. |
| T-10-27-06 | Tampering | malformed/non-finite data | mitigate | Validate before comparison and reject NaN/infinity/duplicates/missing fields. |
| T-10-27-07 | Tampering | unsafe/FFI boundary | accept | Comparator is safe Rust over process-decoded semantic records. |
</threat-model>

<verification>
Run comparator mutation tests and mechanically assert registry equality with the complete schema field inventory.
</verification>

<success-criteria>
- No Phase 10 result field is ignored.
- Exact and tolerant classes are explicit and tested at boundaries.
- D0 and D1 have distinct enforced meanings.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-27-SUMMARY.md`.</output>
