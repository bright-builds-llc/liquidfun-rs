---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "17"
type: execute
wave: 6
depends_on: ["10-04", "10-05", "10-06", "10-12", "10-15"]
files_modified:
  - crates/liquidfun/src/particle/solver/preparation.rs
autonomous: true
requirements: [PART-12, PART-13, TEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "The solver prepares contacts, weights, depth, reactive topology, force, and gravity in the exact locked pass positions."
    - "Preparation passes activate only from their exact flags/state and preserve deterministic row/contact order."
  artifacts:
    - path: crates/liquidfun/src/particle/solver/preparation.rs
      provides: "S01-S06 and S13 preparation kernels"
    - path: crates/liquidfun/src/particle/solver/preparation.rs
      provides: "Crate-private kernels plus co-located control, activation, interaction, and order unit witnesses"
  key_links:
    - from: crates/liquidfun/src/particle/solver/preparation.rs
      to: crates/liquidfun/src/particle/solver/manifest.rs
      via: "one implementation per manifest pass"
      pattern: "ParticleContacts|BodyContacts|Weight|SolidDepth|ReactiveTopology|Force|Gravity"
---

<objective>
Implement the full-solver preparation and external-force passes.

Purpose: Establish exact contact-derived state before material solvers run and make flag activation observable independently.
Output: Deterministic S01-S06/S13 kernels and focused witnesses.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/solver/manifest.rs
@crates/liquidfun/src/particle/storage.rs
@crates/liquidfun/src/particle/topology.rs
@crates/liquidfun/src/world/particle_coupling.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement exact preparation kernels</name>
  <files>crates/liquidfun/src/particle/solver/preparation.rs</files>
  <read-first>crates/liquidfun/src/particle/solver/manifest.rs — closed O01-O05/S01-S26 order, IDs, and gates; crates/liquidfun/src/world/particle_coupling.rs — Phase 9 contact refresh and particle/body coupling kernels; crates/liquidfun/src/particle/storage/solver_state.rs — weight, depth, force-buffer, gravity, and solver scratch contracts; crates/liquidfun/src/particle/topology/constraints.rs — reactive topology records; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-RESEARCH.md — exact S01-S06 formulas and source anchors; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `ComputeWeight`, `ComputeDepth`, `UpdatePairsAndTriadsWithReactiveParticles`, `SolveForce`, and `SolveGravity`.</read-first>
  <behavior>
    - "S01 refreshes particle contacts, S02 refreshes body contacts, S03 derives weights, S04 computes solid depth, S05 refreshes reactive topology, S06 applies accumulated force, and S13 applies gravity."
    - "An inactive flag/state leaves the corresponding semantic buffers byte-identical while active witnesses change the intended fields."
  </behavior>
  <action>Create safe-Rust pure kernels for S01, S02, S03, S04, S05, S06, and S13 against the single ParticleStorage authority. Reuse the established Phase 9 contact/coupling query semantics rather than creating a second broad-phase path. Preserve upstream contact iteration and dense-row order; route S04/S05 through the operation-specific depth/reactive algorithms from Plan 10-15; consume and clear force exactly once at S06; apply gravity with the locked timestep/mass convention at S13. Keep every pass function crate-private and place its focused control, activation, interaction, empty-system, mixed-flag, zero-timestep, retained-empty, and relative-slot tests in a co-located `#[cfg(test)]` module so they can exercise private kernels without widening visibility.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::solver::preparation</automated></verify>
  <acceptance-criteria>All seven passes have independent control and activation witnesses; S06 clears force once; contact/row order is stable; inactive cases are exact no-diffs; no duplicate contact authority exists.</acceptance-criteria>
  <done>The full solver has deterministic prepared state and external-force kernels at their locked positions.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Broad-phase/contact inputs to solver scratch | Potentially large contact sets cross into bounded per-step work. |
| Flags/force/group state to preparation kernels | Invalid rows or stale ownership could mutate the wrong particle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-17-01 | Tampering | storage rows | mitigate | Resolve all rows through ParticleStorage-owned IDs and validate aligned lane lengths before kernels. |
| T-10-17-02 | Denial of service | contact/topology work | mitigate | Use existing bounded query/contact buffers and checked topology limits; no unbounded retry. |
| T-10-17-03 | Repudiation | pass activation | mitigate | Named pass traces plus control/activation/interaction witnesses. |
| T-10-17-04 | Tampering | force consumption | mitigate | Consume/clear only in S06 after successful candidate calculation. |
| T-10-17-05 | Tampering | panic/rollback | mitigate | Compute fallible depth/reactive candidates before replacing state; propagate typed errors. |
| T-10-17-06 | Information disclosure | private scratch | accept | Scratch remains crate-private and contains no secrets. |
| T-10-17-07 | Tampering | unsafe/FFI boundary | accept | Native safe Rust kernels; no FFI or unsafe path. |
</threat-model>

<verification>
Run the focused pass witnesses and confirm the manifest trace includes each kernel exactly once.
</verification>

<success-criteria>
- S01-S06 and S13 match the locked order and activation rules.
- Deterministic inputs produce deterministic semantic buffers.
- Empty/inactive cases are exact no-ops.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-17-SUMMARY.md`.</output>
