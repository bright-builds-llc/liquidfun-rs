---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "11"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - crates/liquidfun/src/particle/topology.rs
  - crates/liquidfun/src/particle/topology/voronoi.rs
autonomous: true
requirements: [PART-11, TEST-01, TEST-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Bounded Voronoi generation preserves dense seed order, left/down/right/up propagation, equal-distance current-generator retention, and row-major two-node emission."
    - "Extreme finite bounds fail before grid or queue allocation."
  artifacts:
    - path: crates/liquidfun/src/particle/topology/voronoi.rs
      provides: "Pure bounded pinned Voronoi algorithm"
      contains: "VoronoiDiagram"
  key_links:
    - from: crates/liquidfun/src/particle/topology.rs
      to: crates/liquidfun/src/particle/topology/voronoi.rs
      via: "private topology facade"
      pattern: "mod voronoi"
---

<objective>
Transcribe the exact bounded Voronoi algorithm used for elastic triad generation.

Purpose: Make seed/queue/tie/node order independently testable before topology records are emitted.
Output: Safe pure grid planner and focused/property tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2VoronoiDiagram.h
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2VoronoiDiagram.cpp
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp
</context>

<feature>
  <name>Pinned bounded Voronoi topology</name>
  <files>crates/liquidfun/src/particle/topology.rs, crates/liquidfun/src/particle/topology/voronoi.rs</files>
  <behavior>Tests first cover generator insertion order, duplicate/tied positions, left/down/right/up queue propagation, equal squared-distance incumbent retention, row-major cells, two oriented nodes per cell, unnecessary-generator filtering, empty/one/two generator cases, and checked overflow/resource rejection.</behavior>
  <implementation>Transcribe `b2VoronoiDiagram` with checked integer conversions/products and a bounded queue. Preserve dense generator ordinal as the only tie identity. Return oriented private generator triples in source emission order; do not use a generic geometry crate, hash map, sort, or parallel traversal.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Implement exact bounded Voronoi generation</name>
  <files>crates/liquidfun/src/particle/topology.rs, crates/liquidfun/src/particle/topology/voronoi.rs</files>
  <read-first>third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2VoronoiDiagram.h — complete `b2VoronoiDiagram` contract; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2VoronoiDiagram.cpp — complete `Generate` and `GetNodes` algorithms; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `UpdatePairsAndTriads` generator/callback use; crates/liquidfun/src/particle/storage/lanes.rs — particle index and topology record types; crates/liquidfun/src/math.rs — `Vec2` operations and source-order arithmetic; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-RESEARCH.md — "Topology Exactness" and "Pitfall 5: Unbounded Sampling or Voronoi Allocation".</read-first>
  <action>Write the focused fixtures and properties before implementation, but do not commit an intentionally failing state. Define checked grid/queue bounds derived from generator AABB and radius-derived stride; reject zero/negative/non-finite radius, overflow, and capacity excess before allocating. Preserve every listed order/tie rule and emit oriented triples without canonicalization. Add fixed expected-node fixtures plus 128 bounded properties for deterministic replay and valid endpoint ranges. Create one all-green atomic task commit only after the focused tests and ordered Rust gate pass.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>PROPTEST_CASES=128 cargo test -p liquidfun --all-features particle::topology::voronoi</automated></verify>
  <acceptance-criteria>Fixed fixtures assert exact ordered triples; repeated runs are byte-identical; tied-distance tests retain the incumbent; extreme inputs return typed bounds errors without allocation; 128 property cases pass.</acceptance-criteria>
  <done>The exact Voronoi node stream is safe, bounded, and ready for constraint filtering.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Particle geometry to grid dimensions | Finite but extreme coordinates/radius can overflow or exhaust resources. |
| Queue order to topology identity | Reordering/tie changes alter triad membership and solver behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-11-01 | Denial of service | grid/queue | mitigate | Checked axes/products/cell/queue maxima before allocation or iteration. |
| T-10-11-02 | Tampering | generator endpoints | mitigate | Validate finite positions and ordinal bounds before grid insertion/emission. |
| T-10-11-03 | Repudiation | order/ties | mitigate | Fixed exact node fixtures and D0 properties lock source order. |
| T-10-11-04 | Tampering | panic/rollback | mitigate | Pure function owns temporary grid/queue; no storage effects. |
| T-10-11-05 | Tampering | unsafe/FFI boundary | accept | Safe Rust arrays/vectors only; no unsafe or FFI. |
</threat-model>

<verification>
Run fixed/property Voronoi tests and the ordered Rust gate; inspect all casts/products for preceding checks.
</verification>

<success-criteria>
- Source queue/tie/node order is exact.
- All allocation/work is bounded.
- No generic sorting/canonicalization changes topology identity.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-11-SUMMARY.md`.</output>
