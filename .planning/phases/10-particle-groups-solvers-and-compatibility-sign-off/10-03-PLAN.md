---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun/src/particle/storage/lanes.rs
  - crates/liquidfun/src/particle/view.rs
autonomous: true
requirements: [PART-11, TEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Triads preserve all pinned rest offsets and scalar coefficients across inspection and later permutations."
    - "Topology records retain private dense endpoints internally and expose checked stable particle IDs at public boundaries."
  artifacts:
    - path: crates/liquidfun/src/particle/storage/lanes.rs
      provides: "Complete ParticlePair and ParticleTriad solver records"
      contains: "pa:"
    - path: crates/liquidfun/src/particle/view.rs
      provides: "Stable-ID semantic views of complete pair and triad state"
      contains: "ParticleTriadView"
  key_links:
    - from: crates/liquidfun/src/particle/view.rs
      to: crates/liquidfun/src/particle/storage/lanes.rs
      via: "checked dense-to-stable translation"
      pattern: "triad_view|particle_id"
---

<objective>
Complete internal topology record contracts before mutation and elastic-solver code depends on them.

Purpose: Preserve D-10/D-13 historical rest state and extend Phase 9's incomplete triad representation.
Output: Full pair/triad records, semantic stable-ID views, and record-level tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/storage/lanes.rs
@crates/liquidfun/src/particle/storage/permutation.rs
@crates/liquidfun/src/particle/view.rs
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2Particle.h
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete triad rest-state records and views</name>
  <files>crates/liquidfun/src/particle/storage/lanes.rs, crates/liquidfun/src/particle/view.rs</files>
  <read-first>crates/liquidfun/src/particle/storage/lanes.rs — particle lane and topology record storage; crates/liquidfun/src/particle/storage/permutation.rs — stable permutation/remap contract; crates/liquidfun/src/particle/view.rs — semantic pair/triad view conventions; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2Particle.h — `b2ParticleTriad`; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `UpdatePairsAndTriads` triad callback and `SolveElastic` rest-state consumption.</read-first>
  <behavior>
    - "ParticleTriad stores indices, flags, strength, pa, pb, pc, ka, kb, kc, and s."
    - "ParticleTriadView exposes stable ParticleId endpoints plus all semantic rest fields without dense coordinates."
    - "Pair and triad values reject non-finite rest state during candidate validation."
  </behavior>
  <action>Add `Vec2 pa/pb/pc` and `f32 ka/kb/kc/s` to the private `ParticleTriad`; retain private `ParticleIndex` endpoints per D-10. Extend `ParticleTriadView` with copied semantic coefficients and stable `ParticleId` endpoints, and ensure `ParticleSystemView::triads()` performs checked translation. Add focused record/view tests for orientation and signed values. Update any existing test constructors to provide explicit rest state; do not default missing coefficients silently. Add validation helpers callable by Plans 10-07/12 that reject non-finite distance, strength, offsets, or coefficients before mutation. Before committing run the exact ordered Rust gate.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::storage::lanes && cargo test -p liquidfun --all-features particle::view</automated></verify>
  <acceptance-criteria>`ParticleTriad` and `ParticleTriadView` each contain `pa`, `pb`, `pc`, `ka`, `kb`, `kc`, `s`; public endpoints are `ParticleId`; no public `ParticleIndex` appears; invalid non-finite record tests and focused views pass.</acceptance-criteria>
  <done>The topology contract contains every rest value required for elastic solving and preservation.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Topology candidate to solver lane | Malformed dense references or non-finite coefficients could corrupt solver state. |
| Private records to public view | Dense row identity must not escape or alias a different particle after permutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-03-01 | Tampering | `ParticleTriad` | mitigate | Validate all three endpoints and every numeric field before commit. |
| T-10-03-02 | Spoofing | `ParticleTriadView` | mitigate | Translate through current storage identity mapping; never persist public IDs in solver records. |
| T-10-03-03 | Information disclosure | views | mitigate | Expose semantic values only; no pointers, dense rows, capacity, or cache fields. |
| T-10-03-04 | Tampering | unsafe/FFI boundary | accept | Records remain safe Rust; no layout/FFI promise is added. |
</threat-model>

<verification>
Run focused lane/view tests and the ordered Rust gate. Review every triad constructor and permutation use so no field is dropped.
</verification>

<success-criteria>
- Full triad state exists and is finite-validated.
- Public views translate endpoints to stable IDs.
- Existing pair/triad APIs remain borrow-scoped and representation-private.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-03-SUMMARY.md`.</output>
