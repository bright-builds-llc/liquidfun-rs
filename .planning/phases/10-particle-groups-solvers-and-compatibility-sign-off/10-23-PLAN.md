---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "23"
type: execute
wave: 8
depends_on: ["10-16", "10-22"]
files_modified:
  - crates/liquidfun/src/particle/solver/manifest.rs
  - crates/liquidfun/tests/particle_solver_baseline.rs
  - crates/liquidfun/tests/particle_solver_flags.rs
  - crates/liquidfun/tests/particle_group_properties.rs
autonomous: true
requirements: [PART-09, PART-10, PART-11, PART-12, PART-13, TEST-01, TEST-02, TEST-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Every outer/sub-pass and public/internal flag has control, activation, and relevant interaction evidence."
    - "Seeded public group workflows preserve invariants, determinism, identity, and transactional failure semantics."
    - "Full-solver stepping preserves the previously proven rigid-world contract."
  artifacts:
    - path: crates/liquidfun/tests/particle_solver_flags.rs
      provides: "Closed pass/flag witness matrix"
    - path: crates/liquidfun/tests/particle_group_properties.rs
      provides: "Seeded public operation-sequence invariant tests"
    - path: crates/liquidfun/tests/particle_solver_baseline.rs
      provides: "Phase 9 rigid-world non-regression suite"
  key_links:
    - from: crates/liquidfun/src/particle/solver/manifest.rs
      to: crates/liquidfun/src/particle/solver/manifest.rs
      via: "co-located cfg(test) witness registry keyed by the private pass enum"
      pattern: "O01|S26"
---

<objective>
Close native Rust solver and group coverage before differential evidence work.

Purpose: Prove the implementation is complete, reachable, deterministic, and non-regressing rather than merely present.
Output: Closed witness matrix, public property sequences, and inherited baseline proof.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/solver/manifest.rs
@crates/liquidfun/tests/rigid_world.rs
@crates/liquidfun/tests/particle_group_mutation.rs
@crates/liquidfun/tests/particle_solver_order.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close the pass and flag witness matrix</name>
  <files>crates/liquidfun/src/particle/solver/manifest.rs, crates/liquidfun/tests/particle_solver_flags.rs, crates/liquidfun/tests/particle_solver_baseline.rs</files>
  <read-first>crates/liquidfun/src/particle/solver/preparation.rs — Plan 10-17 S01-S06 witnesses; crates/liquidfun/src/particle/solver/material.rs — Plan 10-18 S07-S12 witnesses; crates/liquidfun/src/particle/solver/pressure.rs — Plan 10-19 S14-S17 witnesses; crates/liquidfun/src/particle/solver/constraints.rs — Plan 10-20 S18-S20 witnesses; crates/liquidfun/src/particle/solver/rigid.rs — Plan 10-21 S21/S24 witnesses; crates/liquidfun/src/particle/solver/boundary.rs — Plan 10-21 S22/S23/S25/S26 witnesses; crates/liquidfun/src/particle/solver.rs — Plan 10-22 full-solver semantic and private trace tests; crates/liquidfun/src/particle/solver/manifest.rs — closed pass entries and public/internal flag inventory; crates/liquidfun/tests/rigid_world.rs — Phase 9 rigid-world baseline scenarios/assertions.</read-first>
  <behavior>
    - "A closed table maps every O01-O05/S01-S26 and every supported flag to named control, activation, and required interaction tests."
    - "Inherited rigid-world scenarios remain unchanged under the full solver when Phase 10 behaviors are inactive."
  </behavior>
  <action>Inside `solver/manifest.rs`, extend the co-located `#[cfg(test)]` module with a closed witness registry keyed by the private pass enum; reject missing/duplicate/unknown entries and assert exact cardinality without exporting the enum or enabling an integration test to name it. Bind each entry to native control/activation and required interaction witness names. In `particle_solver_flags.rs`, test flag semantics only through curated public World APIs and semantic outcomes, never private PassId values or private traces. Port/re-run Phase 9 rigid-world baselines through the full solver, including bodies/joints/particles/callbacks, and require exact semantic equivalence when new flags/groups are inactive. Do not weaken old assertions or silently refresh expected values.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::solver::manifest && cargo test -p liquidfun --all-features --test particle_solver_flags && cargo test -p liquidfun --all-features --test particle_solver_baseline</automated></verify>
  <acceptance-criteria>The registry has exactly 31 pass entries and the closed flag inventory with no omissions/duplicates; every binding names executable tests; all inherited baselines pass without weakened tolerances.</acceptance-criteria>
  <done>Native behavioral coverage is complete and prior behavior remains protected.</done>
</task>

<task type="auto">
  <name>Task 2: Exercise seeded public group workflows</name>
  <files>crates/liquidfun/tests/particle_group_properties.rs</files>
  <read-first>crates/liquidfun/src/world/particle_object.rs — supported public group creation/mutation/query API; crates/liquidfun/src/particle/group.rs — fill/stroke recipes and group flags; crates/liquidfun/src/particle/view.rs — stable semantic ID/member/order views; crates/liquidfun/src/particle/storage/validation.rs — typed invariant and error snapshot helpers; crates/liquidfun/tests/particle_lifecycle.rs — existing deterministic RNG, generator-version, shrink, and persisted-regression conventions; crates/liquidfun/tests/particle_group_mutation.rs — join/split/reactive public workflow semantics from Plan 10-16.</read-first>
  <behavior>
    - "128 seeded create, append, join, split, flag, destroy, and step sequences preserve storage/arena/association/topology invariants."
    - "Replaying the same generator version and seed yields identical semantic snapshots and mutation reports; injected failures yield no diff."
  </behavior>
  <action>Per D-23, D-24, and D-30, build bounded proptest operation generators using only curated public APIs and a versioned deterministic seed/model. Cover fill/stroke/explicit creation, append, join, split, group/particle flags, lifetime/zombie cleanup, retained empty groups, reactive regeneration, and steps. After each operation assert ID uniqueness/generation, contiguous membership and order, association agreement, pair/triad referential integrity, finite semantic state, and typed error no-diff. Persist minimized failures as ordinary regression fixtures rather than depending on transient shrink output. Cap particles/groups/operations explicitly.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>PROPTEST_CASES=128 cargo test -p liquidfun --all-features --test particle_group_properties</automated></verify>
  <acceptance-criteria>128 bounded sequences pass; same-seed replay is exact; every checked failure is effect-free; invariants are asserted after each operation, not only at sequence end.</acceptance-criteria>
  <done>Public group and solver workflows survive deterministic adversarial mutation sequences.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Witness registry to completeness claims | Missing or duplicate labels could create false assurance. |
| Generated public operations to world state | Adversarial bounded sequences stress ownership, resources, and rollback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-23-01 | Spoofing | witness labels | mitigate | Closed enum/flag keys, exact cardinality, duplicate/unknown rejection. |
| T-10-23-02 | Repudiation | coverage claims | mitigate | Every claim binds to an executable named test and inherited baseline. |
| T-10-23-03 | Denial of service | property sequences | mitigate | Explicit operation/entity/case bounds and reproducible seeds. |
| T-10-23-04 | Tampering | generated handles | mitigate | Include stale/wrong-owner inputs and assert typed no-diff results. |
| T-10-23-05 | Tampering | panic/rollback | mitigate | Snapshot before each operation and verify existing poison/rollback policy after failures. |
| T-10-23-06 | Information disclosure | failure fixtures | mitigate | Persist semantic requests only; no addresses or environment secrets. |
| T-10-23-07 | Tampering | unsafe/FFI boundary | accept | Native public-API tests only. |
</threat-model>

<verification>
Run all three suites with fixed seeds and inspect the registry for exact closed inventory equality.
</verification>

<success-criteria>
- No pass or flag lacks a positive and negative witness.
- Public randomized workflows preserve every ownership/topology invariant.
- Phase 9 baselines remain intact.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-23-SUMMARY.md`.</output>
