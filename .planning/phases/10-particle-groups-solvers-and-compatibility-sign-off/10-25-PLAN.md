---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "25"
type: execute
wave: 10
depends_on: ["10-24"]
files_modified:
  - crates/liquidfun-differential/src/rigid_world.rs
  - crates/liquidfun-differential/src/rigid_world/phase10.rs
  - crates/liquidfun-differential/src/rigid_world/phase10/native.rs
  - crates/liquidfun-differential/tests/phase10_native.rs
autonomous: true
requirements: [PART-09, PART-10, PART-11, PART-12, PART-13, PART-18, TEST-01, TEST-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "The native adapter executes every Phase 10 operation only through supported public liquidfun APIs."
    - "Native results preserve stable semantic identity/order and emit the complete shared result schema."
    - "Invalid scenarios and runtime failures return typed harness errors without partial result records."
  artifacts:
    - path: crates/liquidfun-differential/src/rigid_world/phase10/native.rs
      provides: "Public-API native scenario executor and semantic capture"
    - path: crates/liquidfun-differential/tests/phase10_native.rs
      provides: "Operation coverage, ownership, and fail-closed adapter tests"
  key_links:
    - from: crates/liquidfun-differential/src/rigid_world/phase10/native.rs
      to: crates/liquidfun/src/world/particle_object.rs
      via: "curated stable-handle World APIs"
      pattern: "create_particle_group|join_particle_groups|split_particle_group"
---

<objective>
Execute and capture Phase 10 scenarios through the native Rust engine.

Purpose: Bind the shared protocol to consumer-visible behavior without privileged storage access.
Output: Public-API native adapter and exact semantic capture tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun-differential/src/rigid_world.rs
@crates/liquidfun-differential/src/rigid_world/phase9.rs
@crates/liquidfun-test-protocol/src/scenario/rigid_world/phase10.rs
@crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase10.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the public-API native Phase 10 adapter</name>
  <files>crates/liquidfun-differential/src/rigid_world.rs, crates/liquidfun-differential/src/rigid_world/phase10.rs, crates/liquidfun-differential/src/rigid_world/phase10/native.rs, crates/liquidfun-differential/tests/phase10_native.rs</files>
  <read-first>crates/liquidfun-differential/src/rigid_world/phase9.rs — Phase 9 native adapter, operation dispatch, and semantic-ID maps; crates/liquidfun-test-protocol/src/scenario/rigid_world/phase10.rs — Phase 10 operations and bounds; crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase10.rs — Phase 10 semantic results; crates/liquidfun/src/world/particle_object.rs — curated public group/particle/world APIs; crates/liquidfun/src/particle/view.rs — semantic particle/group/pair/triad views; crates/liquidfun/src/world/step.rs — typed `StepError` and transactional step failure semantics; crates/liquidfun/src/world/config.rs — typed step-configuration errors.</read-first>
  <behavior>
    - "Every valid operation is executed in input order and every semantic result field is captured in canonical order."
    - "Unknown/stale/wrong-owner IDs, capacity failures, and step errors fail the request without emitting a partial success record."
  </behavior>
  <action>Extend the existing rigid-world native adapter with a Phase 10 module. Maintain explicit protocol-ID to stable-handle maps for worlds/systems/groups/particles/bodies/joints; execute fill/stroke/explicit group creation, append, join, split, flag mutation, destruction, and step via documented public APIs only. Capture group member order/flags/transforms, particles, pairs/full triads, contacts/events, bodies/joints, typed control/activation/interaction witness roles, and semantic outcomes through curated public views. Do not import private storage rows, private/test hooks, `PassId`, pass traces, or pass inventory. Preserve request order and canonical result ordering. Add one adapter test per operation/result family plus multi-system ownership, stale-ID, capacity, panic/poison, and exact same-seed replay cases.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun-differential --all-features --test phase10_native</automated></verify>
  <acceptance-criteria>All Phase 10 schema variants execute/capture; adapter imports only public liquidfun APIs; identity/order/topology are exact; invalid/failing requests return one typed error and no partial success record.</acceptance-criteria>
  <done>The native engine is completely reachable through the shared strict Phase 10 harness.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Validated protocol operations to public World APIs | Semantic IDs become stable handles and mutations. |
| Engine state to protocol result | Internal state is projected into portable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-25-01 | Spoofing | adapter ID maps | mitigate | Typed per-kind maps plus world/system/generation validation on every lookup. |
| T-10-25-02 | Tampering | operation ordering | mitigate | Execute immutable validated operations sequentially and record indexed outcomes. |
| T-10-25-03 | Denial of service | scenario execution | mitigate | Protocol bounds plus engine capacity/iteration limits. |
| T-10-25-04 | Repudiation | native capture | mitigate | Required request ID/seed/version and complete canonical semantic result. |
| T-10-25-05 | Information disclosure | capture | mitigate | Curated semantic views only; no dense rows, pointers, or diagnostics on stdout. |
| T-10-25-06 | Tampering | panic/rollback | mitigate | Existing catch-unwind/poison shell and request-level fail-closed result emission. |
| T-10-25-07 | Elevation of privilege | private engine access | mitigate | Dependency/API compile checks prohibit internal storage imports. |
</threat-model>

<verification>
Run adapter tests and inspect imports to confirm the differential crate consumes only curated public exports.
</verification>

<success-criteria>
- Every Phase 10 request has a native execution path.
- Every comparison-relevant field has a native capture path.
- Adapter failures cannot masquerade as physics mismatches.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-25-SUMMARY.md`.</output>
