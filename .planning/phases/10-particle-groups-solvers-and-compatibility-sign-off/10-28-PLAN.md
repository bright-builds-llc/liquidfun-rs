---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "28"
type: execute
wave: 12
depends_on: ["10-25", "10-26", "10-27"]
files_modified:
  - crates/liquidfun-differential/src/rigid_world/phase10/evidence.rs
  - crates/liquidfun-differential/src/rigid_world/phase10/comparator.rs
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/phase10-v1.json
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/cases/group-construction-and-mutation.jsonl
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/cases/topology-join-split-reactive.jsonl
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/cases/solver-material-flags.jsonl
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/cases/pressure-constraints-and-rigid.jsonl
  - crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/cases/boundary-order-and-inherited.jsonl
  - crates/liquidfun-differential/tests/phase10_corpus.rs
autonomous: true
requirements: [PART-09, PART-10, PART-11, PART-12, PART-13, PART-18, TEST-01, TEST-02, TEST-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "A closed bounded corpus gives every Phase 10 leaf an implementation, test, control, activation, policy, and semantic witness binding."
    - "Every Phase 6-9 proof family remains present and unchanged in the combined rigid-world evidence."
    - "D0 replay is canonical-byte identical and debug/release/native/oracle comparisons use the same request bytes."
  artifacts:
    - path: crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/phase10-v1.json
      provides: "Closed case/leaf/policy/request-digest manifest"
    - path: crates/liquidfun-differential/src/rigid_world/phase10/evidence.rs
      provides: "Typed leaf and proof-role binding evaluator"
    - path: crates/liquidfun-differential/tests/phase10_corpus.rs
      provides: "Two-engine corpus, D0/replay/build-mode, and corruption tests"
  key_links:
    - from: crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/phase10-v1.json
      to: crates/liquidfun-differential/tests/phase10_corpus.rs
      via: "exact request digests and closed leaf bindings"
      pattern: "request_sha256|leaf"
---

<objective>
Seal the closed semantic Phase 10 differential corpus.

Purpose: Convert the complete implementation into individual, executable, cross-engine proof leaves while retaining all earlier evidence.
Output: Five bounded scenario families, leaf/policy manifest, calibrated named policies, and corruption-resistant corpus tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun-differential/src/rigid_world/phase9/evidence.rs
@crates/liquidfun-differential/tests/phase9_corpus.rs
@crates/liquidfun-differential/src/rigid_world/phase10/comparator.rs
@crates/liquidfun-differential/tests/phase10_native.rs
@crates/liquidfun-differential/tests/phase10_oracle.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define a closed per-leaf evidence contract</name>
  <files>crates/liquidfun-differential/src/rigid_world/phase10/evidence.rs, crates/liquidfun-differential/tests/phase10_corpus.rs</files>
  <read-first>tools/xtask/src/phase9_evidence.rs — Phase 9 evidence schema, proof roles, typed validation, and corruption-test patterns; crates/liquidfun/tests/particle_solver_flags.rs — complete pass/flag witness registry; crates/liquidfun/tests/particle_group_properties.rs — group/topology workflow witness registry; crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase10.rs — Phase 10 semantic result schema; crates/liquidfun-differential/src/rigid_world/phase10/comparator.rs — comparator policy registry; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-CONTEXT.md — D-22 and D-25 through D-32.</read-first>
  <behavior>
    - "Every flag including zero-bit water, every semantic solver behavior corresponding to an O/S pass, every group flag/mutation/topology operation, and every inherited lifecycle/buffer/contact/query/callback path has exactly one typed leaf."
    - "Each leaf binds implementation, focused/integration/property test, control, activation, optional required interaction, observation, policy, and evidence payload."
  </behavior>
  <action>Per D-22 and D-25 through D-32, create a versioned closed leaf registry derived from typed semantic behavior, public flag, group/topology, and inherited inventories, not free-form labels. Require one binding for every Phase 10 semantic behavior leaf corresponding to the private O/S implementation inventory and every retained Phase 6-9 family, including implementation/native-test references, valid action/checkpoint/observation indices, typed control and activation roles, required interaction roles, exact policy path, and semantic proof payload role. The cross-engine registry must never contain private `PassId`, admission trace, or pass inventory; exact pass order/multiplicity is referenced only through Plan 10-23's native unit-test result. Reject unknown/missing/duplicate semantic leaves, flags, observations, policies, group behaviors, out-of-range indices, declaration-only assertions, baseline substitution, forbidden proof-role aliasing, unsafe paths, and count/bound excess. Add digest/identity-recomputed corruption tests so metadata repair cannot conceal semantic substitution.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun-differential --all-features --test phase10_corpus evidence_contract</automated></verify>
  <acceptance-criteria>The registry equals the complete typed inventory; every leaf has executable control/activation and needed interaction bindings; every malformed/substituted contract class fails for its exact reason.</acceptance-criteria>
  <done>Phase 10 completeness is a closed machine-checkable contract, including inherited proof.</done>
</task>

<task type="auto">
  <name>Task 2: Build and seal the bounded two-engine corpus</name>
  <files>crates/liquidfun-differential/src/rigid_world/phase10/comparator.rs, crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/phase10-v1.json, crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/cases/group-construction-and-mutation.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/cases/topology-join-split-reactive.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/cases/solver-material-flags.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/cases/pressure-constraints-and-rigid.jsonl, crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/cases/boundary-order-and-inherited.jsonl, crates/liquidfun-differential/tests/phase10_corpus.rs</files>
  <read-first>crates/liquidfun-differential/src/rigid_world/phase10/evidence.rs — Task 1 closed leaf/proof-role registry and artifact manifest; crates/liquidfun-differential/src/rigid_world/phase10/native.rs — public-API native adapter; crates/liquidfun-differential/src/rigid_world/phase10/comparator.rs — comparator policy paths; tools/reference/src/rigid_world_phase10_decode.hpp — strict oracle request adapter; tools/reference/src/rigid_world_phase10_execute.hpp — pinned oracle operation execution; reference/artifacts/phase10/group-topology-witnesses.json — upstream/source-scale topology probes; crates/liquidfun-differential/tests/phase9_corpus.rs — retained Phase 9 fixture/assertion patterns; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-RESEARCH.md — "Required Focused Test Families" coverage source grouped into the five planned case files; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-CONTEXT.md — D-28 control/activation/interaction witness coverage and D-31 retained Phase 6-9 evidence.</read-first>
  <behavior>
    - "Each case executes identical request bytes in native and oracle debug/release modes and evaluates all bound semantic leaves."
    - "Two same-build executions produce byte-identical canonical D0 traces; replay/minimization/first-divergence and inherited families remain real persisted proofs."
  </behavior>
  <action>Per D-23 and D-28 through D-32, populate exactly the five named bounded case files with fixed short horizons and source-significant order. Cover group recipes/destination/empty lifecycle, join/split/Voronoi/pair/full-triad/reactive/depth, all material/baseline flags and interactions, pressure/constraints/rigid, boundary/moving-body/order, pause/iterations, and every retained Phase 6-9 family. Use diagnostic D2 distributions and pinned-source scale only to select per-path exact-bit/ULP/absolute-relative/dimensioned-absolute thresholds; record justification and boundary tests, and never loosen a policy to obtain a match. Persist distinct replay, debug/release, minimization/copied, first-divergence, D0, native, oracle, comparison, and inherited proof payloads under canonical case-rooted roles. Record exact request/content/policy/leaf/manifest digests. Require same request bytes, D0 byte identity excluding only declared timing, and debug/release semantic agreement.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun-differential --all-features --test phase10_corpus -- --nocapture</automated></verify>
  <acceptance-criteria>Exactly five cases cover every closed leaf; request digests match bytes; all policy thresholds are justified/named/boundary-tested; D0 is byte-identical; debug/release/replay/inherited proofs are distinct, semantic, and complete.</acceptance-criteria>
  <done>The complete Phase 10 semantic corpus is deterministic, bounded, and sealed by content digests.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Fixture files/leaf bindings to executable claims | Labels or substituted payloads could impersonate behavioral evidence. |
| Diagnostic numeric distributions to comparator policy | Post-hoc tolerance inflation could hide divergence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-28-01 | Spoofing | leaf/proof identity | mitigate | Typed closed inventories, canonical role paths, exact digests, and no baseline substitution. |
| T-10-28-02 | Tampering | tolerance calibration | mitigate | Source/evidence justification, fixed horizons, boundary mutation tests, no global epsilon. |
| T-10-28-03 | Repudiation | completeness claims | mitigate | Exact registry equality and per-leaf executable binding. |
| T-10-28-04 | Denial of service | fixture execution | mitigate | Fixed five-case corpus with explicit scenario/entity/step/proof bounds. |
| T-10-28-05 | Information disclosure | persisted artifacts | mitigate | Semantic records only; no environment, pointers, or secret-bearing stderr. |
| T-10-28-06 | Tampering | corrupted/recomputed metadata | mitigate | Recomputed-digest adversarial tests validate semantic topology before deduplication. |
| T-10-28-07 | Tampering | unsafe/FFI boundary | mitigate | Foreign execution remains process-isolated and sanitizer-addressed later. |
</threat-model>

<verification>
Run the complete corpus in debug and release, repeat D0, and run all leaf/policy/proof corruption tests.
</verification>

<success-criteria>
- Every Phase 10 and inherited leaf has individual semantic evidence wiring.
- Five bounded cases produce complete native/oracle comparisons.
- D0, replay, build-mode, and first-divergence proofs are content-bound and real.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-28-SUMMARY.md`.</output>
