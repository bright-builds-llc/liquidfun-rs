---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "08"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - crates/liquidfun/src/particle/group.rs
  - crates/liquidfun/src/particle/group/sampling.rs
autonomous: true
requirements: [PART-09, TEST-01, TEST-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Filled unions, strokes, and explicit positions produce bounded source-ordered samples exactly as the pinned algorithms specify."
    - "Sampling failure occurs before group/particle identity allocation or any lifecycle effect."
  artifacts:
    - path: crates/liquidfun/src/particle/group/sampling.rs
      provides: "Pure bounded source-ordered sampling planners"
      contains: "sample_filled_union"
    - path: crates/liquidfun/src/particle/group/sampling.rs
      provides: "Pure sampling implementation with co-located shape/property evidence for order and bounds"
  key_links:
    - from: crates/liquidfun/src/particle/group.rs
      to: crates/liquidfun/src/particle/group/sampling.rs
      via: "validated recipe to pure sample plan"
      pattern: "sampling"
---

<objective>
Implement and property-test the exact pure group sampling algorithms.

Purpose: Close the geometry/order half of PART-09 without allocating identities or mutating storage.
Output: Bounded fill-union, stroke-chain, explicit-position, transform, and initial-velocity planners.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/group.rs
@crates/liquidfun/src/collision/shape.rs
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp
</context>

<feature>
  <name>Source-ordered particle-group sampling</name>
  <files>crates/liquidfun/src/particle/group.rs, crates/liquidfun/src/particle/group/sampling.rs</files>
  <behavior>
    Tests: Add Arrange/Act/Assert tests for union-overlap deduplication; `floor(lower/stride)*stride`; y-outer/x-inner traversal with strict upper bounds; chain `positionOnEdge` carry; explicit input order; transform-after-membership; `linearVelocity + cross(angularVelocity, worldPosition-group.position)` expression order; tiny-stride/huge-AABB checked rejection.
    Implementation: Implement pure `SamplePlan` functions for filled unions, edge/chain strokes, and positions. Preflight finite bounds, checked axis counts/products, source-derived maximum work, and effective particle capacity before allocating output. Use current shape point tests; do not add a generic tessellator.
    Simplification: Keep all sampling decisions pure, return owned samples plus exact velocities, and expose no identity/storage/world effects.
  </behavior>
  <implementation>Use pinned `CreateParticlesFillShapeForGroup`, `CompositeShape`, `CreateParticlesStrokeShapeForGroup`, and `CreateParticleForGroup` expression/order literally. Filled multiple shapes are one union; strokes carry edge distance across chain children; positions preserve caller order. Add bounded proptest cases for translated shapes and union idempotence. Work test-first without committing an intentionally failing state; create one atomic task commit only after the focused tests and full ordered Rust gate pass.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Implement and verify source-ordered sampling</name>
  <files>crates/liquidfun/src/particle/group.rs, crates/liquidfun/src/particle/group/sampling.rs</files>
  <read-first>crates/liquidfun/src/particle/group.rs — `ParticleGroupRecipe` validation and `ParticleGroupSource` geometry variants; crates/liquidfun/src/collision/shape.rs — current shape trait and child-index APIs; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `CreateParticleForGroup`, `CreateParticlesStrokeShapeForGroup`, `CreateParticlesFillShapeForGroup`, and `CreateParticleGroup`; third_party/liquidfun/liquidfun/Box2D/Unittests/Function/FunctionTests.cpp — particle-group creation cases; third_party/liquidfun/liquidfun/Box2D/Box2D/Documentation/Programmers-Guide/Chapter11_Particles.md — particle-group examples.</read-first>
  <action>Write the focused tests in a co-located `#[cfg(test)]` module before the private sampling implementation, preserve exact formula grouping and source order, and reject work bounds before materialization. Do not widen sampler visibility for an integration test and do not commit an intentionally failing test state. No public identity allocation occurs in this plan. Complete one all-green atomic task commit after the implementation, focused tests, and ordered Rust gate pass.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>PROPTEST_CASES=128 cargo test -p liquidfun --all-features particle::group::sampling</automated></verify>
  <acceptance-criteria>All named fixed cases and 128 property cases pass; huge finite inputs return a typed bound/capacity error without allocation; filled overlap emits no duplicate; chain sampling differs from a per-edge reset control.</acceptance-criteria>
  <done>The pure sampling plan is exact, bounded, deterministic, and ready for atomic storage creation.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Consumer geometry/stride to sampling loops | Tiny stride or huge finite bounds can exhaust CPU/memory or overflow counts. |
| Sample plan to later storage commit | Non-finite derived position/velocity must not enter identity allocation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-08-01 | Denial of service | fill/stroke planners | mitigate | Checked axis/segment counts, products, queue/output bounds, and capacity before loops/allocation. |
| T-10-08-02 | Tampering | transform/velocity arithmetic | mitigate | Validate every source and derived Vec2 as finite before returning SamplePlan. |
| T-10-08-03 | Repudiation | sample order | mitigate | Exact fixed witnesses and deterministic properties preserve y/x, child, and input order. |
| T-10-08-04 | Tampering | panic/rollback | mitigate | Pure planners have no live state; errors/panics allocate no identity or lifecycle event. |
| T-10-08-05 | Tampering | unsafe/FFI boundary | accept | Source-transcribed safe Rust only; no FFI or unsafe geometry path. |
</threat-model>

<verification>
Run fixed/property sampling tests, review checked conversions before allocations, and run the ordered Rust gate.
</verification>

<success-criteria>
- All three sources match exact pinned order.
- Work is preflight-bounded.
- Derived position/velocity is finite.
- Sampling remains pure and effect-free.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-08-SUMMARY.md`.</output>
