---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "26"
type: execute
wave: 10
depends_on: ["10-24"]
files_modified:
  - tools/reference/src/rigid_world_phase10_decode.hpp
  - tools/reference/src/rigid_world_phase10_execute.hpp
  - tools/reference/src/rigid_world.hpp
  - tools/reference/src/rigid_world.cpp
  - crates/liquidfun-differential/tests/phase10_oracle.rs
autonomous: true
requirements: [PART-09, PART-10, PART-11, PART-12, PART-13, PART-18, TEST-01, TEST-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "The pinned upstream oracle executes the same bounded Phase 10 requests and returns the same semantic schema."
    - "Foreign crashes, malformed records, stderr diagnostics, and nonzero exits are harness failures, never physics mismatches."
    - "The published Rust crates remain independent of C++ and the oracle."
  artifacts:
    - path: tools/reference/src/rigid_world_phase10_decode.hpp
      provides: "Strict bounded C++ Phase 10 decode"
    - path: tools/reference/src/rigid_world_phase10_execute.hpp
      provides: "Upstream scenario execution and semantic capture"
    - path: crates/liquidfun-differential/tests/phase10_oracle.rs
      provides: "Oracle protocol, error, and operation coverage"
  key_links:
    - from: tools/reference/src/rigid_world.cpp
      to: tools/reference/src/rigid_world_phase10_execute.hpp
      via: "existing long-lived JSONL rigid-world dispatch"
      pattern: "phase10"
---

<objective>
Extend the pinned C++ oracle with Phase 10 scenario execution and capture.

Purpose: Produce an independent semantic reference without adding FFI or production C++ dependencies.
Output: Strict C++ decode/execution modules and out-of-process oracle tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@tools/reference/src/rigid_world.hpp
@tools/reference/src/rigid_world.cpp
@tools/reference/src/rigid_world_decode.hpp
@tools/reference/src/rigid_world_phase9_execute.hpp
@crates/liquidfun-test-protocol/src/schema/rigid_world/phase10.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decode and execute Phase 10 against pinned LiquidFun</name>
  <files>tools/reference/src/rigid_world_phase10_decode.hpp, tools/reference/src/rigid_world_phase10_execute.hpp, tools/reference/src/rigid_world.hpp, tools/reference/src/rigid_world.cpp, crates/liquidfun-differential/tests/phase10_oracle.rs</files>
  <read-first>tools/reference/src/rigid_world_phase9_decode.hpp — Phase 9 strict C++ decode and duplicate/unknown-field rejection; tools/reference/src/rigid_world_phase9_execute.hpp — Phase 9 operation dispatch and semantic-ID maps; tools/reference/src/rigid_world.cpp — JSON Lines stdout/stderr and error-envelope boundary; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.h — pinned particle-system/group public API at revision `7f20402173fd143a3988c921bc384459c6a858f2`; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleGroup.h — pinned group definitions and flags; crates/liquidfun-test-protocol/src/scenario/rigid_world/phase10.rs — Phase 10 operations and bounds; crates/liquidfun-test-protocol/src/schema/rigid_world/phase10.rs — strict Phase 10 wire schema; tools/reference/CMakeLists.txt — private C++ oracle build constraints.</read-first>
  <action>Add small named C++ modules rather than embedding substantial logic in strings or the dispatcher. Strictly decode closed Phase 10 operations with pre-allocation bounds, exact float bits, duplicate/unknown rejection, and contextual errors. Execute fill/stroke/explicit creation, append, join, split, flags, destruction, and steps against the pinned upstream APIs; maintain stable protocol IDs independent of pointers; capture canonical group/member order, topology including full triads, particles/contacts/events, bodies/joints, typed control/activation/interaction witness roles, and their semantic outcomes. The C++ schema/executor must not know Rust-private pass IDs or emit pass admission/order traces. Emit protocol records only on stdout and diagnostics only on stderr. Catch exceptions at the request loop, classify timeout/crash/nonzero/malformed output as harness errors, and keep the process alive across valid batches. Do not introduce an FFI library, alter vendored upstream, or link any published crate to C++.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun-differential --all-features --test phase10_oracle</automated></verify>
  <acceptance-criteria>Every valid Phase 10 operation/result family has an oracle witness; malformed/oversized inputs fail before unbounded work; semantic IDs never derive from pointer values; stdout stays valid JSONL; batch recovery/error classification tests pass.</acceptance-criteria>
  <done>The pinned upstream oracle is a complete, isolated Phase 10 semantic reference.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| JSONL stdin to native C++ allocations/upstream calls | Untrusted records cross into foreign code and legacy APIs. |
| C++ process stdout/stderr/exit to Rust harness | Potential crashes or malformed output cross process isolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-26-01 | Spoofing | pointer-backed upstream objects | mitigate | Independent typed semantic-ID maps; never serialize pointer identity. |
| T-10-26-02 | Tampering | parser variants | mitigate | Closed tag/field decode, duplicate rejection, exact schema/version checks. |
| T-10-26-03 | Denial of service | C++ allocation/execution | mitigate | Preflight byte/count/iteration bounds and harness timeout/process restart. |
| T-10-26-04 | Repudiation | oracle identity | mitigate | Emit pinned commit/compiler/build/protocol provenance on every result. |
| T-10-26-05 | Information disclosure | diagnostics | mitigate | stdout records contain semantic data only; bounded stderr is archived separately. |
| T-10-26-06 | Tampering | crash/partial output | mitigate | Process isolation; partial/malformed/nonzero results are hard harness errors. |
| T-10-26-07 | Elevation of privilege | FFI/production dependency | mitigate | No FFI surface; Cargo package smoke checks exclude the oracle. |
</threat-model>

<verification>
Run the oracle suite through the documented CMake/Ninja/xtask path and verify Cargo-only package/build lanes never discover C++.
</verification>

<success-criteria>
- The C++ process supports the complete Phase 10 schema.
- Foreign failures remain isolated and distinctly classified.
- Published crates retain zero oracle/C++ dependency.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-26-SUMMARY.md`.</output>
