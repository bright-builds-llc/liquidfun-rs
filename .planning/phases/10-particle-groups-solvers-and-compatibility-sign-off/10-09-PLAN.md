---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "09"
type: execute
wave: 5
depends_on: ["10-05", "10-07", "10-08", "10-12", "10-13"]
files_modified:
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/src/world/particle_object.rs
  - crates/liquidfun/tests/particle_groups.rs
autonomous: true
requirements: [PART-09, PART-10, TEST-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Public group creation and append validate every sample, capacity, identity, topology, and association before one atomic commit."
    - "Consumers inspect live group state through stable IDs and borrow-scoped storage-backed views."
  artifacts:
    - path: crates/liquidfun/src/world/particle_object.rs
      provides: "Public create/append/view group workflows"
      contains: "create_particle_group"
    - path: crates/liquidfun/tests/particle_groups.rs
      provides: "Black-box public group creation and rollback tests"
  key_links:
    - from: crates/liquidfun/src/world/particle_object.rs
      to: crates/liquidfun/src/particle/storage/mutation.rs
      via: "preflighted create candidate then one commit"
      pattern: "plan_group|commit_group"
---

<objective>
Expose atomic group creation, append, and inspection through the supported World API.

Purpose: Turn the contract/sampler/storage/topology foundations into the complete D-02/D-05 consumer workflow.
Output: Stable group allocation shell, one storage commit, append-to-target semantics, association handling, and integration tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/world/object.rs
@crates/liquidfun/src/world/particle_object.rs
@crates/liquidfun/src/particle/group.rs
@crates/liquidfun/src/particle/storage/mutation.rs
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement atomic create and append workflows</name>
  <files>crates/liquidfun/src/world/object.rs, crates/liquidfun/src/world/particle_object.rs, crates/liquidfun/tests/particle_groups.rs</files>
  <read-first>crates/liquidfun/src/world/object.rs — world arena insertion/preflight and `ParticleGroup` records; crates/liquidfun/src/world/particle_object.rs — particle-system insertion, receipt, and handle checks; crates/liquidfun/src/particle/group.rs — group recipe and flag APIs; crates/liquidfun/src/particle/view.rs — semantic view contracts; crates/liquidfun/src/particle/storage/mutation.rs — preflighted create/append candidates; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-12-PLAN.md — `<feature>` exact pair/triad topology-record contract consumed after group creation; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `CreateParticleGroup` and create-then-`JoinParticleGroups` append behavior; crates/liquidfun/src/world/particle_lifecycle.rs — Phase 9 creation receipt and lifecycle commit conventions.</read-first>
  <behavior>
    - "New returns a fresh ParticleGroupId and source-ordered ParticleIds."
    - "AppendTo validates a live same-system target, plans a temporary group plus exact join, commits once, returns the target ID, and exposes no temporary identity."
    - "Any sampling/capacity/handle/allocation/topology/association failure leaves world/storage/journal/diagnostic identity unchanged."
  </behavior>
  <action>Deepen the world `ParticleGroup` arena record into identity/owner shell only; physics metadata stays in ParticleStorage. Implement `World::create_particle_group(system, recipe)` and association-capable equivalent following: validate world lock/poison, resolve same-system AppendTo, sample purely, preflight particle/group/diagnostic IDs and lifecycle capacity, build complete storage/topology candidate, validate it, allocate the public group shell only when commit cannot fail, then install storage and association effects once. For append, execute pinned temporary-create-plus-join semantics inside the one hidden candidate and return the validated target ID. Implement `particle_group_view(group)` by validating world/system ownership then borrowing the storage record. Add public tests for every source, all recipe fields, stable order, append identity, cross-system/stale target rejection, capacity/topology failure snapshots, and no lifecycle occurrence on failure. Run the exact ordered Rust gate before commit.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features --test particle_groups</automated></verify>
  <acceptance-criteria>Public tests create fill/stroke/position groups, inspect every D-04 field, append returns the original target, wrong-system/stale/capacity failures preserve exact snapshots and identity counts, and no temporary append group is externally enumerable.</acceptance-criteria>
  <done>Consumers can safely create, append, and inspect complete particle groups through supported APIs.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Public group/append call to world and storage | Untrusted handle, recipe, association, and geometry cross into identity/state mutation. |
| Arena shell to storage record | Duplicate authority or wrong owner could desynchronize identity and physics state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-09-01 | Spoofing | AppendTo/group view | mitigate | Full world/system/generation validation precedes candidate construction. |
| T-10-09-02 | Tampering | create transaction | mitigate | Preflight all identities, samples, topology, capacity, and journals; one no-fail commit. |
| T-10-09-03 | Denial of service | group creation | mitigate | Reuse source-derived sampling bounds and declared particle/group capacities. |
| T-10-09-04 | Repudiation | lifecycle effects | mitigate | Tests assert exact absence on failure and source-order occurrences on success. |
| T-10-09-05 | Tampering | panic/rollback | mitigate | Pure candidate and delayed arena allocation preserve state on callback/allocation panic. |
| T-10-09-06 | Tampering | unsafe/FFI boundary | accept | Public workflow is safe Rust and invokes no C++ at runtime. |
</threat-model>

<verification>
Run public group tests, existing lifecycle/buffer tests, and ordered Rust gate. Diff snapshots include identities, groups, all lanes, topology, caches, and journal.
</verification>

<success-criteria>
- Every recipe source creates source-ordered stable particles.
- Append is one atomic target-preserving transaction.
- Views expose complete semantic state.
- All failure paths are effect-free.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-09-SUMMARY.md`.</output>
