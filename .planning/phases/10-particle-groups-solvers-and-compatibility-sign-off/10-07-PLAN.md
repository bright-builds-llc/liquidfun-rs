---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "07"
type: execute
wave: 3
depends_on: ["10-05", "10-06"]
files_modified:
  - crates/liquidfun/src/particle/storage/mutation.rs
  - crates/liquidfun/src/particle/storage/permutation.rs
  - crates/liquidfun/src/particle/storage/properties.rs
  - crates/liquidfun/src/particle/storage/properties/group_model.rs
autonomous: true
requirements: [PART-10, TEST-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Create, join, split, compaction, reactive regeneration, flag changes, and ordinary rotation use distinct mutation candidate kinds."
    - "A failed candidate leaves all particle IDs, lanes, groups, topology, caches, and lifecycle state byte-for-byte unchanged."
  artifacts:
    - path: crates/liquidfun/src/particle/storage/mutation.rs
      provides: "Operation-specific validated mutation candidates"
      contains: "MutationCandidate"
    - path: crates/liquidfun/src/particle/storage/properties/group_model.rs
      provides: "Reproducible group/topology state-machine properties"
      contains: "proptest"
  key_links:
    - from: crates/liquidfun/src/particle/storage/mutation.rs
      to: crates/liquidfun/src/particle/storage/permutation.rs
      via: "explicit remap policy per operation"
      pattern: "rotate|split|join|reactive"
---

<objective>
Build the validate-plan-commit substrate for all group and topology operations.

Purpose: Enforce D-05/D-09/D-11/D-14 transactionality before public mutations are exposed.
Output: Closed operation-specific candidates, complete permutation/remap policies, and property-model rollback evidence.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/storage.rs
@crates/liquidfun/src/particle/storage/permutation.rs
@crates/liquidfun/src/particle/storage/properties.rs
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define operation-specific mutation candidates</name>
  <files>crates/liquidfun/src/particle/storage/mutation.rs, crates/liquidfun/src/particle/storage/permutation.rs</files>
  <read-first>crates/liquidfun/src/particle/storage.rs — `ParticleStorage` mutation/validation boundary; crates/liquidfun/src/particle/group.rs — group recipe, flag, and identity contracts; crates/liquidfun/src/particle/storage/solver_state.rs — deterministic solver scratch candidate; crates/liquidfun/src/particle/storage/permutation.rs — permutation implementation and co-located tests; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `RotateBuffer`, `JoinParticleGroups`, `SplitParticleGroup`, `SolveZombie`, and `UpdatePairsAndTriadsWithReactiveParticles`.</read-first>
  <behavior>
    - "Ordinary rotation remaps existing topology without sorting or changing rest values."
    - "Join/add/reactive candidates may append and source-sort only at their exact operation."
    - "Split retargets historical records and preserves every rest field."
    - "Commit is no-fail after complete candidate validation."
  </behavior>
  <action>Create a closed private `MutationCandidateKind` and operation-specific candidate payloads for create, join, split, zombie compaction, reactive regeneration, group-flag change, and ordinary rotation. Each payload must carry the exact permutation, group records, all lanes/scratch, pair/triad remap-or-append mode, cache/depth invalidations, aggregate flags, and lifecycle effects. Refactor `permutation.rs` so callers choose `PreserveHistoricalOrder` versus `AppendStableSortFirstDuplicate`; never use a generic recompute-topology fallback. Candidate validation must finish every checked index conversion, finite check, capacity check, identity allocation, and journal bound before a no-fail commit. Add mutation-kind unit tests. Run the exact ordered Rust gate before commit.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::storage::mutation && cargo test -p liquidfun --all-features particle::storage::permutation</automated></verify>
  <acceptance-criteria>Seven distinct candidate kinds exist; rotation/split tests prove pair/triad rest bytes and relative order survive; invalid candidates produce no storage diff; no generic `recompute_topology` helper exists.</acceptance-criteria>
  <done>Every later group/topology operation has a named transactional commit contract.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the reproducible property model</name>
  <files>crates/liquidfun/src/particle/storage/properties.rs, crates/liquidfun/src/particle/storage/properties/group_model.rs</files>
  <read-first>crates/liquidfun/tests/particle_lifecycle.rs — existing lifecycle property model and generator conventions; crates/liquidfun/src/particle/storage/permutation.rs — existing permutation tests/properties; crates/liquidfun/src/particle/storage/mutation.rs — Task 1 operation-specific mutation candidates; crates/liquidfun/src/particle/storage/lane_inventory.rs — full lane inventory; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-CONTEXT.md — decisions D-05, D-06, D-07, D-11, and D-14; standards/core/testing.md — property-test and Arrange/Act/Assert requirements.</read-first>
  <behavior>
    - "Seeded sequences preserve stable IDs, contiguous ranges, aligned lanes, valid topology endpoints, and complete rollback."
    - "The model distinguishes operation-specific sort/remap behavior."
  </behavior>
  <action>Add a bounded `proptest` model with explicit create-group, rotate, join, split, mark-zombie, compact, reactive, and flag-change commands. Compare production state after every command with an independent semantic model; inject invalid handle/range/capacity/non-finite candidates and assert exact before/after equality. Use fixed regression seeds and bounded command/vector sizes so tests are reproducible. Test one invariant concern per property with Arrange/Act/Assert comments where setup is nontrivial. Run the exact ordered Rust gate before commit.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>PROPTEST_CASES=128 cargo test -p liquidfun --all-features particle::storage::properties::group_model</automated></verify>
  <acceptance-criteria>The property model executes every candidate kind; at least 128 cases pass; injected failures assert no state change; stable IDs and topology-rest preservation are independently checked.</acceptance-criteria>
  <done>TEST-04 has an executable storage/group transaction model before public group workflows are wired.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Operation request to mutation candidate | Invalid identities, ranges, and topology can target unrelated state. |
| Validated candidate to no-fail commit | Any hidden fallibility can expose partial lanes or lifecycle effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-07-01 | Tampering | mutation candidates | mitigate | Closed operation kinds and complete validation prohibit ambiguous generic mutation. |
| T-10-07-02 | Spoofing | handles/topology endpoints | mitigate | Validate world/system/generation and dense endpoint bounds before planning. |
| T-10-07-03 | Denial of service | property/generated operations | mitigate | Bound commands, groups, particles, and topology products; persist failing seeds. |
| T-10-07-04 | Repudiation | rollback | mitigate | Exact before/after semantic snapshots prove failures emit no effect. |
| T-10-07-05 | Tampering | panic/rollback | mitigate | No live mutation until complete candidate; panic injection tests retain state. |
| T-10-07-06 | Tampering | unsafe/FFI boundary | accept | No unsafe/FFI path; all candidates own safe Rust values. |
</threat-model>

<verification>
Run focused candidate/permutation tests, 128-case property model, and ordered Rust gate.
</verification>

<success-criteria>
- Operation-specific semantics are structural.
- Commit is no-fail after validation.
- Reproducible properties cover identity, lanes, groups, topology, and rollback.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-07-SUMMARY.md`.</output>
