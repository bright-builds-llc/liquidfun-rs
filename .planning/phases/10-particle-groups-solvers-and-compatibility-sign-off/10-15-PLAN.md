---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "15"
type: execute
wave: 5
depends_on: ["10-12", "10-13"]
files_modified:
  - crates/liquidfun/src/particle/topology.rs
  - crates/liquidfun/src/particle/storage/group.rs
autonomous: true
requirements: [PART-10, PART-11, PART-13, TEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Reactive regeneration appends exact eligible topology and clears REACTIVE only after successful update."
    - "Solid depth and rigid statistics invalidate/recompute only at pinned source points."
  artifacts:
    - path: crates/liquidfun/src/particle/topology.rs
      provides: "Reactive topology transaction"
      contains: "reactive"
    - path: crates/liquidfun/src/particle/storage/group.rs
      provides: "Depth scheduling and group statistics recomputation"
      contains: "needs_update_depth"
  key_links:
    - from: crates/liquidfun/src/particle/topology.rs
      to: crates/liquidfun/src/particle/topology/constraints.rs
      via: "reactive-filtered append"
      pattern: "generate_pairs_and_triads"
---

<objective>
Implement reactive topology updates and exact group depth/statistics scheduling.

Purpose: Close D-14/D-15 source timing shared by topology and solver gates.
Output: Reactive append/clear transaction, depth invalidation/recompute, and cache tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/topology.rs
@crates/liquidfun/src/particle/storage/group.rs
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleGroup.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement reactive append/clear and depth/cache scheduling</name>
  <files>crates/liquidfun/src/particle/topology.rs, crates/liquidfun/src/particle/storage/group.rs</files>
  <read-first>crates/liquidfun/src/particle/topology/constraints.rs — append/preserve pair/triad generation; crates/liquidfun/src/particle/storage/group.rs — group records, aggregate flags, statistics, and depth-cache state; crates/liquidfun/src/particle/storage/mutation.rs — preflighted reactive mutation candidate; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `UpdatePairsAndTriadsWithReactiveParticles`, `ComputeDepth`, `SetGroupFlags`, and `SolveRigid`; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleGroup.cpp — `b2ParticleGroup::UpdateStatistics`; crates/liquidfun/src/particle/solver/manifest.rs — S04 solid-depth and S05 reactive-topology gate predicates.</read-first>
  <behavior>
    - "Only records involving reactive particles are candidates; append uses exact stable sort/first duplicate policy."
    - "REACTIVE clears from individual particles only after pair/triad update candidate validates."
    - "SOLID transitions schedule NEEDS_UPDATE_DEPTH; S04 clears it after full finite depth recomputation."
    - "Group cache timestamp invalidates on membership/position/velocity/flag changes and recomputes in source expression order."
  </behavior>
  <action>Implement pure reactive filter plus candidate commit; if topology generation fails, preserve flags/topology exactly. Implement depth candidate allocation and iterative contact-order calculation from pinned ComputeDepth with checked finite intermediates; schedule/clear private depth flag at exact source points. Implement source-ordered group statistics recomputation for mass, center, linear velocity, inertia, angular velocity and transform-dependent rigid state, including exact zero empty-group branch. Add control/activation/failure, multi-group depth, cache timestamp, and no-clear-on-failure tests in co-located `#[cfg(test)]` modules so private kernels remain private. Never place cache state in the world arena. Run the ordered Rust gate before commit.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::topology::reactive && cargo test -p liquidfun --all-features particle::storage::group</automated></verify>
  <acceptance-criteria>Reactive controls add nothing; activation appends exact records and clears flags only on success; failed update leaves flags/topology unchanged; depth schedules/clears exactly; group cache values/timestamps match fixed source cases.</acceptance-criteria>
  <done>Reactive topology, solid depth, and group statistics obey their pinned transactional timing.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Reactive flags/contacts to topology mutation | Failure after flag clearing would lose required work. |
| Group contacts/lanes to depth/statistics | Non-finite or inconsistent caches can poison later solvers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-15-01 | Tampering | reactive clear | mitigate | Clear only inside validated topology commit. |
| T-10-15-02 | Denial of service | depth iteration | mitigate | Bounded particle/contact scans and configured source iteration limits. |
| T-10-15-03 | Tampering | cache arithmetic | mitigate | Validate every finite intermediate and install complete cache once. |
| T-10-15-04 | Repudiation | scheduling/timestamps | mitigate | Private native scheduling tests plus public semantic control/activation outcomes prove source points without exporting pass traces. |
| T-10-15-05 | Tampering | panic/rollback | mitigate | Candidate-owned topology/depth/cache prevents partial exposure. |
| T-10-15-06 | Tampering | unsafe/FFI boundary | accept | Safe Rust only; no FFI cache/layout surface. |
</threat-model>

<verification>
Run reactive/depth/cache tests, topology tests, and ordered Rust gate.
</verification>

<success-criteria>
- Reactive clear is commit-bound.
- Depth scheduling and recomputation are exact.
- Group aggregate state is finite, cached, and storage-owned.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-15-SUMMARY.md`.</output>
