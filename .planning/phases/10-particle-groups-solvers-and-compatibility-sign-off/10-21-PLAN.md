---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "21"
type: execute
wave: 6
depends_on: ["10-02", "10-04", "10-05", "10-06", "10-12", "10-15"]
files_modified:
  - crates/liquidfun/src/particle/solver/rigid.rs
  - crates/liquidfun/src/particle/solver/boundary.rs
autonomous: true
requirements: [PART-09, PART-12, PART-13, TEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Rigid damping, barrier, collision, rigid projection, wall, and integration run as exact ordered boundary passes."
    - "Collision uses the previous body transform and rigid-group membership/identity remains stable through motion."
  artifacts:
    - path: crates/liquidfun/src/particle/solver/rigid.rs
      provides: "S21 rigid damping and S24 rigid projection"
    - path: crates/liquidfun/src/particle/solver/boundary.rs
      provides: "S22-S23 and S25-S26 boundary/integration kernels"
    - path: crates/liquidfun/src/particle/solver/rigid.rs
      provides: "Crate-private rigid kernels with co-located group/ownership unit witnesses"
    - path: crates/liquidfun/src/particle/solver/boundary.rs
      provides: "Crate-private boundary kernels with co-located collision/wall/integration unit witnesses"
  key_links:
    - from: crates/liquidfun/src/particle/solver/boundary.rs
      to: crates/liquidfun/src/world/particle_coupling.rs
      via: "body-contact impulses and previous-transform collision"
      pattern: "previous.*transform|body_contact"
---

<objective>
Implement rigid-group, barrier, collision, wall, and final integration passes.

Purpose: Complete the solver tail where group identity, body coupling, and position updates interact most strongly.
Output: Exact S21-S26 kernels and boundary/ownership regression evidence.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/solver/manifest.rs
@crates/liquidfun/src/particle/storage.rs
@crates/liquidfun/src/world/particle_coupling.rs
@crates/liquidfun/src/world/body.rs
@reference/artifacts/phase10/group-topology-witnesses.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement rigid-group solver passes</name>
  <files>crates/liquidfun/src/particle/solver/rigid.rs</files>
  <read-first>reference/artifacts/phase10/group-topology-witnesses.json — Plan 10-12 rigid-group finite-degenerate outcomes; reference/artifacts/phase10/group-topology-witnesses.provenance.json — pinned witness identity; crates/liquidfun/src/particle/storage/group.rs — group transform, strength, statistics, and member-order contracts; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-RESEARCH.md — exact S21 and S24 formulas/source anchors; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `SolveRigidDamping` and `SolveRigid`; crates/liquidfun/src/world/particle_coupling.rs — Phase 9 body impulse candidate/commit path.</read-first>
  <behavior>
    - "Rigid damping applies exact particle/body and inter-group damping; rigid projection updates transform and member velocities without changing identity/order."
    - "Non-rigid controls and retained-empty rigid groups are exact no-ops."
  </behavior>
  <action>Implement crate-private S21 rigid_damping and S24 rigid kernels with exact mass/inertia/center/transform accumulation, member traversal, flag gates, and candidate body impulses. Apply the mandatory Plan 10-12 `rigid_group_empty` and `rigid_group_one_particle` classifications verbatim and fail if their artifact/provenance is absent or mismatched. Preserve group and particle IDs, association rows, and stable member order. Keep body mutation in the existing candidate/commit shell rather than mutating bodies inside the pure kernel. Place control/activation/interaction witnesses for a single rigid group, two groups, rigid-to-body contact, empty retained group, one-particle group, and translated/rotated configurations in a co-located `#[cfg(test)]` module.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::solver::rigid</automated></verify>
  <acceptance-criteria>S21/S24 are separate; rigid transforms and velocities match upstream witnesses; IDs/order/associations do not change; body impulses remain candidate-owned; non-rigid/empty controls have no diff.</acceptance-criteria>
  <done>Rigid groups participate in damping and projection without violating ownership or identity.</done>
</task>

<task type="auto">
  <name>Task 2: Implement barrier, collision, wall, and integration</name>
  <files>crates/liquidfun/src/particle/solver/boundary.rs</files>
  <read-first>.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-RESEARCH.md — exact S22, S23, S25, and S26 formulas/source anchors; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `SolveBarrier`, `SolveCollision`, `SolveWall`, and final position integration; crates/liquidfun/src/world/object.rs — previous/current body transform storage; crates/liquidfun/src/world/particle_coupling.rs — collision filtering, listener, and body-coupling contracts; crates/liquidfun/src/world/particle_lifecycle.rs — particle destruction and callback journal conventions.</read-first>
  <behavior>
    - "Barrier and wall flags constrain only their exact targets; collision samples body motion from the previous transform."
    - "S26 integrates once after every other sub-pass and records destruction/callback effects through the bounded journal."
  </behavior>
  <action>Implement crate-private S22, S23, S25, and S26 kernels in exact sequence. Use prior-body transforms for collision, existing filter/query semantics, stable body/contact ordering, exact barrier/wall gates, and the critical velocity already established by S20. Integrate positions exactly once at S26; emit any resulting body/particle side effects into bounded candidates/journals for later commit. Place stationary and moving-body controls, previous-vs-current transform discriminator, barrier endpoint ordering, wall immobility, one-step integration, mixed rigid/boundary interactions, resource-limit errors, and deterministic repeat tests in a co-located `#[cfg(test)]` module.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::solver::boundary</automated></verify>
  <acceptance-criteria>S22/S23/S25/S26 each have controls and activations; collision demonstrably uses previous transform; integration occurs once and last; journal overflow is typed/effect-free; deterministic repeats match.</acceptance-criteria>
  <done>The exact solver tail handles barriers, bodies, walls, and final integration transactionally.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Group membership/transforms to rigid kernels | Stale rows could move particles under the wrong group identity. |
| Body transforms/contact callbacks to particle collision | Cross-subsystem state and user callbacks influence candidate mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-21-01 | Spoofing | group/member ownership | mitigate | Validate system/group/particle generations and contiguous membership before solving. |
| T-10-21-02 | Tampering | body transform selection | mitigate | Explicit previous-transform API plus a discriminator regression test. |
| T-10-21-03 | Denial of service | contacts/journal | mitigate | Bounded contact, candidate, and callback journal capacities with typed overflow. |
| T-10-21-04 | Repudiation | boundary effects | mitigate | Pass-labelled semantic traces and exact candidate reports. |
| T-10-21-05 | Elevation of privilege | callbacks | mitigate | Callbacks observe curated semantic records and cannot receive raw storage references. |
| T-10-21-06 | Tampering | panic/rollback | mitigate | Pure candidates first; commit particle/body/journal state together under world poison semantics. |
| T-10-21-07 | Tampering | unsafe/FFI boundary | accept | Native safe Rust; no raw pointer or foreign call. |
</threat-model>

<verification>
Run both focused test filters and confirm exact S21-S26 order in the shared manifest trace.
</verification>

<success-criteria>
- Rigid and boundary passes preserve stable group/particle ownership.
- Previous-transform collision is explicitly proven.
- Final integration is single, last, bounded, and transactional.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-21-SUMMARY.md`.</output>
