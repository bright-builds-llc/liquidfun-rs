---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "31"
type: execute
wave: 15
depends_on: ["10-30"]
files_modified:
  - TESTING.md
  - target/phase10-evidence/run.json
  - target/phase10-evidence/phase10-canonical
  - target/phase10-evidence/phase10-sanitizer
autonomous: true
requirements: [PART-18]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Exactly one fresh Oracle CI run is dispatched after the implementation commit is pushed."
    - "The successful run contains one same-SHA canonical and sanitizer pair on the locked D1 stack."
    - "Freshly downloaded artifacts independently pass exact-ref validation before any compatibility edit."
  artifacts:
    - path: target/phase10-evidence/run.json
      provides: "Live run/job/artifact/SHA/API digest authority snapshot"
    - path: target/phase10-evidence/phase10-canonical
      provides: "Fresh independently downloaded canonical evidence"
    - path: target/phase10-evidence/phase10-sanitizer
      provides: "Fresh independently downloaded sanitizer evidence"
  key_links:
    - from: target/phase10-evidence/run.json
      to: target/phase10-evidence/phase10-canonical
      via: "same-run/SHA exact artifact identity and digest"
      pattern: "run_id|head_sha"
---

<objective>
Acquire and independently validate one fresh canonical Phase 10 authority set.

Purpose: Cross the D2-to-D1 boundary exactly once and only after the reviewed implementation is immutable.
Output: One successful run record, two fresh exact artifacts, and an exact-ref validation summary.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-30-SUMMARY.md
@.github/workflows/oracle.yml
@TESTING.md
@tools/xtask/src/phase10_evidence.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push the existing clean implementation HEAD, dispatch once, and capture immutable run identity</name>
  <files>target/phase10-evidence/run.json</files>
  <read-first>.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-30-SUMMARY.md — proof that implementation tasks are committed and local Phase 10 evidence is green; AGENTS.md — exact ordered Rust verification and clean-commit requirements; .git/config — current branch/upstream remote configuration; .github/workflows/oracle.yml — `workflow_dispatch` inputs and exact Phase 10 job/artifact names; tools/xtask/src/phase10_evidence.rs — historical run/artifact denysets and authority identity contract.</read-first>
  <action>This is a no-commit authority-acquisition orchestration task. Per D-32/D-33, require `git status --porcelain` is empty before any command, record `IMPLEMENTATION_SHA=$(git rev-parse HEAD)`, and reject detached or untracked-remote state. Run exactly `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` in that order with no intervening mutation. Do not create, amend, squash, or otherwise manufacture an implementation commit: all implementation changes were committed by Plans 10-01 through 10-30, and `git rev-parse HEAD` plus clean status must still equal `IMPLEMENTATION_SHA` after the gate. Push that existing HEAD once, fetch origin, and require local HEAD equals the remote branch head. Query existing Phase 10 workflow runs to prove zero run already targets this exact HEAD, dispatch Oracle CI exactly once, capture its run ID immediately, and watch to terminal success. Write only the gitignored acquisition record `target/phase10-evidence/run.json` from fresh API queries with full SHA, event/workflow, exact job/artifact IDs/names/conclusions/digests/toolchains/platform, URLs, and capture timestamps. The execute-plan task-commit step must be skipped because this task intentionally creates no tracked repository change. Do not redispatch on physics/test failure; diagnose in a later plan instead.</action>
  <commit-policy>Do not create a task commit. Require the tracked worktree to remain clean and HEAD to remain exactly `IMPLEMENTATION_SHA`; this task acquires external authority for the already-committed implementation.</commit-policy>
  <verify><automated>test "$(git rev-parse HEAD)" = "$(git rev-parse "origin/$(git branch --show-current)")" && RUN_ID=$(jq -r .run_id target/phase10-evidence/run.json) && HEAD_SHA=$(jq -r .head_sha target/phase10-evidence/run.json) && test "$HEAD_SHA" = "$(git rev-parse HEAD)" && gh run view "$RUN_ID" --json conclusion,headSha,event,workflowName | jq -e --arg sha "$HEAD_SHA" '.conclusion=="success" and .headSha==$sha and .event=="workflow_dispatch" and .workflowName=="Oracle CI"'</automated></verify>
  <acceptance-criteria>There is no new local commit; tracked status stays clean and HEAD stays `IMPLEMENTATION_SHA`; there is exactly one new run for exact HEAD; it succeeds at the pushed immutable SHA; run.json contains fresh complete live identities; no compatibility file changed and no duplicate dispatch occurred.</acceptance-criteria>
  <done>One successful current run is the sole candidate Phase 10 authority.</done>
</task>

<task type="auto">
  <name>Task 2: Redownload and independently exact-ref validate both artifacts</name>
  <files>TESTING.md, target/phase10-evidence/run.json, target/phase10-evidence/phase10-canonical, target/phase10-evidence/phase10-sanitizer</files>
  <read-first>target/phase10-evidence/run.json — Task 1 immutable run/job/artifact identity record; .github/workflows/oracle.yml — expected live GitHub workflow/job/artifact contract; tools/xtask/src/phase10_evidence.rs — archive safety checks, exact-ref validator CLI, hash recomputation, and denysets; reference/compatibility.json — current compatibility authority that must remain unchanged in this plan; TESTING.md — exact-ref acquisition and validation procedure.</read-first>
  <action>Per D-31 through D-33, re-query live run/job/artifact metadata independently, validate archive entries before extraction, and download the exact unexpired canonical and sanitizer artifacts into fresh fixed directories. Require one run/SHA, distinct exact artifact IDs, locked Linux x86_64 Rust 1.97.0/Clang 22.1.8 identities, passing logs, exact file topology, all five cases/closed leaves/policies/inherited proofs, D0 bytes, debug/release/replay, sanitizer, provenance, and every recomputed digest/identity. Run `phase10-evidence validate --mode exact-ref` with all known prior/failed run and artifact IDs denylisted. Record only immutable URLs and the reusable validation procedure in TESTING.md after success. Do not mutate the compatibility ledger or generated report.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <markdown-check>Before the task's atomic commit, run `just markdown-check` and require it passes for every changed repository-owned non-GSD Markdown file. Never run mdformat over `.planning/**`.</markdown-check>
  <verify><automated>cargo xtask phase10-evidence validate --mode exact-ref --run-json target/phase10-evidence/run.json --canonical-dir target/phase10-evidence/phase10-canonical --sanitizer-dir target/phase10-evidence/phase10-sanitizer && cargo xtask provenance check && just markdown-check</automated></verify>
  <acceptance-criteria>Fresh independent downloads validate every authority/content invariant; canonical and sanitizer agree on the complete semantic manifest; TESTING records immutable procedure; any stale/mixed/unsafe/incomplete input blocks.</acceptance-criteria>
  <done>A complete current same-run D1 authority set is validated before promotion.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Local git state to remote CI dispatch | An uncommitted, stale, or wrong SHA could be tested and mistaken for current code. |
| GitHub API/archive data to local authority | Failed, mixed, substituted, expired, or unsafe artifacts can impersonate proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-31-01 | Spoofing | head/run identity | mitigate | Push/fetch SHA equality, zero-before/exactly-one-after cardinality, fresh API capture. |
| T-10-31-02 | Tampering | downloaded archives | mitigate | Safe pre-extraction inspection and API/archive/payload hash recomputation. |
| T-10-31-03 | Repudiation | dispatch/acquisition | mitigate | Immutable run/job/artifact URLs, IDs, timestamps, and summary. |
| T-10-31-04 | Denial of service | wait/download/archive | mitigate | Workflow timeouts, bounded polling, artifact size/compression/file limits. |
| T-10-31-05 | Information disclosure | CI/logs | mitigate | No secret echo; bounded stderr and semantic artifact allowlist. |
| T-10-31-06 | Elevation of privilege | archive extraction | mitigate | Isolated target root, no traversal/links/special files/collisions. |
| T-10-31-07 | Tampering | partial authority | mitigate | No promotion edit until both artifacts and complete same-run manifest validate. |
</threat-model>

<verification>
Prove SHA/run cardinality, terminal success, safe fresh download, locked stack, and complete exact-ref validation.
</verification>

<success-criteria>
- Exactly one current D1 run is dispatched and succeeds.
- Both distinct artifacts derive from that run and SHA.
- Independent exact-ref validation passes before ledger mutation.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-31-SUMMARY.md`.</output>
