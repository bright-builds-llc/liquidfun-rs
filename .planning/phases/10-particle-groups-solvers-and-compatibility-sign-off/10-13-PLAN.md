---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "13"
type: execute
wave: 4
depends_on: ["10-07", "10-12"]
files_modified:
  - crates/liquidfun/src/particle/storage/mutation.rs
autonomous: true
requirements: [PART-10, PART-11, TEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Join preserves group A and every particle ID, rotates B to end then A before B, adds cross-threshold constraints only, unions flags, and invalidates B last."
    - "Join failure exposes no rotation, topology, flag, cache, identity, or lifecycle effect."
  artifacts:
    - path: crates/liquidfun/src/particle/storage/mutation.rs
      provides: "Exact validated join transaction"
      contains: "plan_join"
    - path: crates/liquidfun/src/particle/storage/mutation.rs
      provides: "Private join transaction with co-located ordering, identity, topology, and rollback unit evidence"
  key_links:
    - from: crates/liquidfun/src/particle/storage/mutation.rs
      to: crates/liquidfun/src/particle/topology/constraints.rs
      via: "cross-boundary connection filter after rotation"
      pattern: "threshold|cross"
---

<objective>
Implement the exact group join transaction below ParticleStorage.

Purpose: Preserve D-06/D-11/D-14 identity, rotation, and historical topology semantics.
Output: Source-ordered join planner/commit and focused tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/storage/mutation.rs
@crates/liquidfun/src/particle/storage/permutation.rs
@crates/liquidfun/src/particle/topology/constraints.rs
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement source-ordered join with cross-only topology</name>
  <files>crates/liquidfun/src/particle/storage/mutation.rs</files>
  <read-first>crates/liquidfun/src/particle/storage/mutation.rs — operation-specific group mutation candidates; crates/liquidfun/src/particle/storage/permutation.rs — full-record stable permutation/remap; crates/liquidfun/src/particle/topology/constraints.rs — append/preserve topology contracts; crates/liquidfun/src/particle/storage/group.rs — group flags, ranges, and cache invalidation; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — lines 1111-1162 `JoinParticleGroups` and `RotateBuffer`; third_party/liquidfun/liquidfun/Box2D/Unittests/Function/FunctionTests.cpp — join and empty-group cases.</read-first>
  <behavior>
    - "Validate distinct live same-system A/B before effects."
    - "Rotate B to system end, A immediately before B, refresh contacts, generate only contacts crossing the old threshold, assign B members to A, union public/internal required flags, extend A, invalidate B last."
    - "Historical A/A and B/B pair/triad order/rest values survive unchanged."
  </behavior>
  <action>Implement `plan_join(group_a, group_b)` on a copied candidate using the exact sequence above. Preserve A's record/ID and every ParticleId; use checked two-rotation permutations; invoke the cross-threshold connection filter only after contact refresh state is available; stable-sort/dedup only the newly appended topology at the source operation; invalidate B's shell/association as the final commit effect. Schedule depth/cache invalidation at the pinned flag/range points. Add co-located `#[cfg(test)]` tests with interleaved other groups, empty A/B, historical pairs/triads, flag unions, cross-only candidates, stale/wrong-system handles, capacity failures, and exact rollback snapshots; do not widen private mutation APIs. Run the ordered Rust gate before commit.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::storage::mutation::tests::join</automated></verify>
  <acceptance-criteria>Tests assert exact post-join stable-ID order and A identity; B is stale only after success; historical topology bytes/order persist; only cross-threshold records are added; all injected failures have no diff.</acceptance-criteria>
  <done>The storage join transaction exactly preserves identity/history while adding only source-required cross constraints.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Two group handles to one mutation | Cross-system/stale/aliased IDs can corrupt membership. |
| Rotation/topology candidate to commit | Partial remap can leave constraints targeting wrong particles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-13-01 | Spoofing | group A/B | mitigate | Validate distinct live same-world/same-system identities first. |
| T-10-13-02 | Tampering | rotations/remaps | mitigate | Build and validate complete two-rotation candidate before commit. |
| T-10-13-03 | Denial of service | cross connections | mitigate | Bound refreshed contacts and candidate topology by storage/protocol maxima. |
| T-10-13-04 | Repudiation | identity/history | mitigate | Exact stable-ID and rest-byte tests record before/after semantics. |
| T-10-13-05 | Tampering | panic/rollback | mitigate | Final invalidation and all effects occur in one no-fail commit. |
| T-10-13-06 | Tampering | unsafe/FFI boundary | accept | No unsafe/FFI join path. |
</threat-model>

<verification>
Run join, permutation, topology, and ordered Rust gates.
</verification>

<success-criteria>
- A and all particles retain identity.
- B invalidates last.
- Rotation and cross-only topology are exact.
- Failures are effect-free.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-13-SUMMARY.md`.</output>
