---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "14"
type: execute
wave: 5
depends_on: ["10-12", "10-13"]
files_modified:
  - crates/liquidfun/src/particle/storage/mutation.rs
  - crates/liquidfun/src/particle/topology/connectivity.rs
autonomous: true
requirements: [PART-10, PART-11, TEST-01, TEST-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Split uses contact-order union by longer list, tie-retains list A, preserves original ID for the first longest component, and allocates later IDs in component-source order."
    - "Every particle ID and historical pair/triad rest record survives the equivalent row permutation and retarget."
  artifacts:
    - path: crates/liquidfun/src/particle/topology/connectivity.rs
      provides: "Pinned split component planner"
      contains: "first_longest"
    - path: crates/liquidfun/src/particle/topology/connectivity.rs
      provides: "Private split connectivity with co-located ties, zombies, intermingling, identity, topology, and rollback unit evidence"
  key_links:
    - from: crates/liquidfun/src/particle/storage/mutation.rs
      to: crates/liquidfun/src/particle/topology/connectivity.rs
      via: "contact-order component plan"
      pattern: "split|component"
---

<objective>
Implement exact connected-component split semantics with stable public identity.

Purpose: Close D-07/D-14 without regenerating historical constraints or exposing dense index churn.
Output: Connectivity planner, split candidate/commit, and property-backed integration tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/storage/mutation.rs
@crates/liquidfun/src/particle/topology/constraints.rs
@reference/artifacts/phase10/group-topology-witnesses.json
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp
@third_party/liquidfun/liquidfun/Box2D/Unittests/Function/FunctionTests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pinned connectivity and split transaction</name>
  <files>crates/liquidfun/src/particle/topology/connectivity.rs, crates/liquidfun/src/particle/storage/mutation.rs</files>
  <read-first>reference/artifacts/phase10/group-topology-witnesses.json — Plan 10-12 split metadata and degenerate-case decisions; reference/artifacts/phase10/group-topology-witnesses.provenance.json — Plan 10-12 pinned evidence identity; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `SplitParticleGroup`, `ParticleListNode`, `CreateParticleGroupsFromParticleList`, and `UpdatePairsAndTriadsWithParticleList`; third_party/liquidfun/liquidfun/Box2D/Unittests/Function/FunctionTests.cpp — `SplitParticleGroupInterminglingWithOtherGroups`; crates/liquidfun/src/particle/storage/mutation.rs — split mutation candidate; crates/liquidfun/src/particle/storage/permutation.rs — full-record permutation/remap; crates/liquidfun/src/identity.rs — Phase 9 stable semantic ID model.</read-first>
  <behavior>
    - "Scan same-group contacts in contact order; union longer linked list, ties retain list A."
    - "Choose first longest source component; merge zombies into it; original group ID survives there."
    - "Later groups use component-source order and probed source metadata defaults."
    - "Historical pair/triad records are retargeted, not regenerated."
  </behavior>
  <action>Consume the mandatory pinned `split_created_metadata` outcome from Plan 10-12 verbatim; the probe is not optional and this task must fail if the artifact/provenance is absent or mismatched. Implement the pure connectivity planner and split candidate using the exact rules above, including the recorded later-component strength/transform/user-data/default state. Preflight all new group/diagnostic IDs, associations, journal bounds, row permutation, group ranges, and topology retargets before commit. Preserve ParticleIds and every pair/triad rest field/order; do not call general topology regeneration. Add focused and 128-case bounded properties in co-located `#[cfg(test)]` modules for first-longest ties, zombies, intermingled other groups, component order, metadata, IDs, historical topology, and invalid-candidate rollback; do not widen private connectivity/mutation APIs. Run the exact ordered Rust gate before commit.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>PROPTEST_CASES=128 cargo test -p liquidfun --all-features particle::topology::connectivity</automated></verify>
  <acceptance-criteria>Exact tie/contact-order fixtures pass; original ID belongs to first longest component; later IDs follow source component order; zombies merge into survivor; all ParticleIds/rest bytes survive; 128 properties and rollback cases pass.</acceptance-criteria>
  <done>Split is source-equivalent, stable-ID preserving, and transactionally safe.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Contact graph to component plan | Malformed endpoints/order can fabricate membership or excessive work. |
| Component plan to identity/topology commit | Allocation or retarget failure could expose partial groups. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-14-01 | Tampering | connectivity | mitigate | Validate same-group contact endpoints and use bounded deterministic linked-list union. |
| T-10-14-02 | Spoofing | new/surviving group IDs | mitigate | Preflight arena identities; bind original to first-longest and new IDs to ordered later components. |
| T-10-14-03 | Denial of service | graph/property sequences | mitigate | Linear bounded scans and capped property cases/commands. |
| T-10-14-04 | Repudiation | historical constraints | mitigate | Exact record-byte assertions prove retarget rather than regeneration. |
| T-10-14-05 | Tampering | panic/rollback | mitigate | Candidate owns all new IDs/ranges/topology; one no-fail commit. |
| T-10-14-06 | Tampering | unsafe/FFI boundary | mitigate | Any C++ probe remains an out-of-process/private test artifact; production is safe Rust. |
</threat-model>

<verification>
Run split fixed/property tests, focused private oracle probe if created, and ordered Rust gate.
</verification>

<success-criteria>
- Connectivity/tie/component order matches source.
- Stable identities and historical constraints survive.
- Metadata outcome is evidence-backed.
- Failure is effect-free.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-14-SUMMARY.md`.</output>
