---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "29"
type: execute
wave: 13
depends_on: ["10-28"]
files_modified:
  - tools/xtask/src/main.rs
  - tools/xtask/src/phase10_evidence.rs
  - tools/xtask/tests/phase10_evidence_cli.rs
autonomous: true
requirements: [PART-18, TEST-01, TEST-02, TEST-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "One validator enforces identical semantic/content rules for local D2 and remote exact-ref evidence."
    - "Exact-ref additionally proves one current same-run/SHA Linux x86_64 Rust 1.97.0/Clang 22.1.8 authority set."
    - "Unsafe archives, mixed jobs/artifacts, stale authority, failed logs, or incomplete leaves fail before promotion."
  artifacts:
    - path: tools/xtask/src/phase10_evidence.rs
      provides: "Bounded local/exact-ref Phase 10 evidence validator"
    - path: tools/xtask/tests/phase10_evidence_cli.rs
      provides: "Content, archive, identity, mixing, and stale-authority regressions"
  key_links:
    - from: tools/xtask/src/phase10_evidence.rs
      to: crates/liquidfun-differential/src/rigid_world/phase10/evidence.rs
      via: "shared typed evidence evaluation"
      pattern: "phase10"
---

<objective>
Create one fail-closed local and exact-reference evidence validator.

Purpose: Ensure no later script, workflow, or promotion step can interpret Phase 10 authority more permissively.
Output: `cargo xtask phase10-evidence validate` and adversarial CLI tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@tools/xtask/src/phase9_evidence.rs
@tools/xtask/tests/phase9_evidence_cli.rs
@crates/liquidfun-differential/src/rigid_world/phase10/evidence.rs
@crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/phase10-v1.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate bounded semantic evidence identically in both modes</name>
  <files>tools/xtask/src/main.rs, tools/xtask/src/phase10_evidence.rs, tools/xtask/tests/phase10_evidence_cli.rs</files>
  <read-first>tools/xtask/src/phase9_evidence.rs — validator command parsing, typed errors, safe paths, and hash recomputation; crates/liquidfun-differential/src/rigid_world/phase10/evidence.rs — Phase 10 manifest, typed leaf evaluator, proof-role registry, and exact artifact topology; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-CONTEXT.md — D-25 through D-31 D0, replay, debug/release, and sanitizer proof roles.</read-first>
  <behavior>
    - "Local and exact-ref modes reject the same malformed, incomplete, aliased, substituted, failed, or digest-invalid semantic artifacts."
    - "Validation preflights bounded regular-file sets, paths, links, record sizes/counts/depth, logs, and every recomputed identity."
  </behavior>
  <action>Per D-25, D-31, and D-33, add `cargo xtask phase10-evidence validate --mode local|exact-ref` around a shared typed implementation. Enforce canonical relative containment, exact bounded regular-file sets, no links, schema/version, passing logs, exact five-case manifest, closed leaf/policy sets, per-leaf bindings, inherited families, D0 bytes, replay/debug-release/minimization/first-divergence proof topology, native/oracle/comparison payloads, and all recomputed hashes/identities. Exact-ref must additionally accept run metadata and repeatable deny-run/artifact IDs. Add Arrange/Act/Assert negative tests for every malformed/content/path/resource/alias/substitution class with recomputed outer digests.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p xtask --all-features --test phase10_evidence_cli content</automated></verify>
  <acceptance-criteria>Both modes share semantic validation; every corruption class fails closed after recomputed metadata; safe path/file/count bounds precede extraction or allocation.</acceptance-criteria>
  <done>One implementation owns all Phase 10 evidence-content truth.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce immutable same-run D1 authority</name>
  <files>tools/xtask/src/phase10_evidence.rs, tools/xtask/tests/phase10_evidence_cli.rs</files>
  <read-first>tools/xtask/src/phase9_evidence.rs — exact-ref GitHub run/job/artifact metadata validation and denysets; tools/xtask/src/phase10_evidence.rs — Phase 10 exact job/artifact naming and identity contract; .github/workflows/oracle.yml — canonical platform/toolchain pins and Oracle CI job structure; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-CONTEXT.md — D-25 and D-31 through D-33 branch, SHA, same-run, and provenance rules.</read-first>
  <behavior>
    - "Exact-ref accepts only one workflow_dispatch run at the approved full SHA with exactly one successful canonical and one successful sanitizer job/artifact set."
    - "Mixed run/SHA/job/artifact/toolchain/platform, stale/expired/denylisted artifacts, API/archive digest mismatch, or missing live metadata fails."
  </behavior>
  <action>Per D-32/D-33, require Linux x86_64, Rust 1.97.0, Clang 22.1.8, exact pinned upstream/protocol/schema/generator, one current full SHA, one workflow run, exact successful debug/release/replay/D0/canonical job and fail-fast sanitizer job, distinct exact artifact names/IDs, unexpired live metadata, API archive digests, and equality among run.json, independently captured live snapshots, extracted identities, payloads, and traces. Validate archives before extraction against traversal, absolute paths, links, duplicate/case-collision names, unsupported types, oversized/compressed-bomb entries, and extras. Add denylist, mixing, substitution, missing-job, toolchain, stale-head, expiry, archive, and live-metadata mutation tests.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p xtask --all-features --test phase10_evidence_cli exact_ref</automated></verify>
  <acceptance-criteria>Only a complete current same-run/SHA canonical+sanitizer pair on the locked D1 stack can validate; every stale/mixed/substituted/unsafe case is rejected before any promotion output.</acceptance-criteria>
  <done>The validator encodes the complete immutable D1 authority contract.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Local/downloaded files to evidence validator | Untrusted paths, archives, JSON, logs, and digests enter privileged tooling. |
| GitHub metadata to D1 authority | Stale or mixed run/job/artifact identities can impersonate current evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-29-01 | Spoofing | run/job/artifact identity | mitigate | Live API equality, full SHA, exact cardinality, distinct IDs, denysets, and digest binding. |
| T-10-29-02 | Tampering | archives/payloads | mitigate | Pre-extraction safe-entry validation and recomputed content/API/archive hashes. |
| T-10-29-03 | Repudiation | validation verdict | mitigate | Contextual typed errors and complete validated identity manifest. |
| T-10-29-04 | Denial of service | parser/archive | mitigate | Byte/count/depth/entry/compression bounds before allocation/extraction. |
| T-10-29-05 | Information disclosure | logs/commands | mitigate | Argument-based subprocesses, bounded diagnostics, and no secret/environment echo. |
| T-10-29-06 | Elevation of privilege | archive paths | mitigate | Canonical containment, no links/special files/traversal/case collisions. |
| T-10-29-07 | Tampering | panic/rollback | mitigate | Stage into isolated target directories and publish no authority on error. |
</threat-model>

<verification>
Run the complete adversarial CLI suite in local and exact-ref modes.
</verification>

<success-criteria>
- Local and remote evidence use one semantic validator.
- D1 platform and same-run identity cannot be weakened by callers.
- Archive/parser/resource failures are safe and fail-closed.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-29-SUMMARY.md`.</output>
