---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "20"
type: execute
wave: 6
depends_on: ["10-02", "10-03", "10-04", "10-05", "10-06", "10-12", "10-14"]
files_modified:
  - crates/liquidfun/src/particle/solver/constraints.rs
autonomous: true
requirements: [PART-11, PART-12, PART-13, TEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Elastic triads, spring pairs, and velocity limiting use complete exact topology records."
    - "Degenerate, stale, and inactive topology cannot produce non-finite or misdirected velocity changes."
  artifacts:
    - path: crates/liquidfun/src/particle/solver/constraints.rs
      provides: "S18-S20 topology and velocity-limit kernels"
    - path: crates/liquidfun/src/particle/solver/constraints.rs
      provides: "Crate-private pair/triad kernels with co-located control, activation, and degeneracy unit witnesses"
  key_links:
    - from: crates/liquidfun/src/particle/solver/constraints.rs
      to: crates/liquidfun/src/particle/topology.rs
      via: "full pa,pb,pc,ka,kb,kc,s triads and pair rest geometry"
      pattern: "pa|pb|pc|ka|kb|kc"
---

<objective>
Implement elastic, spring, and velocity-limit constraint passes.

Purpose: Turn exact pair/triad topology into stable solver behavior without reconstructing missing rest data.
Output: S18-S20 kernels and focused finite/deterministic evidence.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/topology.rs
@crates/liquidfun/src/particle/solver/manifest.rs
@crates/liquidfun/src/particle/definition.rs
@crates/liquidfun/src/particle/storage.rs
@reference/artifacts/phase10/group-topology-witnesses.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Solve pair and triad constraints exactly</name>
  <files>crates/liquidfun/src/particle/solver/constraints.rs</files>
  <read-first>reference/artifacts/phase10/group-topology-witnesses.json — Plan 10-12 topology and finite-degenerate outcomes; reference/artifacts/phase10/group-topology-witnesses.provenance.json — pinned source/compiler/result identity; crates/liquidfun/src/particle/storage/lanes.rs — complete pair/triad rest-state records; crates/liquidfun/src/particle/topology/constraints.rs — topology append/preserve/retarget contracts; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-RESEARCH.md — exact S18-S20 formulas and `.25` defaults; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `SolveElastic`, `SolveSpring`, and `LimitVelocity`; crates/liquidfun/src/particle/storage/mutation.rs — join/split/reactive topology retargeting; crates/liquidfun/src/particle/solver.rs — parent solver module boundaries and crate-private sibling-kernel conventions.</read-first>
  <behavior>
    - "Elastic uses stored pa,pb,pc,ka,kb,kc,s; spring uses stored pair rest geometry; limit_velocity clamps only above the exact critical speed."
    - "Inactive flags and empty topology are no-ops; degenerate valid topology remains finite and deterministic."
  </behavior>
  <action>Implement separate crate-private S18, S19, and S20 kernels using stored complete topology records and upstream `f32` operation order. Do not regenerate rest geometry during solve or infer it from current positions. Apply exact elastic/spring strength `.25`, flag gates, critical velocity calculation, and stable topology order. For zero-length pairs and degenerate triads, apply the exact per-case `preserve_source_behavior` or `typed_error` decision from the mandatory Plan 10-12 artifact; fail if that artifact/provenance is absent or mismatched, and never add a broad epsilon or catch-all suppression. Place controls, single-constraint activation, multi-constraint interaction/order, threshold-edge, translated/rotated rest-state, retargeted join/split topology, and every probe-backed finite-degenerate witness in a co-located `#[cfg(test)]` module; do not widen kernel visibility.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::solver::constraints</automated></verify>
  <acceptance-criteria>Elastic consumes every full triad field; spring consumes stored rest geometry; limiting matches the threshold exactly; inactive/degenerate cases are finite and deterministic; topology is never reconstructed in a kernel.</acceptance-criteria>
  <done>Topology constraints and velocity limiting match their exact solver contracts.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Historical topology to live storage rows | Join/split/compaction-retargeted identities select current particles. |
| Rest geometry to numerical constraint kernels | Degenerate or non-finite records could poison velocities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-20-01 | Spoofing | topology identities | mitigate | Resolve stable particle IDs through current ParticleStorage generations before solving. |
| T-10-20-02 | Tampering | rest records | mitigate | Create/retarget only through validated topology constructors and preserve all fields. |
| T-10-20-03 | Denial of service | topology loops | mitigate | Linear bounded traversal over capped pair/triad stores. |
| T-10-20-04 | Repudiation | degenerate handling | mitigate | Explicit named finite/threshold witnesses, not silent catch-all suppression. |
| T-10-20-05 | Tampering | panic/rollback | mitigate | Validate topology/storage alignment before velocity writes; no partial fallible solve. |
| T-10-20-06 | Information disclosure | topology views | mitigate | Public views expose semantic IDs/rest values, never private row indices. |
| T-10-20-07 | Tampering | unsafe/FFI boundary | accept | Native safe Rust kernels only. |
</threat-model>

<verification>
Run constraint tests and inspect that every triad field participates in S18 and no current-position rest reconstruction exists.
</verification>

<success-criteria>
- S18-S20 match exact gates, strengths, and operation order.
- Join/split-retargeted topology remains usable.
- All tested degenerate inputs remain finite without hiding invalid storage.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-20-SUMMARY.md`.</output>
