---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "06"
type: execute
wave: 2
depends_on: ["10-02", "10-03"]
files_modified:
  - crates/liquidfun/src/particle/storage/lanes.rs
  - crates/liquidfun/src/particle/storage/lane_inventory.rs
  - crates/liquidfun/src/particle/storage/solver_state.rs
autonomous: true
requirements: [PART-10, PART-12, PART-13, TEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Static pressure, tensile accumulation, depth, aggregate flags, and pending force state are aligned, deterministic, and candidate-owned."
    - "Every permutation inventory explicitly accounts for every new solver lane."
  artifacts:
    - path: crates/liquidfun/src/particle/storage/solver_state.rs
      provides: "Deterministic optional scratch and aggregate solver state"
      contains: "SolverState"
    - path: crates/liquidfun/src/particle/storage/lane_inventory.rs
      provides: "Executable inventory of all required and optional lanes"
      contains: "static_pressure"
  key_links:
    - from: crates/liquidfun/src/particle/storage/lane_inventory.rs
      to: crates/liquidfun/src/particle/storage/lanes.rs
      via: "complete lane inventory tests"
      pattern: "depth|tensile|static_pressure"
    - from: crates/liquidfun/src/particle/storage/solver_state.rs
      to: crates/liquidfun/src/particle/storage/group.rs
      via: "single dirty/current aggregate group-flag authority refreshed by stable group-record scan"
      pattern: "aggregate_group_flags|group_records"
---

<objective>
Add every aligned scratch/state lane needed by the exact solver graph and make permutation coverage executable.

Purpose: Ensure no kernel can lazily create untracked, non-transactional, or misaligned state.
Output: Candidate-owned solver state, deterministic optional allocation, and a complete lane inventory.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/storage/lanes.rs
@crates/liquidfun/src/particle/storage/lane_inventory.rs
@crates/liquidfun/src/particle/storage/permutation.rs
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic solver scratch and lane inventory coverage</name>
  <files>crates/liquidfun/src/particle/storage/lanes.rs, crates/liquidfun/src/particle/storage/lane_inventory.rs, crates/liquidfun/src/particle/storage/solver_state.rs</files>
  <read-first>crates/liquidfun/src/particle/storage/lanes.rs — particle and topology lane definitions; crates/liquidfun/src/particle/storage/lane_inventory.rs — exhaustive lane inventory contract; crates/liquidfun/src/particle/storage/permutation.rs — stable lane permutation/remap logic and tests; crates/liquidfun/src/particle/definition.rs — `ParticleSystemDef` checked solver coefficients created by Plan 10-02; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.h — lines 1070-1120, accumulation, accumulation2, depth buffers, and aggregate flags; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — corresponding solver scratch allocation sites.</read-first>
  <behavior>
    - "Optional static-pressure f32, tensile Vec2, and depth f32 lanes are absent until their exact gate requires them, then backfilled deterministically to particle count."
    - "Allocation failure/non-finite candidate state occurs before live replacement."
    - "Every new lane participates in length validation, rotation, compaction, destruction, and buffer ownership rules."
  </behavior>
  <action>Create private `SolverState` containing dirty/current aggregate particle flags, the sole dirty/current aggregate group flags, pending system-force marker, and optional aligned `static_pressures`, `tensile_accumulations`, and `depths`. Per D-09, no group record/table/cache may own a second aggregate value: refresh the `SolverState` group aggregate by a stable scan of Plan 10-05 records, update it eagerly or mark it dirty only at pinned mutation points, and test that no other writable aggregate exists. Add source-point allocation helpers that clone/prepare complete vectors before installation; no kernel may allocate directly into live storage. Extend `OwnedLaneBundle`/lane inventory only where ownership belongs there and explicitly state that consumer buffers do not own derived scratch. Update the executable inventory and focused tests so omission of any new lane from count validation/permutation/compaction fails. Preserve depth alignment for views and retained empty groups. Before committing run the exact ordered Rust gate.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::storage::lane_inventory && cargo test -p liquidfun --all-features particle::storage::solver_state</automated></verify>
  <acceptance-criteria>All three optional solver lanes plus particle/group aggregate dirty/current state and pending-force state exist; `SolverState` is the only writable aggregate group-flag authority; inventory tests enumerate the lanes; N/N+1 alignment and lazy backfill tests pass; external buffer teardown does not return derived solver lanes.</acceptance-criteria>
  <done>Every solver scratch value is deterministic, storage-owned, and covered by mutation invariants.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Dynamic flags/count to scratch allocation | Adversarial particle counts or flag changes can trigger large allocations. |
| Scratch candidate to live storage | Partial lane installation can desynchronize rows and topology. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-06-01 | Denial of service | optional lane allocation | mitigate | Checked declared capacity/count and preflight all products before allocation. |
| T-10-06-02 | Tampering | lane commit | mitigate | Prepare complete aligned vectors and replace only after inventory validation. |
| T-10-06-03 | Repudiation | aggregate gates | mitigate | Dirty/current state and pass-trace tests identify exact refresh points. |
| T-10-06-04 | Tampering | panic/rollback | mitigate | Candidate-owned allocation means allocation errors/panics leave live lanes unchanged. |
| T-10-06-05 | Tampering | unsafe/FFI boundary | accept | Safe Vec-backed state only; no unsafe or ABI layout is added. |
</threat-model>

<verification>
Run lane inventory, alignment, buffer teardown, and ordered Rust gates. Review every permutation field list for the three new lanes.
</verification>

<success-criteria>
- All scratch/state is under ParticleStorage.
- Optional allocation is deterministic and bounded.
- The lane inventory fails closed on omissions.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-06-SUMMARY.md`.</output>
