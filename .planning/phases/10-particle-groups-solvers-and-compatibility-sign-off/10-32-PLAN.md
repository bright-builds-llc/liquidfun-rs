---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "32"
type: execute
wave: 16
depends_on: ["10-31"]
files_modified:
  - reference/compatibility.json
  - tools/xtask/src/inventory/validation.rs
  - tools/xtask/tests/inventory_cli.rs
  - COMPATIBILITY.md
  - TESTING.md
  - .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PART-09, PART-10, PART-11, PART-12, PART-13, PART-18, TEST-01, TEST-02, TEST-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Every Phase 10 leaf has a supported, documented-difference, or intentionally-unsupported outcome backed by the validated authority."
    - "Only claims proven by the complete current same-run D1 set are promoted; inherited authority and later-phase boundaries remain truthful."
    - "Generated compatibility output is deterministic and all Rust, evidence, security, schema, documentation, and verifier gates pass."
  artifacts:
    - path: reference/compatibility.json
      provides: "Scoped authoritative Phase 10 outcomes and exact evidence references"
    - path: COMPATIBILITY.md
      provides: "Deterministically generated truthful public report"
    - path: .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-VERIFICATION.md
      provides: "Goal-backward phase completion audit"
  key_links:
    - from: target/phase10-evidence/run.json
      to: reference/compatibility.json
      via: "independently validated exact-ref authority allowlist"
      pattern: "phase10-canonical|phase10-sanitizer"
    - from: reference/compatibility.json
      to: COMPATIBILITY.md
      via: "inventory generate/check only"
      pattern: "@generated"
---

<objective>
Promote only proven Phase 10 outcomes and complete the phase audit.

Purpose: Turn exact semantic evidence into truthful compatibility claims without leaking scope into Phase 11/12.
Output: Fail-closed ledger validation, generated report, full gates, security review, and passed Phase 10 verification.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-31-SUMMARY.md
@reference/compatibility.json
@tools/xtask/src/inventory/validation.rs
@tools/xtask/tests/inventory_cli.rs
@COMPATIBILITY.md
@TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote the closed leaf set from validated authority only</name>
  <files>reference/compatibility.json, tools/xtask/src/inventory/validation.rs, tools/xtask/tests/inventory_cli.rs, COMPATIBILITY.md, TESTING.md</files>
  <read-first>.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-31-SUMMARY.md — validated run, job, artifact, and exact-ref identities; target/phase10-evidence/run.json — immutable authority record; tools/xtask/src/phase10_evidence.rs — closed Phase 10 leaf/results manifest and authority evaluator; reference/compatibility.json — compatibility schema, current Phase 10 rows, and Phase 6-9 authority references; tools/xtask/src/inventory/report.rs — deterministic generated-report ownership; .planning/ROADMAP.md — Phase 11 and Phase 12 deferred boundaries.</read-first>
  <behavior>
    - "Promotion rejects incomplete/mixed/stale/denylisted authority, missing leaves/policies/proofs, failed logs, wrong toolchains, and any claim beyond exact evidence."
    - "Every Phase 10 leaf receives exactly one explicit outcome and supported rows cite only the validated current authority."
  </behavior>
  <action>Per D-26 and D-31 through D-34, first add Arrange/Act/Assert inventory tests that reject every known failed/stale run/artifact, mixed run/SHA/job/artifact pairs, missing/duplicate/unknown leaf or policy, non-Match supported leaf, failed sanitizer/log/D0/replay/debug-release/provenance record, wrong platform/toolchain/upstream/schema, incomplete inherited proof, and any examples/testbed/performance/broad-platform/release claim. Pin validation to the exact 10-31 full SHA/run/job/artifact IDs/API/archive/payload/trace/leaf/policy/manifest digests and immutable TESTING/summary anchors. Update only Phase 10 rows/dimensions actually proven, recording documented-difference or intentionally-unsupported outcomes explicitly where evidence requires them. Preserve Phase 6-9 references and Phase 11/12 pending rows. Never hand-edit COMPATIBILITY.md; generate/check twice and require byte identity.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <markdown-check>Before the task's atomic commit, run `just markdown-check` and require it passes for every changed repository-owned non-GSD Markdown file. Never run mdformat over `.planning/**`.</markdown-check>
  <verify><automated>cargo test -p xtask --all-features --test inventory_cli && cargo xtask inventory generate && cargo xtask inventory check && cargo xtask inventory generate && cargo xtask inventory check && just markdown-check</automated></verify>
  <acceptance-criteria>All negative authority/scope tests pass; every Phase 10 leaf has one truthful outcome; only validated D1 claims are promoted; inherited and deferred claims are unchanged; generated output is byte-idempotent.</acceptance-criteria>
  <done>The compatibility ledger and generated report truthfully reflect the complete exact Phase 10 outcome set.</done>
</task>

<task type="auto">
  <name>Task 2: Run final verification, security, and project-state gates</name>
  <files>reference/compatibility.json, tools/xtask/src/inventory/validation.rs, tools/xtask/tests/inventory_cli.rs, COMPATIBILITY.md, TESTING.md, .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read-first>.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-31-SUMMARY.md — summary chain for Plans 10-01 through 10-31 and their threat-model outcomes; .planning/ROADMAP.md — Phase 10 goal, requirements, plans, and success criteria; .planning/REQUIREMENTS.md — PART-09 through PART-13/PART-18 and TEST-01/TEST-02/TEST-04 traceability; target/phase10-evidence/run.json — exact-ref authority identity; AGENTS.md — exact Rust pre-commit order and repository verification rules; tools/xtask/src/inventory/validation.rs — schema and compatibility dependency gates; tools/xtask/src/provenance.rs — provenance validation gate; tools/xtask/src/phase10_evidence.rs — exact-ref security/path/hash gate; .github/workflows/oracle.yml — workflow lint/contract gate; Justfile — `markdown-check` and upstream read-only recipes.</read-first>
  <action>Run the complete native focused/integration/property suite, five-case corpus, protocol/oracle/comparator mutation tests, local paired evidence, exact-ref validation, inventory/provenance/dependency/schema/workflow/Markdown/upstream read-only/diff checks. Perform ASVS L1 review across public validation/ownership, solver resources/panic rollback, JSONL/parser bounds, C++ isolation, archive/path safety, command injection, secrets, hash/provenance, same-run authority, and generated claims; any high severity blocks. Run a fresh gsd-verifier goal-backward audit and require 10-VERIFICATION.md passes all nine requirements and every D-01 through D-34 decision with no deferred Phase 11/12 claim. Update STATE/ROADMAP through GSD tracking only after the passed audit. Immediately before every commit, with no intervening mutation, run exactly `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`; push once and require clean local/remote SHA equality. Never dispatch another oracle run.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <markdown-check>Before the task's atomic commit, run `just markdown-check` and require it passes for every changed repository-owned non-GSD Markdown file. Never run mdformat over `.planning/**`.</markdown-check>
  <verify><automated>cargo test -p liquidfun --all-features && PROPTEST_CASES=128 cargo test -p liquidfun --all-features --test particle_group_properties && cargo test -p liquidfun-differential --all-features --test phase10_corpus && cargo test -p liquidfun-differential --all-features --test phase10_protocol && cargo test -p liquidfun-differential --all-features --test phase10_oracle && cargo test -p liquidfun-differential --all-features --test phase10_comparator && cargo test -p xtask --all-features --test phase10_evidence_cli && cargo xtask phase10-evidence validate --mode exact-ref --run-json target/phase10-evidence/run.json --canonical-dir target/phase10-evidence/phase10-canonical --sanitizer-dir target/phase10-evidence/phase10-sanitizer && cargo xtask provenance check && cargo xtask inventory check && cargo deny check && node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 10 | jq -e '.blocking==false' && actionlint .github/workflows/oracle.yml && just markdown-check && git diff --exit-code -- third_party/liquidfun && git diff --check && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features</automated></verify>
  <acceptance-criteria>Every command passes; ASVS review has no high finding; verifier passes all requirements/decisions; tracking marks Phase 10 complete only after proof; final branch is clean and local HEAD equals remote; no second dispatch or later-phase claim exists.</acceptance-criteria>
  <done>Phase 10 is fully verified, truthfully reported, securely evidence-bound, committed, and synchronized.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Exact-ref evidence to compatibility authority | Incomplete, stale, mixed, or forged proof could overstate public maturity. |
| Authority JSON to generated report/project tracking | Manual or overbroad claims could escape the exact evidence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-32-01 | Spoofing | authority references | mitigate | Exact allowlisted SHA/run/job/artifact/digest/summary anchors and denytests. |
| T-10-32-02 | Tampering | compatibility/report | mitigate | JSON authority validation plus deterministic generate/check; no hand editing. |
| T-10-32-03 | Repudiation | phase completion | mitigate | Closed leaf outcomes, full command evidence, ASVS review, and fresh verifier report. |
| T-10-32-04 | Denial of service | final gates | mitigate | Bounded focused suites/artifacts and no repeated remote dispatch. |
| T-10-32-05 | Information disclosure | published evidence/docs | mitigate | Immutable public semantic references only; no secrets/local paths/raw logs. |
| T-10-32-06 | Elevation of privilege | CI/archive/shell chain | mitigate | Reuse validated safe extraction/argument paths and least-permission workflow. |
| T-10-32-07 | Tampering | panic/rollback/unsafe/FFI claims | mitigate | Native regression/transaction tests, sanitizer artifact, C++ process isolation, and ASVS review. |
</threat-model>

<verification>
Require exact-ref revalidation, scoped inventory promotion, double deterministic generation, full test/evidence/security/schema/documentation gates, fresh verifier pass, and final clean remote equality.
</verification>

<success-criteria>
- Every Phase 10 leaf has one explicit evidenced outcome.
- Only the complete current same-run D1 authority supports promoted claims.
- All nine requirements and D-01 through D-34 are verified.
- Phase 11/12 scope remains unclaimed.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-32-SUMMARY.md`.</output>
