---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "19"
type: execute
wave: 4
depends_on: ["10-02", "10-04", "10-05", "10-06", "10-12"]
files_modified:
  - crates/liquidfun/src/particle/solver/pressure.rs
autonomous: true
requirements: [PART-12, PART-13, TEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Static pressure, pressure, damping, and extra damping execute as four distinct passes with exact gates."
    - "Powder, tensile, body-contact, and static-pressure interactions alter the intended pressure/damping terms only."
  artifacts:
    - path: crates/liquidfun/src/particle/solver/pressure.rs
      provides: "S14-S17 pressure and damping kernels"
    - path: crates/liquidfun/src/particle/solver/pressure.rs
      provides: "Crate-private pressure kernels with co-located control and interaction unit witnesses"
  key_links:
    - from: crates/liquidfun/src/particle/solver/pressure.rs
      to: crates/liquidfun/src/particle/definition.rs
      via: "pressure .05 and static pressure strength/relaxation .2"
      pattern: "pressure_strength|static_pressure_strength|static_pressure_relaxation"
---

<objective>
Implement the complete pressure and damping family.

Purpose: Replace the Phase 9 compatibility prefix with exact, independently gated LiquidFun pressure behavior.
Output: S14-S17 kernels with suppression, contact, and coefficient evidence.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/solver.rs
@crates/liquidfun/src/particle/solver/manifest.rs
@crates/liquidfun/src/particle/definition.rs
@crates/liquidfun/src/particle/storage.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement S14-S17 as exact distinct passes</name>
  <files>crates/liquidfun/src/particle/solver/pressure.rs</files>
  <read-first>crates/liquidfun/src/world/particle_coupling.rs — Phase 9 `run_particle_contact_prefix` behavior being superseded; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-RESEARCH.md — exact S14-S17 formulas and source anchors; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `SolvePressure`, `SolveStaticPressure`, `SolveDamping`, and `SolveExtraDamping`; crates/liquidfun/src/particle/storage/lanes.rs — particle-contact and body-contact records; crates/liquidfun/src/particle/definition.rs — checked pressure/damping coefficient validation; crates/liquidfun/src/particle/solver/manifest.rs — S14-S17 distinct pass entries and gates.</read-first>
  <behavior>
    - "Static-pressure iterations converge through the configured strength/relaxation; pressure and damping retain exact contact traversal."
    - "Powder/tensile suppression, body-contact damping, and extra-damping gates are demonstrated independently."
  </behavior>
  <action>Write the witnesses before implementation without committing an intentionally failing state, then implement crate-private S14 static_pressure, S15 pressure, S16 damping, and S17 extra_damping kernels. Match upstream `f32` statement order, critical velocity/pressure limits, contact/body-contact traversal, static-pressure iteration semantics, and exact flag suppression. Consume the checked definition coefficients instead of literals, including pressure `.05` and static pressure strength/relaxation `.2`. Keep the existing Phase 9 prefix unchanged until plan 10-22 replaces it atomically. Place zero-iteration, zero-strength, powder/tensile, body-contact extra-damping, empty/no-contact, and deterministic multi-contact-order witnesses in a co-located `#[cfg(test)]` module; do not widen kernel visibility for integration tests. Create one all-green atomic task commit only after focused tests and the ordered Rust gate pass.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::solver::pressure</automated></verify>
  <acceptance-criteria>S14-S17 are individually named and witnessed; Phase 9 prefix is not double-applied; exact coefficients/gates/order match research; controls are byte-identical.</acceptance-criteria>
  <done>The pressure/damping family is complete and ready for one-shot orchestration replacement.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Iteration counts/configuration to static-pressure loop | Public values control repeated numerical work. |
| Contact records to velocity/pressure mutation | Derived rows and normals influence solver-visible state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-19-01 | Denial of service | static-pressure iterations | mitigate | Validate against the project bound before stepping and use fixed bounded loops. |
| T-10-19-02 | Tampering | pressure coefficients | mitigate | Finite/range validation and immutable checked definitions. |
| T-10-19-03 | Tampering | contact indices | mitigate | Bounds-check storage-derived records and preserve one storage authority. |
| T-10-19-04 | Repudiation | suppression/gating | mitigate | Named control/activation/interaction witnesses for every pass. |
| T-10-19-05 | Tampering | panic/rollback | mitigate | Reject invalid configuration before stepping; kernels operate only on validated aligned lanes. |
| T-10-19-06 | Information disclosure | scratch pressure | accept | Private numerical scratch contains no sensitive information. |
| T-10-19-07 | Tampering | unsafe/FFI boundary | accept | Native safe Rust only. |
</threat-model>

<verification>
Run pressure-family tests and confirm no test or implementation invokes the old prefix and new kernels together.
</verification>

<success-criteria>
- Static pressure, pressure, damping, and extra damping are exact separate kernels.
- Every gate and interaction has a negative and positive witness.
- Configuration bounds prevent unbounded iteration.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-19-SUMMARY.md`.</output>
