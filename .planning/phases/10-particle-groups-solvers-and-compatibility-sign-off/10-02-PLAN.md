---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun/src/particle/definition.rs
autonomous: true
requirements: [PART-12, PART-13, TEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Every Phase 10 solver coefficient is configured through a finite checked builder and has the pinned source default."
    - "Configuration bits are stable public observations and invalid values leave the original definition unchanged."
  artifacts:
    - path: crates/liquidfun/src/particle/definition.rs
      provides: "Checked Phase 10 solver coefficients and exact defaults"
      contains: "pressure_strength"
  key_links:
    - from: crates/liquidfun/src/particle/definition.rs
      to: third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.h
      via: "exact default constants and field semantics"
      pattern: "0\\.05|0\\.25|0\\.2|1\\.0|0\\.5"
---

<objective>
Complete `ParticleSystemDef` with the exact checked coefficients consumed by S07-S19.

Purpose: Prevent solver plans from hardcoding constants or accepting unchecked non-finite configuration.
Output: Exact pinned defaults, accessors, builders, typed errors, and focused bit-level tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/definition.rs
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.h
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exact checked solver coefficients</name>
  <files>crates/liquidfun/src/particle/definition.rs</files>
  <read-first>crates/liquidfun/src/particle/definition.rs — `ParticleSystemDef`, all current validation errors, defaults, and tests; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.h — lines 153-265, `b2ParticleSystemDef` coefficient fields/defaults; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — `SolveViscous`, `SolveRepulsive`, `SolvePowder`, `SolveTensile`, `SolveSolid`, `SolveColorMixing`, `SolvePressure`, `SolveStaticPressure`, `SolveDamping`, `SolveExtraDamping`, `SolveElastic`, and `SolveSpring` coefficient use.</read-first>
  <behavior>
    - "Defaults are pressure 0.05, elastic 0.25, spring 0.25, viscous 0.25, surface-tension pressure 0.2, surface-tension normal 0.2, repulsive 1.0, powder 0.5, ejection 0.5, static-pressure strength 0.2, static-pressure relaxation 0.2, color mixing 0.5."
    - "NaN and infinities are rejected for every coefficient."
    - "Negative values are rejected; zero remains accepted only where the upstream checked contract uses zero as disable/no-effect."
  </behavior>
  <action>Extend `ParticleSystemDef` with private fields, public accessors, and `with_*` checked builders for all twelve named coefficients. Preserve the exact decimal tokens and `f32::to_bits()` values from the pinned constructor. Add specific `ParticleSystemDefError` variants so diagnostics name the rejected coefficient. Validate before assigning so a failed builder returns no mutated candidate. Do not retain `PRESSURE_STRENGTH` or any new magic coefficient in coupling modules. Add one-concern Arrange/Act/Assert tests for every default and table-driven finite/negative rejection. Before committing run `cargo fmt --all`, then warning-denied all-target/all-feature Clippy, all-target/all-feature build, and all-feature tests in that order.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::definition</automated></verify>
  <acceptance-criteria>All twelve field names and builders exist; default tests assert the exact `to_bits()` value for 0.05/0.25/0.2/1.0/0.5 as applicable; each non-finite input returns a coefficient-specific error; focused tests exit 0.</acceptance-criteria>
  <done>Every solver kernel can obtain its source-default coefficient from one validated definition authority.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Consumer numeric configuration to solver state | Untrusted floats can poison every particle lane or trigger unbounded relaxation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-02-01 | Tampering | coefficient builders | mitigate | Reject non-finite/invalid ranges before assignment and preserve exact checked candidates. |
| T-10-02-02 | Denial of service | static-pressure configuration | mitigate | Retain the existing positive i32-bounded iteration count and finite relaxation/strength checks. |
| T-10-02-03 | Repudiation | default values | mitigate | Assert exact f32 bits from the pinned constructor, not approximate decimal equality. |
| T-10-02-04 | Tampering | unsafe/FFI boundary | accept | No unsafe/FFI is added; coefficient transport remains ordinary initialized Rust values. |
</threat-model>

<verification>
Run coefficient unit tests and the ordered Rust gate. Grep the production tree after later integration to ensure no duplicate Phase 9 `PRESSURE_STRENGTH` remains.
</verification>

<success-criteria>
- Twelve exact defaults and checked builders exist.
- Invalid configuration is typed and effect-free.
- Exact-bit tests prevent default drift.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-02-SUMMARY.md`.</output>
