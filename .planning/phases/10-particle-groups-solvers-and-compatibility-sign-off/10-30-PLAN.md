---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "30"
type: execute
wave: 14
depends_on: ["10-29"]
files_modified:
  - scripts/phase10-evidence.sh
  - .github/workflows/oracle.yml
  - Justfile
  - TESTING.md
autonomous: true
requirements: [PART-18, TEST-01, TEST-02, TEST-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Local D2 canonical and sanitizer artifacts are produced identity-last and validate without being promotable."
    - "One Oracle CI dispatch produces the complete canonical and sanitizer D1 pair from the same commit/run."
    - "The documented commands are thin, visible, fail-fast, and use the reusable validator."
  artifacts:
    - path: scripts/phase10-evidence.sh
      provides: "Identity-last bounded Phase 10 artifact generation"
    - path: .github/workflows/oracle.yml
      provides: "Same-run canonical and sanitizer artifact jobs"
    - path: TESTING.md
      provides: "D0-D3, local, dispatch, exact-ref, and promotion procedure"
  key_links:
    - from: .github/workflows/oracle.yml
      to: scripts/phase10-evidence.sh
      via: "same-run canonical/sanitizer jobs"
      pattern: "phase10-evidence"
---

<objective>
Wire validated Phase 10 evidence production into local and canonical CI workflows.

Purpose: Make complete artifacts reproducible before dispatch while preserving the D2/D1 authority boundary.
Output: Fail-fast runner, same-run workflow jobs, thin command facade, and exact operating documentation.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@scripts/phase9-evidence.sh
@.github/workflows/oracle.yml
@Justfile
@TESTING.md
@tools/xtask/src/phase10_evidence.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce and validate fresh local D2 artifacts</name>
  <files>scripts/phase10-evidence.sh, Justfile, TESTING.md</files>
  <read-first>scripts/phase9-evidence.sh — Phase 9 fail-fast identity-last runner; tools/xtask/src/phase10_evidence.rs — Phase 10 corpus generation and validator CLI; tools/reference/CMakePresets.json — oracle-debug, oracle-release, and oracle-asan-ubsan presets; AGENTS.md — Bash/Zsh and Rust pre-commit requirements; Justfile — thin recipe conventions; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-CONTEXT.md — D-25 through D-33 D0-D3 authority and promotion rules.</read-first>
  <action>Per D-31 through D-33, add a `set -euo pipefail` Phase 10 runner that executes the same request bytes through debug/release native/oracle, replay, D0, inherited proof, and comparison paths, writes bounded case payloads first, computes every digest/trace, and writes artifact identity/manifest last. Add thin visible just recipes. Document D0 byte identity, local macOS D2 non-promotion, canonical D1 requirements, sanitizer fail-fast behavior, exact-ref acquisition, and promotion gates. Generate fresh local canonical and ASan/UBSan directories, run `phase10-evidence validate --mode local`, and retain Phase 9 artifacts/authority unchanged. Do not dispatch or edit compatibility claims.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <markdown-check>Before the task's atomic commit, run `just markdown-check` and require it passes for every changed repository-owned non-GSD Markdown file. Never run mdformat over `.planning/**`.</markdown-check>
  <verify><automated>LIQUIDFUN_PHASE10_ORACLE_MODE=canonical bash scripts/phase10-evidence.sh canonical target/phase10-evidence-local/canonical && UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 LIQUIDFUN_PHASE10_ORACLE_MODE=sanitizer bash scripts/phase10-evidence.sh sanitizer target/phase10-evidence-local/sanitizer && cargo xtask phase10-evidence validate --mode local --canonical-dir target/phase10-evidence-local/canonical --sanitizer-dir target/phase10-evidence-local/sanitizer</automated></verify>
  <acceptance-criteria>Fresh local paired artifacts validate completely; identities are written last; sanitizer failure is fatal; docs label local output D2/non-promotable; no compatibility authority changes.</acceptance-criteria>
  <done>The complete Phase 10 pipeline is locally proven without overstating authority.</done>
</task>

<task type="auto">
  <name>Task 2: Add one same-run canonical D1 workflow pair</name>
  <files>.github/workflows/oracle.yml, TESTING.md</files>
  <read-first>.github/workflows/oracle.yml — pinned actions, toolchains, permissions, retention, and Phase 9 jobs that must remain unchanged; tools/xtask/src/phase10_evidence.rs — exact Phase 10 validator job/artifact naming contract; AGENTS.md — Rust pre-commit requirements and repository CI-cost constraints; .planning/PROJECT.md — canonical platform/toolchain and expensive-lane policy; TESTING.md — current Oracle CI/evidence documentation.</read-first>
  <action>Per D-25 and D-32/D-33, extend the existing Oracle CI workflow, not a parallel workflow. On one `workflow_dispatch` run at one SHA, run Linux x86_64 Rust 1.97.0/Clang 22.1.8 canonical debug/release/replay/D0 corpus generation and a distinct fail-fast ASan/UBSan generation; validate each before uploading exact named artifacts with SHA-256 metadata and finite retention. Pin actions by full SHA, use least permissions, quote fixed arguments, and prevent secrets/artifact contents from entering logs. Keep expensive work manual/scheduled and Phase 9 jobs unchanged. Add workflow-contract tests/documented expected job/artifact names consumed by exact-ref validation.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <markdown-check>Before the task's atomic commit, run `just markdown-check` and require it passes for every changed repository-owned non-GSD Markdown file. Never run mdformat over `.planning/**`.</markdown-check>
  <verify><automated>actionlint .github/workflows/oracle.yml && cargo test -p xtask --all-features --test phase10_evidence_cli workflow_contract && just markdown-check</automated></verify>
  <acceptance-criteria>Workflow lint/tests pass; one run yields exact canonical and sanitizer jobs/artifacts at one SHA on the locked stack; action pins/permissions/retention are explicit; Phase 9 behavior is unchanged.</acceptance-criteria>
  <done>One future dispatch can produce the only admissible Phase 10 D1 authority shape.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Shell/workflow arguments to filesystem/processes | Paths and modes control artifact creation and foreign executables. |
| CI jobs/artifacts to later exact-ref validation | Workflow configuration establishes authority names and provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-30-01 | Tampering | evidence runner | mitigate | Fixed modes/paths, pipefail, identity-last output, reusable validator. |
| T-10-30-02 | Spoofing | CI toolchain/artifacts | mitigate | Pinned stack/actions, exact job/artifact names, full SHA metadata. |
| T-10-30-03 | Repudiation | D1/D2 classification | mitigate | Required platform/toolchain/mode identity and explicit documentation. |
| T-10-30-04 | Denial of service | CI workload | mitigate | Bounded corpus, manual/scheduled expensive lanes, timeouts and retention. |
| T-10-30-05 | Information disclosure | workflow logs | mitigate | Least permissions, no secret echo, bounded diagnostics/artifacts. |
| T-10-30-06 | Elevation of privilege | shell interpolation/actions | mitigate | Argument arrays/fixed inputs and full-SHA action pins. |
| T-10-30-07 | Tampering | partial generation | mitigate | Staging plus identity-last publication; sanitizer/nonzero failures abort upload. |
</threat-model>

<verification>
Run local paired generation/validation, workflow lint/contract tests, Markdown checks, and inspect action pins/permissions.
</verification>

<success-criteria>
- Local D2 output is complete but structurally non-promotable.
- CI defines one same-run D1 canonical+sanitizer authority set.
- Existing Phase 9 evidence remains intact.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-30-SUMMARY.md`.</output>
