---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "04"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - crates/liquidfun/src/particle.rs
  - crates/liquidfun/src/particle/solver.rs
  - crates/liquidfun/src/particle/solver/manifest.rs
autonomous: true
requirements: [PART-12, PART-13, TEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "One private versioned manifest contains O01-O05 and S01-S26 exactly once in pinned order with exact gates and multiplicity."
    - "Unknown, missing, duplicate, or reordered pass declarations fail validation before dispatch."
  artifacts:
    - path: crates/liquidfun/src/particle/solver/manifest.rs
      provides: "phase10-pass-graph-v1 authority"
      contains: "phase10-pass-graph-v1"
    - path: crates/liquidfun/src/particle/solver.rs
      provides: "Private manifest validation and test-only tracing seam"
      contains: "PassId"
  key_links:
    - from: crates/liquidfun/src/particle/solver.rs
      to: crates/liquidfun/src/particle/solver/manifest.rs
      via: "single ordered manifest iterator"
      pattern: "PASS_GRAPH|manifest"
---

<objective>
Create the private executable authority for the exact Phase 10 pass graph.

Purpose: Lock D-16 through D-18 and D-27 before any kernel is wired, preventing duplicated Phase 9 pressure/damping or ad hoc scheduling.
Output: Closed pass IDs, gates, multiplicity, order validator, and private test tracing.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-RESEARCH.md
@crates/liquidfun/src/particle.rs
@crates/liquidfun/src/world/particle_coupling.rs
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transcribe and validate phase10-pass-graph-v1</name>
  <files>crates/liquidfun/src/particle.rs, crates/liquidfun/src/particle/solver.rs, crates/liquidfun/src/particle/solver/manifest.rs</files>
  <read-first>crates/liquidfun/src/particle.rs — particle module boundaries and exports; crates/liquidfun/src/world/particle_coupling.rs — `run_particle_contact_prefix` and Phase 9 coupling order; .planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-RESEARCH.md — "Exact `phase10-pass-graph-v1`" tables for O01-O05 and S01-S26; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — lines 2973-3117, `b2ParticleSystem::Solve`; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.h — solver gates, `m_allParticleFlags`, and `m_allGroupFlags` constants/fields.</read-first>
  <behavior>
    - "Outer order is O01 lifetime, O02 zombie_compaction, O03 refresh_particle_flags, O04 refresh_group_flags, O05 pause_gate."
    - "Per-iteration order is exactly S01 particle_contacts, S02 body_contacts, S03 weight, S04 solid_depth, S05 reactive_topology, S06 force, S07 viscous, S08 repulsive, S09 powder, S10 tensile, S11 solid, S12 color_mixing, S13 gravity, S14 static_pressure, S15 pressure, S16 damping, S17 extra_damping, S18 elastic, S19 spring, S20 limit_velocity, S21 rigid_damping, S22 barrier, S23 collision, S24 rigid, S25 wall, S26 integrate."
    - "Empty returns before O01; paused nonempty systems execute O01-O05 and no S pass."
  </behavior>
  <action>Create private `PassId`, `PassScope`, `PassGate`, `PassMultiplicity`, and `PassDescriptor` types and one constant `phase10-pass-graph-v1` array containing the exact IDs/order above. Encode gates explicitly: expiration lane; zombie aggregate; dirty particle/group aggregates; pause terminator; unconditional S01/S02/S03/S13/S15/S16/S20/S23/S26; needs-depth S04; aggregate flag/group gates for all other passes including static-pressure as the sole extra-damping flag. Add a validator that compares the complete closed sequence and rejects unknown/missing/duplicate/reordered descriptors. Add test-only trace entries `(pass_id, particle_iteration)` behind `cfg(test)` or unpublished `differential-internals`, never `pub`. Write mutation tests that delete, duplicate, swap, or add a pass and assert stable validation errors. Declare only the private solver module in `particle.rs`. Do not dispatch kernels yet. Before committing run the exact ordered Rust gate.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::solver::manifest</automated></verify>
  <acceptance-criteria>The manifest has 31 unique entries (5 outer + 26 substep); tests assert the complete ordered string list above; delete/duplicate/swap/unknown mutations fail; `cargo doc` exposes no `PassId` or pass trace.</acceptance-criteria>
  <done>One machine-checkable private pass graph owns every Phase 10 solver admission and order decision.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Static pass declaration to solver dispatch | Missing or reordered passes can silently change physics. |
| Test instrumentation to public API | Private pass IDs must not become consumer contracts or substitute for semantic evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-04-01 | Tampering | pass manifest | mitigate | Closed enum and exact 31-entry validator reject unknown/missing/duplicate/reordered entries. |
| T-10-04-02 | Repudiation | pass trace | mitigate | Native co-located tests record exact pass and iteration from the same manifest and compare exact order/multiplicity; traces never enter shared D0/D1 protocol evidence. |
| T-10-04-03 | Information disclosure | test tracing | mitigate | Keep all tracing crate-private and test/unpublished-feature gated. |
| T-10-04-04 | Denial of service | iteration multiplicity | mitigate | Reuse checked `StepConfiguration::particle_iterations` maximum; no dynamic scheduler or recursion. |
| T-10-04-05 | Tampering | unsafe/FFI boundary | accept | Manifest is safe Rust data; no FFI or unsafe dispatch exists. |
</threat-model>

<verification>
Run manifest mutation tests, rustdoc visibility checks, and the ordered Rust gate. Compare the literal ordered IDs to pinned `Solve`.
</verification>

<success-criteria>
- Exactly 31 pass descriptors exist in source order.
- Every gate and multiplicity is closed and test-covered.
- Test tracing is private and cannot replace cross-engine semantic evidence.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-04-SUMMARY.md`.</output>
