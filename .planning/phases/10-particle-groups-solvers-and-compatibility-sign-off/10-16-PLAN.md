---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "16"
type: execute
wave: 7
depends_on: ["10-10", "10-14", "10-15"]
files_modified:
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/src/world/particle_object.rs
  - crates/liquidfun/tests/particle_group_mutation.rs
autonomous: true
requirements: [PART-09, PART-10, PART-11, TEST-02, TEST-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "Consumers can join, split, set group flags, and destroy groups through validated stable handles with exact identity outcomes."
    - "Locked, poisoned, stale, wrong-world/system, capacity, and topology failures are typed and transactional."
  artifacts:
    - path: crates/liquidfun/src/world/particle_object.rs
      provides: "Supported public group mutation APIs"
      contains: "join_particle_groups"
    - path: crates/liquidfun/tests/particle_group_mutation.rs
      provides: "Black-box mutation and identity workflows"
  key_links:
    - from: crates/liquidfun/src/world/particle_object.rs
      to: crates/liquidfun/src/particle/storage/mutation.rs
      via: "validated world shell to operation-specific candidates"
      pattern: "plan_join|plan_split"
---

<objective>
Wire complete group mutation semantics into the public World API.

Purpose: Deliver PART-09/PART-10 consumer workflows without leaking internal topology or duplicating authority.
Output: Public join/split/flags/destruction methods, typed errors, and black-box/property integration evidence.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/world/object.rs
@crates/liquidfun/src/world/particle_object.rs
@crates/liquidfun/src/particle/storage/mutation.rs
@crates/liquidfun/tests/particle_groups.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose stable-handle group mutations</name>
  <files>crates/liquidfun/src/world/object.rs, crates/liquidfun/src/world/particle_object.rs, crates/liquidfun/tests/particle_group_mutation.rs</files>
  <read-first>crates/liquidfun/src/world.rs — `World` lock/poison and public error conventions; crates/liquidfun/src/world/object.rs — stable body/particle-system/group handles and arenas; crates/liquidfun/src/world/particle_object.rs — particle/group creation, view, lifecycle, and association cleanup APIs; crates/liquidfun/src/particle/storage/mutation.rs — join/split/reactive/depth mutation candidates; crates/liquidfun/src/particle/storage/group.rs — group range/cache state; crates/liquidfun/src/particle/view.rs — borrow-scoped semantic views and mutation report conventions.</read-first>
  <behavior>
    - "join_particle_groups(A,B) returns A; split_particle_group(G) returns source-ordered IDs with G first; set flags validates public bits; destroy shell obeys empty/member lifecycle rules."
    - "No method accepts dense rows, raw topology, internal flags, or cross-system groups."
  </behavior>
  <action>Add documented World methods for join, split, group flag mutation, explicit empty-group shell destruction where valid, and connectivity inspection only if it can be expressed with stable IDs. Resolve/validate every handle and lock/poison state before cloning candidates; preflight arena IDs, associations, journal capacity, and mutation results; commit arena/storage/report together. Map internal errors to named public group errors without swallowing cause. Add black-box tests for exact join/split ID order, membership/views, flags/depth/cache effects, retained empty, associations, stale/cross-world/system/locked/poisoned cases, and 128 reproducible public operation sequences. Run the exact ordered Rust gate before commit.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>PROPTEST_CASES=128 cargo test -p liquidfun --all-features --test particle_group_mutation</automated></verify>
  <acceptance-criteria>Supported API tests use only curated exports; join returns A; split returns original first plus ordered new IDs; every failure is typed with exact no-diff snapshots; no public dense/topology/internal-state mutation exists; 128 sequences pass.</acceptance-criteria>
  <done>Consumers have complete safe group mutation workflows with stable identities and transactional semantics.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Public stable-handle APIs to world mutation | Untrusted handle combinations, flags, and lock state cross into multi-object changes. |
| Internal candidate errors to public diagnostics | Swallowed/ambiguous errors can conceal partial or wrong-owner behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-16-01 | Spoofing | public handles | mitigate | Validate kind/world/system/generation before any candidate or identity allocation. |
| T-10-16-02 | Tampering | public flags/mutations | mitigate | Closed public flags and operation-specific candidates only. |
| T-10-16-03 | Denial of service | public sequences | mitigate | Existing bounded arenas/topology and bounded property operations. |
| T-10-16-04 | Repudiation | errors/reports | mitigate | Typed errors and owned exact mutation/lifecycle evidence. |
| T-10-16-05 | Tampering | panic/rollback | mitigate | Clone/validate/commit with existing poison semantics; no partial arena/storage replacement. |
| T-10-16-06 | Information disclosure | API surface | mitigate | Stable IDs/semantic views only; private rows/caches/topology remain hidden. |
| T-10-16-07 | Tampering | unsafe/FFI boundary | accept | Native safe Rust public API only. |
</threat-model>

<verification>
Run public mutation/property tests, compile/rustdoc API checks, and ordered Rust gate.
</verification>

<success-criteria>
- All group workflows are publicly reachable and documented.
- Stable ID/order/ownership semantics are exact.
- Every failure is typed and effect-free.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-16-SUMMARY.md`.</output>
