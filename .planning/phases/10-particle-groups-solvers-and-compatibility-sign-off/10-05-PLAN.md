---
phase: 10-particle-groups-solvers-and-compatibility-sign-off
plan: "05"
type: execute
wave: 2
depends_on: ["10-01", "10-03"]
files_modified:
  - crates/liquidfun/src/particle/storage.rs
  - crates/liquidfun/src/particle/storage/group.rs
  - crates/liquidfun/src/particle/storage/validation.rs
autonomous: true
requirements: [PART-09, PART-10, PART-11, TEST-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-07-19T05-17-27
generated_at: 2026-07-19T06:37:52.617Z
must_haves:
  truths:
    - "ParticleStorage group records are the sole authority for each group's flags, range, strength, transform, user association, internal state, depth, and cached statistics; aggregate group flags are not stored here."
    - "Every group row is same-system, contiguous, non-overlapping, and exactly agrees with the particle membership lane."
  artifacts:
    - path: crates/liquidfun/src/particle/storage/group.rs
      provides: "Storage-owned group metadata, cache state, and invariant validation"
      contains: "GroupRecord"
    - path: crates/liquidfun/src/particle/storage.rs
      provides: "One authoritative group table in ParticleStorage"
      contains: "group_records"
  key_links:
    - from: crates/liquidfun/src/particle/storage/group.rs
      to: crates/liquidfun/src/particle/storage.rs
      via: "validated table owned by ParticleStorage"
      pattern: "GroupRecord|validate_groups"
---

<objective>
Replace incomplete range-only membership with a complete storage-owned group authority.

Purpose: Ensure the existing cloned `ParticleSystem.storage` transaction automatically includes every per-group solver-significant field per D-09/D-15.
Output: Private group records, cache invalidation state, and complete invariants; Plan 10-06 owns the single aggregate group-flag state.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@crates/liquidfun/src/particle/storage.rs
@crates/liquidfun/src/particle/storage/lanes.rs
@crates/liquidfun/src/particle/storage/validation.rs
@crates/liquidfun/src/world/object.rs
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleGroup.h
@third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleGroup.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add storage-owned group records and cache invariants</name>
  <files>crates/liquidfun/src/particle/storage.rs, crates/liquidfun/src/particle/storage/group.rs, crates/liquidfun/src/particle/storage/validation.rs</files>
  <read-first>crates/liquidfun/src/particle/storage.rs — `ParticleStorage` fields, clone, and validation paths; crates/liquidfun/src/particle/storage/lanes.rs — owned lane definitions; crates/liquidfun/src/particle/storage/validation.rs — storage invariants and typed validation errors; crates/liquidfun/src/world/object.rs — `ParticleSystem` and `ParticleGroup` arena records; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleGroup.h — `b2ParticleGroup` public/private state; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleGroup.cpp — `b2ParticleGroup::UpdateStatistics`; third_party/liquidfun/liquidfun/Box2D/Box2D/Particle/b2ParticleSystem.cpp — aggregate particle-group flag maintenance.</read-first>
  <behavior>
    - "Each live group record is keyed by a same-system ParticleGroupId and owns public/internal flags, [first,last), strength, transform, user association key, and timestamped mass/center/linear velocity/inertia/angular velocity cache."
    - "Stable group-table iteration exposes the individual public/internal flags that Plan 10-06's single aggregate authority refreshes."
    - "Empty retained groups have first==last and exact zero aggregate values."
    - "Invalid overlap, gap, wrong-system ID, member-lane disagreement, non-finite cache, or leaked internal flag fails candidate validation."
  </behavior>
  <action>Create `storage/group.rs` with private `GroupRecord`, separate private `InternalGroupFlags` (`WILL_BE_DESTROYED`, `NEEDS_UPDATE_DEPTH`), and `GroupStatisticsCache`. Add `group_records: Vec<GroupRecord>` to `ParticleStorage`; replace or derive the old `group_ranges` representation from this one table rather than keeping two writable authorities. Per D-09, do not add dirty/current aggregate group flags to `GroupRecord`, the group table, or any parallel cache: Plan 10-06 `SolverState` is their only writable authority and derives them by stable scan of these records. Validation must scan records and particle membership in source order, accept zero-length only with CAN_BE_EMPTY/internal deferred-destroy semantics, and require complete contiguity/non-overlap. Cache invalidation must be explicit and source-timestamped; zero-member cache values are `Vec2::ZERO`, 0.0 mass/inertia/angular velocity. Add focused tests for valid contiguous groups, wrong-system handles, overlapping/gapped ranges, empty retention, stable flag scanning, and cache invalidation. Do not move solver state into the world arena. Before committing run the exact ordered Rust gate.</action>
  <pre-commit>Before the task's one atomic commit, run exactly in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing intermediate state.</pre-commit>
  <verify><automated>cargo test -p liquidfun --all-features particle::storage::group && cargo test -p liquidfun --all-features particle::storage::validation</automated></verify>
  <acceptance-criteria>`ParticleStorage` contains one group record table; there is no second mutable `group_ranges` authority and no aggregate group-flag field in group records/table state; internal flags are private; all invalid range/ownership/cache tests fail before mutation; focused tests exit 0.</acceptance-criteria>
  <done>Group physics state is transactionally cloned and validated with the owning particle storage.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| World group handle to storage group row | Wrong-world/system or stale identity could target unrelated membership. |
| Candidate group table to live storage | Partial ranges/caches could create memory-safe but semantically corrupt solver state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-05-01 | Spoofing | group lookup | mitigate | Validate world, system, slot, generation, and record ownership before access. |
| T-10-05-02 | Tampering | group table | mitigate | Complete source-order validation precedes the single candidate replacement. |
| T-10-05-03 | Denial of service | group scans | mitigate | Bound group count by existing identity/particle i32 limits and use linear ordered validation. |
| T-10-05-04 | Repudiation | cached statistics | mitigate | Timestamp and invalidation state make recomputation points explicit and testable. |
| T-10-05-05 | Tampering | panic/rollback | mitigate | Pure validation and cloned storage candidate ensure panic/error exposes no partial table or cache. |
| T-10-05-06 | Tampering | unsafe/FFI boundary | accept | No unsafe/FFI is introduced; records have no public layout promise. |
</threat-model>

<verification>
Run group/storage invariant tests, property baselines, and the ordered Rust gate. Inspect the struct graph to confirm no parallel mutable group metadata remains in `World`.
</verification>

<success-criteria>
- One storage table owns all solver-significant group state.
- Every range and membership invariant is checked.
- Cache invalidation/recomputation state is transaction-safe.
</success-criteria>

<output>After completion, create `.planning/phases/10-particle-groups-solvers-and-compatibility-sign-off/10-05-SUMMARY.md`.</output>
