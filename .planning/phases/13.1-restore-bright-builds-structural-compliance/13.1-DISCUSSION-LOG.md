# Phase 13.1: Restore Bright Builds Structural Compliance - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md; this log preserves the alternatives considered.

**Date:** 2026-07-28T00:14:49.676Z
**Phase:** 13.1-restore-bright-builds-structural-compliance
**Mode:** Yolo
**Areas discussed:** Ownership policy, refactor boundaries, execution recovery, verification

## Ownership Policy

| Option | Description | Selected |
| --- | --- | --- |
| Refactor owned files | Classify provenance, refactor every first-party finding, and reserve exceptions for genuinely external files. | ✓ |
| Baseline all findings | Add exact-path exceptions for every pre-existing oversized file. | |
| Change the checker | Modify the managed checker or threshold to suppress the baseline. | |

**User's choice:** Refactor files the repository owns; add exceptions only for files it does not own or third-party files.
**Notes:** Three parallel audits classified all 103 findings as first-party and refactorable. Actual third-party and vendored paths are already excluded, so no current exception is justified.

## Refactor Boundaries

| Option | Description | Selected |
| --- | --- | --- |
| Stable facades and contract rings | Preserve public roots and wire/ABI declarations while extracting cohesive private modules. | ✓ |
| Uniform mechanical chunks | Split every file by contiguous size without regard to responsibility. | |
| Broad API redesign | Use the cleanup to change public paths, schemas, or CLI behavior. | |

**User's choice:** Auto-selected the stable-facade recommendation.
**Notes:** Integration-test root filenames, Rust imports, serialized fields, deterministic order, CLI behavior, and C++ exports remain stable.

## Execution Recovery

| Option | Description | Selected |
| --- | --- | --- |
| Isolated clean worktree | Preserve Phase 14, refactor in dependency waves with external patch snapshots, and commit once all checks pass. | ✓ |
| Current dirty worktree | Mix Phase 13.1 with paused Phase 14 edits until the global gate is green. | |
| Temporary owned-file exceptions | Commit each wave behind temporary allowlist entries and remove them later. | |

**User's choice:** Auto-selected the isolated clean-worktree recommendation.
**Notes:** Repository rules forbid any commit while the managed checker is red, and owned-file exceptions are not authorized.

## Verification

| Option | Description | Selected |
| --- | --- | --- |
| Layered contract matrix | Focused owning tests per extraction, aggregate gates per wave, and exact evidence/native checks at affected boundaries. | ✓ |
| Aggregate Cargo only | Rely solely on the final workspace Cargo gate. | |
| Static size check only | Treat file length and successful compilation as sufficient. | |

**User's choice:** Auto-selected the layered verification recommendation.
**Notes:** Source-derived identities may be recomputed consistently; semantic bytes and authority cannot be self-blessed or silently changed.

## the agent's Discretion

- Exact private module names inside each cohesive batch.
- Recovery snapshot names and temporary location.
- Small additional splits for files that remain near the threshold after test extraction.

## Deferred Ideas

- Windows particle-group behavior remains Phase 14 work.
- Exact-candidate evidence remains Phase 15 work.
