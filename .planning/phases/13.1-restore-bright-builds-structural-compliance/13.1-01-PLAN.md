---
phase: 13.1-restore-bright-builds-structural-compliance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13.1-2026-07-28T00-14-49
generated_at: 2026-07-28T00:38:59Z
must_haves:
  truths:
    - "Every pre-existing tracked and untracked change in the original worktree has recoverable machine-readable preservation evidence."
    - "Phase 13.1 starts in a clean detached worktree at the original full HEAD."
    - "No tracked file, exception, stage, or commit is created by setup."
  artifacts:
    - path: "/tmp/liquidfun-rs-phase13.1/preservation.json"
      provides: "Full HEAD, exact porcelain set, tracked hashes, untracked archive inventory/hashes, and archive digest"
    - path: "/tmp/liquidfun-rs-phase13.1/untracked.tar"
      provides: "Recoverable contents of every untracked Phase 14/planning file"
    - path: "/tmp/liquidfun-rs-phase13.1/baseline-findings.json"
      provides: "Exact ordered 103-path Bright Builds baseline and scanned/exceptions/findings counters"
  key_links:
    - from: "/tmp/liquidfun-rs-phase13.1/preservation.json"
      to: "/tmp/liquidfun-rs-phase13.1/untracked.tar"
      via: "recorded SHA-256 and exact archived path/content inventory"
      pattern: "untracked_archive_sha256"
---

<objective>
Create the recovery and isolation boundary before structural source work.

Purpose: Implement D-08 through D-11 without risking paused Phase 14 or planning state.
Output: Verified external preservation manifest/archive, clean detached worktree, and recovery directory.
</objective>

<execution-context>
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-EXECUTION.md
</execution-context>
<aggregate-execution>Execute only as bounded sequential segment 13.1-01 through execute-phase Pattern-B task-only agent semantics under `13.1-EXECUTION.md`. The standard per-plan execute-plan route is unsupported because its commits/summaries conflict with higher repository pre-commit rules. Do not stage, commit, write a SUMMARY, or modify lifecycle/planning state; keep progress/evidence and the complete tracked-plus-untracked candidate snapshot external. Plans 01-14 and Plan 15 Tasks 1-2 form one aggregate candidate; only the orchestrator may perform Plan 15 Task 3 after the full unchanged candidate is green.</aggregate-execution>

<context>
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-CONTEXT.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/verification.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the original dirty state and untracked contents</name>
  <files>None; write only below `/tmp/liquidfun-rs-phase13.1`</files>
  <action>Read D-08 through D-11 and inspect the complete original status before effects. Resolve the original root and full HEAD dynamically. Refuse to reuse `/tmp/liquidfun-rs-phase13.1` unless its existing manifest names the same root/HEAD and its artifacts validate. Run `bun scripts/bright-builds-check.ts file-lengths`, require the audited counters `scanned=648 exceptions=0 findings=103`, and parse its exact ordered normalized finding paths and line counts into `baseline-findings.json` with the raw-output SHA-256. Create a NUL-safe exact `git status --porcelain=v1 -z --untracked-files=all` capture; record the ordered status records as encoded machine-readable values, not a lossy display. For every dirty tracked file record repo-relative path, status, blob/worktree SHA-256, size, and mode. For every untracked file—including Phase 13.1/14 planning artifacts—record path, SHA-256, size, and mode and add its exact contents to a NUL-safe external tar archive. Write `preservation.json` with original root, full HEAD, exact status set, both inventories, archive inventory, and archive SHA-256. Record the manifest SHA-256 separately. Never print file contents, stage, commit, edit tracked files, or use a broad deletion.</action>
  <verify>
    <automated>jq -e '.original_head | test("^[0-9a-f]{40}$") and (.porcelain_records|type=="array") and (.tracked_dirty|type=="array") and (.untracked|type=="array") and (.untracked_archive_sha256|test("^[0-9a-f]{64}$"))' /tmp/liquidfun-rs-phase13.1/preservation.json &gt;/dev/null &amp;&amp; jq -e '.scanned==648 and .exceptions==0 and .findings==103 and (.paths|length)==103 and ((.paths|unique|length)==103)' /tmp/liquidfun-rs-phase13.1/baseline-findings.json &gt;/dev/null &amp;&amp; shasum -a 256 -c /tmp/liquidfun-rs-phase13.1/preservation.sha256</automated>
  </verify>
  <done>The manifest losslessly identifies the full original dirty set and every tracked/untracked byte has a hash or archive recovery path.</done>
</task>

<task type="auto">
  <name>Task 2: Create and prove the isolated Phase 13.1 worktree</name>
  <files>None; Git administrative data and `/tmp/liquidfun-rs-phase13.1/worktree` only</files>
  <action>Read the completed preservation manifest and aggregate execution contract first. Create a detached worktree at `/tmp/liquidfun-rs-phase13.1/worktree` from the manifest's exact full HEAD, or validate an existing one rather than deleting it. Prove it is a Git worktree with `git rev-parse --is-inside-work-tree`, its HEAD equals the manifest, and its porcelain is empty. Create the external segment/evidence directories. Re-run a read-only original-state verifier that recomputes the exact porcelain set, all dirty tracked hashes, and all untracked hashes/archive inventory against `preservation.json`; stop on any difference. Per D-10, use only the supported execute-phase Pattern-B aggregate route: no segment agent may stage, commit, write a summary, or change lifecycle/planning state; only the orchestrator may perform Plan 15 Task 3 after the unchanged candidate is fully green. After this segment's tests pass, refresh `/tmp/liquidfun-rs-phase13.1/segments/13.1-01/candidate/` exactly per `13.1-EXECUTION.md`: tracked full-index binary patch plus the NUL-safe untracked manifest/archive (paths, modes, sizes, SHA-256 and archive digest), deterministic complete candidate-tree manifest/digest, and chained snapshot metadata. Record progress only in external evidence.</action>
  <verify>
    <automated>test "$(git -C /tmp/liquidfun-rs-phase13.1/worktree rev-parse --is-inside-work-tree)" = true &amp;&amp; test "$(git -C /tmp/liquidfun-rs-phase13.1/worktree rev-parse HEAD)" = "$(jq -r .original_head /tmp/liquidfun-rs-phase13.1/preservation.json)" &amp;&amp; test -z "$(git -C /tmp/liquidfun-rs-phase13.1/worktree status --porcelain)" &amp;&amp; test -f /tmp/liquidfun-rs-phase13.1/segments/13.1-01/candidate/tracked.patch &amp;&amp; test -s /tmp/liquidfun-rs-phase13.1/segments/13.1-01/candidate/untracked-manifest.json &amp;&amp; test -f /tmp/liquidfun-rs-phase13.1/segments/13.1-01/candidate/untracked.tar &amp;&amp; test -s /tmp/liquidfun-rs-phase13.1/segments/13.1-01/candidate/candidate-tree-manifest.json &amp;&amp; test -s /tmp/liquidfun-rs-phase13.1/segments/13.1-01/candidate/snapshot.json</automated>
  </verify>
  <done>The isolated worktree is clean at the preserved HEAD, the original dirty state verifies byte-for-byte, and no commit exists.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Dirty original → isolated worktree | Existing user/Phase 14 work must remain recoverable and unmodified. |
| Untracked filesystem → archive/manifest | Paths and bytes require NUL-safe capture without traversal ambiguity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-13.1-01 | T | Preservation manifest | mitigate | Hash every file and archive plus the manifest; use exact porcelain records. |
| T-13.1-02 | R | Worktree setup | mitigate | Bind clean detached worktree to full preserved HEAD and record evidence. |
| T-13.1-03 | D | Stale temp state | mitigate | Validate/reuse or stop; never recursively delete unexplained state. |
</threat-model>

<verification>
Manifest/schema/digest checks pass; archive inventory matches untracked files; original state re-verifies; isolated worktree is clean at exact HEAD.
</verification>

<success-criteria>
Phase 13.1 can proceed and later fast-forward safely without losing or mixing any pre-existing tracked or untracked work.
</success-criteria>

<output>Do not write a SUMMARY or any planning/lifecycle artifact. Write only `/tmp/liquidfun-rs-phase13.1/segments/13.1-01/evidence.json` and the complete candidate snapshot required by `13.1-EXECUTION.md`; the orchestrator creates this plan summary only in the authorized post-integration docs set.</output>
