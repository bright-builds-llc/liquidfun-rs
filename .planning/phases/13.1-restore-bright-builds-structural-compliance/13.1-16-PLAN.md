---
phase: 13.1-restore-bright-builds-structural-compliance
plan: "16"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun-differential/tests/round_trip.rs
  - crates/liquidfun-differential/tests/round_trip/real_oracle.rs
  - crates/liquidfun-differential/tests/round_trip/evidence.rs
autonomous: true
requirements: []
gap_closure: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13.1-2026-07-28T00-14-49
generated_at: 2026-07-31T17:25:40Z
must_haves:
  truths:
    - "The second-request failure bundle always retains the validated session identity during the normal aggregate differential package run."
    - "Parallel tests cannot turn an intended post-handshake failure into a pre-handshake resource-contention failure."
  artifacts:
    - path: "crates/liquidfun-differential/tests/round_trip/real_oracle.rs"
      provides: "Process-slot and isolated fake-repository test harness"
    - path: "crates/liquidfun-differential/tests/round_trip/evidence.rs"
      provides: "Aggregate regression for second-request session identity"
  key_links:
    - from: "crates/liquidfun-differential/tests/round_trip/evidence.rs"
      to: "identity.json and report.json"
      via: "post-handshake injected second-request failures"
      pattern: "session_identity_sha256"
---

<objective>
Remove concurrency sensitivity from the aggregate differential evidence tests.

Purpose: Restore the package-level gate without weakening session identity or changing protocol bytes.
Output: A resource-isolated CLI test harness and a repeatable aggregate identity regression.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-CONTEXT.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-VERIFICATION.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-04-SUMMARY.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-06-SUMMARY.md
@crates/liquidfun-differential/src/supervisor.rs
@crates/liquidfun-differential/src/runner.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce, classify, and eliminate aggregate session-identity contention</name>
  <files>crates/liquidfun-differential/tests/round_trip.rs, crates/liquidfun-differential/tests/round_trip/real_oracle.rs, crates/liquidfun-differential/tests/round_trip/evidence.rs</files>
  <read-first>AGENTS.md; .codex/tasks/lessons.md lesson-use-explicit-ci-identities; .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-VERIFICATION.md; crates/liquidfun-differential/tests/round_trip.rs; crates/liquidfun-differential/tests/round_trip/real_oracle.rs; crates/liquidfun-differential/tests/round_trip/evidence.rs; crates/liquidfun-differential/src/supervisor.rs; crates/liquidfun-differential/src/supervisor/failure.rs; crates/liquidfun-test-protocol/src/limits.rs</read-first>
  <behavior>
    - "Four concurrent CLI cases using reuse and sanitizer profiles reach their injected second request and retain one 64-hex validated session identity."
    - "A pre-handshake failure remains distinguishable and is never accepted as evidence for the second-request cases."
  </behavior>
  <action>Use an isolated `CARGO_TARGET_DIR` and leave Rust's normal test-thread count unset. In one complete RED→GREEN task, first add a regression that launches the existing `second_malformed` and `second_value_mismatch` cases concurrently for both `reuse` and `sanitizer`, asserts the intended exit/result kind, and asserts that `identity.json` and `report.json` contain the same 64-hex `session_identity_sha256`; run it once during RED and record the actual `failure_kind` in the test diagnostic so startup timeout, early process failure, and post-handshake evidence loss remain distinguishable. Then make the final task state green: add a test-only `Mutex`-backed process slot in the round-trip root and acquire it inside `run_cli_with_root` for the full fake-repository creation, child execution, and returned-output boundary. Replace the loose `PathBuf` fixture with an owned test repository guard that allocates a unique PID-plus-atomic-sequence directory, rejects a pre-existing nonempty directory, and cleans up only its exact root on drop. Keep the guard alive until the caller has read the failure bundle. Preserve CLI argv, exit codes, request bytes, report schema, session identity semantics, and normal package test concurrency outside this critical section. Update callers to consume the guard path without changing test names. Do not change the five-second production startup limit, make identity optional for a post-handshake failure, serialize the whole package, set `RUST_TEST_THREADS`, or retry deterministic failures.</action>
  <verify>
    <automated>CARGO_TARGET_DIR=target/phase13-1-gap-differential cargo test -p liquidfun-differential --test round_trip cli_reuse_and_sanitizer_bundles_bind_the_second_request_and_session_identity -- --nocapture &amp;&amp; CARGO_TARGET_DIR=target/phase13-1-gap-differential cargo test -p liquidfun-differential --test round_trip --all-features &amp;&amp; CARGO_TARGET_DIR=target/phase13-1-gap-differential cargo test -p liquidfun-differential --all-features</automated>
  </verify>
  <acceptance-criteria>The task's final verification exits 0: the regression exercises concurrent child processes at the package boundary, its diagnostic preserves the RED failure classification, the full package passes with default Rust test threads, and no production limit, public API, protocol byte, or failure classification changes.</acceptance-criteria>
  <done>The aggregate differential package gate is green and session identity is deterministic under its supported concurrency boundary; no task is left with an intentionally failing verification.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Concurrent test processes → fake repository | Test cases must not share executable, behavior, or evidence paths. |
| Oracle handshake → failure bundle | Identity becomes authoritative only after validated handshake completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13.1-32 | T | Fake repository | mitigate | Unique owned roots and exact cleanup prevent cross-case mutation. |
| T-13.1-33 | R | Session evidence | mitigate | Assert the same full identity in identity and report artifacts. |
| T-13.1-34 | D | Aggregate child spawning | mitigate | Bound only the process-heavy section without retries or global test serialization. |
</threat-model>

<verification>The focused concurrency regression and the complete `liquidfun-differential` package run pass with default test threads in an isolated target directory.</verification>
<success-criteria>Aggregate differential execution reproducibly retains second-request session identity with no semantic weakening.</success-criteria>
<output>After completion, create `.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-16-SUMMARY.md`.</output>
