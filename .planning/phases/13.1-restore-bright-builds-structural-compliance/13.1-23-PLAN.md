---
phase: 13.1-restore-bright-builds-structural-compliance
plan: "23"
type: execute
wave: 3
depends_on: ["13.1-19", "13.1-22"]
files_modified:
  - scripts/phase13-1-gap-verification.sh
  - scripts/phase13-1-gap-verification-manifest.json
  - scripts/phase13-1-validate-gap-evidence.sh
  - tools/xtask/tests/phase13_1_gap_verification.rs
  - tools/xtask/tests/phase13_1_gap_verification/producer.rs
  - tools/xtask/tests/phase13_1_gap_verification/producer/lifecycle.rs
  - fuzz/Cargo.lock
  - .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-FAILED-ATTEMPT-ab06bfc.md
autonomous: true
requirements: []
gap_closure: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13.1-2026-07-28T00-14-49
generated_at: 2026-08-02T18:00:53.578Z
must_haves:
  truths:
    - "The manifest invokes the real testbed capability CLI with its reviewed fixture and a repository-relative candidate output path, and every auxiliary argv contract is exact."
    - "One producer invocation either validates and returns existing terminal evidence, resumes an exact all-zero digest-matching prefix without rerunning successes, or refuses ambiguous/failed state without dispatching again."
    - "Canonical authority binds the positive run ID parsed from the exact repository Actions URL returned by dispatch; immediate listing visibility and stale same-candidate runs cannot select authority."
    - "A pre-dispatch intent/result journal makes interruption fail closed and prevents duplicate dispatch when an exact run URL is unavailable."
    - "An independent validator rejects any substitution of the command 55 dispatch URL, canonical run ID, command 56/58 run views, or dispatch journal identities and digests."
    - "The tracked fuzz lock makes `cargo +nightly-2026-07-15 fuzz build` leave the corrected candidate clean."
    - "The failed ab06bfc attempt remains durable supplemental history with exact hashes and chronology, never terminal or authoritative evidence."
  artifacts:
    - path: "scripts/phase13-1-gap-verification.sh"
      provides: "Interruption-safe exact-run producer state machine"
    - path: "scripts/phase13-1-gap-verification-manifest.json"
      provides: "Corrected 72-command contract with exact run binding and capability argv"
    - path: "scripts/phase13-1-validate-gap-evidence.sh"
      provides: "Independent dispatch URL, run-view, and journal binding validation"
    - path: "tools/xtask/tests/phase13_1_gap_verification/producer/lifecycle.rs"
      provides: "Focused dispatch, resume, idempotence, and duplicate-prevention regressions"
    - path: "fuzz/Cargo.lock"
      provides: "Tracked nested-workspace dependency resolution that prevents fuzz-build dirtiness"
    - path: ".planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-FAILED-ATTEMPT-ab06bfc.md"
      provides: "Truthful durable supplemental record of the failed Plan 21 candidate attempt"
  key_links:
    - from: "scripts/phase13-1-gap-verification.sh"
      to: "GitHub Actions run ID"
      via: "strict parse of dispatch stdout URL for the exact repository slug"
      pattern: "actions/runs"
    - from: "scripts/phase13-1-gap-verification.sh"
      to: "command-records.jsonl and retained logs"
      via: "ordered prefix replay with SHA-256 verification before resume"
      pattern: "command-records.jsonl"
    - from: "scripts/phase13-1-validate-gap-evidence.sh"
      to: "commands 55, 56, and 58 plus dispatch-intent.json and dispatch-result.json"
      via: "independent path confinement, digest replay, exact URL/run/repository/candidate binding, and semantic run-view checks"
      pattern: "canonical_run_id"
    - from: "scripts/phase13-1-gap-verification-manifest.json"
      to: "crates/liquidfun-testbed/src/main.rs"
      via: "--capability-check --fixture ... --output contract"
      pattern: "testbed-capability"
---

<objective>
Repair the corrected-candidate producer before any new publication or canonical dispatch.

Purpose: Close the deterministic Plan 21 evidence failure at its root: the invalid capability argv, racy run discovery, destructive non-resumable producer state, independently mutable evidence links, and untracked fuzz lock.
Output: One atomic RED-to-GREEN lifecycle implementation task, an interruption-safe producer and corrected manifest, an independent evidence validator, tracked fuzz lock, and a durable non-authoritative record of the failed ab06bfc attempt.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-CONTEXT.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-VERIFICATION.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-19-SUMMARY.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-22-SUMMARY.md
@scripts/phase13-1-gap-verification.sh
@scripts/phase13-1-gap-verification-manifest.json
@scripts/phase13-1-validate-gap-evidence.sh
@tools/xtask/tests/phase13_1_gap_verification.rs
@tools/xtask/tests/phase13_1_gap_verification/producer.rs
@crates/liquidfun-testbed/src/main.rs
@crates/liquidfun-testbed/tests/capability.rs
@crates/liquidfun-testbed/CAPABILITY.md
@fuzz/Cargo.toml

<interfaces>
The testbed binary accepts exactly one command followed by `--fixture PATH --output PATH`; the capability command is `--capability-check`. The existing evidence validator accepts `<manifest> <evidence-json> <repository-root> <retained-root>` and proves D1 from the retained `canonical_identity.path` file. Preserve the manifest schema and the 72 logical command positions; replace racy command 56 with an exact-run inspection rather than renumbering commands 57-72.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive the corrected producer and independent validator from RED to GREEN</name>
  <files>scripts/phase13-1-gap-verification.sh, scripts/phase13-1-gap-verification-manifest.json, scripts/phase13-1-validate-gap-evidence.sh, tools/xtask/tests/phase13_1_gap_verification.rs, tools/xtask/tests/phase13_1_gap_verification/producer.rs, tools/xtask/tests/phase13_1_gap_verification/producer/lifecycle.rs, fuzz/Cargo.lock</files>
  <read-first>AGENTS.md Rust and test rules; standards/core/testing.md; standards/core/verification.md; scripts/phase13-1-gap-verification.sh; scripts/phase13-1-gap-verification-manifest.json; scripts/phase13-1-validate-gap-evidence.sh; both existing phase13_1_gap_verification test files; crates/liquidfun-testbed/src/main.rs; crates/liquidfun-testbed/tests/capability.rs; crates/liquidfun-testbed/CAPABILITY.md; fuzz/Cargo.toml</read-first>
  <behavior>
    - "The manifest's full auxiliary command slice remains positions 60-72, and command 66 equals `cargo run -p liquidfun-testbed -- --capability-check --fixture crates/liquidfun-differential/tests/fixtures/catalog/phase11-v1.json --output target/phase13-1-gap-verification/${CANDIDATE}/testbed-capability`."
    - "Command 55 stdout must be exactly `https://github.com/&lt;nameWithOwner&gt;/actions/runs/&lt;positive-decimal-id&gt;` plus at most one line ending; that ID is the sole canonical run ID."
    - "An empty immediate `gh run list` result and stale same-candidate runs cannot influence selection; fixtures prove the list path is never invoked and the dispatch URL ID is used."
    - "A matching zero-exit prefix resumes at the first missing command without rerunning successes; a nonzero prefix, digest/log/argv/environment/order mismatch, or non-prefix record refuses resume."
    - "Existing independently valid terminal evidence returns success without command execution or dispatch."
    - "A prepared dispatch intent plus interruption never causes a second dispatch: recover only from a complete validated retained URL/result, otherwise stop for explicit reconciliation."
    - "Command 56's initial and command 58's terminal run views bind databaseId, URL, workflow_dispatch event, candidate head SHA, status, and terminal success to the command 55 run ID."
    - "Terminal evidence binds confined dispatch-intent and dispatch-result journal paths and SHA-256 digests; the validator independently recomputes and semantically validates both journals."
    - "Independent negative fixtures reject dispatch URL, terminal run ID, initial view, terminal view, intent journal, and result journal substitutions even when an attacker repairs superficial enclosing digests."
    - "The real fuzz build with committed `fuzz/Cargo.lock` leaves `git status --porcelain=v1 --untracked-files=all` empty."
  </behavior>
  <action>Per D-06, D-12, and D-13, execute one atomic TDD cycle in this task. Preserve the stable Cargo target name `phase13_1_gap_verification`; keep shared fixture mechanics in `producer.rs`, place focused lifecycle cases in `producer/lifecycle.rs`, and keep each test to one Arrange/Act/Assert concern. RED: first add the exact 18 lifecycle tests named `manifest_preserves_exact_auxiliary_argv_and_corrected_capability`, `exact_dispatch_url_establishes_run_id`, `dispatch_url_rejects_wrong_repository`, `dispatch_url_rejects_malformed_or_missing_run_id`, `exact_dispatch_url_survives_empty_immediate_listing`, `exact_dispatch_url_ignores_stale_same_candidate_listing`, `exact_zero_exit_prefix_resumes_without_rerunning_successes`, `failed_prefix_refuses_resume`, `terminal_evidence_validation_is_idempotent`, `dispatch_intent_without_result_never_redispatches`, `validated_dispatch_stdout_recovers_result_without_redispatch`, `fuzz_build_keeps_tracked_lock_and_clean_tree`, `validator_rejects_dispatch_url_substitution`, `validator_rejects_terminal_run_id_substitution`, `validator_rejects_initial_run_view_substitution`, `validator_rejects_terminal_run_view_substitution`, `validator_rejects_dispatch_intent_journal_substitution`, and `validator_rejects_dispatch_result_journal_substitution`. Use `cargo test -- --list` to prove all 18 tests are discovered, run the full target, observe the expected failures, and record the failing test names/reasons in execution notes. Never commit a failing RED state.

GREEN: implement the producer, manifest, validator, fixtures, and tracked fuzz lock until the full target and all repository gates pass. Replace destructive startup with a fail-closed state machine. If `final-verification.json` exists, invoke the independent validator and return success without changing bytes or executing commands. Otherwise accept only an exact ordered all-zero prefix of the expanded manifest: match every ID, argv, environment, retained path, regular nonsymlink log, and recomputed digest; preserve record bytes and resume only at the first missing command. Refuse nonzero, gap, duplicate, partial JSON, mismatch, missing log, or digest drift. Use atomic temporary-file-to-rename writes and never delete the candidate directory or truncate valid state.

Before command 55, atomically create confined `dispatch-intent.json` binding candidate/tree/repository `nameWithOwner`/workflow/ref/exact argv/manifest digest. Dispatch only when neither a validated intent/result pair nor recoverable validated command 55 output exists. Capture command 55 stdout in its retained log; accept exactly the repository URL shape above, parse a positive run ID, and atomically create `dispatch-result.json` binding the intent digest, exact URL/run/candidate/repository, and command 55 record/log digest. On restart with intent but no result, recover only from independently validated command 55 output and records; otherwise stop without redispatch. Remove candidate-based `gh run list` discovery. Command 56 must be `gh run view ${CANONICAL_RUN_ID} --json databaseId,headSha,event,status,conclusion,url`; command 58 uses the same fields. Require command 56 `databaseId` and exact Actions URL match the parsed run ID, `event` is `workflow_dispatch`, `headSha` is the candidate, status is queued/in_progress/completed, conclusion is null unless completed and success if completed. Require command 58 the same identity with status completed and conclusion success. Keep commands 57 and 59 as exact watch and artifact download.

Extend terminal evidence with confined dispatch intent/result path-and-digest objects. Update `scripts/phase13-1-validate-gap-evidence.sh` to independently parse command 55's retained stdout, bind its positive run ID to `.canonical_run_id`, validate the command 55 record/log digest, validate command 56 and 58 databaseId/url/event/headSha/status/conclusion semantics, confine and recompute both journal digests, and validate their logical candidate/tree/repository/workflow/ref/argv/manifest/intent/result/command-log bindings. Negative fixtures must independently substitute and then repair enclosing fixture digests for each of the six mutation classes named above so a validator that merely trusts terminal JSON cannot pass. Fake `gh` journals every invocation; tests prove one dispatch and no `gh run list`. Do not use `PHASE13_1_GAP_TEST_RUN_ID` to bypass production lifecycle.

Correct command 66 exactly as specified, preserve all other logical command positions and the sole Phase 15 deferral, generate and track `fuzz/Cargo.lock` from the pinned nightly/cargo-fuzz resolution, and prove the exact fuzz build leaves the candidate clean. Preserve identity-last pending/validator/rename publication, D1/D2 separation, clean-tree checks, and publication-free behavior. Do not add a push, ref mutation, dispatch retry, fixture regeneration, or Phase 15 acceptance path. Only commit after GREEN: all 18 discovered lifecycle tests, the full stable target, Bash checks, and the repository-mandatory ordered Rust/precommit gates pass.</action>
  <verify>
    <automated>listed="$(cargo test -p xtask --test phase13_1_gap_verification --all-features -- --list)" &amp;&amp; lifecycle_list="$(printf '%s\n' "$listed" | rg '^producer::lifecycle::[^:]+: test$')" &amp;&amp; test "$(printf '%s\n' "$lifecycle_list" | sed '/^$/d' | wc -l | tr -d ' ')" = 18 &amp;&amp; for test_name in manifest_preserves_exact_auxiliary_argv_and_corrected_capability exact_dispatch_url_establishes_run_id dispatch_url_rejects_wrong_repository dispatch_url_rejects_malformed_or_missing_run_id exact_dispatch_url_survives_empty_immediate_listing exact_dispatch_url_ignores_stale_same_candidate_listing exact_zero_exit_prefix_resumes_without_rerunning_successes failed_prefix_refuses_resume terminal_evidence_validation_is_idempotent dispatch_intent_without_result_never_redispatches validated_dispatch_stdout_recovers_result_without_redispatch fuzz_build_keeps_tracked_lock_and_clean_tree validator_rejects_dispatch_url_substitution validator_rejects_terminal_run_id_substitution validator_rejects_initial_run_view_substitution validator_rejects_terminal_run_view_substitution validator_rejects_dispatch_intent_journal_substitution validator_rejects_dispatch_result_journal_substitution; do printf '%s\n' "$lifecycle_list" | rg -Fqx "producer::lifecycle::$test_name: test"; done &amp;&amp; cargo test -p xtask --test phase13_1_gap_verification --all-features &amp;&amp; bash -n scripts/phase13-1-gap-verification.sh scripts/phase13-1-validate-gap-evidence.sh &amp;&amp; shellcheck scripts/phase13-1-gap-verification.sh scripts/phase13-1-validate-gap-evidence.sh &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bun scripts/bright-builds-check.ts all</automated>
  </verify>
  <acceptance-criteria>The RED failures are observed and documented but never committed; the GREEN state discovers exactly the named 18 lifecycle tests, runs the complete target, independently rejects every URL/run/view/journal substitution, passes the full ordered precommit sequence, and contains no ambiguous run listing or injected run-ID bypass.</acceptance-criteria>
  <done>The producer, manifest, validator, regression suite, and fuzz lock form one green atomic correction with independently enforced evidence identity.</done>
</task>

<task type="auto">
  <name>Task 2: Record the failed attempt and preflight the fresh candidate locally</name>
  <files>.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-FAILED-ATTEMPT-ab06bfc.md</files>
  <read-first>Target evidence under target/phase13-1-gap-verification/ab06bfc248ec8697d70475d8b086405c448f8886; Task 1 diff and GREEN evidence; AGENTS.md; standards/core/verification.md; .planning/ROADMAP.md; .planning/STATE.md; all Phase 13.1 Plans/SUMMARYs 19-22</read-first>
  <action>Create the durable attempt record with no YAML frontmatter and no standalone body `---`. State that candidate `ab06bfc248ec8697d70475d8b086405c448f8886`, run `30738379116`, and artifact `8830451610` are supplemental historical evidence only and are neither terminal nor authoritative. Record exactly: original one-dispatch authorization accepted by `pRizz` at `2026-08-02T07:32:48Z`; the canonical run completed successfully; immediate discovery returned `[]`; zero-dispatch recovery accepted by `pRizz` at `2026-08-02T17:42:58Z`; recovery runner SHA-256 values `5345e367a47e12d2a349cc7d39e72dfcc617e9494a31c9611d293d6f0f3fc75e` and `88f2726c675a1b063407d885036906e84a7202b4088316913f05c377e9f20e73`; records 1-56 SHA-256 `cbd1eb46237c1213b3aa246955e765d6f8e2dcfe28a053c6d5f593b6a4bfb25d`; discovery log SHA-256 `37517e5f3dc66819f61f5a7bb8ace1921282415f10551d2defa5c3eb0985b570`; transient untracked `fuzz/Cargo.lock` SHA-256/size `64c491a261bd3f9ade44411a1406804e13d2822dbc835da1ff2d34ecf92fea01` / `11600`; commands 1-65 passed once; command 66 failed once with exit 2 because the manifest used invalid `--headless --capabilities`; and commands 67-72 never ran. Cite retained target paths as ignored local corroboration, do not copy or rewrite their records, and explicitly prohibit using them to resume or authorize the next candidate.

Then perform the publication-free corrected-candidate preflight per D-15: prove the exact 18 lifecycle tests are listed and run the full stable test target; run Bash syntax and shellcheck; run the corrected real capability command with output `target/phase13-1-gap-verification/$(git rev-parse HEAD)/testbed-capability`; run `cargo +nightly-2026-07-15 fuzz build` followed immediately by a clean-tree assertion; run `bun scripts/bright-builds-check.ts all`; then run the repository-mandatory ordered Rust sequence `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`. Validate all 23 plan frontmatters and structures and require lifecycle metadata has the expected ID/mode with the stale pre-correction verification as the sole lifecycle reason; Plan 21, not this plan, owns formal re-verification and final `valid`. Review the diff for only Plan 23-owned paths. Do not run the real producer, push, fetch, mutate a ref, call `gh workflow run`, reuse ab06bfc records/artifact, or run Phase 15 acceptance.</action>
  <verify>
    <automated>cargo test -p xtask --test phase13_1_gap_verification --all-features &amp;&amp; bash -n scripts/phase13-1-gap-verification.sh scripts/phase13-1-validate-gap-evidence.sh &amp;&amp; shellcheck scripts/phase13-1-gap-verification.sh scripts/phase13-1-validate-gap-evidence.sh &amp;&amp; candidate_sha="$(git rev-parse HEAD)" &amp;&amp; cargo run -p liquidfun-testbed -- --capability-check --fixture crates/liquidfun-differential/tests/fixtures/catalog/phase11-v1.json --output "target/phase13-1-gap-verification/$candidate_sha/testbed-capability" &amp;&amp; status_before="$(git status --porcelain=v1 --untracked-files=all)" &amp;&amp; cargo +nightly-2026-07-15 fuzz build &amp;&amp; test "$(git status --porcelain=v1 --untracked-files=all)" = "$status_before" &amp;&amp; test -z "$(git status --porcelain=v1 --untracked-files=all -- fuzz)" &amp;&amp; bun scripts/bright-builds-check.ts all &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; for plan in .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-*-PLAN.md; do node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs frontmatter validate "$plan" --schema plan | jq -e '.valid == true' &gt;/dev/null &amp;&amp; node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure "$plan" | jq -e '.valid == true' &gt;/dev/null; done &amp;&amp; node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify lifecycle 13.1 --expect-id 13.1-2026-07-28T00-14-49 --expect-mode yolo --require-plans --require-verification | jq -e '.plans.valid == true and .lifecycle_id == "13.1-2026-07-28T00-14-49" and .lifecycle_mode == "yolo" and .reasons == ["verification is stale relative to context, plan, or summary artifacts"]' &gt;/dev/null</automated>
  </verify>
  <acceptance-criteria>The corrected local candidate is clean and fully green; the failed attempt is durably recorded with every supplied identity/hash/outcome and marked supplemental only; no publication, dispatch, ref mutation, or Phase 15 work occurred.</acceptance-criteria>
  <done>A clean corrected candidate is ready for Plan 21's new exact-candidate publication checkpoint.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Retained partial state → resumed execution | Prior records are untrusted until exact-prefix, zero-exit, path, and digest validation succeeds. |
| Dispatch stdout → canonical run authority | Only the exact repository run URL returned by the authorized dispatch may establish the run ID. |
| Producer terminal JSON → independent validator | Terminal claims, run views, and journal references are untrusted until retained bytes and semantic bindings are independently replayed. |
| Interrupted dispatch → retry decision | Intent without a recoverable validated result must stop, never redispatch. |
| Ignored failed-attempt logs → durable planning history | Historical data may explain failure but cannot become terminal candidate evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13.1-55 | S | Canonical run selection | mitigate | Parse exact repository dispatch URL and validate only that run ID; never list/select by candidate. |
| T-13.1-56 | T | Resume prefix | mitigate | Compare full expanded records and recompute retained log digests before skipping any command. |
| T-13.1-57 | R | Dispatch interruption | mitigate | Atomic intent/result journal plus fail-closed no-redispatch behavior. |
| T-13.1-58 | I | Failed historical attempt | mitigate | Durable supplemental record states exact provenance and explicitly denies authority. |
| T-13.1-59 | D | Destructive restart | mitigate | Preserve output and resume only the exact next command; terminal validation is idempotent. |
| T-13.1-62 | T | Dispatch/run evidence chain | mitigate | Validator reparses command 55 URL, binds command 56/58 run views, recomputes journal/log digests, and negative-tests six independent substitutions. |
| T-13.1-63 | S | Dispatch journals | mitigate | Confine journal paths and validate intent/result candidate, tree, repository, workflow, ref, argv, manifest, URL, run, and cross-digest identities. |
</threat-model>

<verification>Exactly 18 named lifecycle tests are discovered and the complete stable target runs; independent negative fixtures reject URL, run-ID, initial-view, terminal-view, intent-journal, and result-journal substitutions; exact capability execution, fuzz cleanliness, Bash checks, managed checker, the ordered Rust gate, plan schema/structure, lifecycle validation, and diff review pass without publication or dispatch.</verification>
<success-criteria>The next Plan 21 candidate has a proven non-destructive producer, exact dispatch-run and run-view binding, independently validated dispatch journals, tracked fuzz resolution, correct capability command, complete lifecycle regressions, and truthful non-authoritative failed-attempt history.</success-criteria>
<output>After completion, create `.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-23-SUMMARY.md`.</output>
