---
phase: 13.1-restore-bright-builds-structural-compliance
plan: "15"
type: execute
wave: 15
depends_on: ["13.1-14"]
files_modified: []
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13.1-2026-07-28T00-14-49
generated_at: 2026-07-28T00:38:59Z
must_haves:
  truths:
    - "The exact baseline 103 paths are owned exactly once, now compliant, and no allowlist entry exists."
    - "Every required Rust, Cargo-only, package, schema, differential, xtask, native, CMake/CTest, shell, fuzz, testbed, Markdown, and diff gate passes before commit."
    - "Exactly one structural source commit fast-forwards original main while the preservation manifest still verifies the complete prior dirty state."
    - "Lifecycle/docs commits are permitted only after the source commit is green and integrated."
  artifacts:
    - path: "/tmp/liquidfun-rs-phase13.1/final-verification.json"
      provides: "Machine-readable command, exit, candidate, inventory, and diff-review evidence"
    - path: "/tmp/liquidfun-rs-phase13.1/source-commit.txt"
      provides: "Single verified structural source commit SHA"
  key_links:
    - from: "verified isolated source commit"
      to: "original main"
      via: "manifest-guarded `git merge --ff-only`"
      pattern: "ff-only"
---
<objective>
Reconcile exact scope, run every final gate, create one source commit, and integrate without disturbing Phase 14.

Purpose: Fulfill D-01 through D-15 and break the red-gate circularity safely.
Output: Green repository, one atomic source commit on original main, preserved dirty work, final verification evidence, and permission for later lifecycle/docs commit.
</objective>
<execution-context>
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-EXECUTION.md
</execution-context>
<aggregate-execution>Execute only as bounded sequential segment 13.1-15 through execute-phase Pattern-B task-only agent semantics under `13.1-EXECUTION.md`. The standard per-plan execute-plan route is unsupported because its commits/summaries conflict with higher repository pre-commit rules. Do not stage, commit, write a SUMMARY, or modify lifecycle/planning state; keep progress/evidence and the complete tracked-plus-untracked candidate snapshot external. Plans 01-14 and Plan 15 Tasks 1-2 form one aggregate candidate; only the orchestrator may perform Plan 15 Task 3 after the full unchanged candidate is green.</aggregate-execution>
<context>
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-CONTEXT.md
@AGENTS.md
@tools/reference/CMakeLists.txt
</context>
<tasks>
<task type="auto">
  <name>Task 1: Reconcile the exact 103-path scope and review the complete diff</name>
  <files>None; review and external verification evidence only</files>
  <action>Read every Phase 13.1 external segment evidence record, original preservation manifest, and baseline findings inventory before any commit. Per D-01, deterministically extract all `files_modified` entries that were members of the original checker output and prove they equal the original 103-path set with no missing/extra/duplicate paths; support files/new sibling modules are a separate reviewed set. Per D-02, run the live checker and require `scanned=648 exceptions=0 findings=0`, and prove `.bright-builds-rules-checks.tsv` is absent or unchanged with no current entries. Per D-03, if any supposedly external/generated finding appears, stop and require exact provenance proof rather than authorizing an owned-file exception. Review `git status --short`, `git diff --check`, `git diff --stat`, full name-status, public export/schema/fixture/evidence/Cargo target/CMake input diffs, and all new sibling paths against the concrete patterns authorized by Plans 02-14. Reject arbitrary numbered chunks, unrelated changes, modified reviewed evidence, paused Phase 14 paths, or any file over 628 lines. Record exact inventories/digests in final verification JSON.</action>
  <verify><automated>cd /tmp/liquidfun-rs-phase13.1/worktree &amp;&amp; bun scripts/bright-builds-check.ts all &amp;&amp; git diff --check &amp;&amp; test -z "$(git diff --name-only -- crates/liquidfun/tests/particle_group_properties.rs crates/liquidfun/tests/particle_group_properties/model.rs crates/liquidfun/tests/particle_group_properties/snapshot.rs)"</automated></verify>
  <done>Exact-once scope is proven, checker is 0/0, diff is intentional/cohesive, no exception/evidence/Phase 14 drift exists.</done>
</task>
<task type="auto">
  <name>Task 2: Run the complete pre-commit verification matrix</name>
  <files>None; verification outputs below `/tmp/liquidfun-rs-phase13.1` and ignored target directories only</files>
  <action>Read AGENTS pre-commit order, `13.1-EXECUTION.md`, the final Plan 14 snapshot, and D-12 through D-15 first. Run, fail-fast, and record every command/exit in `final-verification.json`. Required ordered Rust gate: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then rerun `bun scripts/bright-builds-check.ts all`. Run full package tests for `liquidfun-test-protocol`, `liquidfun-differential`, and `xtask`; schema/fixture/comparison/minimizer/supervisor tests; `cargo xtask package verify`; docs/inventory corpus/provenance/upstream/phase13 acceptance/check gates. Refresh the final cumulative snapshot with `tracked.patch` from `git diff --binary --full-index`, `untracked-manifest.json`, `untracked.tar`, `candidate-tree-manifest.json`, and `snapshot.json`; the untracked contract contains encoded NUL-safe paths, modes, sizes, file SHA-256 values, and archive digest. Create or validate a clean detached worktree at `/tmp/liquidfun-rs-phase13.1/cargo-only` from the preserved base, apply `tracked.patch`, validate every archive member against `untracked-manifest.json`, and extract `untracked.tar`; do not initialize `third_party/liquidfun`. Recompute `candidate-tree-manifest.json` in that worktree and require byte-for-byte manifest equality and equal SHA-256 digest with the aggregate worktree before running `cargo xtask check`, ordinary Cargo build/test, and `cargo xtask package verify`. Run `cargo xtask upstream configure/build` for both `oracle-debug` and `oracle-release`, build `liquidfun-reference-protocol-tests`, and run protocol CTest in both build dirs. Run rigid-world debug/release compare, replay, and determinism. Run shell syntax/contract checks, exact nightly fuzz build, headless testbed build/tests/capability commands, `cargo test --workspace --doc`, denied-warning workspace rustdoc, and `just markdown-check`. Rerun diff review and checker after format. No staging/commit is allowed until all commands pass; no retry may conceal deterministic failure.</action>
  <verify><automated>cd /tmp/liquidfun-rs-phase13.1/worktree &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bun scripts/bright-builds-check.ts all &amp;&amp; cargo test -p liquidfun-test-protocol --all-features &amp;&amp; cargo test -p liquidfun-differential --all-features &amp;&amp; cargo test -p xtask --all-features &amp;&amp; cargo xtask package verify &amp;&amp; cargo xtask upstream configure --preset oracle-debug &amp;&amp; cargo xtask upstream build --preset oracle-debug &amp;&amp; cmake --build target/reference/oracle-debug --target liquidfun-reference-protocol-tests &amp;&amp; ctest --test-dir target/reference/oracle-debug --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$' &amp;&amp; cargo xtask upstream configure --preset oracle-release &amp;&amp; cargo xtask upstream build --preset oracle-release &amp;&amp; cmake --build target/reference/oracle-release --target liquidfun-reference-protocol-tests &amp;&amp; ctest --test-dir target/reference/oracle-release --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$'</automated></verify>
  <done>Every specified gate passes on one unchanged candidate diff and the final evidence record is complete.</done>
</task>
<task type="auto">
  <name>Task 3: Commit and integrate source, prove preservation, then create lifecycle docs</name>
  <files>All reviewed structural/support source paths from Plans 02-14; after integration only, all 15 Phase 13.1 summaries plus authorized lifecycle, STATE, and ROADMAP documents; never Phase 14 source paths</files>
  <action>This task is orchestrator-only. Read final verification evidence and preservation manifest first. Re-run checker and ordered Rust gate if the candidate changed after Task 2. Stage only the reviewed structural source/new sibling/support paths; explicitly exclude all original dirty paths, Phase 13.1 planning files, STATE/ROADMAP/config/lessons, and target/temp output. Review staged diff/name list and create exactly one commit `refactor(13.1): restore structural compliance`. Record its full SHA and prove its parent equals the preserved original HEAD. Before integration, re-run the manifest verifier against the original root and require the exact original porcelain set plus every tracked/untracked hash/archive inventory. Require original `main` HEAD still equals the preserved base and the structural commit paths are disjoint from dirty paths. Fast-forward with `git -C &lt;original-root&gt; merge --ff-only &lt;source-sha&gt;`; no cherry-pick/merge commit. Immediately afterward, before any planning write, re-run the complete preservation verifier: the exact pre-existing porcelain records and all dirty tracked/untracked hashes/content must match, while original main equals source SHA. Run the Bright checker in the original worktree and require green. Only after this exact post-fast-forward baseline proof, create all `13.1-01-SUMMARY.md` through `13.1-15-SUMMARY.md` from external segment evidence and create the authorized lifecycle/STATE/ROADMAP updates as one post-integration set. The docs set is restricted to `.planning/ROADMAP.md`, `.planning/STATE.md`, and this phase directory's 15 plans, 15 summaries, `13.1-CONTEXT.md`, `13.1-DISCUSSION-LOG.md`, and `13.1-EXECUTION.md`; never include structural source, Phase 14 source, config, lessons, or unrelated planning paths. Before the docs commit, run the mandatory ordered Rust gate again without omission: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then run `bun scripts/bright-builds-check.ts all`, `just markdown-check`, validate every summary with the GSD summary schema, and run the Phase 13.1 lifecycle validator with plans required. Bind the green command evidence to the staged-tree digest, audit the staged names against the exact allowlist, and create one separate docs commit. Record `docs_sha`; prove its sole parent is `source_sha`, final `main` equals `docs_sha`, every committed path is authorized, all 15 summaries validate, and lifecycle remains valid. No segment agent writes these artifacts.</action>
  <verify><automated>original_root="$(jq -r .original_root /tmp/liquidfun-rs-phase13.1/preservation.json)" &amp;&amp; source_sha="$(cat /tmp/liquidfun-rs-phase13.1/source-commit.txt)" &amp;&amp; docs_sha="$(cat /tmp/liquidfun-rs-phase13.1/docs-commit.txt)" &amp;&amp; test "$(git -C "$original_root" rev-parse main)" = "$docs_sha" &amp;&amp; test "$(git -C "$original_root" rev-parse "$docs_sha^")" = "$source_sha" &amp;&amp; test "$(git -C "$original_root" rev-list --count "$source_sha..$docs_sha")" = 1 &amp;&amp; test "$(git -C "$original_root" rev-parse --is-inside-work-tree)" = true &amp;&amp; cd "$original_root" &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bun scripts/bright-builds-check.ts all &amp;&amp; just markdown-check &amp;&amp; test -z "$(git diff-tree --no-commit-id --name-only -r "$docs_sha" | rg -v '^\\.planning/(ROADMAP\\.md|STATE\\.md|phases/13\\.1-restore-bright-builds-structural-compliance/(13\\.1-(0[1-9]|1[0-5])-(PLAN|SUMMARY)\\.md|13\\.1-(CONTEXT|DISCUSSION-LOG|EXECUTION)\\.md))$')" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r "$docs_sha" | wc -l | tr -d ' ')" -gt 0 &amp;&amp; for summary in .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-*-SUMMARY.md; do node "/Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs" frontmatter validate "$summary" --schema summary | jq -e '.valid == true' &gt;/dev/null; done &amp;&amp; test "$(find .planning/phases/13.1-restore-bright-builds-structural-compliance -maxdepth 1 -name '13.1-*-SUMMARY.md' | wc -l | tr -d ' ')" = 15 &amp;&amp; test "$(node "/Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 13.1 --require-plans --raw)" = valid</automated></verify>
  <done>One atomic structural source commit is integrated, the immediate post-fast-forward baseline proof passes before planning writes, all 15 summaries and authorized lifecycle documents are then created from external evidence, and a separate audited docs-only commit contains no source or Phase 14 paths.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Uncommitted candidate → source commit | Only reviewed green structural paths may be staged. |
| Detached commit → dirty original main | Fast-forward must preserve exact tracked/untracked user work. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-13.1-28 | T | Scope reconciliation | mitigate | Exact 103-set equality, zero exceptions, constrained new-path review. |
| T-13.1-29 | R | Verification | mitigate | Machine-readable command/candidate evidence; fail-fast unchanged diff. |
| T-13.1-30 | T | Fast-forward | mitigate | Pre/post manifest verification, disjoint-path proof, ff-only integration. |
| T-13.1-31 | E | Commit authority | mitigate | Only final plan stages; lifecycle/docs allowed only after green integration. |
</threat-model>
<verification>
Exact scope reconciliation, complete final matrix, staged/full diff review, one-parent source commit, ff-only original integration, preservation manifest verification, and original-worktree Bright checker all pass.
</verification>
<success-criteria>
Repository-wide structural compliance is green with all 103 owned files refactored, zero exceptions, one atomic source commit integrated, and paused work intact.
</success-criteria>
<output>Do not write a SUMMARY or any planning/lifecycle artifact. Write only `/tmp/liquidfun-rs-phase13.1/segments/13.1-15/evidence.json` and the complete candidate snapshot required by `13.1-EXECUTION.md`; the orchestrator creates this plan summary only in the authorized post-integration docs set.</output>
