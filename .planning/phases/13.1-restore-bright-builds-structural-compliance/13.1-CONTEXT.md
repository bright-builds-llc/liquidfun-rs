---
generated_by: gsd-discuss-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13.1-2026-07-28T00-14-49
generated_at: 2026-07-28T00:14:49.676Z
---

# Phase 13.1: Restore Bright Builds Structural Compliance - Context

**Gathered:** 2026-07-27
**Status:** Ready for planning
**Mode:** Yolo

<domain>
## Phase Boundary

Restore the repository-wide Bright Builds starter check to green by decomposing every
first-party tracked source file currently above 628 physical lines. Preserve behavior,
public Rust paths, wire schemas, CLI contracts, Cargo test-target identities, C++ adapter
interfaces, deterministic ordering, and evidence semantics. This is a prerequisite for
resuming Phase 14; it does not implement the Windows particle-group repair itself.

</domain>

<decisions>
## Implementation Decisions

### Ownership and exception policy

- **D-01:** Treat all 103 current file-length findings as repository-owned and refactorable. The ownership audit found no generated, vendored, copied, or otherwise non-refactorable finding.
- **D-02:** Add no `.bright-builds-rules-checks.tsv` entries for the current baseline. The actual external surfaces under `third_party/` and `vendor/` are already excluded by the managed checker.
- **D-03:** If implementation discovers a genuinely external or generated finding that the audit missed, stop and prove its provenance before adding one exact-path exception with a specific reason. Do not use temporary exceptions for owned files.

### Refactor boundaries

- **D-04:** Preserve externally observed roots as stable facades. Move cohesive behavior into `foo.rs` plus `foo/` sibling modules and retain existing re-exports, visibility, public paths, CLI entrypoints, exported C++ functions, and test-binary names.
- **D-05:** Keep exact wire, schema, evidence, and foreign-language declarations in explicit contract rings while extracting parsing, validation, encoding, comparison, and effectful execution behind them.
- **D-06:** Keep integration-test root filenames and test names stable. Extract behavior-family cases, fixtures, and support into private sibling modules without changing filterable target identity or deterministic registration assumptions.
- **D-07:** Prefer cohesive files below roughly 500 lines when a natural boundary exists, while making 628 physical lines the enforced maximum. Do not create arbitrary contiguous chunks merely to satisfy the counter.

### Execution and recovery

- **D-08:** Preserve the current uncommitted Phase 14 diagnostic work in the original worktree. Execute Phase 13.1 from an isolated clean worktree based on the current committed Phase 14 planning head.
- **D-09:** Work in dependency order: protocol contracts before differential consumers; then core engine/public contracts; xtask and evidence tooling; C++ reference; testbed, fuzz, shell, and integration-test decomposition.
- **D-10:** Because repository policy forbids commits while the checker is red, use scoped verification and recoverable external patch snapshots between waves, then create one atomic Phase 13.1 source commit only after all 103 findings and every final gate are green.
- **D-11:** Integrate the green structural commit back into the original worktree before resuming Phase 14. Do not mix or overwrite the existing Phase 14 edits.

### Verification and evidence integrity

- **D-12:** Run focused owning tests after each extraction and aggregate package/surface gates after each wave. A compile-only result is insufficient for schema, CLI, replay, package, shell, native, or evidence boundaries.
- **D-13:** Preserve externally observable bytes, schemas, resolved inputs, diagnostics, CLI exit behavior, and semantic digests. Exact source, candidate-SHA, or adapter-content identities may change only when the existing provenance pipeline recomputes and rebinds them consistently.
- **D-14:** Verify C++ adapter refactors with the canonical CMake builds, protocol CTest coverage, updated adapter-input closure, and upstream read-only checks. Do not force old content identities to remain equal after legitimate source movement.
- **D-15:** Before the atomic commit, run the ordered Rust gate (`cargo fmt --all`, Clippy with denied warnings, all-target build, all-feature tests), the managed Bright Builds checker, package/Cargo-only isolation, protocol/schema/differential/xtask contracts, native reference verification, Markdown checks for non-GSD docs, and diff review.

### the agent's Discretion

- Exact private module names and the smallest coherent extraction within each locked batch.
- Recovery snapshot filenames and temporary location, provided they remain outside tracked source and contain no secrets.
- Whether a near-threshold file clears through test extraction alone or one additional semantic split.

</decisions>

<canonical-refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Repository and managed standards

- `AGENTS.md` — Repository-local workflow, Rust verification, and Markdown rules.
- `AGENTS.bright-builds.md` — Managed Bright Builds entrypoint and checker contract.
- `standards/core/architecture.md` — Deep-module and functional-core/imperative-shell boundaries.
- `standards/core/code-shape.md` — Cohesive module boundaries and the 628-line refactor trigger.
- `standards/core/testing.md` — Behavioral and Arrange/Act/Assert test expectations.
- `standards/core/verification.md` — Mandatory pre-commit and changed-surface verification.
- `standards/languages/rust.md` — Rust module, error, and API conventions.
- `scripts/bright-builds-check.ts` — Exact tracked-file scope, exclusions, threshold, and exception format.

### Project architecture and evidence

- `.planning/PROJECT.md` — Native-Rust, determinism, provenance, and evidence constraints.
- `.planning/ROADMAP.md` — Phase ordering and Phase 13.1 prerequisite boundary.
- `reference/source-map.toml` — Authority for locally derived or altered upstream material.
- `tools/reference/adapter-inputs.txt` — Repository-owned C++ adapter input closure.
- `CMakeLists.txt` — Native reference targets and source registration.

</canonical-refs>

<code-context>
## Existing Code Insights

### Reusable Assets

- Existing `foo.rs` plus `foo/` modules across the workspace provide the preferred extraction pattern.
- Existing schema, CLI, replay, package, provenance, and CTest suites provide contract-preservation gates.
- The managed checker deterministically reports all tracked applicable sources and already excludes conventional external dependency roots.

### Established Patterns

- Public Rust facades re-export private implementation types rather than exposing storage layout.
- Protocol and evidence code separates validated domain values from effectful runners.
- C++ remains a private adapter/oracle boundary whose exact input closure is content-bound.
- Integration-test roots are stable Cargo targets with sibling support modules.

### Integration Points

- `liquidfun-test-protocol` must remain stable before `liquidfun-differential` consumers move.
- Core `liquidfun` public paths, authoritative particle storage, and deterministic solver ordering must remain unchanged.
- `tools/xtask`, release scripts, CMake source lists, and adapter-input digests must stay mutually consistent.
- The clean Phase 13.1 commit must integrate without touching the paused Phase 14 diagnostic files.

</code-context>

<specifics>
## Specific Ideas

- Refactor what the repository owns; use exceptions only for files it genuinely does not own.
- Treat oversized-file cleanup as a real architecture pass, not a blanket grandfathering exercise.

</specifics>

<deferred>
## Deferred Ideas

- Phase 14 continues the Windows particle-group invariant diagnosis and repair after this prerequisite is green.
- Broader behavior changes, compatibility promotion, and Phase 15 candidate evidence remain outside this structural phase.

</deferred>

***

*Phase: 13.1-restore-bright-builds-structural-compliance*
*Context gathered: 2026-07-27*
