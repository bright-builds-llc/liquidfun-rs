---
phase: 13.1-restore-bright-builds-structural-compliance
plan: "19"
type: execute
wave: 2
depends_on: ["13.1-17"]
files_modified:
  - scripts/phase13-1-gap-verification.sh
  - scripts/phase13-1-gap-verification-manifest.json
  - scripts/phase13-1-validate-gap-evidence.sh
  - tools/xtask/tests/phase13_1_gap_verification.rs
autonomous: true
requirements: []
gap_closure: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13.1-2026-07-28T00-14-49
generated_at: 2026-07-31T17:25:40Z
must_haves:
  truths:
    - "One repository-owned command reproduces the complete non-deferred Phase 13.1 verification matrix on one exact source candidate."
    - "Every xtask integration target is selected explicitly, and `phase13_acceptance_contract` is mechanically proven to be the sole excluded target."
    - "Terminal evidence is written identity-last and binds commands, exits, logs, environment, candidate commit, tree, and original structural commit."
    - "A separate executable validator, not the producer, proves manifest order, environments, candidate/tree/ancestry, retained-log digests, canonical D1 identity, and publication failure behavior."
  artifacts:
    - path: "scripts/phase13-1-gap-verification.sh"
      provides: "Fail-fast candidate-bound gap verification producer"
    - path: "scripts/phase13-1-gap-verification-manifest.json"
      provides: "Authoritative ordered commands, environments, artifacts, and sole deferral"
    - path: "scripts/phase13-1-validate-gap-evidence.sh"
      provides: "Independent executable verifier for producer output and retained logs"
    - path: "tools/xtask/tests/phase13_1_gap_verification.rs"
      provides: "Manifest inventory plus producer/validator positive and negative publication contracts"
  key_links:
    - from: "scripts/phase13-1-gap-verification.sh"
      to: "target/phase13-1-gap-verification/<candidate>/final-verification.json"
      via: "identity-last atomic publication after every required command passes"
      pattern: "final-verification.json"
    - from: "scripts/phase13-1-validate-gap-evidence.sh"
      to: "scripts/phase13-1-gap-verification-manifest.json"
      via: "independent command/environment/order comparison and retained-log digest replay"
      pattern: "phase13_acceptance_contract"
---

<objective>
Make final Phase 13.1 gap verification reproducible and durable.

Purpose: Replace the lost ad hoc `/tmp` proof with a checked repository-owned producer and an independent validator while excluding only the explicitly deferred Phase 15 exact-head acceptance target.
Output: Authoritative manifest, safe verification producer, separate executable validator, and positive/negative contract tests.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@standards/core/verification.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-15-PLAN.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-VERIFICATION.md
@scripts/phase12-release-evidence.sh
@scripts/phase12-release-evidence/common.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the authoritative non-deferred command manifest</name>
  <files>scripts/phase13-1-gap-verification-manifest.json, tools/xtask/tests/phase13_1_gap_verification.rs</files>
  <read-first>AGENTS.md Bash and Rust rules; standards/core/verification.md; .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-VERIFICATION.md; .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-15-PLAN.md; scripts/phase12-release-evidence.sh; scripts/phase12-release-evidence/common.sh; scripts/phase12-miri.sh; tools/xtask/tests/regression_workflow.rs</read-first>
  <behavior>
    - "The manifest contains every `tools/xtask/tests/*.rs` target except `phase13_acceptance_contract`, with no package-wide xtask test command."
    - "The 27 selected xtask targets have exact ordered argv arrays and explicit environment objects."
    - "The manifest also orders the Rust gate, checker, package contracts, local D2 and canonical D1 native gates, auxiliary surfaces, Markdown, diff, and lifecycle inputs."
  </behavior>
  <action>Create JSON schema `phase13-1-gap-verification-manifest-v1` with an ordered `commands` array whose entries contain immutable `id`, argv array, exact environment object, retained stdout/stderr log paths, and evidence class; an `artifacts` array for canonical runner identity/logs; and `deferred_xtask_targets:["phase13_acceptance_contract"]`. Enumerate these xtask targets as individual `cargo test -p xtask --test &lt;target&gt; --all-features` commands in lexical order: `catalog_cli`, `corpus_closure`, `corpus_discovery`, `corpus_model`, `coverage_workflow`, `differential_cli`, `docs_contract`, `inventory_cli`, `nightly_toolchain`, `package_cli`, `performance_cli`, `performance_workflow`, `phase10_evidence_cli`, `phase11_evidence_cli`, `phase13_1_canonical_native_workflow`, `phase13_1_gap_verification`, `phase13_evidence_contract`, `phase13_promotion_contract`, `phase9_evidence_cli`, `phase9_witness_provenance`, `platform_workflow`, `provenance_cli`, `regression_workflow`, `release_attestation`, `release_cli`, `safety_evidence_contract`, and `upstream_cli`. Do not include `cargo test -p xtask --all-features`, `phase13_acceptance_contract`, or `cargo xtask phase13 acceptance`. Also encode the exact ordered Rust gate, managed checker, protocol/differential packages, package/docs/corpus/provenance/upstream checks, local D2 native checks, the named canonical workflow dispatch/watch/download, auxiliary shell/fuzz/testbed/doc commands, Markdown, final checker/diff/clean-tree checks, and merge/lifecycle inputs. Add a Rust contract test that inventories every `tools/xtask/tests/*.rs` stem and proves `selected ∪ {phase13_acceptance_contract}` equals that inventory with empty overlap; it must also compare exact argv/environment/order, prove the excluded target is the sole deferral recorded in `13.1-VERIFICATION.md`, and reject any broad xtask package command or Phase 13 acceptance CLI.</action>
  <verify>
    <automated>cargo test -p xtask --test phase13_1_gap_verification</automated>
  </verify>
  <acceptance-criteria>The automated test exits 0 with exactly 27 explicitly selected xtask targets and exactly one excluded target, `phase13_acceptance_contract`; no broad xtask package command or Phase 15 work is present.</acceptance-criteria>
  <done>The non-deferred matrix has one authoritative, mechanically complete command and environment manifest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the independent executable evidence validator</name>
  <files>scripts/phase13-1-validate-gap-evidence.sh, tools/xtask/tests/phase13_1_gap_verification.rs</files>
  <read-first>AGENTS.md Bash and Rust rules; standards/core/architecture.md; standards/core/testing.md; standards/core/verification.md; scripts/phase13-1-gap-verification-manifest.json; .github/workflows/phase13-1-canonical-native.yml; scripts/phase12-release-evidence/common.sh; tools/xtask/tests/phase13_1_gap_verification.rs; .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-VERIFICATION.md</read-first>
  <behavior>
    - "The validator parses evidence independently of the producer and compares every command id, argv, environment, order, exit, and log path to the manifest."
    - "It recomputes every retained local and canonical log digest, proves candidate commit/tree/HEAD binding and structural ancestry, and rejects path escape or symlink substitution."
    - "Failed-command terminal publication, missing log, digest mismatch, reordered command, environment drift, candidate drift, tree drift, non-D1 canonical identity, and post-run dirty tree all fail."
  </behavior>
  <action>Implement `scripts/phase13-1-validate-gap-evidence.sh` with `set -euo pipefail` and arguments `&lt;manifest&gt; &lt;evidence-json&gt; &lt;repository-root&gt; &lt;retained-root&gt;`. It must use `jq`, Git, and platform-safe SHA-256 helpers from the existing release evidence shell library, never source or invoke the producer, and never mutate evidence. Parse schema before use; compare the complete ordered command and environment records byte-for-byte in logical JSON form to the manifest after expansion of only `${CANDIDATE}`, `${CANDIDATE_TREE}`, `${OUTPUT_ROOT}`, `${REMOTE_REF}`, and `${CANONICAL_RUN_ID}`. Require every exit zero, unique command ID, confined regular nonsymlink log, recomputed digest, full lowercase candidate SHA, existing candidate object, exact Git tree, candidate still equal to clean HEAD, structural commit `981908ea87b6789b6b6e9aa136e65a369c5c736d` with parent `5394fd92ed036fe4f90358501848a61fa81cfc6d` and candidate ancestry, merge facts, zero checker findings/exceptions, and a downloaded canonical identity whose candidate/tree match, tool versions are Rust 1.97.0/Clang 22.1.8/CMake 4.3.3/Ninja 1.13.2, platform is Ubuntu 24.04 x86_64, tier is D1, and every canonical retained-log digest recomputes. Require `complete:true` only after those checks. Extend the Rust test to build temporary Git repositories and synthetic manifest/evidence/log fixtures. Positive evidence must pass; separate tests must reject failed-command terminal publication, missing log, digest mismatch, order drift, environment drift, candidate/tree drift, structural ancestry failure, symlink/path escape, dirty post-run tree, and false/missing canonical D1 identity.</action>
  <verify>
    <automated>bash -n scripts/phase13-1-validate-gap-evidence.sh &amp;&amp; shellcheck scripts/phase13-1-validate-gap-evidence.sh &amp;&amp; cargo test -p xtask --test phase13_1_gap_verification --all-features evidence_validator</automated>
  </verify>
  <acceptance-criteria>The separate validator and all positive/negative executable fixtures exit as expected, including failed-command, missing-log, and candidate-drift publication failures; the validator never calls producer code.</acceptance-criteria>
  <done>Candidate evidence has an independent executable authority that replays every integrity claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the repository-owned verification producer</name>
  <files>scripts/phase13-1-gap-verification.sh, tools/xtask/tests/phase13_1_gap_verification.rs</files>
  <read-first>scripts/phase13-1-gap-verification.sh; scripts/phase13-1-gap-verification-manifest.json; scripts/phase13-1-validate-gap-evidence.sh; tools/xtask/tests/phase13_1_gap_verification.rs; .github/workflows/phase13-1-canonical-native.yml; AGENTS.md; standards/core/verification.md; .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-VERIFICATION.md; justfile; CMakePresets.json; rust-toolchain.toml; rust-toolchain-nightly.toml; tools/reference/CMakeLists.txt; .mdformat.toml; scripts/phase12-release-evidence/common.sh</read-first>
  <behavior>
    - "Missing/short/non-HEAD/non-remote candidate SHA, dirty source tree, failed command, absent log, canonical failure, or candidate drift prevents terminal evidence."
    - "The producer executes the manifest exactly and delegates terminal acceptance to the independent validator."
  </behavior>
  <action>Implement the Bash producer with `set -euo pipefail` and arguments `&lt;full-candidate-sha&gt; &lt;output-root&gt; &lt;remote-default-branch&gt;`. Require candidate equals clean HEAD, `gh repo view --json defaultBranchRef` names the supplied branch, `refs/remotes/origin/&lt;default&gt;` and `git ls-remote origin refs/heads/&lt;default&gt;` both resolve to the candidate, the remote candidate contains `.github/workflows/phase13-1-canonical-native.yml` with the same blob SHA as the local candidate, the GitHub Actions workflow API recognizes that workflow on the default branch, the original structural commit is an ancestor, and output resolves below repository `target/`. The producer must never push, force-push, rewrite, create, or select a remote ref; Plan 21's explicit publication checkpoint is the sole authority for making the workflow reachable. Record OS/architecture, Rust/Cargo, CMake, Ninja, C++ compiler, each preset, exact environment overrides, command argv, exit, and SHA-256 of bounded stdout/stderr logs. Execute manifest entries in order without `eval`; expand only the five declared placeholders and pass argv as arrays. Run without `RUST_TEST_THREADS`; use one isolated `CARGO_TARGET_DIR` and `CARGO_BUILD_JOBS=1` only for the documented macOS cache/build pressure. For xtask, run the 27 explicit test targets from Task 1 and never the package-wide command. Dispatch `.github/workflows/phase13-1-canonical-native.yml` with `--ref &lt;remote-default-branch&gt;` and input `candidate_sha=&lt;full-candidate-sha&gt;`, wait exactly once with `gh run watch --exit-status`, assert run `headSha` equals candidate, download the success artifact, and retain its identity/logs. Do not run `phase13_acceptance_contract`, `cargo xtask phase13 acceptance`, or mutate reviewed fixtures. On failure retain partial JSONL records and logs but remove any pending/terminal JSON. On success write a confined `final-verification.json.pending` containing schema, candidate SHA/tree, structural source SHA/parent, merge facts, exact manifest digest, commands, environments, log digests, canonical run/artifact identity, zero findings/exceptions, and `complete:true`; invoke the independent validator on that pending file, then atomically rename it to `final-verification.json` only if validation exits 0. Add a production-source contract plus temporary-repository executions for wrong candidate, dirty tree, non-default branch, missing remote workflow, failed command, missing log, canonical drift, and candidate drift; a test-only manifest is accepted only inside a temporary repository with `test_fixture:true` and must never weaken production manifest checks.</action>
  <verify>
    <automated>bash -n scripts/phase13-1-gap-verification.sh &amp;&amp; shellcheck scripts/phase13-1-gap-verification.sh &amp;&amp; cargo test -p xtask --test phase13_1_gap_verification --all-features</automated>
  </verify>
  <acceptance-criteria>The producer is rerunnable, confined, fail-fast, identity-last, uses the exact manifest, never publishes a Git ref, dispatches only a workflow already present on the remote default branch at the exact candidate, obtains candidate-bound canonical evidence, invokes the independent validator before evidence publication, and proves no terminal evidence survives every named negative case.</acceptance-criteria>
  <done>One checked producer plus a separate validator can generate and independently certify every open non-history gate without running deferred Phase 15 acceptance.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Repository candidate → evidence producer | The producer must bind one immutable clean source state. |
| Command output → terminal JSON | Logs are untrusted until exit and digest validation complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13.1-40 | S | Candidate identity | mitigate | Full SHA equals clean HEAD and contains the original structural commit. |
| T-13.1-41 | T | Evidence logs | mitigate | SHA-256 every bounded log and publish identity last. |
| T-13.1-42 | R | Matrix completeness | mitigate | Source-contract test asserts every required command family and the intentional Phase 15 exclusion. |
| T-13.1-49 | E | Producer self-certification | mitigate | Separate executable validator compares an authoritative manifest and replays all retained digests and Git identities. |
</threat-model>

<verification>Manifest inventory tests, producer/validator Rust contracts, Bash syntax, shellcheck, and positive/negative executable fixtures pass without running the expensive final matrix.</verification>
<success-criteria>The complete non-deferred matrix is reproducible on one candidate, excludes only the documented exact-head target, includes candidate-bound canonical proof, and cannot self-certify partial or stale results.</success-criteria>
<output>After completion, create `.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-19-SUMMARY.md`.</output>
