---
phase: 13.1-restore-bright-builds-structural-compliance
plan: "17"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun-differential/src/oracle_identity.rs
  - crates/liquidfun-differential/tests/oracle_identity.rs
  - .github/workflows/phase13-1-canonical-native.yml
  - tools/xtask/tests/phase13_1_canonical_native_workflow.rs
autonomous: true
requirements: []
gap_closure: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13.1-2026-07-28T00-14-49
generated_at: 2026-07-31T17:25:40Z
must_haves:
  truths:
    - "Equivalent result translation units in different CMake targets produce one effective compile signature."
    - "A real flag difference still fails closed as divergent compile flags."
    - "Rigid compare, replay, and determinism pass on the local AppleClang Debug/Release boundary."
    - "A named canonical Ubuntu 24.04, Clang 22.1.8, CMake 4.3.3, and Ninja 1.13.2 runner can prove the same validator in Debug and Release against an exact candidate SHA."
  artifacts:
    - path: "crates/liquidfun-differential/src/oracle_identity.rs"
      provides: "Target-path-independent effective compile signature normalization"
    - path: "crates/liquidfun-differential/tests/oracle_identity.rs"
      provides: "Acceptance and rejection contracts for target-directory normalization"
    - path: ".github/workflows/phase13-1-canonical-native.yml"
      provides: "Candidate-bound canonical Linux Debug/Release native runner"
    - path: "tools/xtask/tests/phase13_1_canonical_native_workflow.rs"
      provides: "Executable workflow identity, command, and artifact contract"
  key_links:
    - from: "crates/liquidfun-differential/src/oracle_identity.rs"
      to: "tools/reference/src/generate_build_identity.cmake.in"
      via: "identical source-unit and `CMakeFiles/<result-target>.dir` normalization"
      pattern: "result-target"
    - from: ".github/workflows/phase13-1-canonical-native.yml"
      to: "target/phase13-1-canonical-native/<candidate>/identity.json"
      via: "exact-SHA checkout followed by canonical Debug/Release validation"
      pattern: "clang version 22\\.1\\.8"
---

<objective>
Repair the native effective-compile identity boundary without weakening canonical flag checks.

Purpose: Make the Rust checkout validator agree with the existing CMake identity generator on supported multi-target builds without allowing local portability evidence to impersonate canonical authority.
Output: Shared normalization semantics, focused tests, green local D2 rigid gates, and an exact candidate-bound canonical D1 workflow contract.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-VERIFICATION.md
@tools/reference/CMakeLists.txt
@tools/reference/src/generate_build_identity.cmake.in
@crates/liquidfun-differential/src/oracle_identity.rs
@crates/liquidfun-differential/tests/oracle_identity.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize result-target object directories in Rust identity checks</name>
  <files>crates/liquidfun-differential/src/oracle_identity.rs, crates/liquidfun-differential/tests/oracle_identity.rs</files>
  <read-first>AGENTS.md; .codex/tasks/lessons.md lessons reproduce-ci-at-exact-boundary and validate-all-canonical-presets; .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-VERIFICATION.md; tools/reference/CMakeLists.txt; tools/reference/src/generate_build_identity.cmake.in; crates/liquidfun-differential/src/oracle_identity.rs; crates/liquidfun-differential/tests/oracle_identity.rs</read-first>
  <behavior>
    - "`protocol_bits.cpp` under `CMakeFiles/liquidfun-reference-protocol-bits.dir` and the other result units under `CMakeFiles/liquidfun-reference-protocol.dir` are equivalent when all effective flags match."
    - "Changing `-DREVIEWED=1` to `-DUNREVIEWED=1` on one unit remains `DivergentCompileFlags`."
    - "Repository and build relocation do not change the digest."
  </behavior>
  <action>Extend the pure Rust command-signature normalizer to mirror the existing CMake generator exactly: after repository/build path replacement and result source filename replacement, replace every `CMakeFiles/&lt;target&gt;.dir` object-directory component with `CMakeFiles/&lt;result-target&gt;.dir`. Implement this with standard-library string scanning, rejecting malformed unmatched components rather than adding regex or shell parsing. Keep the full normalized commands used for the digest unchanged except for relocation placeholders; only comparison signatures receive the target placeholder. Update the fixture compile database so `protocol_bits.cpp` uses the bits target and the other three units use the protocol target, then prove equivalent targets pass and a true flag delta still fails.</action>
  <verify>
    <automated>cargo test -p liquidfun-differential --test oracle_identity --all-features</automated>
  </verify>
  <acceptance-criteria>The Rust validator and configured CMake generator apply the same two placeholders, equivalent multi-target commands validate, divergent flags fail closed, and digest relocation tests pass.</acceptance-criteria>
  <done>The identity validator distinguishes target layout from effective compiler semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Prove Debug and Release rigid gates on the local D2 compiler boundary</name>
  <files>crates/liquidfun-differential/src/oracle_identity.rs, crates/liquidfun-differential/tests/oracle_identity.rs</files>
  <read-first>AGENTS.md; .codex/tasks/lessons.md lesson-validate-all-canonical-presets; .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-VERIFICATION.md; crates/liquidfun-differential/src/oracle_identity.rs; crates/liquidfun-differential/tests/oracle_identity.rs; tools/reference/CMakeLists.txt; tools/reference/src/generate_build_identity.cmake.in; CMakePresets.json; tools/xtask/src/differential/oracle.rs; justfile; reference/upstream-lock.toml; tools/reference/adapter-inputs.txt</read-first>
  <action>Record `uname -s`, `uname -m`, `cmake --version`, Ninja version, `/usr/bin/c++ --version`, preset, build type, and the four normalized result compile commands before running native gates. On the required local boundary use `cargo xtask upstream configure/build` for both `oracle-debug` and `oracle-release`, build and run the protocol CTest in both presets, then run rigid compare in both presets plus Debug replay and two-run determinism. Require the local identity to remain D2 supported; do not label AppleClang as canonical D1, remove forbidden-flag checks, force old digests, edit the read-only upstream, or regenerate reviewed physics fixtures.</action>
  <verify>
    <automated>cargo xtask upstream configure --preset oracle-debug &amp;&amp; cargo xtask upstream build --preset oracle-debug &amp;&amp; cmake --build target/reference/oracle-debug --target liquidfun-reference-protocol-tests &amp;&amp; ctest --test-dir target/reference/oracle-debug --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$' &amp;&amp; cargo xtask upstream configure --preset oracle-release &amp;&amp; cargo xtask upstream build --preset oracle-release &amp;&amp; cmake --build target/reference/oracle-release --target liquidfun-reference-protocol-tests &amp;&amp; ctest --test-dir target/reference/oracle-release --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$' &amp;&amp; cargo xtask differential compare --scenario rigid-world --preset oracle-debug --session-profile one-shot &amp;&amp; cargo xtask differential compare --scenario rigid-world --preset oracle-release --session-profile one-shot &amp;&amp; cargo xtask differential replay --scenario rigid-world --preset oracle-debug --session-profile one-shot &amp;&amp; cargo xtask differential verify-determinism --scenario rigid-world --preset oracle-debug --runs 2</automated>
  </verify>
  <acceptance-criteria>Both presets build and pass CTest; local rigid compare/replay/determinism exit 0; recorded identity names the actual AppleClang/CMake/Ninja boundary and never claims D1 authority.</acceptance-criteria>
  <done>The local native/reference matrix is green with exact compiler and preset provenance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define the exact candidate-bound canonical native runner</name>
  <files>.github/workflows/phase13-1-canonical-native.yml, tools/xtask/tests/phase13_1_canonical_native_workflow.rs</files>
  <read-first>AGENTS.md; .codex/tasks/lessons.md lessons reproduce-ci-at-exact-boundary, use-explicit-ci-identities, and validate-all-canonical-presets; .github/workflows/oracle.yml; .github/workflows/phase13-acceptance.yml; CMakePresets.json; tools/reference/CMakeLists.txt; tools/reference/src/generate_build_identity.cmake.in; crates/liquidfun-differential/src/oracle_identity.rs; crates/liquidfun-differential/tests/oracle_identity.rs</read-first>
  <behavior>
    - "The workflow checks out the caller-supplied full candidate SHA with submodules and proves `git rev-parse HEAD` equals that SHA before any native command."
    - "The canonical identity is exactly Ubuntu 24.04 x86_64, Rust 1.97.0, Clang 22.1.8, CMake 4.3.3, and Ninja 1.13.2; AppleClang output cannot satisfy it."
    - "Debug and Release configure/build/protocol CTest plus rigid compare in both presets, Debug replay, and two-run Debug determinism all execute with strict warnings and existing forbidden-flag checks intact."
    - "A failed step uploads bounded logs but cannot publish terminal `identity.json`; success uploads candidate-bound logs, digests, compile commands, commit/tree, and a D1 identity."
  </behavior>
  <action>Create `.github/workflows/phase13-1-canonical-native.yml` as a named `workflow_dispatch` runner with required string input `candidate_sha`. Use `ubuntu-24.04`, `permissions: contents: read`, no cancel-in-progress, checkout SHA pin `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0`, upload-artifact SHA pin `043fb46d1a93c77aae656e7c1c64a875d1fc6a0a`, `fetch-depth: 0`, `persist-credentials: false`, and recursive submodules. Reject any input not matching 40 lowercase hex, then assert checked-out HEAD and tree. Install and assert Rust 1.97.0, Clang 22.1.8 through the checksum-pinned LLVM 22 installer already used by `oracle.yml`, CMake 4.3.3 archive SHA-256 `927b2368a946c37269c3a66225ab00544e756459cdd0b5d0da438694fb9ff802`, and Ninja 1.13.2 archive SHA-256 `5749cbc4e668273514150a80e387a957f933c6ed3f5f11e03fb30955e2bbead6`; set `CC=clang-22`, `CXX=clang++-22`, `LIQUIDFUN_XTASK_CMAKE=cmake`, `LIQUIDFUN_XTASK_NINJA=ninja`, and `LIQUIDFUN_XTASK_CXX=clang++-22`. Run focused Rust oracle-identity tests, canonical protocol identity tests, Debug and Release configure/build/protocol CTest, rigid compare for both presets, Debug replay, and Debug determinism with two runs. Preserve strict warning and forbidden-flag checks. Retain bounded per-command logs and normalized four-unit compile commands, record their SHA-256 digests, and write `target/phase13-1-canonical-native/<candidate>/identity.json` only after all commands pass with candidate SHA/tree, workflow run/job IDs, exact tool versions, presets, command order, exits, and `evidence_tier:D1`; upload success and failure artifacts under distinct names. Add a Rust workflow contract test that asserts every exact identity, pin, command, order, failure/success publication rule, and absence of any AppleClang-to-D1 promotion. Plan 21, not local Task 2, must execute this named runner against its frozen final candidate and independently validate the downloaded artifact.</action>
  <verify>
    <automated>cargo test -p xtask --test phase13_1_canonical_native_workflow --all-features</automated>
  </verify>
  <acceptance-criteria>The workflow contract test exits 0 and proves an exact candidate-bound canonical Debug/Release lane exists; this task does not claim D1 success from local AppleClang, and Plan 21 is explicitly responsible for obtaining candidate-bound runner evidence.</acceptance-criteria>
  <done>A named canonical runner contract exists for the changed validator, with D1 authority separated from local D2 evidence.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CMake compile database → Rust identity | Generated commands are untrusted until normalized and completely checked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13.1-35 | T | Compile signature | mitigate | Normalize only target-directory identity; retain all effective flags. |
| T-13.1-36 | S | Evidence tier | mitigate | Record actual compiler/platform and require D2 on local AppleClang. |
| T-13.1-37 | R | Native gate | mitigate | Run both presets, protocol CTest, compare, replay, and determinism. |
| T-13.1-48 | S | Canonical runner | mitigate | Exact-SHA checkout and exact Linux/compiler/tool assertions precede D1 publication. |
</threat-model>

<verification>Focused identity tests and the exact local D2 Debug/Release matrix pass; the named canonical workflow contract proves the same commands and authority checks will run on the frozen Plan 21 candidate.</verification>
<success-criteria>Supported multi-target native builds validate consistently while real compiler flag divergence remains fail-closed, and final D1 authority can come only from the exact candidate-bound canonical runner.</success-criteria>
<output>After completion, create `.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-17-SUMMARY.md`.</output>
