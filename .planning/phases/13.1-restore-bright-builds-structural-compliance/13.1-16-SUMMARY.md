---
phase: 13.1-restore-bright-builds-structural-compliance
plan: "16"
subsystem: differential-testing
tags: [rust, differential, concurrency, session-identity, test-isolation]
requires:
  - phase: 13.1-04
    provides: Stable differential CLI and supervisor facades
  - phase: 13.1-06
    provides: Stable round-trip integration-test roots and evidence contracts
provides:
  - Package-boundary process isolation for fake-oracle round-trip tests
  - Owned unique fake repositories with exact lifecycle cleanup
  - Concurrent second-request identity regression for reuse and sanitizer profiles
affects: [phase-13.1-verification, differential-evidence, round-trip-tests]
tech-stack:
  added: []
  patterns: [mutex-scoped external process slot, owned test repository guard]
key-files:
  created: []
  modified:
    - crates/liquidfun-differential/tests/round_trip.rs
    - crates/liquidfun-differential/tests/round_trip/real_oracle.rs
    - crates/liquidfun-differential/tests/round_trip/evidence.rs
    - crates/liquidfun-differential/tests/round_trip/cli_reports.rs
key-decisions:
  - "Serialize only fake-oracle repository setup and child execution; leave Rust's default package test concurrency unchanged."
  - "Own each PID-plus-atomic repository root through a guard that rejects nonempty reuse and removes only its exact root."
patterns-established:
  - "Process slot: bound scarce external child execution without serializing unrelated tests."
  - "Owned fixture root: keep evidence readable through caller assertions, then clean the exact temporary repository on drop."
requirements-completed: []
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 13.1-2026-07-28T00-14-49
generated_at: 2026-08-01T04:49:10Z
duration: 10 min
completed: 2026-08-01
---

# Phase 13.1 Plan 16: Aggregate Session-Identity Isolation Summary

**Package-level fake-oracle execution now retains validated second-request identity under default Rust test concurrency through a narrow process slot and owned isolated repositories.**

## Performance

- **Duration:** 10 min
- **Started:** 2026-08-01T04:38:45Z
- **Completed:** 2026-08-01T04:49:10Z
- **Tasks:** 1
- **Files modified:** 4

## Accomplishments

- Added a four-case concurrent regression covering `second_malformed` and `second_value_mismatch` under both reuse and sanitizer profiles.
- Bound every accepted second-request bundle to the same validated 64-hex session identity in `identity.json` and `report.json`, with failure classification and stderr retained in diagnostics.
- Isolated fake-oracle process setup and execution behind a test-only mutex while preserving normal package concurrency, CLI arguments, exit codes, protocol bytes, report schema, and the five-second production startup limit.
- Replaced loose temporary `PathBuf` fixtures with owned PID-plus-atomic repository guards that reject nonempty reuse and clean only their exact roots after caller assertions finish.

## TDD Evidence

- **RED:** The focused four-way regression passed in isolation, while the complete `round_trip` target reproduced aggregate contention as a failed concurrent CLI match test. This distinguished package-level pre-handshake child contention from post-handshake session-identity logic.
- **GREEN:** The focused regression, complete `round_trip` target, and complete `liquidfun-differential` package all passed with default Rust test threads in `target/phase13-1-gap-differential`.
- The RED test and GREEN harness were committed together because repository policy requires the full ordered Rust gate to pass before every commit.

## Task Commits

Each task was committed atomically:

1. **Task 1: Reproduce, classify, and eliminate aggregate session-identity contention** - `8ca5731` (fix)

## Files Created/Modified

- `crates/liquidfun-differential/tests/round_trip.rs` - Defines the shared test-only process slot.
- `crates/liquidfun-differential/tests/round_trip/real_oracle.rs` - Owns isolated fake repository roots and scopes child execution to the process slot.
- `crates/liquidfun-differential/tests/round_trip/evidence.rs` - Runs the four second-request identity cases concurrently with classified diagnostics.
- `crates/liquidfun-differential/tests/round_trip/cli_reports.rs` - Keeps direct fake-oracle replay callers within the same process boundary.

## Verification

- `CARGO_TARGET_DIR=target/phase13-1-gap-differential cargo test -p liquidfun-differential --test round_trip cli_reuse_and_sanitizer_bundles_bind_the_second_request_and_session_identity -- --nocapture`
- `CARGO_TARGET_DIR=target/phase13-1-gap-differential cargo test -p liquidfun-differential --test round_trip --all-features`
- `CARGO_TARGET_DIR=target/phase13-1-gap-differential cargo test -p liquidfun-differential --all-features`
- `cargo fmt --all`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo build --all-targets --all-features`
- `cargo test --all-features`
- `bun scripts/bright-builds-check.ts all` (`918 scanned / 0 exceptions / 0 findings`)
- `git diff --check`

## Decisions Made

- Kept the mutex entirely in the integration-test harness so production startup deadlines and supervision semantics remain unchanged.
- Scoped serialization to fake repository creation and child execution; unrelated Rust tests continue using the default test-thread count.
- Returned the owned repository guard with CLI output so evidence remains available until the caller has completed all bundle assertions.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected stale derived STATE position after the formal updater**

- **Found during:** Plan metadata update
- **Issue:** `state advance-plan` advanced the pre-existing stale counter from Plan 1 to Plan 2 even though summaries exist through Plan 16.
- **Fix:** Corrected the derived current position to Plan 17, milestone progress to 96%, and completed-plan velocity to 237 while retaining the tool-recorded metric, decisions, session, and roadmap counts.
- **Files modified:** `.planning/STATE.md`
- **Verification:** The next incomplete roadmap entry is `13.1-17-PLAN.md`, the roadmap reports 16 of 22 Phase 13.1 summaries, and the state frontmatter reports 237 of 246 completed plans.
- **Committed in:** Final plan metadata commit

**Total deviations:** 1 auto-fixed (1 bug).
**Impact on plan:** Metadata now reports the factual next execution position; source scope and behavior are unchanged.

## Issues Encountered

- The focused concurrent regression did not fail alone. The complete target reproduced the original aggregate contention in another simultaneous fake-oracle CLI case, confirming that the root cause was shared package-level process pressure rather than missing identity after a validated handshake.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The aggregate differential package gate is green with default Rust test concurrency.
- Plan 13.1-17 can address the next independent verification gap without weakening session identity or process deadlines.

## Self-Check: PASSED

- All four modified source files and this summary exist.
- Task commit `8ca5731` is present in repository history.
- No blocking stubs or unplanned security-relevant production surface were introduced.

***

*Phase: 13.1-restore-bright-builds-structural-compliance*
*Completed: 2026-08-01*
