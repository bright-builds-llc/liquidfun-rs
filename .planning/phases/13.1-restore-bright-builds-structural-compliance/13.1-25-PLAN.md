---
phase: 13.1-restore-bright-builds-structural-compliance
plan: "25"
type: execute
wave: 5
depends_on: ["13.1-24"]
files_modified:
  - crates/liquidfun-differential/tests/supervisor_failures.rs
  - crates/liquidfun-differential/tests/supervisor_failures/fake_repository.rs
  - .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-FAILED-ATTEMPT-b64ce4c.md
  - .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-PLAN.md
autonomous: true
requirements: []
gap_closure: true
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13.1-2026-07-28T00-14-49
generated_at: 2026-08-20T15:07:48.929Z
must_haves:
  truths:
    - "The supervisor-failure harness creates every fake repository as a unique direct child of the canonical system temporary directory, outside the workspace, and never reuses a pre-existing root."
    - "Each fake repository owns exactly one copied oracle-debug executable plus its behavior sidecar, removes only its exact root through the default Drop cleanup seam, preserves siblings, and suppresses an injected cleanup error during active unwind so the original panic survives without a double panic."
    - "TestSupervisor keeps the fake repository alive while an OracleSupervisor may own a child, then explicitly drops the supervisor before the repository; its Deref and DerefMut surface preserves existing test call sites."
    - "Exactly six deterministic fixture-lifecycle regressions extend supervisor_failures from 10 to 16 tests, including live reuse-child teardown, without changing production timeout/profile behavior or the fake oracle's seven-second startup-timeout behavior."
    - "A fresh producer-equivalent CARGO_TARGET_DIR runs the complete liquidfun-differential package exactly once with CARGO_BUILD_JOBS=1 and passes; deterministic failure is not retried."
    - "Candidate b64ce4c, its successful ordinary publication, commands 1-7, retained prefix, and local failure logs remain supplemental-only history; command 7 failed before any dispatch, so no run or artifact exists for that attempt."
    - "Plan 21 moves to wave 6 behind Plan 25 and requires a new exact authorization for a candidate unequal to ab06bfc, 9cef103, and b64ce4c; none of the three attempts can authorize, seed, resume, or satisfy it."
    - "Focused/full differential, Bright Builds, ordered Rust, all-25-plan schema/structure, dependency-acyclicity, delimiter, stale-only lifecycle, unchanged-file, retained-hash, and diff checks pass before every commit with zero new external effects."
  artifacts:
    - path: "crates/liquidfun-differential/tests/supervisor_failures/fake_repository.rs"
      provides: "Stdlib-only owned FakeRepository and ordered-drop TestSupervisor fixture seam"
    - path: "crates/liquidfun-differential/tests/supervisor_failures.rs"
      provides: "Stable supervisor failure target wired to the owned external fixture and exactly 16 tests"
    - path: ".planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-FAILED-ATTEMPT-b64ce4c.md"
      provides: "Exact supplemental record of the pre-dispatch differential-package failure"
    - path: ".planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-PLAN.md"
      provides: "Wave-6 fresh authorization and three-attempt quarantine contract"
  key_links:
    - from: "crates/liquidfun-differential/tests/supervisor_failures.rs"
      to: "crates/liquidfun-differential/tests/supervisor_failures/fake_repository.rs"
      via: "private path module plus FakeRepository/TestSupervisor helpers"
      pattern: "supervisor_failures/fake_repository.rs"
    - from: "TestSupervisor"
      to: "OracleSupervisor and FakeRepository"
      via: "Option-owned values explicitly taken and dropped in child-before-root order"
      pattern: "maybe_supervisor.*maybe_repository"
    - from: "13.1-21-FAILED-ATTEMPT-b64ce4c.md"
      to: "target/phase13-1-gap-verification/b64ce4c4ba965b71d6a889db0a8a2ea8d39a851c"
      via: "exact path, exit, and SHA-256 citations without byte mutation or prefix reuse"
      pattern: "93aeaf318d9968fa329de2d50a38a155b5e5c0c013f73c758541ef927add874d"
    - from: "13.1-21-PLAN.md"
      to: "13.1-25-SUMMARY.md"
      via: "wave-6 dependency and a new exact checkpoint after local repair/quarantine completion"
      pattern: "13.1-25"
---

<objective>
Eliminate workspace-local fake-oracle lifecycle contamination, preserve the failed b64ce4c attempt as supplemental evidence, and restore a clean pre-effect boundary for Plan 21.

Purpose: The third Plan 21 candidate reached the producer's differential-package command and exposed an unowned workspace-local supervisor fixture whose copied executable could miss the five-second startup boundary under aggregate load. Repair the test harness root cause without weakening production timeouts or consuming another external attempt.
Output: An owned stdlib temporary-repository module, exactly six lifecycle regressions and 16 passing supervisor tests, one fresh no-retry aggregate package proof, an exact b64ce4c failure record, and a wave-6 Plan 21 requiring new authority distinct from all three failed attempts.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-CONTEXT.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-VERIFICATION.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-16-PLAN.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-16-SUMMARY.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-PLAN.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-FAILED-ATTEMPT-ab06bfc.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-FAILED-ATTEMPT-9cef103.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-24-PLAN.md
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-24-SUMMARY.md
@crates/liquidfun-differential/tests/supervisor_failures.rs
@crates/liquidfun-differential/tests/fixtures/fake_oracle.rs
@crates/liquidfun-differential/src/supervisor.rs
@scripts/phase13-1-gap-verification.sh
@scripts/phase13-1-gap-verification-manifest.json
@scripts/phase13-1-validate-gap-evidence.sh

<interfaces>
The existing integration target has exactly ten tests. Its `fake_repository(behavior: &str) -> PathBuf` creates `../../target/supervisor-tests/{pid}-{atomic}` in the workspace, copies `CARGO_BIN_EXE_liquidfun-fake-oracle` to `target/reference/oracle-debug/liquidfun-reference[.exe]`, and writes `behavior.txt` beside it. `supervisor(behavior, profile) -> OracleSupervisor` drops that loose root immediately after construction even though the supervisor may retain a reuse-profile child.

`OracleExecutable::resolve(&Path, OraclePreset::Debug) -> Result<OracleExecutable, _>` requires the exact `target/reference/oracle-debug/liquidfun-reference[.exe]` layout. `OracleSupervisor::new(executable, SessionProfile, REVISION)` owns child lifecycle; its existing `execute`, `process_generation`, and `requests_in_current_process` methods must remain callable through the test wrapper.

The fake oracle reads `behavior.txt` from its executable directory. Its `startup_timeout` branch sleeps for exactly `Duration::from_secs(7)`. The production `phase2-default-v1` startup deadline remains five seconds; this plan changes neither value, neither profile, nor any production/public interface.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Own and deterministically tear down external fake repositories</name>
  <files>crates/liquidfun-differential/tests/supervisor_failures.rs, crates/liquidfun-differential/tests/supervisor_failures/fake_repository.rs</files>
  <read-first>AGENTS.md Rust and pre-commit rules; AGENTS.bright-builds.md; standards-overrides.md; standards/core/architecture.md; standards/core/code-shape.md; standards/core/testing.md; standards/core/verification.md; standards/languages/rust.md; active global and repository lessons; 13.1-CONTEXT.md decisions D-04, D-06, D-07, D-12, D-13, and D-15; Plans/SUMMARYs 16, 21, and 24; crates/liquidfun-differential/tests/supervisor_failures.rs; crates/liquidfun-differential/tests/round_trip/real_oracle.rs as the earlier owned-root pattern; crates/liquidfun-differential/tests/fixtures/fake_oracle.rs; crates/liquidfun-differential/src/supervisor.rs; crates/liquidfun-differential/src/supervisor/executable.rs; crates/liquidfun-test-protocol/src/limits.rs; Cargo manifests and lockfile; retained b64ce4c command 7 stdout/stderr and command record</read-first>
  <behavior>
    - "external_temp_layout_is_canonical_and_exact: a repository root is a direct child of canonical std::env::temp_dir(), is outside the canonical workspace, and contains only the exact oracle-debug executable/behavior layout required by resolution."
    - "unique_repositories_keep_unique_behavior_sidecars: two live repositories have unequal roots and each copied executable reads only the behavior.txt bytes written beside it."
    - "preexisting_unique_root_is_rejected_without_claim_or_cleanup: an atomically chosen direct-child path that already exists is rejected and its sentinel survives."
    - "exact_root_cleanup_preserves_temp_siblings: normal drop removes the owned repository root and leaves a separately created sibling byte-identical."
    - "unwind_cleanup_failure_preserves_original_panic_without_secondary_panic: catch_unwind drives an injected cleanup error through the same private cleanup function used by FakeRepository::drop, proves the original panic payload survives, and reaches post-unwind assertions without aborting."
    - "reuse_child_is_torn_down_before_repository_cleanup: a reuse-profile request starts a live child, then TestSupervisor drop closes/reaps it before the copied executable and exact repository root are removed."
  </behavior>
  <action>Use one atomic RED-to-GREEN TDD cycle. RED first: add exactly the six named tests above with explicit Arrange/Act/Assert sections; list the target and run the new tests to prove at least the external-layout/cleanup/ordered-drop expectations fail against the loose PathBuf fixture. Record the RED command and failure classifications in execution notes, but do not commit any failing state. The tests and complete GREEN harness repair must be staged and committed together as one atomic code commit.

Create the private sibling module with `#[path = "supervisor_failures/fake_repository.rs"] mod fake_repository;` and keep the stable Cargo target filename and all ten existing test names. Implement a stdlib-only `FakeRepository`; add no dependency and do not use `tempfile`. Canonicalize `std::env::temp_dir()` and the workspace root before allocation, fail if the canonical temporary directory is equal to or nested below the workspace, and derive each candidate root as one direct child named with `std::process::id()` plus a process-local `AtomicU64` fetched with `Ordering::Relaxed`. Claim that exact root atomically with `fs::create_dir`; if it already exists, fail without deleting, emptying, adopting, or modifying it. Construct the owning guard immediately after the successful root creation so every later setup panic unwinds through ownership. Under that root create exactly `target/reference/oracle-debug`, copy rather than symlink `CARGO_BIN_EXE_liquidfun-fake-oracle` to `liquidfun-reference` or `liquidfun-reference.exe`, and write the exact behavior string to the sibling `behavior.txt`. Provide `path()` and `Deref<Target = Path>` so existing resolution and path joins remain explicit.

Route every drop cleanup through one private `cleanup_owned_root(root: &amp;Path, cleanup_mode: CleanupMode) -&gt; io::Result&lt;()&gt;` seam stored in the guard. Its production/default mode must call the real `fs::remove_dir_all`; its test-only injected-failure mode must return a deterministic `io::Error` and must be constructible only by private test helpers. `FakeRepository::drop` must call that seam, remove only its exact owned root in default mode, never target the canonical temp parent or any sibling, and treat an already absent root as clean. If the seam returns an error during ordinary execution, panic with the exact root and error; if it returns an error while `std::thread::panicking()` is true, log/report the cleanup error and return without panicking again. The unwind regression must configure a guard with injected cleanup failure, trigger a sentinel panic while that guard is live, recover with `catch_unwind`, and assert the recovered payload is the original sentinel; reaching the assertion proves no secondary panic/abort. Retain the root path outside the closure and manually remove that test-only leftover only after the original-panic assertion. Do not use a subprocess unless the in-process catch_unwind proof cannot exercise the actual Drop seam. Keep normal successful cleanup and sibling preservation exclusively in `exact_root_cleanup_preserves_temp_siblings`. Test-only root reservation/population helpers may be private to this module, but the production fixture constructor must always use the canonical-temp direct-child PID-plus-AtomicU64 path, default real cleanup mode, and the same atomic ownership rule.

Implement `TestSupervisor` with fields named `maybe_supervisor: Option<OracleSupervisor>` and `maybe_repository: Option<FakeRepository>`. Its constructor creates the repository, resolves the copied Debug executable, then constructs the supervisor. Implement `Deref<Target = OracleSupervisor>` and `DerefMut` through the present option values so all existing execute/counter calls retain their shape. In `Drop`, `take()` and drop `maybe_supervisor` first, then `take()` and drop `maybe_repository`; this order is mandatory for reuse children and Windows executable handles. Expose only the narrow repository-path accessor needed by the teardown regression. Change `supervisor` to return `TestSupervisor`; update math-probe and symlink tests to retain a `FakeRepository` guard through the last child/process assertion. Do not serialize the package, add sleeps or retries, raise the five-second production timeout, change a `SessionProfile`, change fake-oracle `Duration::from_secs(7)`, edit production source, touch scripts/manifest/workflow, alter a public API, or link the executable.

After GREEN, require exactly 16 listed tests and run the complete supervisor target. Then create a new system-temp verification parent with `mktemp -d`, select its absent `cargo-target` child as `CARGO_TARGET_DIR`, and run `CARGO_BUILD_JOBS=1 cargo test -p liquidfun-differential --all-features` exactly once. A nonzero result is a hard stop: do not retry the package command and do not proceed to a commit. Before the atomic code commit, run Bright Builds and the mandatory repository Rust sequence in exact order: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`. Prove the b64ce4c candidate's production supervisor tree, `crates/liquidfun-test-protocol/src/limits.rs`, fake oracle, Cargo manifests/lockfile, gap scripts, manifest, and workflow are unchanged. Exact-check the protected production assertion `assert_eq!(limits.startup_timeout(), Duration::from_secs(5));` and the fake oracle's `Duration::from_secs(7)` branch. Review `git diff --check` and the scoped diff before committing.</action>
  <verify>
    <automated>listed="$(cargo test -p liquidfun-differential --test supervisor_failures --all-features -- --list)" &amp;&amp; test "$(printf '%s\n' "$listed" | rg -c ': test$')" = 16 &amp;&amp; for test_name in external_temp_layout_is_canonical_and_exact unique_repositories_keep_unique_behavior_sidecars preexisting_unique_root_is_rejected_without_claim_or_cleanup exact_root_cleanup_preserves_temp_siblings unwind_cleanup_failure_preserves_original_panic_without_secondary_panic reuse_child_is_torn_down_before_repository_cleanup; do printf '%s\n' "$listed" | rg -q "(^|::)$test_name: test$"; done &amp;&amp; cargo test -p liquidfun-differential --test supervisor_failures --all-features &amp;&amp; fresh_parent="$(mktemp -d "${TMPDIR:-/tmp}/liquidfun-plan25-differential.XXXXXX")" &amp;&amp; fresh_target="$fresh_parent/cargo-target" &amp;&amp; test ! -e "$fresh_target" &amp;&amp; trap 'rm -rf -- "$fresh_parent"' EXIT &amp;&amp; CARGO_BUILD_JOBS=1 CARGO_TARGET_DIR="$fresh_target" cargo test -p liquidfun-differential --all-features &amp;&amp; git diff --exit-code b64ce4c4ba965b71d6a889db0a8a2ea8d39a851c -- crates/liquidfun-differential/src crates/liquidfun-test-protocol/src/limits.rs crates/liquidfun-differential/tests/fixtures/fake_oracle.rs crates/liquidfun-differential/Cargo.toml Cargo.toml Cargo.lock scripts/phase13-1-gap-verification.sh scripts/phase13-1-gap-verification-manifest.json scripts/phase13-1-validate-gap-evidence.sh .github/workflows/phase13-1-canonical-native.yml &amp;&amp; rg -Fqx '        assert_eq!(limits.startup_timeout(), Duration::from_secs(5));' crates/liquidfun-test-protocol/src/limits.rs &amp;&amp; rg -q 'Duration::from_secs\(7\)' crates/liquidfun-differential/tests/fixtures/fake_oracle.rs &amp;&amp; bun scripts/bright-builds-check.ts all &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance-criteria>Exactly six new deterministic tests produce 16 total supervisor_failures tests; the stable ten existing tests still pass; FakeRepository is stdlib-only, canonical-temp-external, direct-child, PID-plus-AtomicU64, atomically owned, exact-layout, copy-based, sidecar-driven, exact-root-cleaned, sibling-preserving, and non-double-panicking; its actual Drop cleanup seam is exercised with an injected io::Error during active unwind and preserves the original panic without a subprocess; TestSupervisor uses both required Option fields, Deref/DerefMut, and explicit supervisor-before-repository drop; one fresh producer-equivalent package run passes without retry; production source, the exact five-second startup assertion in limits.rs, fake seven-second behavior, dependencies, manifests, scripts, workflow, public APIs, and Phase 15 scope remain unchanged; the code and tests share one GREEN commit and no RED commit exists.</acceptance-criteria>
  <done>The aggregate differential package no longer depends on workspace-local fake executable residue or wrong drop order, and the repair is proven at the exact producer-equivalent local boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Quarantine b64ce4c and require a fourth exact candidate authorization</name>
  <files>.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-FAILED-ATTEMPT-b64ce4c.md, .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-PLAN.md</files>
  <read-first>Task 1 commit and complete GREEN logs; AGENTS.md; AGENTS.bright-builds.md; standards/core/verification.md; .planning/ROADMAP.md; .planning/STATE.md; 13.1-CONTEXT.md; 13.1-VERIFICATION.md; Plans/SUMMARYs 16, 21, and 24 plus Plan 25; both existing failed-attempt documents; target/phase13-1-gap-verification/b64ce4c4ba965b71d6a889db0a8a2ea8d39a851c/command-records.jsonl and logs/001 through logs/007 only; local Git objects/reflogs for candidate/tree/publication corroboration; scripts/phase13-1-gap-verification-manifest.json. Do not use gh, fetch, push, dispatch, run inspection, or artifact inspection.</read-first>
  <action>First recompute every retained b64ce4c digest below and stop on any mismatch; never edit, normalize, copy, link, import, or move a retained byte to make a digest match. Create `13.1-21-FAILED-ATTEMPT-b64ce4c.md` as parser-owned Markdown with no YAML frontmatter and zero standalone `---` lines. Include these exact facts and sentences: `Retained root: target/phase13-1-gap-verification/b64ce4c4ba965b71d6a889db0a8a2ea8d39a851c`; candidate `b64ce4c4ba965b71d6a889db0a8a2ea8d39a851c`; tree `34c08d5d87a9f6b8f5b671b6e3e875e2187dda35`; `The authorized ordinary publication succeeded.`; `The exact output directory was absent before the sole producer invocation.`; `Commands 1-6 passed exactly once with exit 0: rust-fmt, rust-clippy, rust-build, rust-test, managed-checker, protocol-package.`; `Command 7 differential-package failed exactly once with exit 101.`; `Commands 8-72 did not run.` Record that exact test `supervisor_failures::one_shot_and_reuse_enforce_reset_epochs_and_periodic_cycling` failed on its first one-shot request with `StartupTimeout` after exactly `5.003871208s`, `maybe_exit_status: Some(-9)`, `maybe_request_sha256` `735be9a75bccdd56400eb6a630a99e6b9c3c7461c7030162a3291b36a3af84c5`, `maybe_scenario_sha256` `49642b2ea489384be7850f595269e6366003f7bfab260ab1f9270a9cfcb0fd9e`, `limit_profile_sha256` `2a98531ff616320ee670b0e99b1f7bfdbbb751354ee7be886d81f4321fb88b3e`, no session identity, empty retained stderr, and `child_killed: true`/`child_reaped: true`. State exactly `The first one-shot request failed; the other nine supervisor_failures tests passed.` and `No producer retry occurred.` Because execution stopped at command 7 before command 55, state exactly `The attempt consumed zero dispatches and produced no dispatch journal, run ID, artifact ID, dispatch intent/result, canonical directory, terminal JSON, pending terminal JSON, canonical identity/artifact, or formal re-verification.` Add exact label-to-digest lines `command-records.jsonl SHA-256: 93aeaf318d9968fa329de2d50a38a155b5e5c0c013f73c758541ef927add874d`, `logs/007-differential-package.stdout.log SHA-256: 7f2c4469122a604cf4ba27b8792e1a80b58fa8301ed52725f296585e020907b6`, and `logs/007-differential-package.stderr.log SHA-256: f28081d73d2f9849ba23956dc65ed89215159b5722c81e9adade26bdde618909`. State exactly `This attempt is supplemental-only: it cannot authorize, seed, resume, be copied, linked, imported, or reused, provide terminal authority, or satisfy a later summary or verifier.`

Cite the ignored retained root `target/phase13-1-gap-verification/b64ce4c4ba965b71d6a889db0a8a2ea8d39a851c` and all retained file digests: `command-records.jsonl` `93aeaf318d9968fa329de2d50a38a155b5e5c0c013f73c758541ef927add874d`; command-1 stdout/stderr both `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`; command-2 stdout `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`, stderr `2d44327db13401cea19dd4f7e31dae42302eccf13f68e04270bca8115e862ade`; command-3 stdout `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`, stderr `fc75768d420684f19cf118efe081f3203dcf1944358dc7a0c2af5a5e39b87dff`; command-4 stdout `289d747219f8df6d3bad2f8258dc147e3bc3e28d72fa29baff6983dacc6ad025`, stderr `aa8cd901279d02ee4e4c473682e86c26b9fca6bf0f3c54a13340b58262e6722e`; command-5 stdout `22309d18f8aca7daa213df825b857d36696067b96af57604178112a0b5f40c8c`, stderr `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`; command-6 stdout `294b57e358e6cce87b1d78c2edd8ca63d83bfe2ae7541d7e836fbbac79fb00d2`, stderr `c02cce74ec61d22a30a9177e8943bdba347a55dbb536ea3367ef39361f02bb94`; command-7 stdout `7f2c4469122a604cf4ba27b8792e1a80b58fa8301ed52725f296585e020907b6`, stderr `f28081d73d2f9849ba23956dc65ed89215159b5722c81e9adade26bdde618909`; authoritative manifest `40e74df2b2652ffe69b48f2272d6a27f6fa91c5bfb4ab1e969787955e280d7f7`. Mark the candidate, successful publication, authorization, prefix, records, and logs supplemental-only. They cannot authorize a later effect, equal a fresh candidate, seed/resume a command prefix, be inspected for new proof after this local accounting, supply records/evidence, provide terminal authority, or satisfy a future summary/verifier.

The record must use complete exact label-to-digest rows, including a separate named row every time the empty-file digest repeats: `command-records.jsonl SHA-256: 93aeaf318d9968fa329de2d50a38a155b5e5c0c013f73c758541ef927add874d`; `logs/001-rust-fmt.stdout.log SHA-256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`; `logs/001-rust-fmt.stderr.log SHA-256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`; `logs/002-rust-clippy.stdout.log SHA-256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`; `logs/002-rust-clippy.stderr.log SHA-256: 2d44327db13401cea19dd4f7e31dae42302eccf13f68e04270bca8115e862ade`; `logs/003-rust-build.stdout.log SHA-256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`; `logs/003-rust-build.stderr.log SHA-256: fc75768d420684f19cf118efe081f3203dcf1944358dc7a0c2af5a5e39b87dff`; `logs/004-rust-test.stdout.log SHA-256: 289d747219f8df6d3bad2f8258dc147e3bc3e28d72fa29baff6983dacc6ad025`; `logs/004-rust-test.stderr.log SHA-256: aa8cd901279d02ee4e4c473682e86c26b9fca6bf0f3c54a13340b58262e6722e`; `logs/005-managed-checker.stdout.log SHA-256: 22309d18f8aca7daa213df825b857d36696067b96af57604178112a0b5f40c8c`; `logs/005-managed-checker.stderr.log SHA-256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`; `logs/006-protocol-package.stdout.log SHA-256: 294b57e358e6cce87b1d78c2edd8ca63d83bfe2ae7541d7e836fbbac79fb00d2`; `logs/006-protocol-package.stderr.log SHA-256: c02cce74ec61d22a30a9177e8943bdba347a55dbb536ea3367ef39361f02bb94`; `logs/007-differential-package.stdout.log SHA-256: 7f2c4469122a604cf4ba27b8792e1a80b58fa8301ed52725f296585e020907b6`; `logs/007-differential-package.stderr.log SHA-256: f28081d73d2f9849ba23956dc65ed89215159b5722c81e9adade26bdde618909`; `scripts/phase13-1-gap-verification-manifest.json SHA-256: 40e74df2b2652ffe69b48f2272d6a27f6fa91c5bfb4ab1e969787955e280d7f7`. Do not substitute an unordered digest list for these named bindings.

Revise `13.1-21-PLAN.md` everywhere, not only its frontmatter: set wave 6 and add `13.1-25` to `depends_on`; add the b64ce4c failure document and Plan 25 summary to context/artifacts/read-first inputs; describe three separate failed attempts in truths, key links, checkpoint action/criteria, producer action/criteria, summary action/criteria, threat boundaries, STRIDE mitigations, verification, success criteria, and output. The b64ce4c entry must preserve that publication succeeded but dispatch/run/artifact counts are zero. Require the next candidate SHA to differ from all three exact candidates and invalidate any earlier b64ce4c authorization. Task 1 of Plan 21 must present a new exact repository/origin/base/tree/commit/file checkpoint after Plan 25 is fully committed, request the same exact `authorize-publish-and-dispatch &lt;candidate_sha&gt; refs/heads/&lt;default_branch&gt; repo=&lt;repository_slug&gt; accepted_by=&lt;identity&gt;` response, and cover exactly one ordinary non-force publication plus exactly one dispatch for only that new fourth candidate. No authority carries forward from any of the three failed attempts.

Before the Task 2 documentation commit, repeat the focused 16-test target, run the complete liquidfun-differential package once in another fresh system-temp CARGO_TARGET_DIR with CARGO_BUILD_JOBS=1 and no retry, then run Bright Builds and the mandatory Rust sequence in exact order. Recheck all retained hashes/exits and all Task 1 unchanged-file proofs, including no diff to `crates/liquidfun-test-protocol/src/limits.rs`, the exact `assert_eq!(limits.startup_timeout(), Duration::from_secs(5));` line, and fake-oracle `Duration::from_secs(7)`. Validate all 25 plan schemas and structures. Require exactly two standalone `---` delimiters in every plan and zero standalone `---` lines in the b64 failure record. Run a dependency-graph DFS over all Phase 13.1 plan frontmatters and require every dependency exists, no plan depends on itself, and no cycle exists; also require Plan 25 wave 5 depends on 24 and Plan 21 wave 6 depends on 25. Run lifecycle preflight with ID `13.1-2026-07-28T00-14-49`, mode `yolo`, 25 valid plans, and only `verification is stale relative to context, plan, or summary artifacts`. Review `git diff --check`, the exact scoped diff, and `git status`; do not invoke the producer, gh, fetch, push, dispatch, run/artifact inspection, retry, cancellation, or Phase 15. Commit only after every gate is green.</action>
  <verify>
    <automated>candidate=b64ce4c4ba965b71d6a889db0a8a2ea8d39a851c &amp;&amp; retained="target/phase13-1-gap-verification/$candidate" &amp;&amp; failure_record=.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-FAILED-ATTEMPT-b64ce4c.md &amp;&amp; test "$(git rev-parse "$candidate^{tree}")" = 34c08d5d87a9f6b8f5b671b6e3e875e2187dda35 &amp;&amp; test "$(shasum -a 256 "$retained/command-records.jsonl" | awk '{print $1}')" = 93aeaf318d9968fa329de2d50a38a155b5e5c0c013f73c758541ef927add874d &amp;&amp; jq -s -e 'length == 7 and ([.[].id] == ["rust-fmt","rust-clippy","rust-build","rust-test","managed-checker","protocol-package","differential-package"]) and ([.[].exit_code] == [0,0,0,0,0,0,101])' "$retained/command-records.jsonl" &amp;&amp; for spec in "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855:$retained/logs/001-rust-fmt.stdout.log" "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855:$retained/logs/001-rust-fmt.stderr.log" "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855:$retained/logs/002-rust-clippy.stdout.log" "2d44327db13401cea19dd4f7e31dae42302eccf13f68e04270bca8115e862ade:$retained/logs/002-rust-clippy.stderr.log" "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855:$retained/logs/003-rust-build.stdout.log" "fc75768d420684f19cf118efe081f3203dcf1944358dc7a0c2af5a5e39b87dff:$retained/logs/003-rust-build.stderr.log" "289d747219f8df6d3bad2f8258dc147e3bc3e28d72fa29baff6983dacc6ad025:$retained/logs/004-rust-test.stdout.log" "aa8cd901279d02ee4e4c473682e86c26b9fca6bf0f3c54a13340b58262e6722e:$retained/logs/004-rust-test.stderr.log" "22309d18f8aca7daa213df825b857d36696067b96af57604178112a0b5f40c8c:$retained/logs/005-managed-checker.stdout.log" "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855:$retained/logs/005-managed-checker.stderr.log" "294b57e358e6cce87b1d78c2edd8ca63d83bfe2ae7541d7e836fbbac79fb00d2:$retained/logs/006-protocol-package.stdout.log" "c02cce74ec61d22a30a9177e8943bdba347a55dbb536ea3367ef39361f02bb94:$retained/logs/006-protocol-package.stderr.log" "7f2c4469122a604cf4ba27b8792e1a80b58fa8301ed52725f296585e020907b6:$retained/logs/007-differential-package.stdout.log" "f28081d73d2f9849ba23956dc65ed89215159b5722c81e9adade26bdde618909:$retained/logs/007-differential-package.stderr.log"; do expected="${spec%%:*}"; retained_file="${spec#*:}"; test "$(shasum -a 256 "$retained_file" | awk '{print $1}')" = "$expected"; done &amp;&amp; test "$(shasum -a 256 scripts/phase13-1-gap-verification-manifest.json | awk '{print $1}')" = 40e74df2b2652ffe69b48f2272d6a27f6fa91c5bfb4ab1e969787955e280d7f7 &amp;&amp; test ! -e "$retained/dispatch-intent.json" &amp;&amp; test ! -e "$retained/dispatch-result.json" &amp;&amp; test ! -e "$retained/final-verification.json" &amp;&amp; test ! -e "$retained/final-verification.json.pending" &amp;&amp; test ! -e "$retained/canonical" &amp;&amp; test -s "$failure_record" &amp;&amp; ! rg -q '^[-]{3}$' "$failure_record" &amp;&amp; rg -Fq 'Retained root: target/phase13-1-gap-verification/b64ce4c4ba965b71d6a889db0a8a2ea8d39a851c' "$failure_record" &amp;&amp; rg -Fq 'b64ce4c4ba965b71d6a889db0a8a2ea8d39a851c' "$failure_record" &amp;&amp; rg -Fq '34c08d5d87a9f6b8f5b671b6e3e875e2187dda35' "$failure_record" &amp;&amp; rg -Fq 'The authorized ordinary publication succeeded.' "$failure_record" &amp;&amp; rg -Fq 'The exact output directory was absent before the sole producer invocation.' "$failure_record" &amp;&amp; rg -Fq 'Commands 1-6 passed exactly once with exit 0: rust-fmt, rust-clippy, rust-build, rust-test, managed-checker, protocol-package.' "$failure_record" &amp;&amp; rg -Fq 'Command 7 differential-package failed exactly once with exit 101.' "$failure_record" &amp;&amp; rg -Fq 'Commands 8-72 did not run.' "$failure_record" &amp;&amp; rg -Fq 'supervisor_failures::one_shot_and_reuse_enforce_reset_epochs_and_periodic_cycling' "$failure_record" &amp;&amp; rg -Fq 'StartupTimeout' "$failure_record" &amp;&amp; rg -Fq '5.003871208s' "$failure_record" &amp;&amp; rg -Fq 'maybe_exit_status: Some(-9)' "$failure_record" &amp;&amp; rg -Fq 'no session identity' "$failure_record" &amp;&amp; rg -Fq 'empty retained stderr' "$failure_record" &amp;&amp; rg -Fq 'child_killed: true' "$failure_record" &amp;&amp; rg -Fq 'child_reaped: true' "$failure_record" &amp;&amp; rg -Fq 'The first one-shot request failed; the other nine supervisor_failures tests passed.' "$failure_record" &amp;&amp; rg -Fq 'No producer retry occurred.' "$failure_record" &amp;&amp; rg -Fq 'The attempt consumed zero dispatches and produced no dispatch journal, run ID, artifact ID, dispatch intent/result, canonical directory, terminal JSON, pending terminal JSON, canonical identity/artifact, or formal re-verification.' "$failure_record" &amp;&amp; rg -Fq 'command-records.jsonl SHA-256: 93aeaf318d9968fa329de2d50a38a155b5e5c0c013f73c758541ef927add874d' "$failure_record" &amp;&amp; rg -Fq 'logs/007-differential-package.stdout.log SHA-256: 7f2c4469122a604cf4ba27b8792e1a80b58fa8301ed52725f296585e020907b6' "$failure_record" &amp;&amp; rg -Fq 'logs/007-differential-package.stderr.log SHA-256: f28081d73d2f9849ba23956dc65ed89215159b5722c81e9adade26bdde618909' "$failure_record" &amp;&amp; rg -Fq 'This attempt is supplemental-only: it cannot authorize, seed, resume, be copied, linked, imported, or reused, provide terminal authority, or satisfy a later summary or verifier.' "$failure_record" &amp;&amp; rg -Fq 'command-records.jsonl SHA-256: 93aeaf318d9968fa329de2d50a38a155b5e5c0c013f73c758541ef927add874d' "$failure_record" &amp;&amp; rg -Fq 'logs/001-rust-fmt.stdout.log SHA-256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855' "$failure_record" &amp;&amp; rg -Fq 'logs/001-rust-fmt.stderr.log SHA-256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855' "$failure_record" &amp;&amp; rg -Fq 'logs/002-rust-clippy.stdout.log SHA-256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855' "$failure_record" &amp;&amp; rg -Fq 'logs/002-rust-clippy.stderr.log SHA-256: 2d44327db13401cea19dd4f7e31dae42302eccf13f68e04270bca8115e862ade' "$failure_record" &amp;&amp; rg -Fq 'logs/003-rust-build.stdout.log SHA-256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855' "$failure_record" &amp;&amp; rg -Fq 'logs/003-rust-build.stderr.log SHA-256: fc75768d420684f19cf118efe081f3203dcf1944358dc7a0c2af5a5e39b87dff' "$failure_record" &amp;&amp; rg -Fq 'logs/004-rust-test.stdout.log SHA-256: 289d747219f8df6d3bad2f8258dc147e3bc3e28d72fa29baff6983dacc6ad025' "$failure_record" &amp;&amp; rg -Fq 'logs/004-rust-test.stderr.log SHA-256: aa8cd901279d02ee4e4c473682e86c26b9fca6bf0f3c54a13340b58262e6722e' "$failure_record" &amp;&amp; rg -Fq 'logs/005-managed-checker.stdout.log SHA-256: 22309d18f8aca7daa213df825b857d36696067b96af57604178112a0b5f40c8c' "$failure_record" &amp;&amp; rg -Fq 'logs/005-managed-checker.stderr.log SHA-256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855' "$failure_record" &amp;&amp; rg -Fq 'logs/006-protocol-package.stdout.log SHA-256: 294b57e358e6cce87b1d78c2edd8ca63d83bfe2ae7541d7e836fbbac79fb00d2' "$failure_record" &amp;&amp; rg -Fq 'logs/006-protocol-package.stderr.log SHA-256: c02cce74ec61d22a30a9177e8943bdba347a55dbb536ea3367ef39361f02bb94' "$failure_record" &amp;&amp; rg -Fq 'logs/007-differential-package.stdout.log SHA-256: 7f2c4469122a604cf4ba27b8792e1a80b58fa8301ed52725f296585e020907b6' "$failure_record" &amp;&amp; rg -Fq 'logs/007-differential-package.stderr.log SHA-256: f28081d73d2f9849ba23956dc65ed89215159b5722c81e9adade26bdde618909' "$failure_record" &amp;&amp; rg -Fq 'scripts/phase13-1-gap-verification-manifest.json SHA-256: 40e74df2b2652ffe69b48f2272d6a27f6fa91c5bfb4ab1e969787955e280d7f7' "$failure_record" &amp;&amp; rg -q '^wave: 6$' .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-PLAN.md &amp;&amp; rg -q '13.1-25' .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-PLAN.md &amp;&amp; rg -q 'b64ce4c4ba965b71d6a889db0a8a2ea8d39a851c' .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-21-PLAN.md &amp;&amp; listed="$(cargo test -p liquidfun-differential --test supervisor_failures --all-features -- --list)" &amp;&amp; test "$(printf '%s\n' "$listed" | rg -c ': test$')" = 16 &amp;&amp; cargo test -p liquidfun-differential --test supervisor_failures --all-features &amp;&amp; fresh_parent="$(mktemp -d "${TMPDIR:-/tmp}/liquidfun-plan25-docs.XXXXXX")" &amp;&amp; fresh_target="$fresh_parent/cargo-target" &amp;&amp; test ! -e "$fresh_target" &amp;&amp; trap 'rm -rf -- "$fresh_parent"' EXIT &amp;&amp; CARGO_BUILD_JOBS=1 CARGO_TARGET_DIR="$fresh_target" cargo test -p liquidfun-differential --all-features &amp;&amp; git diff --exit-code b64ce4c4ba965b71d6a889db0a8a2ea8d39a851c -- crates/liquidfun-differential/src crates/liquidfun-test-protocol/src/limits.rs crates/liquidfun-differential/tests/fixtures/fake_oracle.rs crates/liquidfun-differential/Cargo.toml Cargo.toml Cargo.lock scripts/phase13-1-gap-verification.sh scripts/phase13-1-gap-verification-manifest.json scripts/phase13-1-validate-gap-evidence.sh .github/workflows/phase13-1-canonical-native.yml &amp;&amp; rg -Fqx '        assert_eq!(limits.startup_timeout(), Duration::from_secs(5));' crates/liquidfun-test-protocol/src/limits.rs &amp;&amp; rg -q 'Duration::from_secs\(7\)' crates/liquidfun-differential/tests/fixtures/fake_oracle.rs &amp;&amp; bun scripts/bright-builds-check.ts all &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; for plan in .planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-*-PLAN.md; do test "$(rg -c '^[-]{3}$' "$plan")" = 2 &amp;&amp; node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs frontmatter validate "$plan" --schema plan | jq -e '.valid == true' &gt;/dev/null &amp;&amp; node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure "$plan" | jq -e '.valid == true' &gt;/dev/null; done &amp;&amp; test "$(find .planning/phases/13.1-restore-bright-builds-structural-compliance -maxdepth 1 -name '13.1-*-PLAN.md' | wc -l | tr -d ' ')" = 25 &amp;&amp; node -e 'const fs=require("fs"),d=".planning/phases/13.1-restore-bright-builds-structural-compliance",m=new Map;for(const f of fs.readdirSync(d).filter(x=&gt;/^13\.1-\d\d-PLAN\.md$/.test(x))){const y=fs.readFileSync(`${d}/${f}`,"utf8").split(/^(?:-{3})\s*$/m)[1],p=y.match(/^plan:\s*"([^"]+)"\s*$/m)[1],w=+y.match(/^wave:\s*(\d+)$/m)[1],a=JSON.parse(y.match(/^depends_on:\s*(\[[^\n]*\])/m)[1]);m.set(`13.1-${p}`,{w,a})}const s=new Set,v=new Set;function q(id){if(s.has(id))throw Error("cycle "+id);if(v.has(id))return;s.add(id);for(const x of m.get(id).a){if(!m.has(x)||x===id)throw Error("invalid dependency "+id+" -&gt; "+x);q(x)}s.delete(id);v.add(id)}for(const id of m.keys())q(id);if(m.size!==25||m.get("13.1-25").w!==5||JSON.stringify(m.get("13.1-25").a)!==JSON.stringify(["13.1-24"])||m.get("13.1-21").w!==6||!m.get("13.1-21").a.includes("13.1-25"))throw Error("wave/dependency contract")' &amp;&amp; lifecycle="$(node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify lifecycle 13.1 --expect-id 13.1-2026-07-28T00-14-49 --expect-mode yolo --require-plans --require-verification)" &amp;&amp; jq -e '.plans.valid == true and .plans.total == 25 and .lifecycle_id == "13.1-2026-07-28T00-14-49" and .lifecycle_mode == "yolo" and .reasons == ["verification is stale relative to context, plan, or summary artifacts"]' &lt;&lt;&lt;"$lifecycle" &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance-criteria>The b64ce4c document has zero standalone delimiters and exact-checks the full retained-root path, absent output directory before the sole invocation, candidate, tree, successful publication, commands 1-6 names/once/exit 0, command 7 name/once/exit 101, the exact failing test, first one-shot failure, other nine passing tests, StartupTimeout, 5.003871208s, exit -9, killed/reaped, absent commands 8-72, no retry, zero dispatch/run/artifact/journal/canonical-directory/terminal facts, the exact supplemental-only denial sentence, the three request/scenario/profile hashes, exact label-to-digest bindings for command-records and command-7 stdout/stderr, and every retained file/manifest hash without mutating retained bytes; Plan 21 is comprehensively three-attempt-aware, wave 6, dependent on Plan 25, and requests wholly new exact authority for a candidate unequal to all three; focused and one fresh full package run pass before the docs commit; all 25 plans are valid, acyclic, and have exactly two delimiters; lifecycle is stale-only; production, limits.rs with exact five-second assertion, fake seven-second behavior, scripts, and dependencies remain unchanged; no external effect or Phase 15 action occurs.</acceptance-criteria>
  <done>The third failed candidate is permanently supplemental, the locally repaired fourth candidate can be formed only after Plan 25 commits, and Plan 21 cannot publish or dispatch without a new exact human authorization.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Workspace and Cargo target → fake oracle fixture | Repository-local executable residue and concurrent compilation must not determine whether a copied child starts inside the production deadline. |
| Canonical system temp → owned fake repository | The harness may claim and delete only one unique direct child and must reject any pre-existing path. |
| OracleSupervisor child → repository cleanup | A live reuse child must be closed/reaped before its executable and containing root are removed. |
| Panic unwind → cleanup failure | Cleanup must preserve the original panic rather than aborting through a second panic. |
| Retained b64ce4c prefix → tracked failure record | Only exact local hashes/exits may cross; retained bytes remain immutable and supplemental. |
| Three failed attempts → Plan 21 checkpoint | No old authorization, candidate, prefix, records, run, or artifact may authorize or satisfy the fourth candidate. |
| Plan 25 local work → GitHub state | This plan has zero publication, dispatch, run, artifact, fetch, push, or remote-inspection authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13.1-71 | T | Fake repository path | mitigate | Canonicalize temp/workspace, require external direct-child PID-plus-AtomicU64 roots, and atomically reject pre-existence. |
| T-13.1-72 | E | Cleanup ownership | mitigate | Construct ownership immediately after create_dir and remove only the exact root; preserve siblings in tests. |
| T-13.1-73 | D | Reuse child teardown | mitigate | Option-own both values and explicitly drop OracleSupervisor before FakeRepository; prove cleanup after a live reuse request. |
| T-13.1-74 | D | Panic cleanup | mitigate | Route Drop through a stored test-only cleanup mode, inject io::Error during active unwind, and use catch_unwind to prove the original sentinel panic survives without a secondary panic. |
| T-13.1-75 | S | b64ce4c evidence authority | mitigate | Record exact hashes/exits and zero-dispatch facts, then forbid authorization, reuse, terminal authority, and summary satisfaction. |
| T-13.1-76 | R | Deterministic package failure | mitigate | Run the producer-equivalent differential package once in a fresh target and stop without retry on failure. |
| T-13.1-77 | E | Fourth publication authority | mitigate | Move Plan 21 to wave 6 and require a new exact checkpoint for a SHA unequal to all three prior candidates. |
| T-13.1-78 | T | Production semantics | mitigate | Prove no diff to production supervisor, fake seven-second behavior, dependencies, scripts, manifest, workflow, or public APIs. |
</threat-model>

<verification>Exactly 16 supervisor tests and one no-retry producer-equivalent fresh-target differential package run pass; every commit is preceded by focused/full differential, Bright Builds, and ordered Rust gates; b64ce4c retained hashes/exits and zero-dispatch facts revalidate; production, fake timing, dependencies, scripts, manifest, workflow, and public APIs are unchanged; all 25 plans validate with an acyclic 24 → 25 → 21 chain, exactly two delimiters each, and stale-only lifecycle; diff review and final clean-tree proof complete with zero external effects.</verification>

<success-criteria>The supervisor fixture owns deterministic external temporary repositories and tears down reuse children before exact-root cleanup; the b64ce4c producer failure is immutable supplemental history; Plan 21 can proceed only in wave 6 after a new candidate, distinct from all three failed candidates, receives a new exact human authorization; Phase 15 remains deferred.</success-criteria>

<output>After completion, create `.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-25-SUMMARY.md`. Include the single atomic RED-to-GREEN code/test commit with no RED commit, all 16 test names/count, the actual Drop-seam injected-cleanup-error unwind proof, both one-shot fresh-target package proofs and no-retry statements, all Bright/ordered-Rust gates run before every commit, unchanged-file proofs covering `crates/liquidfun-test-protocol/src/limits.rs` plus its exact five-second assertion and the fake oracle's seven-second branch, the complete exact-checked b64ce4c retained-root/pre-invocation/target-result/authority facts, three failure hashes, label-to-digest bindings, retained hashes, canonical-directory absence, and exit quarantine with zero standalone delimiters, the comprehensive Plan 21 three-attempt/new-authorization revision, all-25 schema/structure/acyclic/delimiter checks, stale-only lifecycle output, scoped diff review, explicit zero external effects, Phase 15 deferral, and final clean `git status`. Before any completion or metadata commit containing the summary, rerun the focused/full differential, Bright Builds, mandatory ordered Rust, retained-hash, protected unchanged-file including limits.rs/five seconds/fake seven seconds, all-plan, dependency, delimiter, lifecycle, and diff checks; leave the worktree clean.</output>
