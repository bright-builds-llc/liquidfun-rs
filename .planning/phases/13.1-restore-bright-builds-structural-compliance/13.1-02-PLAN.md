---
phase: 13.1-restore-bright-builds-structural-compliance
plan: "02"
type: execute
wave: 2
depends_on: ["13.1-01"]
files_modified:
  - crates/liquidfun-test-protocol/src/catalog/mapping.rs
  - crates/liquidfun-test-protocol/src/catalog/model.rs
  - crates/liquidfun-test-protocol/src/catalog/resolve.rs
  - crates/liquidfun-test-protocol/src/performance/wire.rs
  - crates/liquidfun-test-protocol/src/provenance.rs
  - crates/liquidfun-test-protocol/src/scenario/math_probe.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs
  - crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13.1-2026-07-28T00-14-49
generated_at: 2026-07-28T00:38:59Z
must_haves:
  truths:
    - "Eight protocol roots are at most 628 lines with exact public/wire behavior."
    - "New code exists only in the listed roots or their exact sibling module directories."
  artifacts:
    - path: "crates/liquidfun-test-protocol/src/catalog/mapping.rs"
      provides: "Stable catalog mapping facade"
    - path: "crates/liquidfun-test-protocol/src/provenance.rs"
      provides: "Stable provenance contract ring"
  key_links:
    - from: "stable protocol facades"
      to: "private sibling modules"
      via: "unchanged exports and validated domain values"
      pattern: "mod |pub use"
---

<objective>
Refactor the first protocol batch before downstream consumers.

Purpose: Apply D-04, D-05, D-07, D-09, and D-13.
Output: Eight compliant protocol roots and cumulative external patch.
</objective>

<execution-context>
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-EXECUTION.md
</execution-context>
<aggregate-execution>Execute only as bounded sequential segment 13.1-02 through execute-phase Pattern-B task-only agent semantics under `13.1-EXECUTION.md`. The standard per-plan execute-plan route is unsupported because its commits/summaries conflict with higher repository pre-commit rules. Do not stage, commit, write a SUMMARY, or modify lifecycle/planning state; keep progress/evidence and the complete tracked-plus-untracked candidate snapshot external. Plans 01-14 and Plan 15 Tasks 1-2 form one aggregate candidate; only the orchestrator may perform Plan 15 Task 3 after the full unchanged candidate is green.</aggregate-execution>

<context>
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split catalog, performance, and provenance rings</name>
  <files>crates/liquidfun-test-protocol/src/catalog/mapping.rs, crates/liquidfun-test-protocol/src/catalog/model.rs, crates/liquidfun-test-protocol/src/catalog/resolve.rs, crates/liquidfun-test-protocol/src/performance/wire.rs, crates/liquidfun-test-protocol/src/provenance.rs</files>
  <action>Read each full root, parent module, and focused tests first. Work only in the isolated worktree. Extract cohesive helpers only under `crates/liquidfun-test-protocol/src/catalog/mapping/*.rs`, `catalog/model/*.rs`, `catalog/resolve/*.rs`, `performance/wire/*.rs`, and `provenance/*.rs`. Keep roots as stable facades/contract rings. Preserve serde names/order, exact bytes, hashes, public paths, error precedence, and deterministic ordering. No exceptions, fixture rewrites, staging, or commits.</action>
  <verify>
    <automated>cd /tmp/liquidfun-rs-phase13.1/worktree &amp;&amp; cargo test -p liquidfun-test-protocol --all-features --lib catalog &amp;&amp; cargo test -p liquidfun-test-protocol --all-features --lib provenance &amp;&amp; for f in crates/liquidfun-test-protocol/src/catalog/mapping.rs crates/liquidfun-test-protocol/src/catalog/model.rs crates/liquidfun-test-protocol/src/catalog/resolve.rs crates/liquidfun-test-protocol/src/performance/wire.rs crates/liquidfun-test-protocol/src/provenance.rs; do test "$(wc -l &lt; "$f")" -le 628; done</automated>
  </verify>
  <done>Five roots and all newly created sibling modules are compliant and focused tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Split math and rigid scenario roots and snapshot</name>
  <files>crates/liquidfun-test-protocol/src/scenario/math_probe.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs, crates/liquidfun-test-protocol/src/scenario/rigid_world/result.rs</files>
  <action>Read complete roots and their tests first. Extract only below `scenario/math_probe/*.rs`, `scenario/rigid_world/phase9/*.rs`, and `scenario/rigid_world/result/*.rs`. Preserve scenario IDs, float-bit transport, schema, field order, validation, and public paths. Verify every changed/new source is at most 628 lines. Refresh the segment 13.1-02 cumulative candidate snapshot required by the aggregate contract; do not stage or commit. After this segment's tests pass, refresh `/tmp/liquidfun-rs-phase13.1/segments/13.1-02/candidate/` exactly per `13.1-EXECUTION.md`: tracked full-index binary patch plus the NUL-safe untracked manifest/archive (paths, modes, sizes, SHA-256 and archive digest), deterministic complete candidate-tree manifest/digest, and chained snapshot metadata. Record progress only in external evidence.</action>
  <verify>
    <automated>cd /tmp/liquidfun-rs-phase13.1/worktree &amp;&amp; cargo test -p liquidfun-test-protocol --all-features scenario &amp;&amp; git diff --check &amp;&amp; test -f /tmp/liquidfun-rs-phase13.1/segments/13.1-02/candidate/tracked.patch &amp;&amp; test -s /tmp/liquidfun-rs-phase13.1/segments/13.1-02/candidate/untracked-manifest.json &amp;&amp; test -f /tmp/liquidfun-rs-phase13.1/segments/13.1-02/candidate/untracked.tar &amp;&amp; test -s /tmp/liquidfun-rs-phase13.1/segments/13.1-02/candidate/candidate-tree-manifest.json &amp;&amp; test -s /tmp/liquidfun-rs-phase13.1/segments/13.1-02/candidate/snapshot.json</automated>
  </verify>
  <done>All eight roots are compliant, scenario tests pass, and cumulative work is externally recoverable with no commit.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Raw protocol bytes → typed values | Validation and identity must remain exact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-13.1-04 | T | Protocol extraction | mitigate | Stable rings, constrained sibling paths, exact focused tests. |
| T-13.1-05 | R | Intermediate diff | mitigate | Cumulative binary patch with SHA-256 and no commit. |
</threat-model>

<verification>Focused protocol tests, per-file line checks, `git diff --check`, and patch digest pass.</verification>
<success-criteria>The first protocol batch is compliant without observable contract drift.</success-criteria>
<output>Do not write a SUMMARY or any planning/lifecycle artifact. Write only `/tmp/liquidfun-rs-phase13.1/segments/13.1-02/evidence.json` and the complete candidate snapshot required by `13.1-EXECUTION.md`; the orchestrator creates this plan summary only in the authorized post-integration docs set.</output>
