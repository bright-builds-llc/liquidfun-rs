---
phase: 13.1-restore-bright-builds-structural-compliance
plan: "14"
type: execute
wave: 14
depends_on: ["13.1-13"]
files_modified:
  - tools/reference/src/protocol.cpp
  - tools/reference/src/rigid_world.cpp
  - tools/reference/src/rigid_world_phase8_execute.hpp
  - tools/reference/src/rigid_world_phase9_execute.hpp
  - tools/reference/tests/protocol_tests.cpp
  - crates/liquidfun-testbed/src/bin/interactive.rs
  - crates/liquidfun-testbed/src/capability/render.rs
  - crates/liquidfun-testbed/src/ui/protocol_viewport.rs
  - crates/liquidfun-testbed/tests/controller_ui.rs
  - fuzz/src/lib.rs
  - scripts/phase12-release-evidence.sh
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13.1-2026-07-28T00-14-49
generated_at: 2026-07-28T00:38:59Z
must_haves:
  truths:
    - "C++ exported functions, protocol bytes, CTest identity, and adapter input closure remain valid."
    - "Testbed remains passive/headless-compatible; fuzz targets and release shell contract remain stable."
    - "All 103 original findings are cleared with zero exceptions before final verification."
  artifacts:
    - path: "tools/reference/src/protocol.cpp"
      provides: "Stable C++ protocol adapter facade"
    - path: "crates/liquidfun-testbed/src/bin/interactive.rs"
      provides: "Stable interactive binary target"
    - path: "scripts/phase12-release-evidence.sh"
      provides: "Stable release evidence shell entrypoint"
  key_links:
    - from: "tools/reference source split"
      to: "tools/reference/CMakeLists.txt and tools/reference/adapter-inputs.txt"
      via: "registered sources and recomputed input closure"
      pattern: "liquidfun-reference"
---
<objective>Refactor native reference, testbed, fuzz, and shell surfaces and clear the last findings.</objective>
<execution-context>
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-EXECUTION.md
</execution-context>
<aggregate-execution>Execute only as bounded sequential segment 13.1-14 through execute-phase Pattern-B task-only agent semantics under `13.1-EXECUTION.md`. The standard per-plan execute-plan route is unsupported because its commits/summaries conflict with higher repository pre-commit rules. Do not stage, commit, write a SUMMARY, or modify lifecycle/planning state; keep progress/evidence and the complete tracked-plus-untracked candidate snapshot external. Plans 01-14 and Plan 15 Tasks 1-2 form one aggregate candidate; only the orchestrator may perform Plan 15 Task 3 after the full unchanged candidate is green.</aggregate-execution>
<context>
@.planning/phases/13.1-restore-bright-builds-structural-compliance/13.1-CONTEXT.md
@tools/reference/CMakeLists.txt
@tools/reference/adapter-inputs.txt
</context>
<tasks>
<task type="auto">
  <name>Task 1: Split native adapter and protocol tests</name>
  <files>tools/reference/src/protocol.cpp, tools/reference/src/rigid_world.cpp, tools/reference/src/rigid_world_phase8_execute.hpp, tools/reference/src/rigid_world_phase9_execute.hpp, tools/reference/tests/protocol_tests.cpp, tools/reference/CMakeLists.txt, tools/reference/adapter-inputs.txt</files>
  <action>Read all five full findings, exported declarations/call sites, `tools/reference/CMakeLists.txt`, adapter input list, presets, and CTest registration first. Initialize the exact pinned submodule in the isolated worktree if absent, then verify upstream read-only state. Extract cohesive code only below `tools/reference/src/protocol/`, `src/rigid_world/`, `src/rigid_world_phase8_execute/`, `src/rigid_world_phase9_execute/`, and `tools/reference/tests/protocol_tests/` using `.cpp`/`.hpp` files. Preserve exported C++ functions, exception/status mapping, protocol stdout bytes, reset epochs, semantic order, and test names. Update `tools/reference/CMakeLists.txt` and `tools/reference/adapter-inputs.txt` for every new repository-owned source; allow existing provenance tooling to recompute content identities per D-14. Never edit upstream or force old hashes. Configure/build both exact presets and run protocol CTest.</action>
  <verify><automated>cd /tmp/liquidfun-rs-phase13.1/worktree &amp;&amp; cargo xtask upstream verify &amp;&amp; cargo xtask upstream configure --preset oracle-debug &amp;&amp; cargo xtask upstream build --preset oracle-debug &amp;&amp; cmake --build target/reference/oracle-debug --target liquidfun-reference-protocol-tests &amp;&amp; ctest --test-dir target/reference/oracle-debug --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$' &amp;&amp; cargo xtask upstream configure --preset oracle-release &amp;&amp; cargo xtask upstream build --preset oracle-release &amp;&amp; cmake --build target/reference/oracle-release --target liquidfun-reference-protocol-tests &amp;&amp; ctest --test-dir target/reference/oracle-release --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$'</automated></verify>
  <done>Five native findings are compliant; debug/release native builds and protocol CTests pass; input closure is consistent.</done>
</task>
<task type="auto">
  <name>Task 2: Split testbed, fuzz, and release shell</name>
  <files>crates/liquidfun-testbed/src/bin/interactive.rs, crates/liquidfun-testbed/src/capability/render.rs, crates/liquidfun-testbed/src/ui/protocol_viewport.rs, crates/liquidfun-testbed/tests/controller_ui.rs, fuzz/src/lib.rs, scripts/phase12-release-evidence.sh</files>
  <action>Read all six full findings, stable target/test names, workflows, and focused tests first. Extract testbed code only under `src/bin/interactive/*.rs`, `capability/render/*.rs`, `ui/protocol_viewport/*.rs`, and `tests/controller_ui/*.rs`; preserve interactive target, passive rendering, command timing, semantic ordering, UI labels/behavior, and test names. Extract fuzz families only under `fuzz/src/lib/*.rs`; preserve five target registrations, bounds, decoding, and nested-workspace isolation. Extract shell functions only to `scripts/phase12-release-evidence/*.sh`, sourced by the stable entrypoint; each new shell file uses safe Bash conventions, and command vocabulary/output/status/identity-last behavior remain exact. Run headless testbed, fuzz build, shell syntax/contract checks, every changed/new file line check, `bun scripts/bright-builds-check.ts all`, and refresh the complete segment 13.1-14 candidate snapshot. Do not commit. After this segment's tests pass, refresh `/tmp/liquidfun-rs-phase13.1/segments/13.1-14/candidate/` exactly per `13.1-EXECUTION.md`: tracked full-index binary patch plus the NUL-safe untracked manifest/archive (paths, modes, sizes, SHA-256 and archive digest), deterministic complete candidate-tree manifest/digest, and chained snapshot metadata. Record progress only in external evidence.</action>
  <verify><automated>cd /tmp/liquidfun-rs-phase13.1/worktree &amp;&amp; DISPLAY= WAYLAND_DISPLAY= MIR_SOCKET= XDG_RUNTIME_DIR= cargo test -p liquidfun-testbed --all-features &amp;&amp; cargo build -p liquidfun-testbed --all-targets --all-features &amp;&amp; cargo +nightly-2026-07-15 fuzz build &amp;&amp; bash -n scripts/phase12-release-evidence.sh scripts/phase12-release-evidence/*.sh &amp;&amp; bun scripts/bright-builds-check.ts all &amp;&amp; git diff --check &amp;&amp; test -f /tmp/liquidfun-rs-phase13.1/segments/13.1-14/candidate/tracked.patch &amp;&amp; test -s /tmp/liquidfun-rs-phase13.1/segments/13.1-14/candidate/untracked-manifest.json &amp;&amp; test -f /tmp/liquidfun-rs-phase13.1/segments/13.1-14/candidate/untracked.tar &amp;&amp; test -s /tmp/liquidfun-rs-phase13.1/segments/13.1-14/candidate/candidate-tree-manifest.json &amp;&amp; test -s /tmp/liquidfun-rs-phase13.1/segments/13.1-14/candidate/snapshot.json</automated></verify>
  <done>The last 11 findings are cleared; checker reports 0 findings/0 exceptions; auxiliary contracts pass; no commit exists.</done>
</task>
</tasks>
<threat-model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Rust protocol ↔ native C++ adapter | ABI/process/protocol bytes and reset identity must remain exact. |
| UI/fuzz/shell inputs → effects | Presentation cannot mutate authority; bounded input and release confinement remain enforced. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-13.1-25 | T | Native split | mitigate | Explicit CMake/input closure plus both presets and CTest. |
| T-13.1-26 | E | Testbed | mitigate | Passive typed commands and headless tests. |
| T-13.1-27 | D | Fuzz/shell | mitigate | Preserve bounds/rerun safety; fuzz build and syntax/contract tests. |
</threat-model>
<verification>Native debug/release configure/build/CTest, headless testbed, fuzz build, shell checks, Bright Builds all, diff/snapshot checks pass.</verification>
<success-criteria>All 103 live findings are structurally resolved with zero exceptions and cross-language/tool contracts intact.</success-criteria>
<output>Do not write a SUMMARY or any planning/lifecycle artifact. Write only `/tmp/liquidfun-rs-phase13.1/segments/13.1-14/evidence.json` and the complete candidate snapshot required by `13.1-EXECUTION.md`; the orchestrator creates this plan summary only in the authorized post-integration docs set.</output>
