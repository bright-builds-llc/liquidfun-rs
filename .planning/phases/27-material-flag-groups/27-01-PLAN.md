---
phase: 27-material-flag-groups
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun-wasm/src/scene.rs
  - crates/liquidfun-wasm/src/scene/basin_family.rs
  - crates/liquidfun-wasm/src/scene/surface_tension.rs
  - crates/liquidfun-wasm/src/session/tests.rs
autonomous: true
requirements: [MAT-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 27-2026-09-22T05-14-55
generated_at: 2026-09-22T05:30:00Z
must_haves:
  truths:
    - "Visitor can watch Surface Tension: three colored tensile groups bead and bleed color when a ball hits them."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/surface_tension.rs
      provides: "Pinned Surface Tension basin + three tensile color-mixing groups + dynamic ball"
      exports: ["build"]
    - path: crates/liquidfun-wasm/src/scene/basin_family.rs
      provides: "Private shared vertical-wall basin + falling ball helper"
    - path: crates/liquidfun-wasm/src/scene.rs
      provides: "SceneId::SurfaceTension allowlist entry and build_scene arm"
  key_links:
    - from: crates/liquidfun-wasm/src/scene.rs
      to: crates/liquidfun-wasm/src/scene/surface_tension.rs
      via: "build_scene(SceneId::SurfaceTension) → surface_tension::build"
      pattern: "SceneId::SurfaceTension => surface_tension::build"
    - from: crates/liquidfun-wasm/src/scene/surface_tension.rs
      to: liquidfun public API
      via: "TENSILE | COLOR_MIXING on all three group recipes"
      pattern: "TENSILE.*COLOR_MIXING|COLOR_MIXING.*TENSILE"
---

<objective>
Port the pinned LiquidFun Surface Tension test as a watch-first WASM scene: vertical-wall basin, three colored tensile|color-mixing groups, and one falling dynamic ball.

Purpose: Deliver MAT-01 recognizable beading and color bleed on the public `liquidfun` API without engine feature work, catalog advertising, or catch-up/cap changes (D-07, D-13, D-14, D-15).
Output: `basin_family.rs` helper, `surface_tension.rs`, `SceneId::SurfaceTension` factory wiring, and headless construction/flag/advance tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/27-material-flag-groups/27-CONTEXT.md
@.planning/phases/27-material-flag-groups/27-RESEARCH.md
@.planning/phases/27-material-flag-groups/27-UI-SPEC.md
@crates/liquidfun-wasm/src/scene.rs
@crates/liquidfun-wasm/src/scene/particles.rs
@crates/liquidfun-wasm/src/scene/color_mixer.rs
@crates/liquidfun-wasm/src/scene/liquid_timer.rs
@crates/liquidfun-wasm/src/session/tests.rs
@third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testSurfaceTension.js

<interfaces>
Pinned layout constants (from `testSurfaceTension.js` / research; do not invent alternate geometry):

```text
Shared basin family (vertical walls ending at y=2 — NOT Particles slanted tops):
Floor: (-4,-1), (4,-1), (4,0), (-4,0)
Left:  (-4,-0.1), (-2,-0.1), (-2,2), (-4,2)
Right: (2,-0.1), (4,-0.1), (4,2), (2,2)
ParticleSystemDef.radius = 0.035
ParticleSystemDef.damping = 0.2  (JS; prefer JS over C++ omission)
Gravity: (0, -10)
Dynamic ball: BodyType::Dynamic, CircleShape center (0,8) radius 0.5, density 0.5

Surface Tension groups (all TENSILE | COLOR_MIXING, no group flags):
Red circle: center (0,2) radius 0.5, color (255,0,0,255)
Green circle: center (-1,2) radius 0.5, color (0,255,0,255)
Blue box vertices: (0,3), (2,3), (2,3.5), (0,3.5), color (0,0,255,255)
```

Existing factory surface (extend; keep the eight existing variants):

```rust
pub(crate) enum SceneId {
    DamBreak, Fountain, FloatOrSink, ColorMixer, JellyDrop, WaterWheel,
    Particles, LiquidTimer, /* + SurfaceTension this plan */
}
pub(crate) fn parse_scene_id(raw: &str) -> Result<SceneId, SessionError>;
pub(crate) fn build_scene(id: SceneId, presets: &[(String, String)]) -> Result<BuiltScene, SessionError>;
```

Do not add `elastic-particles` or `rigid-particles` in this plan. Do not change `MAX_ADVANCE_STEPS`.
</interfaces>
</context>

<feature>
  <name>Surface Tension WASM scene</name>
  <files>crates/liquidfun-wasm/src/scene/surface_tension.rs, crates/liquidfun-wasm/src/scene/basin_family.rs, crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <behavior>
    - `parse_scene_id("surface-tension")` → `Ok(SceneId::SurfaceTension)`
    - `SessionCore::create(SceneId::SurfaceTension)` succeeds; particle count &gt; 0; at least one rigid circle exported
    - Created particles include `TENSILE | COLOR_MIXING` bits
    - Advance several steps does not panic; particle count stays &gt; 0
    - Unknown control/action returns `SessionError::UnknownControl`; `apply_pointer` is a no-op success
    - `MAX_ADVANCE_STEPS` source still equals `4`
  </behavior>
  <implementation>
    Scene-only port per D-07/D-13/D-14/D-15. Private `basin_family` helper for vertical-wall basin + ball (Claude's Discretion). No new `liquidfun` APIs. No catalog/TS edits. Do not raise catch-up cap or cut particle counts.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing Surface Tension allowlist and flag tests</name>
  <files>crates/liquidfun-wasm/src/session/tests.rs, crates/liquidfun-wasm/src/scene/surface_tension.rs</files>
  <read_first>
    1. `.planning/phases/27-material-flag-groups/27-CONTEXT.md` D-07, D-13, D-14, D-15
    1. `.planning/phases/27-material-flag-groups/27-RESEARCH.md` Capability Verdict, Pattern 3, Pitfall 1, Pitfall 2
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testSurfaceTension.js`
    1. `crates/liquidfun-wasm/src/session/tests.rs` `parse_scene_id_maps_allowlisted_tokens`
    1. `crates/liquidfun-wasm/src/scene/liquid_timer.rs` flag-assert test pattern
    1. `standards/core/testing.md`
  </read_first>
  <behavior>
    1. Test: `parse_scene_id("surface-tension")` equals `Ok(SceneId::SurfaceTension)`.
    1. Test: `SessionCore::create(SceneId::SurfaceTension)` returns Ok and `particle_count() > 0`.
    1. Test: created session exposes at least one rigid circle (ball).
    1. Test: sampled particle flags contain `TENSILE` and `COLOR_MIXING`.
    1. Test: `advance(4)` several times does not panic and particle count stays &gt; 0.
    1. Test: unknown control name yields `SessionError::UnknownControl`.
  </behavior>
  <action>
    Per D-07 and testing standards, write Arrange/Act/Assert unit tests first. Extend `parse_scene_id_maps_allowlisted_tokens` in `crates/liquidfun-wasm/src/session/tests.rs` with `("surface-tension", SceneId::SurfaceTension)` — this will not compile until Task 2 adds the enum variant. Add `#[cfg(test)]` module tests in `surface_tension.rs` that construct via `SessionCore::create(SceneId::SurfaceTension)` and assert `ParticleFlags::TENSILE | ParticleFlags::COLOR_MIXING` on created particles (mirror Liquid Timer's TENSILE|VISCOUS assert). Do not edit `web/**`. Do not add `elastic-particles` or `rigid-particles`. Do not change `MAX_ADVANCE_STEPS`.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture 2&gt;&amp;1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n '"surface-tension"' crates/liquidfun-wasm/src/session/tests.rs` matches the allowlist table.
    1. `rg -n 'SceneId::SurfaceTension' crates/liquidfun-wasm/src/scene/surface_tension.rs crates/liquidfun-wasm/src/session/tests.rs` matches at least one test reference.
    1. `rg -n 'TENSILE|COLOR_MIXING' crates/liquidfun-wasm/src/scene/surface_tension.rs` matches flag asserts in tests.
    1. Tests exist before production `surface_tension::build` is complete (RED or compile-fail expected until Task 2).
  </acceptance_criteria>
  <done>Failing Surface Tension allowlist/flag/construction coverage is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement basin helper, Surface Tension scene, and factory wiring</name>
  <files>crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/scene/basin_family.rs, crates/liquidfun-wasm/src/scene/surface_tension.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <read_first>
    1. `crates/liquidfun-wasm/src/scene/particles.rs` watch-first hooks + collect_segments/circles
    1. `crates/liquidfun-wasm/src/scene/color_mixer.rs` COLOR_MIXING group recipe pattern
    1. `crates/liquidfun-wasm/src/scene.rs` `SceneId`, `parse_scene_id`, `build_scene`, `attach_basin_fixture`
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testSurfaceTension.js`
    1. `standards/languages/rust.md` foo.rs + foo/ module shape
    1. `standards/core/architecture.md` scene construction in WASM shell
  </read_first>
  <behavior>
    Same as Task 1; all listed tests must pass.
  </behavior>
  <action>
    Per D-07/D-13/D-14/D-15: create private `mod basin_family;` with helpers that attach the three vertical-wall basin polygons (floor + left + right ending at `y=2`, NOT Particles slanted `y=3` tops) via `attach_basin_fixture`, and spawn the dynamic ball at `(0, 8)` r=`0.5` density `0.5`. Add `mod surface_tension;` and `SceneId::SurfaceTension`. Map `"surface-tension"` in `parse_scene_id`. Call `surface_tension::build` from `build_scene`. Implement `surface_tension::build` using basin_family, `ParticleSystemDef` radius `0.035` and damping `0.2`, three groups all with `ParticleFlags::TENSILE | ParticleFlags::COLOR_MIXING` and colors RGB as pinned (red circle `(0,2)` r=`0.5`, green circle `(-1,2)` r=`0.5`, blue box vertices `(0,3)/(2,3)/(2,3.5)/(0,3.5)`). Reject non-empty unknown presets/controls/actions with `SessionError::UnknownControl`. Make `apply_pointer` a no-op success (D-05). Export basin segments plus the ball in `collect_segments` / `collect_circles`. Do not cut particle counts or raise `MAX_ADVANCE_STEPS` above 4. Do not add engine APIs. Do not touch `web/**`. Do not add Elastic/Rigid scene ids yet.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm surface_tension -- --nocapture &amp;&amp; cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture &amp;&amp; rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'SceneId::SurfaceTension' crates/liquidfun-wasm/src/scene.rs` matches enum, parse, and build arms.
    1. `rg -n '0\.035' crates/liquidfun-wasm/src/scene/surface_tension.rs` matches the system radius.
    1. `rg -n 'with_damping\(0\.2\)|damping.*0\.2' crates/liquidfun-wasm/src/scene/surface_tension.rs` matches damping 0.2.
    1. `rg -n 'TENSILE|COLOR_MIXING' crates/liquidfun-wasm/src/scene/surface_tension.rs` matches production recipe bits.
    1. `rg -n 'Vec2::new\(0\.0,\s*8\.0\)|Vec2::new\(0,\s*8\)' crates/liquidfun-wasm/src/scene/basin_family.rs crates/liquidfun-wasm/src/scene/surface_tension.rs` matches the ball position.
    1. `rg -n 'elastic-particles|ElasticParticles|rigid-particles|RigidParticles' crates/liquidfun-wasm/src/scene.rs crates/liquidfun-wasm/src/scene/surface_tension.rs` is empty.
    1. `rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs` matches.
    1. `cargo test -p liquidfun-wasm surface_tension` exits 0.
    1. `cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens` exits 0.
  </acceptance_criteria>
  <done>Surface Tension is constructible and advance-safe through the WASM session factory with TENSILE|COLOR_MIXING groups.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hash/scene id string → WASM factory | Untrusted route token crosses into allowlisted `SceneId` parse. |
| Scene module → liquidfun public API | Scene construction must not invent unsafe FFI or raw pointers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | T | `parse_scene_id` | mitigate | Exact lowercase `"surface-tension"` match only; unknown tokens still `UnknownScene`; no case folding. |
| T-27-01-02 | I | Flag recipe honesty | mitigate | Headless tests assert `TENSILE \| COLOR_MIXING` so Catalog cannot later claim Surface Tension without those bits. |
| T-27-01-03 | D | Catch-up under load | mitigate | Do not raise `MAX_ADVANCE_STEPS` above 4; verify constant remains 4. |
| T-27-01-04 | E | Watch-first controls | mitigate | Reject unknown controls; `apply_pointer` no-op — no hidden mutation surface. |
</threat_model>

<verification>
`cargo test -p liquidfun-wasm surface_tension` and `parse_scene_id_maps_allowlisted_tokens` exit 0; `MAX_ADVANCE_STEPS` stays 4.
</verification>

<success_criteria>
1. MAT-01 native scene constructs with basin + three tensile color-mixing groups + falling ball.
1. Allowlist parse accepts `surface-tension` only as exact token.
1. No catalog advertising yet; no engine API added.
</success_criteria>

<output>
After completion, create `.planning/phases/27-material-flag-groups/27-01-SUMMARY.md`
</output>
