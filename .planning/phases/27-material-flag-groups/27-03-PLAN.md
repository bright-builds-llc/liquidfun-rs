---
phase: 27-material-flag-groups
plan: "03"
type: tdd
wave: 3
depends_on: ["02"]
files_modified:
  - crates/liquidfun-wasm/src/scene.rs
  - crates/liquidfun-wasm/src/scene/rigid_particles.rs
  - crates/liquidfun-wasm/src/session/tests.rs
autonomous: true
requirements: [MAT-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 27-2026-09-22T05-14-55
generated_at: 2026-09-22T05:30:00Z
must_haves:
  truths:
    - "Visitor can watch Rigid Particles: three colored clumps stay solid and do not stretch like jelly when a ball hits them."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/rigid_particles.rs
      provides: "Pinned Rigid Particles solid clumps + spinning box + ball"
      exports: ["build"]
    - path: crates/liquidfun-wasm/src/scene.rs
      provides: "SceneId::RigidParticles allowlist entry and build_scene arm"
  key_links:
    - from: crates/liquidfun-wasm/src/scene.rs
      to: crates/liquidfun-wasm/src/scene/rigid_particles.rs
      via: "build_scene(SceneId::RigidParticles) → rigid_particles::build"
      pattern: "SceneId::RigidParticles => rigid_particles::build"
    - from: crates/liquidfun-wasm/src/scene/rigid_particles.rs
      to: liquidfun public API
      via: "ParticleGroupFlags::RIGID | SOLID without elastic particle flags"
      pattern: "ParticleGroupFlags::RIGID|RIGID.*SOLID"
---

<objective>
Port the pinned LiquidFun Rigid Particles test as a watch-first WASM scene: same basin family as Elastic Particles, three colored rigid-and-solid groups (including spinning box), and falling ball.

Purpose: Deliver MAT-03 recognizable solid-clump contrast (does not stretch like jelly) using existing `RIGID | SOLID` group flags — first gallery consumer of that path (D-09, D-13, D-14).
Output: `rigid_particles.rs`, factory wiring, and group-flag construction tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/27-material-flag-groups/27-CONTEXT.md
@.planning/phases/27-material-flag-groups/27-RESEARCH.md
@.planning/phases/27-material-flag-groups/27-02-SUMMARY.md
@crates/liquidfun-wasm/src/scene.rs
@crates/liquidfun-wasm/src/scene/basin_family.rs
@crates/liquidfun-wasm/src/scene/elastic_particles.rs
@third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testRigidParticles.js

<interfaces>
Pinned Rigid layout (from `testRigidParticles.js` — same poses as Elastic):

```text
Reuse basin_family + ball.
ParticleSystemDef.radius = 0.035
Red circle (0,3) r=0.5, group RIGID|SOLID, default WATER particle flags, color (255,0,0,255)
Green circle (-1,3) r=0.5, group RIGID|SOLID, color (0,255,0,255)
Blue box half-extents 1×0.5 at (1,4), angle -0.5, ω=2.0, group RIGID|SOLID, color (0,0,255,255)
```

Do NOT set `ParticleFlags::ELASTIC` or `SPRING` on Rigid groups.
</interfaces>
</context>

<feature>
  <name>Rigid Particles WASM scene</name>
  <files>crates/liquidfun-wasm/src/scene/rigid_particles.rs, crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <behavior>
    - `parse_scene_id("rigid-particles")` → `Ok(SceneId::RigidParticles)`
    - Construction succeeds; particle count &gt; 0; rigid circle ball present
    - Groups carry `RIGID | SOLID`; particles do not carry ELASTIC or SPRING
    - Blue box uses angle -0.5 and angular velocity 2.0
    - Watch-first hooks; advance safe; `MAX_ADVANCE_STEPS` stays 4
  </behavior>
  <implementation>
    Scene-only on public recipe API. No engine work unless correctly flagged clumps still stretch like jelly after wiring (D-13 discretionary escalate only then).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing Rigid Particles allowlist and group-flag tests</name>
  <files>crates/liquidfun-wasm/src/session/tests.rs, crates/liquidfun-wasm/src/scene/rigid_particles.rs</files>
  <read_first>
    1. `.planning/phases/27-material-flag-groups/27-CONTEXT.md` D-09, D-13, D-14
    1. `.planning/phases/27-material-flag-groups/27-RESEARCH.md` Capability Verdict Rigid row, Pitfall 1
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testRigidParticles.js`
    1. `crates/liquidfun-wasm/src/session/tests.rs` allowlist after Plans 01–02
    1. `crates/liquidfun-wasm/src/scene/elastic_particles.rs` shared layout poses to mirror without elastic flags
  </read_first>
  <behavior>
    1. Test: `parse_scene_id("rigid-particles")` equals `Ok(SceneId::RigidParticles)`.
    1. Test: `SessionCore::create(SceneId::RigidParticles)` Ok with `particle_count() > 0` and ≥1 rigid circle.
    1. Test: groups include `ParticleGroupFlags::RIGID` and `SOLID`.
    1. Test: sampled particles do not contain `ELASTIC` or `SPRING`.
    1. Test: advance several steps does not panic; unknown control rejected.
  </behavior>
  <action>
    Per D-09: extend `parse_scene_id_maps_allowlisted_tokens` with `("rigid-particles", SceneId::RigidParticles)`. Add `#[cfg(test)]` tests in `rigid_particles.rs` asserting `RIGID | SOLID` group flags and absence of ELASTIC/SPRING particle flags. Do not edit `web/**`. Do not change `MAX_ADVANCE_STEPS`.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture 2&gt;&amp;1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n '"rigid-particles"' crates/liquidfun-wasm/src/session/tests.rs` matches.
    1. `rg -n 'SceneId::RigidParticles' crates/liquidfun-wasm/src/scene/rigid_particles.rs crates/liquidfun-wasm/src/session/tests.rs` matches test references.
    1. `rg -n 'RIGID|SOLID|ELASTIC|SPRING' crates/liquidfun-wasm/src/scene/rigid_particles.rs` matches test asserts for required and forbidden flags.
    1. Tests exist before production `rigid_particles::build` is complete (RED/compile-fail expected until Task 2).
  </acceptance_criteria>
  <done>Failing Rigid Particles allowlist/group-flag coverage is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Rigid Particles scene and factory wiring</name>
  <files>crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/scene/rigid_particles.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <read_first>
    1. `crates/liquidfun-wasm/src/scene/elastic_particles.rs` poses and spinning-box recipe (reuse geometry, swap flags)
    1. `crates/liquidfun-wasm/src/scene/basin_family.rs`
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testRigidParticles.js`
    1. `crates/liquidfun-wasm/src/scene.rs` SceneId match arms after Plan 02
  </read_first>
  <behavior>
    Same as Task 1; all listed tests must pass.
  </behavior>
  <action>
    Per D-09/D-13/D-14/D-15: add `mod rigid_particles;` and `SceneId::RigidParticles`. Map `"rigid-particles"` in `parse_scene_id`. Call `rigid_particles::build` from `build_scene`. Implement build using `basin_family`, radius `0.035`, three groups at Elastic poses with `with_group_flags(ParticleGroupFlags::RIGID | ParticleGroupFlags::SOLID)` and default water particle flags (do not call `with_particle_flags(ELASTIC)` or `SPRING`). Blue box: same transform angle `-0.5` and `with_angular_velocity(2.0)`. Colors RGB. Watch-first hooks. Do not cut counts or raise `MAX_ADVANCE_STEPS`. Do not add engine APIs unless heads-up tests prove clumps stretch like jelly after correct flags (escalate only then per D-13). Do not touch `web/**`.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm rigid_particles -- --nocapture &amp;&amp; cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture &amp;&amp; rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'SceneId::RigidParticles' crates/liquidfun-wasm/src/scene.rs` matches enum, parse, and build arms.
    1. `rg -n 'ParticleGroupFlags::RIGID' crates/liquidfun-wasm/src/scene/rigid_particles.rs` matches production recipes.
    1. `rg -n 'ParticleGroupFlags::SOLID' crates/liquidfun-wasm/src/scene/rigid_particles.rs` matches.
    1. `rg -n 'ParticleFlags::ELASTIC|ParticleFlags::SPRING' crates/liquidfun-wasm/src/scene/rigid_particles.rs` is empty in production recipe code (tests may mention them as forbidden).
    1. `rg -n 'with_angular_velocity\(2' crates/liquidfun-wasm/src/scene/rigid_particles.rs` matches.
    1. `rg -n '"surface-tension"|"elastic-particles"|"rigid-particles"' crates/liquidfun-wasm/src/scene.rs` matches all three parse arms.
    1. `cargo test -p liquidfun-wasm rigid_particles` exits 0.
    1. `cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens` exits 0.
  </acceptance_criteria>
  <done>Rigid Particles is constructible with RIGID|SOLID clumps that do not use elastic particle flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scene id string → WASM factory | Untrusted token must stay allowlisted. |
| Group flags → solidity story | Elastic particle flags on rigid groups would spoof MAT-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | T | `parse_scene_id` | mitigate | Exact `"rigid-particles"` only. |
| T-27-03-02 | I | Rigid recipe | mitigate | Tests require `RIGID \| SOLID` and forbid ELASTIC/SPRING particle flags. |
| T-27-03-03 | D | Catch-up cap | mitigate | Keep `MAX_ADVANCE_STEPS = 4`. |
| T-27-03-04 | E | Watch-first | mitigate | Unknown controls rejected; pointer no-op. |
</threat_model>

<verification>
`cargo test -p liquidfun-wasm rigid_particles` and allowlist parse tests exit 0; all three material SceneIds parse; `MAX_ADVANCE_STEPS` stays 4.
</verification>

<success_criteria>
1. MAT-03 native scene constructs with three rigid-solid clumps and falling ball.
1. No elastic particle flags on rigid groups.
1. WASM factory can build all three Phase 27 scenes before catalog advertising.
</success_criteria>

<output>
After completion, create `.planning/phases/27-material-flag-groups/27-03-SUMMARY.md`
</output>
