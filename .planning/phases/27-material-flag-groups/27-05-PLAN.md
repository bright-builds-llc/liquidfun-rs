---
phase: 27-material-flag-groups
plan: "05"
type: tdd
wave: 5
depends_on: ["04"]
files_modified:
  - web/tests/scenes.test.ts
  - web/tests/navigation.test.ts
  - web/scripts/demo-media/model.ts
  - web/tests/demo-media-model.test.ts
autonomous: true
requirements: [MAT-01, MAT-02, MAT-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 27-2026-09-22T05-14-55
generated_at: 2026-09-22T05:30:00Z
must_haves:
  truths:
    - "Vitest locks eleven SCENE_IDS in order with UI-SPEC descriptions, watch-first hints, empty controls, and pinned credit URLs for the three material scenes."
    - "SCENE_CAPTURE_PLANS length and order match SCENE_IDS including surface-tension, elastic-particles, and rigid-particles."
  artifacts:
    - path: web/tests/scenes.test.ts
      provides: "Eleven-scene catalog contract tests"
    - path: web/scripts/demo-media/model.ts
      provides: "Capture plans for three material scenes"
  key_links:
    - from: web/tests/scenes.test.ts
      to: web/src/catalog/scenes.ts
      via: "UI_SPEC_DESCRIPTIONS / credit href asserts"
      pattern: "surface-tension|elastic-particles|rigid-particles"
    - from: web/scripts/demo-media/model.ts
      to: web/src/catalog/scenes.ts
      via: "assertSceneCapturePlanCoverage"
      pattern: "SCENE_CAPTURE_PLANS"
---

<objective>
Lock the eleven-scene catalog contract in Vitest and keep demo-media capture plans synchronized with SCENE_IDS.

Purpose: Prevent eight-scene hardcoding rot and keep MAT-01/02/03 metadata regressions cheap to catch before Chromium smoke (research Pitfall 3/4).
Output: Updated unit tests and watch-first capture plan stubs for the three material scenes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@.planning/phases/27-material-flag-groups/27-UI-SPEC.md
@.planning/phases/27-material-flag-groups/27-RESEARCH.md
@.planning/phases/27-material-flag-groups/27-04-SUMMARY.md
@web/src/catalog/scenes.ts
@web/tests/scenes.test.ts
@web/scripts/demo-media/model.ts

<interfaces>
From Plan 04 / UI-SPEC — tests must assert:

```ts
SCENE_IDS === [
  "dam-break","fountain","float-or-sink","color-mixer",
  "jelly-drop","water-wheel","particles","liquid-timer",
  "surface-tension","elastic-particles","rigid-particles",
]
```

Watch-first hint exact string for all three new scenes:
`This scene is watch-first. Use Play scene, Pause scene, and Reset scene.`

Pinned commit in inspiration hrefs:
`7f20402173fd143a3988c921bc384459c6a858f2`

Capture plans for watch-first scenes: append entries with `id`/`title`/`route` matching catalog; use `action: { kind: "click", point: { x: 0.5, y: 0.5 } }` (pointer is no-op in WASM — capture script still needs a SceneAction). Keep `interactionStep: 180`.
</interfaces>
</context>

<feature>
  <name>Eleven-scene catalog unit contract</name>
  <files>web/tests/scenes.test.ts, web/scripts/demo-media/model.ts</files>
  <behavior>
    - lists eleven locked scenes in approved order
    - descriptions/hints match UI-SPEC for the three material scenes
    - controls length 0 for all three
    - inspiration hrefs include six pinned JS/C++ files at the pin
    - implementation paths end with surface_tension.rs / elastic_particles.rs / rigid_particles.rs and never google/liquidfun
    - SCENE_CAPTURE_PLANS covers all SCENE_IDS in order
  </behavior>
  <implementation>
    Update Vitest tables and capture plans only. No Playwright in this plan.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand scenes.test.ts to eleven scenes and credits</name>
  <files>web/tests/scenes.test.ts, web/tests/navigation.test.ts</files>
  <read_first>
    1. `.planning/phases/27-material-flag-groups/27-UI-SPEC.md` catalog descriptions, hints, credits
    1. `.planning/phases/27-material-flag-groups/27-RESEARCH.md` Pitfall 3, Pitfall 5
    1. `web/tests/scenes.test.ts`
    1. `web/src/catalog/scenes.ts`
    1. `web/tests/navigation.test.ts`
  </read_first>
  <behavior>
    1. Test title/order length expects 11, not 8.
    1. `UI_SPEC_DESCRIPTIONS` / `UI_SPEC_HINTS` Records include the three new ids with exact UI-SPEC strings.
    1. Assert all three new scenes have `controls.length === 0`.
    1. Assert inspiration labels/hrefs for the six pinned files at the pinned commit.
    1. Assert implementation paths are `crates/liquidfun-wasm/src/scene/surface_tension.rs`, `.../elastic_particles.rs`, and `.../rigid_particles.rs`.
    1. Assert no implementation path contains `google/liquidfun`.
  </behavior>
  <action>
    Per D-10/D-11/D-12: rewrite the “lists eight locked scenes” test to “lists eleven locked scenes…” with the full expected id array. Extend `UI_SPEC_DESCRIPTIONS` and `UI_SPEC_HINTS` Records (TypeScript exhaustiveness requires the three new keys). Add focused asserts for empty controls and pinned credit URLs. Keep existing eight-scene string expectations unchanged. Update any hard-coded length `8` in `navigation.test.ts` only if present. Prefer Arrange/Act/Assert.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/scenes.test.ts tests/navigation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'lists eleven locked scenes|toHaveLength\(11\)' web/tests/scenes.test.ts` matches.
    1. `rg -n 'surface-tension|elastic-particles|rigid-particles' web/tests/scenes.test.ts` matches description/hint tables.
    1. `rg -n 'lists eight locked scenes|toHaveLength\(8\)' web/tests/scenes.test.ts` is empty.
    1. `rg -n '7f20402173fd143a3988c921bc384459c6a858f2' web/tests/scenes.test.ts` matches credit asserts.
    1. `rg -n 'google/liquidfun' web/tests/scenes.test.ts` matches a negative assert (implementation must not use that path).
    1. `cd web && bun run test:unit -- tests/scenes.test.ts tests/navigation.test.ts` exits 0.
  </acceptance_criteria>
  <done>Unit tests lock eleven-scene catalog metadata and credits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sync SCENE_CAPTURE_PLANS with eleven SCENE_IDS</name>
  <files>web/scripts/demo-media/model.ts, web/tests/demo-media-model.test.ts</files>
  <read_first>
    1. `web/scripts/demo-media/model.ts` `SCENE_CAPTURE_PLANS` + `assertSceneCapturePlanCoverage`
    1. `web/tests/demo-media-model.test.ts`
    1. `web/src/catalog/scenes.ts` `SCENE_IDS`
  </read_first>
  <behavior>
    1. `SCENE_CAPTURE_PLANS` appends Surface Tension, Elastic Particles, and Rigid Particles after Liquid Timer in catalog order.
    1. `assertSceneCapturePlanCoverage` does not throw on import.
    1. Existing demo-media-model unit test still passes with eleven ids.
  </behavior>
  <action>
    Append three `SceneCapturePlan` objects after `liquid-timer`:
    - `{ id: "surface-tension", title: "Surface Tension", route: "/liquidfun-rs/#/scene/surface-tension", interactionStep: 180, action: { kind: "click", point: { x: 0.5, y: 0.5 } } }`
    - `{ id: "elastic-particles", title: "Elastic Particles", route: "/liquidfun-rs/#/scene/elastic-particles", interactionStep: 180, action: { kind: "click", point: { x: 0.5, y: 0.5 } } }`
    - `{ id: "rigid-particles", title: "Rigid Particles", route: "/liquidfun-rs/#/scene/rigid-particles", interactionStep: 180, action: { kind: "click", point: { x: 0.5, y: 0.5 } } }`
    Do not change capture profile timing constants. Do not require the click to mutate physics (watch-first no-op is fine). Update `demo-media-model.test.ts` expectations if they hard-code eight.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run test:unit -- tests/demo-media-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'id: "surface-tension"|id: "elastic-particles"|id: "rigid-particles"' web/scripts/demo-media/model.ts` matches all three.
    1. `rg -n 'SCENE_IDS\.length' web/scripts/demo-media/model.ts` still used by coverage assert.
    1. `cd web && bun run test:unit -- tests/demo-media-model.test.ts` exits 0.
  </acceptance_criteria>
  <done>Capture-plan allowlist matches the eleven-scene catalog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit tests → catalog honesty | Tests are the regression gate for credit and copy claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05-01 | I | Credit URL asserts | mitigate | Vitest requires pinned commit blob paths for JS+C++ inspiration for all three scenes. |
| T-27-05-02 | S | Implementation path asserts | mitigate | Reject google/liquidfun as implementationPath. |
| T-27-05-03 | T | Capture plan sync | mitigate | `assertSceneCapturePlanCoverage` fails import if lengths diverge. |
</threat_model>

<verification>
`cd web && bun run test:unit -- tests/scenes.test.ts tests/navigation.test.ts tests/demo-media-model.test.ts` exits 0.
</verification>

<success_criteria>
1. Eleven-scene catalog metadata and credits are locked in Vitest.
1. Capture plans stay synchronized with SCENE_IDS.
</success_criteria>

<output>
After completion, create `.planning/phases/27-material-flag-groups/27-05-SUMMARY.md`
</output>
