---
phase: 27-material-flag-groups
plan: "04"
type: execute
wave: 4
depends_on: ["01", "02", "03"]
files_modified:
  - web/src/catalog/scenes.ts
  - web/src/catalog/previews.tsx
  - web/src/player/runtime.ts
autonomous: true
requirements: [MAT-01, MAT-02, MAT-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 27-2026-09-22T05-14-55
generated_at: 2026-09-22T05:30:00Z
must_haves:
  truths:
    - "SCENE_IDS order is the eight existing demos then surface-tension, elastic-particles, rigid-particles; all three new records are ready: true with empty controls."
    - "Each new scene credits pinned JS + C++ blob URLs at commit 7f20402173fd143a3988c921bc384459c6a858f2 via host-locked implementation paths."
    - "PAGE_SUMMARY says eleven demos; Static preview SVG cases exist for all three new ids; captions remain Static preview."
  artifacts:
    - path: web/src/catalog/scenes.ts
      provides: "Eleven-scene catalog records, credits, watch-first hints"
    - path: web/src/catalog/previews.tsx
      provides: "Surface Tension, Elastic Particles, Rigid Particles static SVG previews"
    - path: web/src/player/runtime.ts
      provides: "PAGE_SUMMARY eleven-demo copy"
  key_links:
    - from: web/src/catalog/scenes.ts
      to: crates/liquidfun-wasm SceneId tokens
      via: "id surface-tension / elastic-particles / rigid-particles match parse_scene_id"
      pattern: "\"surface-tension\"|\"elastic-particles\"|\"rigid-particles\""
    - from: web/src/catalog/previews.tsx
      to: DemoNavigation caption
      via: "ScenePreview switch exhaustiveness"
      pattern: "case \"surface-tension\"|case \"elastic-particles\"|case \"rigid-particles\""
---

<objective>
Append Surface Tension, Elastic Particles, and Rigid Particles to the shared catalog/player chrome with honest credits, static previews, and watch-first empty controls.

Purpose: Advertise the three ready material scenes (D-01–D-06, D-10–D-12) only after WASM Plans 01–03 can build them; keep PlaygroundShell and ban `.catalog-card`.
Output: Updated `scenes.ts`, `previews.tsx`, and `PAGE_SUMMARY`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@standards/core/frontend-ui.md
@standards/languages/typescript-javascript.md
@standards-overrides.md
@.planning/phases/27-material-flag-groups/27-CONTEXT.md
@.planning/phases/27-material-flag-groups/27-UI-SPEC.md
@.planning/phases/27-material-flag-groups/27-01-SUMMARY.md
@.planning/phases/27-material-flag-groups/27-02-SUMMARY.md
@.planning/phases/27-material-flag-groups/27-03-SUMMARY.md
@web/src/catalog/scenes.ts
@web/src/catalog/previews.tsx
@web/src/player/runtime.ts
@web/src/components/scene-credits.ts

<interfaces>
Exact catalog append (D-01 / 27-UI-SPEC):

```ts
export const SCENE_IDS = [
  "dam-break",
  "fountain",
  "float-or-sink",
  "color-mixer",
  "jelly-drop",
  "water-wheel",
  "particles",
  "liquid-timer",
  "surface-tension",
  "elastic-particles",
  "rigid-particles",
] as const;
```

New records (exact strings from 27-UI-SPEC):

| Field | Surface Tension | Elastic Particles | Rigid Particles |
|-------|-----------------|-------------------|-----------------|
| title | `Surface Tension` | `Elastic Particles` | `Rigid Particles` |
| ready | `true` | `true` | `true` |
| description | `Watch three colored tensile groups bead and bleed color when a ball hits them.` | `Watch three soft particle clumps deform when a ball falls on them.` | `Watch three colored rigid clumps stay solid when a ball hits them.` |
| interactionHint | `This scene is watch-first. Use Play scene, Pause scene, and Reset scene.` | same (`WATCH_FIRST_HINT`) | same |
| controls | `[]` | `[]` | `[]` |
| implementationPath | `crates/liquidfun-wasm/src/scene/surface_tension.rs` | `.../elastic_particles.rs` | `.../rigid_particles.rs` |

Inspiration (commit `7f20402173fd143a3988c921bc384459c6a858f2`):

- Surface Tension: `Pinned SurfaceTension.js` → `testSurfaceTension.js`; `Pinned ParticlesSurfaceTension.h` → `ParticlesSurfaceTension.h`
- Elastic Particles: `Pinned ElasticParticles.js` → `testElasticParticles.js`; `Pinned ElasticParticles.h` → `ElasticParticles.h`
- Rigid Particles: `Pinned RigidParticles.js` → `testRigidParticles.js`; `Pinned RigidParticles.h` → `RigidParticles.h`

Optional `LiquidFun showcase` entry allowed for parity with existing demos.

`PAGE_SUMMARY` exact replacement:

```ts
"Play experimental Rust physics scenes in the browser. All eleven demos run this repository's engine through WebAssembly."
```

Preview tokens (27-UI-SPEC): red `#F87171`, green `#3DDC97`, blue `#4DA3FF`, rigid `#CBD5E1`, canvas `#071018`. Caption text stays `Static preview` in DemoNavigation (do not change caption string).
Empty SceneControls hide already exists from Phase 26 — do not regress it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append scene records and pinned credits</name>
  <files>web/src/catalog/scenes.ts, web/src/player/runtime.ts</files>
  <read_first>
    1. `.planning/phases/27-material-flag-groups/27-CONTEXT.md` D-01, D-04, D-05, D-10, D-11, D-12
    1. `.planning/phases/27-material-flag-groups/27-UI-SPEC.md` Copywriting Contract + Credits copy + Catalog demo descriptions
    1. `web/src/catalog/scenes.ts` full file (eight-scene baseline)
    1. `web/src/player/runtime.ts` `PAGE_SUMMARY`
    1. `web/src/components/scene-credits.ts` host-lock helpers (do not weaken)
  </read_first>
  <action>
    Per D-01/D-04/D-05/D-10/D-11/D-12: append `"surface-tension"`, `"elastic-particles"`, and `"rigid-particles"` to `SCENE_IDS` after `"liquid-timer"` (never reorder the first eight). Append matching `SCENES` records with `ready: true`, exact UI-SPEC descriptions, `WATCH_FIRST_HINT`, `controls: []`, implementation paths via `sceneSource("surface_tension.rs")` / `sceneSource("elastic_particles.rs")` / `sceneSource("rigid_particles.rs")`, and inspiration arrays with the two pinned blob URLs each (plus optional `SHOWCASE`). Implementation hrefs must stay host-locked — never set implementationPath to a google/liquidfun URL. Replace `PAGE_SUMMARY` with the exact eleven-demo sentence. Do not add material/stiffness/color/particle-type controls. Do not restore `.catalog-card`. Do not edit WASM scene modules in this task.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck 2&gt;&amp;1 | head -60</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n '"surface-tension"|"elastic-particles"|"rigid-particles"' web/src/catalog/scenes.ts` matches SCENE_IDS and records.
    1. `rg -n 'All eleven demos' web/src/player/runtime.ts` matches.
    1. `rg -n 'All eight demos' web/src/player/runtime.ts` is empty.
    1. `rg -n '7f20402173fd143a3988c921bc384459c6a858f2' web/src/catalog/scenes.ts` matches ≥6 inspiration hrefs for the three new scenes.
    1. `rg -n 'Pinned SurfaceTension\.js|Pinned ParticlesSurfaceTension\.h|Pinned ElasticParticles\.js|Pinned ElasticParticles\.h|Pinned RigidParticles\.js|Pinned RigidParticles\.h' web/src/catalog/scenes.ts` matches all six labels.
    1. `rg -n 'controls:\s*\[\]' web/src/catalog/scenes.ts` matches the three new scenes.
    1. `rg -n 'sealed C\+\+|bit-exact|live thumbnail' web/src/catalog/scenes.ts web/src/player/runtime.ts` is empty.
    1. Typecheck may still fail until Task 2 updates `previews.tsx` exhaustive switch — finish Task 2 before claiming plan done.
  </acceptance_criteria>
  <done>Catalog metadata and PAGE_SUMMARY advertise eleven ready scenes with pinned credits.</done>
</task>

<task type="auto">
  <name>Task 2: Static SVG previews for three material scenes</name>
  <files>web/src/catalog/previews.tsx</files>
  <read_first>
    1. `.planning/phases/27-material-flag-groups/27-UI-SPEC.md` Catalog Shell + Preview art direction
    1. `.planning/phases/27-material-flag-groups/27-CONTEXT.md` D-02, D-03
    1. `web/src/catalog/previews.tsx` Particles/Liquid Timer cases as pattern
    1. `web/src/components/DemoNavigation.tsx` caption `Static preview` (do not change caption text)
  </read_first>
  <action>
    Per D-02/D-03/UI-SPEC preview art direction: add three still inline SVG cases — Surface Tension (vertical-wall basin `#CBD5E1`, red/green circles `#F87171`/`#3DDC97`, blue box `#4DA3FF`, ball above), Elastic Particles (same basin family, soft circle marks + tilted blue box, ball), Rigid Particles (same basin family, firmer outlines without jelly squash cue, ball). Wire into `ScenePreview` switch cases `"surface-tension"`, `"elastic-particles"`, `"rigid-particles"`. Keep SVGs still (no CSS/SMIL animation). Do not use `&lt;img&gt;`, canvas, or WASM. Do not introduce `.catalog-card`. Leave DemoNavigation mapping unchanged. Confirm empty-controls hide from Phase 26 still present (`rg 'controls\.length' web/src/components/SceneControls.tsx`).
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'case "surface-tension"|case "elastic-particles"|case "rigid-particles"' web/src/catalog/previews.tsx` matches all three.
    1. `rg -n '#F87171|#3DDC97|#4DA3FF|#CBD5E1' web/src/catalog/previews.tsx` matches new preview tokens.
    1. `rg -n 'catalog-card' web/src` is empty (or only comments asserting absence — prefer empty).
    1. `rg -n 'controls\.length' web/src/components/SceneControls.tsx web/src/components/PlayerSceneChrome.tsx` still matches a hide gate.
    1. `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Eleven-scene TypeScript catalog compiles with three new static previews.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog metadata → visitor | Copy and links shape trust about what runs. |
| Inspiration hrefs → external GitHub | Allowed citation; implementation must stay host-locked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04-01 | S | Implementation credit links | mitigate | Keep `implementationPath` under `crates/liquidfun-wasm/src/scene/{surface_tension,elastic_particles,rigid_particles}.rs`; host-lock unchanged. |
| T-27-04-02 | I | Catalog honesty | mitigate | No sealed-parity / live-thumbnail copy; `Static preview` caption unchanged; ready only after WASM builds. |
| T-27-04-03 | T | Scene id allowlist | mitigate | Only append ids that WASM Plans 01–03 already parse. |
| T-27-04-04 | S | `.catalog-card` revival | mitigate | Do not introduce card-grid classes; shell e2e later asserts count 0. |
</threat_model>

<verification>
`cd web && bun run typecheck` passes; scenes.ts and previews cover eleven ids with credits and watch-first empty controls.
</verification>

<success_criteria>
1. Catalog lists eleven ready scenes with PLAY-03-style pinned credits and watch-first chrome for the three material scenes.
1. No card grid; PAGE_SUMMARY says eleven.
</success_criteria>

<output>
After completion, create `.planning/phases/27-material-flag-groups/27-04-SUMMARY.md`
</output>
