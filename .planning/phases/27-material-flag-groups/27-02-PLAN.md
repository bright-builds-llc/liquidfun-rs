---
phase: 27-material-flag-groups
plan: "02"
type: tdd
wave: 2
depends_on: ["01"]
files_modified:
  - crates/liquidfun-wasm/src/scene.rs
  - crates/liquidfun-wasm/src/scene/elastic_particles.rs
  - crates/liquidfun-wasm/src/session/tests.rs
autonomous: true
requirements: [MAT-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 27-2026-09-22T05-14-55
generated_at: 2026-09-22T05:30:00Z
must_haves:
  truths:
    - "SessionCore::create(SceneId::ElasticParticles) builds the shared vertical-wall basin, three soft SOLID groups (SPRING / ELASTIC / ELASTIC spinning box), and one dynamic ball."
    - "parse_scene_id(\"elastic-particles\") returns SceneId::ElasticParticles."
    - "The three groups keep distinct particle-flag recipes (SPRING vs ELASTIC); all use ParticleGroupFlags::SOLID; MAX_ADVANCE_STEPS remains 4."
  artifacts:
    - path: crates/liquidfun-wasm/src/scene/elastic_particles.rs
      provides: "Pinned Elastic Particles soft clumps + spinning box + ball"
      exports: ["build"]
    - path: crates/liquidfun-wasm/src/scene.rs
      provides: "SceneId::ElasticParticles allowlist entry and build_scene arm"
  key_links:
    - from: crates/liquidfun-wasm/src/scene.rs
      to: crates/liquidfun-wasm/src/scene/elastic_particles.rs
      via: "build_scene(SceneId::ElasticParticles) → elastic_particles::build"
      pattern: "SceneId::ElasticParticles => elastic_particles::build"
    - from: crates/liquidfun-wasm/src/scene/elastic_particles.rs
      to: liquidfun public API
      via: "with_group_flags(SOLID) + SPRING/ELASTIC particle flags + angular velocity"
      pattern: "ParticleGroupFlags::SOLID|with_angular_velocity\\(2"
---

<objective>
Port the pinned LiquidFun Elastic Particles test as a watch-first WASM scene: shared basin family, red spring-solid circle, green elastic-solid circle, blue elastic-solid spinning box, and falling ball.

Purpose: Deliver MAT-02 recognizable soft deformation contrast on the public API without collapsing into Jelly Drop's combined ELASTIC|SPRING recipe (D-08, D-13, D-14).
Output: `elastic_particles.rs`, factory wiring, and flag/construction tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT-SCOPE.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/27-material-flag-groups/27-CONTEXT.md
@.planning/phases/27-material-flag-groups/27-RESEARCH.md
@.planning/phases/27-material-flag-groups/27-01-SUMMARY.md
@crates/liquidfun-wasm/src/scene.rs
@crates/liquidfun-wasm/src/scene/basin_family.rs
@crates/liquidfun-wasm/src/scene/jelly_drop.rs
@crates/liquidfun-wasm/src/scene/surface_tension.rs
@third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testElasticParticles.js

<interfaces>
Pinned Elastic / Rigid shared extras (from `testElasticParticles.js`):

```text
Reuse basin_family vertical walls + ball from Plan 01.
ParticleSystemDef.radius = 0.035
Red circle: center (0,3) r=0.5, particle SPRING, group SOLID, color (255,0,0,255)
Green circle: center (-1,3) r=0.5, particle ELASTIC, group SOLID, color (0,255,0,255)
Blue box: oriented half-extents 1×0.5 at (1,4), angle -0.5, angularVelocity 2.0,
          particle ELASTIC, group SOLID, color (0,0,255,255)
```

Do NOT copy Jelly Drop's `ELASTIC | SPRING` combined particle flags onto all groups.
Do not add `rigid-particles` in this plan.
</interfaces>
</context>

<feature>
  <name>Elastic Particles WASM scene</name>
  <files>crates/liquidfun-wasm/src/scene/elastic_particles.rs, crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <behavior>
    - `parse_scene_id("elastic-particles")` → `Ok(SceneId::ElasticParticles)`
    - Construction succeeds; particle count &gt; 0; rigid circle ball present
    - At least one particle/group uses SPRING; at least one uses ELASTIC; groups carry SOLID
    - Blue box group uses transform angle ≈ -0.5 and angular velocity 2.0
    - Watch-first: unknown controls rejected; pointer no-op; advance safe
  </behavior>
  <implementation>
    Scene-only. Prefer existing `with_group_flags`, `with_transform`, `with_angular_velocity`. Strength knobs only if defaults look flat after correct flags (Claude's Discretion / research A1) — not an API gap.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing Elastic Particles allowlist and recipe tests</name>
  <files>crates/liquidfun-wasm/src/session/tests.rs, crates/liquidfun-wasm/src/scene/elastic_particles.rs</files>
  <read_first>
    1. `.planning/phases/27-material-flag-groups/27-CONTEXT.md` D-08, D-13, D-14
    1. `.planning/phases/27-material-flag-groups/27-RESEARCH.md` Capability Verdict Elastic row, Pitfall 1
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testElasticParticles.js`
    1. `crates/liquidfun-wasm/src/session/tests.rs` allowlist table after Plan 01
    1. `crates/liquidfun-wasm/src/scene/jelly_drop.rs` (anti-pattern: combined ELASTIC|SPRING)
    1. `crates/liquidfun-wasm/src/scene/surface_tension.rs` watch-first test pattern from Plan 01
  </read_first>
  <behavior>
    1. Test: `parse_scene_id("elastic-particles")` equals `Ok(SceneId::ElasticParticles)`.
    1. Test: `SessionCore::create(SceneId::ElasticParticles)` Ok with `particle_count() > 0` and ≥1 rigid circle.
    1. Test: constructed world contains SPRING particles and ELASTIC particles (distinct groups).
    1. Test: groups include `ParticleGroupFlags::SOLID`.
    1. Test: advance several steps does not panic; unknown control rejected.
  </behavior>
  <action>
    Per D-08: extend `parse_scene_id_maps_allowlisted_tokens` with `("elastic-particles", SceneId::ElasticParticles)`. Add `#[cfg(test)]` tests in `elastic_particles.rs` that construct via `SessionCore::create` and assert distinct SPRING vs ELASTIC particle flags plus SOLID group flags (do not assert combined ELASTIC|SPRING on every particle). Do not edit `web/**`. Do not add Rigid Particles yet. Do not change `MAX_ADVANCE_STEPS`.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture 2&gt;&amp;1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n '"elastic-particles"' crates/liquidfun-wasm/src/session/tests.rs` matches.
    1. `rg -n 'SceneId::ElasticParticles' crates/liquidfun-wasm/src/scene/elastic_particles.rs crates/liquidfun-wasm/src/session/tests.rs` matches test references.
    1. `rg -n 'SPRING|ELASTIC|SOLID' crates/liquidfun-wasm/src/scene/elastic_particles.rs` matches test asserts.
    1. Tests exist before production `elastic_particles::build` is complete (RED/compile-fail expected until Task 2).
  </acceptance_criteria>
  <done>Failing Elastic Particles allowlist/recipe coverage is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Elastic Particles scene and factory wiring</name>
  <files>crates/liquidfun-wasm/src/scene.rs, crates/liquidfun-wasm/src/scene/elastic_particles.rs, crates/liquidfun-wasm/src/session/tests.rs</files>
  <read_first>
    1. `crates/liquidfun-wasm/src/scene/basin_family.rs` from Plan 01
    1. `crates/liquidfun-wasm/src/scene/surface_tension.rs` watch-first hooks pattern
    1. `crates/liquidfun-wasm/src/scene/jelly_drop.rs` elastic API usage (do not copy combined flags)
    1. `third_party/liquidfun/liquidfun/Box2D/lfjs/testbed/tests/testElasticParticles.js`
    1. `crates/liquidfun-wasm/src/scene.rs` current SceneId match arms
  </read_first>
  <behavior>
    Same as Task 1; all listed tests must pass.
  </behavior>
  <action>
    Per D-08/D-13/D-14/D-15: add `mod elastic_particles;` and `SceneId::ElasticParticles`. Map `"elastic-particles"` in `parse_scene_id`. Call `elastic_particles::build` from `build_scene`. Implement build using `basin_family` (same vertical walls + ball), radius `0.035`, three groups: (1) red circle `(0,3)` r=`0.5` with `ParticleFlags::SPRING` + `ParticleGroupFlags::SOLID`; (2) green circle `(-1,3)` r=`0.5` with `ParticleFlags::ELASTIC` + `SOLID`; (3) blue box half-extents `(1, 0.5)` at `(1,4)` via `with_transform(Transform::from_position_angle(Vec2::new(1.0, 4.0), -0.5))` and `with_angular_velocity(2.0)` with `ELASTIC` + `SOLID`. Colors RGB as pinned. Reject unknown controls/actions; pointer no-op. Do NOT set `ELASTIC | SPRING` on every group. Do not cut counts or raise `MAX_ADVANCE_STEPS`. Do not add engine APIs. Do not touch `web/**`. Do not add Rigid Particles ids yet.
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm elastic_particles -- --nocapture &amp;&amp; cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens -- --nocapture &amp;&amp; rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'SceneId::ElasticParticles' crates/liquidfun-wasm/src/scene.rs` matches enum, parse, and build arms.
    1. `rg -n 'ParticleFlags::SPRING' crates/liquidfun-wasm/src/scene/elastic_particles.rs` matches the red group recipe.
    1. `rg -n 'ParticleFlags::ELASTIC' crates/liquidfun-wasm/src/scene/elastic_particles.rs` matches green and blue recipes.
    1. `rg -n 'ParticleGroupFlags::SOLID' crates/liquidfun-wasm/src/scene/elastic_particles.rs` matches group flags.
    1. `rg -n 'with_angular_velocity\(2' crates/liquidfun-wasm/src/scene/elastic_particles.rs` matches.
    1. `rg -n 'ELASTIC\s*\|\s*ParticleFlags::SPRING|SPRING\s*\|\s*ParticleFlags::ELASTIC' crates/liquidfun-wasm/src/scene/elastic_particles.rs` is empty (no Jelly-style combined particle flags).
    1. `rg -n 'rigid-particles|RigidParticles' crates/liquidfun-wasm/src/scene.rs crates/liquidfun-wasm/src/scene/elastic_particles.rs` is empty.
    1. `cargo test -p liquidfun-wasm elastic_particles` exits 0.
    1. `cargo test -p liquidfun-wasm parse_scene_id_maps_allowlisted_tokens` exits 0.
  </acceptance_criteria>
  <done>Elastic Particles is constructible with distinct SPRING/ELASTIC SOLID clumps and spinning blue box.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scene id string → WASM factory | Untrusted token must stay allowlisted. |
| Recipe flags → visitor-visible softness | Wrong flags spoof the material story. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | T | `parse_scene_id` | mitigate | Exact `"elastic-particles"` only; unknown still fails. |
| T-27-02-02 | I | Soft-material recipe | mitigate | Tests require distinct SPRING vs ELASTIC and SOLID; forbid Jelly combined flags. |
| T-27-02-03 | D | Catch-up cap | mitigate | Keep `MAX_ADVANCE_STEPS = 4`. |
| T-27-02-04 | E | Watch-first | mitigate | Unknown controls rejected; pointer no-op. |
</threat_model>

<verification>
`cargo test -p liquidfun-wasm elastic_particles` and allowlist parse tests exit 0; `MAX_ADVANCE_STEPS` stays 4.
</verification>

<success_criteria>
1. MAT-02 native scene constructs with three soft SOLID clumps and falling ball.
1. Spring vs elastic contrast preserved in recipes.
1. No catalog advertising yet.
</success_criteria>

<output>
After completion, create `.planning/phases/27-material-flag-groups/27-02-SUMMARY.md`
</output>
