# Phase 27: Material flag groups - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-22T05:15:07.461Z
**Phase:** 27-Material flag groups
**Mode:** Yolo
**Areas discussed:** Catalog shell, Watch-first controls, Recognizable flag-group layouts, Pinned-test credits, Engine additions, Shared session and proof

---

## Catalog shell

| Option | Description | Selected |
|--------|-------------|----------|
| Append three ready entries after the current eight | Keep existing order; hash routes for `surface-tension`, `elastic-particles`, and `rigid-particles`; static SVG previews captioned `Static preview` | ✓ |
| New material-showcase catalog page | Splits the player from the Phase 20 shell | |
| Replace Color Mixer or Jelly Drop with these ports | Drops original scenes this milestone must keep | |

**User's choice:** Yolo recommended default — append the three scenes to the existing `DemoNavigation` shell.
**Notes:** Carries Phase 26 D-01 through D-03. No `.catalog-card` grid.

---

## Watch-first controls

| Option | Description | Selected |
|--------|-------------|----------|
| Play, pause, and Reset only | Matches MAT success criteria; no material picker | ✓ |
| Stiffness and color controls | Extra chrome the recognition cards mark as optional | |
| Full particle-type picker | PRESET-01; out of this phase | |

**User's choice:** Yolo recommended default — watch-first play, pause, and Reset.
**Notes:** Reset recreates the documented initial layout. No pointer or keyboard material modes.

---

## Recognizable flag-group layouts

| Option | Description | Selected |
|--------|-------------|----------|
| Pinned-test layouts: tensile color groups, spring/elastic clumps, rigid solids, each with a falling ball | Matches FEATURES.md must-see cards | ✓ |
| One shared demo with a flag toggle | Collapses three catalog scenes into one and hides the contrast | |
| Reduced particle counts for smoothness | Forbidden by the phase and PROJECT.md | |

**User's choice:** Yolo recommended default — three recognizable ports of the pinned tests.
**Notes:** Surface Tension beads and bleeds color. Elastic clumps deform. Rigid clumps stay solid. Exact stiffness and bit-exact solver parity stay chrome.

---

## Pinned-test credits

| Option | Description | Selected |
|--------|-------------|----------|
| Phase 18 credit chrome plus pinned JS and C++ inspiration links | Host-locked implementation path; pin `7f20402173fd143a3988c921bc384459c6a858f2` | ✓ |
| Link implementation to `google/liquidfun` | Misstates what is running | |
| Omit inspiration links | Breaks the existing credit pattern | |

**User's choice:** Yolo recommended default — cite `testSurfaceTension.js` / `ParticlesSurfaceTension.h`, `testElasticParticles.js` / `ElasticParticles.h`, and `testRigidParticles.js` / `RigidParticles.h`.
**Notes:** Copy stays an experimental recognizable port. No sealed parity claim.

---

## Engine additions

| Option | Description | Selected |
|--------|-------------|----------|
| Scene modules on the public API; engine work only if a scene cannot show the required contrast | Color Mixer and Jelly Drop already cover related flags | ✓ |
| New public material API before the scenes | Extra surface the research stack does not require | |
| Import desktop testbed recipes | Couples the gallery to diagnostic tooling | |

**User's choice:** Yolo recommended default — reuse `TENSILE`, `COLOR_MIXING`, `ELASTIC`, `SPRING`, `SOLID`, and `RIGID`.
**Notes:** Do not cut particle counts or raise `MAX_ADVANCE_STEPS` above 4.

---

## Shared session and proof

| Option | Description | Selected |
|--------|-------------|----------|
| Extend the checked scene factory and Chromium `just web-player-smoke` | Three new scenes plus the current eight still open and run | ✓ |
| Firefox, Safari, and a live Pages redeploy as the phase gate | Broader than the hobby verification policy | |
| Headless unit tests only | Misses the visitor play, pause, and reset criteria | |

**User's choice:** Yolo recommended default — one WASM session and an extended Chromium smoke.
**Notes:** Independent AI review stays eligible. The implementing agent must not approve its own work.

---

## Claude's Discretion

- Exact coordinates, radius, group sizes, box angle, and particle counts, as long as the ports stay recognizable and counts are not reduced for smoothness.
- Static SVG preview artwork captioned `Static preview`.
- A private shared basin helper inside `liquidfun-wasm`.
- A narrow engine addition only when a listed contrast cannot be shown otherwise.
- File split when a scene module approaches the file-length trigger.

## Deferred Ideas

- Soup, Soup Stirrer, Impulse, Wave Machine, and Theo Jansen — Phase 28.
- Sparky, Drawing Particles, and PLAY-01 — Phase 29.
- PRESET-01, DRAW-02, PARITY-01, BOX2D-01.
- Optional pointer nudge and cross-links between related scenes.
