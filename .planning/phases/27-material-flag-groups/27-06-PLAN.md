---
phase: 27-material-flag-groups
plan: "06"
type: execute
wave: 6
depends_on: ["04", "05"]
files_modified:
  - web/e2e/player-helpers.ts
  - web/e2e/player.spec.ts
  - web/e2e/shell.spec.ts
  - web/e2e/reset-honesty.spec.ts
autonomous: true
requirements: [MAT-01, MAT-02, MAT-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 27-2026-09-22T05-14-55
generated_at: 2026-09-22T05:30:00Z
must_haves:
  truths:
    - "Chromium smoke opens Surface Tension, Elastic Particles, and Rigid Particles, plays, pauses, and resets each without requiring pointer gestures or labeled scene controls."
    - "Desktop sidebar shows eleven Static preview captions and eleven aria-hidden SVGs; .catalog-card count stays 0; drawer reverse-tab last link is Rigid Particles."
    - "Current eight scenes still open and run (interactive six keep gesture+control coverage; Particles and Liquid Timer stay on the watch-first path)."
  artifacts:
    - path: web/e2e/player.spec.ts
      provides: "Watch-first play/pause/reset covering five watch-first scenes"
    - path: web/e2e/player-helpers.ts
      provides: "Eleven SCENE_HASH_PATHS + raised all-scene timeout"
    - path: web/e2e/shell.spec.ts
      provides: "Eleven static preview desktop asserts + Rigid Particles last focus"
  key_links:
    - from: web/e2e/player.spec.ts
      to: web/src/catalog/scenes.ts
      via: "controls.length === 0 watch-first branch"
      pattern: "controls\\.length"
    - from: just web-player-smoke
      to: web/e2e/*
      via: "bun scripts/web-build.ts player-smoke"
      pattern: "web-player-smoke"
---

<objective>
Extend Chromium `just web-player-smoke` so eleven scenes prove open/play/pause/reset while watch-first material scenes skip pointer/control requirements, and the prior eight scenes still run.

Purpose: Close MAT-01/02/03 browser proof under D-16 without Firefox/Safari, Linux qualification, or Pages redeploy. Independent AI review remains eligible (D-17); implementing agent must not self-approve.
Output: Updated Playwright helpers/specs and a green player-smoke gate.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/verification.md
@.planning/phases/27-material-flag-groups/27-CONTEXT.md
@.planning/phases/27-material-flag-groups/27-RESEARCH.md
@.planning/phases/27-material-flag-groups/27-UI-SPEC.md
@.planning/phases/27-material-flag-groups/27-04-SUMMARY.md
@.planning/phases/27-material-flag-groups/27-05-SUMMARY.md
@web/e2e/player.spec.ts
@web/e2e/player-helpers.ts
@web/e2e/shell.spec.ts

<interfaces>
Required helper updates:

```ts
export const SCENE_HASH_PATHS: Readonly<Record<SceneId, string>> = {
  // existing eight…
  "surface-tension": "/liquidfun-rs/#/scene/surface-tension",
  "elastic-particles": "/liquidfun-rs/#/scene/elastic-particles",
  "rigid-particles": "/liquidfun-rs/#/scene/rigid-particles",
};

// Raise ALL_SCENE_TIMEOUT_MS for eleven open/reset loops (start at 220_000 unless local smoke proves 160_000 still passes).
```

Watch-first path already selects by `scene.controls.length === 0` (Phase 26) — the three new scenes reuse that rule automatically once `controls: []`. For those scenes assert:
1. `expectReadySceneChrome(page, scene.title)`
2. step index advances while Playing
3. Pause → status `Paused` and step frozen
4. Play → `Playing` and step advances
5. `resetNearZero(page)`
6. Credits visible (`Scene source` / `View scene source`)
7. Do NOT call `performSceneGesture` or `activateLabeledControl`

Interactive six: keep existing gesture+control loop via filter `controls.length > 0`.

Shell:
- Desktop sidebar: **eleven** `Static preview` texts and **eleven** `svg[aria-hidden='true']` inside `.demo-sidebar`.
- Keep `.catalog-card` `toHaveCount(0)`.
- Drawer reverse-tab last link: change from Liquid Timer to **Rigid Particles**.
- Focus wrap: Dismiss + 11 scene links = 12 focusables; adjust reverse-tab modulus comments/math from Phase 26's 9-focusable wrap (was Dismiss+8).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend helpers and keep watch-first player e2e</name>
  <files>web/e2e/player-helpers.ts, web/e2e/player.spec.ts, web/e2e/reset-honesty.spec.ts</files>
  <read_first>
    1. `.planning/phases/27-material-flag-groups/27-CONTEXT.md` D-04, D-05, D-06, D-16
    1. `.planning/phases/27-material-flag-groups/27-RESEARCH.md` Pitfall 3, Pitfall 4, Shared integration points
    1. `web/e2e/player.spec.ts` watch-first vs interactive split
    1. `web/e2e/player-helpers.ts` `SCENE_HASH_PATHS`, `ALL_SCENE_TIMEOUT_MS`
    1. `web/e2e/reset-honesty.spec.ts` timeout imports
  </read_first>
  <action>
    Per D-16/D-04/D-05/D-06: add `surface-tension`, `elastic-particles`, and `rigid-particles` to `SCENE_HASH_PATHS`. Raise `ALL_SCENE_TIMEOUT_MS` enough for eleven open/reset loops (start at `220_000` unless smoke proves lower still passes). Keep the `controls.length === 0` watch-first branch so the three new scenes are covered without gestures. Keep the interactive six-scene gesture+control test. Ensure “opens each native scene…” still iterates all `SCENES` (now eleven) under the raised timeout. Do not add Firefox/WebKit projects. Do not deploy Pages. Do not cut particle counts or raise `MAX_ADVANCE_STEPS`.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'surface-tension|elastic-particles|rigid-particles' web/e2e/player-helpers.ts` matches all three hash paths.
    1. `rg -n 'controls\.length === 0|controls\.length > 0' web/e2e/player.spec.ts` matches the watch-first split.
    1. `rg -n 'ALL_SCENE_TIMEOUT_MS\s*=\s*[0-9_]+' web/e2e/player-helpers.ts` shows a timeout ≥ 160_000 (prefer 220_000).
    1. `cd web && bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Player e2e compiles with eleven hash paths and watch-first play/pause/reset covering material scenes.</done>
</task>

<task type="auto">
  <name>Task 2: Eleven-preview shell asserts and run web-player-smoke</name>
  <files>web/e2e/shell.spec.ts</files>
  <read_first>
    1. `.planning/phases/27-material-flag-groups/27-UI-SPEC.md` Narrow-Viewport Acceptance + Focused Browser Evidence
    1. `.planning/phases/27-material-flag-groups/27-CONTEXT.md` D-03, D-16, D-17
    1. `web/e2e/shell.spec.ts` eight-preview asserts and Liquid Timer last-link reverse-tab
    1. `justfile` `web-player-smoke` recipe
  </read_first>
  <action>
    Per D-03/D-16/UI-SPEC: update desktop sidebar asserts to expect **eleven** exact `Static preview` texts and **eleven** `svg[aria-hidden='true']` inside `.demo-sidebar`. Keep `.catalog-card` count `0`. Change drawer reverse-tab last link from Liquid Timer to **Rigid Particles**. Update focus-wrap comments/math: Dismiss + 11 links = 12 focusables (adjust reverse-tab count so last link is Rigid Particles). Keep mobile dialog assert that at least one preview + caption is visible. Then run the phase gate `just web-player-smoke` (or `bun scripts/web-build.ts player-smoke` if just unavailable) and fix only Phase-27-related failures (timeouts, missing paths, wrap math). Do not cut particle counts or raise `MAX_ADVANCE_STEPS` to make smoke smoother. Do not add screenshot-hash oracles. Do not schedule a live Pages redeploy. Do not self-approve the phase (D-17 — independent AI review remains eligible later).
  </action>
  <verify>
    <automated>just web-player-smoke</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n 'toHaveCount\(11\)' web/e2e/shell.spec.ts` matches Static preview and svg counts (or equivalent eleven asserts).
    1. `rg -n 'toHaveCount\(8\)' web/e2e/shell.spec.ts` is empty for preview counts.
    1. `rg -n 'Rigid Particles' web/e2e/shell.spec.ts` matches the reverse-tab last-link assert.
    1. `rg -n 'catalog-card' web/e2e/shell.spec.ts` still asserts `toHaveCount(0)`.
    1. `just web-player-smoke` exits 0.
    1. `rg -n 'MAX_ADVANCE_STEPS: u32 = 4' crates/liquidfun-wasm/src/session.rs` still matches.
  </acceptance_criteria>
  <done>Chromium smoke proves eleven-scene catalog chrome and watch-first material play/pause/reset while eight prior scenes still run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hash route → WASM session | Allowlisted SceneId only. |
| Playwright → local static server | No production deploy; no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06-01 | T | Hash scene id | mitigate | Unknown hashes still hit fallback; smoke keeps unknown-scene regression. |
| T-27-06-02 | D | Catch-up under hidden tab | mitigate | Existing max-four proof remains; do not raise cap. |
| T-27-06-03 | I | Preview honesty | mitigate | Shell asserts eleven `Static preview` captions; no live thumbnail paths. |
| T-27-06-04 | E | Watch-first e2e | mitigate | Scenes with empty controls never require pointer mutation to pass. |
| T-27-06-05 | S | Deploy-as-gate | accept | No Pages redeploy in this phase (D-16); local Chromium smoke is the gate — low spoof risk because release/deploy is out of scope. |
</threat_model>

<verification>
`just web-player-smoke` exits 0. Independent AI review (D-17) is eligibility only — the implementing agent must not self-approve the phase.
</verification>

<success_criteria>
1. MAT-01/02/03 proven in Chromium for open/play/pause/reset.
1. Eleven static previews; `.catalog-card` count 0; last drawer link Rigid Particles.
1. Prior eight scenes still open and run; `MAX_ADVANCE_STEPS` stays 4.
</success_criteria>

<output>
After completion, create `.planning/phases/27-material-flag-groups/27-06-SUMMARY.md`
</output>
