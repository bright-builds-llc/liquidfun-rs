---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-07-12T23-36-17
generated_at: 2026-07-13T00:09:12.340Z
phase: 07-rigid-solver-world-operations-and-ccd
plan: "03"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/src/world/island.rs
  - crates/liquidfun/tests/rigid_island_order.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - Awake active non-static seeds build private DFS islands in pinned newest-first body/contact order with LIFO traversal.
    - Static bodies terminate propagation and are reusable across later islands.
    - Island capacity and topology preflight fail before solver or persistent state mutation.
  artifacts:
    - crates/liquidfun/src/world/island.rs
    - crates/liquidfun/tests/rigid_island_order.rs
  key_links:
    - World maintains an explicit newest-first body lane independent of Arena ascending-slot iteration.
    - Contact adjacency ordinals resolve to manager occurrences without public durable contact identity.
---

# Plan 07-03: Source-Ordered Island Construction

<objective>Build source-faithful ephemeral rigid islands and their bounded scratch representation. Purpose: retire the main ordering risk before generalized constraint solving. Output: body-order lane, DFS builder, capacity errors, and order tests.</objective>
<decision_coverage>D-06 and the preflight portion of D-08 are fully implemented; a zero-capacity joint lane is reserved for Phase 8.</decision_coverage>

<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| Persistent graph to island scratch | Corrupt adjacency or unbounded traversal could duplicate/omit bodies and contacts. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T07-03-01 | Tampering | Solver-visible order | mitigate | Explicit newest-first lanes and exact sequence tests; no hash iteration or sorting. |
| T07-03-02 | Denial of service | DFS stack/island buffers | mitigate | Preflight reviewed bounds and checked allocation before flags/state changes. |
| T07-03-03 | Information disclosure | Contact identity | mitigate | Resolve private ordinals internally; expose only semantic snapshots. |
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add explicit source-order lanes and invariant checks</name>
  <files>crates/liquidfun/src/world/object.rs, crates/liquidfun/tests/rigid_island_order.rs</files>
  <read_first>
    - crates/liquidfun/src/arena.rs
    - crates/liquidfun/src/world/object.rs
    - crates/liquidfun/src/world/contact_manager.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
  </read_first>
  <action>Add a private newest-first body order lane updated transactionally on create/destroy and invariant helpers resolving body/contact adjacency. Do not change public handles or Arena iteration. Add model tests for create/destroy/reuse order and prove the lane never depends on slot reuse or HashMap iteration.</action>
  <verify><automated>cargo test -p liquidfun --test rigid_island_order body_order --all-features</automated></verify>
  <acceptance_criteria>The command exits 0 for creation, middle/head/tail destruction, slot reuse, and cross-world scenarios; body order matches pinned list semantics.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build bounded DFS islands without persistent flags</name>
  <files>crates/liquidfun/src/world.rs, crates/liquidfun/src/world/island.rs, crates/liquidfun/tests/rigid_island_order.rs</files>
  <read_first>
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Island.h
    - crates/liquidfun/src/world/contact_manager.rs
    - crates/liquidfun/src/world/contact.rs
  </read_first>
  <action>Implement private bounded island scratch with body IDs, manager contact occurrence indices, position/velocity lanes, visitation bitsets, and reserved empty joint lane. Seed and traverse exactly per D-06; skip asleep/inactive/static seeds, wake popped bodies in candidate state, skip disabled/non-touching/sensor contacts, push newest-first adjacency onto a LIFO stack, stop at statics, and clear static visitation after each island. Preflight all capacities and adjacency resolution before returning islands.</action>
  <verify><automated>cargo test -p liquidfun --test rigid_island_order dfs --all-features</automated></verify>
  <acceptance_criteria>Exact island/body/contact order witnesses, disconnected islands, shared statics, sensor/disabled skips, and N/N+1 capacity failures pass with no persistent mutation.</acceptance_criteria>
</task>
</tasks>

<verification>Run the complete island-order target and scan island.rs for sort/hash iteration or persistent incremental island storage.</verification>
<success_criteria>The solver receives deterministic source-ordered ephemeral islands with explicit bounded failure behavior.</success_criteria>
<output>After completion, create `.planning/phases/07-rigid-solver-world-operations-and-ccd/07-03-SUMMARY.md`.</output>
