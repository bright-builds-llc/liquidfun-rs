---
phase: 07-rigid-solver-world-operations-and-ccd
plan: "12"
type: execute
wave: 9
depends_on:
  - "07-11"
files_modified:
  - crates/liquidfun-differential/src/rigid_fixtures.rs
  - crates/liquidfun-differential/tests/rigid_fixture_workflow.rs
  - tools/xtask/src/differential.rs
  - protocol/fixtures/accepted/rigid-world-request.jsonl
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-07-12T23-36-17
generated_at: 2026-07-13T00:09:12.340Z
must_haves:
  truths:
    - The checked-in rigid-world request contains only the locked deterministic bounded Phase 7 witness families.
    - Local compare, replay, minimization, and sanitizer runs record D2 evidence with complete reproducible provenance and first-divergence artifacts.
    - D2 cannot stage, review, promote, write, or create a canonical D1 artifact; only the configured canonical lane can exercise D1 authority.
  artifacts:
    - path: protocol/fixtures/accepted/rigid-world-request.jsonl
      provides: Locked deterministic bounded Phase 7 witness corpus
    - path: crates/liquidfun-differential/src/rigid_fixtures.rs
      provides: Replay, minimization, provenance, and authority-checked fixture lifecycle
    - path: crates/liquidfun-differential/tests/rigid_fixture_workflow.rs
      provides: No-write D2 rejection coverage for D1 stage, review, and promotion
  key_links:
    - from: protocol/fixtures/accepted/rigid-world-request.jsonl
      to: tools/xtask/src/differential.rs
      via: Closed rigid-world compare, replay, determinism, and minimization commands
    - from: tools/xtask/src/differential.rs
      to: crates/liquidfun-differential/src/rigid_fixtures.rs
      via: Evidence tier and checkout/build identity checked before every fixture write
---

# Plan 12: Deterministic Phase 7 Evidence Workflow and D2 Governance

## Objective

Run the locked Phase 7 witness corpus through the existing evidence lifecycle while proving that local D2 results cannot acquire canonical D1 authority.

## Decisions Covered

- D-16: the deterministic corpus records semantic anti-tunneling, completion, contact, impulse, and barrier-side outcomes.
- D-22: Phase 7 extends the existing rigid-world fixture and xtask path rather than creating another harness.
- D-24: local runs record D2 evidence only; D2 cannot stage, review, promote, or create canonical D1 artifacts.

<threat_model>
- A mutable or generated corpus outside the reviewed request could make replay results non-reproducible.
- Missing build, policy, or checkout identity could let incompatible evidence enter a candidate fixture.
- A local D2 result could be mislabeled or written into canonical D1 staging if authority is checked after filesystem mutation.
- C++ crashes, sanitizer findings, malformed output, or timeouts could be mistaken for physics mismatches or passing evidence.
</threat_model>

<tasks>

<task id="07-12-01" type="implementation">
  <name>Wire the locked deterministic Phase 7 corpus into compare and replay</name>
  <files>crates/liquidfun-differential/src/rigid_fixtures.rs, tools/xtask/src/differential.rs, protocol/fixtures/accepted/rigid-world-request.jsonl</files>
  <read_first>
    - protocol/fixtures/accepted/rigid-world-request.jsonl
    - crates/liquidfun-differential/src/rigid_fixtures.rs
    - crates/liquidfun-differential/src/rigid_evidence.rs
    - tools/xtask/src/differential.rs
    - TESTING.md
  </read_first>
  <action>
    Per D-16, D-22, and D-24, extend only the checked-in deterministic rigid-world request with the Phase 7 force/configuration, multi-contact island, sleep/wake, bullet/CCD, query/ray, and origin-shift witness families defined by Plan 07-10. Keep the existing action and harness bounds. Route compare, replay, determinism, and same-signature minimization through the existing xtask commands and record upstream revision, protocol/schema/policy versions, compiler/build and adapter identities, scenario ID, timestep, iterations, continuous budget, exact float bits, stderr, and first divergence. Classify local debug, replay, and sanitizer results as D2; keep D0 limited to exactly two byte-identical same-build runs.
  </action>
  <verify>
    - cargo xtask differential compare --scenario rigid-world --preset oracle-debug --session-profile one-shot
    - cargo xtask differential replay --scenario rigid-world --preset oracle-debug --session-profile one-shot
    - cargo xtask differential verify-determinism --scenario rigid-world --preset oracle-debug --runs 2
    - UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 cargo xtask differential compare --scenario rigid-world --preset oracle-asan-ubsan --session-profile one-shot
  </verify>
  <acceptance_criteria>
    - Every locked witness executes against both engines from one bounded checked-in request and produces reproducible provenance-complete results.
    - Replay reproduces exact inputs and minimization retains the same first-divergence classification.
    - Local debug/replay/sanitizer results are labeled D2 and determinism is labeled D0 only after two byte-identical same-build runs.
  </acceptance_criteria>
</task>

<task id="07-12-02" type="test">
  <name>Prove D2 cannot mutate canonical D1 evidence state</name>
  <files>crates/liquidfun-differential/tests/rigid_fixture_workflow.rs, tools/xtask/src/differential.rs</files>
  <read_first>
    - crates/liquidfun-differential/tests/rigid_fixture_workflow.rs
    - crates/liquidfun-differential/src/rigid_fixtures.rs
    - tools/xtask/src/differential.rs
    - TESTING.md
    - .planning/STATE.md
  </read_first>
  <action>
    Add focused authority tests that present otherwise valid local D2 Phase 7 comparison evidence to every fixture mutation boundary. Require stage, review, and promote to recompute the current checkout, adapter, effective compile-command, policy, request, and evidence-tier identity before their first write and reject D2 with byte-identical candidate, review, promoted, and manifest directories. Keep canonical D1 construction confined to the configured canonical test/lane identity; no local command may synthesize or override it. Preserve harness failures as a separate classification from physics mismatches.
  </action>
  <verify>
    - cargo test -p liquidfun-differential --test rigid_fixture_workflow --all-features
    - cargo test -p xtask --test differential_cli --all-features
  </verify>
  <acceptance_criteria>
    - Tests prove D2 stage, review, and promote attempts fail before every filesystem write.
    - Canonical D1 identity cannot be supplied by an unrestricted local flag, environment override, or edited result record.
    - Harness failures, physics mismatches, D0 determinism, D2 local results, and canonical D1 authority remain distinct closed classifications.
  </acceptance_criteria>
</task>

</tasks>

## Verification

- `cargo xtask differential compare --scenario rigid-world --preset oracle-debug --session-profile one-shot`
- `cargo xtask differential replay --scenario rigid-world --preset oracle-debug --session-profile one-shot`
- `cargo test -p liquidfun-differential --test rigid_fixture_workflow --all-features`
- `cargo test -p xtask --test differential_cli --all-features`

## Success Criteria

- The deterministic Phase 7 corpus is reproducible, replayable, minimizable, and provenance-complete.
- Local results remain D2 and cannot mutate or create canonical D1 artifacts.
- Evidence failures retain exact harness-versus-physics classification and first-divergence context.

## Must Haves

- No corpus generation beyond the locked deterministic request.
- No D1 write before canonical authority and identity validation.
- No D2 stage, review, promotion, or canonical artifact creation.

