---
phase: 07-rigid-solver-world-operations-and-ccd
plan: "11"
type: execute
wave: 8
depends_on:
  - "07-05"
  - "07-07"
  - "07-09"
  - "07-10"
files_modified:
  - crates/liquidfun-differential/src/rigid_world.rs
  - crates/liquidfun-differential/src/rigid_evidence.rs
  - crates/liquidfun-differential/src/comparator.rs
  - crates/liquidfun-differential/src/minimizer.rs
  - crates/liquidfun-differential/tests/rigid_world.rs
  - tools/reference/src/rigid_world.hpp
  - tools/reference/src/rigid_world.cpp
  - tools/reference/src/oracle_adapter.cpp
autonomous: true
requirements: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-07-12T23-36-17
generated_at: 2026-07-13T00:09:12.340Z
must_haves:
  truths:
    - Rust and pinned C++ execute the same closed deterministic Phase 7 witness request and emit the same bounded semantic shape.
    - First divergence retains action, stage, entity, field, exact values, policy, and completion classification through replay and minimization.
    - Adapter and comparator output retains enough bounded semantic detail for the next plan's D2-only replay and governance checks.
  artifacts:
    - path: crates/liquidfun-differential/src/rigid_world.rs
      provides: Native execution of the extended rigid-world request
    - path: tools/reference/src/rigid_world.cpp
      provides: Pinned C++ execution of the same extended request
    - path: crates/liquidfun-differential/src/comparator.rs
      provides: Closed exact and field-policy Phase 7 comparison
  key_links:
    - from: crates/liquidfun-differential/src/rigid_world.rs
      to: tools/reference/src/rigid_world.cpp
      via: Identical validated action ordering and semantic result shape
---

# Plan 11: Phase 7 Differential Adapters and Evidence Workflows

## Objective

Execute the same bounded Phase 7 scenarios in native Rust and the pinned C++ oracle, compare semantic outcomes under the closed policy, and preserve replay/minimization/promotion controls.

## Decisions Covered

- D-16: differential fixtures include explicit anti-tunneling and continuous-progress witnesses.
- D-19: comparator-only canonicalization preserves query multiplicity, status traces, and ray tie sets.
- D-21: query/ray/origin-shift evidence covers the locked cases.
- D-22: Phase 7 extends the current rigid protocol, adapters, and evidence workflow.
- D-23: structural comparison is exact and numeric comparison is closed per observable.
- D-24: local runs may record D2 evidence only; D2 cannot stage, review, promote, or create canonical D1 artifacts.

<threat_model>
- Adapter asymmetry could make Rust and C++ execute different scenarios while producing superficially comparable records.
- C++ crashes, stderr diagnostics, malformed output, or timeouts could be mistaken for physics mismatches or passing evidence.
- Comparator normalization could erase duplicate callbacks, ordering guarantees, completion status, or first-divergence context.
- Automatic promotion could turn one local run into an unsupported broad parity claim.
</threat_model>

<tasks>

<task id="07-11-01" type="implementation">
  <name>Implement symmetric Rust and C++ Phase 7 adapters</name>
  <files>crates/liquidfun-differential/src/rigid_world.rs, tools/reference/src/rigid_world.hpp, tools/reference/src/rigid_world.cpp, tools/reference/src/oracle_adapter.cpp</files>
  <read_first>
    - crates/liquidfun-differential/src/rigid_world.rs
    - crates/liquidfun-differential/src/rigid_evidence.rs
    - tools/reference/src/rigid_world.hpp
    - tools/reference/src/rigid_world.cpp
    - tools/reference/src/rigid_world_decode.hpp
    - tools/reference/src/oracle_adapter.cpp
    - crates/liquidfun-test-protocol/src/scenario/rigid_world.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
  </read_first>
  <action>
    Extend both adapters from the same Phase 7 protocol types and bounds. Execute granular body controls, force/impulse wake policy, configured stepping, sleep/wake transitions, CCD/sub-step/budget cases, streaming query/ray directives, and origin shifts. Emit stable scenario/body/fixture-child/contact identities, ordered traces only where the contract guarantees order, completion and partial-progress status, semantic body/contact state, and query/ray results; never emit pointers or raw memory. Keep stdout exclusively JSON Lines and treat malformed input, over-limit work, invalid handles, exceptions, crashes, timeouts, nonzero exit, or unexpected stdout as harness errors distinct from physics mismatches. Compile C++ behavior from the pinned oracle rather than reimplementing substantial physics inside the adapter.
  </action>
  <verify>
    - cargo test -p liquidfun-differential --test rigid_world --all-features
    - cargo xtask upstream configure --preset oracle-debug
    - cargo xtask upstream build --preset oracle-debug
  </verify>
  <acceptance_criteria>
    - Rust and C++ adapters accept and reject the same bounded Phase 7 contract.
    - Harness failures are never reported as physics mismatches or passing evidence.
    - Adapter output contains semantic stable identities and no foreign addresses or storage layout.
  </acceptance_criteria>
</task>

<task id="07-11-02" type="implementation">
  <name>Compare Phase 7 evidence without erasing first divergence</name>
  <files>crates/liquidfun-differential/src/rigid_evidence.rs, crates/liquidfun-differential/src/comparator.rs, crates/liquidfun-differential/src/minimizer.rs, crates/liquidfun-differential/tests/rigid_world.rs</files>
  <read_first>
    - crates/liquidfun-differential/src/comparator.rs
    - crates/liquidfun-differential/src/rigid_evidence.rs
    - crates/liquidfun-differential/src/minimizer.rs
    - protocol/tolerances/phase7-v1.toml
  </read_first>
  <action>
    Dispatch every Phase 7 field through its registered exact or numeric policy and fail closed on unknown paths. Compare ordered solver/state/status traces in declared order, query occurrences as multiplicity-preserving multisets, and equal-minimum ray identities as sets while separately comparing fraction/normal/point fields. Report the first divergent action, stage, entity, field, bit/decimal values, applicable policy, and surrounding completion status. Extend replay and minimization so a failing seed keeps the operation that first diverges, required setup dependencies, callback directives, budget, and exact float bits; reject any minimization that changes the failure classification.
  </action>
  <verify>
    - cargo test -p liquidfun-differential --test rigid_world comparator --all-features
    - cargo test -p liquidfun-differential --test rigid_world minimization --all-features
  </verify>
  <acceptance_criteria>
    - Every compared field uses a registered policy and unknown observables fail closed.
    - Canonicalization retains duplicate counts, declared ordering, directives, and completion status.
    - Replay and minimization reproduce the same first-divergence classification from persisted exact inputs.
  </acceptance_criteria>
</task>

</tasks>

## Verification

- `cargo test -p liquidfun-differential --test rigid_world --all-features`

## Success Criteria

- Both engines execute one bounded closed Phase 7 scenario contract.
- Comparator reports exact semantic first divergences under observable-specific policies.
- Evidence remains reproducible, minimizable, provenance-complete, and governed by D0-D3 controls.

## Must Haves

- C++ remains a private read-only oracle, never a runtime dependency.
- Harness errors remain distinct from physics mismatches.
- No automatic D3 promotion or broad parity statement.
