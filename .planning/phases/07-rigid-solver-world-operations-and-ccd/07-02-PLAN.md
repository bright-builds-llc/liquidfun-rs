---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-07-12T23-36-17
generated_at: 2026-07-13T00:09:12.340Z
phase: 07-rigid-solver-world-operations-and-ccd
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/world/config.rs
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/src/world/step.rs
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/tests/rigid_world_config.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - World owns checked gravity, warm-starting, continuous-physics, sub-stepping, automatic-force-clearing, and prior inverse-timestep state.
    - StepConfiguration rejects invalid timestep and iteration inputs before contact or solver effects.
    - Automatic force clearing defaults on and explicit clearing supports application-managed multi-substep loops.
  artifacts:
    - crates/liquidfun/src/world/config.rs
    - crates/liquidfun/tests/rigid_world_config.rs
  key_links:
    - World::step consumes an invariant-bearing configuration and computes dt_ratio from retained inv_dt0.
    - Force-clearing policy reaches private BodyState accumulators without exposing them.
---

# Plan 07-02: Checked World and Step Configuration

<objective>Replace the fixed Phase 6 step tuple with checked world configuration and timestep/iteration inputs. Purpose: establish RIGD-08 invariants before islands and CCD. Output: configuration types, World controls, force clearing, and tests.</objective>
<decision_coverage>D-05 and D-07 are fully implemented; D-14 report status is defined for later CCD wiring.</decision_coverage>

<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| Step input to simulation | Timestep and iteration counts can amplify resource use or poison numeric state. |
| World flags to lifecycle | Clearing and continuation flags alter cross-call state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T07-02-01 | Denial of service | Iteration counts | mitigate | Parse into reviewed finite maxima before locking or effects. |
| T07-02-02 | Tampering | inv_dt0/dt_ratio | mitigate | Update retained inverse timestep only after successful positive-duration step commit. |
| T07-02-03 | Repudiation | Step completion | mitigate | Closed Complete/ContinuousPending status in owned StepReport. |
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define checked world and step configuration</name>
  <files>crates/liquidfun/src/world/config.rs, crates/liquidfun/src/world.rs, crates/liquidfun/src/lib.rs, crates/liquidfun/tests/rigid_world_config.rs</files>
  <read_first>
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2TimeStep.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
    - crates/liquidfun/src/world/step.rs
    - crates/liquidfun/src/math/settings.rs
  </read_first>
  <action>Per D-05 and D-07, create checked StepConfiguration with finite nonnegative timestep and reviewed positive velocity/position iteration bounds, plus closed StepCompletion. Add checked World gravity and boolean configuration accessors with upstream defaults: warm starting and continuous physics true, sub-stepping false, auto-clear true. Keep World::new backward compatible with zero gravity and add checked set/get gravity. Preserve inv_dt0 across zero-duration calls and define the exact dt_ratio expression. Write boundary N/N+1, zero-step, non-finite, and default tests first.</action>
  <verify><automated>cargo test -p liquidfun --test rigid_world_config configuration --all-features</automated></verify>
  <acceptance_criteria>The command exits 0 and proves every default, bound, zero-step rule, gravity rejection, and exact dt_ratio bit pattern.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire force clearing and checked step entry</name>
  <files>crates/liquidfun/src/world/object.rs, crates/liquidfun/src/world/step.rs, crates/liquidfun/tests/rigid_world_config.rs</files>
  <read_first>
    - crates/liquidfun/src/world/object.rs
    - crates/liquidfun/src/world/step.rs
    - crates/liquidfun/src/world/body/control.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
  </read_first>
  <action>Change World::step to accept StepConfiguration alongside the existing hook/limits, validating before lock or contact effects. Add World::clear_forces and automatic post-step clearing, including successful ContinuousPending reports, while errors before a coherent commit leave accumulators unchanged. Keep Phase 6 call sites compiling by migrating them to an explicit fixed configuration helper in tests/tooling, not by preserving hidden fixed behavior.</action>
  <verify><automated>cargo test -p liquidfun --test rigid_world_config force_clearing --all-features &amp;&amp; cargo test -p liquidfun --test rigid_contact_solver --all-features</automated></verify>
  <acceptance_criteria>Both commands exit 0; tests prove auto/manual clearing, disabled persistence, zero-duration behavior, and invalid-config no-effect.</acceptance_criteria>
</task>
</tasks>

<verification>Run both focused targets and `cargo clippy -p liquidfun --all-targets --all-features -- -D warnings`.</verification>
<success_criteria>All world stepping/configuration inputs needed by later solver plans are checked, explicit, and source-compatible.</success_criteria>
<output>After completion, create `.planning/phases/07-rigid-solver-world-operations-and-ccd/07-02-SUMMARY.md`.</output>
