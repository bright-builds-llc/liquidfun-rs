---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-07-12T23-36-17
generated_at: 2026-07-13T00:09:12.340Z
phase: 07-rigid-solver-world-operations-and-ccd
plan: "06"
type: execute
wave: 4
depends_on: ["07-02"]
files_modified:
  - crates/liquidfun/src/collision/broad_phase.rs
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/world/query.rs
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/tests/rigid_world_queries.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - Consumers stream AABB and ray hits through borrow-scoped visitors with typed controls and no public proxy identity.
    - Query traversal order is explicitly unspecified, collision masks are not applied automatically, and multi-child occurrences are not deduplicated.
    - Invalid ray geometry or clip construction fails without modifying World or applying an invalid clip; prior callback side effects remain caller-owned.
  artifacts:
    - crates/liquidfun/src/world/query.rs
    - crates/liquidfun/tests/rigid_world_queries.rs
  key_links:
    - World visitors wrap BroadPhase tree traversal and map FixtureProxy to FixtureId/child plus owned hit data.
    - Shape::ray_cast narrows the tree sub-input before application directives control continuation/clipping.
---

# Plan 07-06: World AABB Queries and Ray Casts

<objective>Expose safe streaming world fixture queries and ray casts over existing tree/shape kernels. Purpose: implement RIGD-09 without hardening traversal order or proxy identity. Output: public directives/hit types, broad-phase adapters, and callback tests.</objective>
<decision_coverage>D-17, D-18, and the production half of D-19 are fully implemented.</decision_coverage>

<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| Application callback to traversal | Callback directives can terminate or mutate the allowed ray interval. |
| Private proxy to public hit | Storage identity must be replaced by semantic fixture/child data. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T07-06-01 | Tampering | Ray clip fraction | mitigate | Validate finite fraction inside current inclusive interval before continuing. |
| T07-06-02 | Information disclosure | ProxyId | mitigate | World wrapper never exports tree/proxy slots; hits own only semantic data. |
| T07-06-03 | Denial of service | Visitor traversal | mitigate | Typed terminate control and existing bounded tree depth/state invariants. |
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add borrow-scoped AABB query visitors</name>
  <files>crates/liquidfun/src/collision/broad_phase.rs, crates/liquidfun/src/world/query.rs, crates/liquidfun/tests/rigid_world_queries.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/tree/traversal.rs
    - crates/liquidfun/src/collision/broad_phase.rs
    - crates/liquidfun/src/world/proxy.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2WorldCallbacks.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
  </read_first>
  <action>Add crate-private BroadPhase query/ray visitor adapters that preserve DynamicTree LIFO traversal while hiding ProxyId/BroadProxy. Define public QueryDirective Continue/Terminate and borrow-scoped fixture occurrence data containing FixtureId and child index. World::query_aabb validates the AABB before callbacks, does not apply fixture FilterData automatically, emits repeated children, and documents unspecified order and mutation restrictions.</action>
  <verify><automated>cargo test -p liquidfun --test rigid_world_queries aabb --all-features</automated></verify>
  <acceptance_criteria>Empty/full/partial/terminate/explicit-filter/duplicate-child tests pass; public API contains no ProxyId and does not deduplicate occurrences.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add typed ray-hit directives and clipping</name>
  <files>crates/liquidfun/src/world.rs, crates/liquidfun/src/world/query.rs, crates/liquidfun/src/lib.rs, crates/liquidfun/tests/rigid_world_queries.rs</files>
  <read_first>
    - crates/liquidfun/src/collision/shape.rs
    - crates/liquidfun/src/collision/types.rs
    - crates/liquidfun/src/collision/tree/traversal.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2World.cpp
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Documentation/Programmers-Guide/Chapter10_World.md
  </read_first>
  <action>Define public RayCastDirective variants Ignore, Terminate, Continue, and Clip carrying an invariant-bearing checked fraction, plus owned WorldRayHit with fixture, child, point, normal, and fraction. World::ray_cast must tree-cull, exact-shape ray-cast each child, call applications only for real hits, preserve distinct upstream -1/0/1/fraction meanings, and never apply a non-finite or out-of-current-interval clip. Invalid ray geometry or checked-fraction construction leaves World unchanged; callback effects that occurred before a later directive are application-owned and are not rolled back. Document that equal-distance callback identity is unspecified.</action>
  <verify><automated>cargo test -p liquidfun --test rigid_world_queries ray --all-features &amp;&amp; cargo test -p liquidfun --test collision_broad_phase --all-features</automated></verify>
  <acceptance_criteria>Both commands exit 0 for ignore/terminate/continue/clip, nearest hit, tie, chain children, degenerate ray, and invalid geometry/clip construction; tests prove no World mutation or invalid clip application and do not promise rollback of caller-owned callback effects.</acceptance_criteria>
</task>
</tasks>

<verification>Run world-query and collision traversal targets plus strict Clippy; scan exports for no proxy/tree identity leakage.</verification>
<success_criteria>Consumers can query and ray-cast world fixtures with safe typed controls and accurately unspecified ordering.</success_criteria>
<output>After completion, create `.planning/phases/07-rigid-solver-world-operations-and-ccd/07-06-SUMMARY.md`.</output>
