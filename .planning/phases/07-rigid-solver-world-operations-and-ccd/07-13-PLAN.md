---
phase: 07-rigid-solver-world-operations-and-ccd
plan: "13"
type: execute
wave: 10
depends_on:
  - "07-12"
files_modified:
  - crates/liquidfun/src/lib.rs
  - README.md
  - ARCHITECTURE.md
  - TESTING.md
  - COMPATIBILITY.md
  - reference/compatibility.json
  - tools/xtask/src/docs.rs
  - tools/xtask/tests/docs_contract.rs
autonomous: true
requirements: [RIGD-03, RIGD-05, RIGD-06, RIGD-07, RIGD-08, RIGD-09]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-07-12T23-36-17
generated_at: 2026-07-13T00:09:12.340Z
must_haves:
  truths:
    - Public and contributor documentation states the exact Phase 7 behavior, error atomicity, ordering limits, and evidence authority without broad parity claims.
    - The machine-readable compatibility ledger records only executed reviewed evidence and deterministically generates COMPATIBILITY.md.
    - Focused evidence checks and the mandatory Rust format, lint, build, and test gate all pass in repository order.
  artifacts:
    - path: ARCHITECTURE.md
      provides: Phase 7 native-world, private CCD, and oracle-isolation boundaries
    - path: TESTING.md
      provides: Phase 7 deterministic witness, policy, D0/D1/D2, replay, and sanitizer contract
    - path: reference/compatibility.json
      provides: Authoritative reviewed compatibility dimensions
    - path: COMPATIBILITY.md
      provides: Deterministically generated human-readable compatibility report
  key_links:
    - from: reference/compatibility.json
      to: COMPATIBILITY.md
      via: cargo xtask inventory generate followed by read-only inventory check
    - from: tools/xtask/src/docs.rs
      to: ARCHITECTURE.md
      via: Required Phase 7 boundary and truthfulness assertions
    - from: tools/xtask/src/docs.rs
      to: TESTING.md
      via: Required D2-only local and canonical-D1 evidence language
---

# Plan 13: Phase 7 Documentation, Ledger, and Final Verification

## Objective

Document the implemented Phase 7 contract, update generated compatibility views from reviewed evidence, and run the complete verification gate without overstating maturity.

## Decisions Covered

- D-22: documentation describes the existing rigid protocol/evidence path and its bounded Phase 7 extension.
- D-23: the evidence contract records exact structural and closed per-observable numeric policies.
- D-24: ledger updates preserve D0-D3 review controls and may record only evidence actually produced and reviewed in this phase.

<threat_model>
- Human-edited generated compatibility views could diverge from the machine-readable ledger.
- Documentation could imply complete rigid parity, D3 review, or platform coverage not demonstrated by Phase 7 evidence.
- Provenance containing absolute local paths or volatile timestamps could make artifacts non-reproducible or leak workstation details.
- Skipping all-feature Rust gates after oracle success could leave the published Cargo-only crate broken.
</threat_model>

<tasks>

<task id="07-13-01" type="documentation">
  <name>Document the checked Phase 7 public and evidence contracts</name>
  <files>crates/liquidfun/src/lib.rs, README.md, ARCHITECTURE.md, TESTING.md, tools/xtask/src/docs.rs, tools/xtask/tests/docs_contract.rs</files>
  <read_first>
    - crates/liquidfun/src/lib.rs
    - README.md
    - ARCHITECTURE.md
    - TESTING.md
    - COMPATIBILITY.md
    - crates/liquidfun-test-protocol/src/scenario/rigid_world.rs
    - protocol/tolerances/phase7-v1.toml
    - tools/xtask/src/docs.rs
  </read_first>
  <action>
    Update public rustdoc and repository documentation to describe granular checked body controls, wake policy, step validation/completion, force clearing, deterministic island/source ordering, warm-start and sleep behavior, private CCD/sub-step resume semantics, coherent budget exhaustion, streaming query/ray directives, origin shifting, and error atomicity. Document which callback orders are intentionally unspecified, which evidence fields are exact or tolerance-based, and how query multisets and ray tie sets are canonicalized only for comparison. State that C++ tooling is private and optional, normal users remain Cargo-only, and Phase 7 evidence is bounded local evidence rather than complete rigid, D3, platform-wide, or release parity.
  </action>
  <verify>
    - cargo test --doc -p liquidfun
    - cargo xtask docs check
    - cargo test -p xtask --test docs_contract
  </verify>
  <acceptance_criteria>
    - Every new public type and method has succinct rustdoc covering validation, wake, ordering, atomicity, and completion semantics where applicable.
    - Architecture and testing docs accurately explain private CCD state, out-of-process evidence, and closed comparison policies.
    - README maturity language matches the reviewed evidence and contains no complete-parity or D3 implication.
  </acceptance_criteria>
</task>

<task id="07-13-02" type="verification">
  <name>Regenerate the ledger and run the Phase 7 completion gate</name>
  <files>reference/compatibility.json, COMPATIBILITY.md</files>
  <read_first>
    - reference/compatibility.json
    - COMPATIBILITY.md
    - tools/xtask/src/inventory.rs
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards/core/verification.md
    - standards/languages/rust.md
  </read_first>
  <action>
    Per D-24, update `reference/compatibility.json` only from Phase 7 artifacts that were actually executed and explicitly reviewed, retaining each applicable row's evidence dimensions and residual limitation. Generate `COMPATIBILITY.md` through the existing inventory command and require read-only inventory/docs checks to detect hand-edited drift, missing evidence, absolute paths, or unsupported maturity claims. Run focused protocol and local D2 differential replay before the mandatory Rust gate in exact repository order: `cargo fmt --all`, Clippy, build, tests. Review the final diff and record residual limitations. Do not edit `.planning/REQUIREMENTS.md` directly, never advance local D2 to D1/D3, and make no full-rigid, broader-requirement, or multi-platform claim; terminal requirement status is handled by the GSD execution workflow only after this plan's evidence passes.
  </action>
  <verify>
    - cargo xtask differential replay --scenario rigid-world --preset oracle-debug --session-profile one-shot
    - cargo xtask inventory generate
    - cargo xtask inventory check
    - cargo xtask docs check
    - cargo fmt --all
    - cargo clippy --all-targets --all-features -- -D warnings
    - cargo build --all-targets --all-features
    - cargo test --all-features
    - git diff --check
  </verify>
  <acceptance_criteria>
    - Machine and generated human-readable ledgers agree and cite only reproducible reviewed artifacts.
    - All applicable Phase 7, documentation, oracle, formatting, lint, build, and test checks pass in the prescribed order.
    - Final documentation and ledger state make no unsupported D3, full-rigid-parity, broader-requirement, or multi-platform claim.
  </acceptance_criteria>
</task>

</tasks>

## Verification

- `cargo xtask differential replay --scenario rigid-world --preset oracle-debug --session-profile one-shot`
- `cargo xtask inventory generate`
- `cargo xtask inventory check`
- `cargo xtask docs check`
- `cargo fmt --all`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo build --all-targets --all-features`
- `cargo test --all-features`
- `git diff --check`

## Success Criteria

- Public and contributor documentation matches the implemented Phase 7 behavior and limitations.
- Compatibility views are reproducibly generated from reviewed machine evidence.
- The full Cargo-only verification gate passes and no maturity claim outruns evidence.

## Must Haves

- Machine-readable ledger remains authoritative over its generated Markdown view.
- No absolute local paths or volatile unnormalized provenance enter committed evidence.
- No requirement completion, D3 promotion, or broad parity claim without its explicit evidence and review.
