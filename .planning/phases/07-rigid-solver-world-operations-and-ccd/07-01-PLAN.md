---
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-07-12T23-36-17
generated_at: 2026-07-13T00:09:12.340Z
phase: 07-rigid-solver-world-operations-and-ccd
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/world/body.rs
  - crates/liquidfun/src/world/body/control.rs
  - crates/liquidfun/src/world/object.rs
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/tests/rigid_body_controls.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - Consumers can inspect and granularly mutate every Phase 7 body control through a validated BodyId and typed WakePolicy.
    - Invalid inputs and derived overflow leave all body, force, contact, proxy, and sleep state unchanged.
    - Static/kinematic and sleeping preserve-sleep calls retain the pinned successful no-effect branches.
  artifacts:
    - crates/liquidfun/src/world/body/control.rs
    - crates/liquidfun/tests/rigid_body_controls.rs
  key_links:
    - World methods validate handles and complete candidate BodyState values before one commit.
    - Fixed-rotation changes reuse source-ordered mass aggregation and clear angular velocity atomically.
---

# Plan 07-01: Checked Body Control Contract

<objective>Implement the complete granular body force, impulse, velocity, damping, gravity-scale, fixed-rotation, sleep-permission, awake, and bullet API. Purpose: satisfy the locked authority and wake/no-effect contract before solver work consumes it. Output: invariant-bearing body state, typed errors, World methods, and focused black-box tests.</objective>

<decision_coverage>D-01, D-02, D-03, and D-04 are fully implemented.</decision_coverage>

<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| Public scalar/vector input to BodyState | Untrusted floats and handles must not create non-finite or cross-world state. |
| Body control to coupled mass/sleep state | A control may affect velocity, accumulated force, inertia, and wake state together. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T07-01-01 | Tampering | Force/impulse accumulation | mitigate | Build and finite-check a complete candidate before replacement. |
| T07-01-02 | Elevation of privilege | BodyId resolution | mitigate | Preserve world-key/generation validation; expose no mutable body façade or raw storage. |
| T07-01-03 | Denial of service | Non-finite arithmetic | mitigate | Reject every non-finite input/intermediate and negative damping before effects. |
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Expand checked body definitions and runtime state</name>
  <files>crates/liquidfun/src/world/body.rs, crates/liquidfun/src/world/body/control.rs, crates/liquidfun/tests/rigid_body_controls.rs</files>
  <read_first>
    - .planning/phases/07-rigid-solver-world-operations-and-ccd/07-CONTEXT.md
    - crates/liquidfun/src/world/body.rs
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.h
    - third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2Body.cpp
  </read_first>
  <action>Per D-01 through D-04, add checked BodyDef configuration and BodySnapshot accessors for linear/angular velocity, damping, gravity scale, allowed-sleep, awake, fixed-rotation, and bullet state while retaining the existing constructor through additive checked builders/setters. Add private force, torque, sleep-time, and flags to BodyState plus a public WakePolicy enum. Implement pure candidate functions for velocity setters, force/torque/impulse application, sleep transitions, damping/gravity-scale/bullet changes, and fixed-rotation mass reset. Preserve exact upstream branch order: non-dynamic force/impulse is Ok no-op; preserve-sleep on asleep is Ok no-op; nonzero velocity wakes, zero does not; sleep clears velocities/force/torque; disabling sleep wakes; fixed rotation clears angular velocity and recomputes mass. Tests must be written RED first with one concern and Arrange/Act/Assert.</action>
  <verify><automated>cargo test -p liquidfun --test rigid_body_controls definitions --all-features</automated></verify>
  <acceptance_criteria>Focused tests cover defaults, all checked fields, every wake/no-wake branch, static/kinematic no-effect, sleep clearing, fixed-rotation reset, and bit-identical no-mutation errors; the command exits 0.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose granular World body-control methods</name>
  <files>crates/liquidfun/src/world.rs, crates/liquidfun/src/world/object.rs, crates/liquidfun/src/lib.rs, crates/liquidfun/tests/rigid_body_controls.rs</files>
  <read_first>
    - crates/liquidfun/src/world/object.rs
    - crates/liquidfun/src/world.rs
    - crates/liquidfun/src/lib.rs
    - .planning/phases/03-rust-object-model-and-storage-architecture/03-CONTEXT.md
    - standards/languages/rust.md
  </read_first>
  <action>Add named World methods for all locked controls and curated exports for WakePolicy and non-exhaustive typed errors. Every method must poison-check, resolve the complete BodyId, prepare its candidate including derived cross products/accumulations, and replace state once. Keep valid ignored upstream branches as Ok(()), not a public outcome taxonomy. Do not add BodyMut, a command DSL, unchecked setters, raw accumulators, or storage borrows. Add cross-world/stale-handle and candidate-overflow black-box tests.</action>
  <verify><automated>cargo test -p liquidfun --test rigid_body_controls world_api --all-features &amp;&amp; cargo check -p liquidfun --all-features</automated></verify>
  <acceptance_criteria>Both commands exit 0; consumer tests use only public exports; source scans find WakePolicy and granular World methods and find no public BodyMut/control-command DSL.</acceptance_criteria>
</task>
</tasks>

<verification>Run the complete rigid_body_controls target, warning-denied Clippy for liquidfun, and inspect the diff for exact source branches and candidate-only mutation.</verification>
<success_criteria>RIGD-03 body controls and the direct RIGD-06 sleep/awake controls are safe, granular, source-recognizable, and atomically checked.</success_criteria>
<output>After completion, create `.planning/phases/07-rigid-solver-world-operations-and-ccd/07-01-SUMMARY.md`.</output>
