---
phase: 22-observability-shell
plan: "06"
type: execute
wave: 5
depends_on: [22-05]
files_modified:
  - BENCHMARKING.md
autonomous: true
requirements: [PERF-PAIR, PERF-PROFILE, PERF-TIMERS]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 22-2026-09-20T22-34-43
generated_at: 2026-09-20T22:51:03Z
must_haves:
  truths:
    - "cargo xtask package verify still passes and cargo tree -p liquidfun --edges normal contains none of serde, samply, dhat, cmake, or flate2."
    - "Default --release is unchanged: Cargo.toml has [profile.profiling] with debug = true and no [profile.release] debug override."
    - "docs/playground-dam-break-timing.md and reference/performance/manifest.toml are unmodified in this phase; reviewed_reports stays []."
    - "just playground-dam-break-bench, playground-dam-break-profile, and playground-dam-break-timers are one-line cargo xtask aliases with no cmake/samply flags."
    - "BENCHMARKING.md states that the unprofiled pair also writes gitignored stamps under target/dam-break-perf/ and does not paste wall-ms numbers."
  artifacts:
    - path: BENCHMARKING.md
      provides: "One-sentence stamp note; still forbids copying into manifest.toml"
      contains: "target/dam-break-perf"
    - path: crates/liquidfun/Cargo.toml
      provides: "Unchanged production deps (bitflags only)"
      contains: "bitflags"
    - path: reference/performance/manifest.toml
      provides: "Still-empty reviewed_reports"
      contains: "reviewed_reports = []"
  key_links:
    - from: tools/xtask/src/package.rs
      to: crates/liquidfun
      via: "package verify isolation"
      pattern: "FORBIDDEN_PREFIXES"
    - from: justfile
      to: cargo xtask playground
      via: "three one-line aliases"
      pattern: "playground-dam-break-"
---

<objective>
Prove package isolation, honest benchmarking copy, and Bright Builds file-length after the observability shell is wired — without refreshing public Dam Break numbers or filling the performance manifest.

Purpose: D-12 / D-14 and success criterion 3. Isolation is easy to lose if samply/serde leak into `liquidfun`.
Output: One BENCHMARKING.md sentence plus passing `package verify`, `cargo tree`, greps, and `bun scripts/bright-builds-check.ts all`. Independent AI review remains a later policy step; this plan does not self-approve the phase.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/verification.md
@PROJECT-SCOPE.md
@.planning/phases/22-observability-shell/22-CONTEXT.md
@.planning/phases/22-observability-shell/22-RESEARCH.md
@BENCHMARKING.md
@crates/liquidfun/Cargo.toml
@reference/performance/manifest.toml
@docs/playground-dam-break-timing.md
@justfile
@Cargo.toml

<interfaces>
Current exploratory paragraph in `BENCHMARKING.md`:

```
The playground Dam Break headless pair is optional local diagnosis, not a Phase
12 reviewed report. Reproduce it with `just playground-dam-break-bench` and see
[docs/playground-dam-break-timing.md](docs/playground-dam-break-timing.md). That
table is an unreviewed local sample. It is not compatibility evidence and must
not be copied into `reference/performance/manifest.toml`.
```

Add one sentence after that block (not inside `docs/playground-dam-break-timing.md`) stating that a successful pair also writes gitignored `target/dam-break-perf/&lt;utc-stamp&gt;/pair.json` and `pair.md`, that samply/`step_profiled` outputs in sibling stamps are not the 3× timing authority, and that `.json.gz` / `.trace` files must not be committed. Do not paste wall ms, ratios, host names, or CPU brands.

`liquidfun` production `[dependencies]` must remain `bitflags` only. `cargo tree -p liquidfun --edges normal` must not list `serde`, `serde_json`, `samply`, `dhat`, `cmake`, `cc`, or `flate2`.

Do not edit `docs/playground-dam-break-timing.md`, `reference/performance/manifest.toml`, `crates/liquidfun/src/**`, or CMake presets. After Markdown edits, run `just markdown-check` (mdformat 1.0.0 / Python 3.13). Do not mdformat `.planning/**`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document gitignored pair stamps in BENCHMARKING.md</name>
  <files>BENCHMARKING.md</files>
  <read_first>
    1. `.planning/phases/22-observability-shell/22-CONTEXT.md` D-12, D-14
    1. `.planning/phases/22-observability-shell/22-RESEARCH.md` Pitfall 8
    1. `BENCHMARKING.md` Exploratory local diagnosis section
    1. `docs/playground-dam-break-timing.md` (read only; do not edit)
    1. `reference/performance/manifest.toml` (read only; do not edit)
    1. `AGENTS.md` Repo-Local Guidance for mdformat / `just markdown-check`
  </read_first>
  <action>
    Per D-14, edit only `BENCHMARKING.md`. In the Exploratory local diagnosis section, keep the existing "not a Phase 12 reviewed report" wording. Insert one additional sentence (or two short sentences if wrapping requires it) that:

    - Names `target/dam-break-perf/&lt;utc-stamp&gt;/` as gitignored evidence for `pair.json` / `pair.md`.
    - States that `just playground-dam-break-profile` and `just playground-dam-break-timers` write sibling stamps that are not the 3× number (`timing_authority` remains unprofiled wall-clock).
    - States not to commit `.json.gz` or `.trace` files and not to copy pair numbers into `reference/performance/manifest.toml`.

    Do not add wall-ms figures. Do not mention a live ~300× sample as a public claim. Do not create `docs/native-performance-audit.md` (Phase 23). Run `just markdown-check` after the edit.
  </action>
  <verify>
    <automated>just markdown-check &amp;&amp; rg -n "target/dam-break-perf" BENCHMARKING.md</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "target/dam-break-perf" BENCHMARKING.md` matches.
    1. `rg -n "json.gz|not the 3" BENCHMARKING.md` matches gzip and a not-the-3× statement (wording may vary but must deny profiled authority).
    1. `rg -n "wall_ms|300×|300x" BENCHMARKING.md` does not newly introduce a numeric Dam Break pair result (no new digits pasted as a claim).
    1. `git diff -- docs/playground-dam-break-timing.md reference/performance/manifest.toml` is empty.
    1. `just markdown-check` exits 0.
  </acceptance_criteria>
  <done>BENCHMARKING.md mentions gitignored stamps without turning exploratory numbers into a public claim.</done>
</task>

<task type="auto">
  <name>Task 2: Isolation, recipe, and Bright Builds gates</name>
  <files>BENCHMARKING.md</files>
  <read_first>
    1. `.planning/phases/22-observability-shell/22-CONTEXT.md` D-05, D-06, D-07, D-10, D-12, D-14
    1. `standards/core/verification.md`
    1. `crates/liquidfun/Cargo.toml`
    1. `Cargo.toml` `[profile.profiling]`
    1. `justfile` playground recipes
    1. `tools/xtask/src/package.rs` FORBIDDEN_PREFIXES
  </read_first>
  <action>
    Per D-12, run isolation and honesty checks. Do not "fix" failures by adding exact-file exceptions to the Bright Builds TSV unless a third-party file is the only offender; split repo-owned files instead if `file-lengths` fails. Do not edit `crates/liquidfun` to add deps. If `package verify` fails, diagnose leaked paths rather than weakening `FORBIDDEN_*`.

    Required commands (all must exit 0), from the repo root:

    1. `cargo xtask package verify`
    1. `cargo tree -p liquidfun --edges normal` piped to a check that rejects `serde`, `samply`, `dhat`, `cmake`, `flate2` as crate names
    1. `cargo fmt --all --check`
    1. `cargo clippy -p xtask -p liquidfun-wasm -p liquidfun --all-targets --all-features -- -D warnings`
    1. `cargo test -p xtask --test playground_cli -- --test-threads=1`
    1. `cargo test -p liquidfun-wasm --lib dam_break_timers -- --test-threads=1`
    1. `bun scripts/bright-builds-check.ts all`

    Also assert with `rg` (no file writes except if clippy/fmt forces a format-only fix in files already owned by earlier plans — prefer fmt-only if `cargo fmt --all --check` fails on 22-01..05 files):

    - `justfile` playground recipes are exactly three one-line `cargo xtask playground dam-break-{bench,profile,timers}` aliases; no `samply`, `cmake`, `-g`.
    - `Cargo.toml` has `[profile.profiling]` `debug = true` and no `[profile.release]`.
    - `crates/liquidfun/Cargo.toml` `[dependencies]` contains `bitflags` and does not contain `serde` or `thiserror` wait — thiserror might not be there; only bitflags today. Do not add thiserror.
    - `rg step_profiled crates/liquidfun-wasm/src/dam_break_bench.rs crates/liquidfun-wasm/src/bin/dam_break_bench.rs` empty.
    - `reference/performance/manifest.toml` still has `reviewed_reports = []`.
    - No `playground/mod.rs`.
    - `git ls-files '*.json.gz' '*.trace'` empty.

    Record command outputs in `22-06-SUMMARY.md`. Do not approve the phase as an independent reviewer (owner policy: implementing agent must not approve its own work). Do not run a live Dam Break pair as a passing gate.
  </action>
  <verify>
    <automated>cargo xtask package verify &amp;&amp; cargo tree -p liquidfun --edges normal &amp;&amp; bun scripts/bright-builds-check.ts all</automated>
  </verify>
  <acceptance_criteria>
    1. `cargo xtask package verify` exits 0.
    1. `cargo tree -p liquidfun --edges normal` output does not contain `serde`, `samply`, `dhat`, or `flate2`.
    1. `rg -n "^bitflags" crates/liquidfun/Cargo.toml` matches and `rg -n "serde|samply|dhat" crates/liquidfun/Cargo.toml` is empty.
    1. `rg -n "samply|cmake" justfile` is empty.
    1. `rg -n "^\\[profile.profiling\\]$" Cargo.toml` matches and `rg -n "^\\[profile.release\\]" Cargo.toml` is empty.
    1. `rg -n "step_profiled" crates/liquidfun-wasm/src/dam_break_bench.rs crates/liquidfun-wasm/src/bin/dam_break_bench.rs` is empty.
    1. `rg -n "reviewed_reports = \\[\\]" reference/performance/manifest.toml` matches.
    1. `git diff -- docs/playground-dam-break-timing.md reference/performance/manifest.toml` is empty.
    1. `bun scripts/bright-builds-check.ts all` exits 0.
  </acceptance_criteria>
  <done>Isolation, just/xtask layering, and Bright Builds checks pass; public timing docs and the empty manifest were not refreshed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published `liquidfun` crate graph | Must not gain tooling deps from this phase. |
| Committed docs vs gitignored stamps | BENCHMARKING.md may name the directory; must not embed host dumps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06-01 | E | `liquidfun` supply chain | mitigate | `package verify` + `cargo tree -p liquidfun`; Cargo.toml still bitflags-only (D-12). |
| T-22-06-02 | I | committed `.json.gz` / host dumps | mitigate | `git ls-files` empty for gzip/trace; BENCHMARKING.md forbids commit (D-14). |
| T-22-06-03 | S | public timing claim | mitigate | Do not edit `docs/playground-dam-break-timing.md`; do not fill `manifest.toml`. |
| T-22-06-04 | T | hiding flags in just | mitigate | rg justfile for cmake/samply; recipes remain one-line cargo xtask (D-06/D-07). |
| T-22-06-05 | S | self-approval | accept | Independent AI review is required later by owner policy; this plan only records automated evidence. |
</threat_model>

<verification>
`cargo xtask package verify`, `cargo tree -p liquidfun --edges normal`, `bun scripts/bright-builds-check.ts all`, `just markdown-check`. No live Dam Break pair. No package publication.
</verification>

<success_criteria>
The observability shell is isolated from published `liquidfun`, just stays a one-line printer, and committed docs do not become a Phase 12 performance claim.
</success_criteria>

<output>
After completion, create `.planning/phases/22-observability-shell/22-06-SUMMARY.md`
</output>
