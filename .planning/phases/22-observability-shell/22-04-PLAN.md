---
phase: 22-observability-shell
plan: "04"
type: execute
wave: 3
depends_on: [22-02, 22-03]
files_modified:
  - tools/xtask/src/playground.rs
  - tools/xtask/src/playground/profile.rs
  - tools/xtask/src/main.rs
  - justfile
  - tools/xtask/tests/playground_cli.rs
  - tools/xtask/tests/fixtures/fake_upstream_tool.rs
autonomous: true
requirements: [PERF-PROFILE]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 22-2026-09-20T22-34-43
generated_at: 2026-09-20T22:51:03Z
must_haves:
  truths:
    - "just playground-dam-break-profile is a one-line alias for cargo xtask playground dam-break-profile with no cmake or samply flags."
    - "A fake profile run writes rust.json.gz plus profile-identity.json into a new stamp; identity has not_timing_authority true, cargo_profile profiling, samply_version 0.13.1, and the exact command; the stamp has no pair.json."
    - "Missing samply (LIQUIDFUN_XTASK_SAMPLY pointing at a nonexistent path) exits nonzero with 0.13.1 install text, writes no rust.json.gz, and does not skip."
    - "Profile cargo invocation uses --profile profiling and never cargo run --release; samply argv never starts with cargo."
  artifacts:
    - path: justfile
      provides: "Thin playground-dam-break-profile alias"
      contains: "cargo xtask playground dam-break-profile"
    - path: tools/xtask/src/playground/profile.rs
      provides: "profile::run spawn + identity persist"
      contains: "not_timing_authority"
    - path: tools/xtask/tests/fixtures/fake_upstream_tool.rs
      provides: "Fake samply 0.13.1 record --save-only -o"
      contains: "samply"
  key_links:
    - from: justfile
      to: tools/xtask/src/playground.rs
      via: "one-line cargo xtask playground dam-break-profile"
      pattern: "dam-break-profile"
    - from: profile::run
      to: samply_record_argv
      via: "Command::new(samply_program).args(argv)"
      pattern: "LIQUIDFUN_XTASK_SAMPLY"
---

<objective>
Wire `dam-break-profile`: rebuild `dam-break-bench` with `[profile.profiling]`, wrap it with samply 0.13.1 `--save-only`, and persist `rust.json.gz` plus identity into a new evidence stamp.

Purpose: PERF-PROFILE / D-03 / D-06 / D-07 / D-08 / D-09. Profiled timings must never become the 3× number.
Output: xtask command, thin just alias, fake-samply CLI tests, fail-closed missing-samply CLI test. No `--cpp-debuginfo`, no new CMake preset.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/code-shape.md
@.planning/phases/22-observability-shell/22-CONTEXT.md
@.planning/phases/22-observability-shell/22-RESEARCH.md
@tools/xtask/src/playground.rs
@tools/xtask/src/playground/profile.rs
@tools/xtask/src/playground/counts.rs
@justfile
@tools/xtask/tests/playground_cli.rs
@tools/xtask/tests/fixtures/fake_upstream_tool.rs

<interfaces>
Dispatch: `playground::run` grows a `dam-break-profile` arm calling `profile::run(command_args)`. Accept `--warmup`/`--steps` via existing `counts::parse_counts` (defaults 60/600). Reject unknown flags.

`profile::run` sequence (D-08/D-09):

1. `parse_counts`.
2. Resolve samply program: `LIQUIDFUN_XTASK_SAMPLY` else `"samply"`. `Command::new(program).arg("--version")`. On spawn failure or `require_samply_version` failure, return `missing_samply_error()` / version error. Do not mint a stamp. Do not write `rust.json.gz`.
3. `Command::new(cargo)` with cargo lookup `LIQUIDFUN_XTASK_CARGO` else `CARGO` else `"cargo"`: `build -p liquidfun-wasm --bin dam-break-bench --profile profiling`. Fail on nonzero. Do not pass `--release`. Do not call `upstream::run` or cmake (D-07: default profile path is Rust-only).
4. `mint_exclusive_stamp`.
5. `Command::new(samply_program).args(samply_record_argv(output, binary, warmup, steps)).current_dir(root)`. Output path is `stamp.join("rust.json.gz")`. Binary is `profiling_dam_break_bench_bin(root)` (honor `CARGO_TARGET_DIR` if set, else `root/target/profiling/...`).
6. On samply nonzero: return error; do not write a placeholder gzip; do not write `profile-identity.json`.
7. On success: write `profile-identity.json` with `kind: samply_cpu`, `not_timing_authority: true`, `cargo_profile: profiling`, `samply_version: 0.13.1`, host/HEAD/compiler (`rustc --version` first line via `RUSTC` else `rustc`), `binary`, `output: rust.json.gz`, `command` as the exact argv including program, warmup/steps.

Do **not** implement `--cpp-debuginfo`. Do **not** edit `tools/reference/CMakeLists.txt` or `CMakePresets.json`. Do **not** hide samply/cmake flags in `just` (D-06/D-07).

`justfile` add after `playground-dam-break-bench`:

```just
playground-dam-break-profile:
    cargo xtask playground dam-break-profile
```

Timed-loop targeting (Claude's discretion + 22-RESEARCH A1): wrap the existing binary; Instant already excludes construction/warmup. Set `warmup_included_in_samples: true` on identity JSON. Do not use `--pid`.

Update `tools/xtask/src/main.rs` USAGE playground line to mention pair/profile if it currently says only timing. Keep `unsafe_code = "forbid"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement dam-break-profile command and thin just alias</name>
  <files>tools/xtask/src/playground.rs, tools/xtask/src/playground/profile.rs, tools/xtask/src/main.rs, justfile</files>
  <read_first>
    1. `.planning/phases/22-observability-shell/22-CONTEXT.md` D-03, D-06, D-07, D-08, D-09, D-10
    1. `.planning/phases/22-observability-shell/22-RESEARCH.md` Pattern 1, Pattern 3, Pitfall 3, Pitfall 5
    1. `tools/xtask/src/playground/profile.rs` from 22-03
    1. `tools/xtask/src/playground.rs` dispatcher
    1. `justfile` `playground-dam-break-bench` recipe
  </read_first>
  <action>
    Per D-06, add the one-line just alias with no extra arguments. Per D-07, that line must not mention cmake, ninja, `-g`, samply, or `--cpp-debuginfo`.

    Extend USAGE in `playground.rs` (and `main.rs` playground blurb) to list `dam-break-bench` and `dam-break-profile`. Dispatcher: `dam-break-profile =&gt; profile::run(command_args)`. Keep `dam-break-bench` on `pair::run`. Unknown commands stay usage errors.

    Implement `profile::run` exactly as the interfaces sequence. Use `Command::new(program).args(...)` only. Collect `profile-identity.json` `command` as `Vec&lt;String&gt;` of the samply program plus `samply_record_argv` converted with `.to_string_lossy()`. Compiler field: first line of `rustc --version` (env `RUSTC` else `rustc`), same spirit as `dam_break_bench.rs`. Git head via existing `identity::host_identity`.

    Do not copy any wall_ms into `pair.json`. Do not call `World::step_profiled`. Do not enable `DiagnosticStepProfiler`. Do not edit `crates/liquidfun/**` or `crates/liquidfun-wasm/src/dam_break_bench.rs`. No `unwrap()`. If `fs::write` of identity fails, return `PlaygroundError`. Stamp mint uses the same unix-now / optional `LIQUIDFUN_XTASK_STAMP_UNIX` injection 22-02 added (if present).

    File-length: if `profile.rs` would exceed 628 lines, split `playground/profile/identity_write.rs` using `profile.rs` plus `profile/` (no `mod.rs`).
  </action>
  <verify>
    <automated>rg -n "playground-dam-break-profile:" justfile &amp;&amp; rg -n "cargo xtask playground dam-break-profile" justfile &amp;&amp; rg -n "dam-break-profile" tools/xtask/src/playground.rs</automated>
  </verify>
  <acceptance_criteria>
    1. `justfile` contains a `playground-dam-break-profile` recipe whose only command line is `cargo xtask playground dam-break-profile`.
    1. `rg -n "samply|cmake|-g|cpp-debuginfo" justfile` is empty.
    1. `rg -n "profile::run" tools/xtask/src/playground.rs` matches.
    1. `rg -n "--profile" tools/xtask/src/playground/profile.rs` matches `profiling`.
    1. `rg -n "upstream::run" tools/xtask/src/playground/profile.rs` is empty.
    1. `rg -n "cpp-debuginfo|REFERENCE_PROFILE_DEBUG_INFO" tools/xtask/src` is empty.
    1. `rg -n "not_timing_authority" tools/xtask/src/playground/profile.rs` matches.
    1. `rg -n "step_profiled" tools/xtask/src/playground/profile.rs crates/liquidfun-wasm/src/dam_break_bench.rs crates/liquidfun-wasm/src/bin/dam_break_bench.rs` is empty.
  </acceptance_criteria>
  <done>Profile command and just alias exist, Rust-only, with identity marked not timing authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fake samply CLI success and production missing-samply fail-closed</name>
  <files>tools/xtask/tests/fixtures/fake_upstream_tool.rs, tools/xtask/tests/playground_cli.rs</files>
  <read_first>
    1. `.planning/phases/22-observability-shell/22-CONTEXT.md` D-09, D-13
    1. `.planning/phases/22-observability-shell/22-RESEARCH.md` fake samply, Pitfall 4
    1. `tools/xtask/tests/playground_cli.rs` from 22-02
    1. `tools/xtask/tests/fixtures/fake_upstream_tool.rs`
  </read_first>
  <behavior>
    1. CLI: `playground dam-break-profile --warmup 0 --steps 1` with fake cargo+samply writes nonempty `rust.json.gz` and `profile-identity.json` with `not_timing_authority: true`, `cargo_profile: profiling`, `samply_version: 0.13.1`, `command` containing `--save-only` and `--unstable-presymbolicate` and `dam-break-bench` and not containing a leading `cargo` record target; stamp directory has no `pair.json`.
    1. Fake cargo marker/args contain `--profile profiling` and `--bin dam-break-bench` and do not contain `--release`.
    1. CLI: `LIQUIDFUN_XTASK_SAMPLY` set to a path that does not exist → nonzero exit, stderr contains `0.13.1` and `cargo install --locked samply --version 0.13.1` and does not contain `skip`; no `rust.json.gz` is created under the fixture `target/dam-break-perf`.
    1. Pre-existing stamp directory bytes stay unchanged when a second profile run mints a new stamp (reuse 22-02 collision strategy).
  </behavior>
  <action>
    Per D-13, extend `fake_upstream_tool.rs`:

    - Stem contains `samply`: `--version` prints `samply 0.13.1`. `record` with `--save-only` and `-o &lt;path&gt;` writes at least 16 nonempty bytes to that path and exits 0. Record the joined args to `LIQUIDFUN_TEST_SAMPLY_MARKER` if set. Do not write gzip unless `-o` is present. Wrong subcommand exits 1.
    - Stem contains `cargo`: if args contain `--profile` and `profiling`, create `target/profiling/dam-break-bench` (respect `CARGO_TARGET_DIR` if set) as a copy of the current fake binary or a chmod +x stub, write args to `LIQUIDFUN_TEST_CARGO_MARKER` if set, exit 0. Keep 22-02 `dam-break-bench` JSON `run --release` behavior when args contain `run` and `dam-break-bench`.

    Copy fake-samply in `playground_cli.rs` FakeTools. Tests must set `LIQUIDFUN_XTASK_SAMPLY` to fake samply for the success case.

    The missing-samply test must **not** use the fake: set `LIQUIDFUN_XTASK_SAMPLY` to `/nonexistent/liquidfun-samply-missing` (or a unique missing path). Do not put fake samply on PATH for that test. This is the production lookup path (env override then spawn), proving fail-closed is not skipped by the fake (T-22-04-06).

    Do not run host `samply` or a live Dam Break pair. Do not `git add` any `.json.gz`.
  </action>
  <verify>
    <automated>cargo test -p xtask --test playground_cli -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "samply 0.13.1" tools/xtask/tests/fixtures/fake_upstream_tool.rs` matches.
    1. `rg -n "LIQUIDFUN_XTASK_SAMPLY" tools/xtask/src/playground/profile.rs` matches.
    1. `rg -n "nonexistent/liquidfun-samply-missing|samply-missing" tools/xtask/tests/playground_cli.rs` matches a missing-path fail-closed test.
    1. `rg -n "skip" tools/xtask/tests/playground_cli.rs` does not assert skip-as-success.
    1. `cargo test -p xtask --test playground_cli` exits 0.
    1. `justfile` `playground-dam-break-bench` recipe is unchanged (`cargo xtask playground dam-break-bench`).
  </acceptance_criteria>
  <done>Fake samply persists rust.json.gz; missing samply fails closed without a placeholder gzip; pair.json is not written on the profile path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `LIQUIDFUN_XTASK_SAMPLY` → `Command::new` | Path to a binary; not interpolated into a shell. |
| samply `-o` path | Must be the minted stamp `rust.json.gz`, not a user path from argv. |
| Fake samply in tests vs production lookup | Success tests inject the fake; fail-closed test uses a missing path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | S | placeholder `rust.json.gz` | mitigate | Write gzip only via samply `-o` after a successful spawn; missing tool writes nothing (D-09). |
| T-22-04-02 | T | mixing profiled times into the gate | mitigate | No `pair.json` on this command; identity `not_timing_authority: true`; pair path stays `--release` (D-03, D-10). |
| T-22-04-03 | E | samply/cargo spawn | mitigate | Argv lists; env var is a program path. |
| T-22-04-04 | T | hidden cmake `-g` | mitigate | Profile command must not call cmake; no `--cpp-debuginfo`; just has no flags (D-07). |
| T-22-04-05 | I | `rust.json.gz` | mitigate | Destination under gitignored `target/`; do not commit (D-14). |
| T-22-04-06 | S | fake-tool tests skipping fail-closed | mitigate | Dedicated CLI test with nonexistent `LIQUIDFUN_XTASK_SAMPLY` and no fake samply. |
</threat_model>

<verification>
`cargo test -p xtask --test playground_cli -- --test-threads=1`. Do not require host samply 0.13.1 as DoD. Do not run live Instruments.
</verification>

<success_criteria>
Developer-facing `just playground-dam-break-profile` exists; fake capture writes rust.json.gz + identity; missing samply fails closed; `--release` pair binary is untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/22-observability-shell/22-04-SUMMARY.md`
</output>
