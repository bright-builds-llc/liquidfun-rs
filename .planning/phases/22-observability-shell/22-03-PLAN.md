---
phase: 22-observability-shell
plan: "03"
type: tdd
wave: 2
depends_on: [22-01]
files_modified:
  - Cargo.toml
  - tools/xtask/src/playground.rs
  - tools/xtask/src/playground/profile.rs
autonomous: true
requirements: [PERF-PROFILE]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 22-2026-09-20T22-34-43
generated_at: 2026-09-20T22:51:03Z
must_haves:
  truths:
    - "Workspace Cargo.toml defines [profile.profiling] with inherits = \"release\", debug = true, and strip = false, and [profile.release] is not given debug = true."
    - "samply_record_argv launches samply record --save-only --unstable-presymbolicate -o <stamp>/rust.json.gz -- <profiling-binary> --warmup N --steps M and never starts with cargo."
    - "Missing samply or a version other than 0.13.1 returns Err whose Display includes cargo install --locked samply --version 0.13.1 and does not mention skipping."
  artifacts:
    - path: Cargo.toml
      provides: "Named profiling profile that does not mutate --release"
      contains: "[profile.profiling]"
    - path: tools/xtask/src/playground/profile.rs
      provides: "Pure samply version check and argv builder"
      exports: ["samply_record_argv", "require_samply_0_13_1"]
  key_links:
    - from: tools/xtask/src/playground.rs
      to: tools/xtask/src/playground/profile.rs
      via: "mod profile"
      pattern: "mod profile;"
    - from: samply_record_argv
      to: target/profiling/dam-break-bench
      via: "argv after -- is the built binary plus warmup/steps"
      pattern: "unstable-presymbolicate"
---

<objective>
Add the workspace `[profile.profiling]` gate-isolation profile and a tested fail-closed samply 0.13.1 argv core before any process spawn.

Purpose: PERF-PROFILE / D-08 / D-09. Named profile avoids `CARGO_PROFILE_RELEASE_DEBUG`. Missing samply must not skip.
Output: `Cargo.toml` profiling profile plus `playground/profile.rs` helpers with RED→GREEN unit tests. No just alias and no CLI spawn yet (22-04).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/22-observability-shell/22-CONTEXT.md
@.planning/phases/22-observability-shell/22-RESEARCH.md
@Cargo.toml
@tools/xtask/src/playground.rs

<interfaces>
D-08 locks:

```toml
[profile.profiling]
inherits = "release"
debug = true
strip = false
```

Do not add `debug = true` under `[profile.release]`. Do not introduce `CARGO_PROFILE_RELEASE_DEBUG`.

Claude's discretion (22-CONTEXT.md): exact samply argv and how the timed loop is targeted. Honor D-08 by wrapping the already Instant-isolated `dam-break-bench` binary (construction/warmup stay outside Instant). Do not implement `--pid` attach in this phase.

Production argv (samply 0.13.1):

```text
samply record --save-only --unstable-presymbolicate \
  -o <stamp>/rust.json.gz \
  -- <repo>/target/profiling/dam-break-bench --warmup 60 --steps 600
```

On Windows the binary name is `dam-break-bench.exe`. Helper should take `samply_program`, `output_gz`, `binary`, `warmup_steps`, `measured_steps`.

Missing/wrong-version stderr must include all of: pin `0.13.1`; `cargo install --locked samply --version 0.13.1`; `brew install samply`; `samply setup` on macOS; must not mention skipping.

This plan may add `mod profile;` to `playground.rs` but must not add a `dam-break-profile` match arm (22-04). Do not edit `justfile`, `fake_upstream_tool.rs`, or `crates/liquidfun/**`.
</interfaces>
</context>

<feature>
  <name>Profiling profile plus fail-closed samply argv</name>
  <files>Cargo.toml, tools/xtask/src/playground.rs, tools/xtask/src/playground/profile.rs</files>
  <behavior>
    Workspace `[profile.profiling]` inherits release with full debuginfo. `require_samply_0_13_1` accepts stdout `samply 0.13.1` and rejects missing/other versions with install text. `samply_record_argv` is a `Vec&lt;OsString&gt;` starting at `record`, containing `--save-only`, `--unstable-presymbolicate`, `-o`, the rust.json.gz path, `--`, the profiling-profile binary, `--warmup`, `--steps`.
  </behavior>
  <implementation>
    Pure functions plus Cargo.toml. No Command spawn except in 22-04. Version check function takes the `--version` stdout string so unit tests need no binary.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for samply argv, version pin, and missing-tool copy</name>
  <files>tools/xtask/src/playground.rs, tools/xtask/src/playground/profile.rs</files>
  <read_first>
    1. `.planning/phases/22-observability-shell/22-CONTEXT.md` D-08, D-09
    1. `.planning/phases/22-observability-shell/22-RESEARCH.md` Pattern 3, Pitfall 3, Pitfall 4, missing-samply stderr
    1. `standards/core/testing.md`
    1. `tools/xtask/src/playground.rs` after 22-01
  </read_first>
  <behavior>
    1. `parse_samply_version("samply 0.13.1\n")` equals `Some("0.13.1")`.
    1. `parse_samply_version("samply 0.12.0")` equals `None`.
    1. `parse_samply_version("")` equals `None`.
    1. `require_samply_version("samply 0.13.1")` is `Ok(())`.
    1. `require_samply_version("samply 0.13.2")` is `Err` whose Display contains `0.13.1`, `cargo install --locked samply --version 0.13.1`, `brew install samply`, and `samply setup`, and does not contain `skip`.
    1. `missing_samply_error().to_string()` contains the same install text and does not contain `skip`.
    1. `samply_record_argv(samply, output, binary, 60, 600)` equals the locked flag sequence; index 0 is `record` (program is separate); no element equals `cargo`; last flags are `--warmup` `60` `--steps` `600`.
  </behavior>
  <action>
    Per D-09, add `mod profile;` to `playground.rs`. Create `playground/profile.rs` with tests first. If helpers are missing, tests must fail (RED). Do not spawn processes. Do not add `[profile.profiling]` until Task 2. Do not implement `profile::run()` CLI yet. One concern per test. `maybe_` names for `Option` (for example `maybe_version`). Before writing GREEN helpers, run `cargo test -p xtask --lib playground::profile` once and confirm it fails; paste that RED excerpt in `22-03-SUMMARY.md`.
  </action>
  <verify>
    <automated>test -f tools/xtask/src/playground/profile.rs &amp;&amp; rg -n "#\\[test\\]" tools/xtask/src/playground/profile.rs</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "mod profile;" tools/xtask/src/playground.rs` matches.
    1. `test -f tools/xtask/src/playground/profile.rs` succeeds.
    1. `rg -n "#\\[test\\]" tools/xtask/src/playground/profile.rs` matches at least six tests.
    1. `rg -n "fn parse_samply_version|fn samply_record_argv|missing_samply_error" tools/xtask/src/playground/profile.rs` matches the test names even if production fns are still missing (tests may be written against those names).
  </acceptance_criteria>
  <done>Failing tests describe samply 0.13.1 argv and fail-closed copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN profiling profile and samply helpers</name>
  <files>Cargo.toml, tools/xtask/src/playground/profile.rs</files>
  <read_first>
    1. `Cargo.toml` workspace lints (no existing `[profile.*]` block)
    1. Task 1 tests in `tools/xtask/src/playground/profile.rs`
    1. `.planning/phases/22-observability-shell/22-CONTEXT.md` D-08, D-10, D-12
  </read_first>
  <behavior>
    1. Same tests as Task 1 now pass.
    1. Root `Cargo.toml` contains `[profile.profiling]`, `inherits = "release"`, `debug = true`, `strip = false`.
    1. Root `Cargo.toml` has no `[profile.release]` debug override.
  </behavior>
  <action>
    Per D-08, append the exact `[profile.profiling]` table to the workspace `Cargo.toml` (after `[workspace.lints.clippy]` is fine). Do not change `[profile.release]` (there is none today; do not create one).

    Implement in `profile.rs`:

    - `parse_samply_version(stdout: &amp;str) -&gt; Option&lt;String&gt;` requiring the first non-empty line to contain `samply` and version `0.13.1` as a token (allow a trailing newline). Use `maybe_version` for the `Option`.
    - `fn missing_samply_error() -&gt; PlaygroundError` with kind `samply` and the locked install text.
    - `fn require_samply_version(stdout: &amp;str) -&gt; Result&lt;(), PlaygroundError&gt;` calling `parse_samply_version` and returning `missing_samply_error()` on mismatch.
    - `fn samply_record_argv(output_gz: &amp;Path, binary: &amp;Path, warmup_steps: u32, measured_steps: u32) -&gt; Vec&lt;OsString&gt;` as `record`, `--save-only`, `--unstable-presymbolicate`, `-o`, output, `--`, binary, `--warmup`, n, `--steps`, m.

    Also add `fn profiling_dam_break_bench_bin(repository_root: &amp;Path) -&gt; PathBuf` joining `target/profiling/` + `dam-break-bench` (`exe` suffix on Windows). Do not read `CARGO_PROFILE_RELEASE_DEBUG`. Do not add serde/samply to `liquidfun`. No `unwrap()`. `let...else` on parse failures.

    Do not spawn samply. Do not add just recipes. Do not implement `--cpp-debuginfo` (D-07 is honored by omitting cmake entirely from this module).
  </action>
  <verify>
    <automated>cargo test -p xtask --lib playground::profile -- --test-threads=1 &amp;&amp; python3 -c 'from pathlib import Path; t=Path("Cargo.toml").read_text(); assert "[profile.profiling]" in t and "inherits = \"release\"" in t and "debug = true" in t and "strip = false" in t; assert "[profile.release]" not in t'</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "^\\[profile.profiling\\]$" Cargo.toml` matches.
    1. `rg -n "inherits = \"release\"" Cargo.toml` matches inside that table (file contains both `inherits = "release"` and `debug = true` and `strip = false`).
    1. `rg -n "^\\[profile.release\\]" Cargo.toml` is empty.
    1. `rg -n "CARGO_PROFILE_RELEASE_DEBUG" tools/xtask/src/playground/profile.rs` is empty.
    1. `rg -n "fn samply_record_argv|fn require_samply_version|fn missing_samply_error" tools/xtask/src/playground/profile.rs` matches all three.
    1. `missing_samply_error().to_string()` (covered by tests) contains `cargo install --locked samply --version 0.13.1` and must not contain the word `skip` as user-facing advice.
    1. `cargo test -p xtask --lib playground::profile` exits 0.
    1. `tools/xtask/src/playground/profile.rs` has ≤628 physical lines.
  </acceptance_criteria>
  <done>`[profile.profiling]` exists and samply 0.13.1 argv/error helpers pass unit tests without spawning samply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| samply `--version` stdout → version parse | Untrusted tool output; only `0.13.1` is accepted. |
| Argv builder → later `Command` | Lists only; no shell joining. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | S | missing samply | mitigate | Fail closed with install text; no `Ok(())` skip path; no placeholder gzip (D-09). |
| T-22-03-02 | T | `--release` gate binary | mitigate | Named `[profile.profiling]`; tests assert no `[profile.release]` debug=true (D-10). |
| T-22-03-03 | E | samply argv | mitigate | `Vec&lt;OsString&gt;` flags; never `sh -c` or `samply record cargo` (Pitfall 3). |
| T-22-03-04 | T | fake tests vs production | mitigate | Version helper is pure and used by production 22-04; tests do not stub success when version mismatches. |
</threat_model>

<verification>
`cargo test -p xtask --lib playground::profile`. Confirm `Cargo.toml` profiling table. Do not run real samply. Do not run `cargo build --profile profiling` as DoD.
</verification>

<success_criteria>
Profiling Cargo profile is declared without mutating `--release`, and samply 0.13.1 success/failure is expressed as pure tested functions.
</success_criteria>

<output>
After completion, create `.planning/phases/22-observability-shell/22-03-SUMMARY.md`
</output>
