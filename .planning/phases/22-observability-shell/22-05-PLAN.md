---
phase: 22-observability-shell
plan: "05"
type: execute
wave: 4
depends_on: [22-04]
files_modified:
  - crates/liquidfun-wasm/src/session.rs
  - crates/liquidfun-wasm/src/dam_break_timers.rs
  - crates/liquidfun-wasm/src/bin/dam_break_timers.rs
  - crates/liquidfun-wasm/src/lib.rs
  - crates/liquidfun-wasm/Cargo.toml
  - tools/xtask/src/playground.rs
  - tools/xtask/src/playground/timers.rs
  - tools/xtask/src/main.rs
  - justfile
  - tools/xtask/tests/playground_cli.rs
  - tools/xtask/tests/fixtures/fake_upstream_tool.rs
autonomous: true
requirements: [PERF-TIMERS]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 22-2026-09-20T22-34-43
generated_at: 2026-09-20T22:51:03Z
must_haves:
  truths:
    - "A separate dam-break-timers native binary calls World::step_profiled through SessionCore::advance_profiled; dam-break-bench still calls only ordinary advance/World::step."
    - "just playground-dam-break-timers is a one-line cargo xtask playground dam-break-timers alias with no cmake/samply flags."
    - "A fake timers run writes timers.json into a new stamp with not_timing_authority true and parent tokens particle_prepare, particle_solve, and rigid_solve; that stamp has no pair.json."
    - "MAX_ADVANCE_STEPS remains 4; timers loop advance(1)/advance_profiled(1) and do not pass 600 as a single advance count."
  artifacts:
    - path: crates/liquidfun-wasm/src/bin/dam_break_timers.rs
      provides: "Native timer CLI printing parent JSON"
    - path: crates/liquidfun-wasm/src/dam_break_timers.rs
      provides: "Dam Break recipe reuse + parent aggregation"
      contains: "particle_prepare"
    - path: tools/xtask/src/playground/timers.rs
      provides: "xtask dam-break-timers persist"
      contains: "timers.json"
    - path: justfile
      provides: "Thin playground-dam-break-timers alias"
      contains: "cargo xtask playground dam-break-timers"
  key_links:
    - from: crates/liquidfun-wasm/src/session.rs
      to: liquidfun::World::step_profiled
      via: "advance_profiled only"
      pattern: "step_profiled"
    - from: crates/liquidfun-wasm/src/bin/dam_break_bench.rs
      to: World::step
      via: "unchanged run_dam_break_bench / session.advance(1)"
      pattern: "run_dam_break_bench"
---

<objective>
Add a separate Dam Break diagnostic process that emits coarse Phase 12 parent timers (`particle_prepare` / `particle_solve` / `rigid_solve` plus the rest of `DiagnosticProfileParent::ALL`) without putting `step_profiled` on the unprofiled gate binary.

Purpose: PERF-TIMERS / D-04 / D-11 / D-10.
Output: `dam-break-timers` bin, `advance_profiled`, xtask persist of `timers.json`, thin just alias, fake CLI test, grep proof the gate path stays on `World::step`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/languages/rust.md
@standards/core/code-shape.md
@.planning/phases/22-observability-shell/22-CONTEXT.md
@.planning/phases/22-observability-shell/22-RESEARCH.md
@crates/liquidfun-wasm/src/session.rs
@crates/liquidfun-wasm/src/lib.rs
@crates/liquidfun-wasm/src/dam_break_bench.rs
@crates/liquidfun-wasm/src/bin/dam_break_bench.rs
@crates/liquidfun/src/world/observation/profile.rs
@crates/liquidfun/src/world/step/execution.rs
@crates/liquidfun/tests/phase12_profiles.rs
@justfile

<interfaces>
`SessionCore::advance` must keep calling `World::step` (profiler disabled). Add native-only:

```rust
#[cfg(not(target_arch = "wasm32"))]
pub(crate) fn advance_profiled(&mut self) -> Result<DiagnosticStepProfile, SessionError>
```

Implementation: same `on_advance` hook as `advance`, then `self.world.step_profiled(self.step_configuration, &amp;mut NoDecisionHook, self.step_limits)`, increment `step_index` by 1 on success. Do not raise `MAX_ADVANCE_STEPS` (still 4). Do not add `wasm_bindgen` `advance_profiled` on `ProofSession`.

Parent aggregation: sum `duration` only for phases where `phase.maybe_common_parent()` is `Some` (do not add Rust child durations into parents). Tokens from `DiagnosticProfileParent::ALL`: `contact_update`, `rigid_solve`, `continuous_solve`, `particle_prepare`, `particle_solve`, `finalize`. Dam Break Medium has particles; `run_dam_break_timers(0, 1)` must include `particle_prepare`, `particle_solve`, and `rigid_solve` with `wall_ms &gt;= 0`.

Reuse locked recipe constants from `dam_break_bench` (`EXPECTED_PARTICLE_COUNT` 1920, `SessionCore::create(SceneId::DamBreak)`). Warm-up uses `advance(1)` in a loop. Measured steps use `advance_profiled()` in a loop.

Timer stdout JSON (no serde on `liquidfun-wasm`; follow `DamBreakBenchReport::to_json` `format!` style):

```json
{
  "kind": "step_profiled_parents",
  "schema": "phase12-profile-v1",
  "not_timing_authority": true,
  "particles": 1920,
  "warmup_steps": 60,
  "measured_steps": 600,
  "parents": {
    "particle_prepare": {"wall_ms": 1.0},
    "particle_solve": {"wall_ms": 1.0},
    "rigid_solve": {"wall_ms": 1.0}
  }
}
```

Include every `DiagnosticProfileParent::ALL` token in `parents` (0.0 if a parent never appeared).

xtask `timers::run`: `--release` `cargo run -p liquidfun-wasm --bin dam-break-timers` (not `--profile profiling`, not `dam-break-bench`), parse JSON, mint a **new** stamp (D-04), write `timers.json` merging host identity (`git_head`, os, arch, compiler from sample or rustc) plus `not_timing_authority: true`. Do not write `pair.json`. Do not call cmake.

`crates/liquidfun` gains no new dependencies and no kernel edits. `session.rs` is already ~537 lines; keep `advance_profiled` thin and put tests in `dam_break_timers.rs` so `session.rs` stays ≤628 lines.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Native timer binary and advance_profiled</name>
  <files>crates/liquidfun-wasm/src/session.rs, crates/liquidfun-wasm/src/dam_break_timers.rs, crates/liquidfun-wasm/src/bin/dam_break_timers.rs, crates/liquidfun-wasm/src/lib.rs, crates/liquidfun-wasm/Cargo.toml</files>
  <read_first>
    1. `.planning/phases/22-observability-shell/22-CONTEXT.md` D-04, D-10, D-11
    1. `.planning/phases/22-observability-shell/22-RESEARCH.md` Pattern 4, Pitfall 6
    1. `crates/liquidfun-wasm/src/session.rs` `advance`, `MAX_ADVANCE_STEPS`
    1. `crates/liquidfun-wasm/src/lib.rs` `#[cfg(not(target_arch = "wasm32"))]` dam_break_bench
    1. `crates/liquidfun-wasm/Cargo.toml` `[[bin]]` dam-break-bench
    1. `crates/liquidfun-wasm/src/dam_break_bench.rs` recipe + Instant loop
    1. `crates/liquidfun/src/world/observation/profile.rs` `DiagnosticProfileParent::ALL`, `maybe_common_parent`
  </read_first>
  <behavior>
    1. `run_dam_break_timers(0, 1)` returns parents JSON/object whose `parents` map contains `particle_prepare`, `particle_solve`, and `rigid_solve`.
    1. `run_dam_break_timers(0, 1)` `kind` is `step_profiled_parents` and `not_timing_authority` is true.
    1. `run_dam_break_timers(0, 1)` `particles` equals 1920.
    1. `MAX_ADVANCE_STEPS` remains 4 (`rg` in session.rs).
    1. `crates/liquidfun-wasm/src/dam_break_bench.rs` and `src/bin/dam_break_bench.rs` contain no `step_profiled` and no `advance_profiled`.
  </behavior>
  <action>
    Per D-11, do not wire timers through `dam_break_bench.rs`. Copy the `dam-break-bench` bin argv parser into `src/bin/dam_break_timers.rs` calling `run_dam_break_timers`. Add `[[bin]] name = "dam-break-timers"` with `test = false` `bench = false`. Export the module from `lib.rs` under `#[cfg(not(target_arch = "wasm32"))]` only, same as `dam_break_bench`.

    Add `advance_profiled` as specified. Measured loop must be `for _ in 0..measured_steps { session.advance_profiled()? }` after warmup `advance(1)` loops. Reject `measured_steps == 0`.

    Aggregate with `maybe_common_parent` only. Convert `Duration` via `as_secs_f64() * 1000.0`. No `unwrap()`. No serde dependency on `liquidfun-wasm` or `liquidfun`. Do not import Instant into new wasm-exported modules beyond what `dam_break_bench` already does (native cfg). Do not edit particle/rigid kernels under `crates/liquidfun/src`.

    Tests live in `dam_break_timers.rs` `#[cfg(test)]`. One timed step is enough (DoD must not require 600 profiled steps in CI).
  </action>
  <verify>
    <automated>cargo test -p liquidfun-wasm --lib dam_break_timers -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "fn advance_profiled" crates/liquidfun-wasm/src/session.rs` matches.
    1. `rg -n "step_profiled" crates/liquidfun-wasm/src/session.rs` matches inside `advance_profiled` only.
    1. `rg -n "step_profiled|advance_profiled|DiagnosticStepProfiler" crates/liquidfun-wasm/src/dam_break_bench.rs crates/liquidfun-wasm/src/bin/dam_break_bench.rs` is empty.
    1. `rg -n "MAX_ADVANCE_STEPS: u32 = 4" crates/liquidfun-wasm/src/session.rs` matches.
    1. `rg -n "name = \"dam-break-timers\"" crates/liquidfun-wasm/Cargo.toml` matches.
    1. `rg -n "particle_prepare" crates/liquidfun-wasm/src/dam_break_timers.rs` matches.
    1. Physical lines of `crates/liquidfun-wasm/src/session.rs` ≤628.
    1. `cargo test -p liquidfun-wasm --lib dam_break_timers` exits 0.
  </acceptance_criteria>
  <done>Timer binary emits Phase 12 parents; the unprofiled gate binary still uses ordinary step.</done>
</task>

<task type="auto">
  <name>Task 2: xtask dam-break-timers persist, just alias, fake CLI</name>
  <files>tools/xtask/src/playground.rs, tools/xtask/src/playground/timers.rs, tools/xtask/src/main.rs, justfile, tools/xtask/tests/playground_cli.rs, tools/xtask/tests/fixtures/fake_upstream_tool.rs</files>
  <read_first>
    1. `.planning/phases/22-observability-shell/22-CONTEXT.md` D-04, D-06
    1. `tools/xtask/src/playground.rs` dispatcher after 22-04
    1. `tools/xtask/src/playground/pair.rs` persist pattern
    1. `justfile` profile alias from 22-04
  </read_first>
  <action>
    Per D-04/D-06, add `mod timers;` and dispatch `dam-break-timers =&gt; timers::run(command_args)` with `counts::parse_counts`. `timers::run` must not call cmake or samply. Cargo: `run -p liquidfun-wasm --release --quiet --bin dam-break-timers -- --warmup --steps`. Parse JSON object from stdout like pair.rs `extract_json_object`. Require `kind == "step_profiled_parents"` and `not_timing_authority == true` (boolean). Mint a **new** stamp. Write `timers.json` (parsed value plus identity `git_head`/`os`/`arch` if not already present). Print the stamp path or a short Markdown summary to stdout; do not print a Rust/C++ ratio (this is not the gate).

    just alias:

    ```just
    playground-dam-break-timers:
        cargo xtask playground dam-break-timers
    ```

    Fake cargo: when args contain `dam-break-timers`, print valid timer JSON with the three required parents and `not_timing_authority: true`. CLI test `--warmup 0 --steps 1` asserts a new stamp with `timers.json` and without `pair.json` / `rust.json.gz`.

    Update playground USAGE. Keep just recipes one line each. No `unwrap()`.
  </action>
  <verify>
    <automated>cargo test -p xtask --test playground_cli -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `justfile` contains `playground-dam-break-timers` whose only command is `cargo xtask playground dam-break-timers`.
    1. `rg -n "samply|cmake" justfile` is empty.
    1. `rg -n "timers::run" tools/xtask/src/playground.rs` matches.
    1. `rg -n "dam-break-timers" tools/xtask/src/playground/timers.rs` matches.
    1. `rg -n "upstream::run|samply" tools/xtask/src/playground/timers.rs` is empty.
    1. `rg -n "not_timing_authority" tools/xtask/src/playground/timers.rs` matches.
    1. `cargo test -p xtask --test playground_cli` exits 0.
    1. `tools/xtask/src/playground.rs` and `timers.rs` each ≤628 physical lines.
  </acceptance_criteria>
  <done>Timer command persists timers.json into a new stamp and stays off the unprofiled pair process.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Timer bin stdout → timers.json | Parse JSON; require kind and not_timing_authority before persist. |
| wasm ProofSession vs native timer bin | `advance_profiled` is `cfg(not(target_arch = "wasm32"))` and not exported to JS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05-01 | T | contaminating the 3× gate | mitigate | No `step_profiled` in `dam_break_bench`; grep acceptance; timers `not_timing_authority` (D-10/D-11). |
| T-22-05-02 | T | shared stamp with pair | mitigate | `mint_exclusive_stamp` on the timer command only; tests assert no `pair.json` in that stamp (D-04). |
| T-22-05-03 | D | `advance(600)` | mitigate | Loop `advance_profiled()` once per step; `MAX_ADVANCE_STEPS` stays 4 (Pitfall 6). |
| T-22-05-04 | E | cargo spawn | mitigate | Argv list `--bin dam-break-timers`; unknown flags rejected. |
| T-22-05-05 | I | timer host fields | mitigate | Gitignored stamp only; do not copy into `manifest.toml`. |
</threat_model>

<verification>
`cargo test -p liquidfun-wasm --lib dam_break_timers` and `cargo test -p xtask --test playground_cli`. Do not treat timer wall_ms as the 3× number. Do not run 600-step live timers as DoD.
</verification>

<success_criteria>
Parent-phase timers exist on a separate Dam Break process; the unprofiled `--release` `dam-break-bench` path still uses ordinary `World::step`.
</success_criteria>

<output>
After completion, create `.planning/phases/22-observability-shell/22-05-SUMMARY.md`
</output>
