---
phase: 22-observability-shell
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/xtask/src/playground.rs
  - tools/xtask/src/playground/error.rs
  - tools/xtask/src/playground/counts.rs
  - tools/xtask/src/playground/identity.rs
  - tools/xtask/src/playground/pair.rs
  - tools/xtask/src/playground/stamp.rs
autonomous: true
requirements: [PERF-PAIR]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 22-2026-09-20T22-34-43
generated_at: 2026-09-20T22:51:03Z
must_haves:
  truths:
    - "The playground xtask module is split as playground.rs plus playground/*.rs with no playground/mod.rs, and each of those files stays under 628 physical lines."
    - "cargo xtask playground dam-break-bench still accepts only that subcommand with the same --warmup/--steps defaults (60/600) after the split."
    - "mint_exclusive_stamp creates target/dam-break-perf/<YYYY-MM-DDTHH-MM-SSZ>/ with exclusive create_dir, bumps the unix second on AlreadyExists, and never removes or writes into a pre-existing stamp."
  artifacts:
    - path: tools/xtask/src/playground.rs
      provides: "Dispatcher only: USAGE, mod declarations, run() match, PlaygroundError re-export"
      contains: "mod stamp"
    - path: tools/xtask/src/playground/stamp.rs
      provides: "UTC stamp format plus exclusive mint"
      exports: ["format_utc_stamp", "mint_exclusive_stamp"]
    - path: tools/xtask/src/playground/pair.rs
      provides: "Existing unprofiled pair orchestration (stdout Markdown, no persist yet)"
      contains: "configure_and_build_cpp"
  key_links:
    - from: tools/xtask/src/playground.rs
      to: tools/xtask/src/playground/pair.rs
      via: "run() dispatches dam-break-bench"
      pattern: "pair::run"
    - from: tools/xtask/src/playground/stamp.rs
      to: target/dam-break-perf
      via: "exclusive create_dir under repository-relative evidence root"
      pattern: "dam-break-perf"
---

<objective>
Split the 540-line playground xtask module before it crosses the Bright Builds 628-line trigger, and land the exclusive UTC evidence-stamp core used by every Phase 22 command.

Purpose: D-01 requires minted `target/dam-break-perf/<utc-stamp>/` directories that never clobber. File-length rules require the split on the first implementation plan.
Output: `playground.rs` dispatcher plus `playground/{error,counts,identity,pair,stamp}.rs` with passing unit tests. Pair output remains stdout-only until 22-02.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/rust.md
@PROJECT-SCOPE.md
@.planning/phases/22-observability-shell/22-CONTEXT.md
@.planning/phases/22-observability-shell/22-RESEARCH.md
@tools/xtask/src/playground.rs
@tools/xtask/src/main.rs
@tools/xtask/src/upstream.rs

<interfaces>
Keep `PlaygroundError` reachable as `playground::PlaygroundError` for `tools/xtask/src/main.rs` (`XtaskError::Playground`). Re-export it from `playground.rs`; do not add `playground/mod.rs`.

Current dispatcher contract (preserve after the split):

```rust
pub(crate) fn run(args: &[String]) -> Result<(), PlaygroundError>;
```

Today `run` accepts only `dam-break-bench`. Unknown commands stay usage errors. Do not add `dam-break-profile` or `dam-break-timers` in this plan.

`HostIdentity` fields already collected today: `git_head`, `os`, `arch`, `cpu_brand`, `logical_cores`.

`BenchSample` serde fields already parsed today: `engine`, `particles`, `warmup_steps`, `measured_steps`, `wall_ms`, `ms_per_step`, `steps_per_s`, `realtime_factor`, `compiler`.

Stamp format locked by D-01: filename-safe UTC `YYYY-MM-DDTHH-MM-SSZ` under `target/dam-break-perf/`.

`upstream::tool_program` pattern to copy for git (do not edit `upstream.rs`):

```rust
env::var_os("LIQUIDFUN_XTASK_GIT").unwrap_or_else(|| OsString::from("git"))
```

`just playground-dam-break-bench` already exists; do not edit `justfile`, `Cargo.toml`, `crates/liquidfun/**`, or `crates/liquidfun-wasm/**` in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split playground.rs into foo.rs plus foo/ without behavior change</name>
  <files>tools/xtask/src/playground.rs, tools/xtask/src/playground/error.rs, tools/xtask/src/playground/counts.rs, tools/xtask/src/playground/identity.rs, tools/xtask/src/playground/pair.rs</files>
  <read_first>
    1. `.planning/phases/22-observability-shell/22-CONTEXT.md` D-01, D-05, D-06
    1. `.planning/phases/22-observability-shell/22-RESEARCH.md` Pattern 1, Pitfall 7, Recommended Project Structure
    1. `standards/languages/rust.md` foo.rs plus foo/
    1. `standards/core/code-shape.md` 628-line trigger
    1. `tools/xtask/src/playground.rs` entire file
    1. `tools/xtask/src/main.rs` PlaygroundError mapping
    1. `tools/xtask/src/inventory.rs` as the in-repo `foo.rs` plus `foo/` split example
  </read_first>
  <action>
    Per D-05, keep the only live subcommand `dam-break-bench`. Move the existing 540-line `tools/xtask/src/playground.rs` into `playground.rs` plus `playground/` (never `playground/mod.rs`).

    `playground.rs` (dispatcher only): module docs, `mod error; mod counts; mod identity; mod pair;`, `pub(crate) use error::PlaygroundError;`, USAGE still listing only `dam-break-bench`, and `run()` that splits the first arg and calls `pair::run(command_args)` when the command is `dam-break-bench`. Keep the two existing usage tests (`missing_subcommand_is_a_usage_error`, `unknown_subcommand_is_a_usage_error`) here.

    `playground/error.rs`: `PlaygroundError`, `new`, `usage`, `Display`, `Error`, `From&lt;UpstreamError&gt;`. Prefix optional fields with `maybe_` if you add any `Option`. No `unwrap()`.

    `playground/counts.rs`: `DEFAULT_WARMUP_STEPS` 60, `DEFAULT_MEASURED_STEPS` 600, `BenchCounts`, `parse_counts`, `parse_u32_flag`, and `parse_counts_defaults_to_locked_warmup_and_steps`.

    `playground/identity.rs`: `HostIdentity`, `host_identity`, `repository_root` (same Cargo.toml + `crates/liquidfun/Cargo.toml` ancestor walk), `git_head`, `cpu_brand`, `first_command_line`, `linux_cpu_brand`. Change `git_head` to `Command::new(env::var_os("LIQUIDFUN_XTASK_GIT").unwrap_or_else(|| OsString::from("git")))` so later fake-git tests work; production unset env still runs `git`. Keep `"unknown"` on failure. Do not add chrono/time crates.

    `playground/pair.rs`: `BenchSample`, `RUST_ENGINE`, `CPP_ENGINE`, `pair::run` (configure C++ `oracle-release` extra target `playground-dam-break-bench`, native `cargo run -p liquidfun-wasm --release --bin dam-break-bench`, validate, print Markdown). Move JSON extract/parse/validate/render helpers and their unit tests here. Do not persist files yet (22-02). Do not add a Rust/C++ ratio yet (22-02). Do not call stamp APIs yet.

    Do not hide cmake in strings; keep `upstream::run` argv lists. Do not edit physics, `justfile`, or `liquidfun`. After the move, `cargo fmt --all`. Each new/touched file must stay under 628 physical lines.
  </action>
  <verify>
    <automated>cargo test -p xtask --lib playground -- --test-threads=1 &amp;&amp; python3 -c 'from pathlib import Path; paths=["tools/xtask/src/playground.rs"]+sorted(str(p) for p in Path("tools/xtask/src/playground").glob("*.rs"));
assert not Path("tools/xtask/src/playground/mod.rs").exists();
oversized=[p for p in paths if sum(1 for _ in open(p))>628]; raise SystemExit(oversized or 0)'</automated>
  </verify>
  <acceptance_criteria>
    1. `test ! -e tools/xtask/src/playground/mod.rs` succeeds.
    1. `rg -n "^mod error;|^mod counts;|^mod identity;|^mod pair;" tools/xtask/src/playground.rs` matches all four.
    1. `rg -n "pair::run" tools/xtask/src/playground.rs` matches.
    1. `rg -n "dam-break-profile|dam-break-timers" tools/xtask/src/playground.rs tools/xtask/src/playground` is empty.
    1. `rg -n "pair.json|rust.json.gz|timers.json" tools/xtask/src/playground.rs tools/xtask/src/playground` is empty.
    1. `rg -n "LIQUIDFUN_XTASK_GIT" tools/xtask/src/playground/identity.rs` matches.
    1. `rg -n "pub\(crate\) struct PlaygroundError" tools/xtask/src/playground/error.rs` matches.
    1. `rg -n "XtaskError::Playground|playground::PlaygroundError" tools/xtask/src/main.rs` still matches.
    1. `cargo test -p xtask --lib playground` exits 0.
    1. Every `tools/xtask/src/playground.rs` and `tools/xtask/src/playground/*.rs` file has ≤628 physical lines.
  </acceptance_criteria>
  <done>Dispatcher plus split modules compile and the existing pair unit tests still pass without persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD exclusive UTC stamp minting</name>
  <files>tools/xtask/src/playground.rs, tools/xtask/src/playground/stamp.rs</files>
  <read_first>
    1. `.planning/phases/22-observability-shell/22-CONTEXT.md` D-01
    1. `.planning/phases/22-observability-shell/22-RESEARCH.md` Pattern 2, Pitfall 2
    1. `standards/core/testing.md` Arrange / Act / Assert, one concern per test
    1. `standards/core/architecture.md` functional core versus imperative shell
    1. `tools/xtask/src/playground/identity.rs` `repository_root` after Task 1
  </read_first>
  <behavior>
    1. `format_utc_stamp(0)` equals `1970-01-01T00-00-00Z`.
    1. `format_utc_stamp(1_000_000_000)` equals `2001-09-09T01-46-40Z`.
    1. `format_utc_stamp` never emits `:`, `/`, or spaces; it matches `^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}-[0-9]{2}-[0-9]{2}Z$`.
    1. `mint_exclusive_stamp(root, 1_000_000_000)` creates `root/target/dam-break-perf/2001-09-09T01-46-40Z/` and returns that path.
    1. If that stamp directory already exists (with a marker file), `mint_exclusive_stamp` returns `.../2001-09-09T01-46-41Z`, leaves the original marker bytes unchanged, and does not call `remove_dir_all`.
    1. If eight consecutive stamp directories already exist, mint returns `Err` whose `Display` contains `stamp` and `exists` (or `already exists`) and does not write into any of them.
  </behavior>
  <action>
    Per D-01, add `mod stamp;` in `playground.rs`. Write failing Arrange/Act/Assert unit tests first in `playground/stamp.rs` (or `#[cfg(test)]` in that file), run them and confirm RED, then implement:

    - Pure core: `format_utc_stamp(unix_seconds: u64) -&gt; String` converting Unix seconds to UTC `YYYY-MM-DDTHH-MM-SSZ` with no new crates (no chrono, no `time`, no `date` subprocess).
    - Shell: `mint_exclusive_stamp(repository_root: &amp;Path, unix_seconds: u64) -&gt; Result&lt;PathBuf, PlaygroundError&gt;`. `create_dir_all` only the parent `target/dam-break-perf`. Then `fs::create_dir` the stamp directory. On `ErrorKind::AlreadyExists`, add one second and retry, up to 8 attempts. Never `remove_dir_all`, never `create_dir_all` on the stamp path itself, never write files into a directory that already existed, never accept a stamp string from argv.
    - Constant evidence relative dir: `target/dam-break-perf` (not `target/v12-native-perf`).
    - Use `let...else` / early returns. No `unwrap()`. Optional handles named `maybe_*`.

    Do not wire stamps into `pair::run` yet. Do not add profile/timer commands. Tests must use a temp directory under `target/xtask-test-fixtures/`, not the live repo `target/dam-break-perf`.
  </action>
  <verify>
    <automated>cargo test -p xtask --lib playground::stamp -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "mod stamp;" tools/xtask/src/playground.rs` matches.
    1. `rg -n "fn format_utc_stamp|fn mint_exclusive_stamp" tools/xtask/src/playground/stamp.rs` matches both.
    1. `rg -n "dam-break-perf" tools/xtask/src/playground/stamp.rs` matches.
    1. `rg -n "v12-native-perf" tools/xtask/src/playground` is empty.
    1. `rg -n "remove_dir_all" tools/xtask/src/playground/stamp.rs` is empty.
    1. `rg -n "create_dir_all" tools/xtask/src/playground/stamp.rs` matches only the parent evidence directory, not the stamp folder.
    1. `rg -n "unwrap\\(" tools/xtask/src/playground/stamp.rs` is empty.
    1. `cargo test -p xtask --lib playground::stamp` exits 0.
    1. `tools/xtask/src/playground/stamp.rs` has ≤628 physical lines.
  </acceptance_criteria>
  <done>Exclusive UTC stamps are unit-tested and unused by the pair command until 22-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated stamp path → filesystem | Stamp names are produced from unix seconds, never from CLI text. |
| Env `LIQUIDFUN_XTASK_GIT` → `Command::new` | Override is a program path, not a shell line. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | T | `mint_exclusive_stamp` | mitigate | Exclusive `create_dir`; bump unix second on `AlreadyExists`; never `remove_dir_all` or merge into an existing stamp (D-01). |
| T-22-01-02 | T | stamp path | mitigate | Join `target/dam-break-perf` + generated `YYYY-MM-DDTHH-MM-SSZ` only; reject no argv override in this phase. |
| T-22-01-03 | E | `LIQUIDFUN_XTASK_GIT` | mitigate | `Command::new(program).args(["rev-parse", "HEAD"])`; never `sh -c`. |
| T-22-01-04 | I | evidence dirs | mitigate | Stamps live under gitignored `/target/`; this plan writes no committed host dumps. |
| T-22-01-05 | D | stamp retries | mitigate | Cap retries at 8 then fail closed with exists text instead of spinning or overwriting. |
</threat_model>

<verification>
`cargo test -p xtask --lib playground -- --test-threads=1`. Do not run a live Dam Break pair, cmake, or samply. Do not run `bun scripts/bright-builds-check.ts all` until 22-06 (file-length of the split files is checked in Task 1).
</verification>

<success_criteria>
Playground xtask is a dispatcher plus focused modules; exclusive stamp minting is tested; `dam-break-bench` still only prints Markdown.
</success_criteria>

<output>
After completion, create `.planning/phases/22-observability-shell/22-01-SUMMARY.md`
</output>
