---
phase: 11-examples-headless-tooling-and-testbed
plan: "26"
type: execute
wave: 10
depends_on: ["11-25"]
files_modified:
  - crates/liquidfun-testbed/src/controller_adapter.rs
  - crates/liquidfun-testbed/src/ui/viewport.rs
  - crates/liquidfun-testbed/src/ui/run_controls.rs
  - crates/liquidfun-testbed/src/ui/settings.rs
  - crates/liquidfun-testbed/src/ui/overlays.rs
  - crates/liquidfun-testbed/src/input.rs
  - crates/liquidfun-testbed/tests/controller_ui.rs
autonomous: true
requirements: ["RIGD-10", "EXMP-04", "EXMP-05"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Run, pause, one-step, restart, settings, scenario actions, and capture map exactly to typed controller commands."
    - "Viewport renders only semantic primitives; camera, hover, selection, panels, and screenshots cause no ticks or checkpoints."
    - "All required overlays, mouse/trackpad gestures, keyboard shortcuts, field validation, and controller versus particle pause labels behave as specified."
  artifacts:
    - path: "crates/liquidfun-testbed/src/controller_adapter.rs"
      provides: "Typed command submission and immutable state projection"
    - path: "crates/liquidfun-testbed/src/ui/viewport.rs"
      provides: "Semantic primitive renderer and synchronized camera"
    - path: "crates/liquidfun-testbed/src/ui/settings.rs"
      provides: "Validated staged settings with Apply and Restart"
  key_links:
    - from: "crates/liquidfun-testbed/src/controller_adapter.rs"
      to: "crates/liquidfun-differential/src/session.rs"
      via: "typed command boundary"
      pattern: "SessionCommand"
    - from: "crates/liquidfun-testbed/src/ui/viewport.rs"
      to: "liquidfun::DebugPrimitive"
      via: "immutable geometry rendering"
      pattern: "DebugPrimitiveKey"
---

<objective>
Implement viewport rendering, controller commands, settings, overlays, camera, and deterministic capture.

Purpose: Deliver the full interactive EXMP-04 workflow without giving render frames or input handlers simulation authority.
Output: crates/liquidfun-testbed/src/controller_adapter.rs, crates/liquidfun-testbed/src/ui/viewport.rs, crates/liquidfun-testbed/src/ui/settings.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@crates/liquidfun-differential/src/session.rs
@crates/liquidfun/src/debug_draw.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire all interaction through the controller and semantic viewport</name>
  <files>crates/liquidfun-testbed/src/controller_adapter.rs, crates/liquidfun-testbed/src/ui/viewport.rs, crates/liquidfun-testbed/src/ui/run_controls.rs, crates/liquidfun-testbed/src/ui/settings.rs, crates/liquidfun-testbed/src/ui/overlays.rs, crates/liquidfun-testbed/src/input.rs, crates/liquidfun-testbed/tests/controller_ui.rs</files>
  <read-first>11-CONTEXT.md D-09, D-10, D-13, D-15, D-19, and D-20; UI-SPEC Controller Semantics, Keyboard, Mouse/Trackpad, Settings, Debug Layers, state/copy contracts; Plans 11-07, 11-09, 11-13, and 11-25.</read-first>
  <behavior>
    - "Run, pause, one-step, restart, settings, scenario actions, and capture map exactly to typed controller commands."
    - "Viewport renders only semantic primitives; camera, hover, selection, panels, and screenshots cause no ticks or checkpoints."
  </behavior>
  <action>Render shapes, joints, contacts/normals, particle contacts, broad-phase AABBs, centers, statistics, and diagnostic profiles from owned semantic primitives/observations only. Add Run Scenario, Pause/Resume, Step Once, Restart, Capture Checkpoint, and Apply and Restart controls whose enabledness mirrors SessionState. Implement exact shortcuts Space, Right, R, C, slash, F, brackets, 1-4, Home, question mark, Escape, suppressed during field editing, plus typed scenario-specific keys from the action map. Settings parse on blur/Enter, retain prior accepted values on failure, show exact inline guidance, enforce finite timestep greater than zero and iterations 1-1024, and enable Apply and Restart only for valid changes. Camera zooms about pointer, pans, resets, synchronizes comparison viewports, and never submits a simulation command. Distinguish Session paused from Particle system pause action. Screenshots use confined diagnostic output and display the non-authority clarification.</action>
  <pre-commit>Before any tracked task commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>cargo test -p liquidfun-testbed --test controller_ui</automated>
  </verify>
  <acceptance-criteria>Tests assert every controller transition/shortcut/field/error/enabledness, exactly one tick for step, no tick for pause/camera/screenshot/panel actions, all overlay layers, stable semantic selection, settings restart, and screenshot diagnostic labeling.</acceptance-criteria>
  <done>The interactive testbed controls and renders the headless model without owning simulation behavior.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| keyboard/mouse/settings input to controller | Untrusted state crosses into an effectful or authoritative Phase 11 operation. |
| semantic geometry and screenshot paths to renderer/filesystem | Validated output crosses a package, process, file, UI, or evidence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-26-01 | Tampering | keyboard/mouse/settings input to controller | mitigate | Suppress shortcuts while editing, parse closed commands and bounded settings, prevent duplicate in-flight submission, and reject invalid state transitions. |
| T-11-26-02 | Spoofing | semantic geometry and screenshot paths to renderer/filesystem | mitigate | Validate finite bounded primitives, select by stable keys, clamp camera, confine output below target, and reject traversal/links. |
| T-11-26-03 | Denial of service | resource use | mitigate | Enforce reviewed input, geometry, image, archive, process, horizon, diagnostic, and UI limits with fail-fast timeouts. |
| T-11-26-04 | Information disclosure | diagnostics, UI, CI, and reports | mitigate | Redact secrets, raw records, private indices, pointers, stack traces, and unbounded stderr; expose only bounded semantic context and safe URLs. |
| T-11-26-05 | Elevation of privilege | dependency, renderer, and oracle isolation | mitigate | Keep all renderer and C++ capabilities private, unpublished, non-default, least-privilege, and unreachable from liquidfun production dependencies. |
| T-11-26-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and all relevant Rust, C++, workflow, package, corpus, Markdown, renderer, or exact-reference gates stated by the task.
</verification>

<success-criteria>
- Run, pause, one-step, restart, settings, scenario actions, and capture map exactly to typed controller commands.
- Viewport renders only semantic primitives; camera, hover, selection, panels, and screenshots cause no ticks or checkpoints.
- All required overlays, mouse/trackpad gestures, keyboard shortcuts, field validation, and controller versus particle pause labels behave as specified.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-26-SUMMARY.md`.</output>
