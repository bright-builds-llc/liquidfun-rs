---
phase: 11-examples-headless-tooling-and-testbed
plan: "17"
type: execute
wave: 6
depends_on: ["11-06", "11-14"]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/liquidfun-benchmarks/Cargo.toml
  - crates/liquidfun-benchmarks/src/lib.rs
  - crates/liquidfun-benchmarks/benches/catalog.rs
  - crates/liquidfun-benchmarks/tests/catalog_equivalence.rs
autonomous: true
requirements: ["EXMP-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "An unpublished non-default benchmark package consumes the typed catalog and native backend."
    - "Scenario resolution and restart occur outside the measured interval, and each benchmark declares an exact logical horizon."
    - "Benchmark smoke validates semantic checkpoints before accepting timing samples."
  artifacts:
    - path: "crates/liquidfun-benchmarks/benches/catalog.rs"
      provides: "Criterion catalog benchmark bridge"
    - path: "crates/liquidfun-benchmarks/tests/catalog_equivalence.rs"
      provides: "Resolved-byte and pre-timing semantic validation"
    - path: "crates/liquidfun-benchmarks/Cargo.toml"
      provides: "Private non-default benchmark package"
  key_links:
    - from: "crates/liquidfun-benchmarks/benches/catalog.rs"
      to: "crates/liquidfun-test-protocol/src/catalog.rs"
      via: "shared resolved plan"
      pattern: "ResolvedScenario"
    - from: "crates/liquidfun-benchmarks/benches/catalog.rs"
      to: "crates/liquidfun-differential/src/catalog_native.rs"
      via: "native execution and checkpoint validation"
      pattern: "NativeCatalogBackend"
---

<objective>
Route benchmark setup through canonical resolved scenarios.

Purpose: Satisfy D-11 without taking Phase 12 performance budgets or claims into this phase.
Output: crates/liquidfun-benchmarks/benches/catalog.rs, crates/liquidfun-benchmarks/tests/catalog_equivalence.rs, crates/liquidfun-benchmarks/Cargo.toml.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@Cargo.toml
@.planning/research/STACK.md
@crates/liquidfun-differential/src/catalog_native.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the private benchmark bridge with measured-region discipline</name>
  <files>Cargo.toml, Cargo.lock, crates/liquidfun-benchmarks/Cargo.toml, crates/liquidfun-benchmarks/src/lib.rs, crates/liquidfun-benchmarks/benches/catalog.rs, crates/liquidfun-benchmarks/tests/catalog_equivalence.rs</files>
  <read-first>11-CONTEXT.md D-11, D-15, D-22, and deferred Phase 12 performance scope; .planning/research/STACK.md Criterion 0.8.2 and paired-benchmark guidance; Plans 11-06 and 11-14; root workspace/default-members and package isolation.</read-first>
  <behavior>
    - "An unpublished non-default benchmark package consumes the typed catalog and native backend."
    - "Scenario resolution and restart occur outside the measured interval, and each benchmark declares an exact logical horizon."
  </behavior>
  <action>Add criterion 0.8.2 only to a new `publish = false` liquidfun-benchmarks member outside default-members. Provide CatalogBenchmarkCase with stable slug/version, exact resolved hash/settings, explicit warmup, measured logical horizon, checkpoint validator, and restart factory. Resolve/generate and construct or restart the backend before `iter_custom` or the measured closure; measure only the declared logical ticks. Require a validated expected semantic checkpoint before timing and reject an oracle or native mismatch instead of emitting timing. Add representative rigid, joint, particle, group, mixed, query, and ray cases as smoke compilation/equivalence only; do not add thresholds, optimization claims, broad workload budgets, or platform authority.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>cargo test -p liquidfun-benchmarks --test catalog_equivalence && cargo bench -p liquidfun-benchmarks --bench catalog --no-run</automated>
  </verify>
  <acceptance-criteria>Equivalence tests pass, benchmark targets compile, every case reports exact horizon/resolved hash, resolution/restart is outside measured code, mismatched semantics block timing, and the package is unpublished/non-default.</acceptance-criteria>
  <done>Benchmarks reuse the canonical scenario source without expanding Phase 11 into performance sign-off.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| benchmark case metadata to measured execution | Untrusted input crosses into an effectful Phase 11 path. |
| benchmark dependency graph to published crate | Validated output crosses a package, process, file, or authority boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-17-01 | Tampering | benchmark case metadata to measured execution | mitigate | Validate fixed slugs, hashes, settings, horizons, and checkpoint equivalence before timing. |
| T-11-17-02 | Spoofing | benchmark dependency graph to published crate | mitigate | Keep Criterion and differential/protocol dependencies in one unpublished non-default package with package-isolation tests. |
| T-11-17-03 | Denial of service | bounded execution and artifacts | mitigate | Enforce reviewed byte, record, action, horizon, process, archive, and diagnostic limits with fail-fast timeouts. |
| T-11-17-04 | Information disclosure | CLI, CI, and artifact diagnostics | mitigate | Redact secrets, private identities, paths, raw records, stack traces, and unbounded stderr; retain only bounded semantic context. |
| T-11-17-05 | Elevation of privilege | dependency/process boundary | mitigate | Keep renderer, benchmark, protocol, differential, and C++ dependencies private and absent from liquidfun's published graph. |
| T-11-17-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped automated command, ordered Rust commit gate, and any named Markdown, workflow, package, or sanitizer checks.
</verification>

<success-criteria>
- An unpublished non-default benchmark package consumes the typed catalog and native backend.
- Scenario resolution and restart occur outside the measured interval, and each benchmark declares an exact logical horizon.
- Benchmark smoke validates semantic checkpoints before accepting timing samples.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-17-SUMMARY.md`.</output>
