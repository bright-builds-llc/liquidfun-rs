---
phase: 11-examples-headless-tooling-and-testbed
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - tools/xtask/src/inventory.rs
  - tools/xtask/src/inventory/corpus/discovery.rs
  - tools/xtask/tests/corpus_discovery.rs
  - reference/upstream-corpus.json
autonomous: true
requirements: ["TEST-03", "EXMP-01"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Oracle-enabled refresh enumerates GoogleTest declarations and the authoritative TestEntries registration table, not just files."
    - "Stable IDs and source symbols remain deterministic across repeated discovery."
    - "Cargo-only checks validate the checked snapshot without reading third_party or invoking native tools."
  artifacts:
    - path: "tools/xtask/src/inventory/corpus/discovery.rs"
      provides: "Pinned semantic declaration and registration discovery"
    - path: "reference/upstream-corpus.json"
      provides: "Machine-authoritative checked semantic snapshot"
    - path: "tools/xtask/tests/corpus_discovery.rs"
      provides: "Determinism, malformed-source, and Cargo-only tests"
  key_links:
    - from: "tools/xtask/src/inventory.rs"
      to: "tools/xtask/src/inventory/corpus/discovery.rs"
      via: "closed refresh and check commands"
      pattern: "corpus"
    - from: "tools/xtask/src/inventory/corpus/discovery.rs"
      to: "third_party/liquidfun/liquidfun/Box2D/Testbed/Tests/TestEntries.cpp"
      via: "registration parsing"
      pattern: "TestEntry"
---

<objective>
Enumerate the pinned upstream semantic corpus and maintain its checked snapshot.

Purpose: Make D-02 executable without requiring C++ or the submodule on Cargo-only checks.
Output: tools/xtask/src/inventory/corpus/discovery.rs, reference/upstream-corpus.json, tools/xtask/tests/corpus_discovery.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@third_party/liquidfun/liquidfun/Box2D/Unittests/
@third_party/liquidfun/liquidfun/Box2D/Testbed/Tests/TestEntries.cpp
@tools/xtask/src/inventory/discovery.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement semantic discovery and commit the pinned snapshot</name>
  <files>tools/xtask/src/inventory.rs, tools/xtask/src/inventory/corpus/discovery.rs, tools/xtask/tests/corpus_discovery.rs, reference/upstream-corpus.json</files>
  <read-first>11-CONTEXT.md D-02 and D-03; tools/xtask/src/inventory/discovery.rs for pinned-tree confinement and deterministic output; third_party/liquidfun/liquidfun/Box2D/Unittests for actual TEST and TEST_F declarations; TestEntries.cpp for the authoritative testbed registry; reference/upstream-lock.toml for the exact revision.</read-first>
  <behavior>
    - "Oracle-enabled refresh enumerates GoogleTest declarations and the authoritative TestEntries registration table, not just files."
    - "Stable IDs and source symbols remain deterministic across repeated discovery."
  </behavior>
  <action>Add closed `inventory corpus refresh` and `inventory corpus check-snapshot` paths. Refresh may run only with the verified initialized pinned checkout; enumerate actual GoogleTest suite/case declarations, TestEntries registrations, and source-defined example scenarios, preserving source path and symbol/registration identity. Refuse symlinks, paths outside the pinned root, duplicate semantic registrations, malformed macros, unknown source files, and revision mismatch. Merge discovered identities with existing reviewed classification fields without silently creating terminal dispositions, sort by stable source identity, and atomically write reference/upstream-corpus.json. Cargo-only check parses and canonicalizes the tracked snapshot entirely in memory and never consults third_party. Add repeated-run byte identity and isolated-copy tests.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cargo test -p xtask --test corpus_discovery && cargo xtask inventory corpus check-snapshot</automated>
  </verify>
  <acceptance-criteria>Tests pass; repeated verified refreshes produce identical canonical bytes; the checked snapshot contains item-level test and registration identities; Cargo-only validation succeeds with third_party absent; stale, duplicate, malformed, or wrong-revision input fails.</acceptance-criteria>
  <done>The semantic upstream corpus is exhaustively and reproducibly discoverable with an isolated checked snapshot.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| pinned source tree to generated snapshot | Untrusted or effectful data crosses into Phase 11 domain logic. |
| source declarations to bounded parser | Validated authority crosses into downstream consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-02-01 | Tampering | pinned source tree to generated snapshot | mitigate | Verify gitlink/revision first, reject symlinks and traversal, parse only allowlisted roots, and write atomically. |
| T-11-02-02 | Spoofing | source declarations to bounded parser | mitigate | Cap files, bytes, declarations, token length, and diagnostics before allocation or report generation. |
| T-11-02-03 | Denial of service | bounded collections and work | mitigate | Enforce reviewed byte, record, action, entity, and diagnostic limits before allocation or execution. |
| T-11-02-04 | Information disclosure | errors and logs | mitigate | Emit bounded semantic IDs and categories; never expose private indices, raw records, pointers, secrets, or unbounded stderr. |
| T-11-02-05 | Elevation of privilege | dependency and process boundary | mitigate | Keep production Cargo and published liquidfun free of renderer, oracle, protocol, and foreign runtime dependencies. |
| T-11-02-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped automated command, then the repository Rust commit gate. Confirm tracked authority changes are deterministic and read-only checks leave the worktree clean.
</verification>

<success-criteria>
- Oracle-enabled refresh enumerates GoogleTest declarations and the authoritative TestEntries registration table, not just files.
- Stable IDs and source symbols remain deterministic across repeated discovery.
- Cargo-only checks validate the checked snapshot without reading third_party or invoking native tools.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-02-SUMMARY.md`.</output>
