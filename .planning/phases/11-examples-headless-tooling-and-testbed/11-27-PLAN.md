---
phase: 11-examples-headless-tooling-and-testbed
plan: "27"
type: execute
wave: 11
depends_on: ["11-26"]
files_modified:
  - crates/liquidfun-testbed/src/ui/differences.rs
  - crates/liquidfun-testbed/src/ui/inspector.rs
  - crates/liquidfun-testbed/src/ui/layout.rs
  - crates/liquidfun-testbed/src/ui/accessibility.rs
  - crates/liquidfun-testbed/src/screenshot.rs
  - crates/liquidfun-testbed/tests/visual_contract.rs
autonomous: true
requirements: ["EXMP-04", "EXMP-05"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Overlay and side-by-side modes render the same canonical comparison entries, camera, focus keys, and policy labels."
    - "Responsive widths, drawers/sheets, focus order, 44px targets, contrast/state redundancy, and minimum-window behavior follow the approved contract."
    - "Mismatch/error/oracle-unavailable states retain last valid checkpoints, bounded details, and correct semantic versus diagnostic authority labels."
  artifacts:
    - path: "crates/liquidfun-testbed/src/ui/differences.rs"
      provides: "Canonical path-ordered diff list and focused overlay"
    - path: "crates/liquidfun-testbed/src/ui/layout.rs"
      provides: "Responsive desktop/drawer/sheet/minimum-window layout"
    - path: "crates/liquidfun-testbed/tests/visual_contract.rs"
      provides: "UI-SPEC contract and deterministic diagnostic capture tests"
  key_links:
    - from: "crates/liquidfun-testbed/src/ui/differences.rs"
      to: "crates/liquidfun-differential/src/comparison_model.rs"
      via: "immutable canonical entries"
      pattern: "ComparisonEntry"
    - from: "crates/liquidfun-testbed/src/ui/layout.rs"
      to: "crates/liquidfun-testbed/src/app/state.rs"
      via: "presentation-only responsive state"
      pattern: "AppState"
---

<objective>
Complete semantic differences, responsive layout, accessibility, error states, and diagnostic capture.

Purpose: Deliver EXMP-05 and the approved UI contract from the shared comparison model without renderer-side reinterpretation.
Output: crates/liquidfun-testbed/src/ui/differences.rs, crates/liquidfun-testbed/src/ui/layout.rs, crates/liquidfun-testbed/tests/visual_contract.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@crates/liquidfun-differential/src/comparison_model.rs
@crates/liquidfun-testbed/CAPABILITY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the full differences, responsive, accessibility, and error contract</name>
  <files>crates/liquidfun-testbed/src/ui/differences.rs, crates/liquidfun-testbed/src/ui/inspector.rs, crates/liquidfun-testbed/src/ui/layout.rs, crates/liquidfun-testbed/src/ui/accessibility.rs, crates/liquidfun-testbed/src/screenshot.rs, crates/liquidfun-testbed/tests/visual_contract.rs</files>
  <read-first>11-CONTEXT.md D-15, D-16, and D-19 through D-21; complete UI-SPEC Difference Presentation, responsive table, state/copy, accessibility, palette, layout, and provenance sections; Plans 11-13 and 11-25/26; selected renderer capability record.</read-first>
  <behavior>
    - "Overlay and side-by-side modes render the same canonical comparison entries, camera, focus keys, and policy labels."
    - "Responsive widths, drawers/sheets, focus order, 44px targets, contrast/state redundancy, and minimum-window behavior follow the approved contract."
  </behavior>
  <action>Default to overlay when both backends exist and provide Overlay/Side by side as presentation-only controls. Render matches at 35 percent, Rust solid orange with R, oracle dashed purple with O, within-policy amber diamond plus policy, mismatch red cross/halo/path, and synchronized cameras. List differences in canonical semantic-path order, preserve an explicit canonical sort option, focus stable primitive keys, show bounded Rust/oracle values and policy, and implement wrapping previous/next with ordinal announcement. Add inspector Run/Observe/Differences/Provenance tabs and exact empty/loading/error/oracle-unavailable copy. Implement widths at 1280, 960-1279, 720-959, below 720, and below 640x480 exactly; drawers/sheets manage focus and return it, resizing/DPI/panel changes preserve controller/camera-world/selection identity. Enforce 44px targets, persistent 2px focus ring, contrast metadata, icon/text/shape redundancy, selectable values, no flashing, reduced motion under 200ms, and one concise focused-mismatch announcement. Diagnostic screenshots include provenance/hash and explicit non-authority text but never enter comparison/evidence.</action>
  <pre-commit>Before any tracked task commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>cargo test -p liquidfun-testbed --test visual_contract && cargo run -p liquidfun-testbed -- --visual-contract-check --fixture crates/liquidfun-differential/tests/fixtures/catalog/phase11-v1.json --output target/testbed-visual-contract</automated>
  </verify>
  <acceptance-criteria>Tests/check artifacts cover every breakpoint, focus/keyboard path, state/copy, contrast/target rule, overlay/side-by-side equivalence, canonical diff order/focus/navigation, bounded diagnostics, provenance, and screenshot non-authority; UI does not re-read engine state or policies.</acceptance-criteria>
  <done>The optional testbed provides a complete accessible semantic comparison experience over the headless authority.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| comparison records to visual focus and values | Untrusted state crosses into an effectful or authoritative Phase 11 operation. |
| responsive/input/screenshot presentation to simulation/evidence | Validated output crosses a package, process, file, UI, or evidence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-27-01 | Tampering | comparison records to visual focus and values | mitigate | Accept only validated bounded ComparisonModel input, preserve canonical paths and stable keys, and never reinterpret policies. |
| T-11-27-02 | Spoofing | responsive/input/screenshot presentation to simulation/evidence | mitigate | Keep layout/camera/focus/capture as presentation state, emit no controller ticks, and forbid pixels/timing from semantic evidence. |
| T-11-27-03 | Denial of service | resource use | mitigate | Enforce reviewed input, geometry, image, archive, process, horizon, diagnostic, and UI limits with fail-fast timeouts. |
| T-11-27-04 | Information disclosure | diagnostics, UI, CI, and reports | mitigate | Redact secrets, raw records, private indices, pointers, stack traces, and unbounded stderr; expose only bounded semantic context and safe URLs. |
| T-11-27-05 | Elevation of privilege | dependency, renderer, and oracle isolation | mitigate | Keep all renderer and C++ capabilities private, unpublished, non-default, least-privilege, and unreachable from liquidfun production dependencies. |
| T-11-27-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and all relevant Rust, C++, workflow, package, corpus, Markdown, renderer, or exact-reference gates stated by the task.
</verification>

<success-criteria>
- Overlay and side-by-side modes render the same canonical comparison entries, camera, focus keys, and policy labels.
- Responsive widths, drawers/sheets, focus order, 44px targets, contrast/state redundancy, and minimum-window behavior follow the approved contract.
- Mismatch/error/oracle-unavailable states retain last valid checkpoints, bounded details, and correct semantic versus diagnostic authority labels.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-27-SUMMARY.md`.</output>
