---
phase: 11-examples-headless-tooling-and-testbed
plan: "08"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/src/world.rs
  - crates/liquidfun/src/world/diagnostics.rs
  - crates/liquidfun/src/world/observation.rs
  - crates/liquidfun/src/world/observation/profile.rs
  - crates/liquidfun/src/world/step.rs
  - crates/liquidfun/tests/world_observations.rs
autonomous: true
requirements: ["RIGD-10"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Default-feature consumers can inspect counts, tree metrics, current contacts, particle contacts, broad-phase observations, and particle statistics through public semantic IDs."
    - "Profile phase names and bounded durations are available as diagnostics but excluded from equality, hashing, and parity checkpoints."
    - "Observation collection is owned or borrow-scoped, bounded, source-ordered, and contains no private coordinates."
  artifacts:
    - path: "crates/liquidfun/src/world/observation.rs"
      provides: "Public semantic observation snapshot and errors"
    - path: "crates/liquidfun/src/world/observation/profile.rs"
      provides: "Diagnostic-only named phase profile"
    - path: "crates/liquidfun/tests/world_observations.rs"
      provides: "Default-feature public API coverage"
  key_links:
    - from: "crates/liquidfun/src/world/observation.rs"
      to: "crates/liquidfun/src/world/diagnostics.rs"
      via: "reuse exact counts and tree metrics"
      pattern: "WorldDiagnostics"
    - from: "crates/liquidfun/src/world/observation.rs"
      to: "crates/liquidfun/src/particle/view.rs"
      via: "stable particle/body contact translation"
      pattern: "ParticleContactView"
---

<objective>
Expose stable public semantic observations and diagnostic phase profiles.

Purpose: Complete the public RIGD-10 boundary without exposing arenas, contacts, tree nodes, or dense particle rows.
Output: crates/liquidfun/src/world/observation.rs, crates/liquidfun/src/world/observation/profile.rs, crates/liquidfun/tests/world_observations.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@crates/liquidfun/src/world/diagnostics.rs
@crates/liquidfun/src/world/step.rs
@crates/liquidfun/src/particle/view.rs
@crates/liquidfun/src/particle/statistics.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make layered observability a documented default-feature API</name>
  <files>crates/liquidfun/src/lib.rs, crates/liquidfun/src/world.rs, crates/liquidfun/src/world/diagnostics.rs, crates/liquidfun/src/world/observation.rs, crates/liquidfun/src/world/observation/profile.rs, crates/liquidfun/src/world/step.rs, crates/liquidfun/tests/world_observations.rs</files>
  <read-first>11-CONTEXT.md D-12 and D-15; current diagnostics, step report, contact snapshots, query occurrences, particle views/statistics, world.rs feature gates, and lib.rs exports; RIGD-10 requirement; UI-SPEC Observe and profile contracts.</read-first>
  <behavior>
    - "Default-feature consumers can inspect counts, tree metrics, current contacts, particle contacts, broad-phase observations, and particle statistics through public semantic IDs."
    - "Profile phase names and bounded durations are available as diagnostics but excluded from equality, hashing, and parity checkpoints."
  </behavior>
  <action>Remove the differential-internals/doc-hidden restriction from the curated WorldDiagnostics surface while leaving semantic reconstruction policy explicit. Add reviewed WorldObservationLimits, WorldObservation, ContactObservation, ParticleContactObservation, ParticleBodyContactObservation, BroadPhaseObservation, and typed collection errors. Collect stable body/fixture/system/particle IDs, source-significant occurrence order, exact counts/tree metrics, public AABBs/query semantics, and particle statistics using existing public or crate-private translation seams; never expose contact-manager indices, proxy IDs, arena slots, tree nodes, or dense rows. Add opt-in `step_profiled` or an equivalent curated API returning StepReport plus a separate DiagnosticStepProfile whose closed phase names and Duration values are explicitly nondeterministic and excluded from PartialEq/hash/checkpoint conversion. Keep ordinary `step` behavior unchanged. Document units and bounds.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cargo test -p liquidfun --all-features --test world_observations && cargo test -p liquidfun --all-features --test world_diagnostics && cargo test -p liquidfun --all-features --test particle_views</automated>
  </verify>
  <acceptance-criteria>Tests pass through the public default-feature API; all required observations resolve stable semantic IDs; at-limit/one-over bounds are tested; private coordinates are absent; profile durations cannot enter deterministic equality or checkpoint conversion.</acceptance-criteria>
  <done>Consumers can inspect the full renderer-neutral diagnostic surface required by RIGD-10.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| private world state to public observations | Data crosses into a trusted Phase 11 component. |
| wall-clock profiling to compatibility evidence | Trusted semantic output crosses into another consumer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-08-01 | Tampering | private world state to public observations | mitigate | Translate only stable IDs and owned values, preflight limits, and return typed invariant errors before partial output. |
| T-11-08-02 | Spoofing | wall-clock profiling to compatibility evidence | mitigate | Keep profiles in a distinct non-hashable diagnostic type and prohibit conversion into canonical checkpoints. |
| T-11-08-03 | Denial of service | resource use | mitigate | Enforce reviewed bytes, records, actions, geometry, processes, durations, and diagnostics before work or allocation. |
| T-11-08-04 | Information disclosure | errors and diagnostics | mitigate | Emit bounded semantic categories and IDs; redact raw records, private indices, pointers, stack traces, secrets, and unbounded stderr. |
| T-11-08-05 | Elevation of privilege | dependency/process isolation | mitigate | Keep renderer and C++ execution private, process-isolated, unpublished, and unreachable from the liquidfun production dependency graph. |
| T-11-08-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and the ordered repository Rust commit gate. For C++ changes, also run the named protocol target under the reviewed preset.
</verification>

<success-criteria>
- Default-feature consumers can inspect counts, tree metrics, current contacts, particle contacts, broad-phase observations, and particle statistics through public semantic IDs.
- Profile phase names and bounded durations are available as diagnostics but excluded from equality, hashing, and parity checkpoints.
- Observation collection is owned or borrow-scoped, bounded, source-ordered, and contains no private coordinates.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-08-SUMMARY.md`.</output>
