---
phase: 11-examples-headless-tooling-and-testbed
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/liquidfun-test-protocol/Cargo.toml
  - crates/liquidfun-test-protocol/src/lib.rs
  - crates/liquidfun-test-protocol/src/catalog.rs
  - crates/liquidfun-test-protocol/src/catalog/model.rs
  - crates/liquidfun-test-protocol/src/catalog/resolve.rs
  - crates/liquidfun-test-protocol/tests/catalog_resolution.rs
autonomous: true
requirements: ["EXMP-02", "EXMP-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "A stable slug is distinct from display title and resolves through a versioned typed catalog definition."
    - "A resolved run records schema, scenario, generator, seed, exact settings, canonical bytes, and SHA-256 identity."
    - "All setup and interactive operations are closed typed actions with stable action/entity IDs, exact f32 bits, explicit order, and bounds."
  artifacts:
    - path: "crates/liquidfun-test-protocol/src/catalog/model.rs"
      provides: "Catalog, run identity, action, and mapping contracts"
    - path: "crates/liquidfun-test-protocol/src/catalog/resolve.rs"
      provides: "Pure deterministic resolver and canonical encoder"
    - path: "crates/liquidfun-test-protocol/tests/catalog_resolution.rs"
      provides: "Named, seeded, boundary, and replay tests"
  key_links:
    - from: "crates/liquidfun-test-protocol/src/catalog/resolve.rs"
      to: "crates/liquidfun-test-protocol/src/scenario/rigid_world.rs"
      via: "resolved closed actions reuse the existing semantic protocol"
      pattern: "RigidWorldAction"
    - from: "crates/liquidfun-test-protocol/src/catalog/resolve.rs"
      to: "sha2"
      via: "content hash of canonical resolved bytes"
      pattern: "Sha256"
---

<objective>
Define canonical catalog entries and immutable resolved scenario bytes.

Purpose: Make D-06 through D-08 and D-07 replay identity concrete before backends, controllers, or renderers exist.
Output: crates/liquidfun-test-protocol/src/catalog/model.rs, crates/liquidfun-test-protocol/src/catalog/resolve.rs, crates/liquidfun-test-protocol/tests/catalog_resolution.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@crates/liquidfun-test-protocol/src/scenario.rs
@crates/liquidfun-test-protocol/src/scenario/rigid_world.rs
@crates/liquidfun-test-protocol/src/float_bits.rs
@crates/liquidfun-test-protocol/src/ids.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement catalog contracts and pure deterministic resolution</name>
  <files>Cargo.toml, Cargo.lock, crates/liquidfun-test-protocol/Cargo.toml, crates/liquidfun-test-protocol/src/lib.rs, crates/liquidfun-test-protocol/src/catalog.rs, crates/liquidfun-test-protocol/src/catalog/model.rs, crates/liquidfun-test-protocol/src/catalog/resolve.rs, crates/liquidfun-test-protocol/tests/catalog_resolution.rs</files>
  <read-first>11-CONTEXT.md D-06 through D-08 and D-23; protocol scenario, rigid-world, float_bits, ids, limits, and codec modules for exact transport and closed boundaries; root Cargo.toml for private dependency placement; .planning/research/STACK.md for the exact rand_chacha 0.10.0 generator recommendation.</read-first>
  <behavior>
    - "A stable slug is distinct from display title and resolves through a versioned typed catalog definition."
    - "A resolved run records schema, scenario, generator, seed, exact settings, canonical bytes, and SHA-256 identity."
  </behavior>
  <action>Add the exact private rand_chacha =0.10.0 dependency without changing liquidfun dependencies. Define validated CatalogSlug, ScenarioVersion, CatalogSchemaVersion, GeneratorId, GeneratorVersion, RunSettings, RunIdentity, ScenarioEligibility, ScenarioActionId, typed action schedule, checkpoint declaration, CatalogDefinition, and ResolvedScenario. Resolver input accepts a stable slug, optional seed only when declared, and validated timestep plus velocity/position/particle iterations from 1 through 1024. Use a named ChaCha variant, explicit generator version, exact FloatBits, deterministic entity/action ordinals, reviewed count/byte limits, canonical JSON bytes, and SHA-256 over those exact bytes. Persistable resolved bytes must decode back to the identical domain value and hash; display title never participates as identity. Do not add physics, filesystem, clock, renderer, or backend callbacks to the resolver.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cargo test -p liquidfun-test-protocol --test catalog_resolution</automated>
  </verify>
  <acceptance-criteria>The test target passes named and seeded golden cases, two-run byte identity, exact settings/hash identity, decode replay, at-limit acceptance, one-over rejection, invalid seed/settings rejection, and stable slug versus mutable title behavior.</acceptance-criteria>
  <done>Every consumer can share one immutable, bounded, replayable resolved plan instead of backend-specific scenario logic.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| catalog input to deterministic resolver | Untrusted or effectful data crosses into Phase 11 domain logic. |
| resolved bytes to replay identity | Validated authority crosses into downstream consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-03-01 | Tampering | catalog input to deterministic resolver | mitigate | Parse slugs, versions, seeds, exact float bits, settings, and bounds into domain types before generation. |
| T-11-03-02 | Spoofing | resolved bytes to replay identity | mitigate | Canonicalize one closed schema, hash exact bytes, verify hash on decode, and reject oversized or unknown records. |
| T-11-03-03 | Denial of service | bounded collections and work | mitigate | Enforce reviewed byte, record, action, entity, and diagnostic limits before allocation or execution. |
| T-11-03-04 | Information disclosure | errors and logs | mitigate | Emit bounded semantic IDs and categories; never expose private indices, raw records, pointers, secrets, or unbounded stderr. |
| T-11-03-05 | Elevation of privilege | dependency and process boundary | mitigate | Keep production Cargo and published liquidfun free of renderer, oracle, protocol, and foreign runtime dependencies. |
| T-11-03-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped automated command, then the repository Rust commit gate. Confirm tracked authority changes are deterministic and read-only checks leave the worktree clean.
</verification>

<success-criteria>
- A stable slug is distinct from display title and resolves through a versioned typed catalog definition.
- A resolved run records schema, scenario, generator, seed, exact settings, canonical bytes, and SHA-256 identity.
- All setup and interactive operations are closed typed actions with stable action/entity IDs, exact f32 bits, explicit order, and bounds.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-03-SUMMARY.md`.</output>
