---
phase: 11-examples-headless-tooling-and-testbed
plan: "14"
type: execute
wave: 5
depends_on: ["11-07", "11-11", "11-12", "11-13"]
files_modified:
  - crates/liquidfun-differential/src/runner.rs
  - crates/liquidfun-differential/src/supervisor.rs
  - crates/liquidfun-differential/src/supervisor/catalog.rs
  - crates/liquidfun-differential/src/fixtures/replay.rs
  - crates/liquidfun-differential/src/failure_bundle.rs
  - crates/liquidfun-differential/tests/catalog_round_trip.rs
  - crates/liquidfun-differential/tests/catalog_failures.rs
autonomous: true
requirements: ["EXMP-02", "EXMP-03", "EXMP-05"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "One resolved byte sequence feeds native execution, oracle execution, replay, comparison, minimization, and failure persistence."
    - "Process timeouts, crashes, malformed records, provenance failures, and resource failures remain distinct from physics mismatches."
    - "Failure bundles persist exact resolved bytes, action/checkpoint log, comparison, bounded diagnostics, and provenance; a seed alone is never stored as replay authority."
  artifacts:
    - path: "crates/liquidfun-differential/src/supervisor/catalog.rs"
      provides: "Catalog request supervision and bounded capture"
    - path: "crates/liquidfun-differential/src/fixtures/replay.rs"
      provides: "Resolved-byte replay integration"
    - path: "crates/liquidfun-differential/src/failure_bundle.rs"
      provides: "Phase 11 durable replay bundle"
  key_links:
    - from: "crates/liquidfun-differential/src/runner.rs"
      to: "crates/liquidfun-differential/src/session.rs"
      via: "headless session orchestration"
      pattern: "run_catalog"
    - from: "crates/liquidfun-differential/src/supervisor/catalog.rs"
      to: "tools/reference liquidfun-reference"
      via: "long-lived JSONL process"
      pattern: "catalog_run"
---

<objective>
Integrate catalog sessions with native/oracle supervision, replay, and failure bundles.

Purpose: Route Phase 11 through the mature harness and preserve provenance, isolation, and diagnosis guarantees.
Output: crates/liquidfun-differential/src/supervisor/catalog.rs, crates/liquidfun-differential/src/fixtures/replay.rs, crates/liquidfun-differential/src/failure_bundle.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@crates/liquidfun-differential/src/runner.rs
@crates/liquidfun-differential/src/supervisor/rigid_world.rs
@crates/liquidfun-differential/src/fixtures/replay.rs
@crates/liquidfun-differential/src/failure_bundle.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire catalog runs through the existing harness lifecycle</name>
  <files>crates/liquidfun-differential/src/runner.rs, crates/liquidfun-differential/src/supervisor.rs, crates/liquidfun-differential/src/supervisor/catalog.rs, crates/liquidfun-differential/src/fixtures/replay.rs, crates/liquidfun-differential/src/failure_bundle.rs, crates/liquidfun-differential/tests/catalog_round_trip.rs, crates/liquidfun-differential/tests/catalog_failures.rs</files>
  <read-first>11-CONTEXT.md D-07, D-10, D-16, D-23, and D-26; Plans 11-07 and 11-11 through 11-13; existing runner outcomes, child supervisor profiles/drains, fixture replay, minimizer signatures, failure bundle confinement, and provenance validation.</read-first>
  <behavior>
    - "One resolved byte sequence feeds native execution, oracle execution, replay, comparison, minimization, and failure persistence."
    - "Process timeouts, crashes, malformed records, provenance failures, and resource failures remain distinct from physics mismatches."
  </behavior>
  <action>Add run_catalog_resolved and compare_catalog entrypoints that accept a validated registry slug/seed/settings or exact resolved bytes, resolve only once, then pass identical bytes to NativeCatalogBackend and the supervised C++ catalog request. Extend the synchronous child state machine with catalog records while retaining concurrent bounded stdout/stderr drains, deadlines, poison/kill/reap behavior, reset proof, and provenance handshake. Validate both checkpoints before building the ComparisonModel. Extend replay and failure bundles to require exact resolved bytes/hash, action log, checkpoint schedule, native/oracle checkpoints, comparison entries, first-divergence signature, build identities, bounded stderr, and controller state; reject seed-only or path-escaping bundles. Preserve distinct Match/PhysicsMismatch/HarnessFailure outcomes and existing scenarios unchanged.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cargo test -p liquidfun-differential --test catalog_round_trip && cargo test -p liquidfun-differential --test catalog_failures</automated>
  </verify>
  <acceptance-criteria>Tests prove one resolved payload across native/oracle/replay, D0 native repeatability, correct pause/step/restart capture, malformed/crash/timeout/provenance/reset classification, bounded failure bundles, path confinement, and unchanged legacy runner behavior.</acceptance-criteria>
  <done>The full Phase 11 headless comparison workflow uses the established isolated harness end to end.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| catalog session to child process | Data crosses into a trusted Phase 11 component. |
| failure artifacts to filesystem | Trusted semantic output crosses into another consumer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-14-01 | Tampering | catalog session to child process | mitigate | Use allowlisted executable/preset/profile, exact request bytes, timeouts, concurrent bounded drains, kill/reap, and reset proof. |
| T-11-14-02 | Spoofing | failure artifacts to filesystem | mitigate | Derive confined target paths from typed IDs, reject links/traversal/collisions, cap artifact sizes, redact diagnostics, and publish atomically. |
| T-11-14-03 | Denial of service | resource use | mitigate | Enforce reviewed bytes, records, actions, geometry, processes, durations, and diagnostics before work or allocation. |
| T-11-14-04 | Information disclosure | errors and diagnostics | mitigate | Emit bounded semantic categories and IDs; redact raw records, private indices, pointers, stack traces, secrets, and unbounded stderr. |
| T-11-14-05 | Elevation of privilege | dependency/process isolation | mitigate | Keep renderer and C++ execution private, process-isolated, unpublished, and unreachable from the liquidfun production dependency graph. |
| T-11-14-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and the ordered repository Rust commit gate. For C++ changes, also run the named protocol target under the reviewed preset.
</verification>

<success-criteria>
- One resolved byte sequence feeds native execution, oracle execution, replay, comparison, minimization, and failure persistence.
- Process timeouts, crashes, malformed records, provenance failures, and resource failures remain distinct from physics mismatches.
- Failure bundles persist exact resolved bytes, action/checkpoint log, comparison, bounded diagnostics, and provenance; a seed alone is never stored as replay authority.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-14-SUMMARY.md`.</output>
