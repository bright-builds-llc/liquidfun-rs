---
phase: 11-examples-headless-tooling-and-testbed
plan: "22"
type: execute
wave: 10
depends_on: ["11-21"]
files_modified:
  - TESTING.md
  - target/phase11-evidence/run.json
  - target/phase11-evidence/phase11-canonical
  - target/phase11-evidence/phase11-sanitizer
autonomous: true
requirements: ["TEST-03", "EXMP-01", "EXMP-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Exactly one new Oracle CI workflow_dispatch run targets the clean pushed implementation SHA."
    - "Canonical and sanitizer artifacts are distinct, successful, same-run, same-SHA, and locked-stack."
    - "Fresh downloads pass archive safety and complete exact-ref semantic validation before any ledger edit."
  artifacts:
    - path: "target/phase11-evidence/run.json"
      provides: "Fresh live run, job, artifact, SHA, and digest snapshot"
    - path: "target/phase11-evidence/phase11-canonical"
      provides: "Independently downloaded canonical artifact"
    - path: "target/phase11-evidence/phase11-sanitizer"
      provides: "Independently downloaded sanitizer artifact"
  key_links:
    - from: "target/phase11-evidence/run.json"
      to: "target/phase11-evidence/phase11-canonical"
      via: "same-run/SHA live artifact identity"
      pattern: "run_id"
    - from: "target/phase11-evidence/run.json"
      to: "target/phase11-evidence/phase11-sanitizer"
      via: "distinct sanitizer identity and digest"
      pattern: "artifact_id"
---

<objective>
Acquire and independently validate one fresh canonical Phase 11 authority set.

Purpose: Cross D2 to D1 once for the already-committed headless implementation before compatibility promotion.
Output: target/phase11-evidence/run.json, target/phase11-evidence/phase11-canonical, target/phase11-evidence/phase11-sanitizer.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-21-SUMMARY.md
@.github/workflows/oracle.yml
@tools/xtask/src/phase11_evidence.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dispatch once, redownload safely, and exact-ref validate</name>
  <files>TESTING.md, target/phase11-evidence/run.json, target/phase11-evidence/phase11-canonical, target/phase11-evidence/phase11-sanitizer</files>
  <read-first>11-CONTEXT.md D-26; Plan 11-21 summary and workflow; tools/xtask Phase 11 authority/path validator; AGENTS exact Rust pre-commit sequence; git remote/upstream configuration; TESTING acquisition procedure.</read-first>
  <behavior>
    - "Exactly one new Oracle CI workflow_dispatch run targets the clean pushed implementation SHA."
    - "Canonical and sanitizer artifacts are distinct, successful, same-run, same-SHA, and locked-stack."
  </behavior>
  <action>Treat this as authority acquisition for an already committed implementation. Require an empty tracked worktree, non-detached tracked branch, record IMPLEMENTATION_SHA, run the exact four Rust gates with no mutation, push that existing SHA once, fetch, and require local equals remote. Query current Phase 11 runs to prove zero for this SHA, dispatch Oracle CI exactly once, capture the run ID, and watch terminal success; never redispatch a physics/test failure. Write only target/phase11-evidence/run.json from fresh API queries with full SHA, workflow/event, job/artifact IDs/names/conclusions/digests/sizes/URLs/timestamps/toolchains/platform. Inspect both archives before extraction, download into fresh fixed target directories, re-query live metadata, and run exact-ref validation with historical failures denied. Update TESTING only with immutable URLs and the reusable procedure after success; do not edit compatibility/corpus authority. Skip the automatic task commit for target-only acquisition until the scoped TESTING change, and require HEAD remains IMPLEMENTATION_SHA before that documentation commit.</action>
  <pre-commit>Before any tracked task commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>RUN_ID=$(jq -r .run_id target/phase11-evidence/run.json) && HEAD_SHA=$(jq -r .head_sha target/phase11-evidence/run.json) && test "$HEAD_SHA" = "$(git rev-parse HEAD)" && gh run view "$RUN_ID" --json conclusion,headSha,event,workflowName | jq -e --arg sha "$HEAD_SHA" '.conclusion=="success" and .headSha==$sha and .event=="workflow_dispatch" and .workflowName=="Oracle CI"' && cargo xtask phase11-evidence validate --mode exact-ref --run-json target/phase11-evidence/run.json --canonical-dir target/phase11-evidence/phase11-canonical --sanitizer-dir target/phase11-evidence/phase11-sanitizer</automated>
  </verify>
  <acceptance-criteria>There is one successful current run and no duplicate dispatch; both safe fresh downloads validate all identity/content invariants; TESTING records immutable procedure; no corpus or compatibility authority changed.</acceptance-criteria>
  <done>A complete current same-run D1 authority set is validated and ready for separate promotion.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| local git state and GitHub API to dispatch | Untrusted state crosses into an effectful or authoritative Phase 11 operation. |
| remote archives to local authority | Validated output crosses a package, process, file, UI, or evidence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-22-01 | Tampering | local git state and GitHub API to dispatch | mitigate | Require clean immutable SHA, push/fetch equality, zero-before/exactly-one-after cardinality, and fresh API capture. |
| T-11-22-02 | Spoofing | remote archives to local authority | mitigate | Inspect before extraction, isolate targets, recompute hashes, validate same-run identities and complete semantics, and deny unsafe/mixed/stale data. |
| T-11-22-03 | Denial of service | resource use | mitigate | Enforce reviewed input, geometry, image, archive, process, horizon, diagnostic, and UI limits with fail-fast timeouts. |
| T-11-22-04 | Information disclosure | diagnostics, UI, CI, and reports | mitigate | Redact secrets, raw records, private indices, pointers, stack traces, and unbounded stderr; expose only bounded semantic context and safe URLs. |
| T-11-22-05 | Elevation of privilege | dependency, renderer, and oracle isolation | mitigate | Keep all renderer and C++ capabilities private, unpublished, non-default, least-privilege, and unreachable from liquidfun production dependencies. |
| T-11-22-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and all relevant Rust, C++, workflow, package, corpus, Markdown, renderer, or exact-reference gates stated by the task.
</verification>

<success-criteria>
- Exactly one new Oracle CI workflow_dispatch run targets the clean pushed implementation SHA.
- Canonical and sanitizer artifacts are distinct, successful, same-run, same-SHA, and locked-stack.
- Fresh downloads pass archive safety and complete exact-ref semantic validation before any ledger edit.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-22-SUMMARY.md`.</output>
