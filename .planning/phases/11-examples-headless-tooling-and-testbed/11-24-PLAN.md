---
phase: 11-examples-headless-tooling-and-testbed
plan: "24"
type: execute
wave: 8
depends_on: ["11-13", "11-18"]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/liquidfun-testbed/Cargo.toml
  - crates/liquidfun-testbed/src/main.rs
  - crates/liquidfun-testbed/src/capability.rs
  - crates/liquidfun-testbed/tests/capability.rs
  - crates/liquidfun-testbed/CAPABILITY.md
autonomous: true
requirements: ["EXMP-04", "EXMP-05", "EXMP-06"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Macroquad 0.4.15 is tried first only after the headless gate and retained only if every named capability passes."
    - "A heavier winit/wgpu/egui stack is selected only after a concrete recorded Macroquad failure in an allowed category."
    - "The selected package is unpublished, non-default, renderer-only, and consumes immutable controller/comparison inputs."
  artifacts:
    - path: "crates/liquidfun-testbed/src/capability.rs"
      provides: "Executable renderer capability matrix"
    - path: "crates/liquidfun-testbed/CAPABILITY.md"
      provides: "Recorded measurements, decision, and any concrete fallback trigger"
    - path: "crates/liquidfun-testbed/Cargo.toml"
      provides: "Private selected renderer dependencies"
  key_links:
    - from: "crates/liquidfun-testbed/src/capability.rs"
      to: "crates/liquidfun-differential/src/session.rs"
      via: "passive controller input contract"
      pattern: "SessionController"
    - from: "crates/liquidfun-testbed/src/capability.rs"
      to: "crates/liquidfun-differential/src/comparison_model.rs"
      via: "immutable comparison input"
      pattern: "ComparisonModel"
---

<objective>
Run the post-headless renderer capability gate and select the private adapter stack.

Purpose: Honor D-17 and D-18 with executable evidence before visual implementation depends on a renderer.
Output: crates/liquidfun-testbed/src/capability.rs, crates/liquidfun-testbed/CAPABILITY.md, crates/liquidfun-testbed/Cargo.toml.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-18-SUMMARY.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute the Macroquad-first capability matrix and record the selected stack</name>
  <files>Cargo.toml, Cargo.lock, crates/liquidfun-testbed/Cargo.toml, crates/liquidfun-testbed/src/main.rs, crates/liquidfun-testbed/src/capability.rs, crates/liquidfun-testbed/tests/capability.rs, crates/liquidfun-testbed/CAPABILITY.md</files>
  <read-first>11-CONTEXT.md D-17 through D-19 and D-22; UI-SPEC complete design/interaction/accessibility/responsive contracts; Plan 11-18 headless gate summary; STACK Macroquad 0.4.15 and winit 0.30.13/wgpu 30/egui 0.35 fallback criteria; root workspace/package isolation.</read-first>
  <behavior>
    - "Macroquad 0.4.15 is tried first only after the headless gate and retained only if every named capability passes."
    - "A heavier winit/wgpu/egui stack is selected only after a concrete recorded Macroquad failure in an allowed category."
  </behavior>
  <action>Add an unpublished non-default liquidfun-testbed package and first pin Macroquad 0.4.15 only there. Implement a deterministic capability command that loads immutable fixture snapshots, renders rigid contacts/normals, particle contacts/colors, broad-phase AABBs, profiles, synchronized overlay and side-by-side differences, focus halos/labels, semantic capture acknowledgement, diagnostic screenshot, keyboard focus, dense text, DPI/resizing, and minimum 640x480 behavior. Record objective measurements and artifact hashes in CAPABILITY.md. Retain Macroquad only if every required capability passes. If any fails, record the exact allowed category, reproduction command, expected versus observed result, and artifact, remove Macroquad, pin winit 0.30.13 plus wgpu 30 plus egui 0.35 only in the same private package, and rerun the identical matrix; no fallback is allowed for preference or aesthetics. Bevy and renderer-owned simulation schedules are prohibited.</action>
  <pre-commit>Before any tracked task commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>cargo test -p liquidfun-testbed --test capability && cargo run -p liquidfun-testbed -- --capability-check --fixture crates/liquidfun-differential/tests/fixtures/catalog/phase11-v1.json --output target/testbed-capability && test -s crates/liquidfun-testbed/CAPABILITY.md && if cargo tree -p liquidfun --edges normal | rg -q '(^|[[:space:]])(macroquad|winit|wgpu|egui)([[:space:]]|$)'; then exit 1; fi</automated>
  </verify>
  <acceptance-criteria>The capability test/command pass the selected stack; CAPABILITY records all required measurements and hashes plus any legitimate fallback failure; package metadata is publish false/non-default; liquidfun dependency tree remains renderer-free.</acceptance-criteria>
  <done>One evidence-backed private rendering stack is selected without changing simulation or consumer architecture.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| capability fixture and output path to renderer/filesystem | Untrusted state crosses into an effectful or authoritative Phase 11 operation. |
| renderer/input loop to simulation authority | Validated output crosses a package, process, file, UI, or evidence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-24-01 | Tampering | capability fixture and output path to renderer/filesystem | mitigate | Use checked tracked fixture IDs, finite geometry/text/image bounds, confined target output, and reject links/traversal. |
| T-11-24-02 | Spoofing | renderer/input loop to simulation authority | mitigate | Consume immutable snapshots, emit only typed controller commands, and prove frames/camera/screenshots create no ticks or checkpoints. |
| T-11-24-03 | Denial of service | resource use | mitigate | Enforce reviewed input, geometry, image, archive, process, horizon, diagnostic, and UI limits with fail-fast timeouts. |
| T-11-24-04 | Information disclosure | diagnostics, UI, CI, and reports | mitigate | Redact secrets, raw records, private indices, pointers, stack traces, and unbounded stderr; expose only bounded semantic context and safe URLs. |
| T-11-24-05 | Elevation of privilege | dependency, renderer, and oracle isolation | mitigate | Keep all renderer and C++ capabilities private, unpublished, non-default, least-privilege, and unreachable from liquidfun production dependencies. |
| T-11-24-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and all relevant Rust, C++, workflow, package, corpus, Markdown, renderer, or exact-reference gates stated by the task.
</verification>

<success-criteria>
- Macroquad 0.4.15 is tried first only after the headless gate and retained only if every named capability passes.
- A heavier winit/wgpu/egui stack is selected only after a concrete recorded Macroquad failure in an allowed category.
- The selected package is unpublished, non-default, renderer-only, and consumes immutable controller/comparison inputs.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-24-SUMMARY.md`.</output>
