---
phase: 11-examples-headless-tooling-and-testbed
plan: "20"
type: execute
wave: 8
depends_on: ["11-19"]
files_modified:
  - tools/xtask/src/main.rs
  - tools/xtask/src/phase11_evidence.rs
  - tools/xtask/src/phase11_evidence/content.rs
  - tools/xtask/src/phase11_evidence/authority.rs
  - tools/xtask/src/phase11_evidence/paths.rs
  - tools/xtask/tests/phase11_evidence_cli.rs
  - reference/artifacts/manifest.toml
autonomous: true
requirements: ["TEST-03", "EXMP-01", "EXMP-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Local and exact-ref modes use the same semantic corpus evaluator; authority checks cannot reinterpret content."
    - "Exact-ref accepts only one same-SHA canonical and sanitizer pair on the locked toolchain with fresh live metadata."
    - "Unsafe archives, mixed runs, stale identities, missing leaves, digest drift, or partial output fail before promotion."
  artifacts:
    - path: "tools/xtask/src/phase11_evidence.rs"
      provides: "Closed validator CLI and artifact identity"
    - path: "tools/xtask/src/phase11_evidence/content.rs"
      provides: "Exact scenario/checkpoint/mapping evaluator"
    - path: "tools/xtask/src/phase11_evidence/authority.rs"
      provides: "D1 same-run live/archive authority checks"
  key_links:
    - from: "tools/xtask/src/phase11_evidence/content.rs"
      to: "crates/liquidfun-differential/tests/fixtures/catalog/phase11-v1.json"
      via: "shared closed corpus contract"
      pattern: "manifest_sha256"
    - from: "tools/xtask/src/phase11_evidence/authority.rs"
      to: "GitHub run metadata snapshot"
      via: "same-run SHA/job/artifact validation"
      pattern: "run_id"
---

<objective>
Build one reusable local and exact-reference Phase 11 evidence validator.

Purpose: Retain D0-D3, provenance, archive safety, same-run authority, and identity-last rules before CI generation.
Output: tools/xtask/src/phase11_evidence.rs, tools/xtask/src/phase11_evidence/content.rs, tools/xtask/src/phase11_evidence/authority.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@tools/xtask/src/phase10_evidence.rs
@tools/xtask/src/phase10_evidence/authority.rs
@tools/xtask/src/phase10_evidence/content.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement shared semantic and authority validation</name>
  <files>tools/xtask/src/main.rs, tools/xtask/src/phase11_evidence.rs, tools/xtask/src/phase11_evidence/content.rs, tools/xtask/src/phase11_evidence/authority.rs, tools/xtask/src/phase11_evidence/paths.rs, tools/xtask/tests/phase11_evidence_cli.rs, reference/artifacts/manifest.toml</files>
  <read-first>11-CONTEXT.md D-23 through D-26; Plan 11-19 corpus; Phase 10 evidence validator content/authority/path modules, archive limits, live metadata, denysets, and identity-last contract; reference artifact manifest.</read-first>
  <behavior>
    - "Local and exact-ref modes use the same semantic corpus evaluator; authority checks cannot reinterpret content."
    - "Exact-ref accepts only one same-SHA canonical and sanitizer pair on the locked toolchain with fresh live metadata."
  </behavior>
  <action>Add `phase11-evidence validate --mode local|exact-ref` and typed artifact identity. Content validation recomputes exact resolved/request/checkpoint/comparison/mapping/policy/inherited-evidence hashes, runs the same complete semantic evaluator for both modes, proves debug/release/replay D0 and sanitizer cases, and rejects pixels, durations, frames, private IDs, unknown policies, or omitted leaves. Authority validation requires one workflow_dispatch run and SHA, distinct successful canonical/sanitizer jobs and artifacts, locked Linux x86_64 Rust 1.97.0 and Clang 22.1.8 identities, fresh live API metadata, safe archive topology, exact sizes/digests/timestamps, and explicit historical denial. Inspect archives before extraction; reject traversal, absolute paths, links, devices, collisions, compression/file/byte excess, mixed runs, artifact ID zero in live metadata, or identity written before content completion. Register only the artifact schema in manifest.toml; do not promote compatibility.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>cargo test -p xtask --test phase11_evidence_cli && cargo xtask phase11-evidence validate --mode local --canonical-dir crates/liquidfun-differential/tests/fixtures/catalog --sanitizer-dir crates/liquidfun-differential/tests/fixtures/catalog</automated>
  </verify>
  <acceptance-criteria>CLI tests pass valid local content and all malformed/unsafe/mixed/partial cases; exact-ref fixtures enforce live same-run authority; local evidence is structurally non-promotable; no compatibility file changes.</acceptance-criteria>
  <done>One fail-closed validator governs all Phase 11 evidence content and authority.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| artifact archives and metadata to local filesystem | Untrusted input crosses into an effectful Phase 11 path. |
| local/remote evidence to D1 authority | Validated output crosses a package, process, file, or authority boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-20-01 | Tampering | artifact archives and metadata to local filesystem | mitigate | Inspect bounded archive entries before isolated extraction; reject traversal, links, devices, collisions, excess, and digest mismatch. |
| T-11-20-02 | Spoofing | local/remote evidence to D1 authority | mitigate | Require one SHA/run, distinct successful jobs/artifacts, locked identities, fresh API metadata, complete semantic content, and denysets. |
| T-11-20-03 | Denial of service | bounded execution and artifacts | mitigate | Enforce reviewed byte, record, action, horizon, process, archive, and diagnostic limits with fail-fast timeouts. |
| T-11-20-04 | Information disclosure | CLI, CI, and artifact diagnostics | mitigate | Redact secrets, private identities, paths, raw records, stack traces, and unbounded stderr; retain only bounded semantic context. |
| T-11-20-05 | Elevation of privilege | dependency/process boundary | mitigate | Keep renderer, benchmark, protocol, differential, and C++ dependencies private and absent from liquidfun's published graph. |
| T-11-20-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped automated command, ordered Rust commit gate, and any named Markdown, workflow, package, or sanitizer checks.
</verification>

<success-criteria>
- Local and exact-ref modes use the same semantic corpus evaluator; authority checks cannot reinterpret content.
- Exact-ref accepts only one same-SHA canonical and sanitizer pair on the locked toolchain with fresh live metadata.
- Unsafe archives, mixed runs, stale identities, missing leaves, digest drift, or partial output fail before promotion.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-20-SUMMARY.md`.</output>
