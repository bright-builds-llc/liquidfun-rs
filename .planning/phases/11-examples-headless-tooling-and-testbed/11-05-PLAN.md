---
phase: 11-examples-headless-tooling-and-testbed
plan: "05"
type: execute
wave: 2
depends_on: ["11-03"]
files_modified:
  - crates/liquidfun-test-protocol/src/catalog/scenarios/particles.rs
  - crates/liquidfun-test-protocol/src/catalog/scenarios/groups.rs
  - crates/liquidfun-test-protocol/src/catalog/scenarios/queries_callbacks.rs
autonomous: true
requirements: ["EXMP-01", "EXMP-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Particle scenarios cover lifecycle, contacts, flags, buffers, forces, statistics, queries, callbacks, and coupling."
    - "Group scenarios cover construction, topology, join, split, reactive behavior, solver flags, and destruction."
    - "Session pause and particle-system pause remain different typed actions."
  artifacts:
    - path: "crates/liquidfun-test-protocol/src/catalog/scenarios/particles.rs"
      provides: "Particle lifecycle and solver scenarios"
    - path: "crates/liquidfun-test-protocol/src/catalog/scenarios/groups.rs"
      provides: "Group topology and mutation scenarios"
    - path: "crates/liquidfun-test-protocol/src/catalog/scenarios/queries_callbacks.rs"
      provides: "Query, callback, and mutation scenarios"
  key_links:
    - from: "crates/liquidfun-test-protocol/src/catalog/scenarios/particles.rs"
      to: "crates/liquidfun-test-protocol/src/scenario/rigid_world/phase9.rs"
      via: "particle actions and observations"
      pattern: "Particle"
    - from: "crates/liquidfun-test-protocol/src/catalog/scenarios/groups.rs"
      to: "crates/liquidfun-test-protocol/src/scenario/rigid_world/phase10.rs"
      via: "group actions and observations"
      pattern: "ParticleGroup"
---

<objective>
Author representative particle, group, query, callback, and mutation catalog families.

Purpose: Give LiquidFun-specific behaviors the same canonical cross-consumer source as rigid scenarios.
Output: crates/liquidfun-test-protocol/src/catalog/scenarios/particles.rs, crates/liquidfun-test-protocol/src/catalog/scenarios/groups.rs, crates/liquidfun-test-protocol/src/catalog/scenarios/queries_callbacks.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@crates/liquidfun-differential/tests/fixtures/rigid_world/phase9/
@crates/liquidfun-differential/tests/fixtures/rigid_world/phase10/
@crates/liquidfun/tests/particle_solver_flags.rs
@crates/liquidfun/tests/particle_group_mutation.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode particle and group scenario definitions</name>
  <files>crates/liquidfun-test-protocol/src/catalog/scenarios/particles.rs, crates/liquidfun-test-protocol/src/catalog/scenarios/groups.rs, crates/liquidfun-test-protocol/src/catalog/scenarios/queries_callbacks.rs</files>
  <read-first>11-CONTEXT.md D-06 through D-11 and D-24; Phase 9 and Phase 10 sealed case manifests; public particle views, statistics, queries, callbacks, lifecycle, solver flags, and group mutation tests; catalog model from Plan 11-03.</read-first>
  <behavior>
    - "Particle scenarios cover lifecycle, contacts, flags, buffers, forces, statistics, queries, callbacks, and coupling."
    - "Group scenarios cover construction, topology, join, split, reactive behavior, solver flags, and destruction."
  </behavior>
  <action>Create typed definitions with stable slugs/version 1 for particle storage and lifecycle, contacts/listeners/filters/coupling, force/statistics/query/ray behavior, particle-system pause, every flag family, group construction/topology/join/split/reactive/rigid behavior, callback commands, accepted and rejected mutations, and destruction. Reuse existing Phase 9 and 10 protocol actions and public semantic witness roles; introduce no pass IDs or dense indices. Explicitly label particle-system pause as a physics action and never as controller pause. Declare exact settings, bounded horizons, checkpoint IDs, mappings, and regression/benchmark/visual eligibility. Add co-located uniqueness and deterministic-resolution tests.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cargo test -p liquidfun-test-protocol catalog::scenarios::particles && cargo test -p liquidfun-test-protocol catalog::scenarios::groups && cargo test -p liquidfun-test-protocol catalog::scenarios::queries_callbacks</automated>
  </verify>
  <acceptance-criteria>All tests pass; representative particle/group/query/callback/mutation behavior resolves deterministically; particle-system pause remains a distinct scheduled action; mappings are complete and no private storage identity is representable.</acceptance-criteria>
  <done>The canonical catalog covers the LiquidFun-specific representative scenario surface.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| large particle/group definitions to resource use | Untrusted or effectful data crosses into Phase 11 domain logic. |
| semantic identities to ownership | Validated authority crosses into downstream consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-05-01 | Tampering | large particle/group definitions to resource use | mitigate | Preflight particles, groups, topology, actions, checkpoints, and work horizons against named reviewed maxima. |
| T-11-05-02 | Spoofing | semantic identities to ownership | mitigate | Validate system, group, particle, fixture, body, and action ownership before a plan can resolve. |
| T-11-05-03 | Denial of service | bounded collections and work | mitigate | Enforce reviewed byte, record, action, entity, and diagnostic limits before allocation or execution. |
| T-11-05-04 | Information disclosure | errors and logs | mitigate | Emit bounded semantic IDs and categories; never expose private indices, raw records, pointers, secrets, or unbounded stderr. |
| T-11-05-05 | Elevation of privilege | dependency and process boundary | mitigate | Keep production Cargo and published liquidfun free of renderer, oracle, protocol, and foreign runtime dependencies. |
| T-11-05-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped automated command, then the repository Rust commit gate. Confirm tracked authority changes are deterministic and read-only checks leave the worktree clean.
</verification>

<success-criteria>
- Particle scenarios cover lifecycle, contacts, flags, buffers, forces, statistics, queries, callbacks, and coupling.
- Group scenarios cover construction, topology, join, split, reactive behavior, solver flags, and destruction.
- Session pause and particle-system pause remain different typed actions.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-05-SUMMARY.md`.</output>
