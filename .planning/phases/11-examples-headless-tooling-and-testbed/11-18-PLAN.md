---
phase: 11-examples-headless-tooling-and-testbed
plan: "18"
type: execute
wave: 7
depends_on: ["11-15", "11-16", "11-17"]
files_modified:
  - crates/liquidfun/tests/phase11_public_observability.rs
  - crates/liquidfun-differential/tests/headless_catalog.rs
  - tools/xtask/src/package.rs
  - tools/xtask/tests/package_cli.rs
  - .github/workflows/ci.yml
autonomous: true
requirements: ["RIGD-10", "EXMP-02", "EXMP-03", "EXMP-06"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Representative public and headless workflows pass with no graphical environment or initialized upstream source."
    - "cargo build/test/package for liquidfun never resolve renderer, testbed, protocol, differential, benchmark, or C++ dependencies."
    - "The catalog/controller/capture/comparison stack passes before any renderer dependency is selected."
  artifacts:
    - path: "crates/liquidfun-differential/tests/headless_catalog.rs"
      provides: "End-to-end controller/capture/comparison tests"
    - path: "tools/xtask/src/package.rs"
      provides: "Phase 11 dependency and archive isolation checks"
    - path: ".github/workflows/ci.yml"
      provides: "Submodule-free headless/package gate"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tools/xtask/src/package.rs"
      via: "Cargo-only package verification"
      pattern: "package verify"
    - from: "crates/liquidfun-differential/tests/headless_catalog.rs"
      to: "tools/reference optional oracle"
      via: "Rust-only fallback and prerequisite-labeled compare"
      pattern: "Oracle unavailable"
---

<objective>
Prove the complete headless capability and consumer isolation gate.

Purpose: Meet D-17's pre-renderer gate and preserve liquidfun as the sole ordinary consumer package.
Output: crates/liquidfun-differential/tests/headless_catalog.rs, tools/xtask/src/package.rs, .github/workflows/ci.yml.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@tools/xtask/src/package.rs
@.github/workflows/ci.yml
@crates/liquidfun/tests/world_diagnostics.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close public workflow, headless E2E, and package isolation before renderer selection</name>
  <files>crates/liquidfun/tests/phase11_public_observability.rs, crates/liquidfun-differential/tests/headless_catalog.rs, tools/xtask/src/package.rs, tools/xtask/tests/package_cli.rs, .github/workflows/ci.yml</files>
  <read-first>11-CONTEXT.md D-12, D-17, D-22 through D-24; Plans 11-08 through 11-17; current package verifier and Cargo-only CI; active lesson preserve-ci-isolation-boundaries; root workspace/default-members and liquidfun Cargo manifest.</read-first>
  <behavior>
    - "Representative public and headless workflows pass with no graphical environment or initialized upstream source."
    - "cargo build/test/package for liquidfun never resolve renderer, testbed, protocol, differential, benchmark, or C++ dependencies."
  </behavior>
  <action>Add public default-feature integration tests for counts/tree metrics/current contacts/particle contacts/broad-phase observations/statistics/debug primitives and headless integration tests for named/seeded resolution, pause/no tick, step/one tick/still paused, identical restart, settings validation, scenario actions, capture, replay, comparison, oracle-unavailable Rust-only diagnostics, and representative rigid/joint/rope/particle/group/query/callback/mutation flows. Extend package verification to inspect Cargo metadata and packaged archive, prove `liquidfun` is the sole default publishable package, deny renderer/window/game-engine/C++/protocol/differential/benchmark/testbed dependencies or features from liquidfun, and build/test the `.crate` in a temporary copy without third_party, reference, display variables, or repository paths. Add the focused gate to submodule-free CI before any visual plan.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>cargo test -p liquidfun --all-features --test phase11_public_observability && cargo test -p liquidfun-differential --test headless_catalog && cargo test -p xtask --test package_cli && cargo xtask package verify</automated>
  </verify>
  <acceptance-criteria>All gates pass in a headless submodule-free environment; the full catalog/controller/capture/comparison capability is green; package metadata/archive contains no private or graphical dependency; CI remains submodule false.</acceptance-criteria>
  <done>The locked headless capability is verified and authorizes the later renderer spike.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| headless tests to optional external oracle | Untrusted input crosses into an effectful Phase 11 path. |
| workspace/package metadata to consumer trust | Validated output crosses a package, process, file, or authority boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-18-01 | Tampering | headless tests to optional external oracle | mitigate | Treat missing oracle as an explicit prerequisite or Rust-only state, never as a match; keep C++ out of Cargo-only CI. |
| T-11-18-02 | Spoofing | workspace/package metadata to consumer trust | mitigate | Inspect resolved dependency graphs and archive contents in an isolated copy, reject forbidden paths/features/dependencies, and do not follow links. |
| T-11-18-03 | Denial of service | bounded execution and artifacts | mitigate | Enforce reviewed byte, record, action, horizon, process, archive, and diagnostic limits with fail-fast timeouts. |
| T-11-18-04 | Information disclosure | CLI, CI, and artifact diagnostics | mitigate | Redact secrets, private identities, paths, raw records, stack traces, and unbounded stderr; retain only bounded semantic context. |
| T-11-18-05 | Elevation of privilege | dependency/process boundary | mitigate | Keep renderer, benchmark, protocol, differential, and C++ dependencies private and absent from liquidfun's published graph. |
| T-11-18-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped automated command, ordered Rust commit gate, and any named Markdown, workflow, package, or sanitizer checks.
</verification>

<success-criteria>
- Representative public and headless workflows pass with no graphical environment or initialized upstream source.
- cargo build/test/package for liquidfun never resolve renderer, testbed, protocol, differential, benchmark, or C++ dependencies.
- The catalog/controller/capture/comparison stack passes before any renderer dependency is selected.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-18-SUMMARY.md`.</output>
