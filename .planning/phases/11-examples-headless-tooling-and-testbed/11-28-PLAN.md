---
phase: 11-examples-headless-tooling-and-testbed
plan: "28"
type: execute
wave: 12
depends_on: ["11-02", "11-06", "11-23", "11-27"]
files_modified:
  - tools/xtask/src/inventory/corpus/validation.rs
  - tools/xtask/src/inventory/corpus/report.rs
  - tools/xtask/tests/corpus_closure.rs
  - reference/upstream-corpus.json
  - UPSTREAM-CORPUS.md
autonomous: true
requirements: ["TEST-03", "EXMP-01", "EXMP-03", "EXMP-04", "EXMP-05"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Every applicable GoogleTest, example, and registered testbed entry has one terminal reviewed outcome and compatibility impact."
    - "Every referenced scenario, test, fixture, evidence leaf, review record, source identity, and eligibility resolves exactly once."
    - "Unknown, duplicate, unregistered, unmapped, stale, vague, or unexplained rows are zero and report totals match machine authority."
  artifacts:
    - path: "tools/xtask/src/inventory/corpus/validation.rs"
      provides: "Cross-ledger item-level closure validator"
    - path: "reference/upstream-corpus.json"
      provides: "Complete terminal reviewed corpus authority"
    - path: "UPSTREAM-CORPUS.md"
      provides: "Deterministic generated totals and item-level outcomes"
  key_links:
    - from: "tools/xtask/src/inventory/corpus/validation.rs"
      to: "reference/discovery.json"
      via: "many semantic items to one verified source path"
      pattern: "source_path"
    - from: "tools/xtask/src/inventory/corpus/validation.rs"
      to: "reference/scenario-catalog.json"
      via: "ported/equivalent scenario references"
      pattern: "catalog_slug"
    - from: "tools/xtask/src/inventory/corpus/report.rs"
      to: "reference/upstream-corpus.json"
      via: "generated read-only projection"
      pattern: "unresolved"
---

<objective>
Close every upstream semantic corpus row through fail-closed joins and generate the exact report.

Purpose: Deliver D-01 through D-05 and D-25 at item granularity after scenarios, evidence, and visualization eligibility exist.
Output: tools/xtask/src/inventory/corpus/validation.rs, reference/upstream-corpus.json, UPSTREAM-CORPUS.md.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@reference/upstream-corpus.json
@reference/discovery.json
@reference/compatibility.json
@reference/scenario-catalog.json
@reference/artifacts/phase11/scenario-mappings.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement cross-ledger closure, classify every item, and generate the report</name>
  <files>tools/xtask/src/inventory/corpus/validation.rs, tools/xtask/src/inventory/corpus/report.rs, tools/xtask/tests/corpus_closure.rs, reference/upstream-corpus.json, UPSTREAM-CORPUS.md</files>
  <read-first>11-CONTEXT.md D-01 through D-05 and D-25; Plans 11-02, 11-06, 11-19, 11-23, and 11-27 outputs; existing inventory validation/report patterns; pinned semantic counts including parameterized GoogleTests, registered entries, unregistered Rope.h, and HelloWorld.</read-first>
  <behavior>
    - "Every applicable GoogleTest, example, and registered testbed entry has one terminal reviewed outcome and compatibility impact."
    - "Every referenced scenario, test, fixture, evidence leaf, review record, source identity, and eligibility resolves exactly once."
  </behavior>
  <action>Add validation that permits many semantic items per discovered source path but requires unique source-derived item IDs and exact pinned revision. Resolve every source path through discovery, every port/equivalent outcome through the typed catalog and public test IDs, every regression/fixture/evidence/compatibility leaf/review through its machine authority, and every visual/benchmark eligibility through scenario mappings. Enforce terminal dispositions native port, equivalent evidence, reviewed irrelevance, documented difference, or intentional non-support with separate compatibility impact and specific non-circular reviewed rationale. Explicitly account for authoritative expanded parameterized tests or reviewed declaration expansion, all TestEntries registrations, unregistered Rope.h, and HelloWorld. Reject unknown/duplicate/unregistered/unmapped/stale items, missing evidence, vague rationale, stale hashes/revisions, and classification totals drift. Populate every row to terminal status and render UPSTREAM-CORPUS.md deterministically with exact totals, disposition/impact tables, evidence links, and zero unresolved rows; render/check twice without silent authority edits.</action>
  <pre-commit>Before any tracked task commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>cargo test -p xtask --test corpus_closure && cargo xtask inventory corpus check-closure && cargo xtask inventory corpus generate-report && cargo xtask inventory corpus check-closure && cargo xtask inventory corpus generate-report && git diff --exit-code -- UPSTREAM-CORPUS.md</automated>
  </verify>
  <acceptance-criteria>Closure tests pass all deliberate gap classes; every semantic test/example/registration has one reviewed terminal outcome; every join resolves; unresolved total is zero; repeated report generation is byte-identical.</acceptance-criteria>
  <done>TEST-03 and EXMP-01 are closed at semantic item granularity with reviewable machine authority.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| multiple manifests and ledgers to closure authority | Untrusted state crosses into an effectful or authoritative Phase 11 operation. |
| corpus report to public claims | Validated output crosses a package, process, file, UI, or evidence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-28-01 | Tampering | multiple manifests and ledgers to closure authority | mitigate | Resolve exact revision, IDs, paths, hashes, reviews, mappings, and evidence across all authorities; reject duplicates/stale/circular references. |
| T-11-28-02 | Spoofing | corpus report to public claims | mitigate | Generate deterministic totals only from validated authority and show zero/unresolved explicitly without auto-classification. |
| T-11-28-03 | Denial of service | resource use | mitigate | Enforce reviewed input, geometry, image, archive, process, horizon, diagnostic, and UI limits with fail-fast timeouts. |
| T-11-28-04 | Information disclosure | diagnostics, UI, CI, and reports | mitigate | Redact secrets, raw records, private indices, pointers, stack traces, and unbounded stderr; expose only bounded semantic context and safe URLs. |
| T-11-28-05 | Elevation of privilege | dependency, renderer, and oracle isolation | mitigate | Keep all renderer and C++ capabilities private, unpublished, non-default, least-privilege, and unreachable from liquidfun production dependencies. |
| T-11-28-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and all relevant Rust, C++, workflow, package, corpus, Markdown, renderer, or exact-reference gates stated by the task.
</verification>

<success-criteria>
- Every applicable GoogleTest, example, and registered testbed entry has one terminal reviewed outcome and compatibility impact.
- Every referenced scenario, test, fixture, evidence leaf, review record, source identity, and eligibility resolves exactly once.
- Unknown, duplicate, unregistered, unmapped, stale, vague, or unexplained rows are zero and report totals match machine authority.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-28-SUMMARY.md`.</output>
