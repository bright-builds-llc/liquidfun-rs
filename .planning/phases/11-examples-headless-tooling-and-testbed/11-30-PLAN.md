---
phase: 11-examples-headless-tooling-and-testbed
plan: "30"
type: execute
wave: 14
depends_on: ["11-29"]
files_modified:
  - crates/liquidfun-testbed/src/bin/interactive.rs
  - crates/liquidfun-testbed/tests/comparison_lifecycle.rs
autonomous: true
gap_closure: true
requirements: ["EXMP-05"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-23T03:48:48.492Z
must_haves:
  truths:
    - "A successful live Rust/oracle comparison removes the prior comparison identity failure before the current comparison is presented."
    - "Selecting, restarting, or applying settings resets comparison-scoped model, identity, and error state together without clearing an unrelated bounded application error."
    - "Agent-controlled desktop UAT can reproduce identity failure followed by success in Overlay and Side by side without the old resolved_sha256 error remaining visible."
  artifacts:
    - path: "crates/liquidfun-testbed/src/bin/interactive.rs"
      provides: "Private compiled diagnostics state owning comparison lifecycle and generic bounded errors"
      contains: "DesktopDiagnostics"
    - path: "crates/liquidfun-testbed/tests/comparison_lifecycle.rs"
      provides: "Compiled behavioral regression for failure, success, reset, and unrelated-error isolation"
      contains: "comparison_failure_then_success_retires_the_stale_identity_error"
  key_links:
    - from: "crates/liquidfun-testbed/src/bin/interactive.rs"
      to: "compare_canonical_checkpoints"
      via: "refresh_comparison applies each current Result through the compiled diagnostics transition"
      pattern: "DesktopDiagnostics"
    - from: "crates/liquidfun-testbed/src/bin/interactive.rs"
      to: "draw_inspector"
      via: "comparison failures and unrelated bounded application errors remain distinct presentation channels"
      pattern: "Comparison error"
---

<objective>
Close the diagnosed Phase 11 UAT gap by retiring stale comparison identity failures when the desktop comparison succeeds or resets.

Purpose: Make the live comparison presentation reflect the current comparison lifecycle while preserving unrelated bounded application diagnostics.
Output: A narrowly scoped desktop state repair and focused failure-to-success regression.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UAT.md
@.planning/debug/stale-comparison-error.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-27-SUMMARY.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-29-SUMMARY.md
@crates/liquidfun-testbed/src/bin/interactive.rs

<interfaces>
From crates/liquidfun-testbed/src/bin/interactive.rs:
```rust
struct DesktopApp {
    maybe_oracle: Option<CanonicalCheckpoint>,
    diagnostics: DesktopDiagnostics<ComparisonModel, (Sha256Hex, CheckpointId)>,
}

struct ComparisonLifecycle<Model, Identity>;
struct DesktopDiagnostics<Model, Identity> {
    comparison: ComparisonLifecycle<Model, Identity>,
    maybe_error: Option<String>,
}

fn apply_comparison(&mut self, identity: Identity, result: Result<Model, String>);
fn reset_comparison(&mut self);
fn set_error(&mut self, error: impl std::fmt::Display);
fn maybe_comparison(&self) -> Option<&Model>;
fn maybe_compared_identity(&self) -> Option<&Identity>;
fn maybe_comparison_error(&self) -> Option<&str>;
fn maybe_error(&self) -> Option<&str>;
fn refresh_comparison(&mut self);
fn clear_comparison(&mut self);
fn draw_inspector(&self, region: (u32, u32, u32, u32));
```

The diagnosed transition is:
`identity failure -> comparison reset or corrected identity -> successful ComparisonModel`.
The current defect is that `maybe_error` survives this transition and renders beside the successful model.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Separate and verify the desktop comparison-error lifecycle</name>
  <files>crates/liquidfun-testbed/src/bin/interactive.rs, crates/liquidfun-testbed/tests/comparison_lifecycle.rs</files>
  <read-first>Read the current dirty `interactive.rs`, `11-UAT.md` Test 4 and Gaps, `.planning/debug/stale-comparison-error.md`, and the comparison/error methods in `interactive.rs`. If `crates/liquidfun-testbed/tests/comparison_lifecycle.rs` exists, read and snapshot it; if it does not exist, record an explicit missing-file sentinel and continue without attempting to read or hash it. Also inspect `crates/liquidfun-testbed/tests/interactive.rs` only for established integration-test conventions; do not modify it because it contains concurrent user/agent work.</read-first>
  <behavior>
    - "Failure: a `resolved_sha256` identity failure clears the current comparison model and records one bounded comparison-scoped error."
    - "Success after failure: a later successful model replaces the model and clears only the comparison-scoped error, so Inspect cannot show that old identity failure beside the live comparison."
    - "Reset after failure: scenario selection, Restart, or Apply Settings and Restart clears comparison model, compared identity, and comparison-scoped error together."
    - "Isolation: success and reset do not clear `maybe_error`, which remains reserved for unrelated bounded controller, capture, settings, or runtime failures."
  </behavior>
  <action>WORKTREE GUARD BEFORE EDITING: create a gitignored snapshot directory beneath `target/phase11-plan30-worktree-guard/`. Record NUL-delimited `git status --porcelain=v1 -z`, tracked working and cached binary diffs, and a NUL-safe inventory plus byte hash for every pre-existing dirty non-task file (tracked, staged, and untracked); exclude only the two declared task paths. Hash file bytes without applying clean/smudge filters and record explicit missing/non-regular sentinels. Record the current task-path diffs separately so existing `interactive.rs` hunks remain distinguishable from executor-owned hunks. Handle the proposed test path safely: if absent, record `<missing>` rather than failing. Before any edit, run `cargo fmt --all -- --check`; if it reports any pre-existing unformatted path, especially a non-task or fenced path, stop and report the blocker without formatting or restoring anything.

RED: create a small private generic `ComparisonLifecycle<Model, Identity>` value inside one private compiled `DesktopDiagnostics<Model, Identity>` state in `interactive.rs`. `DesktopDiagnostics` owns both the comparison lifecycle and the generic `maybe_error`; `DesktopApp` must not retain any parallel comparison, identity, comparison-error, or generic-error field. Expose only the exact production transitions and read accessors: `apply_comparison(identity, Result<Model, String>)`, `reset_comparison()`, `set_error(impl Display)`, `maybe_comparison()`, `maybe_compared_identity()`, `maybe_comparison_error()`, and `maybe_error()`. `apply_comparison` bounds a current comparison failure with `bound_message`, replaces the comparison error, and clears the model; success installs the current model/identity and clears only the comparison error. `reset_comparison` clears model, identity, and comparison error only. `set_error` bounds and replaces the generic error. Keep this state free of Macroquad, controller, filesystem, clock, and oracle-process effects.

Make `DesktopDiagnostics` and only the methods required by the compiled integration test `pub(crate)`; they remain private to the unpublished binary/test target and are not added to the `liquidfun-testbed` library API. To compile the exact production diagnostics state without changing Cargo targets or adding a third source file, gate the Macroquad entrypoint with `#[cfg(not(test))]` and have `crates/liquidfun-testbed/tests/comparison_lifecycle.rs` include `interactive.rs` inside an `#[allow(dead_code, unused_imports)]` test-only module. Tests call the compiled production transitions directly and do not parse or assert source text. Add focused Arrange/Act/Assert tests named `comparison_failure_then_success_retires_the_stale_identity_error`, `comparison_failure_then_reset_clears_comparison_state`, and `generic_error_survives_comparison_success_and_reset`. The first executes `apply_comparison(identity_a, Err("checkpoint comparison identity mismatch: resolved_sha256"))` followed by `apply_comparison(identity_b, Ok(model))`, asserting the new model/identity are current and `maybe_comparison_error()` is absent. The second executes failure then `reset_comparison()`, asserting model, identity, and comparison error are absent. The third must call the production `DesktopDiagnostics::set_error` transition with a bounded generic message, then execute comparison failure, success, and reset through the same diagnostics value; after every transition it asserts `maybe_error()` remains byte-for-byte identical while the comparison accessors change as specified. Do not use a separate local generic-error slot, duplicated transition model, or source-wiring assertion. Run the focused test and confirm RED before wiring `DesktopApp`; do not commit RED.

GREEN: replace `DesktopApp`'s independent comparison, identity, and `maybe_error` slots with one `DesktopDiagnostics<ComparisonModel, (Sha256Hex, CheckpointId)>`, initialized empty. `DesktopApp::refresh_comparison` must call `diagnostics.apply_comparison(native_identity, comparison)` for the current comparison result and use diagnostics accessors for identity-cache checks. `DesktopApp::clear_comparison` must call `diagnostics.reset_comparison()`. `DesktopApp::set_error` must delegate to `diagnostics.set_error(error)`; it may not assign a field directly. Update every model/identity/error read to the explicit diagnostics accessors so there is no untested glue that can clear or overwrite the generic channel. In `draw_inspector`, use `maybe_comparison_error()` for explicit `Comparison error:` copy and `maybe_error()` independently for `Last bounded error:`. Both stored error channels must be bounded by their production diagnostics transitions. Keep comparison calculation, identity validation, canonical policies, controller semantics, renderer authority, and error copy otherwise unchanged per D-16, D-19, D-20, D-23, and D-26.

REFACTOR: perform a simplification pass limited to the two task files. Prefer direct state replacement and guard-style control flow; do not introduce a second comparator, alter `ComparisonModel`, widen public APIs, or move simulation/comparison authority into the renderer.

DIRTY-WORKTREE/FENCE: do not edit, stage, restore, format, or commit `.planning/config.json`, `crates/liquidfun-differential/src/rigid_world/phase10/comparator.rs`, `crates/liquidfun-differential/src/rigid_world/phase10/evidence.rs`, or `crates/liquidfun-test-protocol/src/scenario/rigid_world/result/phase10/prefix.rs`. Preserve all pre-existing changes in `interactive.rs`; stage only executor-owned hunks plus the new focused test. Do not revert or overwrite concurrent changes in any other dirty testbed file. Never use restore, checkout, reset, or formatter rollback as recovery: if a guard changes or detects drift in a non-task file, stop, leave user files intact, and report the exact path and before/after evidence.</action>
  <pre-commit>FORMAT SAFETY: after implementation, run pinned `rustfmt --edition 2024 crates/liquidfun-testbed/src/bin/interactive.rs crates/liquidfun-testbed/tests/comparison_lifecycle.rs` on only the two executor-owned paths. Re-run `cargo fmt --all -- --check`; if it names any unrelated path, stop without write-mode workspace formatting. Recompute and compare every pre-existing dirty non-task path's byte hash, status entry, tracked binary diff, and cached diff with the pre-edit snapshot; all must be identical. Then run the mandatory gate in exact order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Because the immediately preceding workspace check proved formatting clean, `cargo fmt --all` must be a no-op; immediately re-verify every non-task hash/status/diff and stop if any changed. Never restore user files as recovery.

COMMIT BOUNDARY: create one atomic fix commit after all verification and UAT pass. Run `git diff --check`; inspect the complete task-path diff against the saved pre-edit task diff; stage only executor-owned hunks in `interactive.rs` plus the new focused test. Require the cached delta introduced by this task contains only those two paths and excludes every pre-existing unrelated hunk; inspect `git diff --cached --check` and the full cached diff. Commit as `fix(11-30): retire stale comparison errors`. Do not commit RED, any pre-existing dirty hunk, or any non-task path.</pre-commit>
  <verify>
    <automated>cargo test -p liquidfun-testbed --test comparison_lifecycle && cargo test -p liquidfun-testbed --test interactive && cargo fmt --all -- --check && cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && git diff --check</automated>
  </verify>
  <acceptance-criteria>The three compiled behavioral regression tests execute failure-to-success, failure-to-reset, and production `set_error` preservation over the exact private `DesktopDiagnostics` state used by `DesktopApp`; the third test proves the generic bounded bytes remain identical after every comparison transition, with no external error slot or source assertion. The existing interactive suite passes; the ordered Rust gate passes in required fmt, Clippy, build, test order; before and after byte hashes/status/diffs prove every pre-existing dirty non-task file unchanged; the reviewed diff contains only the intended diagnostics lifecycle and focused test; the four fenced paths and all unrelated dirty hunks remain untouched and unstaged. After software verification, perform agent-controlled desktop UAT using the same validated `--oracle-checkpoint PATH` flow from Test 4: first select settings/scenario input that produces `checkpoint comparison identity mismatch: resolved_sha256`, then select the exact matching native resolved SHA and capture the reachable checkpoint. Confirm a live comparison appears in Overlay and Side by side, canonical Rust/Oracle/Policy rows remain present, and neither view shows the prior identity error. Do not inject an unrelated application failure during desktop UAT; unrelated-error isolation is deterministically covered by the compiled diagnostics test. Save diagnostic screenshots only beneath `target/phase11-uat/`; screenshots remain non-authoritative. If a validated matching checkpoint or desktop automation is unavailable, report the exact blocker and do not mark UAT or the plan complete.</acceptance-criteria>
  <done>The diagnosed failure-to-success lifecycle is regression-protected, the desktop displays only current comparison diagnostics, unrelated bounded errors remain visible, automated gates pass, and agent-controlled UAT re-confirms Test 4.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| validated native/oracle comparison result to desktop state | Current comparison success or failure replaces presentation state without changing comparator authority. |
| comparison-scoped diagnostics to generic application diagnostics | Resetting stale comparison state must not suppress unrelated bounded failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-30-01 | Spoofing | live comparison status | mitigate | Present success only from the current `compare_canonical_checkpoints` result and retire the prior comparison error in the same transition. |
| T-11-30-02 | Tampering | comparison versus generic diagnostic state | mitigate | Keep separate fields and focused regression assertions so reset cannot silently clear unrelated bounded errors. |
| T-11-30-03 | Repudiation | UAT lifecycle evidence | mitigate | Record the exact failure-to-success procedure, focused commands, and non-authoritative target screenshots. |
| T-11-30-04 | Information disclosure | bounded diagnostic rendering | mitigate | Preserve `bound_message` for comparison failures and do not expose raw records, private storage, paths, or unbounded stderr. |
| T-11-30-05 | Denial of service | repeated comparison refresh | accept | The existing identity cache and comparison limits remain unchanged; this plan adds only constant-time state replacement. |
| T-11-30-06 | Elevation of privilege | renderer/comparator authority boundary | mitigate | Keep the renderer passive and do not modify comparator, protocol, oracle, evidence, or simulation authority. |
</threat-model>

<verification>
1. Focused regression: `cargo test -p liquidfun-testbed --test comparison_lifecycle`.
1. Existing app integration: `cargo test -p liquidfun-testbed --test interactive`.
1. Required ordered Rust gate: fmt, deny-warning Clippy, all-target/all-feature build, all-feature tests.
1. Agent-controlled UAT: identity failure followed by matching success in Overlay and Side by side, with no stale resolved-SHA error.
1. Scope review: working and cached diffs preserve all pre-existing dirty work and exclude every fenced path.
</verification>

<success-criteria>
- A live successful comparison cannot coexist with its earlier identity-mismatch diagnostic.
- Comparison reset retires only comparison-scoped state; unrelated bounded errors remain independently visible.
- Focused lifecycle tests, the existing interactive suite, and all ordered Rust gates pass.
- Agent-controlled Test 4 recheck proves the exact failure-to-success behavior in both comparison modes.
- Only the two declared implementation/test paths enter the atomic fix commit.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-30-SUMMARY.md`.</output>
