---
phase: 11-examples-headless-tooling-and-testbed
plan: "23"
type: execute
wave: 11
depends_on: ["11-22"]
files_modified:
  - reference/artifacts/phase11/exact-ref.json
  - reference/compatibility.json
  - tools/xtask/src/inventory/validation.rs
  - tools/xtask/src/inventory/validation/phase11.rs
  - tools/xtask/tests/inventory_cli/phase11.rs
  - COMPATIBILITY.md
autonomous: true
requirements: ["TEST-03", "EXMP-01", "EXMP-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Promotion is authorized only by the exact run/SHA/jobs/artifacts/digests validated in Plan 11-22."
    - "Only scoped scenario/catalog/headless/observation rows gain reviewed evidence; pixels, timing, broad platforms, and release claims remain excluded."
    - "Compatibility projection is deterministic and read-only checks pass twice."
  artifacts:
    - path: "reference/artifacts/phase11/exact-ref.json"
      provides: "Immutable reviewed authority identity and scoped outcomes"
    - path: "tools/xtask/src/inventory/validation/phase11.rs"
      provides: "Fail-closed promotion validator"
    - path: "reference/compatibility.json"
      provides: "Scoped Phase 11 evidence promotion"
  key_links:
    - from: "tools/xtask/src/inventory/validation/phase11.rs"
      to: "reference/artifacts/phase11/exact-ref.json"
      via: "exact authority gate"
      pattern: "run_id"
    - from: "reference/compatibility.json"
      to: "COMPATIBILITY.md"
      via: "deterministic generated projection"
      pattern: "Phase 11"
---

<objective>
Promote only the reviewed Phase 11 authority into compatibility evidence.

Purpose: Convert validated D1 results into exact machine-ledger state without overstating UI, timing, platform, or release maturity.
Output: reference/artifacts/phase11/exact-ref.json, tools/xtask/src/inventory/validation/phase11.rs, reference/compatibility.json.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@target/phase11-evidence/run.json
@tools/xtask/src/phase11_evidence.rs
@tools/xtask/src/inventory/validation/phase10.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind exact authority and promote the scoped compatibility rows</name>
  <files>reference/artifacts/phase11/exact-ref.json, reference/compatibility.json, tools/xtask/src/inventory/validation.rs, tools/xtask/src/inventory/validation/phase11.rs, tools/xtask/tests/inventory_cli/phase11.rs, COMPATIBILITY.md</files>
  <read-first>11-CONTEXT.md D-25 and D-26; Plan 11-22 exact-ref outputs; Phase 10 promotion validator; reference compatibility evidence dimensions; inventory report generation; Phase 11 scenario mapping and corpus manifests.</read-first>
  <behavior>
    - "Promotion is authorized only by the exact run/SHA/jobs/artifacts/digests validated in Plan 11-22."
    - "Only scoped scenario/catalog/headless/observation rows gain reviewed evidence; pixels, timing, broad platforms, and release claims remain excluded."
  </behavior>
  <action>Create a tracked exact-ref authority record containing the one approved full SHA, run, canonical/sanitizer jobs and artifacts, live/archive digests, toolchains/platform, complete scenario/mapping/policy/proof digests, leaf outcomes, review identity/date, and explicit denial of every prior failed candidate. Add a Phase 11 inventory validator that rejects any authority or ledger mismatch, mixed/omitted proof, unsupported leaf, stale mapping, changed UI/timing field, or promotion beyond scoped rows. Promote only exact catalog execution, deterministic checkpoint/comparison, public semantic observation/debug-draw, and reviewed upstream-equivalence rows supported by the authority. Keep visualization pixels, wall duration, frame rate, broad platform, performance, packaging, and release readiness not evidenced. Generate COMPATIBILITY.md from the machine ledger, run generation/check twice, and require identical bytes and clean tracked diff.</action>
  <pre-commit>Before any tracked task commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>cargo test -p xtask --test inventory_cli phase11 && cargo xtask inventory generate && cargo xtask inventory check && cargo xtask inventory generate && cargo xtask inventory check && just markdown-check</automated>
  </verify>
  <acceptance-criteria>Promotion tests and double generation pass; every promoted row matches the exact reviewed authority and complete leaves; all excluded maturity dimensions remain unclaimed; the second generation is byte-identical.</acceptance-criteria>
  <done>Phase 11 semantic compatibility evidence is promoted narrowly and truthfully.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| exact-ref record to compatibility ledger | Untrusted state crosses into an effectful or authoritative Phase 11 operation. |
| generated compatibility report to public maturity | Validated output crosses a package, process, file, UI, or evidence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-23-01 | Tampering | exact-ref record to compatibility ledger | mitigate | Bind literal run/SHA/job/artifact/digest/review identities and reject every mismatch or omitted proof. |
| T-11-23-02 | Spoofing | generated compatibility report to public maturity | mitigate | Generate only from machine authority, preserve independent evidence dimensions, and forbid UI/timing/platform/release promotion. |
| T-11-23-03 | Denial of service | resource use | mitigate | Enforce reviewed input, geometry, image, archive, process, horizon, diagnostic, and UI limits with fail-fast timeouts. |
| T-11-23-04 | Information disclosure | diagnostics, UI, CI, and reports | mitigate | Redact secrets, raw records, private indices, pointers, stack traces, and unbounded stderr; expose only bounded semantic context and safe URLs. |
| T-11-23-05 | Elevation of privilege | dependency, renderer, and oracle isolation | mitigate | Keep all renderer and C++ capabilities private, unpublished, non-default, least-privilege, and unreachable from liquidfun production dependencies. |
| T-11-23-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and all relevant Rust, C++, workflow, package, corpus, Markdown, renderer, or exact-reference gates stated by the task.
</verification>

<success-criteria>
- Promotion is authorized only by the exact run/SHA/jobs/artifacts/digests validated in Plan 11-22.
- Only scoped scenario/catalog/headless/observation rows gain reviewed evidence; pixels, timing, broad platforms, and release claims remain excluded.
- Compatibility projection is deterministic and read-only checks pass twice.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-23-SUMMARY.md`.</output>
