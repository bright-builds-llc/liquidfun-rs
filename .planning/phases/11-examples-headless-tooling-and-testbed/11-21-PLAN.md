---
phase: 11-examples-headless-tooling-and-testbed
plan: "21"
type: execute
wave: 9
depends_on: ["11-20"]
files_modified:
  - scripts/phase11-evidence.sh
  - .github/workflows/oracle.yml
  - Justfile
  - TESTING.md
  - tools/xtask/tests/phase11_evidence_cli/workflow.rs
autonomous: true
requirements: ["TEST-03", "EXMP-01", "EXMP-03", "EXMP-06"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Local canonical and sanitizer artifacts validate completely but remain D2 and non-promotable."
    - "One manual Oracle CI run creates a canonical and fail-fast sanitizer D1 pair at one immutable SHA."
    - "Commands are fixed, bounded, least-privilege, read-only for accepted authority, and never log secrets or raw unbounded diagnostics."
  artifacts:
    - path: "scripts/phase11-evidence.sh"
      provides: "Fail-fast identity-last evidence generation"
    - path: ".github/workflows/oracle.yml"
      provides: "Same-run canonical and sanitizer Phase 11 jobs"
    - path: "TESTING.md"
      provides: "D0-D3 generation, acquisition, and promotion contract"
  key_links:
    - from: ".github/workflows/oracle.yml"
      to: "scripts/phase11-evidence.sh"
      via: "fixed canonical/sanitizer generation"
      pattern: "phase11-evidence"
    - from: "scripts/phase11-evidence.sh"
      to: "tools/xtask/src/phase11_evidence.rs"
      via: "validation before identity publication"
      pattern: "validate"
---

<objective>
Produce identity-last local evidence and one same-run canonical CI pair.

Purpose: Make Phase 11 semantic evidence reproducible while preserving Cargo-only and sanitizer isolation.
Output: scripts/phase11-evidence.sh, .github/workflows/oracle.yml, TESTING.md.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@scripts/phase10-evidence.sh
@.github/workflows/oracle.yml
@TESTING.md
@Justfile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire local D2 generation and canonical D1 workflow jobs</name>
  <files>scripts/phase11-evidence.sh, .github/workflows/oracle.yml, Justfile, TESTING.md, tools/xtask/tests/phase11_evidence_cli/workflow.rs</files>
  <read-first>11-CONTEXT.md D-22, D-23, and D-26; Plan 11-20 validator; Phase 10 evidence script/workflow/docs; AGENTS Bash safety and Rust commit rules; active lesson reproduce-ci-at-exact-boundary.</read-first>
  <behavior>
    - "Local canonical and sanitizer artifacts validate completely but remain D2 and non-promotable."
    - "One manual Oracle CI run creates a canonical and fail-fast sanitizer D1 pair at one immutable SHA."
  </behavior>
  <action>Add a set -euo pipefail runner with fixed `canonical` and `sanitizer` modes and explicit output root. It verifies the resolved corpus and pinned source/toolchain, executes native debug/release/replay D0 plus process-isolated oracle comparison, records bounded failure data, runs fail-fast ASan/UBSan in sanitizer mode, recomputes every digest, validates the complete artifact, and writes identity last. Add thin Just recipes. Extend the existing Oracle workflow with Phase 11-only manual/scheduled conditions, full-SHA action pins, least permissions, locked Linux/Clang/CMake/Ninja/Rust identities, timeouts, finite retention, and distinct exact artifact names generated from one run/SHA; leave earlier phase jobs unchanged. Do not initialize submodules in Cargo CI or expose secrets/artifact bodies in logs. Document local D2 non-authority, same-run D1, screenshots/timing exclusion, acquisition, and promotion.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>LIQUIDFUN_PHASE11_ORACLE_MODE=canonical bash scripts/phase11-evidence.sh canonical target/phase11-evidence-local/canonical && UBSAN_OPTIONS=halt_on_error=1 ASAN_OPTIONS=abort_on_error=1:halt_on_error=1 LIQUIDFUN_PHASE11_ORACLE_MODE=sanitizer bash scripts/phase11-evidence.sh sanitizer target/phase11-evidence-local/sanitizer && cargo xtask phase11-evidence validate --mode local --canonical-dir target/phase11-evidence-local/canonical --sanitizer-dir target/phase11-evidence-local/sanitizer && actionlint .github/workflows/oracle.yml && just markdown-check</automated>
  </verify>
  <acceptance-criteria>Fresh local pair validates and is non-promotable; workflow contract tests and actionlint pass; one future dispatch yields exact same-SHA canonical/sanitizer artifacts; Cargo CI and earlier jobs remain unchanged; documentation passes mdformat contract.</acceptance-criteria>
  <done>Phase 11 evidence production is reproducible locally and has one admissible canonical authority shape.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| shell/workflow inputs to processes and paths | Untrusted input crosses into an effectful Phase 11 path. |
| CI identities and logs to authority | Validated output crosses a package, process, file, or authority boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-21-01 | Tampering | shell/workflow inputs to processes and paths | mitigate | Use fixed modes, validated explicit output roots, argument arrays, pipefail, timeouts, and identity-last atomic publication. |
| T-11-21-02 | Spoofing | CI identities and logs to authority | mitigate | Pin actions/toolchains, use least permissions, redact secrets and raw payloads, bind exact SHA/job/artifact names, and validate before upload. |
| T-11-21-03 | Denial of service | bounded execution and artifacts | mitigate | Enforce reviewed byte, record, action, horizon, process, archive, and diagnostic limits with fail-fast timeouts. |
| T-11-21-04 | Information disclosure | CLI, CI, and artifact diagnostics | mitigate | Redact secrets, private identities, paths, raw records, stack traces, and unbounded stderr; retain only bounded semantic context. |
| T-11-21-05 | Elevation of privilege | dependency/process boundary | mitigate | Keep renderer, benchmark, protocol, differential, and C++ dependencies private and absent from liquidfun's published graph. |
| T-11-21-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped automated command, ordered Rust commit gate, and any named Markdown, workflow, package, or sanitizer checks.
</verification>

<success-criteria>
- Local canonical and sanitizer artifacts validate completely but remain D2 and non-promotable.
- One manual Oracle CI run creates a canonical and fail-fast sanitizer D1 pair at one immutable SHA.
- Commands are fixed, bounded, least-privilege, read-only for accepted authority, and never log secrets or raw unbounded diagnostics.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-21-SUMMARY.md`.</output>
