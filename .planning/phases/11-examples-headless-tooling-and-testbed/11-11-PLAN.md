---
phase: 11-examples-headless-tooling-and-testbed
plan: "11"
type: execute
wave: 4
depends_on: ["11-06", "11-07", "11-09", "11-10"]
files_modified:
  - crates/liquidfun-differential/src/catalog_native.rs
  - crates/liquidfun-differential/src/catalog_native/executor.rs
  - crates/liquidfun-differential/src/catalog_native/capture.rs
  - crates/liquidfun-differential/src/session/backend.rs
  - crates/liquidfun-differential/tests/catalog_native.rs
autonomous: true
requirements: ["RIGD-10", "EXMP-02", "EXMP-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "The native backend decodes and verifies resolved bytes before any World effect."
    - "All actions execute through curated public APIs with typed semantic ID maps and transactional failure behavior."
    - "Checkpoint capture uses only public observations and debug primitives and is deterministic across replay."
  artifacts:
    - path: "crates/liquidfun-differential/src/catalog_native/executor.rs"
      provides: "Resolved action execution through public APIs"
    - path: "crates/liquidfun-differential/src/catalog_native/capture.rs"
      provides: "Canonical checkpoint builder"
    - path: "crates/liquidfun-differential/tests/catalog_native.rs"
      provides: "Representative native execution, replay, and failure tests"
  key_links:
    - from: "crates/liquidfun-differential/src/session/backend.rs"
      to: "crates/liquidfun-differential/src/catalog_native.rs"
      via: "SessionBackend implementation"
      pattern: "NativeCatalogBackend"
    - from: "crates/liquidfun-differential/src/catalog_native/capture.rs"
      to: "liquidfun::World"
      via: "public observation and primitive APIs"
      pattern: "world_observation"
---

<objective>
Execute resolved catalog plans natively and capture canonical checkpoints.

Purpose: Connect the controller to public liquidfun APIs without duplicating scenario or observation logic.
Output: crates/liquidfun-differential/src/catalog_native/executor.rs, crates/liquidfun-differential/src/catalog_native/capture.rs, crates/liquidfun-differential/tests/catalog_native.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@crates/liquidfun-differential/src/rigid_world.rs
@crates/liquidfun-differential/src/rigid_world/phase10/native.rs
@crates/liquidfun/src/world/observation.rs
@crates/liquidfun/src/debug_draw.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the native catalog backend and authoritative capture builder</name>
  <files>crates/liquidfun-differential/src/catalog_native.rs, crates/liquidfun-differential/src/catalog_native/executor.rs, crates/liquidfun-differential/src/catalog_native/capture.rs, crates/liquidfun-differential/src/session/backend.rs, crates/liquidfun-differential/tests/catalog_native.rs</files>
  <read-first>11-CONTEXT.md D-06 through D-16 and D-23; Plan 11-06 registry, Plan 11-07 SessionBackend, Plan 11-09 debug primitives, Plan 11-10 wire types; existing NativeRigidWorldExecutor, Phase 9/10 native semantic maps, panic containment, and public observation APIs.</read-first>
  <behavior>
    - "The native backend decodes and verifies resolved bytes before any World effect."
    - "All actions execute through curated public APIs with typed semantic ID maps and transactional failure behavior."
  </behavior>
  <action>Implement NativeCatalogBackend by verifying the supplied resolved bytes/hash and executing their closed actions in exact order through existing public World, rope, query, callback, particle, and group APIs. Maintain typed semantic maps for body, fixture, joint, rope, system, group, and particle identities; validate ownership, liveness, and action references before each call. Reuse the existing rigid-world executor paths rather than reimplement formulas or Phase 9/10 behavior. Contain panics at the adapter boundary, restore or discard the whole current run on failure, and return a harness failure with bounded semantic context. Capture at declared action/step ordinals using WorldObservation and collect_debug_primitives; preserve source order and canonicalize only declared unordered sets; exclude durations. Assert the re-encoded checkpoint is canonical before returning.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cargo test -p liquidfun-differential --test catalog_native</automated>
  </verify>
  <acceptance-criteria>Representative rigid, joint, rope, particle, group, query, callback, and rejected-mutation runs pass; two identical replays yield byte-identical checkpoints; wrong hash/owner/stale ID/panic/limit failures produce no partial checkpoint and bounded harness errors.</acceptance-criteria>
  <done>The native engine consumes the same resolved plan and produces authoritative portable checkpoints.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| resolved bytes to native World mutation | Data crosses into a trusted Phase 11 component. |
| public observations to canonical checkpoint | Trusted semantic output crosses into another consumer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-11-01 | Tampering | resolved bytes to native World mutation | mitigate | Verify canonical hash/schema first, resolve typed owners, preflight bounds, contain panics, and discard failed sessions. |
| T-11-11-02 | Spoofing | public observations to canonical checkpoint | mitigate | Use only curated semantic APIs, validate finite/bounded output, and prohibit timing/private storage fields. |
| T-11-11-03 | Denial of service | resource use | mitigate | Enforce reviewed bytes, records, actions, geometry, processes, durations, and diagnostics before work or allocation. |
| T-11-11-04 | Information disclosure | errors and diagnostics | mitigate | Emit bounded semantic categories and IDs; redact raw records, private indices, pointers, stack traces, secrets, and unbounded stderr. |
| T-11-11-05 | Elevation of privilege | dependency/process isolation | mitigate | Keep renderer and C++ execution private, process-isolated, unpublished, and unreachable from the liquidfun production dependency graph. |
| T-11-11-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and the ordered repository Rust commit gate. For C++ changes, also run the named protocol target under the reviewed preset.
</verification>

<success-criteria>
- The native backend decodes and verifies resolved bytes before any World effect.
- All actions execute through curated public APIs with typed semantic ID maps and transactional failure behavior.
- Checkpoint capture uses only public observations and debug primitives and is deterministic across replay.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-11-SUMMARY.md`.</output>
