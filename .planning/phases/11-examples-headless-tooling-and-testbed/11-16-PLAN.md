---
phase: 11-examples-headless-tooling-and-testbed
plan: "16"
type: execute
wave: 6
depends_on: ["11-14"]
files_modified:
  - scenarios/catalog/rigid-stack-v1.json
  - scenarios/catalog/joint-rope-v1.json
  - scenarios/catalog/particle-group-v1.json
  - scenarios/regressions/catalog-manifest.json
  - crates/liquidfun-differential/src/fixtures/replay.rs
  - crates/liquidfun-differential/tests/catalog_regressions.rs
autonomous: true
requirements: ["EXMP-02", "EXMP-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Checked regressions persist canonical resolved bytes, hash, run identity, checkpoint schedule, and provenance."
    - "Replay rejects seed-only, hash-drifted, stale-schema, unknown-action, or path-escaping fixtures."
    - "Representative rigid, joint/rope, and particle/group fixtures execute identically through native replay."
  artifacts:
    - path: "scenarios/regressions/catalog-manifest.json"
      provides: "Strict content-addressed regression registry"
    - path: "scenarios/catalog/particle-group-v1.json"
      provides: "Representative canonical resolved fixture"
    - path: "crates/liquidfun-differential/tests/catalog_regressions.rs"
      provides: "Read-only replay and malformed fixture tests"
  key_links:
    - from: "crates/liquidfun-differential/src/fixtures/replay.rs"
      to: "scenarios/regressions/catalog-manifest.json"
      via: "typed fixture lookup and hash verification"
      pattern: "resolved_sha256"
    - from: "scenarios/catalog/rigid-stack-v1.json"
      to: "crates/liquidfun-differential/src/catalog_native.rs"
      via: "exact resolved-byte replay"
      pattern: "schema_version"
---

<objective>
Make canonical resolved bytes the durable regression input.

Purpose: Ensure named and seeded failures replay from content, not mutable generators or seed-only metadata.
Output: scenarios/regressions/catalog-manifest.json, scenarios/catalog/particle-group-v1.json, crates/liquidfun-differential/tests/catalog_regressions.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@scenarios/regressions/README.md
@crates/liquidfun-differential/src/fixtures/replay.rs
@reference/artifacts/manifest.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add content-addressed catalog regression fixtures and replay validation</name>
  <files>scenarios/catalog/rigid-stack-v1.json, scenarios/catalog/joint-rope-v1.json, scenarios/catalog/particle-group-v1.json, scenarios/regressions/catalog-manifest.json, crates/liquidfun-differential/src/fixtures/replay.rs, crates/liquidfun-differential/tests/catalog_regressions.rs</files>
  <read-first>11-CONTEXT.md D-07, D-11, D-23, D-24, and D-26; Plan 11-14 failure/replay integration; existing scenario and regression fixture lifecycle, artifact provenance, canonical JSON, and no-clobber promotion rules.</read-first>
  <behavior>
    - "Checked regressions persist canonical resolved bytes, hash, run identity, checkpoint schedule, and provenance."
    - "Replay rejects seed-only, hash-drifted, stale-schema, unknown-action, or path-escaping fixtures."
  </behavior>
  <action>Generate three reviewed representative resolved fixtures directly from the typed catalog: rigid stack/contact, joint plus standalone rope, and particle group/query/callback mutation. Store exact canonical resolved bytes with schema/scenario/generator versions, optional seed, exact settings, run and content hashes, checkpoint/action logs, upstream revision, provenance, and expected native D0 identity. Add a strict manifest and extend replay lookup to validate confinement, canonical bytes/hash, complete metadata, and catalog mapping before execution. Reject seed-only reconstruction, regenerated byte substitution, unknown fields/actions, stale versions, duplicate paths/hashes, symlinks, and records over limits. Tests canonicalize in memory and never rewrite tracked fixtures.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>cargo test -p liquidfun-differential --test catalog_regressions</automated>
  </verify>
  <acceptance-criteria>All representative fixtures replay byte-identically and malformed/hash-drifted/seed-only/path-escaping fixtures fail before execution; repeated checks leave tracked files unchanged.</acceptance-criteria>
  <done>Regression and failure replay use the same immutable resolved plans as every other consumer.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| tracked regression manifest to filesystem lookup | Untrusted input crosses into an effectful Phase 11 path. |
| fixture bytes to native execution | Validated output crosses a package, process, file, or authority boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-16-01 | Tampering | tracked regression manifest to filesystem lookup | mitigate | Validate relative allowlisted paths, reject links/traversal/collisions, and verify exact canonical hashes before reads. |
| T-11-16-02 | Spoofing | fixture bytes to native execution | mitigate | Decode bounded strict schema, validate run/catalog versions and actions, and fail before World creation on drift. |
| T-11-16-03 | Denial of service | bounded execution and artifacts | mitigate | Enforce reviewed byte, record, action, horizon, process, archive, and diagnostic limits with fail-fast timeouts. |
| T-11-16-04 | Information disclosure | CLI, CI, and artifact diagnostics | mitigate | Redact secrets, private identities, paths, raw records, stack traces, and unbounded stderr; retain only bounded semantic context. |
| T-11-16-05 | Elevation of privilege | dependency/process boundary | mitigate | Keep renderer, benchmark, protocol, differential, and C++ dependencies private and absent from liquidfun's published graph. |
| T-11-16-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped automated command, ordered Rust commit gate, and any named Markdown, workflow, package, or sanitizer checks.
</verification>

<success-criteria>
- Checked regressions persist canonical resolved bytes, hash, run identity, checkpoint schedule, and provenance.
- Replay rejects seed-only, hash-drifted, stale-schema, unknown-action, or path-escaping fixtures.
- Representative rigid, joint/rope, and particle/group fixtures execute identically through native replay.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-16-SUMMARY.md`.</output>
