---
phase: 11-examples-headless-tooling-and-testbed
plan: "25"
type: execute
wave: 9
depends_on: ["11-24"]
files_modified:
  - crates/liquidfun-testbed/src/app.rs
  - crates/liquidfun-testbed/src/app/state.rs
  - crates/liquidfun-testbed/src/theme.rs
  - crates/liquidfun-testbed/src/ui.rs
  - crates/liquidfun-testbed/src/ui/about.rs
  - crates/liquidfun-testbed/src/ui/scenario_browser.rs
  - crates/liquidfun-testbed/tests/app_shell.rs
autonomous: true
requirements: ["EXMP-04", "EXMP-05", "EXMP-06"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "The app owns only presentation state while scenario, controller, checkpoint, and comparison state remain immutable inputs."
    - "The first experience uses the approved dark palette, typography, spacing, focus, source/license/maintainer, and build provenance contracts."
    - "Scenario selection uses stable slugs/versions, searchable keyboard navigation, eligibility, and exact run identity rather than display titles."
  artifacts:
    - path: "crates/liquidfun-testbed/src/app/state.rs"
      provides: "Presentation-only app state"
    - path: "crates/liquidfun-testbed/src/theme.rs"
      provides: "Exact approved theme/typography/spacing tokens"
    - path: "crates/liquidfun-testbed/src/ui/about.rs"
      provides: "Source, license, Peter/OpenLinks, version, commit, and build provenance"
  key_links:
    - from: "crates/liquidfun-testbed/src/ui/scenario_browser.rs"
      to: "reference/scenario-catalog.json"
      via: "typed registry projection for browsing"
      pattern: "catalog_slug"
    - from: "crates/liquidfun-testbed/src/app.rs"
      to: "crates/liquidfun-differential/src/session.rs"
      via: "read-only state and typed command submission"
      pattern: "SessionState"
---

<objective>
Build the passive visual testbed shell, dark design system, catalog browser, and provenance chrome.

Purpose: Establish D-19 and D-21 presentation boundaries before viewport controls and differences are added.
Output: crates/liquidfun-testbed/src/app/state.rs, crates/liquidfun-testbed/src/theme.rs, crates/liquidfun-testbed/src/ui/about.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@crates/liquidfun-testbed/CAPABILITY.md
@reference/scenario-catalog.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the app shell, design tokens, scenario browser, and About panel</name>
  <files>crates/liquidfun-testbed/src/app.rs, crates/liquidfun-testbed/src/app/state.rs, crates/liquidfun-testbed/src/theme.rs, crates/liquidfun-testbed/src/ui.rs, crates/liquidfun-testbed/src/ui/about.rs, crates/liquidfun-testbed/src/ui/scenario_browser.rs, crates/liquidfun-testbed/tests/app_shell.rs</files>
  <read-first>11-CONTEXT.md D-19 through D-22; every UI-SPEC section, especially Design System, Spacing, Typography, Color, Layout, Copywriting, Provenance, and Accessibility; Plan 11-24 selected renderer; catalog registry/mapping API and controller state.</read-first>
  <behavior>
    - "The app owns only presentation state while scenario, controller, checkpoint, and comparison state remain immutable inputs."
    - "The first experience uses the approved dark palette, typography, spacing, focus, source/license/maintainer, and build provenance contracts."
  </behavior>
  <action>Implement a five-region app shell at 1280px with 48px app bar, 280px scenario rail, fluid minimum-480px viewport slot, 360px inspector slot, and 64px control slot, using only the UI-SPEC 4/8/16/24/32/48/64 spacing, 4/8 radii, 44px targets, four font sizes, weights 400/600, and exact dark/accessibility color tokens. AppState may hold only camera, panels, scroll, focus, filters, selected semantic key, and screenshot options; physics/controller/comparison values are read-only models. Add searchable keyboard catalog rows with display title, stable slug/version, seed and eligibility. Implement exact empty/loading/error/status copy. About and app bar show source GitHub URL, truthful MIT Rust project plus separate upstream notices, Peter Ryszkiewicz/OpenLinks, version, short linked commit, profile, target, Rust toolchain, protocol/adapter/run/oracle identity and evidence tier, with literal Unavailable fallbacks and safe external open or copyable URLs. Add pure layout/theme/state tests.</action>
  <pre-commit>Before any tracked task commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>cargo test -p liquidfun-testbed --test app_shell</automated>
  </verify>
  <acceptance-criteria>Tests assert exact tokens/layout regions/copy, 44px targets, focus ring/contrast metadata, stable-slug selection, presentation-only state, safe provenance fallbacks/URLs, and absence of physics/backend/render-loop ownership.</acceptance-criteria>
  <done>`app_shell` tests pass the five required shell regions and exact design tokens, stable slug/version catalog identity, source/license/maintainer/build provenance with literal `Unavailable` fallbacks, 44px/focus/contrast accessibility metadata, and a passive adapter boundary where `AppState` stores only presentation state and effects use typed controller commands.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| catalog/provenance strings to visible UI and links | Untrusted state crosses into an effectful or authoritative Phase 11 operation. |
| UI state to controller state | Validated output crosses a package, process, file, UI, or evidence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-25-01 | Tampering | catalog/provenance strings to visible UI and links | mitigate | Bound/sanitize text, use allowlisted HTTPS source/license/OpenLinks targets, safe external open, and literal Unavailable for missing values. |
| T-11-25-02 | Spoofing | UI state to controller state | mitigate | Separate presentation-only AppState types and permit effects only through typed SessionCommand submission. |
| T-11-25-03 | Denial of service | resource use | mitigate | Enforce reviewed input, geometry, image, archive, process, horizon, diagnostic, and UI limits with fail-fast timeouts. |
| T-11-25-04 | Information disclosure | diagnostics, UI, CI, and reports | mitigate | Redact secrets, raw records, private indices, pointers, stack traces, and unbounded stderr; expose only bounded semantic context and safe URLs. |
| T-11-25-05 | Elevation of privilege | dependency, renderer, and oracle isolation | mitigate | Keep all renderer and C++ capabilities private, unpublished, non-default, least-privilege, and unreachable from liquidfun production dependencies. |
| T-11-25-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and all relevant Rust, C++, workflow, package, corpus, Markdown, renderer, or exact-reference gates stated by the task.
</verification>

<success-criteria>
- The app owns only presentation state while scenario, controller, checkpoint, and comparison state remain immutable inputs.
- The first experience uses the approved dark palette, typography, spacing, focus, source/license/maintainer, and build provenance contracts.
- Scenario selection uses stable slugs/versions, searchable keyboard navigation, eligibility, and exact run identity rather than display titles.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-25-SUMMARY.md`.</output>
