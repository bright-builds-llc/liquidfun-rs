---
phase: 11-examples-headless-tooling-and-testbed
plan: "12"
type: execute
wave: 4
depends_on: ["11-06", "11-10"]
files_modified:
  - tools/reference/src/protocol.hpp
  - tools/reference/src/protocol.cpp
  - tools/reference/src/main.cpp
  - tools/reference/src/catalog_run.hpp
  - tools/reference/src/catalog_run.cpp
  - tools/reference/src/catalog_checkpoint.hpp
  - tools/reference/src/catalog_checkpoint.cpp
  - tools/reference/src/adapter-inputs.txt
  - tools/reference/CMakeLists.txt
  - tools/reference/tests/protocol_tests.cpp
autonomous: true
requirements: ["EXMP-03", "EXMP-05"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "The C++ child validates the exact resolved request and emits the same canonical checkpoint schema."
    - "Stable semantic maps hide every C++ pointer and dense row from protocol identity."
    - "Malformed requests, exceptions, reset failures, and output-limit violations remain harness failures and do not poison later requests."
  artifacts:
    - path: "tools/reference/src/catalog_run.cpp"
      provides: "Strict resolved-plan decode and upstream execution"
    - path: "tools/reference/src/catalog_checkpoint.cpp"
      provides: "Portable semantic observation and primitive capture"
    - path: "tools/reference/tests/protocol_tests.cpp"
      provides: "Malformed, multi-request reset, and schema parity tests"
  key_links:
    - from: "tools/reference/src/main.cpp"
      to: "tools/reference/src/catalog_run.cpp"
      via: "closed request dispatch"
      pattern: "catalog_run"
    - from: "tools/reference/src/catalog_checkpoint.cpp"
      to: "crates/liquidfun-test-protocol/src/checkpoint.rs"
      via: "mirrored canonical JSONL contract"
      pattern: "checkpoint"
---

<objective>
Execute canonical resolved plans in the pinned C++ oracle process.

Purpose: Extend the existing isolated oracle rather than create a parallel example harness.
Output: tools/reference/src/catalog_run.cpp, tools/reference/src/catalog_checkpoint.cpp, tools/reference/tests/protocol_tests.cpp.
</objective>

<scope-rationale>
This is one executable plan because protocol dispatch, strict catalog decoding/execution, canonical checkpoint capture, adapter-input provenance, CMake registration, and the protocol test target form one inseparable C++ process boundary: no intermediate subset can satisfy the shared schema, reset, stdout-purity, or exception-isolation contract. The implementation remains internally divided across `catalog_run` and `catalog_checkpoint` translation units and is verified by one existing cohesive protocol target, keeping the task bounded without creating an invalid partially wired oracle state.
</scope-rationale>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@tools/reference/src/rigid_world.cpp
@tools/reference/src/rigid_world_decode.hpp
@tools/reference/src/rigid_world_phase10_execute.hpp
@tools/reference/src/rigid_world_phase10_capture.hpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the long-lived oracle for resolved catalog execution and capture</name>
  <files>tools/reference/src/protocol.hpp, tools/reference/src/protocol.cpp, tools/reference/src/main.cpp, tools/reference/src/catalog_run.hpp, tools/reference/src/catalog_run.cpp, tools/reference/src/catalog_checkpoint.hpp, tools/reference/src/catalog_checkpoint.cpp, tools/reference/src/adapter-inputs.txt, tools/reference/CMakeLists.txt, tools/reference/tests/protocol_tests.cpp</files>
  <read-first>11-CONTEXT.md D-06 through D-16, D-23, and D-26; Plan 11-10 protocol schema; existing main/protocol/rigid_world decode and execution layers; Phase 9/10 semantic maps and capture; adapter-input digest contract; protocol test and sanitizer conventions.</read-first>
  <behavior>
    - "The C++ child validates the exact resolved request and emits the same canonical checkpoint schema."
    - "Stable semantic maps hide every C++ pointer and dense row from protocol identity."
  </behavior>
  <action>Add one closed catalog-run request kind to the existing stdin/stdout JSONL process. Strictly parse verified resolved bytes and execute the existing rigid-world/particle/group actions inside one fresh b2World, with typed source IDs mapped to private pointers only inside the request. Add upstream observations for counts/tree metrics, contacts, particle contacts, broad-phase AABBs, particle statistics, profiles by name only, and debug primitives with the same stable semantic keys and canonical ordering rules as Plan 11-10. Catch every exception per request, emit protocol records only on stdout, diagnostics only on bounded stderr, destroy the world and clear all maps before trace_end, and advance reset epoch. Unknown actions/observations/primitives/policies, hash mismatch, bounds, malformed values, or incomplete reset are harness failures. Add all behavior-affecting files to adapter-inputs and build targets; test a rejected request followed by a valid request plus sanitizer-compatible bounds.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cmake --build target/reference/oracle-debug --target liquidfun-reference-protocol-tests && ctest --test-dir target/reference/oracle-debug --output-on-failure --no-tests=error -R '^liquidfun-reference-protocol$'</automated>
  </verify>
  <acceptance-criteria>Protocol tests pass valid representative catalog requests, exact schema bytes, stable IDs/order, exception containment, malformed/oversized rejection, stdout purity, bounded stderr, and reset epochs across reused requests.</acceptance-criteria>
  <done>The pinned oracle executes the same resolved plan and returns portable semantic checkpoints in isolation.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| untrusted JSONL to C++ process | Data crosses into a trusted Phase 11 component. |
| C++ pointers and diagnostics to Rust | Trusted semantic output crosses into another consumer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-12-01 | Tampering | untrusted JSONL to C++ process | mitigate | Use duplicate-aware bounded decoding, exact enums/versions/hashes, finite checks, and fresh-world request isolation. |
| T-11-12-02 | Spoofing | C++ pointers and diagnostics to Rust | mitigate | Translate pointers to declared semantic IDs, emit no raw addresses, bound/redact stderr, and reset all maps before success. |
| T-11-12-03 | Denial of service | resource use | mitigate | Enforce reviewed bytes, records, actions, geometry, processes, durations, and diagnostics before work or allocation. |
| T-11-12-04 | Information disclosure | errors and diagnostics | mitigate | Emit bounded semantic categories and IDs; redact raw records, private indices, pointers, stack traces, secrets, and unbounded stderr. |
| T-11-12-05 | Elevation of privilege | dependency/process isolation | mitigate | Keep renderer and C++ execution private, process-isolated, unpublished, and unreachable from the liquidfun production dependency graph. |
| T-11-12-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and the ordered repository Rust commit gate. For C++ changes, also run the named protocol target under the reviewed preset.
</verification>

<success-criteria>
- The C++ child validates the exact resolved request and emits the same canonical checkpoint schema.
- Stable semantic maps hide every C++ pointer and dense row from protocol identity.
- Malformed requests, exceptions, reset failures, and output-limit violations remain harness failures and do not poison later requests.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-12-SUMMARY.md`.</output>
