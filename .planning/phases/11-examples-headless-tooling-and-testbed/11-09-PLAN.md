---
phase: 11-examples-headless-tooling-and-testbed
plan: "09"
type: execute
wave: 2
depends_on: ["11-08"]
files_modified:
  - crates/liquidfun/src/debug_draw.rs
  - crates/liquidfun/src/debug_draw/primitive.rs
  - crates/liquidfun/src/debug_draw/collector.rs
  - crates/liquidfun/src/lib.rs
  - crates/liquidfun/tests/debug_draw.rs
autonomous: true
requirements: ["RIGD-10", "EXMP-05"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Shapes, joints, contacts/normals, particle contacts, broad-phase AABBs, centers of mass, and labels are represented by a closed primitive enum."
    - "Every primitive carries a stable owner/key, named layer, explicit geometry, and color/style metadata."
    - "Declared unordered primitive sets canonicalize by stable keys while source-significant order is preserved."
  artifacts:
    - path: "crates/liquidfun/src/debug_draw/primitive.rs"
      provides: "Public owner, key, layer, style, and primitive enums"
    - path: "crates/liquidfun/src/debug_draw/collector.rs"
      provides: "Bounded semantic observation-to-primitive collector"
    - path: "crates/liquidfun/tests/debug_draw.rs"
      provides: "Primitive stability, bounds, and no-private-identity tests"
  key_links:
    - from: "crates/liquidfun/src/debug_draw/collector.rs"
      to: "crates/liquidfun/src/world/observation.rs"
      via: "geometry derived from semantic observations"
      pattern: "WorldObservation"
    - from: "crates/liquidfun/src/debug_draw/collector.rs"
      to: "crates/liquidfun/src/debug_draw/primitive.rs"
      via: "owned primitive output"
      pattern: "DebugPrimitive"
---

<objective>
Define and collect renderer-neutral debug primitives with stable semantic keys.

Purpose: Give headless and visual consumers one storage-opaque geometry vocabulary per D-13 through D-15.
Output: crates/liquidfun/src/debug_draw/primitive.rs, crates/liquidfun/src/debug_draw/collector.rs, crates/liquidfun/tests/debug_draw.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@crates/liquidfun/src/world/observation.rs
@crates/liquidfun/src/collision/shape.rs
@crates/liquidfun/src/joint/snapshot.rs
@third_party/liquidfun/liquidfun/Box2D/Box2D/Dynamics/b2WorldCallbacks.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the closed debug-draw model and bounded collector</name>
  <files>crates/liquidfun/src/debug_draw.rs, crates/liquidfun/src/debug_draw/primitive.rs, crates/liquidfun/src/debug_draw/collector.rs, crates/liquidfun/src/lib.rs, crates/liquidfun/tests/debug_draw.rs</files>
  <read-first>11-CONTEXT.md D-13 through D-15; UI-SPEC overlay layers/colors/diff geometry; Plan 11-08 observation API; public shape, joint snapshot, transform, AABB, particle view, and contact observation types; pinned b2WorldCallbacks debug-draw concepts for inventory only.</read-first>
  <behavior>
    - "Shapes, joints, contacts/normals, particle contacts, broad-phase AABBs, centers of mass, and labels are represented by a closed primitive enum."
    - "Every primitive carries a stable owner/key, named layer, explicit geometry, and color/style metadata."
  </behavior>
  <action>Add DebugOwnerKey, DebugPrimitiveKey, DebugLayer, DebugColor, DebugStroke, DebugFill, and a non-exhaustive closed DebugPrimitive vocabulary covering point, segment, polyline/polygon, circle, transform axes, AABB, arrow, and semantic label. Keys combine stable semantic owner identity, layer, primitive kind, child identity, and source occurrence or declared canonical ordinal; never use addresses, arena slots, proxy IDs, dense indices, screen coordinates, or visitation order as durable identity. Add reviewed primitive/vertex/text bounds and a World::collect_debug_primitives(options) collector derived from Plan 11-08 observations. Preserve source-significant contact/joint order; sort only declared unordered AABB/geometry sets by stable key and deterministic geometry tie-breaker. Add an optional narrow sink trait implemented by the owned collector without renderer dependencies.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cargo test -p liquidfun --all-features --test debug_draw</automated>
  </verify>
  <acceptance-criteria>Tests pass for every layer, deterministic two-run keys/order, explicit unordered canonicalization, bounds, exact geometry, semantic ownership, no private coordinates, and renderer-free dependency graph.</acceptance-criteria>
  <done>Any frontend can render the same stable semantic primitives without inspecting engine storage.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| semantic observations to geometry output | Data crosses into a trusted Phase 11 component. |
| primitive text/style to renderer | Trusted semantic output crosses into another consumer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-09-01 | Tampering | semantic observations to geometry output | mitigate | Validate finite geometry and reviewed bounds, derive keys only from stable public identities, and reject incomplete owners. |
| T-11-09-02 | Spoofing | primitive text/style to renderer | mitigate | Bound strings and vertices, use closed layers/styles, and avoid executable markup, paths, or renderer commands. |
| T-11-09-03 | Denial of service | resource use | mitigate | Enforce reviewed bytes, records, actions, geometry, processes, durations, and diagnostics before work or allocation. |
| T-11-09-04 | Information disclosure | errors and diagnostics | mitigate | Emit bounded semantic categories and IDs; redact raw records, private indices, pointers, stack traces, secrets, and unbounded stderr. |
| T-11-09-05 | Elevation of privilege | dependency/process isolation | mitigate | Keep renderer and C++ execution private, process-isolated, unpublished, and unreachable from the liquidfun production dependency graph. |
| T-11-09-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and the ordered repository Rust commit gate. For C++ changes, also run the named protocol target under the reviewed preset.
</verification>

<success-criteria>
- Shapes, joints, contacts/normals, particle contacts, broad-phase AABBs, centers of mass, and labels are represented by a closed primitive enum.
- Every primitive carries a stable owner/key, named layer, explicit geometry, and color/style metadata.
- Declared unordered primitive sets canonicalize by stable keys while source-significant order is preserved.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-09-SUMMARY.md`.</output>
