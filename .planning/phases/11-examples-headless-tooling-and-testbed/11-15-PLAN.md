---
phase: 11-examples-headless-tooling-and-testbed
plan: "15"
type: execute
wave: 6
depends_on: ["11-14"]
files_modified:
  - crates/liquidfun-differential/src/main.rs
  - tools/xtask/src/differential.rs
  - tools/xtask/src/main.rs
  - tools/xtask/tests/catalog_cli.rs
  - Justfile
autonomous: true
requirements: ["EXMP-02", "EXMP-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Contributors can list, inspect, resolve, run, pause, step, restart, act, capture, replay, and compare scenarios by stable slug."
    - "CLI output distinguishes semantic checkpoints, screenshots, profiles, physics mismatches, and harness failures."
    - "All arguments are closed, validated, bounded, and translated into controller commands before effects."
  artifacts:
    - path: "tools/xtask/src/differential.rs"
      provides: "Allowlisted headless catalog command routing"
    - path: "crates/liquidfun-differential/src/main.rs"
      provides: "Machine and human headless session CLI"
    - path: "tools/xtask/tests/catalog_cli.rs"
      provides: "Command shape, exit code, and injection tests"
  key_links:
    - from: "tools/xtask/src/differential.rs"
      to: "crates/liquidfun-differential/src/main.rs"
      via: "fixed structured child arguments"
      pattern: "catalog"
    - from: "Justfile"
      to: "tools/xtask/src/differential.rs"
      via: "thin visible aliases"
      pattern: "catalog-list"
---

<objective>
Expose the catalog controller and comparison workflow through headless commands.

Purpose: Give contributors a renderer-free operational surface for selection, seed resolution, controls, settings, capture, replay, and comparison.
Output: tools/xtask/src/differential.rs, crates/liquidfun-differential/src/main.rs, tools/xtask/tests/catalog_cli.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@tools/xtask/src/differential.rs
@crates/liquidfun-differential/src/main.rs
@Justfile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the bounded headless catalog command surface</name>
  <files>crates/liquidfun-differential/src/main.rs, tools/xtask/src/differential.rs, tools/xtask/src/main.rs, tools/xtask/tests/catalog_cli.rs, Justfile</files>
  <read-first>11-CONTEXT.md D-09 through D-11, D-20, D-23, and D-26; UI-SPEC copy/state/settings contracts; Plan 11-14 runner; existing differential CLI allowlists, structured invocation, exit codes, and thin Justfile recipes.</read-first>
  <behavior>
    - "Contributors can list, inspect, resolve, run, pause, step, restart, act, capture, replay, and compare scenarios by stable slug."
    - "CLI output distinguishes semantic checkpoints, screenshots, profiles, physics mismatches, and harness failures."
  </behavior>
  <action>Extend the existing differential command with `catalog list`, `catalog inspect --scenario`, `catalog run`, `catalog replay`, and `catalog compare`. Run accepts stable slug, optional declared seed, finite positive exact timestep, velocity/position/particle iterations 1 through 1024, a bounded typed command script for pause/resume/step/restart/scenario-action/capture, named oracle preset/session profile, and fixed output mode. Print run identity, resolved hash, controller state, logical step/action ordinal, checkpoint IDs, comparison summary, and diagnostic profile separately; never label wall time or screenshots as compatibility. Unknown scenario/command/option, invalid settings, oversized scripts, or oracle absence returns stable distinct exit/error categories. Add fixed Just recipes with no logic. Tests use a fake tool to prove argument confinement and no shell interpolation.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>cargo test -p xtask --test catalog_cli && cargo run -p xtask -- catalog list</automated>
  </verify>
  <acceptance-criteria>Tests pass every command/exit category and injection case; list is deterministic; headless commands need no renderer or graphical environment; pause/step/restart/capture semantics match the controller.</acceptance-criteria>
  <done>Contributors can exercise the full Phase 11 workflow headlessly through documented fixed commands.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CLI arguments to controller and subprocesses | Untrusted input crosses into an effectful Phase 11 path. |
| headless diagnostics to terminal/files | Validated output crosses a package, process, file, or authority boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-15-01 | Tampering | CLI arguments to controller and subprocesses | mitigate | Parse closed options into domain values, cap scripts/outputs, use argument arrays, and allowlist presets and scenarios. |
| T-11-15-02 | Spoofing | headless diagnostics to terminal/files | mitigate | Redact untrusted content, bound values and stderr, label evidence tiers, and never print private indices or secrets. |
| T-11-15-03 | Denial of service | bounded execution and artifacts | mitigate | Enforce reviewed byte, record, action, horizon, process, archive, and diagnostic limits with fail-fast timeouts. |
| T-11-15-04 | Information disclosure | CLI, CI, and artifact diagnostics | mitigate | Redact secrets, private identities, paths, raw records, stack traces, and unbounded stderr; retain only bounded semantic context. |
| T-11-15-05 | Elevation of privilege | dependency/process boundary | mitigate | Keep renderer, benchmark, protocol, differential, and C++ dependencies private and absent from liquidfun's published graph. |
| T-11-15-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped automated command, ordered Rust commit gate, and any named Markdown, workflow, package, or sanitizer checks.
</verification>

<success-criteria>
- Contributors can list, inspect, resolve, run, pause, step, restart, act, capture, replay, and compare scenarios by stable slug.
- CLI output distinguishes semantic checkpoints, screenshots, profiles, physics mismatches, and harness failures.
- All arguments are closed, validated, bounded, and translated into controller commands before effects.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-15-SUMMARY.md`.</output>
