---
phase: 11-examples-headless-tooling-and-testbed
plan: "29"
type: execute
wave: 13
depends_on: ["11-18", "11-23", "11-27", "11-28"]
files_modified:
  - tools/xtask/src/package.rs
  - tools/xtask/tests/package_cli.rs
  - .github/workflows/ci.yml
  - ARCHITECTURE.md
  - TESTING.md
  - COMPATIBILITY.md
autonomous: true
requirements: ["RIGD-10", "TEST-03", "EXMP-01", "EXMP-02", "EXMP-03", "EXMP-04", "EXMP-05", "EXMP-06"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "All eight Phase 11 requirements and all 26 locked decisions have executable passing evidence with zero unexplained corpus rows."
    - "liquidfun remains the sole default publishable renderer-free crate and packages/tests without testbed, upstream source, reference data, or graphics."
    - "Architecture/testing/compatibility documentation states exact semantic authority, UI diagnostic limits, evidence tiers, commands, and remaining Phase 12 scope truthfully."
  artifacts:
    - path: "tools/xtask/src/package.rs"
      provides: "Final workspace/dependency/archive/headless isolation gate"
    - path: "ARCHITECTURE.md"
      provides: "Catalog, controller, checkpoint, comparison, oracle, and renderer boundaries"
    - path: "TESTING.md"
      provides: "Corpus/headless/evidence/testbed commands and authority rules"
  key_links:
    - from: "tools/xtask/src/package.rs"
      to: "Cargo.toml"
      via: "default/publish/dependency graph enforcement"
      pattern: "default-members"
    - from: "TESTING.md"
      to: "UPSTREAM-CORPUS.md"
      via: "semantic closure workflow"
      pattern: "check-closure"
    - from: "ARCHITECTURE.md"
      to: "crates/liquidfun-testbed"
      via: "passive adapter boundary"
      pattern: "renderer-neutral"
---

<objective>
Run the final Phase 11 isolation, documentation, evidence, and no-gap audit.

Purpose: Prove the integrated headless and optional visual deliverable while leaving Phase 12 claims explicitly deferred.
Output: tools/xtask/src/package.rs, ARCHITECTURE.md, TESTING.md.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@UPSTREAM-CORPUS.md
@reference/upstream-corpus.json
@reference/scenario-catalog.json
@crates/liquidfun-testbed/CAPABILITY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seal package isolation, docs, CI, and full decision coverage</name>
  <files>tools/xtask/src/package.rs, tools/xtask/tests/package_cli.rs, .github/workflows/ci.yml, ARCHITECTURE.md, TESTING.md, COMPATIBILITY.md</files>
  <read-first>11-CONTEXT.md D-01 through D-26 and Deferred Ideas; UI-SPEC approved contract; Plans 11-18, 11-23, 11-27, and 11-28 summaries; current package verifier/CI/docs; ROADMAP Phase 11 goal and requirements; repository Markdown and Rust verification rules.</read-first>
  <behavior>
    - "All eight Phase 11 requirements and all 26 locked decisions have executable passing evidence with zero unexplained corpus rows."
    - "liquidfun remains the sole default publishable renderer-free crate and packages/tests without testbed, upstream source, reference data, or graphics."
  </behavior>
  <action>Strengthen package verification after the testbed exists: Cargo metadata must show liquidfun as the sole default publishable package with no renderer/window/game-engine/C++/protocol/differential/benchmark/testbed dependency or feature; inspect the `.crate`, then build/test it in a temporary external copy with third_party/reference/tools/testbed removed and display variables unset. CI keeps Cargo consumer jobs submodule-free, runs corpus snapshot/report closure without native source, compiles/tests private tooling and the selected testbed without needing a display, and confines live discovery/oracle/sanitizers to oracle jobs. Update ARCHITECTURE with one-way catalog resolution, controller clocks, public observations/debug primitives, checkpoint/comparison, process oracle, benchmark/regression convergence, and passive renderer dependency direction. Update TESTING with exact headless/testbed/corpus/D0-D3/replay/failure/acquisition commands and diagnostic exclusions. Ensure COMPATIBILITY reflects only Plan 11-23 authority and zero corpus gaps, with Phase 12 performance/platform/coverage/packaging/release claims still pending. Add a checked decision matrix test or documented audit mapping D-01 through D-26 and every requirement to exact tests/artifacts. Run full required gates, corpus closure, package isolation, workflow checks, Markdown, dependency policy, and pinned-submodule read-only diff.</action>
  <pre-commit>Before any tracked task commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <markdown-check>If this task changes repository-owned non-GSD Markdown, run `just markdown-check` before commit. Never format `.planning/**` with mdformat.</markdown-check>
  <verify>
    <automated>cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && cargo xtask inventory corpus check-snapshot && cargo xtask inventory corpus check-closure && cargo xtask package verify && cargo xtask check && cargo deny check --locked && actionlint .github/workflows/ci.yml .github/workflows/oracle.yml && just markdown-check && git diff --exit-code -- third_party/liquidfun</automated>
  </verify>
  <acceptance-criteria>Every command passes; D-01 through D-26 and all requirement IDs map to concrete green evidence; zero corpus gaps remain; liquidfun package isolation is proven after renderer addition; docs make no Phase 12 or pixel/timing compatibility claim.</acceptance-criteria>
  <done>Phase 11 is fully implemented, evidence-backed, renderer-isolated, and ready for verification.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| workspace/testbed/tooling graph to published consumer | Untrusted state crosses into an effectful or authoritative Phase 11 operation. |
| generated docs and evidence to public maturity | Validated output crosses a package, process, file, UI, or evidence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-29-01 | Tampering | workspace/testbed/tooling graph to published consumer | mitigate | Resolve Cargo metadata, inspect archive allowlists, build/test an isolated crate, and keep private packages non-default/unpublished. |
| T-11-29-02 | Spoofing | generated docs and evidence to public maturity | mitigate | Cross-check machine authorities, zero-gap corpus, exact D1 scope, diagnostic exclusions, and deferred Phase 12 claims before publication. |
| T-11-29-03 | Denial of service | resource use | mitigate | Enforce reviewed input, geometry, image, archive, process, horizon, diagnostic, and UI limits with fail-fast timeouts. |
| T-11-29-04 | Information disclosure | diagnostics, UI, CI, and reports | mitigate | Redact secrets, raw records, private indices, pointers, stack traces, and unbounded stderr; expose only bounded semantic context and safe URLs. |
| T-11-29-05 | Elevation of privilege | dependency, renderer, and oracle isolation | mitigate | Keep all renderer and C++ capabilities private, unpublished, non-default, least-privilege, and unreachable from liquidfun production dependencies. |
| T-11-29-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and all relevant Rust, C++, workflow, package, corpus, Markdown, renderer, or exact-reference gates stated by the task.
</verification>

<success-criteria>
- All eight Phase 11 requirements and all 26 locked decisions have executable passing evidence with zero unexplained corpus rows.
- liquidfun remains the sole default publishable renderer-free crate and packages/tests without testbed, upstream source, reference data, or graphics.
- Architecture/testing/compatibility documentation states exact semantic authority, UI diagnostic limits, evidence tiers, commands, and remaining Phase 12 scope truthfully.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-29-SUMMARY.md`.</output>
