---
phase: 11-examples-headless-tooling-and-testbed
plan: "10"
type: execute
wave: 3
depends_on: ["11-03", "11-09"]
files_modified:
  - crates/liquidfun-test-protocol/src/lib.rs
  - crates/liquidfun-test-protocol/src/catalog/wire.rs
  - crates/liquidfun-test-protocol/src/checkpoint.rs
  - crates/liquidfun-test-protocol/src/checkpoint/observation.rs
  - crates/liquidfun-test-protocol/src/checkpoint/primitive.rs
  - crates/liquidfun-test-protocol/src/schema/checkpoint.rs
  - crates/liquidfun-test-protocol/tests/checkpoint_protocol.rs
autonomous: true
requirements: ["RIGD-10", "EXMP-02", "EXMP-03", "EXMP-05"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Resolved bytes and run identity cross the process boundary without re-running catalog generators."
    - "Canonical checkpoints encode stable observations and primitives with explicit order/canonicalization declarations."
    - "Unknown actions, observations, primitive kinds, policies, duplicate keys, malformed floats, or oversized records fail as harness errors."
  artifacts:
    - path: "crates/liquidfun-test-protocol/src/catalog/wire.rs"
      provides: "Validated resolved-scenario request envelope"
    - path: "crates/liquidfun-test-protocol/src/checkpoint.rs"
      provides: "Canonical checkpoint identity and record graph"
    - path: "crates/liquidfun-test-protocol/src/schema/checkpoint.rs"
      provides: "Strict JSONL decode/encode and schema presentation"
  key_links:
    - from: "crates/liquidfun-test-protocol/src/catalog/wire.rs"
      to: "crates/liquidfun-test-protocol/src/catalog/model.rs"
      via: "verified resolved bytes and hash"
      pattern: "ResolvedScenario"
    - from: "crates/liquidfun-test-protocol/src/schema/checkpoint.rs"
      to: "crates/liquidfun-test-protocol/src/checkpoint.rs"
      via: "strict bounded record decode"
      pattern: "CanonicalCheckpoint"
---

<objective>
Define strict resolved-scenario and canonical-checkpoint wire contracts.

Purpose: Give native Rust and the process-isolated oracle one bounded semantic currency for catalog execution and comparison.
Output: crates/liquidfun-test-protocol/src/catalog/wire.rs, crates/liquidfun-test-protocol/src/checkpoint.rs, crates/liquidfun-test-protocol/src/schema/checkpoint.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@crates/liquidfun-test-protocol/src/codec.rs
@crates/liquidfun-test-protocol/src/trace.rs
@crates/liquidfun-test-protocol/src/schema/rigid_world.rs
@crates/liquidfun/src/debug_draw/primitive.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the versioned resolved request and canonical checkpoint protocol</name>
  <files>crates/liquidfun-test-protocol/src/lib.rs, crates/liquidfun-test-protocol/src/catalog/wire.rs, crates/liquidfun-test-protocol/src/checkpoint.rs, crates/liquidfun-test-protocol/src/checkpoint/observation.rs, crates/liquidfun-test-protocol/src/checkpoint/primitive.rs, crates/liquidfun-test-protocol/src/schema/checkpoint.rs, crates/liquidfun-test-protocol/tests/checkpoint_protocol.rs</files>
  <read-first>11-CONTEXT.md D-07, D-08, D-10, D-12 through D-16, and D-23; Plans 11-03 and 11-09 types; existing strict codec/limits/failure/trace/schema patterns; Phase 4 tolerance policy registry; Phase 10 protocol malformed and ownership tests.</read-first>
  <behavior>
    - "Resolved bytes and run identity cross the process boundary without re-running catalog generators."
    - "Canonical checkpoints encode stable observations and primitives with explicit order/canonicalization declarations."
  </behavior>
  <action>Add a versioned CatalogRunRequest carrying request ID, exact resolved bytes, resolved SHA-256, catalog/scenario/generator versions, optional seed, exact settings, checkpoint schedule, and build/provenance requirements. Add CanonicalCheckpoint with stable checkpoint ID, action or logical-step ordinal, simulation time bits, structural observations, numeric observations bound to closed Phase 4 policy paths, ordered occurrences, explicitly unordered sets, debug primitives, and profile-name presence only. Exclude wall durations, frames, pixels, and render order. Mirror the public observation/primitive vocabulary into engine-neutral wire types with explicit stable keys and finite exact bit transport. Decode through bounded duplicate-aware strict structs; reject unknown or private fields, duplicate/dangling IDs, ordering violations, invalid hashes, non-finite values outside reviewed bit fields, and every one-over bound before allocation. Generate and byte-check the schema presentation.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cargo test -p liquidfun-test-protocol --test checkpoint_protocol && cargo test -p liquidfun-test-protocol --test fixtures</automated>
  </verify>
  <acceptance-criteria>Tests pass canonical round trips and reject unknown, duplicate, malformed, wrong-hash, wrong-version, dangling, order-violating, and oversized records; profile durations, frames, pixels, and private identities are unrepresentable.</acceptance-criteria>
  <done>Both engines share one strict request/checkpoint contract for all Phase 11 consumers.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| JSONL resolved requests and checkpoints to typed protocol | Data crosses into a trusted Phase 11 component. |
| semantic keys and hashes to comparator | Trusted semantic output crosses into another consumer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-10-01 | Tampering | JSONL resolved requests and checkpoints to typed protocol | mitigate | Enforce framing, depth, byte, record, vertex, string, and collection bounds before allocation; deny unknown and duplicate fields. |
| T-11-10-02 | Spoofing | semantic keys and hashes to comparator | mitigate | Verify request/hash/version/provenance identity and all ownership/order relationships before accepting a checkpoint. |
| T-11-10-03 | Denial of service | resource use | mitigate | Enforce reviewed bytes, records, actions, geometry, processes, durations, and diagnostics before work or allocation. |
| T-11-10-04 | Information disclosure | errors and diagnostics | mitigate | Emit bounded semantic categories and IDs; redact raw records, private indices, pointers, stack traces, secrets, and unbounded stderr. |
| T-11-10-05 | Elevation of privilege | dependency/process isolation | mitigate | Keep renderer and C++ execution private, process-isolated, unpublished, and unreachable from the liquidfun production dependency graph. |
| T-11-10-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and the ordered repository Rust commit gate. For C++ changes, also run the named protocol target under the reviewed preset.
</verification>

<success-criteria>
- Resolved bytes and run identity cross the process boundary without re-running catalog generators.
- Canonical checkpoints encode stable observations and primitives with explicit order/canonicalization declarations.
- Unknown actions, observations, primitive kinds, policies, duplicate keys, malformed floats, or oversized records fail as harness errors.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-10-SUMMARY.md`.</output>
