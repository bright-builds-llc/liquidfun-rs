---
phase: 11-examples-headless-tooling-and-testbed
plan: "13"
type: execute
wave: 4
depends_on: ["11-10"]
files_modified:
  - crates/liquidfun-differential/src/comparison_model.rs
  - crates/liquidfun-differential/src/comparison_model/diff.rs
  - crates/liquidfun-differential/src/comparison_model/policy.rs
  - crates/liquidfun-differential/src/lib.rs
  - crates/liquidfun-differential/tests/comparison_model.rs
autonomous: true
requirements: ["RIGD-10", "EXMP-05"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Comparison records exact matches, within-policy numeric differences, mismatches, Rust-only, and oracle-only observations in canonical path order."
    - "Structural identity, kinds, flags, counts, membership, presence, and source-significant order compare exactly."
    - "Every numeric path binds one closed Phase 4 policy and emits bounded contextual values plus stable primitive focus keys."
  artifacts:
    - path: "crates/liquidfun-differential/src/comparison_model.rs"
      provides: "Public private-tool comparison result model"
    - path: "crates/liquidfun-differential/src/comparison_model/diff.rs"
      provides: "Exhaustive checkpoint field walk"
    - path: "crates/liquidfun-differential/tests/comparison_model.rs"
      provides: "Exact, numeric, missing-side, order, and bounds tests"
  key_links:
    - from: "crates/liquidfun-differential/src/comparison_model/diff.rs"
      to: "crates/liquidfun-test-protocol/src/checkpoint.rs"
      via: "exhaustive checkpoint comparison"
      pattern: "CanonicalCheckpoint"
    - from: "crates/liquidfun-differential/src/comparison_model/policy.rs"
      to: "crates/liquidfun-test-protocol/src/tolerance.rs"
      via: "closed named numeric policy"
      pattern: "TolerancePolicy"
---

<objective>
Produce one complete renderer-neutral semantic comparison model.

Purpose: Ensure headless reports and visual overlays agree without re-reading engine state.
Output: crates/liquidfun-differential/src/comparison_model.rs, crates/liquidfun-differential/src/comparison_model/diff.rs, crates/liquidfun-differential/tests/comparison_model.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-UI-SPEC.md
@crates/liquidfun-differential/src/comparator.rs
@crates/liquidfun-differential/src/report.rs
@crates/liquidfun-differential/src/rigid_world/phase10/comparator.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exhaustive semantic diff construction</name>
  <files>crates/liquidfun-differential/src/comparison_model.rs, crates/liquidfun-differential/src/comparison_model/diff.rs, crates/liquidfun-differential/src/comparison_model/policy.rs, crates/liquidfun-differential/src/lib.rs, crates/liquidfun-differential/tests/comparison_model.rs</files>
  <read-first>11-CONTEXT.md D-14 through D-16 and D-23; UI-SPEC Difference Presentation, state, and accessibility contracts; Plan 11-10 CanonicalCheckpoint; existing comparator helpers, SemanticPath, MismatchKind, MismatchReport, Phase 4 and Phase 10 policy registries.</read-first>
  <behavior>
    - "Comparison records exact matches, within-policy numeric differences, mismatches, Rust-only, and oracle-only observations in canonical path order."
    - "Structural identity, kinds, flags, counts, membership, presence, and source-significant order compare exactly."
  </behavior>
  <action>Add ComparisonState values ExactMatch, WithinPolicy, PhysicsMismatch, RustOnly, and OracleOnly; ComparisonEntry with canonical SemanticPath, structural kind, named policy, bounded Rust/oracle values, optional stable primitive keys, and diagnostic context; and ComparisonModel with checkpoint/run identities and ordered entries. Perform one exhaustive source-ordered field walk. Compare schema/run/checkpoint IDs, stable owners, kinds, flags, counts, membership, presence, and source-significant sequences exactly; canonicalize only fields declared unordered by the checkpoint schema; route every numeric geometry field through a closed explicit Phase 4 policy binding. Missing sides become RustOnly/OracleOnly, never Match. Exclude duration values. Reuse existing mismatch report/signature builders so first divergence and minimization remain stable. Bound entries and rendered values; reject unknown/unbound/wildcard/private paths as harness errors.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cargo test -p liquidfun-differential --test comparison_model</automated>
  </verify>
  <acceptance-criteria>Tests pass exact, within-policy, true mismatch, Rust-only, oracle-only, ordering, canonical-set, duplicate, unknown-policy, duration-exclusion, focused primitive key, and at-limit/one-over cases.</acceptance-criteria>
  <done>All presentations consume one authoritative complete semantic diff model.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| two checkpoints to comparison authority | Data crosses into a trusted Phase 11 component. |
| comparison values to UI and reports | Trusted semantic output crosses into another consumer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-13-01 | Tampering | two checkpoints to comparison authority | mitigate | Verify matching run/checkpoint/schema identity and exhaustively bind every field before comparison. |
| T-11-13-02 | Spoofing | comparison values to UI and reports | mitigate | Bound and redact values/context, keep stable semantic paths, and never expose pointers, private indices, or raw records. |
| T-11-13-03 | Denial of service | resource use | mitigate | Enforce reviewed bytes, records, actions, geometry, processes, durations, and diagnostics before work or allocation. |
| T-11-13-04 | Information disclosure | errors and diagnostics | mitigate | Emit bounded semantic categories and IDs; redact raw records, private indices, pointers, stack traces, secrets, and unbounded stderr. |
| T-11-13-05 | Elevation of privilege | dependency/process isolation | mitigate | Keep renderer and C++ execution private, process-isolated, unpublished, and unreachable from the liquidfun production dependency graph. |
| T-11-13-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped command and the ordered repository Rust commit gate. For C++ changes, also run the named protocol target under the reviewed preset.
</verification>

<success-criteria>
- Comparison records exact matches, within-policy numeric differences, mismatches, Rust-only, and oracle-only observations in canonical path order.
- Structural identity, kinds, flags, counts, membership, presence, and source-significant order compare exactly.
- Every numeric path binds one closed Phase 4 policy and emits bounded contextual values plus stable primitive focus keys.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-13-SUMMARY.md`.</output>
