---
phase: 11-examples-headless-tooling-and-testbed
plan: "07"
type: execute
wave: 2
depends_on: ["11-03"]
files_modified:
  - crates/liquidfun-differential/src/lib.rs
  - crates/liquidfun-differential/src/session.rs
  - crates/liquidfun-differential/src/session/state.rs
  - crates/liquidfun-differential/src/session/tests.rs
autonomous: true
requirements: ["EXMP-02", "EXMP-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Pause executes no tick and creates no checkpoint; step once executes one tick and remains paused."
    - "Restart destroys the session and recreates step zero from identical resolved bytes and validated settings."
    - "Controller commands and checkpoints use action/logical-step ordinals, never frames, wall time, or UI event timing."
  artifacts:
    - path: "crates/liquidfun-differential/src/session/state.rs"
      provides: "Pure closed session state machine"
    - path: "crates/liquidfun-differential/src/session.rs"
      provides: "Thin backend shell and command API"
    - path: "crates/liquidfun-differential/src/session/tests.rs"
      provides: "Transition, settings, and reentrancy tests"
  key_links:
    - from: "crates/liquidfun-differential/src/session.rs"
      to: "crates/liquidfun-test-protocol/src/catalog.rs"
      via: "resolved scenario input"
      pattern: "ResolvedScenario"
    - from: "crates/liquidfun-differential/src/session.rs"
      to: "session backend trait"
      via: "typed command execution and capture"
      pattern: "SessionBackend"
---

<objective>
Implement the renderer-neutral run-session controller.

Purpose: Make D-09 and D-10 deterministic and independently testable before adapters or UI input exist.
Output: crates/liquidfun-differential/src/session/state.rs, crates/liquidfun-differential/src/session.rs, crates/liquidfun-differential/src/session/tests.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@crates/liquidfun-differential/src/runner.rs
@crates/liquidfun-test-protocol/src/catalog.rs
@crates/liquidfun/src/world/config.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the pure controller state machine and thin backend shell</name>
  <files>crates/liquidfun-differential/src/lib.rs, crates/liquidfun-differential/src/session.rs, crates/liquidfun-differential/src/session/state.rs, crates/liquidfun-differential/src/session/tests.rs</files>
  <read-first>11-CONTEXT.md D-09 through D-11, D-19, and D-20; UI-SPEC controller/state/settings contracts; Plan 11-03 ResolvedScenario and RunSettings; differential runner error and ownership patterns; world config iteration bounds.</read-first>
  <behavior>
    - "Pause executes no tick and creates no checkpoint; step once executes one tick and remains paused."
    - "Restart destroys the session and recreates step zero from identical resolved bytes and validated settings."
  </behavior>
  <action>Define SessionState variants NoSelection, Resolving, ReadyPaused, Running, Stepping, Comparing, RecoverableError, and HarnessFailure plus typed SessionCommand values Select, Run, Pause, StepOnce, Restart, ApplySettingsAndRestart, ApplyScenarioAction, and CaptureCheckpoint. Keep a pure transition function over validated inputs and a thin SessionController shell over a SessionBackend trait. Reject reentrant commands and invalid state transitions; validate finite positive timestep and iterations 1 through 1024 before backend effects. Pause performs no backend tick or capture; StepOnce pauses first when necessary, runs exactly one logical tick, then stays paused; Restart drops the backend session and rebuilds from the same resolved bytes/hash. Checkpoints bind stable IDs to explicit action or logical-step ordinals. Particle-system pause is only ApplyScenarioAction. Add one-concern Arrange/Act/Assert tests for every transition and rollback on backend error.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cargo test -p liquidfun-differential session::tests</automated>
  </verify>
  <acceptance-criteria>All transition tests pass, including no-tick pause, one-tick step, identical-byte restart, settings rejection before effects, controller versus particle pause, checkpoint ordinal identity, and duplicate-command exclusion.</acceptance-criteria>
  <done>Headless and visual frontends can submit typed commands without owning physics time or state.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| frontend commands to session state | Untrusted or effectful data crosses into Phase 11 domain logic. |
| controller to backend effects | Validated authority crosses into downstream consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-07-01 | Tampering | frontend commands to session state | mitigate | Closed command/state enums, validated settings, and state-specific admission prevent privilege or schedule bypass. |
| T-11-07-02 | Spoofing | controller to backend effects | mitigate | One in-flight command, rollback on error, bounded diagnostics, and no render-clock input keep execution deterministic. |
| T-11-07-03 | Denial of service | bounded collections and work | mitigate | Enforce reviewed byte, record, action, entity, and diagnostic limits before allocation or execution. |
| T-11-07-04 | Information disclosure | errors and logs | mitigate | Emit bounded semantic IDs and categories; never expose private indices, raw records, pointers, secrets, or unbounded stderr. |
| T-11-07-05 | Elevation of privilege | dependency and process boundary | mitigate | Keep production Cargo and published liquidfun free of renderer, oracle, protocol, and foreign runtime dependencies. |
| T-11-07-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped automated command, then the repository Rust commit gate. Confirm tracked authority changes are deterministic and read-only checks leave the worktree clean.
</verification>

<success-criteria>
- Pause executes no tick and creates no checkpoint; step once executes one tick and remains paused.
- Restart destroys the session and recreates step zero from identical resolved bytes and validated settings.
- Controller commands and checkpoints use action/logical-step ordinals, never frames, wall time, or UI event timing.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-07-SUMMARY.md`.</output>
