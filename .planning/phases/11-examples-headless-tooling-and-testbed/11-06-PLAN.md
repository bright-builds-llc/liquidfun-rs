---
phase: 11-examples-headless-tooling-and-testbed
plan: "06"
type: execute
wave: 3
depends_on: ["11-04", "11-05"]
files_modified:
  - crates/liquidfun-test-protocol/src/catalog/scenarios.rs
  - crates/liquidfun-test-protocol/src/catalog/mapping.rs
  - crates/liquidfun-test-protocol/tests/catalog_registry.rs
  - reference/scenario-catalog.json
autonomous: true
requirements: ["TEST-03", "EXMP-01", "EXMP-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Every catalog slug/version is unique and appears in one deterministic registry order."
    - "Every native scenario maps to tests, oracle or equivalent evidence, regressions, benchmarks, and visualization eligibility."
    - "The checked catalog projection is deterministic, reviewable, and not a second writable authority."
  artifacts:
    - path: "crates/liquidfun-test-protocol/src/catalog/scenarios.rs"
      provides: "Closed typed catalog registry"
    - path: "crates/liquidfun-test-protocol/src/catalog/mapping.rs"
      provides: "Validated cross-consumer mapping records"
    - path: "reference/scenario-catalog.json"
      provides: "Deterministic machine-readable registry projection"
  key_links:
    - from: "crates/liquidfun-test-protocol/src/catalog/scenarios.rs"
      to: "crates/liquidfun-test-protocol/src/catalog/scenarios/rigid.rs"
      via: "family registry composition"
      pattern: "definitions"
    - from: "crates/liquidfun-test-protocol/src/catalog/mapping.rs"
      to: "reference/scenario-catalog.json"
      via: "canonical in-memory projection check"
      pattern: "render"
---

<objective>
Seal the scenario registry and machine-readable consumer mappings.

Purpose: Create the stable join target required by corpus closure and every execution consumer.
Output: crates/liquidfun-test-protocol/src/catalog/scenarios.rs, crates/liquidfun-test-protocol/src/catalog/mapping.rs, reference/scenario-catalog.json.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@crates/liquidfun-test-protocol/src/catalog.rs
@reference/compatibility.json
@reference/artifacts/manifest.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the closed registry, mappings, and deterministic projection</name>
  <files>crates/liquidfun-test-protocol/src/catalog/scenarios.rs, crates/liquidfun-test-protocol/src/catalog/mapping.rs, crates/liquidfun-test-protocol/tests/catalog_registry.rs, reference/scenario-catalog.json</files>
  <read-first>11-CONTEXT.md D-07, D-11, D-23 through D-25; Plans 11-04 and 11-05 family modules; reference/compatibility.json evidence IDs; reference/artifacts/manifest.toml regression/provenance vocabulary; protocol schema presentation tests for read-only generated checks.</read-first>
  <behavior>
    - "Every catalog slug/version is unique and appears in one deterministic registry order."
    - "Every native scenario maps to tests, oracle or equivalent evidence, regressions, benchmarks, and visualization eligibility."
  </behavior>
  <action>Compose every family into one static registry sorted by stable slug then scenario version. Add CatalogMapping with non-empty test IDs, exactly one oracle or reviewed-equivalent evidence disposition, regression use, benchmark eligibility, visualization eligibility, upstream corpus IDs, and compatibility leaf references. Reject duplicates, missing referenced definitions, stale evidence IDs, contradictory eligibility, title-as-identity, and a seeded scenario without generator identity/version. Render reference/scenario-catalog.json deterministically in memory and add a check that compares exact tracked bytes without rewriting. The typed Rust registry remains runtime authority; the JSON file is a review/join projection.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cargo test -p liquidfun-test-protocol --test catalog_registry && cargo xtask differential check-protocol</automated>
  </verify>
  <acceptance-criteria>Registry and projection tests pass; every scenario has a full closed mapping; projection bytes are stable; duplicate/stale/unmapped records fail; the check is read-only.</acceptance-criteria>
  <done>Corpus, runners, regressions, benchmarks, and the testbed can reference one closed scenario registry.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| registry projection to consumers | Untrusted or effectful data crosses into Phase 11 domain logic. |
| mapping claims to evidence authority | Validated authority crosses into downstream consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-06-01 | Tampering | registry projection to consumers | mitigate | Validate canonical bytes against typed authority and reject drift, duplicates, or stale references. |
| T-11-06-02 | Spoofing | mapping claims to evidence authority | mitigate | Resolve every test, evidence, fixture, corpus, and compatibility ID before accepting eligibility. |
| T-11-06-03 | Denial of service | bounded collections and work | mitigate | Enforce reviewed byte, record, action, entity, and diagnostic limits before allocation or execution. |
| T-11-06-04 | Information disclosure | errors and logs | mitigate | Emit bounded semantic IDs and categories; never expose private indices, raw records, pointers, secrets, or unbounded stderr. |
| T-11-06-05 | Elevation of privilege | dependency and process boundary | mitigate | Keep production Cargo and published liquidfun free of renderer, oracle, protocol, and foreign runtime dependencies. |
| T-11-06-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped automated command, then the repository Rust commit gate. Confirm tracked authority changes are deterministic and read-only checks leave the worktree clean.
</verification>

<success-criteria>
- Every catalog slug/version is unique and appears in one deterministic registry order.
- Every native scenario maps to tests, oracle or equivalent evidence, regressions, benchmarks, and visualization eligibility.
- The checked catalog projection is deterministic, reviewable, and not a second writable authority.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-06-SUMMARY.md`.</output>
