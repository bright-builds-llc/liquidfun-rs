---
phase: 11-examples-headless-tooling-and-testbed
plan: "04"
type: execute
wave: 2
depends_on: ["11-03"]
files_modified:
  - crates/liquidfun-test-protocol/src/catalog/scenarios/rigid.rs
  - crates/liquidfun-test-protocol/src/catalog/scenarios/joints.rs
  - crates/liquidfun-test-protocol/src/catalog/scenarios/rope.rs
autonomous: true
requirements: ["EXMP-01", "EXMP-03"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Rigid scenarios cover non-colliding, contacts, stacks, sleep, CCD, filtering, queries, callbacks, mutation, and destruction."
    - "All eleven joint kinds and standalone rope have reviewed catalog cases without backend code."
    - "Each definition declares tests, evidence, regression, benchmark, and visualization eligibility."
  artifacts:
    - path: "crates/liquidfun-test-protocol/src/catalog/scenarios/rigid.rs"
      provides: "Rigid world and query/callback scenario definitions"
    - path: "crates/liquidfun-test-protocol/src/catalog/scenarios/joints.rs"
      provides: "Eleven-joint semantic scenario definitions"
    - path: "crates/liquidfun-test-protocol/src/catalog/scenarios/rope.rs"
      provides: "Standalone rope scenarios"
  key_links:
    - from: "crates/liquidfun-test-protocol/src/catalog/scenarios/rigid.rs"
      to: "crates/liquidfun-test-protocol/src/catalog/model.rs"
      via: "typed definitions and eligibility mappings"
      pattern: "CatalogDefinition"
    - from: "crates/liquidfun-test-protocol/src/catalog/scenarios/joints.rs"
      to: "crates/liquidfun-test-protocol/src/scenario/rigid_world.rs"
      via: "closed action reuse"
      pattern: "Joint"
---

<objective>
Author representative rigid, joint, and standalone-rope catalog families.

Purpose: Express established public behaviors once as canonical plans for all downstream consumers.
Output: crates/liquidfun-test-protocol/src/catalog/scenarios/rigid.rs, crates/liquidfun-test-protocol/src/catalog/scenarios/joints.rs, crates/liquidfun-test-protocol/src/catalog/scenarios/rope.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@crates/liquidfun-differential/tests/fixtures/rigid_world/
@crates/liquidfun/tests/rigid_world.rs
@crates/liquidfun/tests/joint_contract.rs
@crates/liquidfun/tests/standalone_rope.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode rigid, joint, and rope scenarios as typed definitions</name>
  <files>crates/liquidfun-test-protocol/src/catalog/scenarios/rigid.rs, crates/liquidfun-test-protocol/src/catalog/scenarios/joints.rs, crates/liquidfun-test-protocol/src/catalog/scenarios/rope.rs</files>
  <read-first>11-CONTEXT.md D-06 through D-11 and D-24; Phase 6 through 10 rigid-world fixtures; public rigid, query, callback, joint, destruction, and standalone-rope integration tests; catalog model from Plan 11-03.</read-first>
  <behavior>
    - "Rigid scenarios cover non-colliding, contacts, stacks, sleep, CCD, filtering, queries, callbacks, mutation, and destruction."
    - "All eleven joint kinds and standalone rope have reviewed catalog cases without backend code."
  </behavior>
  <action>Create source-oriented typed definitions for the required representative families. Use stable lower-kebab slugs and version 1, concise display titles, explicit fixed or seeded generator declarations, exact settings, bounded setup/action/checkpoint schedules, stable semantic IDs, and mappings to existing test/evidence leaf IDs. Cover non-colliding, colliding, stacked, sleeping, fast-moving, filtered, queried, callback-controlled, mutated, destroyed, every joint kind, gear dependencies, and standalone rope. Definitions may compose catalog helpers but may not call World, inspect storage, branch on backend, or encode renderer callbacks. Add focused co-located unit tests proving unique slugs/actions/entities and deterministic resolution for each family.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cargo test -p liquidfun-test-protocol catalog::scenarios::rigid && cargo test -p liquidfun-test-protocol catalog::scenarios::joints && cargo test -p liquidfun-test-protocol catalog::scenarios::rope</automated>
  </verify>
  <acceptance-criteria>All family tests pass; every required rigid/joint/rope behavior has a unique resolvable definition and closed consumer mapping; no definition imports engine, oracle, filesystem, or renderer code.</acceptance-criteria>
  <done>One canonical catalog expresses the complete representative rigid, joint, and rope scenario surface.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| authored definitions to action execution | Untrusted or effectful data crosses into Phase 11 domain logic. |
| scenario mapping to maturity claims | Validated authority crosses into downstream consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-04-01 | Tampering | authored definitions to action execution | mitigate | Closed constructors, stable unique IDs, finite exact values, and reviewed action/checkpoint bounds prevent malformed plans. |
| T-11-04-02 | Spoofing | scenario mapping to maturity claims | mitigate | Require explicit existing evidence IDs and separate eligibility flags; missing or stale references remain invalid. |
| T-11-04-03 | Denial of service | bounded collections and work | mitigate | Enforce reviewed byte, record, action, entity, and diagnostic limits before allocation or execution. |
| T-11-04-04 | Information disclosure | errors and logs | mitigate | Emit bounded semantic IDs and categories; never expose private indices, raw records, pointers, secrets, or unbounded stderr. |
| T-11-04-05 | Elevation of privilege | dependency and process boundary | mitigate | Keep production Cargo and published liquidfun free of renderer, oracle, protocol, and foreign runtime dependencies. |
| T-11-04-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped automated command, then the repository Rust commit gate. Confirm tracked authority changes are deterministic and read-only checks leave the worktree clean.
</verification>

<success-criteria>
- Rigid scenarios cover non-colliding, contacts, stacks, sleep, CCD, filtering, queries, callbacks, mutation, and destruction.
- All eleven joint kinds and standalone rope have reviewed catalog cases without backend code.
- Each definition declares tests, evidence, regression, benchmark, and visualization eligibility.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-04-SUMMARY.md`.</output>
