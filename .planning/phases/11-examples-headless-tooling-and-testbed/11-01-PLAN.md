---
phase: 11-examples-headless-tooling-and-testbed
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/xtask/src/inventory.rs
  - tools/xtask/src/inventory/corpus.rs
  - tools/xtask/src/inventory/corpus/model.rs
  - tools/xtask/tests/corpus_model.rs
  - tools/xtask/tests/fixtures/corpus/valid-minimal.json
  - tools/xtask/tests/fixtures/corpus/invalid-disposition.json
autonomous: true
requirements: ["TEST-03", "EXMP-01"]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-07-21T21-10-55
generated_at: 2026-07-21T22:20:00.000Z
must_haves:
  truths:
    - "Every semantic upstream test, example, and registered testbed entry has one source-derived typed identity."
    - "Disposition and compatibility impact are independent closed enums with terminal-outcome rules."
    - "Malformed, duplicate, vague, oversized, or path-escaping records fail before validation joins."
  artifacts:
    - path: "tools/xtask/src/inventory/corpus/model.rs"
      provides: "Strict corpus domain types and reviewed bounds"
    - path: "tools/xtask/src/inventory/corpus.rs"
      provides: "Corpus parsing and typed error boundary"
    - path: "tools/xtask/tests/corpus_model.rs"
      provides: "Focused positive and negative model tests"
  key_links:
    - from: "tools/xtask/src/inventory/corpus.rs"
      to: "tools/xtask/src/inventory/corpus/model.rs"
      via: "strict JSON parsing into validated types"
      pattern: "CorpusManifest"
    - from: "tools/xtask/tests/corpus_model.rs"
      to: "tools/xtask/src/inventory/corpus.rs"
      via: "boundary fixtures"
      pattern: "invalid_disposition"
---

<objective>
Define the strict semantic upstream-corpus authority.

Purpose: Create the typed, bounded vocabulary that D-01 through D-05 require before discovery or classification effects occur.
Output: tools/xtask/src/inventory/corpus/model.rs, tools/xtask/src/inventory/corpus.rs, tools/xtask/tests/corpus_model.rs.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-examples-headless-tooling-and-testbed/11-CONTEXT.md
@reference/discovery.json
@reference/compatibility.json
@tools/xtask/src/inventory.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the corpus manifest model and fail-closed parser</name>
  <files>tools/xtask/src/inventory.rs, tools/xtask/src/inventory/corpus.rs, tools/xtask/src/inventory/corpus/model.rs, tools/xtask/tests/corpus_model.rs, tools/xtask/tests/fixtures/corpus/valid-minimal.json, tools/xtask/tests/fixtures/corpus/invalid-disposition.json</files>
  <read-first>11-CONTEXT.md D-01 through D-05; tools/xtask/src/inventory.rs for compatibility/discovery enums, path checks, and error style; reference/discovery.json and reference/compatibility.json for revision and evidence identities; standards/core/architecture.md for parse-at-boundary invariant types.</read-first>
  <behavior>
    - "Every semantic upstream test, example, and registered testbed entry has one source-derived typed identity."
    - "Disposition and compatibility impact are independent closed enums with terminal-outcome rules."
  </behavior>
  <action>Add CorpusSchemaVersion, CorpusItemId, CorpusKind, Applicability, TerminalDisposition, CompatibilityImpact, EvidenceMapping, SourceIdentity, ReviewRecord, CorpusItem, and CorpusManifest. Require a stable source path plus GoogleTest symbol or TestEntries registration identity, exact upstream revision, separate applicability/disposition/impact, non-empty reviewed rationale for every non-port outcome, and bounded unique evidence references. Use deny-unknown-field raw structs and checked constructors; reject absolute paths, parent traversal, duplicate IDs/source identities, empty or self-referential rationales, unknown enum values, and every collection at limit plus one. Wire only the parser/model module into inventory; do not yet mutate authoritative manifests.</action>
  <pre-commit>Before the task's atomic commit, run in order and require all pass: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Do not commit an intentionally failing state.</pre-commit>
  <verify>
    <automated>cargo test -p xtask --test corpus_model -- --nocapture</automated>
  </verify>
  <acceptance-criteria>The test target passes and proves valid records round-trip while unknown fields, invalid paths, duplicate identities, vague rationale, and one-over-limit collections return stable typed errors.</acceptance-criteria>
  <done>The repository has one strict semantic corpus vocabulary ready for source discovery and cross-ledger closure.</done>
</task>

</tasks>

<threat-model>
This is an OWASP ASVS L1 / STRIDE threat model covering all six STRIDE categories. Unresolved high-severity ASVS L1 findings block this plan's execution and completion.
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| untrusted corpus JSON to typed authority | Untrusted or effectful data crosses into Phase 11 domain logic. |
| corpus identity to evidence joins | Validated authority crosses into downstream consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-01-01 | Tampering | untrusted corpus JSON to typed authority | mitigate | Reject unknown fields and variants, enforce byte/count/depth bounds, validate relative paths, and redact diagnostics to item IDs. |
| T-11-01-02 | Spoofing | corpus identity to evidence joins | mitigate | Require unique source-derived IDs, exact revision, explicit review metadata, and no unresolved references. |
| T-11-01-03 | Denial of service | bounded collections and work | mitigate | Enforce reviewed byte, record, action, entity, and diagnostic limits before allocation or execution. |
| T-11-01-04 | Information disclosure | errors and logs | mitigate | Emit bounded semantic IDs and categories; never expose private indices, raw records, pointers, secrets, or unbounded stderr. |
| T-11-01-05 | Elevation of privilege | dependency and process boundary | mitigate | Keep production Cargo and published liquidfun free of renderer, oracle, protocol, and foreign runtime dependencies. |
| T-11-01-06 | Repudiation | plan execution and evidence trail | mitigate | Preserve deterministic inputs, typed outcome categories, scoped verification output, and reviewable artifact/provenance identities so security-relevant actions and claims are attributable. |
</threat-model>

<verification>
Run the scoped automated command, then the repository Rust commit gate. Confirm tracked authority changes are deterministic and read-only checks leave the worktree clean.
</verification>

<success-criteria>
- Every semantic upstream test, example, and registered testbed entry has one source-derived typed identity.
- Disposition and compatibility impact are independent closed enums with terminal-outcome rules.
- Malformed, duplicate, vague, oversized, or path-escaping records fail before validation joins.
</success-criteria>

<output>After completion, create `.planning/phases/11-examples-headless-tooling-and-testbed/11-01-SUMMARY.md`.</output>
