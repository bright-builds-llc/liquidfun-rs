---
phase: 15-re-establish-candidate-evidence
plan: "05"
type: execute
wave: 5
depends_on: ["15-04"]
files_modified: ["scripts/phase12-release-evidence/common.sh","scripts/phase12-release-evidence/producer_validation.sh","scripts/phase12-release-evidence/identity_validation.sh","tools/xtask/tests/release_cli/manifest_cases.rs","scripts/phase12-miri.sh","scripts/phase12-rust-sanitizers.sh","scripts/phase12-coverage.sh",".github/workflows/safety.yml",".github/workflows/coverage.yml"]
autonomous: true
requirements: ["DOCS-09"]
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-09-14T16-41-28
generated_at: "2026-09-14T16:55:28.770Z"
must_haves:
  truths:
    - "Unsafe paths and incomplete or tampered producer payloads are rejected before aggregation; complete valid payload passes."
    - "Failed safety/coverage records survive retry, terminal status is explicit and no failure can produce successful identity."
  artifacts:
    - path: "scripts/phase12-release-evidence/common.sh"
      provides: "Validate confined complete raw producer payloads"
    - path: "scripts/phase12-miri.sh"
      provides: "Preserve safety and coverage failures by attempt"
  key_links:
    - from: "scripts/phase12-release-evidence/common.sh"
      to: "scripts/phase12-miri.sh"
      via: "Tested producer/consumer contract and explicit identity validation"
---

<objective>
Harden retained artifact boundaries and failure capture.
Purpose: Repair and prove the prerequisite without weakening frozen-source acceptance.
Output: Validate confined complete raw producer payloads; Preserve safety and coverage failures by attempt.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-re-establish-candidate-evidence/15-CONTEXT.md
@.planning/phases/15-re-establish-candidate-evidence/15-RESEARCH.md
@tools/xtask/src/release/validation.rs
@reference/release/required-evidence.toml

<interfaces>
Existing shell release boundary: check; prepare(candidate, output, release_run) is changed by Plan04 to import archive and identity; aggregate consumes exact producer run IDs; publish-identity-last emits success only after validation.
Existing typed commands: cargo xtask release audit --manifest PATH --candidate C --output json; release attestation validate-worktree --source PATH --manifest PATH --report PATH; release attestation validate with the same arguments plus --attestation-commit A.
Identity contract: full lowercase 40-hex source C, explicit provider/run/attempt/job/target/toolchain, exact SHA-256 content digests; 21 producer artifacts join to 19 required registry entries. D1 canonical, D2 portability and D3 diagnostic authority remain separate.
</interfaces>

Standing authority D-08 permits routine fixes, review, gates, commits and ordinary main pushes; not release publication, waivers, secret/account changes or fabricated review acknowledgment. Local gate baseline at d990dc6 passed in Linux ARM64 Docker; use image sha256:5f3d56072c0c734ebb2381c59f0a20db1330b8884134c4e3a9d369de2d810ea8 with repo at /workspace, liquidfun-rs-rustup:/usr/local/rustup, liquidfun-rs-cargo-registry:/usr/local/cargo/registry, liquidfun-rs-cargo-target:/cargo-target and CARGO_TARGET_DIR=/cargo-target. This local boundary does not replace canonical Linux x64 or native Windows evidence.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validate confined complete raw producer payloads</name>
  <files>scripts/phase12-release-evidence/common.sh, scripts/phase12-release-evidence/producer_validation.sh, scripts/phase12-release-evidence/identity_validation.sh, tools/xtask/tests/release_cli/manifest_cases.rs</files>
  <read-first>tools/xtask/src/release/validation.rs, reference/release/required-evidence.toml, scripts/phase12-release-evidence/common.sh, scripts/phase12-release-evidence/producer_validation.sh, scripts/phase12-release-evidence/identity_validation.sh, tools/xtask/tests/release_cli/manifest_cases.rs; read existing files before edits and the retained failure described below.</read-first>
  <behavior>Unsafe paths and incomplete or tampered producer payloads are rejected before aggregation; complete valid payload passes. Negative inputs and failed commands must produce a nonzero result with no success identity.</behavior>
  <action>Per D-02/D-05 reject absolute paths, dot/dot-dot and empty components, symlink files/ancestors, extra roots/files and duplicate artifact names before reading payloads. Apply the same lexical/component boundary to payload paths obtained from identities. Verify every digest in performance payload-files.sha256 against retained raw files, cardinality and size bounds, not only the manifest entry. Exercise public shell entrypoints with negative traversal, symlink, substituted raw bytes, missing logs, malformed summary, stale SHA/run/attempt and duplicate inputs; require precise failing boundary and preserve 21 expected artifacts/19 registry entries. Write the described behavioral regression first, run the exact automated test command and retain its expected failure; implement, rerun green, then simplify without reducing the assertion. Use Arrange/Act/Assert and bounded subprocesses.</action>
  <verify><automated>cargo test -p xtask --test release_cli --test release_attestation</automated></verify>
  <acceptance-criteria>Unsafe paths and incomplete or tampered producer payloads are rejected before aggregation; complete valid payload passes.</acceptance-criteria>
  <done>Unsafe paths and incomplete or tampered producer payloads are rejected before aggregation; complete valid payload passes. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve safety and coverage failures by attempt</name>
  <files>scripts/phase12-miri.sh, scripts/phase12-rust-sanitizers.sh, scripts/phase12-coverage.sh, .github/workflows/safety.yml, .github/workflows/coverage.yml</files>
  <read-first>tools/xtask/src/release/validation.rs, reference/release/required-evidence.toml, scripts/phase12-miri.sh, scripts/phase12-rust-sanitizers.sh, scripts/phase12-coverage.sh, .github/workflows/safety.yml, .github/workflows/coverage.yml; read existing files before edits and the retained failure described below.</read-first>
  <behavior>Failed safety/coverage records survive retry, terminal status is explicit and no failure can produce successful identity. Negative inputs and failed commands must produce a nonzero result with no success identity.</behavior>
  <action>Use unique run/attempt roots or refuse existing output, removing destructive candidate-directory preparation. Retain bounded logs, command identities, checksums, status and failure classification on failure; upload diagnostics with always() while publishing a successful identity only after all required checks. Keep separate Miri modes and coverage toolchains from Plan02. Validate a fail-then-fresh-success fixture and prove failed bytes remain unchanged. Apply existing time/log bounds rather than broad unlimited uploads. Write the described behavioral regression first, run the exact automated test command and retain its expected failure; implement, rerun green, then simplify without reducing the assertion. Use Arrange/Act/Assert and bounded subprocesses.</action>
  <verify><automated>cargo test -p xtask --test safety_evidence_contract --test coverage_workflow</automated></verify>
  <acceptance-criteria>Failed safety/coverage records survive retry, terminal status is explicit and no failure can produce successful identity.</acceptance-criteria>
  <done>Failed safety/coverage records survive retry, terminal status is explicit and no failure can produce successful identity. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>
</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Workflow input and downloaded bytes → local tooling | Candidate, paths, hashes and terminal identities must be checked before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-05-01 | S/T | scripts/phase12-release-evidence/common.sh | mitigate | Bind full candidate, exact target/toolchain and SHA-256 bytes; reject mismatched identity at the task boundary. |
| T-15-05-02 | R | Attempt records | mitigate | Preserve original failure logs and provider/run/attempt identities; no overwrite or inferred success. |
| T-15-05-03 | T/I | Retained payload paths | mitigate | Use normalized repository-relative paths with no traversal or symlink components and bounded payload validation before consumption. |

ASVS L1-relevant input/access/integrity controls apply to these tooling boundaries. There is no web login/session or new application authorization surface in this plan.
</threat-model>

<verification>
Run each task's focused automated command and the actual production/negative controls stated in its action. Commands needing canonical x64, pinned Miri or native Windows run at that exact boundary; a local ARM64 pass is supplemental.
Before every commit run in order: cargo fmt --all; cargo clippy --all-targets --all-features -- -D warnings; cargo build --all-targets --all-features; cargo test --all-features. Run bun scripts/bright-builds-check.ts all, required Markdown checks and git diff --check. Do not format .planning with mdformat.
For tool failures preserve the failed attempt, diagnose the exact boundary, repair and rerun affected proof. Never convert missing controlled-host credentials to acceptance. Source repairs after C require requalification and a fresh C.
</verification>

<success-criteria>
- Unsafe paths and incomplete or tampered producer payloads are rejected before aggregation; complete valid payload passes.
- Failed safety/coverage records survive retry, terminal status is explicit and no failure can produce successful identity.
- Every effect uses observed repository/ref/candidate identity and standing authority; no release publication or waiver.
</success-criteria>

<output>
Create the tracked summary after completion.
After completion, create .planning/phases/15-re-establish-candidate-evidence/15-05-SUMMARY.md with actual identities, tests, residual risks and decision coverage.
</output>

