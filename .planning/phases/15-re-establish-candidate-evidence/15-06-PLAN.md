---
phase: 15-re-establish-candidate-evidence
plan: "06"
type: execute
wave: 6
depends_on: ["15-05"]
files_modified: ["scripts/phase15-candidate-evidence.sh","scripts/phase15-candidate-evidence/dispatch.sh","scripts/phase15-candidate-evidence/retention.sh","tools/xtask/tests/phase15_candidate_evidence.rs","tools/xtask/src/docs/contracts.rs","tools/xtask/src/docs/contracts/validation.rs","tools/xtask/tests/docs_contract/promotion_and_workflow.rs","tools/xtask/tests/release_attestation.rs","RELEASE.md"]
autonomous: true
requirements: ["DOCS-09"]
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-09-14T16-41-28
generated_at: "2026-09-14T16:55:28.770Z"
must_haves:
  truths:
    - "Dispatch can be reconciled without duplicate effects; retained artifacts can be independently validated at a fresh checkout."
    - "Readiness requires accepted C/A evidence, fresh-checkout path restoration works, and metadata after A cannot be misidentified as source."
  artifacts:
    - path: "scripts/phase15-candidate-evidence.sh"
      provides: "Add a bounded candidate producer ledger and reconciliation commands"
    - path: "tools/xtask/src/docs/contracts.rs"
      provides: "Make readiness contracts conditional on valid explicit attestation"
  key_links:
    - from: "scripts/phase15-candidate-evidence.sh"
      to: "scripts/phase15-candidate-evidence/dispatch.sh"
      via: "Planned current-wave postcondition: entrypoint sources bounded dispatch implementation; not a pre-wave prerequisite."
      pattern: "source.*phase15-candidate-evidence/dispatch[.]sh"
    - from: "scripts/phase15-candidate-evidence.sh"
      to: "scripts/phase15-candidate-evidence/retention.sh"
      via: "Planned current-wave postcondition: entrypoint sources retention and independent validation implementation; not a pre-wave prerequisite."
      pattern: "source.*phase15-candidate-evidence/retention[.]sh"
---

<objective>
Prove dispatch, materialization and readiness before freeze.
Purpose: Repair and prove the prerequisite without weakening frozen-source acceptance.
Output: Add a bounded candidate producer ledger and reconciliation commands; Make readiness contracts conditional on valid explicit attestation.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-re-establish-candidate-evidence/15-CONTEXT.md
@.planning/phases/15-re-establish-candidate-evidence/15-RESEARCH.md
@tools/xtask/src/release/attestation.rs
@tools/xtask/src/docs/contracts.rs
@RELEASE.md

<interfaces>
Existing shell release boundary: check; prepare(candidate, output, release_run) is changed by Plan04 to import archive and identity; aggregate consumes exact producer run IDs; publish-identity-last emits success only after validation.
Existing typed commands: cargo xtask release audit --manifest PATH --candidate C --output json; release attestation validate-worktree --source PATH --manifest PATH --report PATH; release attestation validate with the same arguments plus --attestation-commit A.
Identity contract: full lowercase 40-hex source C, explicit provider/run/attempt/job/target/toolchain, exact SHA-256 content digests; 21 producer artifacts join to 19 required registry entries. D1 canonical, D2 portability and D3 diagnostic authority remain separate.
</interfaces>

Standing authority D-08 permits routine fixes, review, gates, commits and ordinary main pushes; not release publication, waivers, secret/account changes or fabricated review acknowledgment. Local gate baseline at d990dc6 passed in Linux ARM64 Docker; use image sha256:5f3d56072c0c734ebb2381c59f0a20db1330b8884134c4e3a9d369de2d810ea8 with repo at /workspace, liquidfun-rs-rustup:/usr/local/rustup, liquidfun-rs-cargo-registry:/usr/local/cargo/registry, liquidfun-rs-cargo-target:/cargo-target and CARGO_TARGET_DIR=/cargo-target. This local boundary does not replace canonical Linux x64 or native Windows evidence.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add a bounded candidate producer ledger and reconciliation commands</name>
  <files>scripts/phase15-candidate-evidence.sh, scripts/phase15-candidate-evidence/dispatch.sh, scripts/phase15-candidate-evidence/retention.sh, tools/xtask/tests/phase15_candidate_evidence.rs</files>
  <read-first>tools/xtask/src/release/attestation.rs, tools/xtask/src/docs/contracts.rs, RELEASE.md, scripts/phase15-candidate-evidence.sh, scripts/phase15-candidate-evidence/dispatch.sh, scripts/phase15-candidate-evidence/retention.sh, tools/xtask/tests/phase15_candidate_evidence.rs; read existing files before edits and the retained failure described below.</read-first>
  <behavior>Dispatch can be reconciled without duplicate effects; retained artifacts can be independently validated at a fresh checkout. Negative inputs and failed commands must produce a nonzero result with no success identity.</behavior>
  <action>Per D-01/D-04/D-05/D-08 create thin orchestration with check, dispatch, collect and validate-retained modes. Inputs are full candidate SHA, explicit workflow/run IDs and a new ignored attempt root; persist input intent before dispatch and terminal provider/run/attempt/artifact metadata, original archives, logs and SHA-256 inventories during collect. Reconcile uncertain dispatch by exact repository/workflow/ref/time/SHA and require a unique match before retry. For oracle.yml dispatch an exact resolved ref with evidence_phase=phase11 (no unsupported candidate input); validate returned head_sha=C and job identities. For other workflows use their actual declared inputs. Download exact 21 names, promptly retain bounded complete raw payloads and verify them independently. No new evidence schema that bypasses release audit. Tests fake gh to prove wrong repo/ref/SHA, duplicate uncertain run, partial download, overwritten destination and expired/missing payload fail. Expose validate-retained --candidate C --attempt-root ROOT for later plans, checking all expected producer identities against the closed registry. Write the described behavioral regression first, run the exact automated test command and retain its expected failure; implement, rerun green, then simplify without reducing the assertion. Use Arrange/Act/Assert and bounded subprocesses.</action>
  <verify><automated>cargo test -p xtask --test phase15_candidate_evidence</automated></verify>
  <acceptance-criteria>Dispatch can be reconciled without duplicate effects; retained artifacts can be independently validated at a fresh checkout.</acceptance-criteria>
  <done>Dispatch can be reconciled without duplicate effects; retained artifacts can be independently validated at a fresh checkout. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make readiness contracts conditional on valid explicit attestation</name>
  <files>tools/xtask/src/docs/contracts.rs, tools/xtask/src/docs/contracts/validation.rs, tools/xtask/tests/docs_contract/promotion_and_workflow.rs, tools/xtask/tests/release_attestation.rs, RELEASE.md</files>
  <read-first>tools/xtask/src/release/attestation.rs, tools/xtask/src/docs/contracts.rs, RELEASE.md, tools/xtask/src/docs/contracts.rs, tools/xtask/src/docs/contracts/validation.rs, tools/xtask/tests/docs_contract/promotion_and_workflow.rs, tools/xtask/tests/release_attestation.rs, RELEASE.md; read existing files before edits and the retained failure described below.</read-first>
  <behavior>Readiness requires accepted C/A evidence, fresh-checkout path restoration works, and metadata after A cannot be misidentified as source. Negative inputs and failed commands must produce a nonzero result with no success identity.</behavior>
  <action>Per D-03/D-09 replace hardcoded non-ready-only contracts with tested conditional projection using the existing attestation validator. Default/absent/tampered/stale records remain non-ready; only explicit valid C/A identity plus retained artifacts permit ready copy. Add fresh-checkout tests materializing target paths, validate worktree at C, commit only three JSON records at A directly after C, and validate explicit C..A even when invoked from later docs commit D. Reject intervening .planning/scripts/docs changes and tampered manifest/report/source bytes. Clarify prefreeze source-readiness versus postfreeze final verification in RELEASE.md before C; never widen the nine-path allowlist. This task leaves current public copy non-ready. Write the described behavioral regression first, run the exact automated test command and retain its expected failure; implement, rerun green, then simplify without reducing the assertion. Use Arrange/Act/Assert and bounded subprocesses.</action>
  <verify><automated>cargo test -p xtask --test docs_contract --test release_attestation</automated></verify>
  <acceptance-criteria>Readiness requires accepted C/A evidence, fresh-checkout path restoration works, and metadata after A cannot be misidentified as source.</acceptance-criteria>
  <done>Readiness requires accepted C/A evidence, fresh-checkout path restoration works, and metadata after A cannot be misidentified as source. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>
</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Workflow input and downloaded bytes → local tooling | Candidate, paths, hashes and terminal identities must be checked before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-06-01 | S/T | scripts/phase15-candidate-evidence.sh | mitigate | Bind full candidate, exact target/toolchain and SHA-256 bytes; reject mismatched identity at the task boundary. |
| T-15-06-02 | R | Attempt records | mitigate | Preserve original failure logs and provider/run/attempt identities; no overwrite or inferred success. |
| T-15-06-03 | T/I | Retained payload paths | mitigate | Use normalized repository-relative paths with no traversal or symlink components and bounded payload validation before consumption. |

ASVS L1-relevant input/access/integrity controls apply to these tooling boundaries. There is no web login/session or new application authorization surface in this plan.
</threat-model>

<verification>
Run each task's focused automated command and the actual production/negative controls stated in its action. Commands needing canonical x64, pinned Miri or native Windows run at that exact boundary; a local ARM64 pass is supplemental.
Before every commit run in order: cargo fmt --all; cargo clippy --all-targets --all-features -- -D warnings; cargo build --all-targets --all-features; cargo test --all-features. Run bun scripts/bright-builds-check.ts all, required Markdown checks and git diff --check. Do not format .planning with mdformat.
For tool failures preserve the failed attempt, diagnose the exact boundary, repair and rerun affected proof. Never convert missing controlled-host credentials to acceptance. Source repairs after C require requalification and a fresh C.
</verification>

<success-criteria>
- Dispatch can be reconciled without duplicate effects; retained artifacts can be independently validated at a fresh checkout.
- Readiness requires accepted C/A evidence, fresh-checkout path restoration works, and metadata after A cannot be misidentified as source.
- Every effect uses observed repository/ref/candidate identity and standing authority; no release publication or waiver.
</success-criteria>

<output>
Create the tracked summary after completion.
After completion, create .planning/phases/15-re-establish-candidate-evidence/15-06-SUMMARY.md with actual identities, tests, residual risks and decision coverage.
</output>

