---
phase: 15-re-establish-candidate-evidence
plan: "09"
type: execute
wave: 9
depends_on: ["15-08"]
files_modified: ["target/phase15-candidate/platform/ledger.json","target/phase15-candidate/oracle/ledger.json","target/phase15-candidate/safety/ledger.json","target/phase15-candidate/fuzz/ledger.json","target/phase15-candidate/regressions/ledger.json","target/phase15-candidate/coverage/ledger.json",".github/workflows/oracle.yml","scripts/phase12-release-evidence/producer_validation.sh","tools/reference/CMakeLists.txt","tools/reference/adapter-inputs.txt","tools/reference/src/catalog_checkpoint.cpp","tools/reference/src/catalog_checkpoint.hpp","tools/reference/src/catalog_run_session.cpp","tools/reference/src/catalog_particle_draw.hpp","tools/reference/tests/protocol_tests.cpp","tools/reference/tests/protocol_tests/catalog_particles.hpp","crates/liquidfun-differential/tests/catalog_round_trip.rs","tools/xtask/tests/phase11_evidence_cli/workflow.rs","tools/xtask/tests/release_cli/raw_payloads.rs","tools/xtask/tests/release_cli/coverage_payloads.sh","tools/xtask/src/phase11_evidence.rs","tools/xtask/src/phase11_evidence/content.rs","tools/xtask/tests/phase11_evidence_cli/exact.rs","tools/xtask/tests/phase11_evidence_cli/semantic_result.rs"]
autonomous: true
requirements: ["PLAT-01","PLAT-05","DOCS-09"]
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-09-14T16-41-28
generated_at: "2026-09-14T16:55:28.770Z"
must_haves:
  truths:
    - "Seven platform/package and two Phase11 artifacts are complete, retained and bind C; single archive hash agrees everywhere."
    - "Eleven additional independent producer artifacts are retained and valid at C; no zero-test, missing-log or mislabeled compiler evidence."
  artifacts:
    - path: "target/phase15-candidate/platform/ledger.json"
      provides: "Produce the shared package and canonical Phase11 pair"
    - path: "target/phase15-candidate/safety/ledger.json"
      provides: "Produce safety, fuzz, regressions and all coverage lanes"
  key_links:
    - from: ".github/workflows/release.yml"
      to: ".github/workflows/platform.yml"
      via: "Existing release download binds explicit producer run; current-wave production must retain matching C, job, attempt, target and payload hashes."
      pattern: "run-id: [$][{][{] inputs[.]platform_run_id [}][}]"
    - from: ".github/workflows/release.yml"
      to: ".github/workflows/oracle.yml"
      via: "Existing release download binds explicit producer run; current-wave production must retain matching C, job, attempt, target and payload hashes."
      pattern: "run-id: [$][{][{] inputs[.]oracle_run_id [}][}]"
    - from: ".github/workflows/release.yml"
      to: ".github/workflows/safety.yml"
      via: "Existing release download binds explicit producer run; current-wave production must retain matching C, job, attempt, target and payload hashes."
      pattern: "run-id: [$][{][{] inputs[.]safety_run_id [}][}]"
    - from: ".github/workflows/release.yml"
      to: ".github/workflows/fuzz.yml"
      via: "Existing release download binds explicit producer run; current-wave production must retain matching C, job, attempt, target and payload hashes."
      pattern: "run-id: [$][{][{] inputs[.]fuzz_run_id [}][}]"
    - from: ".github/workflows/release.yml"
      to: ".github/workflows/regressions.yml"
      via: "Existing release download binds explicit producer run; current-wave production must retain matching C, job, attempt, target and payload hashes."
      pattern: "run-id: [$][{][{] inputs[.]regressions_run_id [}][}]"
    - from: ".github/workflows/release.yml"
      to: ".github/workflows/coverage.yml"
      via: "Existing release download binds explicit producer run; current-wave production must retain matching C, job, attempt, target and payload hashes."
      pattern: "run-id: [$][{][{] inputs[.]coverage_run_id [}][}]"
---

<objective>
Collect all independent frozen-candidate producers.
Purpose: Complete actual candidate acceptance with retained, independently verifiable evidence.
Output: Produce the shared package and canonical Phase11 pair; Produce safety, fuzz, regressions and all coverage lanes.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-re-establish-candidate-evidence/15-CONTEXT.md
@.planning/phases/15-re-establish-candidate-evidence/15-RESEARCH.md
@reference/release/required-evidence.toml
@.github/workflows/platform.yml
@.github/workflows/fuzz.yml

<interfaces>
Existing shell release boundary: check; prepare(candidate, output, release_run) is changed by Plan04 to import archive and identity; aggregate consumes exact producer run IDs; publish-identity-last emits success only after validation.
Existing typed commands: cargo xtask release audit --manifest PATH --candidate C --output json; release attestation validate-worktree --source PATH --manifest PATH --report PATH; release attestation validate with the same arguments plus --attestation-commit A.
Identity contract: full lowercase 40-hex source C, explicit provider/run/attempt/job/target/toolchain, exact SHA-256 content digests; 21 producer artifacts join to 19 required registry entries. D1 canonical, D2 portability and D3 diagnostic authority remain separate.
</interfaces>

Standing authority D-08 permits routine fixes, review, gates, commits and ordinary main pushes; not release publication, waivers, secret/account changes or fabricated review acknowledgment. Local gate baseline at d990dc6 passed in Linux ARM64 Docker; use image sha256:5f3d56072c0c734ebb2381c59f0a20db1330b8884134c4e3a9d369de2d810ea8 with repo at /workspace, liquidfun-rs-rustup:/usr/local/rustup, liquidfun-rs-cargo-registry:/usr/local/cargo/registry, liquidfun-rs-cargo-target:/cargo-target and CARGO_TARGET_DIR=/cargo-target. This local boundary does not replace canonical Linux x64 or native Windows evidence.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Produce the shared package and canonical Phase11 pair</name>
  <files>target/phase15-candidate/platform/ledger.json, target/phase15-candidate/oracle/ledger.json</files>
  <read-first>reference/release/required-evidence.toml, .github/workflows/platform.yml, .github/workflows/fuzz.yml; read existing files before edits and the retained failure described below.</read-first>
  
  <action>Runtime qualification at C dc29747e2d66d84e923088efa0b0d16785cc7df7 found the same particle-forces-and-statistics checkpoint-0001 mismatch in both Phase11 lanes (run 35137245396). Preserve the failed run and original-C platform evidence, diagnose against pinned upstream without widening tolerances, correct the responsible engine or capture boundary with a regression (the diagnosis identified missing particle debug capture and separately confirmed total range force/impulse dispatch errors in the private C++ adapter), and repeat source/canonical qualification before selecting replacement C. The failure also exposed missing upload of existing bounded Phase11 failure logs; add diagnostic-only failure artifacts to the two oracle.yml jobs and protect the workflow contract. The retained differential coverage report also exposed an invalid jq array iteration in scripts/phase12-release-evidence/producer_validation.sh; correct it with behavior tests while preserving source-bound expected/observed leaf completeness. Successful repaired-source run 35140576574 then exposed a legacy exact-reference topology mismatch: its final canonical semantic-result.json must be explicitly allowed and strictly validated, while preliminary content, historical bundles and sanitizer topology retain their authority boundaries. No failed artifact may satisfy the accepted evidence registry. Per D-04/D-05/D-06 use Plan06 ledger/dispatch/collect contract at C. Run platform.yml once to create package, retain exact archive and all seven package/MSRV/native/conditional artifacts. Require Rust 1.92.0 MSRV, Linux x64/ARM64, macOS ARM64 and Windows x64 native results on identical bytes; conditional Intel Mac uses the existing explicit unsupported downgrade when native evidence unavailable. Run oracle.yml evidence_phase=phase11 at exact resolved C ref and require both canonical and ASan/UBSan artifacts. Do not use prior Phase14 D2 as candidate proof. Verify raw package hashes, metadata repository/workflow/job/run/attempt/C/toolchains, inventories and all successful checks before marking each producer complete.</action>
  <verify><automated>cargo test -p xtask --test platform_workflow --test phase11_evidence_cli</automated></verify>
  <acceptance-criteria>Seven platform/package and two Phase11 artifacts are complete, retained and bind C; single archive hash agrees everywhere.</acceptance-criteria>
  <done>Seven platform/package and two Phase11 artifacts are complete, retained and bind C; single archive hash agrees everywhere. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>

<task type="auto">
  <name>Task 2: Produce safety, fuzz, regressions and all coverage lanes</name>
  <files>target/phase15-candidate/safety/ledger.json, target/phase15-candidate/fuzz/ledger.json, target/phase15-candidate/regressions/ledger.json, target/phase15-candidate/coverage/ledger.json</files>
  <read-first>reference/release/required-evidence.toml, .github/workflows/platform.yml, .github/workflows/fuzz.yml; read existing files before edits and the retained failure described below.</read-first>
  
  <action>Dispatch safety.yml, fuzz.yml, regressions.yml and coverage.yml at C; independent workflows may run concurrently with distinct attempt roots. Collect two Rust safety artifacts with Plan02 mode counts, all five fuzz targets (protocol, shapes_collision, world_mutation, particles, groups_ownership), named regression artifact and Rust/C++/differential coverage artifacts. Validate actual bounded executions, minimized corpus hashes, terminal success and zero missed differential leaves. Retain original archives, raw logs/measurements and provider/attempt identities immediately. Any new physics mismatch requires diagnosis and source refreeze; a harness/infrastructure retry preserves separate failed artifacts and must still bind C.</action>
  <verify><automated>cargo test -p xtask --test safety_evidence_contract --test coverage_workflow --test regression_workflow</automated></verify>
  <acceptance-criteria>Eleven additional independent producer artifacts are retained and valid at C; no zero-test, missing-log or mislabeled compiler evidence.</acceptance-criteria>
  <done>Eleven additional independent producer artifacts are retained and valid at C; no zero-test, missing-log or mislabeled compiler evidence. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>
</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Remote run/artifact and source record → acceptance | Candidate, paths, hashes and terminal identities must be checked before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-09-01 | S/T | target/phase15-candidate/platform/ledger.json | mitigate | Bind full candidate, exact target/toolchain and SHA-256 bytes; reject mismatched identity at the task boundary. |
| T-15-09-02 | R | Attempt records | mitigate | Preserve original failure logs and provider/run/attempt identities; no overwrite or inferred success. |
| T-15-09-03 | T/I | Retained payload paths | mitigate | Use normalized repository-relative paths with no traversal or symlink components and bounded payload validation before consumption. |

ASVS L1-relevant input/access/integrity controls apply to these tooling boundaries. There is no web login/session or new application authorization surface in this plan.
</threat-model>

<verification>
Run each task's focused automated command and the actual production/negative controls stated in its action. Commands needing canonical x64, pinned Miri or native Windows run at that exact boundary; a local ARM64 pass is supplemental.
Before every commit run in order: cargo fmt --all; cargo clippy --all-targets --all-features -- -D warnings; cargo build --all-targets --all-features; cargo test --all-features. Run bun scripts/bright-builds-check.ts all, required Markdown checks and git diff --check. Do not format .planning with mdformat.
For tool failures preserve the failed attempt, diagnose the exact boundary, repair and rerun affected proof. Never convert missing controlled-host credentials to acceptance. Source repairs after C require requalification and a fresh C.
</verification>

<success-criteria>
- Seven platform/package and two Phase11 artifacts are complete, retained and bind C; single archive hash agrees everywhere.
- Eleven additional independent producer artifacts are retained and valid at C; no zero-test, missing-log or mislabeled compiler evidence.
- Every effect uses observed repository/ref/candidate identity and standing authority; no release publication or waiver.
</success-criteria>

<output>
While C..A is pending, retain the summary in ignored target storage and defer its tracked commit until A has passed. The generic executor must not insert a SUMMARY/STATE/ROADMAP commit between C and A.
After completion, create .planning/phases/15-re-establish-candidate-evidence/15-09-SUMMARY.md with actual identities, tests, residual risks and decision coverage.
</output>

