---
phase: 15-re-establish-candidate-evidence
plan: "03"
type: execute
wave: 3
depends_on: ["15-02"]
files_modified: ["reference/regressions/manifest.toml","scenarios/regressions/phase14-particle-groups.txt","crates/liquidfun/tests/particle_group_properties.rs","tools/xtask/tests/safety_evidence_contract.rs","tools/xtask/src/safety_evidence/contract.rs","scripts/phase12-regressions.sh",".github/workflows/regressions.yml","tools/xtask/tests/regression_workflow.rs"]
autonomous: true
requirements: ["PLAT-05","DOCS-09"]
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-09-14T16-41-28
generated_at: "2026-09-14T16:55:28.770Z"
must_haves:
  truths:
    - "Every registry row is real, hash-bound, correctly classified and consumed by the named replay; no invented parity authority."
    - "Registry executes nonzero exact tests, all logs survive success/failure, retries cannot overwrite prior evidence."
  artifacts:
    - path: "reference/regressions/manifest.toml"
      provides: "Bind actual minimized inputs to named replay tests"
    - path: "scripts/phase12-regressions.sh"
      provides: "Require executed test counts and immutable regression attempts"
  key_links:
    - from: "reference/regressions/manifest.toml"
      to: "scripts/phase12-regressions.sh"
      via: "Tested producer/consumer contract and explicit identity validation"
---

<objective>
Register genuine minimized regressions and retain executed attempts.
Purpose: Repair and prove the prerequisite without weakening frozen-source acceptance.
Output: Bind actual minimized inputs to named replay tests; Require executed test counts and immutable regression attempts.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-re-establish-candidate-evidence/15-CONTEXT.md
@.planning/phases/15-re-establish-candidate-evidence/15-RESEARCH.md
@.planning/phases/14-repair-windows-particle-group-invariants/14-DIAGNOSIS.md
@.planning/phases/14-repair-windows-particle-group-invariants/14-VERIFICATION.md

<interfaces>
Existing shell release boundary: check; prepare(candidate, output, release_run) is changed by Plan04 to import archive and identity; aggregate consumes exact producer run IDs; publish-identity-last emits success only after validation.
Existing typed commands: cargo xtask release audit --manifest PATH --candidate C --output json; release attestation validate-worktree --source PATH --manifest PATH --report PATH; release attestation validate with the same arguments plus --attestation-commit A.
Identity contract: full lowercase 40-hex source C, explicit provider/run/attempt/job/target/toolchain, exact SHA-256 content digests; 21 producer artifacts join to 19 required registry entries. D1 canonical, D2 portability and D3 diagnostic authority remain separate.
</interfaces>

Standing authority D-08 permits routine fixes, review, gates, commits and ordinary main pushes; not release publication, waivers, secret/account changes or fabricated review acknowledgment. Local gate baseline at d990dc6 passed in Linux ARM64 Docker; use image sha256:5f3d56072c0c734ebb2381c59f0a20db1330b8884134c4e3a9d369de2d810ea8 with repo at /workspace, liquidfun-rs-rustup:/usr/local/rustup, liquidfun-rs-cargo-registry:/usr/local/cargo/registry, liquidfun-rs-cargo-target:/cargo-target and CARGO_TARGET_DIR=/cargo-target. This local boundary does not replace canonical Linux x64 or native Windows evidence.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Bind actual minimized inputs to named replay tests</name>
  <files>reference/regressions/manifest.toml, scenarios/regressions/phase14-particle-groups.txt, crates/liquidfun/tests/particle_group_properties.rs, tools/xtask/tests/safety_evidence_contract.rs, tools/xtask/src/safety_evidence/contract.rs</files>
  <read-first>.planning/phases/14-repair-windows-particle-group-invariants/14-DIAGNOSIS.md, .planning/phases/14-repair-windows-particle-group-invariants/14-VERIFICATION.md, reference/regressions/manifest.toml, scenarios/regressions/phase14-particle-groups.txt, crates/liquidfun/tests/particle_group_properties.rs, tools/xtask/tests/safety_evidence_contract.rs, tools/xtask/src/safety_evidence/contract.rs, tools/xtask/src/safety_evidence/contract/validation.rs; read existing files before edits and the retained failure described below.</read-first>
  <behavior>Every registry row is real, hash-bound, correctly classified and consumed by the named replay; no invented parity authority. Negative inputs and failed commands must produce a nonzero result with no success identity.</behavior>
  <action>Per D-02/D-06 replace the empty registry with genuine reviewed Phase14 findings, including audited seed 4149329052036581951 and current seed 190752942043209832. Store immutable input bytes at scenarios/regressions/phase14-particle-groups.txt under the validator's accepted root: version=1 header and two named records containing decimal u64 seed plus comma-separated u8 controls from the retained diagnosis. Use a strict standard-library parser in the test target; reject malformed/duplicate/missing records and preserve generator bounds. Both existing tests persisted_audited_windows_seed and persisted_current_windows_seed must read these exact registered bytes, select their named record, assert equality against existing seed/control/operation constants, and pass parsed values into the real replay. Register the same file's exact SHA-256 for each finding with the correct fully qualified test selection; changing a byte must fail validation or the literal guard, never silently leave replay unchanged. Resolve actual full original/fix commit IDs from git and evidence. Existing closed classes Harness/PhysicsMismatch/Sanitizer/Timeout/Schema do not describe this production invariant panic. Extend only the private regression FailureClass enum in tools/xtask/src/safety_evidence/contract.rs with InvariantViolation, denoting a native library invariant/assertion failure without an oracle comparison. Set these records to InvariantViolation; omit optional oracle_identity/tolerance_identity rather than inventing them. Keep first_divergence_signature specific to retained allocation→InvalidLaneBundle→impossible-state panic. Add typed tests accepting exactly this new category without oracle fields and rejecting unknown categories, invalid roots, altered bytes, and PhysicsMismatch missing oracle/tolerance. Preserve all other validation and parity authority; Task2 updates the shell class allowlist. Demonstrate pre-fix failure from retained immutable Windows evidence and fresh post-fix successful append/native replays. Write the described behavioral regression first, run the exact automated test command and retain its expected failure; implement, rerun green, then simplify without reducing the assertion. Use Arrange/Act/Assert and bounded subprocesses.</action>
  <verify><automated>cargo test -p xtask --test safety_evidence_contract &amp;&amp; cargo xtask safety-evidence validate-regressions --emit-execution-list &amp;&amp; cargo test -p liquidfun --test particle_group_properties</automated></verify>
  <acceptance-criteria>Every registry row is real, hash-bound, correctly classified and consumed by the named replay; no invented parity authority.</acceptance-criteria>
  <done>Every registry row is real, hash-bound, correctly classified and consumed by the named replay; no invented parity authority. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require executed test counts and immutable regression attempts</name>
  <files>scripts/phase12-regressions.sh, .github/workflows/regressions.yml, tools/xtask/tests/regression_workflow.rs</files>
  <read-first>.planning/phases/14-repair-windows-particle-group-invariants/14-DIAGNOSIS.md, .planning/phases/14-repair-windows-particle-group-invariants/14-VERIFICATION.md, scripts/phase12-regressions.sh, .github/workflows/regressions.yml, tools/xtask/tests/regression_workflow.rs; read existing files before edits and the retained failure described below.</read-first>
  <behavior>Registry executes nonzero exact tests, all logs survive success/failure, retries cannot overwrite prior evidence. Negative inputs and failed commands must produce a nonzero result with no success identity.</behavior>
  <action>Per D-05/D-08 add InvariantViolation to the shell execution-entry validator's explicit allowed classes, matching Task1's typed extension; preserve PhysicsMismatch's mandatory oracle/tolerance check. Add subprocess tests proving typed execution-list→shell validation→exact named replay accepts the new class, rejects unknown classes and still rejects missing PhysicsMismatch authority. Add subprocess negatives for zero matched tests, failed selected test, duplicate destination and invalid manifest hash. Each selected exact test must prove one executed pass, not Cargo success with zero tests. Preserve per-test logs and full command/target/source identities. Allocate fresh run/attempt directories or fail if a destination exists; never erase a failed attempt. Upload bounded diagnostics on failure without a success identity; success identity remains last. Validate Linux production and fresh final-candidate Windows replay in later production. Write the described behavioral regression first, run the exact automated test command and retain its expected failure; implement, rerun green, then simplify without reducing the assertion. Use Arrange/Act/Assert and bounded subprocesses.</action>
  <verify><automated>cargo test -p xtask --test regression_workflow &amp;&amp; bash scripts/phase12-regressions.sh check</automated></verify>
  <acceptance-criteria>Registry executes nonzero exact tests, all logs survive success/failure, retries cannot overwrite prior evidence.</acceptance-criteria>
  <done>Registry executes nonzero exact tests, all logs survive success/failure, retries cannot overwrite prior evidence. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>
</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Workflow input and downloaded bytes → local tooling | Candidate, paths, hashes and terminal identities must be checked before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-03-01 | S/T | reference/regressions/manifest.toml | mitigate | Bind full candidate, exact target/toolchain and SHA-256 bytes; reject mismatched identity at the task boundary. |
| T-15-03-02 | R | Attempt records | mitigate | Preserve original failure logs and provider/run/attempt identities; no overwrite or inferred success. |
| T-15-03-03 | T/I | Retained payload paths | mitigate | Use normalized repository-relative paths with no traversal or symlink components and bounded payload validation before consumption. |

ASVS L1-relevant input/access/integrity controls apply to these tooling boundaries. There is no web login/session or new application authorization surface in this plan.
</threat-model>

<verification>
Run each task's focused automated command and the actual production/negative controls stated in its action. Commands needing canonical x64, pinned Miri or native Windows run at that exact boundary; a local ARM64 pass is supplemental.
Before every commit run in order: cargo fmt --all; cargo clippy --all-targets --all-features -- -D warnings; cargo build --all-targets --all-features; cargo test --all-features. Run bun scripts/bright-builds-check.ts all, required Markdown checks and git diff --check. Do not format .planning with mdformat.
For tool failures preserve the failed attempt, diagnose the exact boundary, repair and rerun affected proof. Never convert missing controlled-host credentials to acceptance. Source repairs after C require requalification and a fresh C.
</verification>

<success-criteria>
- Every registry row is real, hash-bound, correctly classified and consumed by the named replay; no invented parity authority.
- Registry executes nonzero exact tests, all logs survive success/failure, retries cannot overwrite prior evidence.
- Every effect uses observed repository/ref/candidate identity and standing authority; no release publication or waiver.
</success-criteria>

<output>
Create the tracked summary after completion.
After completion, create .planning/phases/15-re-establish-candidate-evidence/15-03-SUMMARY.md with actual identities, tests, residual risks and decision coverage.
</output>
