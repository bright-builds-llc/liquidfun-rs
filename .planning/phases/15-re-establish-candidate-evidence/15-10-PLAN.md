---
phase: 15-re-establish-candidate-evidence
plan: "10"
type: execute
wave: 10
depends_on: ["15-09"]
files_modified: ["target/phase15-candidate/performance/host-preflight.json","target/phase15-candidate/performance/ledger.json","target/phase15-candidate/performance/validation.json"]
autonomous: true
requirements: ["DOCS-09"]
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-09-14T16-41-28
generated_at: "2026-09-14T16:55:28.770Z"
must_haves:
  truths:
    - "Approved runner/access and identity are proven before dependent dispatch, or a precise truthful external blocker is retained."
    - "The final 21st producer artifact is complete controlled evidence at C, with every digest and measurement policy check passing."
  artifacts:
    - path: "target/phase15-candidate/performance/host-preflight.json"
      provides: "Resolve and verify the existing approved controlled runner"
    - path: "target/phase15-candidate/performance/ledger.json"
      provides: "Run and retain the full controlled matrix at C"
  key_links:
    - from: ".github/workflows/performance.yml"
      to: "scripts/phase12-performance.sh"
      via: "Existing controlled producer invocation; current-wave actual host authorization, identity equality, calibration and 32-case execution remain required."
      pattern: "scripts/phase12-performance[.]sh paired"
    - from: ".github/workflows/performance.yml"
      to: "scripts/phase12-performance.sh"
      via: "Existing independent performance validation invocation; scheduled diagnostic output cannot satisfy controlled acceptance."
      pattern: "scripts/phase12-performance[.]sh validate"
---

<objective>
Complete approved controlled-host performance evidence.
Purpose: Complete actual candidate acceptance with retained, independently verifiable evidence.
Output: Resolve and verify the existing approved controlled runner; Run and retain the full controlled matrix at C.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-re-establish-candidate-evidence/15-CONTEXT.md
@.planning/phases/15-re-establish-candidate-evidence/15-RESEARCH.md
@BENCHMARKING.md
@.github/workflows/performance.yml
@reference/performance/manifest.toml
@reference/performance/policy.json

<interfaces>
Existing shell release boundary: check; prepare(candidate, output, release_run) is changed by Plan04 to import archive and identity; aggregate consumes exact producer run IDs; publish-identity-last emits success only after validation.
Existing typed commands: cargo xtask release audit --manifest PATH --candidate C --output json; release attestation validate-worktree --source PATH --manifest PATH --report PATH; release attestation validate with the same arguments plus --attestation-commit A.
Identity contract: full lowercase 40-hex source C, explicit provider/run/attempt/job/target/toolchain, exact SHA-256 content digests; 21 producer artifacts join to 19 required registry entries. D1 canonical, D2 portability and D3 diagnostic authority remain separate.
</interfaces>

Standing authority D-08 permits routine fixes, review, gates, commits and ordinary main pushes; not release publication, waivers, secret/account changes or fabricated review acknowledgment. Local gate baseline at d990dc6 passed in Linux ARM64 Docker; use image sha256:5f3d56072c0c734ebb2381c59f0a20db1330b8884134c4e3a9d369de2d810ea8 with repo at /workspace, liquidfun-rs-rustup:/usr/local/rustup, liquidfun-rs-cargo-registry:/usr/local/cargo/registry, liquidfun-rs-cargo-target:/cargo-target and CARGO_TARGET_DIR=/cargo-target. This local boundary does not replace canonical Linux x64 or native Windows evidence.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Resolve and verify the existing approved controlled runner</name>
  <files>target/phase15-candidate/performance/host-preflight.json</files>
  <read-first>BENCHMARKING.md, .github/workflows/performance.yml, reference/performance/manifest.toml, reference/performance/policy.json; read existing files before edits and the retained failure described below.</read-first>
  
  <action>Per D-07/D-08 recheck the pending infrastructure answer and repository/organization access. Require existing approved runner label performance-controlled-linux-x64 and matching PERFORMANCE_CONTROLLED_HOST_IDENTITY without exposing secret values; record authorized host identity source, target/toolchain availability and calibration eligibility. Repository runner list was empty and org APIs 403 at discussion; do not assume access appeared. If credentials/approved host remain unavailable, report this precise external blocker, keep Phase15 incomplete and retain completed Plan09 evidence. Do not create runners, change security or fabricate identity under ordinary push authority.</action>
  <verify><automated>cargo test -p xtask --test performance_workflow</automated></verify>
  <acceptance-criteria>Approved runner/access and identity are proven before dependent dispatch, or a precise truthful external blocker is retained.</acceptance-criteria>
  <done>Approved runner/access and identity are proven before dependent dispatch, or a precise truthful external blocker is retained. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>

<task type="auto">
  <name>Task 2: Run and retain the full controlled matrix at C</name>
  <files>target/phase15-candidate/performance/ledger.json, target/phase15-candidate/performance/validation.json</files>
  <read-first>BENCHMARKING.md, .github/workflows/performance.yml, reference/performance/manifest.toml, reference/performance/policy.json; read existing files before edits and the retained failure described below.</read-first>
  
  <action>With verified access run performance.yml on C in controlled manual mode. Require all 32 cases/14 workloads, calibrated interleaved Rust/C++ raw samples and the existing policy/toolchain/protocol/input identities. Keep default scalar behavior and reviewed optimization policy. Scheduled shared-host D3 cannot fill this slot. Acquire performance artifact, payload-files.sha256 and all raw payloads; independently verify bytes, run/attempt/host/C identities and policy completeness using existing validators. Empty reviewed performance-claim list may be valid only with no generalized claim; it does not excuse missing raw measurements.</action>
  <verify><automated>bash scripts/phase15-candidate-evidence.sh validate-retained --candidate "$candidate_sha" --attempt-root "$attempt_root"</automated></verify>
  <acceptance-criteria>The final 21st producer artifact is complete controlled evidence at C, with every digest and measurement policy check passing.</acceptance-criteria>
  <done>The final 21st producer artifact is complete controlled evidence at C, with every digest and measurement policy check passing. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>
</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Remote run/artifact and source record → acceptance | Candidate, paths, hashes and terminal identities must be checked before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-10-01 | S/T | target/phase15-candidate/performance/host-preflight.json | mitigate | Bind full candidate, exact target/toolchain and SHA-256 bytes; reject mismatched identity at the task boundary. |
| T-15-10-02 | R | Attempt records | mitigate | Preserve original failure logs and provider/run/attempt identities; no overwrite or inferred success. |
| T-15-10-03 | T/I | Retained payload paths | mitigate | Use normalized repository-relative paths with no traversal or symlink components and bounded payload validation before consumption. |

ASVS L1-relevant input/access/integrity controls apply to these tooling boundaries. There is no web login/session or new application authorization surface in this plan.
</threat-model>

<verification>
Run each task's focused automated command and the actual production/negative controls stated in its action. Commands needing canonical x64, pinned Miri or native Windows run at that exact boundary; a local ARM64 pass is supplemental.
Before every commit run in order: cargo fmt --all; cargo clippy --all-targets --all-features -- -D warnings; cargo build --all-targets --all-features; cargo test --all-features. Run bun scripts/bright-builds-check.ts all, required Markdown checks and git diff --check. Do not format .planning with mdformat.
For tool failures preserve the failed attempt, diagnose the exact boundary, repair and rerun affected proof. Never convert missing controlled-host credentials to acceptance. Source repairs after C require requalification and a fresh C.
</verification>

<success-criteria>
- Approved runner/access and identity are proven before dependent dispatch, or a precise truthful external blocker is retained.
- The final 21st producer artifact is complete controlled evidence at C, with every digest and measurement policy check passing.
- Every effect uses observed repository/ref/candidate identity and standing authority; no release publication or waiver.
</success-criteria>

<output>
While C..A is pending, retain the summary in ignored target storage and defer its tracked commit until A has passed. The generic executor must not insert a SUMMARY/STATE/ROADMAP commit between C and A.
After completion, create .planning/phases/15-re-establish-candidate-evidence/15-10-SUMMARY.md with actual identities, tests, residual risks and decision coverage.
</output>

