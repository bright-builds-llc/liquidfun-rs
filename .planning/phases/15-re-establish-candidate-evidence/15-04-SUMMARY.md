---
phase: 15-re-establish-candidate-evidence
plan: "04"
subsystem: release
tags: [cargo, package, provenance, ci]
requires:
  - phase: 15-03
    provides: Proven prerequisite regression producer
provides:
  - Exact platform archive import with preserved original identity
  - Independent real Cargo publish dry-run byte comparison
  - Selected platform run validation before release preparation
affects: [15-07, 15-08, 15-11, candidate-requalification]
tech-stack:
  added: []
  patterns: [bounded package import, identity preservation, isolated Cargo verification]
key-files:
  created:
    - scripts/phase12-release-evidence/package_import.sh
    - tools/xtask/tests/release_cli/package_handoff.rs
    - tools/xtask/tests/release_cli/package_handoff.sh
    - tools/xtask/tests/release_cli/real_package_handoff.sh
  modified:
    - scripts/phase12-release-evidence.sh
    - scripts/phase12-release-evidence/common.sh
    - .github/workflows/release.yml
    - tools/xtask/tests/platform_workflow.rs
    - tools/xtask/tests/release_cli.rs
    - tools/xtask/tests/release_cli/construction_cases.rs
key-decisions:
  - Preserve package identity bytes; authenticate provider identity at selected-run acquisition.
  - Compare Cargo 1.97 dry-run output under package/tmp-crate with the imported archive.
  - Keep release registry package verification owned by release.yml without inventing platform provenance fields.
requirements-completed: []
requirements-supported: [PLAT-01, PLAT-05, DOCS-09]
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-09-14T16-41-28
duration: approximately 25min
completed: 2026-09-14
---

# Phase 15 Plan 04: Exact Package Handoff Summary

**Release preparation imports the platform-created archive unchanged, verifies it through the existing package API, and independently compares its bytes with a real Cargo publication dry run.**

## Tasks and Commits

1. `e09e436` — Import and verify the exact platform package, with bounded negative tests and a real Cargo-only handoff regression.
1. `748a0da` — Download the selected platform archive before preparation, validate the provider run, and exercise native/MSRV metadata joins over the same real bytes.
1. `1137b96` — Strengthen the real-engine proof to a clean detached checkout at an existing source commit, and reject dangling destination symlinks.

The prepare interface now accepts candidate, output directory, release run ID, platform run ID, archive path and original identity path. It rejects missing/substituted inputs, mismatched candidates or hashes, wrong package/run names, traversal and symlink components, excessive input sizes, and reused package destinations. The original identity is copied byte-for-byte. The existing `verify-artifact` command retains authority over full package metadata, inventory, toolchain, source identity and isolated build/test behavior.

Release preparation has no `create-artifact` call. Platform remains the authoritative creator. The retained release registry still identifies the release verifier as the package evidence producer; it does not forge platform fields in the original identity. The publish command remains `cargo publish -p liquidfun --dry-run`, with a fresh target directory. A failed command, absent output, changed identity, changed imported archive or different generated bytes fails before inexpensive success identity publication. All other inexpensive checks remain in place.

The workflow checks GitHub API run ID, repository, source SHA, workflow path, terminal success and allowed event before downloading by the exact platform run/candidate. Preparation follows that download; other producer downloads and aggregation retain their ordering. Conditional Intel Mac policy is unchanged.

## Actual Behavioral Proof

The real integration test creates a clean shared clone detached at the repository's committed HEAD without a native submodule, creates one archive containing the actual complete engine with the actual `package create-artifact` API, imports it, runs actual `package verify-artifact`, and executes actual Cargo publish dry-run with upload aborted by Cargo. It checks checkout cleanliness before production. A shell guard rejects another explicit archive creation. Its preserved archive and identity are compared byte-for-byte afterward. No minimal replacement engine or `--allow-dirty` publication flag is used.

Final retained local proof: `target/xtask-release-tests/real-package-handoff-38176-11/`.

- Clean detached source: `748a0dafd9c438847474670da8b2f33c3c379d5c`.
- Package SHA-256: `6297c72556d202b091814a7c4a080939ea228dc18e93bed28bb65c94f9a02d9b`.
- Archive: 416014 bytes, `liquidfun` 0.0.0, creation toolchain 1.97.0.
- Actual verifier: `aarch64-unknown-linux-gnu`, Rust 1.97.0, D2-supported classification.
- Imported archive: `repository/target/release/package/liquidfun.crate`.
- Independent dry-run archive: `repository/target/release/publish-dry-run/package/tmp-crate/liquidfun-0.0.0.crate`.
- Command output: `stdout.log` and `stderr.log` in that proof directory.

Synthetic provider envelopes explicitly labeled test-only exercise canonical MSRV plus Linux x64, Linux ARM64, macOS ARM64 and Windows metadata joins over those real archive bytes. Every join rejects another run or archive digest. Separate controls reject a successful unrelated workflow/repository/candidate/run, nonterminal or failed provider, changed package bytes, missing identity, symlinks, traversal and failed/missing/changed publication output. These synthetic records do **not** claim native execution or release acceptance.

## Verification and Retained Failures

- Task 1 RED: missing import function, retained in `target/phase15-plan04/task1-attempt01/red.log`.
- The first real fixture correctly failed publication because its root README was untracked; the fixture now commits that package input. The next real attempt exposed Cargo 1.97's `package/tmp-crate` output location; the helper was corrected against observed files. Both failed records remain in task1 attempt directories.
- Task 2 RED: download occurred after prepare, retained in `target/phase15-plan04/task2-attempt01/red.log`.
- Final focused suites: package CLI **22 passed**, release CLI **26 passed**, platform workflow **7 passed**. Logs: `target/phase15-plan04/detached-proof/final-gates.log`.
- Ordered `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` passed before each implementation commit; explicit xtask Clippy also passed. Bright Builds, shfmt, ShellCheck with source paths, actionlint and diff checks passed. `just markdown-check` passed after implementation; parser-owned `.planning` files were not formatted.
- Local verification used the reviewed Linux ARM64 image `sha256:5f3d56072c0c734ebb2381c59f0a20db1330b8884134c4e3a9d369de2d810ea8` and the existing disposable container/volumes. The ordinary unpacked package build/tests and dry-run verification were real Cargo-only commands. No C++ dependency or integration-test packaging change was introduced.

## Scope, Decisions and Limits

Plan scope was expanded to include the CLI arity change and cohesive shell/test helper modules. RED tests were retained without committing failing tests, honoring repository precommit rules. The simplification pass kept package policy in the existing typed API and shell orchestration in a small helper rather than duplicating its metadata parser. No blocking stubs remain.

AGENTS.md, the Bright Builds sidecar/overrides, active lessons and architecture/testing/verification/Rust standards governed execution. Standing authorization dated 2026-09-13 covers these routine fixes and commits. The orchestrator owns STATE.md, ROADMAP.md and REQUIREMENTS.md, which this executor did not stage or modify.

This is prerequisite package-handoff proof, not frozen-candidate acceptance. No remote workflow was dispatched, no package was published and no global requirement was completed. The full release workflow and all same-C native producers remain Plans 15-08 through 15-11 work. The preserved source commit above is a prefreeze implementation revision, not the final source candidate.

## Self-Check: PASSED

All three implementation commits resolve, all four new helper/test files exist, retained real proof and focused failure/success logs exist, and imported/dry-run package hashes were independently checked before recording completion.
