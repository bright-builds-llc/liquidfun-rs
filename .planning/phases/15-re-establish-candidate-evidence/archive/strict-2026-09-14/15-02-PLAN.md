---
phase: 15-re-establish-candidate-evidence
plan: "02"
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - scripts/phase12-miri.sh
  - .github/workflows/safety.yml
  - tools/xtask/tests/safety_evidence_contract.rs
  - tools/xtask/tests/safety_evidence_contract/miri.rs
  - tools/xtask/tests/safety_evidence_contract/miri-control.sh
  - SAFETY.md
  - scripts/phase12-coverage.sh
  - .github/workflows/coverage.yml
  - tools/xtask/tests/coverage_workflow.rs
  - tools/xtask/tests/coverage_workflow/rust_identity.rs
  - tools/xtask/tests/coverage_workflow/rust-identity.sh
  - crates/liquidfun-differential/tests/rust_adapter.rs
  - reference/coverage/contract.json
  - tools/xtask/src/safety_evidence/contract/validation.rs
  - crates/liquidfun-test-protocol/tests/fixtures.rs
  - crates/liquidfun-test-protocol/tests/fixtures/bytes.rs
  - crates/liquidfun/src/particle/storage/properties/group_model.rs
  - crates/liquidfun/src/particle/storage/properties/group_model/configuration.rs
  - tools/xtask/tests/differential_cli.rs
  - tools/xtask/tests/differential_cli/rigid_commands.rs
  - tools/xtask/tests/phase13_1_gap_verification/canonical.rs
  - tools/xtask/tests/phase13_1_gap_verification.rs
  - tools/xtask/tests/phase13_1_gap_verification/producer/lifecycle.rs
  - scripts/phase13-1-gap-verification-manifest.json
  - scripts/phase13-1-validate-gap-evidence.sh
autonomous: true
requirements: ["PLAT-01","DOCS-09"]
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-09-14T16-41-28
generated_at: "2026-09-14T16:55:28.770Z"
must_haves:
  truths:
    - "Default mode retains all other math tests, endpoint passes separately with recorded mode, and scanner errors cannot become clean evidence."
    - "Actual Rust coverage runs on the declared toolchain and identity tests pass under both stable/native and pinned-nightly boundaries without weakening D1."
  artifacts:
    - path: "scripts/phase12-miri.sh"
      provides: "Separate the exact Miri endpoint mode and fail closed on scan errors"
    - path: "scripts/phase12-coverage.sh"
      provides: "Align coverage execution with its declared stable identity"
  key_links:
    - from: ".github/workflows/safety.yml"
      to: "scripts/phase12-miri.sh"
      via: "Existing workflow invocation; current-wave postcondition is recorded default and endpoint Miri modes."
      pattern: "scripts/phase12-miri[.]sh run"
    - from: ".github/workflows/coverage.yml"
      to: "scripts/phase12-coverage.sh"
      via: "Existing Rust coverage invocation; current-wave postcondition aligns actual compiler with declared identity."
      pattern: "scripts/phase12-coverage[.]sh rust"
---

<objective>
Restore truthful safety and coverage execution.
Purpose: Repair and prove the prerequisite without weakening frozen-source acceptance.
Output: Separate the exact Miri endpoint mode and fail closed on scan errors; Align coverage execution with its declared stable identity.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-re-establish-candidate-evidence/15-CONTEXT.md
@.planning/phases/15-re-establish-candidate-evidence/15-RESEARCH.md
@target/phase15-miri-diagnosis/diagnosis.md
@SAFETY.md
@reference/coverage/contract.json

<interfaces>
Existing shell release boundary: check; prepare(candidate, output, release_run) is changed by Plan04 to import archive and identity; aggregate consumes exact producer run IDs; publish-identity-last emits success only after validation.
Existing typed commands: cargo xtask release audit --manifest PATH --candidate C --output json; release attestation validate-worktree --source PATH --manifest PATH --report PATH; release attestation validate with the same arguments plus --attestation-commit A.
Identity contract: full lowercase 40-hex source C, explicit provider/run/attempt/job/target/toolchain, exact SHA-256 content digests; 21 producer artifacts join to 19 required registry entries. D1 canonical, D2 portability and D3 diagnostic authority remain separate.
</interfaces>

Standing authority D-08 permits routine fixes, review, gates, commits and ordinary main pushes; not release publication, waivers, secret/account changes or fabricated review acknowledgment. Local gate baseline at d990dc6 passed in Linux ARM64 Docker; use image sha256:5f3d56072c0c734ebb2381c59f0a20db1330b8884134c4e3a9d369de2d810ea8 with repo at /workspace, liquidfun-rs-rustup:/usr/local/rustup, liquidfun-rs-cargo-registry:/usr/local/cargo/registry, liquidfun-rs-cargo-target:/cargo-target and CARGO_TARGET_DIR=/cargo-target. This local boundary does not replace canonical Linux x64 or native Windows evidence.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Separate the exact Miri endpoint mode and fail closed on scan errors</name>
  <files>scripts/phase12-miri.sh, .github/workflows/safety.yml, tools/xtask/tests/safety_evidence_contract.rs, SAFETY.md</files>
  <read-first>target/phase15-miri-diagnosis/diagnosis.md, SAFETY.md, reference/coverage/contract.json, scripts/phase12-miri.sh, .github/workflows/safety.yml, tools/xtask/tests/safety_evidence_contract.rs, SAFETY.md; read existing files before edits and the retained failure described below.</read-first>
  <behavior>Default mode retains all other math tests, endpoint passes separately with recorded mode, and scanner errors cannot become clean evidence. Negative inputs and failed commands must produce a nonzero result with no success identity.</behavior>
  <action>Per D-02/D-04/D-06 use the diagnosed interpreter rounding policy, leaving production math and native tests unchanged. Under nightly-2026-07-15, run default math:: excluding only math::sweep::tests::transform_endpoints_preserve_exact_expected_bits (44 passing), then that fully qualified test with --exact and only -Zmiri-no-extra-rounding-error (1 passing). Preserve the remainder of the existing Miri suite and every UB check. Record both commands, MIRIFLAGS, target x86_64-unknown-linux-gnu, compiler identity, executed/ignored counts and logs; reject zero execution or missing endpoint mode. Do not use deterministic-floats or global ignore. Probe/install rg before scanning and distinguish rg status 0 (forbidden finding), 1 (clean), and other statuses (hard failure). Add subprocess negatives for absent scanner and lost endpoint invocation before repair; reproduce both Linux Miri invocations after repair. Write the described behavioral regression first, run the exact automated test command and retain its expected failure; implement, rerun green, then simplify without reducing the assertion. Use Arrange/Act/Assert and bounded subprocesses.</action>
  <verify><automated>cargo test -p xtask --test safety_evidence_contract</automated></verify>
  <acceptance-criteria>Default mode retains all other math tests, endpoint passes separately with recorded mode, and scanner errors cannot become clean evidence.</acceptance-criteria>
  <done>Default mode retains all other math tests, endpoint passes separately with recorded mode, and scanner errors cannot become clean evidence. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align coverage execution with its declared stable identity</name>
  <files>scripts/phase12-coverage.sh, .github/workflows/coverage.yml, tools/xtask/tests/coverage_workflow.rs, crates/liquidfun-differential/tests/rust_adapter.rs</files>
  <read-first>target/phase15-miri-diagnosis/diagnosis.md, SAFETY.md, reference/coverage/contract.json, scripts/phase12-coverage.sh, .github/workflows/coverage.yml, tools/xtask/tests/coverage_workflow.rs, crates/liquidfun-differential/tests/rust_adapter.rs; read existing files before edits and the retained failure described below.</read-first>
  <behavior>Actual Rust coverage runs on the declared toolchain and identity tests pass under both stable/native and pinned-nightly boundaries without weakening D1. Negative inputs and failed commands must produce a nonzero result with no success identity.</behavior>
  <action>Reproduce adapter_binds_native_identity_and_request_hashes at the retained nightly coverage boundary, then use Rust 1.97.0 plus matching llvm-tools-preview for Rust coverage as required by the release registry. Verify cargo-llvm-cov 0.8.7 supports the exact stable operation before production; do not label nightly as stable. Where adapter tests intend a fixed tier, construct explicit platform/compiler identities and assert the tier before behavior; preserve separate actual-host classification tests. Retain Rust coverage, C++ coverage and differential leaf evidence separately, with zero missed required leaves. Add regression tests for envelope/toolchain mismatch before fixing. Write the described behavioral regression first, run the exact automated test command and retain its expected failure; implement, rerun green, then simplify without reducing the assertion. Use Arrange/Act/Assert and bounded subprocesses.</action>
  <verify><automated>cargo test -p xtask --test coverage_workflow &amp;&amp; cargo test -p liquidfun-differential --test rust_adapter</automated></verify>
  <acceptance-criteria>Actual Rust coverage runs on the declared toolchain and identity tests pass under both stable/native and pinned-nightly boundaries without weakening D1.</acceptance-criteria>
  <done>Actual Rust coverage runs on the declared toolchain and identity tests pass under both stable/native and pinned-nightly boundaries without weakening D1. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>
</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Workflow input and downloaded bytes → local tooling | Candidate, paths, hashes and terminal identities must be checked before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-02-01 | S/T | scripts/phase12-miri.sh | mitigate | Bind full candidate, exact target/toolchain and SHA-256 bytes; reject mismatched identity at the task boundary. |
| T-15-02-02 | R | Attempt records | mitigate | Preserve original failure logs and provider/run/attempt identities; no overwrite or inferred success. |
| T-15-02-03 | T/I | Retained payload paths | mitigate | Use normalized repository-relative paths with no traversal or symlink components and bounded payload validation before consumption. |

ASVS L1-relevant input/access/integrity controls apply to these tooling boundaries. There is no web login/session or new application authorization surface in this plan.
</threat-model>

<verification>
Execution scope refinements (authorized corrective work; not completion claims): actual Miri exposed codec fixture file reads and proptest file persistence under isolation. Use exact embedded fixture bytes with native filesystem equality, in-memory Miri persistence with unchanged fixed seeds and 128 cases, and a recorded 3600-second group-only bound after a retained 900-second timeout. Other Miri cases retain 900 seconds and the workflow retains 120 minutes. Actual coverage exposed missing protocol-test and fuzz-build prerequisites, inherited coverage wrappers in controlled fake-tool tests, and real CLI sibling lookup that ignored Cargo's selected target directory. Preserve real CLI D1 acceptance, D2/stale rejection, and side-effect assertions in an isolated instrumented target without default-target binaries. Full instrumented xtask execution also exposed the gap validator's historical 14-command/two-preset assumption; update current validation and fixtures to exactly 21 commands, four presets and four compile digests from Plan 15-01, with new omission/tampering rejection, without rewriting historical retained records. Register Plan 15-01's new canonical_toolchain_workflow in the current gap manifest (28 selected targets), preserving existing command IDs and log paths; identify auxiliary commands by their stable IDs rather than shifted array offsets. Coverage itself remains Rust 1.97.0; its nested real fuzz-build smoke uses the existing pinned nightly and cargo-fuzz 0.13.2. Additional files above implement only these observed prerequisites and their negative controls. Final completion still requires actual successful safety and coverage producers, independently checked by the parent.

Run each task's focused automated command and the actual production/negative controls stated in its action. Commands needing canonical x64, pinned Miri or native Windows run at that exact boundary; a local ARM64 pass is supplemental.
Before every commit run in order: cargo fmt --all; cargo clippy --all-targets --all-features -- -D warnings; cargo build --all-targets --all-features; cargo test --all-features. Run bun scripts/bright-builds-check.ts all, required Markdown checks and git diff --check. Do not format .planning with mdformat.
For tool failures preserve the failed attempt, diagnose the exact boundary, repair and rerun affected proof. Never convert missing controlled-host credentials to acceptance. Source repairs after C require requalification and a fresh C.
</verification>

<success-criteria>
- Default mode retains all other math tests, endpoint passes separately with recorded mode, and scanner errors cannot become clean evidence.
- Actual Rust coverage runs on the declared toolchain and identity tests pass under both stable/native and pinned-nightly boundaries without weakening D1.
- Every effect uses observed repository/ref/candidate identity and standing authority; no release publication or waiver.
</success-criteria>

<output>
Create the tracked summary after completion.
After completion, create .planning/phases/15-re-establish-candidate-evidence/15-02-SUMMARY.md with actual identities, tests, residual risks and decision coverage.
</output>
