---
phase: 15-re-establish-candidate-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["scripts/install-canonical-clang.sh","tools/xtask/tests/canonical_toolchain_workflow.rs",".github/workflows/oracle.yml",".github/workflows/coverage.yml",".github/workflows/phase13-evidence-producer.yml",".github/workflows/phase13-acceptance.yml",".github/workflows/phase13-1-canonical-native.yml","tools/xtask/tests/phase13_1_canonical_native_workflow.rs","tools/xtask/tests/canonical_toolchain_workflow/execution.rs","tools/xtask/tests/canonical_toolchain_workflow/fake-tool.sh","tools/xtask/tests/phase13_1_canonical_native_workflow/execution.rs","tools/xtask/tests/phase13_1_canonical_native_workflow/fake-tool.sh","tools/xtask/src/upstream.rs","tools/xtask/tests/upstream_cli.rs","tools/reference/CMakeLists.txt","tools/reference/CMakePresets.json","tools/reference/upstream_tests.cmake","tools/reference/adapter-inputs.txt"]
autonomous: true
requirements: ["PLAT-01","DOCS-09"]
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-09-14T16-41-28
generated_at: "2026-09-14T16:55:28.770Z"
must_haves:
  truths:
    - "Correct immutable input installs the exact compiler; substituted or incomplete inputs fail closed."
    - "All five workflows share verified provisioning; all listed canonical presets actually configure and build."
  artifacts:
    - path: "scripts/install-canonical-clang.sh"
      provides: "Add an integrity-checked shared compiler installer"
    - path: ".github/workflows/oracle.yml"
      provides: "Wire all consumers and smoke canonical configurations"
  key_links:
    - from: ".github/workflows/oracle.yml"
      to: "scripts/install-canonical-clang.sh"
      via: "Shared immutable compiler acquisition"
      pattern: 'bash scripts/install-canonical-clang[.]sh'
    - from: ".github/workflows/coverage.yml"
      to: "scripts/install-canonical-clang.sh"
      via: "Shared immutable compiler acquisition"
      pattern: 'bash scripts/install-canonical-clang[.]sh'
    - from: ".github/workflows/phase13-evidence-producer.yml"
      to: "scripts/install-canonical-clang.sh"
      via: "Shared immutable compiler acquisition"
      pattern: 'bash scripts/install-canonical-clang[.]sh'
    - from: ".github/workflows/phase13-acceptance.yml"
      to: "scripts/install-canonical-clang.sh"
      via: "Shared immutable compiler acquisition"
      pattern: 'bash scripts/install-canonical-clang[.]sh'
    - from: ".github/workflows/phase13-1-canonical-native.yml"
      to: "scripts/install-canonical-clang.sh"
      via: "Shared immutable compiler acquisition"
      pattern: 'bash scripts/install-canonical-clang[.]sh'
---

<objective>
Pin canonical compiler acquisition and prove every preset.
Purpose: Repair and prove the prerequisite without weakening frozen-source acceptance.
Output: Add an integrity-checked shared compiler installer; Wire all consumers and smoke canonical configurations.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-re-establish-candidate-evidence/15-CONTEXT.md
@.planning/phases/15-re-establish-candidate-evidence/15-RESEARCH.md
@UPSTREAM.md
@tools/reference/CMakePresets.json

<interfaces>
Existing shell release boundary: check; prepare(candidate, output, release_run) is changed by Plan04 to import archive and identity; aggregate consumes exact producer run IDs; publish-identity-last emits success only after validation.
Existing typed commands: cargo xtask release audit --manifest PATH --candidate C --output json; release attestation validate-worktree --source PATH --manifest PATH --report PATH; release attestation validate with the same arguments plus --attestation-commit A.
Identity contract: full lowercase 40-hex source C, explicit provider/run/attempt/job/target/toolchain, exact SHA-256 content digests; 21 producer artifacts join to 19 required registry entries. D1 canonical, D2 portability and D3 diagnostic authority remain separate.
</interfaces>

Standing authority D-08 permits routine fixes, review, gates, commits and ordinary main pushes; not release publication, waivers, secret/account changes or fabricated review acknowledgment. Local gate baseline at d990dc6 passed in Linux ARM64 Docker; use image sha256:5f3d56072c0c734ebb2381c59f0a20db1330b8884134c4e3a9d369de2d810ea8 with repo at /workspace, liquidfun-rs-rustup:/usr/local/rustup, liquidfun-rs-cargo-registry:/usr/local/cargo/registry, liquidfun-rs-cargo-target:/cargo-target and CARGO_TARGET_DIR=/cargo-target. This local boundary does not replace canonical Linux x64 or native Windows evidence.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add an integrity-checked shared compiler installer</name>
  <files>scripts/install-canonical-clang.sh, tools/xtask/tests/canonical_toolchain_workflow.rs</files>
  <read-first>UPSTREAM.md, tools/reference/CMakePresets.json, scripts/install-canonical-clang.sh, tools/xtask/tests/canonical_toolchain_workflow.rs; read existing files before edits and the retained failure described below.</read-first>
  <behavior>Correct immutable input installs the exact compiler; substituted or incomplete inputs fail closed. Negative inputs and failed commands must produce a nonzero result with no success identity.</behavior>
  <action>Per D-01/D-02, reproduce retained llvm.sh checksum failure from target/phase15-preflight/ci before replacing acquisition. Add a shared checked-in installer using reviewed immutable upstream input plus verified SHA-256; record provenance and assert clang/clang++ 22.1.8 and matching LLVM tools, target and compile/link compiler. Add executable subprocess tests with fake downloader/compiler: wrong checksum, version, missing tool and download failure must fail before execution. Never run mutable unchecked installer bytes or merely update the checksum for a moving URL. Write the described behavioral regression first, run the exact automated test command and retain its expected failure; implement, rerun green, then simplify without reducing the assertion. Use Arrange/Act/Assert and bounded subprocesses.</action>
  <verify><automated>cargo test -p xtask --test canonical_toolchain_workflow</automated></verify>
  <acceptance-criteria>Correct immutable input installs the exact compiler; substituted or incomplete inputs fail closed.</acceptance-criteria>
  <done>Correct immutable input installs the exact compiler; substituted or incomplete inputs fail closed. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire all consumers and smoke canonical configurations</name>
  <files>.github/workflows/oracle.yml, .github/workflows/coverage.yml, .github/workflows/phase13-evidence-producer.yml, .github/workflows/phase13-acceptance.yml, .github/workflows/phase13-1-canonical-native.yml, tools/xtask/tests/phase13_1_canonical_native_workflow.rs</files>
  <read-first>UPSTREAM.md, tools/reference/CMakePresets.json, .github/workflows/oracle.yml, .github/workflows/coverage.yml, .github/workflows/phase13-evidence-producer.yml, .github/workflows/phase13-acceptance.yml, .github/workflows/phase13-1-canonical-native.yml, tools/xtask/tests/phase13_1_canonical_native_workflow.rs; read existing files before edits and the retained failure described below.</read-first>
  <behavior>All five workflows share verified provisioning; all listed canonical presets actually configure and build. Negative inputs and failed commands must produce a nonzero result with no success identity.</behavior>
  <action>Use the shared installer in all five existing consumers. Keep action pins, compiler policy and upstream commit unchanged. In canonical Linux run upstream verify, configure/build oracle-debug, oracle-release, oracle-asan-ubsan and upstream-tests using the actual 22.1.8 compiler and strict warnings; retain command/version/configure/build logs in a new target/phase15-preflight attempt. Tests must demonstrate all five workflows call the shared helper and enforce identities. In phase13_1_canonical_native_workflow.rs replace the old literal llvm.sh checksum/URL assertion near line 64 with assertions of the shared installer invocation and its immutable input/SHA-256 verification contract; preserve compiler/CMake/Ninja assertions. This includes phase13-1-canonical-native.yml even if its installer URL has already been pinned: duplicated provisioning must share the reviewed helper. Failures require diagnosis before retry, per D-05/D-08. Write the described behavioral regression first, run the exact automated test command and retain its expected failure; implement, rerun green, then simplify without reducing the assertion. Use Arrange/Act/Assert and bounded subprocesses.</action>
  <verify><automated>cargo test -p xtask --test canonical_toolchain_workflow --test phase13_1_canonical_native_workflow</automated></verify>
  <acceptance-criteria>All five workflows share verified provisioning; all listed canonical presets actually configure and build.</acceptance-criteria>
  <done>All five workflows share verified provisioning; all listed canonical presets actually configure and build. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>
</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Workflow input and downloaded bytes → local tooling | Candidate, paths, hashes and terminal identities must be checked before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-01-01 | S/T | scripts/install-canonical-clang.sh | mitigate | Bind full candidate, exact target/toolchain and SHA-256 bytes; reject mismatched identity at the task boundary. |
| T-15-01-02 | R | Attempt records | mitigate | Preserve original failure logs and provider/run/attempt identities; no overwrite or inferred success. |
| T-15-01-03 | T/I | Retained payload paths | mitigate | Use normalized repository-relative paths with no traversal or symlink components and bounded payload validation before consumption. |

ASVS L1-relevant input/access/integrity controls apply to these tooling boundaries. There is no web login/session or new application authorization surface in this plan.
</threat-model>

<verification>
Run each task's focused automated command and the actual production/negative controls stated in its action. Commands needing canonical x64, pinned Miri or native Windows run at that exact boundary; a local ARM64 pass is supplemental.
Before every commit run in order: cargo fmt --all; cargo clippy --all-targets --all-features -- -D warnings; cargo build --all-targets --all-features; cargo test --all-features. Run bun scripts/bright-builds-check.ts all, required Markdown checks and git diff --check. Do not format .planning with mdformat.
For tool failures preserve the failed attempt, diagnose the exact boundary, repair and rerun affected proof. Never convert missing controlled-host credentials to acceptance. Source repairs after C require requalification and a fresh C.
</verification>

<success-criteria>
- Correct immutable input installs the exact compiler; substituted or incomplete inputs fail closed.
- All five workflows share verified provisioning; all listed canonical presets actually configure and build.
- Every effect uses observed repository/ref/candidate identity and standing authority; no release publication or waiver.
</success-criteria>

<output>
Create the tracked summary after completion.
After completion, create .planning/phases/15-re-establish-candidate-evidence/15-01-SUMMARY.md with actual identities, tests, residual risks and decision coverage.
</output>
