---
phase: 15-re-establish-candidate-evidence
plan: "11"
type: execute
wave: 11
depends_on: ["15-10"]
files_modified: ["target/phase15-candidate/release/ledger.json","reference/release/candidate-manifest.json","reference/release/audit-report.json","reference/release/source-candidate.json"]
autonomous: true
requirements: ["DOCS-09","PLAT-01","PLAT-05"]
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-09-14T16-41-28
generated_at: "2026-09-14T16:55:28.770Z"
must_haves:
  truths:
    - "Real aggregate accepts exactly 19 records, fresh-checkout audit passes and complete raw artifacts remain available."
    - "Both worktree-before-commit and explicit committed C..A checks pass with correct tree and exact content hashes."
  artifacts:
    - path: "target/phase15-candidate/release/ledger.json"
      provides: "Run real release aggregation and restore the complete retained bundle"
    - path: "reference/release/source-candidate.json"
      provides: "Validate worktree, commit A directly after C and validate range"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "scripts/phase12-release-evidence.sh"
      via: "Prior-wave repaired aggregation entrypoint; current-wave run must join all 21 artifacts into 19 C-bound records."
      pattern: "bash scripts/phase12-release-evidence[.]sh aggregate"
    - from: ".github/workflows/release.yml"
      to: "tools/xtask/src/release.rs"
      via: "Existing workflow invokes typed auditor before publishing successful identity."
      pattern: "cargo xtask release audit"
    - from: "tools/xtask/src/release/attestation.rs"
      to: "reference/release/source-candidate.json"
      via: "Existing attestation input reader; current-wave exact source/manifest/report hashes and worktree plus C-to-A validations remain required."
      pattern: "read_confined[(]repository_root, &options[.]source,"
---

## Scope supersession — 2026-09-16

This is an optional strict-qualification plan under the owner's revised hobby scope (`PROJECT-SCOPE.md`, Phase 15 D-11/D-12). Do not execute it as a mandatory continuation or report its missing Linux/controlled-host evidence as a hobby-project blocker. Preserve the original requirements below for an explicit future strict-qualification request; unexecuted checks have not passed.


<objective>
Aggregate nineteen records and attest direct C-to-A range.
Purpose: Complete actual candidate acceptance with retained, independently verifiable evidence.
Output: Run real release aggregation and restore the complete retained bundle; Validate worktree, commit A directly after C and validate range.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-re-establish-candidate-evidence/15-CONTEXT.md
@.planning/phases/15-re-establish-candidate-evidence/15-RESEARCH.md
@tools/xtask/src/release.rs
@tools/xtask/src/release/attestation.rs
@reference/release/schema.json

<interfaces>
Existing shell release boundary: check; prepare(candidate, output, release_run) is changed by Plan04 to import archive and identity; aggregate consumes exact producer run IDs; publish-identity-last emits success only after validation.
Existing typed commands: cargo xtask release audit --manifest PATH --candidate C --output json; release attestation validate-worktree --source PATH --manifest PATH --report PATH; release attestation validate with the same arguments plus --attestation-commit A.
Identity contract: full lowercase 40-hex source C, explicit provider/run/attempt/job/target/toolchain, exact SHA-256 content digests; 21 producer artifacts join to 19 required registry entries. D1 canonical, D2 portability and D3 diagnostic authority remain separate.
</interfaces>

Standing authority D-08 permits routine fixes, review, gates, commits and ordinary main pushes; not release publication, waivers, secret/account changes or fabricated review acknowledgment. Local gate baseline at d990dc6 passed in Linux ARM64 Docker; use image sha256:5f3d56072c0c734ebb2381c59f0a20db1330b8884134c4e3a9d369de2d810ea8 with repo at /workspace, liquidfun-rs-rustup:/usr/local/rustup, liquidfun-rs-cargo-registry:/usr/local/cargo/registry, liquidfun-rs-cargo-target:/cargo-target and CARGO_TARGET_DIR=/cargo-target. This local boundary does not replace canonical Linux x64 or native Windows evidence.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Run real release aggregation and restore the complete retained bundle</name>
  <files>target/phase15-candidate/release/ledger.json, reference/release/candidate-manifest.json, reference/release/audit-report.json</files>
  <read-first>tools/xtask/src/release.rs, tools/xtask/src/release/attestation.rs, reference/release/schema.json, reference/release/candidate-manifest.json, reference/release/audit-report.json; read existing files before edits and the retained failure described below.</read-first>
  
  <action>Per D-04/D-05/D-06 run repaired release.yml with C and the seven exact reviewed producer run IDs. Verify all 21 raw producer artifacts map to exactly 19 required registry records; docs/notices/corpus/compatibility are actually audited, same package imported and dry-run byte equality proven. Retain original raw artifacts plus every aggregate envelope/package at its repository-relative target path. In isolated checkout at C restore complete retained bundle and only generated candidate manifest/audit report. Run release audit and independently recompute all hashes. A green workflow alone is insufficient; missing or stale evidence leaves readiness false.</action>
  <verify><automated>cargo xtask release audit --manifest reference/release/candidate-manifest.json --candidate "$candidate_sha" --output json</automated></verify>
  <acceptance-criteria>Real aggregate accepts exactly 19 records, fresh-checkout audit passes and complete raw artifacts remain available.</acceptance-criteria>
  <done>Real aggregate accepts exactly 19 records, fresh-checkout audit passes and complete raw artifacts remain available. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>

<task type="auto">
  <name>Task 2: Validate worktree, commit A directly after C and validate range</name>
  <files>reference/release/source-candidate.json, reference/release/candidate-manifest.json, reference/release/audit-report.json</files>
  <read-first>tools/xtask/src/release.rs, tools/xtask/src/release/attestation.rs, reference/release/schema.json, reference/release/source-candidate.json, reference/release/candidate-manifest.json, reference/release/audit-report.json; read existing files before edits and the retained failure described below.</read-first>
  
  <action>Per D-03/D-09 record source C with SHA-256 of raw git ls-tree -r -z --full-tree C bytes and exact manifest/report byte hashes through existing source-candidate construction. In detached C checkout validate-worktree using all three records. Run mandated precommit gates, inspect changed paths and commit only those three JSON records so A parent is C. Do not permit GSD executor summary/state or ordinary docs in this commit; save execution summary in ignored storage until A exists. Capture full A and run explicit committed attestation validate. Recompute C..A names and prove only allowlisted records differ. Preserve retained target data for later explicit-A validation; never substitute HEAD for C or A. Run the worktree validation before commit and the committed validator only after recording A; the automated stanza lists both required commands, not one precommit invocation.</action>
  <verify><automated>cargo xtask release attestation validate-worktree --source reference/release/source-candidate.json --manifest reference/release/candidate-manifest.json --report reference/release/audit-report.json &amp;&amp; cargo xtask release attestation validate --source reference/release/source-candidate.json --manifest reference/release/candidate-manifest.json --report reference/release/audit-report.json --attestation-commit "$attestation_sha"</automated></verify>
  <acceptance-criteria>Both worktree-before-commit and explicit committed C..A checks pass with correct tree and exact content hashes.</acceptance-criteria>
  <done>Both worktree-before-commit and explicit committed C..A checks pass with correct tree and exact content hashes. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>
</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Remote run/artifact and source record → acceptance | Candidate, paths, hashes and terminal identities must be checked before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-11-01 | S/T | target/phase15-candidate/release/ledger.json | mitigate | Bind full candidate, exact target/toolchain and SHA-256 bytes; reject mismatched identity at the task boundary. |
| T-15-11-02 | R | Attempt records | mitigate | Preserve original failure logs and provider/run/attempt identities; no overwrite or inferred success. |
| T-15-11-03 | T/I | Retained payload paths | mitigate | Use normalized repository-relative paths with no traversal or symlink components and bounded payload validation before consumption. |

ASVS L1-relevant input/access/integrity controls apply to these tooling boundaries. There is no web login/session or new application authorization surface in this plan.
</threat-model>

<verification>
Run each task's focused automated command and the actual production/negative controls stated in its action. Commands needing canonical x64, pinned Miri or native Windows run at that exact boundary; a local ARM64 pass is supplemental.
Before every commit run in order: cargo fmt --all; cargo clippy --all-targets --all-features -- -D warnings; cargo build --all-targets --all-features; cargo test --all-features. Run bun scripts/bright-builds-check.ts all, required Markdown checks and git diff --check. Do not format .planning with mdformat.
For tool failures preserve the failed attempt, diagnose the exact boundary, repair and rerun affected proof. Never convert missing controlled-host credentials to acceptance. Source repairs after C require requalification and a fresh C.
</verification>

<success-criteria>
- Real aggregate accepts exactly 19 records, fresh-checkout audit passes and complete raw artifacts remain available.
- Both worktree-before-commit and explicit committed C..A checks pass with correct tree and exact content hashes.
- Every effect uses observed repository/ref/candidate identity and standing authority; no release publication or waiver.
</success-criteria>

<output>
While C..A is pending, retain the summary in ignored target storage and defer its tracked commit until A has passed. The generic executor must not insert a SUMMARY/STATE/ROADMAP commit between C and A.
After completion, create .planning/phases/15-re-establish-candidate-evidence/15-11-SUMMARY.md with actual identities, tests, residual risks and decision coverage.
</output>

