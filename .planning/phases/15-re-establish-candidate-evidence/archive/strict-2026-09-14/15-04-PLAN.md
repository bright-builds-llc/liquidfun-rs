---
phase: 15-re-establish-candidate-evidence
plan: "04"
type: execute
wave: 4
depends_on: ["15-03"]
files_modified: ["scripts/phase12-release-evidence.sh","scripts/phase12-release-evidence/common.sh","scripts/phase12-release-evidence/package_import.sh","tools/xtask/tests/release_cli.rs","tools/xtask/tests/release_cli/construction_cases.rs","tools/xtask/tests/release_cli/package_handoff.rs","tools/xtask/tests/release_cli/package_handoff.sh","tools/xtask/tests/release_cli/real_package_handoff.sh",".github/workflows/release.yml","tools/xtask/tests/platform_workflow.rs"]
autonomous: true
requirements: ["PLAT-01","PLAT-05","DOCS-09"]
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-09-14T16-41-28
generated_at: "2026-09-14T16:55:28.770Z"
must_haves:
  truths:
    - "Release preparation reuses the exact platform-created archive and independently verifies dry-run byte identity."
    - "One archive reaches every native/MSRV verifier and release preparation without repackaging or cross-candidate joins."
  artifacts:
    - path: "scripts/phase12-release-evidence/common.sh"
      provides: "Implement a verified package import boundary"
    - path: ".github/workflows/release.yml"
      provides: "Acquire package before preparation and test fan-out join"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "scripts/phase12-release-evidence.sh"
      via: "Existing preparation invocation; current-wave repair must supply imported platform archive and identity without repackaging."
      pattern: "bash scripts/phase12-release-evidence[.]sh prepare"
    - from: "scripts/phase12-release-evidence.sh"
      to: "scripts/phase12-release-evidence/common.sh"
      via: "Existing sourced implementation of prepare_output; imported-byte verification is a current-wave behavioral postcondition."
      pattern: "source .[$]script_directory/phase12-release-evidence/common[.]sh"
---

<objective>
Make release consume the platform archive exactly once.
Purpose: Repair and prove the prerequisite without weakening frozen-source acceptance.
Output: Implement a verified package import boundary; Acquire package before preparation and test fan-out join.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-re-establish-candidate-evidence/15-CONTEXT.md
@.planning/phases/15-re-establish-candidate-evidence/15-RESEARCH.md
@RELEASE.md
@.github/workflows/platform.yml
@tools/xtask/src/package.rs

<interfaces>
Existing shell release boundary: check; prepare(candidate, output, release_run) is changed by Plan04 to import archive and identity; aggregate consumes exact producer run IDs; publish-identity-last emits success only after validation.
Existing typed commands: cargo xtask release audit --manifest PATH --candidate C --output json; release attestation validate-worktree --source PATH --manifest PATH --report PATH; release attestation validate with the same arguments plus --attestation-commit A.
Identity contract: full lowercase 40-hex source C, explicit provider/run/attempt/job/target/toolchain, exact SHA-256 content digests; 21 producer artifacts join to 19 required registry entries. D1 canonical, D2 portability and D3 diagnostic authority remain separate.
</interfaces>

Standing authority D-08 permits routine fixes, review, gates, commits and ordinary main pushes; not release publication, waivers, secret/account changes or fabricated review acknowledgment. Local gate baseline at d990dc6 passed in Linux ARM64 Docker; use image sha256:5f3d56072c0c734ebb2381c59f0a20db1330b8884134c4e3a9d369de2d810ea8 with repo at /workspace, liquidfun-rs-rustup:/usr/local/rustup, liquidfun-rs-cargo-registry:/usr/local/cargo/registry, liquidfun-rs-cargo-target:/cargo-target and CARGO_TARGET_DIR=/cargo-target. This local boundary does not replace canonical Linux x64 or native Windows evidence.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement a verified package import boundary</name>
  <files>scripts/phase12-release-evidence/common.sh, tools/xtask/tests/release_cli.rs, tools/xtask/tests/release_cli/construction_cases.rs</files>
  <read-first>RELEASE.md, .github/workflows/platform.yml, tools/xtask/src/package.rs, scripts/phase12-release-evidence/common.sh, tools/xtask/tests/release_cli.rs, tools/xtask/tests/release_cli/construction_cases.rs; read existing files before edits and the retained failure described below.</read-first>
  <behavior>Release preparation reuses the exact platform-created archive and independently verifies dry-run byte identity. Negative inputs and failed commands must produce a nonzero result with no success identity.</behavior>
  <action>Per D-02/D-06 change prepare_output to accept the downloaded platform archive and identity explicitly and verify candidate, producer, archive checksum and package metadata before importing. Keep platform package creation the sole create-artifact call in this flow. Run verify-artifact on imported bytes; preserve release registry producer identity without forging platform provenance. Add real subprocess handoff tests that fail on a second package creation, wrong candidate/hash/bytes, missing identity and mutable archive. Compare the publication dry-run generated archive byte-for-byte with the imported archive; no cargo publish without --dry-run. Write the described behavioral regression first, run the exact automated test command and retain its expected failure; implement, rerun green, then simplify without reducing the assertion. Use Arrange/Act/Assert and bounded subprocesses.</action>
  <verify><automated>cargo test -p xtask --test release_cli --test package_cli</automated></verify>
  <acceptance-criteria>Release preparation reuses the exact platform-created archive and independently verifies dry-run byte identity.</acceptance-criteria>
  <done>Release preparation reuses the exact platform-created archive and independently verifies dry-run byte identity. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Acquire package before preparation and test fan-out join</name>
  <files>.github/workflows/release.yml, tools/xtask/tests/platform_workflow.rs, tools/xtask/tests/release_cli/construction_cases.rs</files>
  <read-first>RELEASE.md, .github/workflows/platform.yml, tools/xtask/src/package.rs, .github/workflows/release.yml, tools/xtask/tests/platform_workflow.rs, tools/xtask/tests/release_cli/construction_cases.rs; read existing files before edits and the retained failure described below.</read-first>
  <behavior>One archive reaches every native/MSRV verifier and release preparation without repackaging or cross-candidate joins. Negative inputs and failed commands must produce a nonzero result with no success identity.</behavior>
  <action>Download exact reviewed platform package artifact before release prepare, using the selected platform run and C identity. Update prepare invocation for imported archive and identity; keep remaining aggregation order explicit. Exercise actual producer-to-consumer contract in isolated temporary repositories without submodules/C++ and prove MSRV/Linux/ARM/macOS/Windows metadata all refer to those same package bytes. Reject a green unrelated platform run. Preserve the existing conditional Intel Mac policy. Write the described behavioral regression first, run the exact automated test command and retain its expected failure; implement, rerun green, then simplify without reducing the assertion. Use Arrange/Act/Assert and bounded subprocesses.</action>
  <verify><automated>cargo test -p xtask --test platform_workflow --test release_cli</automated></verify>
  <acceptance-criteria>One archive reaches every native/MSRV verifier and release preparation without repackaging or cross-candidate joins.</acceptance-criteria>
  <done>One archive reaches every native/MSRV verifier and release preparation without repackaging or cross-candidate joins. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>
</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Workflow input and downloaded bytes → local tooling | Candidate, paths, hashes and terminal identities must be checked before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-04-01 | S/T | scripts/phase12-release-evidence/common.sh | mitigate | Bind full candidate, exact target/toolchain and SHA-256 bytes; reject mismatched identity at the task boundary. |
| T-15-04-02 | R | Attempt records | mitigate | Preserve original failure logs and provider/run/attempt identities; no overwrite or inferred success. |
| T-15-04-03 | T/I | Retained payload paths | mitigate | Use normalized repository-relative paths with no traversal or symlink components and bounded payload validation before consumption. |

ASVS L1-relevant input/access/integrity controls apply to these tooling boundaries. There is no web login/session or new application authorization surface in this plan.
</threat-model>

<verification>
Run each task's focused automated command and the actual production/negative controls stated in its action. Commands needing canonical x64, pinned Miri or native Windows run at that exact boundary; a local ARM64 pass is supplemental.
Before every commit run in order: cargo fmt --all; cargo clippy --all-targets --all-features -- -D warnings; cargo build --all-targets --all-features; cargo test --all-features. Run bun scripts/bright-builds-check.ts all, required Markdown checks and git diff --check. Do not format .planning with mdformat.
For tool failures preserve the failed attempt, diagnose the exact boundary, repair and rerun affected proof. Never convert missing controlled-host credentials to acceptance. Source repairs after C require requalification and a fresh C.
</verification>

<success-criteria>
- Release preparation reuses the exact platform-created archive and independently verifies dry-run byte identity.
- One archive reaches every native/MSRV verifier and release preparation without repackaging or cross-candidate joins.
- Every effect uses observed repository/ref/candidate identity and standing authority; no release publication or waiver.
</success-criteria>

<output>
Create the tracked summary after completion.
After completion, create .planning/phases/15-re-establish-candidate-evidence/15-04-SUMMARY.md with actual identities, tests, residual risks and decision coverage.
</output>
