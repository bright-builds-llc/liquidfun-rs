---
phase: 15-re-establish-candidate-evidence
plan: "08"
type: execute
wave: 8
depends_on: ["15-07"]
files_modified: ["target/phase15-candidate/freeze.json","target/phase15-candidate/prefreeze-checks.json","target/phase15-candidate/prerequisites.json","target/phase15-candidate/prerequisites.sha256"]
autonomous: true
requirements: ["PLAT-01","PLAT-05","DOCS-09"]
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-09-14T16-41-28
generated_at: "2026-09-14T16:55:28.770Z"
must_haves:
  truths:
    - "C is clean, fully qualified and recorded; isolated checkout and later attestation range strategy are concrete."
    - "Exact C Cargo and Oracle prerequisites are terminal green with retained evidence and no skipped required downstream checks."
  artifacts:
    - path: "target/phase15-candidate/freeze.json"
      provides: "Finish source readiness and record the frozen candidate"
    - path: "target/phase15-candidate/prerequisites.json"
      provides: "Require terminal Cargo and ordinary Oracle CI at C"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "crates/liquidfun/tests/particle_group_properties.rs"
      via: "Existing exact named Windows/Linux replay invocation; current-wave proof requires terminal successful run at frozen C."
      pattern: "cargo test -p liquidfun --test particle_group_properties persisted_audited_windows_seed -- --exact"
    - from: ".github/workflows/oracle.yml"
      to: "scripts/install-canonical-clang.sh"
      via: "Prior-wave Plan01 prerequisite: exact canonical tool provisioning. Runtime C/source/target identity and all downstream results still require evidence."
      pattern: "bash scripts/install-canonical-clang[.]sh"
---

<objective>
Freeze C and prove inexpensive exact-source prerequisites.
Purpose: Complete actual candidate acceptance with retained, independently verifiable evidence.
Output: Finish source readiness and record the frozen candidate; Require terminal Cargo and ordinary Oracle CI at C.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-re-establish-candidate-evidence/15-CONTEXT.md
@.planning/phases/15-re-establish-candidate-evidence/15-RESEARCH.md
@RELEASE.md
@.github/workflows/ci.yml
@.github/workflows/oracle.yml

<interfaces>
Existing shell release boundary: check; prepare(candidate, output, release_run) is changed by Plan04 to import archive and identity; aggregate consumes exact producer run IDs; publish-identity-last emits success only after validation.
Existing typed commands: cargo xtask release audit --manifest PATH --candidate C --output json; release attestation validate-worktree --source PATH --manifest PATH --report PATH; release attestation validate with the same arguments plus --attestation-commit A.
Identity contract: full lowercase 40-hex source C, explicit provider/run/attempt/job/target/toolchain, exact SHA-256 content digests; 21 producer artifacts join to 19 required registry entries. D1 canonical, D2 portability and D3 diagnostic authority remain separate.
</interfaces>

Standing authority D-08 permits routine fixes, review, gates, commits and ordinary main pushes; not release publication, waivers, secret/account changes or fabricated review acknowledgment. Local gate baseline at d990dc6 passed in Linux ARM64 Docker; use image sha256:5f3d56072c0c734ebb2381c59f0a20db1330b8884134c4e3a9d369de2d810ea8 with repo at /workspace, liquidfun-rs-rustup:/usr/local/rustup, liquidfun-rs-cargo-registry:/usr/local/cargo/registry, liquidfun-rs-cargo-target:/cargo-target and CARGO_TARGET_DIR=/cargo-target. This local boundary does not replace canonical Linux x64 or native Windows evidence.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Finish source readiness and record the frozen candidate</name>
  <files>target/phase15-candidate/freeze.json, target/phase15-candidate/prefreeze-checks.json</files>
  <read-first>RELEASE.md, .github/workflows/ci.yml, .github/workflows/oracle.yml; read existing files before edits and the retained failure described below.</read-first>
  
  <action>Preflight at c404f1649b1bab2f2a4543930c3bd68933e0334a found an invalid cargo-deny flag order in scripts/phase12-release-evidence/common.sh. Correct it to cargo deny --locked check. The real audit then identified RUSTSEC-2026-0257 in private testbed dependency webbrowser 1.2.1; update Cargo.lock narrowly to patched 1.2.2 without advisory waivers. Preserve failed preflight and superseded candidate records. Because Cargo.lock is part of the replay producer closure, run the existing Plan07 fresh P/B/R/Q production, independent review, promotion and canonical acceptance sequence again before selecting replacement C. Per D-01/D-03/D-08 finish all intended source/tooling/planning changes and summaries through Plan07 before freezing clean reviewed full lowercase 40-hex C. Fetch and integrate compatible main drift, run ordered fmt/clippy/build/test plus managed checker, markdown-check for changed non-GSD Markdown and git diff --check. Ordinary push to bright-builds-llc/liquidfun-rs main is authorized after identity recheck. Record C, upstream, compiler/tool versions, raw git ls-tree -r -z --full-tree C SHA-256 and source-readiness results. Create isolated detached checkout at C for producer/attestation work. Keep ongoing observations in ignored attempt files; no tracked GSD summary commit between C and A. Any required source change invalidates C and requires requalification/refreeze.</action>
  <verify><automated>git diff --check &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; git rev-parse --verify HEAD</automated></verify>
  <acceptance-criteria>C is clean, fully qualified and recorded; isolated checkout and later attestation range strategy are concrete.</acceptance-criteria>
  <done>C is clean, fully qualified and recorded; isolated checkout and later attestation range strategy are concrete. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>

<task type="auto">
  <name>Task 2: Require terminal Cargo and ordinary Oracle CI at C</name>
  <files>target/phase15-candidate/prerequisites.json, target/phase15-candidate/prerequisites.sha256</files>
  <read-first>RELEASE.md, .github/workflows/ci.yml, .github/workflows/oracle.yml; read existing files before edits and the retained failure described below.</read-first>
  
  <action>Run/reconcile exact-C Cargo CI and ordinary Oracle CI, retain metadata/jobs/logs and independently assert all required jobs/steps completed success without prerequisite-caused skips. Verify Windows/Linux package tests and canonical oracle identities, not only workflow conclusion. Run inexpensive release contract, docs/corpus/license checks at C before expensive producers; use the shared platform package in Plan09 when packaging is needed. If a check fails, preserve attempt and diagnose; source repair returns to Plan07/08 with new C. Record explicit ready-to-produce result, not final readiness.</action>
  <verify><automated>bash scripts/phase12-release-evidence.sh check &amp;&amp; cargo xtask docs check &amp;&amp; cargo xtask inventory corpus check-snapshot &amp;&amp; cargo xtask inventory corpus check-closure</automated></verify>
  <acceptance-criteria>Exact C Cargo and Oracle prerequisites are terminal green with retained evidence and no skipped required downstream checks.</acceptance-criteria>
  <done>Exact C Cargo and Oracle prerequisites are terminal green with retained evidence and no skipped required downstream checks. Retain actual command outcomes and identity checks; existence of files alone is insufficient.</done>
</task>
</tasks>

<threat-model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Remote run/artifact and source record → acceptance | Candidate, paths, hashes and terminal identities must be checked before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-08-01 | S/T | target/phase15-candidate/freeze.json | mitigate | Bind full candidate, exact target/toolchain and SHA-256 bytes; reject mismatched identity at the task boundary. |
| T-15-08-02 | R | Attempt records | mitigate | Preserve original failure logs and provider/run/attempt identities; no overwrite or inferred success. |
| T-15-08-03 | T/I | Retained payload paths | mitigate | Use normalized repository-relative paths with no traversal or symlink components and bounded payload validation before consumption. |

ASVS L1-relevant input/access/integrity controls apply to these tooling boundaries. There is no web login/session or new application authorization surface in this plan.
</threat-model>

<verification>
Run each task's focused automated command and the actual production/negative controls stated in its action. Commands needing canonical x64, pinned Miri or native Windows run at that exact boundary; a local ARM64 pass is supplemental.
Before every commit run in order: cargo fmt --all; cargo clippy --all-targets --all-features -- -D warnings; cargo build --all-targets --all-features; cargo test --all-features. Run bun scripts/bright-builds-check.ts all, required Markdown checks and git diff --check. Do not format .planning with mdformat.
For tool failures preserve the failed attempt, diagnose the exact boundary, repair and rerun affected proof. Never convert missing controlled-host credentials to acceptance. Source repairs after C require requalification and a fresh C.
</verification>

<success-criteria>
- C is clean, fully qualified and recorded; isolated checkout and later attestation range strategy are concrete.
- Exact C Cargo and Oracle prerequisites are terminal green with retained evidence and no skipped required downstream checks.
- Every effect uses observed repository/ref/candidate identity and standing authority; no release publication or waiver.
</success-criteria>

<output>
While C..A is pending, retain the summary in ignored target storage and defer its tracked commit until A has passed. The generic executor must not insert a SUMMARY/STATE/ROADMAP commit between C and A.
After completion, create .planning/phases/15-re-establish-candidate-evidence/15-08-SUMMARY.md with actual identities, tests, residual risks and decision coverage.
</output>

